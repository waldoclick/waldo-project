---
phase: 127-security-review-round-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/order/controllers/order.ts
  - apps/strapi/src/api/order/routes/01-order-me.ts
  - apps/strapi/src/api/ad-pack/routes/ad-pack.ts
  - apps/strapi/src/api/ad-reservation/routes/ad-reservation.ts
  - apps/strapi/src/api/ad-featured-reservation/routes/ad-featured-reservation.ts
  - apps/strapi/tests/api/order/order.test.ts
  - apps/strapi/tests/api/ad-pack/ad-pack.route.test.ts
autonomous: true
requirements: [SEC2-AUTHZ]
must_haves:
  truths:
    - "A non-manager calling order.findOne for an order they do not own gets 403 (ctx.forbidden)"
    - "A non-manager calling order.find only sees their own orders; client-supplied filters cannot widen scope"
    - "order.exportCsv is gated behind global::isManager (non-managers get 403)"
    - "Non-managers cannot create/update/delete ad-pack, ad-reservation, or ad-featured-reservation; the manager-only gift action still works"
  artifacts:
    - path: "apps/strapi/src/api/order/controllers/order.ts"
      provides: "ownership check in findOne, user-scoping in find"
      contains: "ctx.forbidden"
    - path: "apps/strapi/src/api/order/routes/01-order-me.ts"
      provides: "isManager policy on export-csv route"
    - path: "apps/strapi/src/api/ad-pack/routes/ad-pack.ts"
      provides: "isManager policies on create/update/delete"
    - path: "apps/strapi/tests/api/order/order.test.ts"
      provides: "regression tests for findOne 403 + find scoping"
  key_links:
    - from: "order.findOne"
      to: "ctx.forbidden()"
      via: "isManager + owner comparison (copied from payment.ts:744)"
      pattern: "ctx.forbidden"
    - from: "order.find"
      to: "{ user: { id: ctx.state.user.id } }"
      via: "non-manager scoping that overrides client filters"
      pattern: "user: \\{ id:"
---

<objective>
Close SEC2-AUTHZ: `order.findOne` (order.ts:386) and `order.find` (order.ts:26) return order PII/payment data without any ownership check or user scoping; `order.exportCsv` route lacks the `isManager` policy that `salesByMonth` already has; and `ad-pack`/`ad-reservation`/`ad-featured-reservation` routes have `config.policies: []`, exposing create/update/delete (mint free credits, rewrite pack prices, IDOR reservations).

Purpose: an authenticated user (going through the proxy with a valid JWT) must not read another user's order, list all orders, export the CSV, or write packs/reservations.
Output: ownership guard in findOne, user-scoped find, isManager gate on export-csv, locked-down pack/reservation routes, and Jest regression tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-security-review-round-2/127-CONTEXT.md
@.planning/phases/127-security-review-round-2/127-RESEARCH.md
@.planning/phases/127-security-review-round-2/127-FINDINGS.md

@apps/strapi/src/api/order/controllers/order.ts
@apps/strapi/src/api/order/routes/01-order-me.ts
@apps/strapi/src/api/payment/controllers/payment.ts
@apps/strapi/src/policies/isManager.ts
@apps/strapi/src/api/ad-pack/routes/ad-pack.ts
@apps/strapi/src/api/ad-reservation/routes/ad-reservation.ts
@apps/strapi/src/api/ad-featured-reservation/routes/ad-featured-reservation.ts

<interfaces>
<!-- Existing manager-check pattern (copy verbatim — used at payment.ts thankyou ~:744):
  const isManager = ((ctx.state.user as { role?: { name?: string } })?.role?.name ?? "").toLowerCase() === "manager";
  if (!isManager && String(orderUser?.id) !== String(userId)) { return ctx.forbidden(); }
Global policy already registered: global::isManager (apps/strapi/src/policies/isManager.ts).
order.ts current state:
  - find (:26): const filters = query.filters || {}; then findMany({ where: filters }) — NO scoping
  - findOne (:386): fetches by id/documentId, populates user+ad, returns WITHOUT ownership check
  - salesByMonth route already has config: { policies: ["global::isManager"] }
  - exportCsv route (01-order-me.ts:9-11) has NO policy — MUST add global::isManager
Reservations are created server-side by PaymentUtils.adReservation.createAdReservation; a public create endpoint is unnecessary (RESEARCH Open Question 3). Keep the existing manager-only `gift` action on reservation routes if present. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing regression tests for order IDOR + route lockdown</name>
  <files>apps/strapi/tests/api/order/order.test.ts, apps/strapi/tests/api/ad-pack/ad-pack.route.test.ts</files>
  <read_first>
    - apps/strapi/src/api/order/controllers/order.ts (find :26-115, findOne :386-419)
    - apps/strapi/src/api/payment/controllers/payment.ts (thankyou ownership pattern ~:744)
    - apps/strapi/tests/api/order/ and apps/strapi/tests/api/payment/ (existing test style)
    - .planning/phases/127-security-review-round-2/127-RESEARCH.md (Plan 02 ownership snippet + createCoreRouter only)
  </read_first>
  <behavior>
    Create `apps/strapi/tests/api/order/order.test.ts` — cases that FAIL against current code:
    - Test 1 (findOne IDOR): ctx.state.user = { id: 7, role: { name: "user" } }; mock the order query to return an order whose `user.id = 99`. Call controller.findOne(ctx) → expect `ctx.forbidden` to have been called (mock ctx.forbidden as jest.fn()).
    - Test 2 (findOne owner allowed): same but order.user.id = 7 → expect ctx.forbidden NOT called and the order returned.
    - Test 3 (findOne manager bypass): ctx.state.user.role.name = "manager", order.user.id = 99 → ctx.forbidden NOT called.
    - Test 4 (find scoping): ctx.state.user = { id: 7, role: { name: "user" } }, ctx.query.filters = { user: { id: 99 } } (attacker tries to widen). Assert the `where` passed to `findMany` is scoped to `{ user: { id: 7 } }` (client filter ignored for non-manager).
    Create `apps/strapi/tests/api/ad-pack/ad-pack.route.test.ts` — assert the route definitions object: create/update/delete entries each have `config.policies` containing `"global::isManager"`. (Import the route module default export and inspect `routes`.)
  </behavior>
  <action>
    Place tests at `apps/strapi/tests/api/order/order.test.ts` and `apps/strapi/tests/api/ad-pack/ad-pack.route.test.ts` (root-level tests/ mirroring src/). Mock `ctx` with `state`, `query`, `params`, `forbidden: jest.fn()`, `send`/return capture. Stub global `strapi.db.query(...).findOne/findMany`. For the route test, simply `import routes from "../../../src/api/ad-pack/routes/ad-pack"` and assert on the array. These are RED now. No production code here.
  </action>
  <verify>
    <automated>cd apps/strapi && pnpm test -- --testPathPattern="order.test|ad-pack.route"</automated>
  </verify>
  <done>Both test files exist; the order IDOR/scoping cases and the ad-pack policy assertions FAIL (red).</done>
  <acceptance_criteria>
    - Files `apps/strapi/tests/api/order/order.test.ts` and `apps/strapi/tests/api/ad-pack/ad-pack.route.test.ts` exist
    - `cd apps/strapi && pnpm test -- --testPathPattern="order.test|ad-pack.route"` exits NON-zero before Task 2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: order ownership + scoping + export-csv gate + pack/reservation route lockdown</name>
  <files>apps/strapi/src/api/order/controllers/order.ts, apps/strapi/src/api/order/routes/01-order-me.ts, apps/strapi/src/api/ad-pack/routes/ad-pack.ts, apps/strapi/src/api/ad-reservation/routes/ad-reservation.ts, apps/strapi/src/api/ad-featured-reservation/routes/ad-featured-reservation.ts</files>
  <read_first>
    - apps/strapi/src/api/order/controllers/order.ts (find :26, findOne :386)
    - apps/strapi/src/api/order/routes/01-order-me.ts (export-csv route :8-11)
    - apps/strapi/src/api/payment/controllers/payment.ts (~:744 isManager+owner pattern)
    - apps/strapi/src/api/ad-pack/routes/ad-pack.ts, ad-reservation/routes/ad-reservation.ts, ad-featured-reservation/routes/ad-featured-reservation.ts
    - .planning/phases/127-security-review-round-2/127-RESEARCH.md (Plan 02 snippets)
  </read_first>
  <action>
    1) order.findOne (order.ts:386) — after the order is fetched and before returning, insert (copy from payment.ts:744):
    ```typescript
    const isManager = ((ctx.state.user as { role?: { name?: string } })?.role?.name ?? "").toLowerCase() === "manager";
    const orderUser = (order as { user?: { id?: number | string } } | null)?.user;
    if (!isManager && String(orderUser?.id) !== String(ctx.state.user?.id)) {
      return ctx.forbidden();
    }
    ```

    2) order.find (order.ts:26) — replace `const filters = query.filters || {};` with manager-aware scoping:
    ```typescript
    const isManager = ((ctx.state.user as { role?: { name?: string } })?.role?.name ?? "").toLowerCase() === "manager";
    const filters = isManager
      ? ((query.filters as Record<string, unknown>) ?? {})
      : { user: { id: ctx.state.user.id } };
    ```
    Ensure the non-manager branch IGNORES any client-supplied `query.filters` that would widen user scope (do not spread client filters into the non-manager where clause). If pagination/sort from query are still needed, keep those but never let client `filters.user` override the scoping.

    3) export-csv route — in `apps/strapi/src/api/order/routes/01-order-me.ts`, add the manager policy to the export-csv entry to match sales-by-month:
    ```typescript
    {
      method: "GET",
      path: "/orders/export-csv",
      handler: "order.exportCsv",
      config: { policies: ["global::isManager"] },
    },
    ```

    4) ad-pack routes — `apps/strapi/src/api/ad-pack/routes/ad-pack.ts`: keep GET find/findOne public (policies: []), gate create/update/delete with `global::isManager`:
    ```typescript
    { method: "POST",   path: "/ad-packs",     handler: "ad-pack.create", config: { policies: ["global::isManager"] } },
    { method: "PUT",    path: "/ad-packs/:id", handler: "ad-pack.update", config: { policies: ["global::isManager"] } },
    { method: "DELETE", path: "/ad-packs/:id", handler: "ad-pack.delete", config: { policies: ["global::isManager"] } },
    ```
    (Preserve the existing find/findOne routes; if the file uses createCoreRouter, convert to an explicit routes array OR pass `config` overrides per action so write actions carry the isManager policy. Match whatever form the sibling routes use.)

    5) ad-reservation and ad-featured-reservation routes — reservations are publishing CREDITS created server-side by the payment flow (RESEARCH Open Question 3). Remove public create/update/delete: gate them with `global::isManager`. Keep the existing manager-only `gift` action exactly as-is. Do NOT add a new public owner-scoped create endpoint unless one of these route files already exposes a user-facing reservation create that the app depends on — if so, override its controller to set `data.user = ctx.state.user.id` and strip client `price`/`user`, and note the decision in SUMMARY.

    Never leave unused vars/imports (Codacy). Use documentId for any content write.
  </action>
  <verify>
    <automated>cd apps/strapi && pnpm test -- --testPathPattern="order.test|ad-pack.route"</automated>
  </verify>
  <done>
    findOne returns 403 for cross-user/non-manager; find is scoped to the caller for non-managers; export-csv carries isManager; pack/reservation write routes carry isManager. All regression tests green.
  </done>
  <acceptance_criteria>
    - `grep -q 'ctx.forbidden' apps/strapi/src/api/order/controllers/order.ts`
    - `grep -q 'user: { id: ctx.state.user.id }' apps/strapi/src/api/order/controllers/order.ts`
    - export-csv route now has the policy: `grep -A3 'order.exportCsv' apps/strapi/src/api/order/routes/01-order-me.ts | grep -q 'global::isManager'`
    - ad-pack create/update/delete gated: `grep -c 'global::isManager' apps/strapi/src/api/ad-pack/routes/ad-pack.ts` >= 3
    - reservation write routes gated: `grep -q 'global::isManager' apps/strapi/src/api/ad-reservation/routes/ad-reservation.ts` and same for ad-featured-reservation
    - `cd apps/strapi && pnpm test -- --testPathPattern="order.test|ad-pack.route"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- order.findOne forbids cross-user non-manager access; manager bypass intact
- order.find scoped for non-managers; client filters cannot widen scope
- export-csv + sales-by-month both gated by isManager
- ad-pack/ad-reservation/ad-featured-reservation write routes gated; gift action preserved
- Regression tests pass; they failed before the fix
- `cd apps/strapi && pnpm test` full suite green
- Manual (note in SUMMARY): export Strapi Public vs Authenticated DB role permissions and confirm order/verification-code write actions are not granted to non-managers
</verification>

<success_criteria>
- No authenticated user can read another user's order or list all orders
- export-csv / sales-by-month are manager-only
- No non-manager can mint reservations or rewrite pack prices
</success_criteria>

<output>
After completion, create `.planning/phases/127-security-review-round-2/127-02-SUMMARY.md`
</output>
