---
phase: 105-pro-subscription-checkout-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/ResumePro.vue
  - apps/website/app/components/ResumeProCard.vue
  - apps/website/app/pages/pro/gracias.vue
  - apps/website/app/components/CheckoutPro.vue
  - apps/website/app/components/FormPro.vue
  - apps/website/app/components/BarPro.vue
  - apps/website/app/components/PaymentProInvoice.vue
  - apps/website/app/components/PaymentProGateway.vue
  - apps/website/app/scss/components/_checkout.scss
  - apps/website/app/scss/components/_form.scss
  - apps/website/app/scss/components/_bar.scss
  - apps/website/app/scss/components/_payment.scss
autonomous: true
requirements: [CHECKOUT-06, CHECKOUT-07, CHECKOUT-08, CHECKOUT-09]

must_haves:
  truths:
    - "ResumePro.vue naming conflict resolved — old component renamed to ResumeProCard.vue"
    - "/pro/gracias.vue still works with renamed ResumeProCard"
    - "CheckoutPro, FormPro, BarPro, PaymentProInvoice, PaymentProGateway components exist and replicate their /pagar counterparts"
    - "SCSS files have --pro modifiers added to existing multi-selector blocks"
  artifacts:
    - path: "apps/website/app/components/ResumeProCard.vue"
      provides: "Renamed card enrollment display (was ResumePro)"
      contains: "resume resume--pro"
    - path: "apps/website/app/components/CheckoutPro.vue"
      provides: "Checkout wrapper for PRO flow"
      contains: "checkout checkout--pro"
    - path: "apps/website/app/components/FormPro.vue"
      provides: "Two-section accordion (invoice + gateway)"
      contains: "form form--pro"
    - path: "apps/website/app/components/BarPro.vue"
      provides: "Bottom action bar for PRO checkout"
      contains: "bar bar--pro"
    - path: "apps/website/app/components/PaymentProInvoice.vue"
      provides: "Boleta/factura radio toggle (no adStore)"
      contains: "payment payment--pro-invoice"
    - path: "apps/website/app/components/PaymentProGateway.vue"
      provides: "Oneclick gateway display"
      contains: "payment payment--pro-gateway"
    - path: "apps/website/app/components/ResumePro.vue"
      provides: "New payment receipt component (mirrors ResumeOrder)"
      contains: "resume resume--pro"
  key_links:
    - from: "apps/website/app/pages/pro/gracias.vue"
      to: "apps/website/app/components/ResumeProCard.vue"
      via: "import ResumeProCard from"
      pattern: "import ResumeProCard"
    - from: "apps/website/app/components/FormPro.vue"
      to: "apps/website/app/components/PaymentProInvoice.vue"
      via: "v-model for isInvoice"
      pattern: "PaymentProInvoice"
    - from: "apps/website/app/components/CheckoutPro.vue"
      to: "apps/website/app/components/FormPro.vue"
      via: "formSubmitted event"
      pattern: "FormPro"
---

<objective>
Frontend components: Rename ResumePro to ResumeProCard, create all PRO checkout components, add SCSS modifiers.

Purpose: Phase 105 needs a complete set of checkout components mirroring the ad checkout flow. This plan creates them all as strict replicas of their `/pagar` counterparts with `Pro` suffix and `--pro` BEM modifiers. No new SCSS files — only modifier additions to existing files.

Output: 6 new Vue components + 1 renamed component + SCSS modifier additions to 4 files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-pro-subscription-checkout-page/105-CONTEXT.md
@.planning/phases/105-pro-subscription-checkout-page/105-RESEARCH.md

@apps/website/app/components/CheckoutDefault.vue
@apps/website/app/components/FormCheckout.vue
@apps/website/app/components/BarCheckout.vue
@apps/website/app/components/PaymentInvoice.vue
@apps/website/app/components/PaymentGateway.vue
@apps/website/app/components/ResumeOrder.vue
@apps/website/app/components/ResumePro.vue
@apps/website/app/pages/pro/gracias.vue
@apps/website/app/scss/components/_checkout.scss
@apps/website/app/scss/components/_form.scss
@apps/website/app/scss/components/_bar.scss
@apps/website/app/scss/components/_payment.scss
@apps/website/app/scss/components/_resume.scss

<interfaces>
From apps/website/app/components/BarCheckout.vue:
```typescript
// Props interface:
{
  percentage?: number;      // default 0
  currentStep?: number;     // default 1
  totalSteps?: number;
  showSteps?: boolean;      // default true
  summaryText?: string;     // default ""
  primaryLabel: string;     // required
  primaryDisabled?: boolean; // default false
  backDisabled?: boolean;   // default false
  showBack?: boolean;       // default true
}
// Emits: (e: "back"): void
```

From apps/website/app/components/PaymentInvoice.vue:
```typescript
// Uses adStore.is_invoice and adStore.featured/pack for visibility
// PRO version must NOT use adStore — uses modelValue prop instead
```

From apps/website/app/components/ResumeOrder.vue:
```typescript
// Props:
{
  showIcon: Boolean,     // default true
  title: String,         // default ""
  description: String,   // default ""
  summary: Object,       // default () => null
  hidePaymentSection: Boolean, // default false
}
// summary fields: amount, currency, paymentMethod, receiptNumber, createdAt,
// authorizationCode, paymentType, cardLast4, fullName, email
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ResumePro to ResumeProCard and update /pro/gracias import</name>
  <files>
    apps/website/app/components/ResumeProCard.vue,
    apps/website/app/pages/pro/gracias.vue
  </files>
  <read_first>
    apps/website/app/components/ResumePro.vue,
    apps/website/app/pages/pro/gracias.vue
  </read_first>
  <action>
**Step 1 — Rename the file using `git mv`:**
```bash
git mv apps/website/app/components/ResumePro.vue apps/website/app/components/ResumeProCard.vue
```

**Step 2 — Update `/pro/gracias.vue` import:**

Change line 25 from:
```typescript
import ResumePro from "@/components/ResumePro.vue";
```
to:
```typescript
import ResumeProCard from "@/components/ResumeProCard.vue";
```

Change the template usage on line 5 from:
```html
<ResumePro
```
to:
```html
<ResumeProCard
```

And the closing tag (if self-closing it stays the same pattern). The component reference in template goes from `ResumePro` to `ResumeProCard`.

The `ResumeProCard.vue` file content stays EXACTLY as-is — same BEM class `resume resume--pro`, same props, same template. Only the filename and import path change. Per D-05 naming conflict resolution.
  </action>
  <verify>
    <automated>test -f /home/gab/Code/waldo-project/apps/website/app/components/ResumeProCard.vue && ! test -f /home/gab/Code/waldo-project/apps/website/app/components/ResumePro.vue && grep -q "ResumeProCard" /home/gab/Code/waldo-project/apps/website/app/pages/pro/gracias.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/website/app/components/ResumeProCard.vue` exists
    - File `apps/website/app/components/ResumePro.vue` does NOT exist (freed for new component)
    - `apps/website/app/pages/pro/gracias.vue` contains `import ResumeProCard from "@/components/ResumeProCard.vue"`
    - `apps/website/app/pages/pro/gracias.vue` template contains `<ResumeProCard` (not `<ResumePro`)
  </acceptance_criteria>
  <done>Old ResumePro renamed to ResumeProCard, /pro/gracias.vue updated to use new name</done>
</task>

<task type="auto">
  <name>Task 2a: Create CheckoutPro, FormPro, and BarPro components</name>
  <files>
    apps/website/app/components/CheckoutPro.vue,
    apps/website/app/components/FormPro.vue,
    apps/website/app/components/BarPro.vue
  </files>
  <read_first>
    apps/website/app/components/CheckoutDefault.vue,
    apps/website/app/components/FormCheckout.vue,
    apps/website/app/components/BarCheckout.vue
  </read_first>
  <action>
Create 3 new components. Each is a STRICT replica of its counterpart with `Pro` suffix and `--pro` BEM modifier. Per D-05, D-06, D-07.

**1. `CheckoutPro.vue`** — Replica of `CheckoutDefault.vue`

Template:
```html
<template>
  <section class="checkout checkout--pro">
    <div class="checkout--pro__container">
      <ClientOnly>
        <FormPro @form-submitted="handleFormSubmitted" @update:is-invoice="(val: boolean) => { isInvoice = val; }" />
      </ClientOnly>
    </div>
  </section>
</template>
```

Script (`<script setup lang="ts">`):
- Import `FormPro` from `"@/components/FormPro.vue"`
- Import `useSweetAlert2`, `useApiClient` (auto-imported composables)
- Import `type { User }` from `"@/types/user"`
- NO adStore, NO adAnalytics, NO usePacksList
- Local state: `const isInvoice = ref(false);`

`handleFormSubmitted`:
```typescript
const handleFormSubmitted = async () => {
  const result = await Swal.fire({
    title: "Estas seguro?",
    text: "Se procedera con la suscripcion PRO mensual.",
    icon: "warning",
    showCancelButton: true,
    confirmButtonText: "Si, proceder al pago",
    cancelButtonText: "Cancelar",
  });

  if (!result.isConfirmed) return;

  try {
    const response = await apiClient<{ data: { urlWebpay?: string; token?: string } }>(
      "payments/pro",
      {
        method: "POST",
        body: { data: { is_invoice: isInvoice.value } },
      },
    );

    if (response?.data?.urlWebpay && response?.data?.token) {
      // Oneclick uses GET redirect — NOT POST form like Webpay Plus
      window.location.href = `${response.data.urlWebpay}?TBK_TOKEN=${response.data.token}`;
    } else {
      Swal.fire("Error", "La respuesta de la API no contiene la informacion necesaria para el pago.", "error");
    }
  } catch (error) {
    console.error("Error creating PRO subscription:", error);
    Swal.fire("Error", "Hubo un error al procesar la suscripcion. Por favor, intentalo de nuevo.", "error");
  }
};
```

**2. `FormPro.vue`** — Replica of `FormCheckout.vue` with only 2 sections

Template: `Form` from vee-validate with class `form form--pro`.
- Header: title "Confirma tu suscripcion PRO", description "Por solo $1.000 mensuales, accede a funciones exclusivas y destaca tu perfil con una cuenta PRO."
- Two accordion fields ONLY (per D-02, D-03):
  1. "Boleta o factura" — open by default (`open.invoice = true`) — contains `<PaymentProInvoice v-model="isInvoice" />`
  2. "Pasarela de pago" — closed by default — contains `<PaymentProGateway />`
- NO ad preview, NO featured, NO method selector, NO resumen de compra
- Bottom: `<BarPro primary-label="Ir a pagar" :show-back="false" :show-steps="false" />`

Script:
```typescript
import { reactive, ref } from "vue";
import { Form } from "vee-validate";
import { ChevronDownIcon } from "lucide-vue-next";

const emit = defineEmits(["formSubmitted", "update:isInvoice"]);
const isInvoice = ref(false);

const open = reactive({
  invoice: true,   // open by default per D-02
  gateway: false,
});

const toggle = (key: keyof typeof open) => { open[key] = !open[key]; };

const invoiceSummary = computed(() => isInvoice.value ? "Factura" : "Boleta");

const handleIsInvoiceUpdate = (val: boolean) => {
  isInvoice.value = val;
  emit("update:isInvoice", val);
};

const handleSubmit = (values: Record<string, unknown>) => {
  emit("formSubmitted", values);
};
```

All BEM classes follow `form--pro__*` namespace: `form--pro__header`, `form--pro__header__title`, `form--pro__header__description`, `form--pro__field`, `form--pro__field__toggle`, `form--pro__field__toggle__left`, `form--pro__field__title`, `form--pro__field__summary`, `form--pro__field__chevron`, `form--pro__field__chevron--open`, `form--pro__field__content`.

**3. `BarPro.vue`** — Replica of `BarCheckout.vue`

Exact copy of `BarCheckout.vue` but with class `bar bar--pro` and `bar--pro__*` elements. Same props interface. Same `SummaryDefault` import. Same emit `(e: "back"): void`. No changes to logic. Only BEM class names change from `bar--checkout__*` to `bar--pro__*`.

Per D-05, D-06, D-07.
  </action>
  <verify>
    <automated>grep -q "checkout checkout--pro" /home/gab/Code/waldo-project/apps/website/app/components/CheckoutPro.vue && grep -q "form form--pro" /home/gab/Code/waldo-project/apps/website/app/components/FormPro.vue && grep -q "open.invoice = true\|invoice: true" /home/gab/Code/waldo-project/apps/website/app/components/FormPro.vue && grep -q "bar bar--pro" /home/gab/Code/waldo-project/apps/website/app/components/BarPro.vue && ! grep -q "adStore\|useAdStore" /home/gab/Code/waldo-project/apps/website/app/components/CheckoutPro.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `CheckoutPro.vue` contains `class="checkout checkout--pro"` and `checkout--pro__container`
    - `CheckoutPro.vue` contains `window.location.href` (NOT form POST) for Oneclick redirect
    - `CheckoutPro.vue` sends `{ data: { is_invoice: isInvoice.value } }` to `payments/pro`
    - `CheckoutPro.vue` does NOT contain `adStore` or `useAdStore`
    - `FormPro.vue` contains `class="form form--pro"` and exactly 2 accordion fields (invoice + gateway)
    - `FormPro.vue` has `invoice: true` (open by default per D-02)
    - `FormPro.vue` does NOT contain `PaymentAd`, `PaymentFeatured`, `PaymentMethod`, `adStore`
    - `BarPro.vue` contains `class="bar bar--pro"` and `bar--pro__` BEM elements
  </acceptance_criteria>
  <done>CheckoutPro, FormPro, and BarPro created as strict replicas with correct BEM naming and no adStore dependencies</done>
</task>

<task type="auto">
  <name>Task 2b: Create PaymentProInvoice, PaymentProGateway, and ResumePro components</name>
  <files>
    apps/website/app/components/PaymentProInvoice.vue,
    apps/website/app/components/PaymentProGateway.vue,
    apps/website/app/components/ResumePro.vue
  </files>
  <read_first>
    apps/website/app/components/PaymentInvoice.vue,
    apps/website/app/components/PaymentGateway.vue,
    apps/website/app/components/ResumeOrder.vue,
    apps/website/app/components/MemoPro.vue
  </read_first>
  <action>
Create 3 new components. Each is a STRICT replica of its counterpart. Per D-04, D-05, D-06, D-07.

**1. `PaymentProInvoice.vue`** — Replica of `PaymentInvoice.vue` (no adStore)

Template: `payment payment--pro-invoice` class. Same radio toggle (boleta/factura) layout.
Key differences from `PaymentInvoice.vue`:
- NO `adStore` import — no `shouldShowInvoice` computed (always visible)
- Uses `v-model` pattern: `defineProps<{ modelValue: boolean }>()` + `defineEmits<{ (e: "update:modelValue", value: boolean): void }>()`
- Local `isInvoice` ref initialized from `props.modelValue`, emits on change
- Same `useUser(user.value)` for `canRequestInvoice` check
- Same company check / disabled factura logic
- Same factura details display when `isInvoice && canRequestInvoice`

```typescript
const props = defineProps<{ modelValue: boolean }>();
const emit = defineEmits<{ (e: "update:modelValue", value: boolean): void }>();

const user = useStrapiUser();
const { canRequestInvoice } = useUser(user.value);
const isInvoice = ref(props.modelValue);

const updateInvoice = () => {
  if (!canRequestInvoice.value) { isInvoice.value = false; }
  emit("update:modelValue", isInvoice.value);
};
```

BEM classes: `payment--pro-invoice__title`, `payment--pro-invoice__description`, `payment--pro-invoice__options`, `payment--pro-invoice__options__item`, `payment--pro-invoice__options__item--disabled`, `payment--pro-invoice__details`.

**2. `PaymentProGateway.vue`** — Replica of `PaymentGateway.vue`

Exactly the same as `PaymentGateway.vue` but class `payment payment--pro-gateway` and label "Transbank Oneclick" instead of "Transbank WebPay":

```html
<template>
  <div class="payment payment--pro-gateway">
    <div class="payment--pro-gateway__options">
      <label class="payment--pro-gateway__options__item payment--pro-gateway__options__item--active">
        <input type="checkbox" name="gateway" value="oneclick" checked disabled />
        <span>Transbank Oneclick</span>
      </label>
    </div>
  </div>
</template>

<script setup lang="ts">
// No logic needed — decorative only, single gateway (Oneclick)
</script>
```

**3. `ResumePro.vue`** — NEW component, replica of `ResumeOrder.vue`

Exact structural copy of `ResumeOrder.vue` with class `resume resume--pro` and `resume--pro__*` BEM elements. Same props interface (showIcon, title, description, summary, hidePaymentSection). Same `CardInfo` usage. Same `getFormattedPrice` and `formatDate` helpers. Same two boxes: "Comprobante de pago" and "Informacion del comprador". Same `<script setup>` with `defineProps`. Use `<script setup lang="ts">` for consistency.

Per D-04, D-05.
  </action>
  <verify>
    <automated>grep -q "payment payment--pro-invoice" /home/gab/Code/waldo-project/apps/website/app/components/PaymentProInvoice.vue && grep -q "modelValue.*boolean\|modelValue.*Boolean" /home/gab/Code/waldo-project/apps/website/app/components/PaymentProInvoice.vue && ! grep -q "adStore\|useAdStore" /home/gab/Code/waldo-project/apps/website/app/components/PaymentProInvoice.vue && grep -q "payment payment--pro-gateway" /home/gab/Code/waldo-project/apps/website/app/components/PaymentProGateway.vue && grep -q "resume resume--pro" /home/gab/Code/waldo-project/apps/website/app/components/ResumePro.vue && grep -q "CardInfo" /home/gab/Code/waldo-project/apps/website/app/components/ResumePro.vue && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `PaymentProInvoice.vue` contains `class="payment payment--pro-invoice"` and `defineProps<{ modelValue: boolean }>`
    - `PaymentProInvoice.vue` does NOT contain `adStore` or `useAdStore`
    - `PaymentProGateway.vue` contains `class="payment payment--pro-gateway"` and text "Transbank Oneclick"
    - `ResumePro.vue` contains `class="resume resume--pro"` and `resume--pro__` BEM elements
    - `ResumePro.vue` contains `CardInfo` components for amount, status, method, receipt, date, auth code, payment type, card last 4, name, email
  </acceptance_criteria>
  <done>PaymentProInvoice, PaymentProGateway, and ResumePro created as strict replicas with correct BEM naming and no adStore dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Add --pro BEM modifiers to existing SCSS files</name>
  <files>
    apps/website/app/scss/components/_checkout.scss,
    apps/website/app/scss/components/_form.scss,
    apps/website/app/scss/components/_bar.scss,
    apps/website/app/scss/components/_payment.scss
  </files>
  <read_first>
    apps/website/app/scss/components/_checkout.scss,
    apps/website/app/scss/components/_form.scss,
    apps/website/app/scss/components/_bar.scss,
    apps/website/app/scss/components/_payment.scss,
    apps/website/app/scss/components/_resume.scss
  </read_first>
  <action>
Per D-06: No new SCSS files. Add `--pro` modifiers to existing multi-selector blocks.

**1. `_checkout.scss`** — Add `&--pro` to the modifier selector alongside `&--default`:

The file has `.checkout { &--default { ... } }`. Add `&--pro` to share the same styles:
```scss
.checkout {
  &--default,
  &--pro {
    // existing styles
  }
}
```
If `--default` is the only modifier and all children use `--default__*`, then `--pro` needs its own child selectors. Check the actual SCSS structure. If children are scoped under `&--default__container`, then add a parallel `&--pro { &__container { ... } }` block that copies the same rules, OR use a mixin/multi-selector.

The simplest approach per existing pattern (see `_bar.scss` multi-selector): add `&--pro` to each multi-selector block where `&--default` appears:
```scss
&--default,
&--pro {
  // all shared styles
  &__container {
    // shared container styles
  }
}
```

**2. `_form.scss`** — Add `&--pro` alongside `&--checkout` in all multi-selector blocks:

Find where `.form { &--checkout { ... } }` is defined. Add `&--pro` to share the same header, field, toggle, chevron, content styles:
```scss
&--checkout,
&--pro {
  // all shared form styles and children
}
```

**3. `_bar.scss`** — Add `&--pro` to the existing multi-selector on line 6-8:

Current: `&--announcement, &--checkout, &--packs {`
Change to: `&--announcement, &--checkout, &--packs, &--pro {`

This gives BarPro all the same fixed positioning, border, background, and child styles.

**4. `_payment.scss`** — Add `&--pro-invoice` alongside `&--invoice` and `&--pro-gateway` alongside `&--gateway`:

Find `&--invoice { ... }` block and add `&--pro-invoice` to share styles:
```scss
&--invoice,
&--pro-invoice {
  // all shared invoice styles
}
```

Find `&--gateway { ... }` block and add `&--pro-gateway`:
```scss
&--gateway,
&--pro-gateway {
  // all shared gateway styles
}
```

**5. `_resume.scss`** — NO CHANGE NEEDED. Per research, `&--pro` is already in the multi-selector block at lines 7-9 (`&--default, &--order, &--pro`).

Per D-06, D-07: No new colors, no new design patterns.
  </action>
  <verify>
    <automated>grep -c "\-\-pro" /home/gab/Code/waldo-project/apps/website/app/scss/components/_checkout.scss /home/gab/Code/waldo-project/apps/website/app/scss/components/_form.scss /home/gab/Code/waldo-project/apps/website/app/scss/components/_bar.scss /home/gab/Code/waldo-project/apps/website/app/scss/components/_payment.scss</automated>
  </verify>
  <acceptance_criteria>
    - `_checkout.scss` contains `--pro` at least once (in multi-selector with `--default`)
    - `_form.scss` contains `--pro` at least once (in multi-selector with `--checkout`)
    - `_bar.scss` contains `--pro` in the multi-selector line with `--announcement, --checkout, --packs`
    - `_payment.scss` contains `--pro-invoice` (in multi-selector with `--invoice`)
    - `_payment.scss` contains `--pro-gateway` (in multi-selector with `--gateway`)
    - `_resume.scss` is NOT modified (already has `--pro`)
  </acceptance_criteria>
  <done>All SCSS files have --pro modifiers added to existing multi-selector blocks, no new SCSS files created</done>
</task>

</tasks>

<verification>
- All 6 new components exist in `apps/website/app/components/`
- `ResumeProCard.vue` exists, old `ResumePro.vue` freed for new component
- `/pro/gracias.vue` imports `ResumeProCard` (not `ResumePro`)
- Each component uses correct BEM class names per D-05
- No `adStore` in any PRO component
- SCSS modifiers added to 4 files (_checkout, _form, _bar, _payment), _resume unchanged
</verification>

<success_criteria>
- 6 new Vue components created as strict replicas with Pro suffix
- 1 component renamed (ResumePro -> ResumeProCard) with import updated
- 4 SCSS files updated with --pro modifiers
- No new colors, no new design patterns per D-07
- CheckoutPro uses GET redirect (window.location.href) not POST form per research
</success_criteria>

<output>
After completion, create `.planning/phases/105-pro-subscription-checkout-page/105-02-SUMMARY.md`
</output>
