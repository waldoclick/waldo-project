---
phase: 126-security-hardening
plan: 04
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - apps/website/server/api/dev-config.get.ts
  - apps/website/server/api/dev-login.post.ts
  - apps/strapi/src/api/contact/services/contact.service.ts
  - apps/strapi/src/services/mjml/escape.ts
  - apps/strapi/src/services/mjml/index.ts
  - apps/strapi/src/middlewares/protect-user-fields.ts
  - apps/website/tests/server/dev-endpoints.test.ts
  - apps/strapi/tests/api/contact/contact-escape.test.ts
  - apps/strapi/tests/middlewares/protect-user-fields.test.ts
autonomous: true
requirements: [SEC-HARDENING]
must_haves:
  truths:
    - "dev-config.get.ts and dev-login.post.ts return 404 when not in dev (import.meta.dev is false)"
    - "User-supplied contact fields containing HTML are escaped before reaching the MJML/nunjucks render, so injected markup is neutralized"
    - "PUT /api/users/123/ (trailing slash) still has protected fields stripped — the regex no longer bypasses on trailing slash"
  artifacts:
    - path: "apps/website/server/api/dev-config.get.ts"
      provides: "import.meta.dev gate returning 404 in production"
      contains: "import.meta.dev"
    - path: "apps/website/server/api/dev-login.post.ts"
      provides: "import.meta.dev gate returning 404 in production"
      contains: "import.meta.dev"
    - path: "apps/strapi/src/services/mjml/escape.ts"
      provides: "escapeHtml helper for boundary escaping of user input"
      contains: "escapeHtml"
    - path: "apps/strapi/src/api/contact/services/contact.service.ts"
      provides: "Escaped user fields (name/email/phone/company/message) passed to sendMjmlEmail"
      contains: "escapeHtml"
    - path: "apps/strapi/src/middlewares/protect-user-fields.ts"
      provides: "USER_UPDATE_PATH_REGEX with optional trailing slash"
      contains: "\\\\/?$"
  key_links:
    - from: "apps/website/server/api/dev-config.get.ts"
      to: "import.meta.dev"
      via: "early 404 createError when not dev"
      pattern: "import.meta.dev"
    - from: "apps/strapi/src/api/contact/services/contact.service.ts"
      to: "apps/strapi/src/services/mjml/escape.ts"
      via: "escapeHtml on user fields before sendMjmlEmail"
      pattern: "escapeHtml"
---

<objective>
Three hardening fixes:
(a) Gate the dev-only Nuxt endpoints `dev-config.get.ts` and `dev-login.post.ts` behind `import.meta.dev` so they return 404 in production (they currently leak dev-config status and expose a dev-login bypass).
(b) Escape user-supplied contact fields (name, email, phone, company, message) at the boundary before they reach the MJML/nunjucks render (nunjucks `autoescape` is intentionally `false` because transactional templates render server-built HTML; do NOT flip the global flag — escape the inputs instead).
(c) Fix `USER_UPDATE_PATH_REGEX` in `protect-user-fields.ts` so a trailing slash (`/api/users/123/`) cannot bypass field stripping.

Purpose: Remove a production dev-bypass, prevent HTML injection in operator/admin emails, and close a regex bypass on the user-field protection.
Output: Gated dev endpoints, an `escapeHtml` helper applied in the contact path, the regex fix, and three regression tests (Vitest + 2 Jest).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-security-hardening/126-CONTEXT.md
@CLAUDE.md

<interfaces>
From apps/website/server/api/dev-config.get.ts — `export default defineEventHandler(async (_event) => { ... })` returning an object. Nitro/Nuxt exposes `import.meta.dev` (boolean, true only in dev). `createError` is auto-imported in Nitro handlers.

From apps/website/server/api/dev-login.post.ts — `export default defineEventHandler(async (event) => { try { ... } catch ... })`. The handler already throws createError with statusCode. Add the dev gate as the FIRST statement (before readBody).

From apps/strapi/src/api/contact/services/contact.service.ts — `createContact(contactData)` calls `sendMjmlEmail` twice:
- "contact-user" with `{ name: contactData.fullname, phone, company }`
- "contact-admin" with `{ name: contactData.fullname, email, phone, company, message }`
These objects feed nunjucks render. Escape each user string before building these payloads.

From apps/strapi/src/services/mjml/index.ts — `renderEmail` uses `nunjucks.configure(..., { autoescape: false })`. DO NOT change autoescape (breaks button URL/link templates). Add a separate `escape.ts` exporting `escapeHtml(value: string): string` and apply it at the contact boundary.

From apps/strapi/src/middlewares/protect-user-fields.ts — current regex: `const USER_UPDATE_PATH_REGEX = /^\/api\/users\/\d+$/;` — fails to match a trailing slash. Change to allow an optional trailing slash.
</interfaces>

<dependency_note>
depends_on 126-01 because both this plan and 126-01 reason about protect-user-fields.ts (126-01 confirms PROTECTED_USER_FIELDS must keep email/password out; this plan edits the file's regex). Running after 126-01 avoids a merge conflict on the same file and keeps the test file edits consistent.
</dependency_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate dev endpoints behind import.meta.dev + Vitest test</name>
  <files>apps/website/server/api/dev-config.get.ts, apps/website/server/api/dev-login.post.ts, apps/website/tests/server/dev-endpoints.test.ts</files>
  <read_first>
    - apps/website/server/api/dev-config.get.ts (full handler)
    - apps/website/server/api/dev-login.post.ts (full handler — note the existing try/catch and createError usage)
    - apps/website/tests/server/recaptcha-proxy.test.ts (Vitest style: vi.mock("h3", ...), createError mock that attaches statusCode, defineEventHandler passthrough, expect.rejects.toMatchObject({ statusCode }))
  </read_first>
  <action>
    Part A — dev-config.get.ts: as the FIRST statement inside the handler, add:
    ```ts
    if (!import.meta.dev) {
      throw createError({ statusCode: 404, statusMessage: "Not Found" });
    }
    ```
    Keep the rest of the handler unchanged. (`createError` is auto-imported in Nitro; do not add an import that breaks the build — match how dev-login.post.ts already calls createError without importing it.)

    Part B — dev-login.post.ts: add the same guard as the FIRST statement of the handler, BEFORE `const body = await readBody(event);`. Because the existing catch re-throws errors that have a statusCode, the 404 will propagate correctly. Keep everything else unchanged.

    Part C — Vitest test apps/website/tests/server/dev-endpoints.test.ts (mirror recaptcha-proxy.test.ts):
    - To make the handlers importable in a unit test, mock h3 utilities the same way recaptcha-proxy.test.ts does (`vi.mock("h3", () => ({ defineEventHandler: vi.fn((fn) => fn), createError: vi.fn((opts) => Object.assign(new Error(opts.statusMessage), opts)), readBody: vi.fn(), setCookie: vi.fn(), getHeader: vi.fn() }))`).
    - Control `import.meta.dev`: use `vi.stubGlobal` is not sufficient for import.meta; instead, since these handlers reference `import.meta.dev`, write the test so it can assert behavior when dev is false. If `import.meta.dev` cannot be toggled directly in the Vitest env (it is statically false in test build), the simplest robust assertion is: import the handler and invoke it with a stub event; expect it to throw an error whose statusCode is 404 (because the test environment is NOT dev). Use:
      ```ts
      import devConfig from "~/server/api/dev-config.get";
      import devLogin from "~/server/api/dev-login.post";
      ```
      and assert `await expect((devConfig as any)({})).rejects.toMatchObject({ statusCode: 404 })` and likewise for devLogin with a minimal event `{}`.
    - If the `~/server` alias does not resolve in Vitest, fall back to a relative import from the test file to `../../server/api/dev-config.get` and `../../server/api/dev-login.post`.
    - Two it() cases: "dev-config returns 404 outside dev" and "dev-login returns 404 outside dev".
  </action>
  <verify>
    <automated>cd apps/website && pnpm exec vitest run tests/server/dev-endpoints.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both dev-config.get.ts and dev-login.post.ts contain `import.meta.dev` and a `statusCode: 404` createError as the first guard
    - apps/website/tests/server/dev-endpoints.test.ts exists with two it() cases asserting statusCode 404
    - `cd apps/website && pnpm exec vitest run tests/server/dev-endpoints.test.ts` exits 0
  </acceptance_criteria>
  <done>Both dev endpoints 404 outside dev and the Vitest test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Escape user-supplied contact fields before email render + Jest test</name>
  <files>apps/strapi/src/services/mjml/escape.ts, apps/strapi/src/services/mjml/index.ts, apps/strapi/src/api/contact/services/contact.service.ts, apps/strapi/tests/api/contact/contact-escape.test.ts</files>
  <read_first>
    - apps/strapi/src/services/mjml/index.ts (renderEmail + the re-exports block; you will add an `escapeHtml` re-export)
    - apps/strapi/src/api/contact/services/contact.service.ts (the two sendMjmlEmail calls and the user fields fed into them: fullname, email, phone, company, message)
    - apps/strapi/tests/api/payment/payment-pro-response.test.ts (Jest mocking style for the strapi domain)
  </read_first>
  <action>
    Part A — create apps/strapi/src/services/mjml/escape.ts:
    ```ts
    /**
     * Escapes HTML-significant characters in user-supplied strings before they are
     * rendered into MJML/nunjucks email templates (nunjucks autoescape is intentionally
     * false for server-built transactional HTML — escape at the boundary instead).
     */
    export const escapeHtml = (value: unknown): string => {
      if (value === null || value === undefined) return "";
      return String(value)
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;")
        .replace(/"/g, "&quot;")
        .replace(/'/g, "&#39;");
    };
    ```
    Re-export it from index.ts: add `export { escapeHtml } from "./escape";` alongside the existing exports. Do NOT change `autoescape: false`.

    Part B — apply in contact.service.ts. Import `escapeHtml` from `../../../services/mjml` (match the existing `sendMjmlEmail` import path from that file). Before the two sendMjmlEmail calls, escape the user fields used in the rendered payloads. Specifically, replace the raw values in BOTH payload objects:
    - contact-user payload: `name: escapeHtml(contactData.fullname), phone: escapeHtml(contactData.phone), company: escapeHtml(contactData.company)`
    - contact-admin payload: `name: escapeHtml(contactData.fullname), email: escapeHtml(contactData.email), phone: escapeHtml(contactData.phone), company: escapeHtml(contactData.company), message: escapeHtml(contactData.message)`
    Do NOT escape the values written to the DB, Google Sheets, or Zoho (those are not HTML sinks) — escape ONLY the values passed into sendMjmlEmail. Keep the to-address (contactData.email passed as the recipient argument) unescaped — escaping is for rendered body fields, not the SMTP recipient. No unused vars/imports.

    Part C — Jest test apps/strapi/tests/api/contact/contact-escape.test.ts:
    - Mock `../../../src/services/mjml` so `sendMjmlEmail` is a jest.fn() capturing its 5th argument (the data payload). Also keep `escapeHtml` real if needed, OR test escapeHtml directly plus assert the service passes escaped values. Simplest: mock sendMjmlEmail, mock GoogleServices/zoho/logger and strapi.db.query create to no-ops, then call `new ContactService(strapiStub).createContact({ fullname: '<script>x</script>', email: 'a@b.cl', phone: '<b>1</b>', company: 'C&D', message: '<img src=x onerror=1>' , ip: '1.1.1.1' })`.
    - Assert that the data object passed to the contact-admin sendMjmlEmail call has `name` NOT containing `<script>` (contains `&lt;script&gt;`) and `message` NOT containing `<img` (contains `&lt;img`).
    - Also add a direct unit test of escapeHtml: `expect(escapeHtml("<a>&\"'")).toBe("&lt;a&gt;&amp;&quot;&#39;")`.
    - Mock relative paths from tests/api/contact → src as `../../../src/...`. Import ContactService from `../../../src/api/contact/services/contact.service`.
  </action>
  <verify>
    <automated>cd apps/strapi && pnpm exec jest tests/api/contact/contact-escape.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/strapi/src/services/mjml/escape.ts exists and exports escapeHtml that converts < to &lt;, > to &gt;, & to &amp;, " to &quot;, ' to &#39;
    - index.ts re-exports escapeHtml; autoescape remains false (grep "autoescape: false" still present)
    - contact.service.ts calls escapeHtml on fullname, phone, company in the contact-user payload and on fullname, email, phone, company, message in the contact-admin payload
    - apps/strapi/tests/api/contact/contact-escape.test.ts proves an HTML payload is escaped in the rendered data and includes a direct escapeHtml assertion
    - `cd apps/strapi && pnpm exec jest tests/api/contact/contact-escape.test.ts` exits 0
  </acceptance_criteria>
  <done>User-supplied contact fields are escaped before email render; an HTML injection payload is neutralized; autoescape stays false.</done>
</task>

<task type="auto">
  <name>Task 3: Fix USER_UPDATE_PATH_REGEX trailing-slash bypass + extend its test</name>
  <files>apps/strapi/src/middlewares/protect-user-fields.ts, apps/strapi/tests/middlewares/protect-user-fields.test.ts</files>
  <read_first>
    - apps/strapi/src/middlewares/protect-user-fields.ts (line 34: USER_UPDATE_PATH_REGEX = /^\/api\/users\/\d+$/)
    - apps/strapi/tests/middlewares/protect-user-fields.test.ts (existing 10 cases; you will ADD a trailing-slash case, not modify existing ones)
  </read_first>
  <action>
    In protect-user-fields.ts, change:
    ```ts
    const USER_UPDATE_PATH_REGEX = /^\/api\/users\/\d+$/;
    ```
    to allow an optional trailing slash:
    ```ts
    const USER_UPDATE_PATH_REGEX = /^\/api\/users\/\d+\/?$/;
    ```
    Update the adjacent comment to note the optional trailing slash. Do NOT touch PROTECTED_USER_FIELDS (per 126-01, email/password must stay OUT; the existing 8 fields stay). No other changes to this file.

    Then ADD one new it() case to protect-user-fields.test.ts (append; do not edit existing cases):
    "strips protected fields on PUT /api/users/123/ (trailing slash) — regex bypass closed":
    - createContext("PUT", "/api/users/123/", { data: { pro_status: "active", firstname: "Alice" } })
    - after middleware: expect body.data NOT to have pro_status, expect firstname "Alice", expect next called.
  </action>
  <verify>
    <automated>cd apps/strapi && pnpm exec jest tests/middlewares/protect-user-fields.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - protect-user-fields.ts USER_UPDATE_PATH_REGEX matches /^\/api\/users\/\d+\/?$/ (grep `\\d+\\/?\$`)
    - PROTECTED_USER_FIELDS unchanged — still 8 fields, no email/password (grep -E '"(email|password)"' returns nothing)
    - protect-user-fields.test.ts has a new it() asserting stripping on the trailing-slash path
    - `cd apps/strapi && pnpm exec jest tests/middlewares/protect-user-fields.test.ts` exits 0 (all prior + new case pass)
  </acceptance_criteria>
  <done>A trailing-slash user-update path no longer bypasses field stripping; existing protections intact.</done>
</task>

</tasks>

<verification>
- Both dev endpoints 404 outside dev (Vitest passes)
- Contact user fields escaped before email render (Jest passes); autoescape still false
- USER_UPDATE_PATH_REGEX handles trailing slash (Jest passes); email/password still not protected
- No unused vars/imports (Codacy)
</verification>

<success_criteria>
- dev-config.get.ts and dev-login.post.ts unreachable (404) in production
- HTML in contact fields is escaped in operator/admin emails
- /api/users/:id/ trailing slash cannot bypass field stripping
- `cd apps/website && pnpm exec vitest run tests/server/dev-endpoints.test.ts` exits 0
- `cd apps/strapi && pnpm exec jest tests/api/contact/contact-escape.test.ts tests/middlewares/protect-user-fields.test.ts` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/126-security-hardening/126-04-SUMMARY.md`
</output>
