---
phase: 120-refactor-pro-subscription-model
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/strapi/tests/api/subscription-pro/subscription-pro.service.test.ts
  - apps/strapi/tests/api/payment/payment-pro-response.test.ts
autonomous: true
requirements: [SUB-SCHEMA-01, SUB-CHARGE-01, SUB-CHARGE-02]

must_haves:
  truths:
    - "Wave 0 test stubs exist and pass (empty describe blocks with placeholder tests)"
    - "subscription-pro service test stub is ready for Plan 01 to fill"
    - "payment-pro-response test stub is ready for Plan 02 to fill"
  artifacts:
    - path: "apps/strapi/tests/api/subscription-pro/subscription-pro.service.test.ts"
      provides: "Test stub for subscription-pro collection CRUD"
      contains: "subscription-pro"
    - path: "apps/strapi/tests/api/payment/payment-pro-response.test.ts"
      provides: "Test stub for charge-before-activate flow"
      contains: "proResponse"
  key_links: []
---

<objective>
Create Wave 0 test stubs so that Plans 01-04 can run jest assertions as part of their verification.

Purpose: Satisfies Nyquist sampling — every task can run `yarn jest --testPathPattern=...` to verify behavior, not just grep checks.
Output: Two test stub files with placeholder describe blocks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for subscription-pro and payment-pro-response</name>
  <files>
    apps/strapi/tests/api/subscription-pro/subscription-pro.service.test.ts,
    apps/strapi/tests/api/payment/payment-pro-response.test.ts
  </files>
  <read_first>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts
  </read_first>
  <action>
1. Create `apps/strapi/tests/api/subscription-pro/subscription-pro.service.test.ts`:
```typescript
describe("subscription-pro collection type", () => {
  describe("schema", () => {
    it.todo("should have oneToOne relation to user with inversedBy");
    it.todo("should have tbk_user, card_type, card_last4, inscription_token, pending_invoice fields");
  });

  describe("bootstrap migration", () => {
    it.todo("should create subscription-pro records for active users with tbk_user");
    it.todo("should create subscription-pro records for cancelled users with tbk_user");
    it.todo("should skip users without tbk_user");
    it.todo("should be idempotent - skip users who already have a subscription-pro record");
  });
});
```

2. Create `apps/strapi/tests/api/payment/payment-pro-response.test.ts`:
```typescript
describe("proResponse charge-before-activate", () => {
  describe("successful inscription + charge", () => {
    it.todo("should call authorizeCharge BEFORE updating user pro_status");
    it.todo("should create subscription-pro record after successful charge");
    it.todo("should set pro_status to active only after charge succeeds");
    it.todo("should redirect to /pro/pagar/gracias with order documentId");
  });

  describe("charge failure", () => {
    it.todo("should NOT activate user when charge fails");
    it.todo("should NOT create subscription-pro record when charge fails");
    it.todo("should redirect to /pro/error?reason=charge-failed");
  });

  describe("inscription failure", () => {
    it.todo("should redirect to /pro/error?reason=rejected when finishInscription fails");
    it.todo("should redirect to /pro/error?reason=cancelled when TBK_TOKEN is missing");
  });
});
```

Follow the same test file structure (imports, mock setup) as `apps/strapi/tests/cron/subscription-charge.cron.test.ts` for consistency. These are stubs with `it.todo()` — Plans 01, 02, and 04 will fill them in with real assertions.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && yarn jest --testPathPattern="subscription-pro|payment-pro-response" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>Both test stub files exist, jest recognizes them and reports todo tests. Wave 0 complete.</done>
</task>

</tasks>

<verification>
- `yarn jest --testPathPattern="subscription-pro"` — runs and reports todo tests
- `yarn jest --testPathPattern="payment-pro-response"` — runs and reports todo tests
- Both files contain `describe` blocks matching the phase requirements
</verification>

<success_criteria>
- Two test stub files exist at the correct paths
- `yarn jest --testPathPattern="subscription-pro|payment-pro-response"` exits 0
- All tests are `.todo()` stubs ready for Plans 01-04 to implement
</success_criteria>

<output>
After completion, create `.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-00-SUMMARY.md`
</output>
