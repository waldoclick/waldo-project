---
phase: 103.1-remove-pro-boolean-use-pro-status-as-single-source-of-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/types/user.d.ts
  - apps/website/app/pages/cuenta/avatar.vue
  - apps/website/app/pages/cuenta/cover.vue
  - apps/website/app/pages/cuenta/username.vue
  - apps/website/app/components/HeroProfile.vue
  - apps/website/app/components/ProfileDefault.vue
  - apps/website/app/components/AccountProfile.vue
  - apps/website/app/components/AccountMain.vue
  - apps/website/app/components/CardAnnouncement.vue
  - apps/website/app/components/AvatarDefault.vue
  - apps/dashboard/app/types/user.ts
  - apps/dashboard/app/pages/users/[id].vue
  - apps/dashboard/app/components/AvatarDefault.vue
autonomous: true
requirements:
  - PRO-SINGLE-05
  - PRO-SINGLE-06
  - PRO-SINGLE-07

must_haves:
  truths:
    - "Website PRO page gates (avatar, cover, username) check pro_status === 'active' not pro boolean"
    - "Website components (HeroProfile, ProfileDefault, AccountProfile, AccountMain, CardAnnouncement, AvatarDefault) use pro_status instead of pro"
    - "Dashboard admin user view shows pro_status string instead of formatted pro boolean"
    - "Website User type definition has pro_status field and no pro boolean field"
    - "Dashboard User type definition has pro_status field and no pro boolean field"
  artifacts:
    - path: "apps/website/app/types/user.d.ts"
      provides: "User type without pro boolean"
    - path: "apps/dashboard/app/types/user.ts"
      provides: "Dashboard User type with pro_status"
    - path: "apps/website/app/pages/cuenta/avatar.vue"
      provides: "PRO gate using pro_status"
      contains: "pro_status"
    - path: "apps/website/app/components/CardAnnouncement.vue"
      provides: "Seller link conditional using pro_status"
      contains: "pro_status"
  key_links:
    - from: "apps/strapi/src/api/ad/services/sanitize-ad.ts"
      to: "apps/website/app/components/CardAnnouncement.vue"
      via: "public API now returns pro_status instead of pro"
      pattern: "pro_status.*active"
    - from: "apps/website/app/types/user.d.ts"
      to: "apps/website/app/pages/cuenta/avatar.vue"
      via: "User type drives page gate logic"
      pattern: "pro_status"
---

<objective>
Replace all frontend reads of `user.pro` boolean with `user.pro_status === "active"` checks across the website and dashboard apps. Update type definitions to remove the `pro` boolean field.

Purpose: Complete the client-side half of the migration so no application code reads the deprecated `pro` boolean. Without this, PRO page gates would break once the server stops exposing `pro` in the sanitized API response.
Output: All website and dashboard code uses `pro_status` exclusively; type definitions reflect the new shape.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103.1-remove-pro-boolean-use-pro-status-as-single-source-of-truth/103.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update type definitions and website PRO page gates</name>
  <files>
    apps/website/app/types/user.d.ts
    apps/dashboard/app/types/user.ts
    apps/website/app/pages/cuenta/avatar.vue
    apps/website/app/pages/cuenta/cover.vue
    apps/website/app/pages/cuenta/username.vue
  </files>
  <read_first>
    apps/website/app/types/user.d.ts
    apps/dashboard/app/types/user.ts
    apps/website/app/pages/cuenta/avatar.vue
    apps/website/app/pages/cuenta/cover.vue
    apps/website/app/pages/cuenta/username.vue
  </read_first>
  <action>
**apps/website/app/types/user.d.ts:**
- Remove the line `pro: boolean;` from the User interface
- Verify that `pro_status: "active" | "inactive" | "cancelled" | null;` already exists (it should from Phase 102)
- If `pro_status` line does not exist, add it: `pro_status: "active" | "inactive" | "cancelled" | null;`
- Keep all other fields (`pro_card_type`, `pro_card_last4`, `pro_expires_at`) unchanged

**apps/dashboard/app/types/user.ts:**
- Remove or comment out `pro?: boolean;`
- Add `pro_status?: "active" | "inactive" | "cancelled" | null;` if not already present
- Keep all other fields unchanged

**apps/website/app/pages/cuenta/avatar.vue:**
- Find the PRO gate: `if (!user.value?.pro)`
- Replace with: `if (user.value?.pro_status !== "active")`
- Keep the error body (showError with statusCode 403) exactly as-is

**apps/website/app/pages/cuenta/cover.vue:**
- Same change: `if (!user.value?.pro)` becomes `if (user.value?.pro_status !== "active")`

**apps/website/app/pages/cuenta/username.vue:**
- Same change: `if (!user.value?.pro)` becomes `if (user.value?.pro_status !== "active")`
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -rn "\.pro\b" apps/website/app/types/user.d.ts apps/website/app/pages/cuenta/avatar.vue apps/website/app/pages/cuenta/cover.vue apps/website/app/pages/cuenta/username.vue | grep -v pro_status | grep -v pro_card | grep -v pro_expires | grep -v pro_inscription | grep -v provider ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/website/app/types/user.d.ts` does NOT contain `pro: boolean` (grep returns 0 matches)
    - `apps/website/app/types/user.d.ts` contains `pro_status: "active" | "inactive" | "cancelled" | null`
    - `apps/dashboard/app/types/user.ts` does NOT contain `pro?: boolean` (grep returns 0 matches)
    - `apps/dashboard/app/types/user.ts` contains `pro_status`
    - `apps/website/app/pages/cuenta/avatar.vue` contains `pro_status !== "active"` (not `!user.value?.pro`)
    - `apps/website/app/pages/cuenta/cover.vue` contains `pro_status !== "active"`
    - `apps/website/app/pages/cuenta/username.vue` contains `pro_status !== "active"`
  </acceptance_criteria>
  <done>Type definitions updated, all three PRO page gates use pro_status</done>
</task>

<task type="auto">
  <name>Task 2: Update all website and dashboard components to use pro_status</name>
  <files>
    apps/website/app/components/HeroProfile.vue
    apps/website/app/components/ProfileDefault.vue
    apps/website/app/components/AccountProfile.vue
    apps/website/app/components/AccountMain.vue
    apps/website/app/components/CardAnnouncement.vue
    apps/website/app/components/AvatarDefault.vue
    apps/dashboard/app/pages/users/[id].vue
    apps/dashboard/app/components/AvatarDefault.vue
  </files>
  <read_first>
    apps/website/app/components/HeroProfile.vue
    apps/website/app/components/ProfileDefault.vue
    apps/website/app/components/AccountProfile.vue
    apps/website/app/components/AccountMain.vue
    apps/website/app/components/CardAnnouncement.vue
    apps/website/app/components/AvatarDefault.vue
    apps/dashboard/app/pages/users/[id].vue
    apps/dashboard/app/components/AvatarDefault.vue
  </read_first>
  <action>
**HeroProfile.vue:**
- Find: `v-if="user?.pro && user?.cover"` (or similar template expression)
- Replace with: `v-if="user?.pro_status === 'active' && user?.cover"`

**ProfileDefault.vue:**
- Find all occurrences of `user.pro` or `!user.pro`
- The MemoPro condition (e.g., `v-if="!user.pro"`) becomes `v-if="user.pro_status !== 'active'"`
- The MemoDefault/card condition (e.g., `v-if="user.pro"`) becomes `v-if="user.pro_status === 'active'"`
- Check both template and script sections

**AccountProfile.vue:**
- Find: `user.pro ? 'Pro' : 'Estándar'` (or similar ternary)
- Replace with: `user.pro_status === "active" ? 'Pro' : 'Estándar'`

**AccountMain.vue:**
- Find: `!user?.pro` (show "Hazte PRO" banner condition)
- Replace with: `user?.pro_status !== "active"`

**CardAnnouncement.vue:**
- Find: `getUser.pro` (show seller link on pro profiles)
- Replace with: `getUser.pro_status === "active"`
- IMPORTANT: This reads from the sanitized API response. Plan 01 changes `sanitize-ad.ts` to return `pro_status` instead of `pro`. Both plans run in the same wave, so the API shape and the consumer must both be updated.

**AvatarDefault.vue (website):**
- Find: `(user as any)?.pro`
- Replace with: `(user as any)?.pro_status === "active"`

**Dashboard — users/[id].vue:**
- Find: `formatBoolean(item.pro)` or similar display of `item.pro`
- Replace with displaying the `item.pro_status` value directly, e.g., `item.pro_status ?? 'inactive'`
- This should show the string value ("active", "inactive", "cancelled") instead of a boolean

**Dashboard — AvatarDefault.vue:**
- Find: `(user as any)?.pro`
- Replace with: `(user as any)?.pro_status === "active"`
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -rn "\.pro\b\|\"pro\"\|'pro'" apps/website/app/components/HeroProfile.vue apps/website/app/components/ProfileDefault.vue apps/website/app/components/AccountProfile.vue apps/website/app/components/AccountMain.vue apps/website/app/components/CardAnnouncement.vue apps/website/app/components/AvatarDefault.vue apps/dashboard/app/pages/users/\[id\].vue apps/dashboard/app/components/AvatarDefault.vue | grep -v pro_status | grep -v pro_card | grep -v pro_expires | grep -v pro_inscription | grep -v "//.*pro" | grep -v provider ; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.pro\b" apps/website/app/components/HeroProfile.vue` returns 0 (only pro_status references remain)
    - `apps/website/app/components/ProfileDefault.vue` contains `pro_status` checks (not `.pro` boolean)
    - `apps/website/app/components/AccountProfile.vue` contains `pro_status === "active" ? 'Pro' : 'Estándar'`
    - `apps/website/app/components/AccountMain.vue` contains `pro_status !== "active"` (not `!user?.pro`)
    - `apps/website/app/components/CardAnnouncement.vue` contains `pro_status === "active"` (not `getUser.pro`)
    - `apps/website/app/components/AvatarDefault.vue` contains `pro_status === "active"` (not `.pro`)
    - `apps/dashboard/app/pages/users/[id].vue` displays `pro_status` string (not `formatBoolean(item.pro)`)
    - `apps/dashboard/app/components/AvatarDefault.vue` contains `pro_status === "active"` (not `.pro`)
  </acceptance_criteria>
  <done>All website and dashboard components use pro_status instead of pro boolean, no frontend code reads user.pro</done>
</task>

</tasks>

<verification>
After both tasks complete, verify no frontend code references the deprecated `pro` boolean:

```bash
# Website — should return 0 matches (excluding pro_status, pro_card, pro_expires, pro_inscription, provider)
grep -rn "\.pro\b" apps/website/app/ --include="*.vue" --include="*.ts" --include="*.d.ts" | grep -v pro_status | grep -v pro_card | grep -v pro_expires | grep -v pro_inscription | grep -v provider | grep -v node_modules

# Dashboard — same check
grep -rn "\.pro\b" apps/dashboard/app/ --include="*.vue" --include="*.ts" | grep -v pro_status | grep -v pro_card | grep -v pro_expires | grep -v pro_inscription | grep -v provider | grep -v node_modules
```

Run TypeScript checks on both apps:
```bash
cd apps/website && npx nuxt typecheck
cd apps/dashboard && npx nuxt typecheck
```
</verification>

<success_criteria>
- All website PRO page gates use `pro_status !== "active"` instead of `!user.pro`
- All website components use `pro_status === "active"` instead of `user.pro`
- Dashboard admin view shows `pro_status` string, not formatted boolean
- User type definitions no longer contain `pro: boolean`
- No `.pro` boolean references remain in website or dashboard code (only `pro_status`, `pro_card_*`, `pro_expires_at`)
</success_criteria>

<output>
After completion, create `.planning/phases/103.1-remove-pro-boolean-use-pro-status-as-single-source-of-truth/103.1-02-SUMMARY.md`
</output>
