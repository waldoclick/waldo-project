---
phase: 117-enforce-root-level-tests-directory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - STRUCT-117-WEB

must_haves:
  truths:
    - "All 23 website test files reside under apps/website/tests/"
    - "Zero test files exist under apps/website/app/"
    - "Test suite baseline unchanged: 107 passing, 17 pre-existing failures"
    - "Vitest config requires no changes for tests/ layout"
  artifacts:
    - path: "apps/website/tests/"
      provides: "Centralized test directory with mirrored structure"
      contains: "23 .test.ts files across 7 subdirectories"
    - path: "apps/website/vitest.config.ts"
      provides: "Test runner configuration with @ alias"
      contains: "alias.*@.*./app"
  key_links:
    - from: "apps/website/tests/**/*.test.ts"
      to: "apps/website/app/**/*.{ts,vue}"
      via: "import using @/ alias"
      pattern: "from ['\"]@/"
---

<objective>
Formally verify and close Phase 117 — confirm that the website test directory structure is fully compliant with the Mandatory Testing Directory Rule.

Purpose: Phase 116 already moved all 23 website test files to apps/website/tests/ with mirrored folder structure. Phase 117 exists as a formal verification and closure record. No file moves needed.
Output: Verification artifacts confirming compliance, updated ROADMAP.md and STATE.md marking phase complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-enforce-root-level-tests-directory-for-website-move-all-test-files-to-apps-website-tests-following-the-mandatory-testing-directory-rule-preserve-mirrored-folder-structure-zero-test-logic-changes/117-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify website test directory compliance and run test baseline</name>
  <files>.planning/phases/117-enforce-root-level-tests-directory-for-website-move-all-test-files-to-apps-website-tests-following-the-mandatory-testing-directory-rule-preserve-mirrored-folder-structure-zero-test-logic-changes/117-01-SUMMARY.md</files>
  <read_first>
    - apps/website/vitest.config.ts
    - .planning/phases/117-enforce-root-level-tests-directory-for-website-move-all-test-files-to-apps-website-tests-following-the-mandatory-testing-directory-rule-preserve-mirrored-folder-structure-zero-test-logic-changes/117-RESEARCH.md
  </read_first>
  <action>
    Run the following verification checks in sequence:

    1. **Structural compliance — test files in tests/**
       Run: `find apps/website/tests -name "*.test.ts" | sort`
       Expected: 23 files listed across 7 subdirectories (components/, composables/, middleware/, pages/, plugins/, server/, stores/)

    2. **Structural compliance — zero test files under app/**
       Run: `find apps/website/app -name "*.test.ts" -o -name "*.spec.ts"`
       Expected: empty output (zero results)

    3. **No spec files anywhere in website**
       Run: `find apps/website -path "*/node_modules" -prune -o -name "*.spec.ts" -print`
       Expected: empty output

    4. **Stubs are infrastructure, not tests**
       Run: `ls apps/website/tests/stubs/`
       Expected: `app.stub.ts` and `imports.stub.ts` only (2 files, neither is a test)

    5. **Import alias verification — all test imports use @/ not relative paths**
       Run: `grep -r "from '\.\./\.\./app/" apps/website/tests/ || echo "CLEAN: no relative app imports"`
       Expected: "CLEAN: no relative app imports"

    6. **Run full test suite to capture baseline**
       Run: `cd apps/website && yarn test --run`
       Expected: 107 tests passing, 17 pre-existing failures (124 total). Do NOT fix the 17 failures — they are pre-existing and out of scope.

    7. **Vitest config unchanged verification**
       Confirm `apps/website/vitest.config.ts` has `@` alias pointing to `./app` and no explicit include pattern (uses Vitest default).

    Record all outputs in the SUMMARY.md file. The SUMMARY must include:
    - All 6 verification check results with pass/fail
    - Test suite output (total tests, passing count, failing count)
    - Explicit statement: "Phase 117 verification: all checks PASSED. Website test directory structure is compliant."
  </action>
  <verify>
    <automated>find apps/website/app -name "*.test.ts" -o -name "*.spec.ts" | wc -l | grep -q "^0$" && echo "PASS: zero test files under app/" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/website/tests -name "*.test.ts" | wc -l` outputs exactly `23`
    - `find apps/website/app -name "*.test.ts" -o -name "*.spec.ts"` outputs nothing (empty)
    - `cd apps/website && yarn test --run` shows 107 passing tests
    - SUMMARY.md file exists at `.planning/phases/117-.../117-01-SUMMARY.md`
    - SUMMARY.md contains the string "all checks PASSED"
  </acceptance_criteria>
  <done>All 7 verification checks pass. Test suite baseline confirmed at 107 passing / 17 pre-existing failures. SUMMARY.md documents the verification record.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and STATE.md to close Phase 117</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    **ROADMAP.md updates:**
    1. Find the Phase 117 section (starts with `### Phase 117:`)
    2. Replace `**Goal:** [To be planned]` with:
       `**Goal:** Formally verify that all 23 website test files reside exclusively under apps/website/tests/ with mirrored folder structure, confirming the Mandatory Testing Directory Rule is enforced. Verification-only phase — structural work completed in Phase 116.`
    3. Replace `**Requirements**: TBD` with:
       `**Requirements**: [STRUCT-117-WEB]`
    4. Replace `**Plans:** 0 plans` with:
       `**Plans:** 1/1 plans complete`
    5. Replace the Plans list:
       ```
       Plans:
       - [x] 117-01-PLAN.md — Verify website test directory compliance and close phase
       ```

    **STATE.md updates:**
    1. Update `stopped_at:` to `Completed 117-01-PLAN.md`
    2. Update `last_updated:` to current ISO timestamp
    3. Update `current focus` to reflect Phase 117 complete
    4. Add to `### Roadmap Evolution` section:
       `- Phase 117 completed: verification-only closure — website test directory structure confirmed compliant after Phase 116 moves`
  </action>
  <verify>
    <automated>grep -c "1/1 plans complete" .planning/ROADMAP.md | grep -q "1" && grep -c "STRUCT-117-WEB" .planning/ROADMAP.md | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 117 contains `**Goal:** Formally verify that all 23 website test files`
    - ROADMAP.md Phase 117 contains `**Requirements**: [STRUCT-117-WEB]`
    - ROADMAP.md Phase 117 contains `**Plans:** 1/1 plans complete`
    - ROADMAP.md Phase 117 contains `- [x] 117-01-PLAN.md`
    - STATE.md contains `stopped_at: Completed 117-01-PLAN.md`
    - STATE.md Roadmap Evolution section contains `Phase 117 completed`
  </acceptance_criteria>
  <done>ROADMAP.md and STATE.md reflect Phase 117 as complete with verification-only closure status.</done>
</task>

</tasks>

<verification>
1. `find apps/website/tests -name "*.test.ts" | wc -l` returns `23`
2. `find apps/website/app -name "*.test.ts" -o -name "*.spec.ts"` returns empty
3. `cd apps/website && yarn test --run` shows 107 passing, 17 failing (pre-existing)
4. ROADMAP.md shows Phase 117 with `1/1 plans complete`
5. STATE.md shows `Completed 117-01-PLAN.md`
</verification>

<success_criteria>
- Website test directory structure formally verified as compliant
- Test suite baseline documented (107 passing / 17 pre-existing failures)
- Phase 117 closed in ROADMAP.md and STATE.md
- SUMMARY.md created with full verification record
</success_criteria>

<output>
After completion, create `.planning/phases/117-enforce-root-level-tests-directory-for-website-move-all-test-files-to-apps-website-tests-following-the-mandatory-testing-directory-rule-preserve-mirrored-folder-structure-zero-test-logic-changes/117-01-SUMMARY.md`
</output>
