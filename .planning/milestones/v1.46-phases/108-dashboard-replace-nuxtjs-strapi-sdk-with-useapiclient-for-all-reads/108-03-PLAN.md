---
phase: 108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads
plan: 03
type: execute
wave: 2
depends_on: ["108-01", "108-02"]
files_modified: []
autonomous: true
requirements: [RDR-108-03]

must_haves:
  truths:
    - "Zero strapi.find() or strapi.findOne() calls remain in the entire dashboard app directory"
    - "TypeScript compiles without errors after all migrations"
    - "Vitest test suite passes"
    - "useStrapiUser() and useStrapiToken() are untouched in out-of-scope files"
  artifacts: []
  key_links: []
---

<objective>
Final verification sweep: confirm zero `strapi.find`/`strapi.findOne` calls remain in the dashboard, run TypeScript typecheck, and run the full test suite.

Purpose: Gate check before the phase is considered complete. Catches any files missed by Plans 01 and 02, and confirms the codebase compiles cleanly.
Output: Clean verification confirming the migration is complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-RESEARCH.md
@.planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-01-SUMMARY.md
@.planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full codebase grep + typecheck + test suite</name>
  <files><!-- verification-only task: no files modified, fixes any stragglers found by grep --></files>
  <read_first>
    .planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-01-SUMMARY.md
    .planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-02-SUMMARY.md
  </read_first>
  <action>
Run three verification commands in sequence:

1. **Grep sweep** -- confirm zero `strapi.find()` or `strapi.findOne()` calls remain in the entire dashboard app:
   ```bash
   grep -rn "strapi\.find\|strapi\.findOne" apps/dashboard/app/
   ```
   Expected: zero matches. If any matches are found, fix them using the same apiClient pattern from Plans 01/02.

2. **useStrapi() audit** -- confirm `useStrapi()` is only used in out-of-scope files:
   ```bash
   grep -rn "useStrapi()" apps/dashboard/app/
   ```
   Expected: zero matches. `useStrapiUser()` and `useStrapiToken()` may still appear (those are fine). But bare `useStrapi()` should be gone from all content-reading files.

3. **TypeScript typecheck:**
   ```bash
   cd apps/dashboard && yarn nuxt typecheck
   ```
   Expected: exits 0, no errors.

4. **Vitest test suite:**
   ```bash
   yarn workspace waldo-dashboard vitest run
   ```
   Expected: all tests pass.

If grep finds missed files, migrate them following the same pattern. If typecheck finds errors, fix the type issues (likely missing casts or incorrect response shape handling). If tests fail, investigate and fix.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -rn "strapi\.find\|strapi\.findOne" apps/dashboard/app/ | grep -cv "^$"; test $? -eq 1 && echo "PASS: no strapi.find/findOne calls found" || echo "FAIL: strapi.find/findOne calls still present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "strapi\.find\|strapi\.findOne" apps/dashboard/app/` returns zero matches
    - `grep -rn "useStrapi()" apps/dashboard/app/` returns zero matches (bare `useStrapi()`, not `useStrapiUser()` or `useStrapiToken()`)
    - `yarn nuxt typecheck` in apps/dashboard exits with code 0
    - `yarn workspace waldo-dashboard vitest run` exits with code 0, all tests pass
    - Out-of-scope files still contain `useStrapiUser()` and `useStrapiToken()` unchanged
  </acceptance_criteria>
  <done>Phase 108 migration verified complete: zero strapi.find/findOne calls remain, TypeScript compiles, tests pass</done>
</task>

</tasks>

<verification>
This plan IS the verification gate. Its single task runs the final checks.
</verification>

<success_criteria>
- All four verification commands pass (grep sweep, useStrapi audit, typecheck, vitest)
- No regressions introduced
- Phase 108 is ready for `/gsd:verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/108-dashboard-replace-nuxtjs-strapi-sdk-with-useapiclient-for-all-reads/108-03-SUMMARY.md`
</output>
