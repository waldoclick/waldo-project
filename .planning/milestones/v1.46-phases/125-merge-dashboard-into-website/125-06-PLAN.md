---
phase: 125-merge-dashboard-into-website
plan: 06
type: execute
wave: 6
depends_on: [125-05]
files_modified:
  - apps/website/app/scss/components/
  - apps/website/app/scss/app.scss
  - apps/website/app/components/
  - apps/website/app/pages/dashboard/
autonomous: true
requirements: [SCSS-01, TYPE-01]
must_haves:
  truths:
    - "Colliding SCSS files have dashboard content appended as &--dashboard modifier blocks"
    - "Dashboard-exclusive SCSS files copied into website and imported in app.scss"
    - "Every copied dashboard-only SCSS file using @extend .container has @use '../base/container' prepended"
    - "yarn workspace waldo-website build succeeds with no Sass errors"
    - "yarn workspace waldo-website nuxi typecheck passes"
  artifacts:
    - path: "apps/website/app/scss/components/_ads.scss"
      provides: "Copied dashboard-exclusive SCSS"
      contains: "@use \"../base/container\""
    - path: "apps/website/app/scss/app.scss"
      provides: "Imports for new SCSS files"
  key_links:
    - from: "apps/website/app/scss/app.scss"
      to: "components/ads, components/chart, etc."
      via: "@use additions"
      pattern: "@use \"components/"
---

<objective>
Merge dashboard SCSS into website (append &--dashboard blocks for colliding files, copy dashboard-exclusive files with @use fixes), then remediate all TypeScript errors exposed by moving 68 pages + 95 components into website's typeCheck:true compiler.

Purpose: SCSS-01 and TYPE-01 are the two build gates. The move exposed dashboard code to strict typecheck for the first time (RESEARCH.md Pitfall 1 — expect 20-60 errors). Dashboard-exclusive types were already copied in Plan 04, so typecheck can resolve them. Fix the remaining errors here, in the plan after the bulk move, so the executor has full context budget for remediation.
Output: merged SCSS that builds clean; typecheck-green website.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md
@.planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md

<interfaces>
Colliding SCSS files (append &--dashboard block INSIDE the existing root class, D-06):
auth, avatar, body, button, card, easy, filter, footer, form, gallery, header, hero, input,
introduce, layout, lightbox, loading, logo, menu, packs, page, policies, recaptcha, search,
swal, terms, title, upload
  Strategy: dashboard's `&--default { ... }` block becomes `&--dashboard { ... }` appended inside
  website's existing root class (e.g. inside `.hero {}`). Do NOT rename website's own modifiers.

Dashboard-only SCSS files (copy as new files, D-06):
ads, articles, badge, better-stack, box, breadcrumbs, categories, chart, cloudflare, communes,
conditions, dropdown, faqs, featured, google-analytics, orders, pagination, regions, reservations,
search-console, statistics, stats, subscription-payments, subscription-pros, table, textarea,
toolbar, users, verify-code

@use container fix (D-16): rather than trusting a hardcoded "18 files" list (which has a known
off-by-one and includes _policies — a COLLISION, not a copy), derive empirically: any copied
dashboard-only file that contains `@extend .container` AND lacks `@use "../base/container"` must have
that @use prepended. The build gate confirms completeness.

Dashboard-exclusive TS types were copied in Plan 04 (order, subscription-pro, subscription-payment,
better-stack) — typecheck can now resolve `@/types/order` etc. Do NOT copy types here.

User type mismatch (RESEARCH.md Pitfall 2): dashboard User.role lacks `type: string`; website's has it.
Use website's User everywhere; remove dashboard-type imports causing "Property 'type' is missing".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge SCSS — append &--dashboard blocks + copy dashboard-only files with @use fix</name>
  <files>apps/website/app/scss/components/, apps/website/app/scss/app.scss</files>
  <read_first>
    - apps/dashboard/app/scss/components/ (source SCSS files)
    - apps/website/app/scss/components/ (collision targets; check existing @use blocks)
    - apps/website/app/scss/app.scss (import list — add new @use lines)
    - apps/website/app/scss/components/_hero.scss (example of root class + modifier structure to append into)
    - .planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md (D-06 merge strategy, D-16 @use fix)
  </read_first>
  <action>
    1. Compute the SCSS collision set:
       `comm -12 <(ls apps/dashboard/app/scss/components/ | sort) <(ls apps/website/app/scss/components/ | sort)`
       The complement (dashboard-only) is `comm -23` of the same two lists.
    2. For each COLLIDING file: open dashboard's version, take its modifier block(s) (e.g. `&--default { ... }`), rename the modifier to `&--dashboard`, and append it INSIDE website's existing root class block (e.g. inside `.hero {}` in website's _hero.scss). Do NOT create a standalone selector and do NOT rename website's existing modifiers (RESEARCH.md anti-pattern). Follow BEM per CLAUDE.md — the appended block is `&--dashboard` with children scoped under it (`&--dashboard__element`).
    3. For each dashboard-ONLY file: copy into apps/website/app/scss/components/ via git mv. Then derive the @use fix empirically (do NOT trust a hardcoded list):
       `for f in <copied dashboard-only files>; do if grep -q "@extend .container" "$f" && ! grep -q '@use "../base/container"' "$f"; then prepend '@use "../base/container";' as the first line; fi; done`
       Also verify each copied file's other @use deps resolve (mixins, variables) — add `@use "../abstracts/variables";` / `@use "../abstracts/mixins";` only if the file references them and they are missing.
    4. Add `@use "components/<name>";` lines to apps/website/app/scss/app.scss for every newly copied dashboard-only file.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace waldo-website build 2>&1 | grep -i "sass\|scss\|extend\|undefined" | head -20; echo "build-exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace waldo-website build` completes with no Sass compilation errors (no "Can't extend .container", no "Undefined variable")
    - `grep -l "&--dashboard" apps/website/app/scss/components/_hero.scss apps/website/app/scss/components/_header.scss` matches (append landed)
    - Empirical @use completeness: `for f in apps/website/app/scss/components/*.scss; do if grep -q "@extend .container" "$f" && ! grep -q '@use "../base/container"' "$f"; then echo "MISSING: $f"; fi; done` prints nothing
    - `grep -c "@use \"components/ads\"\|@use \"components/chart\"\|@use \"components/table\"" apps/website/app/scss/app.scss` returns >= 3 (imports added)
    - `grep "&--default" apps/website/app/scss/components/_hero.scss` STILL matches (website's own modifier untouched)
  </acceptance_criteria>
  <done>Colliding SCSS appended as &--dashboard blocks; dashboard-only files copied with @use container fix applied wherever @extend .container is used; app.scss imports added; build passes with no Sass errors.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript remediation — fix all errors from the migration</name>
  <files>apps/website/app/components/, apps/website/app/pages/dashboard/</files>
  <read_first>
    - apps/website/app/types/user.d.ts (website's User.role shape with `type: string`)
    - .planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md (Pitfall 1 typecheck escalation, Pitfall 2 User shape)
  </read_first>
  <action>
    SCOPE NOTE: `files_modified` lists directories but only files with actual TypeScript errors are touched — RESEARCH.md estimates 20-60 errors across ~20-40 files, not all 168 migrated files. Do NOT rewrite files that already typecheck correctly.

    Run `yarn workspace waldo-website nuxi typecheck` and fix every reported error in the migrated pages/components. Dashboard-exclusive types were already copied in Plan 04, so `@/types/order`, `@/types/subscription-pro`, etc. resolve. Common error categories (RESEARCH.md):
    - "Property 'type' is missing on role": remove the dashboard `import type { User } from "@/types/user"` so website's auto-imported User type applies; or align the object to website's shape.
    - `any` casts that dashboard's typeCheck:false tolerated: replace with proper types (NEVER use `any` — Codacy flags it; use `unknown` + narrowing or the correct interface).
    - Remove all unused imports/variables surfaced (CLAUDE.md — delete, do not prefix with _).
    - If typecheck reports a still-missing module for a type, STOP and report — Plan 04 should have copied all dashboard-exclusive types; a miss there is a Plan 04 gap, not a fix to improvise here (do NOT git mv from apps/dashboard mid-typecheck).
    Iterate until `nuxi typecheck` reports zero errors.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace waldo-website nuxi typecheck 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace waldo-website nuxi typecheck` exits 0 with no errors reported
    - `grep -rc ": any\b\|as any\b" apps/website/app/pages/dashboard | grep -v ":0" | wc -l` returns 0 (no `any` introduced)
    - `yarn workspace waldo-website test --run 2>&1 | tail -5` shows the auth/guard tests from Plan 01 still green
  </acceptance_criteria>
  <done>Website typecheck passes with zero errors; no `any` introduced; Plan 01 tests still green.</done>
</task>

</tasks>

<verification>
- `yarn workspace waldo-website build` succeeds (SCSS-01)
- `yarn workspace waldo-website nuxi typecheck` exits 0 (TYPE-01)
- `yarn workspace waldo-website test --run` all green
- Website's own SCSS modifiers untouched; dashboard content scoped under &--dashboard
</verification>

<success_criteria>
- SCSS merged: append blocks + copied files build clean
- @use container fix applied empirically to every dashboard-only file using @extend .container
- Website typecheck green
- No `any` introduced, no unused imports
</success_criteria>

<output>
After completion, create `.planning/phases/125-merge-dashboard-into-website/125-06-SUMMARY.md`
</output>
