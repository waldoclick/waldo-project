---
phase: 099-onboarding-ui
plan: 01
type: execute
wave: 1
depends_on: ["099-00"]
files_modified:
  - apps/website/app/layouts/onboarding.vue
  - apps/website/app/components/FormProfile.vue
  - apps/website/app/scss/components/_onboarding.scss
  - apps/website/app/scss/app.scss
  - apps/website/tests/components/FormProfile.onboarding.test.ts
autonomous: true
requirements: [LAYOUT-01, FORM-02, FORM-03]

must_haves:
  truths:
    - "A dedicated onboarding layout exists that renders only a slot with no header, footer, or navigation"
    - "FormProfile emits a 'success' event after a successful save"
    - "AccountEdit usage of FormProfile still redirects to /cuenta/perfil after save (backward compatible)"
    - "Onboarding SCSS is imported and available for components"
  artifacts:
    - path: "apps/website/app/layouts/onboarding.vue"
      provides: "Slot-only layout with no chrome"
      contains: "layout--onboarding"
    - path: "apps/website/app/scss/components/_onboarding.scss"
      provides: "BEM styles for onboarding block"
      contains: "onboarding--default"
    - path: "apps/website/app/components/FormProfile.vue"
      provides: "Profile form with success emit"
      contains: "defineEmits"
  key_links:
    - from: "apps/website/app/scss/app.scss"
      to: "apps/website/app/scss/components/_onboarding.scss"
      via: "@use import"
      pattern: '@use "components/onboarding"'
    - from: "apps/website/app/components/FormProfile.vue"
      to: "parent components"
      via: "emit('success') event"
      pattern: 'emit\("success"\)'
---

<objective>
Create the foundational pieces for onboarding UI: the slot-only layout, the SCSS styles, and the FormProfile emit refactor.

Purpose: These are prerequisites for Plan 02 which creates the actual pages and components. The layout must exist before pages can reference it, the SCSS must be imported before components render styled, and FormProfile must emit success before OnboardingDefault can intercept navigation.
Output: `onboarding.vue` layout, `_onboarding.scss` with BEM styles, FormProfile with `@success` emit capability.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/099-onboarding-ui/099-RESEARCH.md

@apps/website/app/layouts/auth.vue
@apps/website/app/components/FormProfile.vue
@apps/website/app/scss/app.scss
@apps/website/app/pages/cuenta/perfil/editar.vue
@apps/website/app/components/AccountEdit.vue

<interfaces>
<!-- Existing layout pattern (auth.vue) for reference -->
From apps/website/app/layouts/auth.vue:
```vue
<template>
  <div class="layout layout--auth">
    <slot />
    <LightboxCookies />
    <LightboxAdblock />
  </div>
</template>
```

From apps/website/app/components/AccountEdit.vue (line 16):
```vue
<FormProfile />
```
Note: No props, no event listeners. Backward compat means this must keep working unchanged.

From apps/website/app/components/FormProfile.vue (line 687):
```javascript
window.location.href = "/cuenta/perfil";
```
Note: Currently no defineEmits or defineProps in FormProfile.

From apps/website/app/scss/app.scss (last line):
```scss
@use "components/checkout";
```
Note: New @use "components/onboarding" goes after this line.

<!-- Wave 0 test stubs (from Plan 00) -->
From apps/website/tests/components/FormProfile.onboarding.test.ts:
```typescript
describe("FormProfile.vue — onboarding mode (FORM-02, FORM-03)", () => {
  it.todo("has defineEmits with 'success' event (FORM-02)");
  it.todo("has defineProps with onboardingMode boolean defaulting to false (FORM-02)");
  it.todo("emits 'success' after successful save (FORM-02)");
  it.todo("does NOT redirect to /cuenta/perfil when onboardingMode is true (FORM-02)");
  it.todo("DOES redirect to /cuenta/perfil when onboardingMode is false (backward compat) (FORM-03)");
});
```
Note: Task 2 must convert these todos into real passing tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding layout and SCSS</name>
  <files>apps/website/app/layouts/onboarding.vue, apps/website/app/scss/components/_onboarding.scss, apps/website/app/scss/app.scss</files>
  <action>
1. Create `apps/website/app/layouts/onboarding.vue` — a slot-only layout with NO header, footer, navigation, or lightbox components. Template: `<div class="layout layout--onboarding"><slot /></div>`. No `<script setup>` needed.

2. Create `apps/website/app/scss/components/_onboarding.scss` with the full BEM styles for `.onboarding` block. Include both modifiers:
   - `.onboarding--default`: flex column, centered, min-height 100vh, padding 40px 20px, white background. Children: `__logo` (margin-bottom 40px), `__form` (width 100%, max-width 700px).
   - `.onboarding--thankyou`: flex centered, min-height 100vh, padding 40px 20px, white background. Children: `__content` (max-width 500px, flex column, gap 20px), `__title` (text-align center), `__text` (text-align center), `__actions` (flex column, gap 15px, margin-top 20px).
   - Import abstracts: `@use "../abstracts/mixins" as *;` and `@use "../abstracts/variables" as *;`

3. Add `@use "components/onboarding";` as the LAST line of `apps/website/app/scss/app.scss` (after the existing `@use "components/checkout";` line).

Do NOT add box-shadow or transform: scale (per CLAUDE.md). Use only brand colors from the palette if needed.
  </action>
  <verify>
    <automated>cd /home/gabriel/Code/waldo-project/apps/website && test -f app/layouts/onboarding.vue && test -f app/scss/components/_onboarding.scss && grep -q 'onboarding' app/scss/app.scss && echo "PASS: layout and SCSS created"</automated>
  </verify>
  <done>Layout file exists with slot-only template using class "layout layout--onboarding". SCSS file exists with BEM styles for onboarding--default and onboarding--thankyou. app.scss imports the new SCSS file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add success emit to FormProfile (backward-compatible) and implement tests</name>
  <files>apps/website/app/components/FormProfile.vue, apps/website/tests/components/FormProfile.onboarding.test.ts</files>
  <behavior>
    - Test 1: FormProfile has defineEmits with 'success' event (FORM-02)
    - Test 2: FormProfile has defineProps with onboardingMode boolean defaulting to false (FORM-02)
    - Test 3: FormProfile emits 'success' after successful save (FORM-02)
    - Test 4: FormProfile does NOT redirect to /cuenta/perfil when onboardingMode is true (FORM-02)
    - Test 5: FormProfile DOES redirect to /cuenta/perfil when onboardingMode is false (FORM-03)
  </behavior>
  <action>
1. First, convert the todo stubs in `apps/website/tests/components/FormProfile.onboarding.test.ts` from `it.todo()` to real test implementations. Follow the existing test pattern from `FormRegister.test.ts`:
   - Mock globals (useRouter, useStrapiClient, useStrapiAuth, useStrapiUser, etc.)
   - vi.mock for #app, vue-router, vee-validate, yup
   - Import FormProfile AFTER mocks
   - Mount with @vue/test-utils
   - Test that the component has defineEmits and defineProps by inspecting emits and props on the mounted wrapper
   - Test emit behavior by triggering the form submit flow and asserting `wrapper.emitted('success')` exists
   - Test redirect behavior by checking `window.location.href` was/was not called depending on onboardingMode prop

2. Run the tests — they should FAIL (red phase) since FormProfile does not yet have the emit/props.

3. Then modify `FormProfile.vue` to make tests pass:
   - At the top of `<script setup>`, add:
     ```
     const emit = defineEmits(["success"]);
     const props = defineProps({ onboardingMode: { type: Boolean, default: false } });
     ```
   - In the `handleSubmit` function, find the line (currently line 687):
     ```
     window.location.href = "/cuenta/perfil";
     ```
     Replace it with:
     ```
     emit("success");
     if (!props.onboardingMode) {
       window.location.href = "/cuenta/perfil";
     }
     ```

4. Run tests again — they should PASS (green phase).

Do NOT use `getCurrentInstance()?.vnode.props?.onSuccess` introspection — it is fragile per research recommendations. The `onboardingMode` prop approach is simpler and explicit.
  </action>
  <verify>
    <automated>cd /home/gabriel/Code/waldo-project/apps/website && yarn vitest run tests/components/FormProfile.onboarding.test.ts</automated>
  </verify>
  <done>FormProfile.vue has `defineEmits(["success"])` and `defineProps({ onboardingMode })`. When `onboardingMode` is false (default), the existing redirect to /cuenta/perfil fires. When true, only the emit fires. All 5 tests in FormProfile.onboarding.test.ts pass.</done>
</task>

</tasks>

<verification>
- `apps/website/app/layouts/onboarding.vue` exists and contains only slot wrapper
- `apps/website/app/scss/components/_onboarding.scss` exists with BEM classes for both modifiers
- `apps/website/app/scss/app.scss` includes `@use "components/onboarding"`
- `apps/website/app/components/FormProfile.vue` has `defineEmits(["success"])` and `defineProps({ onboardingMode })`
- The `window.location.href = "/cuenta/perfil"` redirect is conditional on `!props.onboardingMode`
- `yarn vitest run tests/components/FormProfile.onboarding.test.ts` passes all tests
</verification>

<success_criteria>
1. Onboarding layout renders a blank page with only its slot content (no header/footer/lightboxes)
2. FormProfile emits 'success' on save and only hard-redirects when onboardingMode is false
3. Existing profile editing at /cuenta/perfil/editar continues to work (AccountEdit passes no props)
4. SCSS for both onboarding--default and onboarding--thankyou modifiers is importable
5. All FormProfile onboarding tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/099-onboarding-ui/099-01-SUMMARY.md`
</output>
