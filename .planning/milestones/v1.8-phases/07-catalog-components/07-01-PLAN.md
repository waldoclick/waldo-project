---
phase: 07-catalog-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/components/PacksDefault.vue
  - apps/dashboard/app/components/UsersDefault.vue
  - apps/dashboard/app/components/RegionsDefault.vue
  - apps/dashboard/app/components/FaqsDefault.vue
  - apps/dashboard/app/components/CommunesDefault.vue
  - apps/dashboard/app/components/ConditionsDefault.vue
autonomous: true
requirements:
  - DFX-01
  - DFX-02
  - DFX-03
  - DFX-04
  - DFX-05
  - DFX-06

must_haves:
  truths:
    - "PacksDefault.vue does not contain an onMounted block or call to fetchPacks inside onMounted"
    - "UsersDefault.vue does not contain an onMounted block or call to fetchUsers inside onMounted"
    - "RegionsDefault.vue does not contain an onMounted block or call to fetchRegions inside onMounted"
    - "FaqsDefault.vue does not contain an onMounted block or call to fetchFaqs inside onMounted"
    - "CommunesDefault.vue does not contain an onMounted block or call to fetchCommunes inside onMounted"
    - "ConditionsDefault.vue does not contain an onMounted block or call to fetchConditions inside onMounted"
    - "All six components retain their watch({ immediate: true }) block as the sole data-loading trigger"
    - "yarn build in apps/dashboard exits with code 0 (typeCheck: true passes)"
  artifacts:
    - path: "apps/dashboard/app/components/PacksDefault.vue"
      provides: "Packs list component — single-fetch on mount"
      contains: "watch"
    - path: "apps/dashboard/app/components/UsersDefault.vue"
      provides: "Users list component — single-fetch on mount"
      contains: "watch"
    - path: "apps/dashboard/app/components/RegionsDefault.vue"
      provides: "Regions list component — single-fetch on mount"
      contains: "watch"
    - path: "apps/dashboard/app/components/FaqsDefault.vue"
      provides: "Faqs list component — single-fetch on mount"
      contains: "watch"
    - path: "apps/dashboard/app/components/CommunesDefault.vue"
      provides: "Communes list component — single-fetch on mount"
      contains: "watch"
    - path: "apps/dashboard/app/components/ConditionsDefault.vue"
      provides: "Conditions list component — single-fetch on mount"
      contains: "watch"
  key_links:
    - from: "apps/dashboard/app/components/PacksDefault.vue"
      to: "fetchPacks()"
      via: "watch([...], fetchPacks, { immediate: true }) — sole trigger"
      pattern: "watch\\(.*immediate.*true"
    - from: "apps/dashboard/app/components/RegionsDefault.vue"
      to: "fetchRegions()"
      via: "watch([...], fetchRegions, { immediate: true }) and searchParams: Record<string, unknown>"
      pattern: "Record<string, unknown>"
---

<objective>
Remove the redundant `onMounted` data-loading hook from all six catalog/management components. Each component already calls its fetch function via `watch({ immediate: true })`, making the `onMounted` block a duplicate that fires a second identical network request on every mount.

Purpose: Eliminate the double-fetch bug across catalog components, applying the v1.1 canonical pattern established in AdsTable.vue. This is purely subtractive — no new code is added.
Output: Six modified .vue files with onMounted removed; four of those also have `searchParams: any` corrected to `searchParams: Record<string, unknown>`.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-catalog-components/07-RESEARCH.md

<interfaces>
<!-- Canonical reference: AdsTable.vue watch-only pattern (v1.1) -->
<!-- Executor must replicate this shape in all six components. -->

Canonical pattern (from apps/dashboard/app/components/AdsTable.vue):
```typescript
// Import — onMounted NOT present
import { ref, computed, watch } from "vue";

// Sole data-loading trigger — no onMounted block anywhere in file
watch(
  [
    () => sectionSettings.value.searchTerm,
    () => sectionSettings.value.sortBy,
    () => sectionSettings.value.pageSize,
    () => sectionSettings.value.currentPage,
  ],
  () => {
    fetchAds();
  },
  { immediate: true },
);
// EOF — no onMounted block
```

Strapi SDK v5 cast pattern for searchParams (from PROJECT.md Key Decisions):
```typescript
const searchParams: Record<string, unknown> = {
  pagination: { page: ..., pageSize: ... },
  sort: ...,
};
```

Per-component change summary (from 07-RESEARCH.md):

PacksDefault.vue (DFX-01):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchPacks(); });` (lines ~231-233)
  - searchParams type: already Record<string, unknown> — no change needed

UsersDefault.vue (DFX-02):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchUsers(); });` (lines ~265-267)
  - searchParams type: already Record<string, unknown> — no change needed

RegionsDefault.vue (DFX-03):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchRegions(); });` (lines ~226-228)
  - searchParams type: change `searchParams: any` → `searchParams: Record<string, unknown>` (line ~132)

FaqsDefault.vue (DFX-04):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchFaqs(); });` (lines ~264-266)
  - searchParams type: change `searchParams: any` → `searchParams: Record<string, unknown>` (line ~153)

CommunesDefault.vue (DFX-05):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchCommunes(); });` (lines ~220-222)
  - searchParams type: change `searchParams: any` → `searchParams: Record<string, unknown>` (line ~129)

ConditionsDefault.vue (DFX-06):
  - Import: remove `onMounted` from `import { ref, computed, onMounted, watch }`
  - Delete: `onMounted(() => { fetchConditions(); });` (lines ~214-216)
  - searchParams type: change `searchParams: any` → `searchParams: Record<string, unknown>` (line ~126)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove onMounted from all six catalog components and fix searchParams types</name>
  <files>
    apps/dashboard/app/components/PacksDefault.vue,
    apps/dashboard/app/components/UsersDefault.vue,
    apps/dashboard/app/components/RegionsDefault.vue,
    apps/dashboard/app/components/FaqsDefault.vue,
    apps/dashboard/app/components/CommunesDefault.vue,
    apps/dashboard/app/components/ConditionsDefault.vue
  </files>
  <action>
For each of the six components, make two categories of changes:

**Category A — Remove onMounted (all six components):**
1. In the `import { ... } from "vue"` line, remove `onMounted` from the destructured list. Leave all other imports (`ref`, `computed`, `watch`, etc.) intact.
2. Delete the entire `onMounted(() => { fetchXxx(); });` block (3 lines). Do NOT touch the `watch(...)` block — it must remain exactly as-is.

**Category B — Fix searchParams type (RegionsDefault, FaqsDefault, CommunesDefault, ConditionsDefault only):**
3. Inside the fetch function of each of these four components, find `const searchParams: any = {` and change the type annotation to `Record<string, unknown>`. This is a single-token replacement: `any` → `Record<string, unknown>`.

**Anti-patterns (do NOT do these):**
- Do NOT remove or modify the `watch(...)` block — it is the sole correct data-loading trigger.
- Do NOT add any new guard state (e.g., `hasMounted`, `isFetching`) to compensate.
- Do NOT leave `onMounted` in the import after removing the call — unused imports create lint noise.
- Do NOT use `any` as a fallback if unsure about the searchParams type shape — use `Record<string, unknown>`.

After editing each file, verify with a quick grep that no `onMounted` appears anywhere in the file body (import or call site):
```bash
grep -n "onMounted" apps/dashboard/app/components/PacksDefault.vue    # should return nothing
grep -n "onMounted" apps/dashboard/app/components/UsersDefault.vue     # should return nothing
grep -n "onMounted" apps/dashboard/app/components/RegionsDefault.vue   # should return nothing
grep -n "onMounted" apps/dashboard/app/components/FaqsDefault.vue      # should return nothing
grep -n "onMounted" apps/dashboard/app/components/CommunesDefault.vue  # should return nothing
grep -n "onMounted" apps/dashboard/app/components/ConditionsDefault.vue # should return nothing
```

Also verify the four type fixes:
```bash
grep -n "searchParams: any" apps/dashboard/app/components/RegionsDefault.vue    # should return nothing
grep -n "searchParams: any" apps/dashboard/app/components/FaqsDefault.vue       # should return nothing
grep -n "searchParams: any" apps/dashboard/app/components/CommunesDefault.vue   # should return nothing
grep -n "searchParams: any" apps/dashboard/app/components/ConditionsDefault.vue # should return nothing
```
  </action>
  <verify>
    <automated>cd /home/gabriel/Code/waldo-project/apps/dashboard && grep -rn "onMounted" app/components/PacksDefault.vue app/components/UsersDefault.vue app/components/RegionsDefault.vue app/components/FaqsDefault.vue app/components/CommunesDefault.vue app/components/ConditionsDefault.vue; echo "exit:$?"</automated>
  </verify>
  <done>
    - `onMounted` does not appear in any of the six component files (neither in imports nor in the file body)
    - `watch({ immediate: true })` block is intact in all six files
    - `searchParams: Record<string, unknown>` is used in RegionsDefault, FaqsDefault, CommunesDefault, and ConditionsDefault
    - No new code has been added to any file
  </done>
</task>

<task type="auto">
  <name>Task 2: Run dashboard build to confirm typeCheck passes with no new errors</name>
  <files></files>
  <action>
Run the full dashboard build from the apps/dashboard directory. The build runs `vue-tsc` via `typeCheck: true` in nuxt.config.ts, which validates all .vue files for TypeScript errors.

```bash
cd /home/gabriel/Code/waldo-project/apps/dashboard && yarn build
```

Expected outcome: Build exits with code 0. No new TypeScript errors introduced by the changes in Task 1.

If the build fails: Read the error output carefully. The only expected failure modes after these changes are:
- A missed `searchParams: any` that needs to be changed to `Record<string, unknown>`
- A stray reference to `onMounted` left in an import after removing the call site (unused import — though this typically causes a lint warning, not a build error under typeCheck)

Do NOT proceed past a failing build. Fix the error in the relevant component and re-run.
  </action>
  <verify>
    <automated>cd /home/gabriel/Code/waldo-project/apps/dashboard && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `yarn build` exits with code 0. The typeCheck step produces no errors. The six modified components are valid TypeScript and the build artifact is generated successfully.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Code inspection — none of the six files contains `onMounted`:
   ```bash
   grep -rn "onMounted" \
     apps/dashboard/app/components/PacksDefault.vue \
     apps/dashboard/app/components/UsersDefault.vue \
     apps/dashboard/app/components/RegionsDefault.vue \
     apps/dashboard/app/components/FaqsDefault.vue \
     apps/dashboard/app/components/CommunesDefault.vue \
     apps/dashboard/app/components/ConditionsDefault.vue
   ```
   Expected: no output.

2. Type inspection — four components use `Record<string, unknown>` for searchParams:
   ```bash
   grep -n "searchParams:" \
     apps/dashboard/app/components/RegionsDefault.vue \
     apps/dashboard/app/components/FaqsDefault.vue \
     apps/dashboard/app/components/CommunesDefault.vue \
     apps/dashboard/app/components/ConditionsDefault.vue
   ```
   Expected: each line shows `Record<string, unknown>`, not `any`.

3. Build gate:
   ```bash
   cd apps/dashboard && yarn build
   ```
   Expected: exit code 0, no TypeScript errors.
</verification>

<success_criteria>
- All six catalog components (`PacksDefault`, `UsersDefault`, `RegionsDefault`, `FaqsDefault`, `CommunesDefault`, `ConditionsDefault`) contain no `onMounted` call or `onMounted` import
- All six retain `watch({ immediate: true })` as their sole data-loading trigger
- `searchParams` is typed as `Record<string, unknown>` in RegionsDefault, FaqsDefault, CommunesDefault, and ConditionsDefault
- `cd apps/dashboard && yarn build` exits 0 with typeCheck clean
- Requirements satisfied: DFX-01, DFX-02, DFX-03, DFX-04, DFX-05, DFX-06
</success_criteria>

<output>
After completion, create `.planning/phases/07-catalog-components/07-01-SUMMARY.md` following the summary template at `@/home/gabriel/.claude/get-shit-done/templates/summary.md`.
</output>
