---
phase: 15-links-redirects-build-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/components/MenuDefault.vue
  - apps/dashboard/app/components/DropdownUser.vue
  - apps/dashboard/app/components/DropdownSales.vue
  - apps/dashboard/app/components/DropdownPendings.vue
  - apps/dashboard/app/components/StatisticsDefault.vue
autonomous: true
requirements: [LINK-01, LINK-02]

must_haves:
  truths:
    - "The sidebar menu navigates to English URLs for all sections"
    - "The user dropdown links to /account/profile, /account/profile/edit, /account/change-password"
    - "The orders dropdown links to /orders"
    - "The pending ads dropdown links to /ads/pending and /ads/[id]"
    - "The statistics cards link to English route paths"
  artifacts:
    - path: "apps/dashboard/app/components/MenuDefault.vue"
      provides: "Sidebar navigation with all English routes"
      contains: "/ads, /orders, /reservations, /featured, /users, /categories, /conditions, /regions, /communes"
    - path: "apps/dashboard/app/components/DropdownUser.vue"
      provides: "User dropdown with English account links"
      contains: "/account/profile"
    - path: "apps/dashboard/app/components/StatisticsDefault.vue"
      provides: "Statistics cards with English route links"
      contains: "/ads/pending"
  key_links:
    - from: "apps/dashboard/app/components/MenuDefault.vue"
      to: "/ads, /reservations, /featured, /orders, /users"
      via: "NuxtLink to attribute and isRouteActive() calls"
      pattern: "to=\"/ads"
    - from: "apps/dashboard/app/components/DropdownUser.vue"
      to: "/account/profile"
      via: "NuxtLink to attribute"
      pattern: "to=\"/account/profile\""
---

<objective>
Update all navigation components to use English route paths. MenuDefault.vue is the primary sidebar navigation with ~30 Spanish route references. DropdownUser, DropdownSales, DropdownPendings and StatisticsDefault also contain Spanish paths.

Purpose: Ensures clicking any navigation element in the dashboard takes the user to an English URL (LINK-01). These components handle sidebar, header dropdowns, and the main dashboard statistics panel.
Output: 5 Vue components with zero Spanish route path strings.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MenuDefault.vue — all sidebar navigation routes to English</name>
  <files>apps/dashboard/app/components/MenuDefault.vue</files>
  <action>
Replace all Spanish route paths and route key strings in MenuDefault.vue with their English equivalents. This file has the sidebar navigation — every NuxtLink, isRouteActive() call, startsWith() check, and openMenu key must use English.

Route mapping (Spanish → English):
- `/ordenes` → `/orders`
- `/anuncios` → `/ads`
- `/anuncios/pendientes` → `/ads/pending`
- `/anuncios/activos` → `/ads/active`
- `/anuncios/expirados` → `/ads/expired`
- `/anuncios/baneados` → `/ads/banned`
- `/anuncios/rechazados` → `/ads/rejected`
- `/anuncios/abandonados` → `/ads/abandoned`
- `/reservas` → `/reservations`
- `/reservas/usadas` → `/reservations/used`
- `/reservas/libres` → `/reservations/free`
- `/destacados` → `/featured`
- `/destacados/usados` → `/featured/used`
- `/destacados/libres` → `/featured/free`
- `/usuarios` → `/users`
- `/categorias` → `/categories`
- `/condiciones` → `/conditions`
- `/regiones` → `/regions`
- `/comunas` → `/communes`

String key mapping (used in openMenu ref and toggleMenu calls):
- `'anuncios'` → `'ads'`
- `'reservas'` → `'reservations'`
- `'destacados'` → `'featured'`

In the watch() callback at the bottom, update all path.startsWith() calls to use English paths and update the openMenu.value assignments to use the English string keys.

IMPORTANT: Do NOT change any Spanish UI labels (e.g., `<span>Órdenes</span>`, `<span>Anuncios</span>`, `<span>Reservas</span>`, `<span>Comunas</span>`, etc.) — only route path strings and internal JS key strings are changed.
  </action>
  <verify>
    <automated>grep -c "anuncios\|/ordenes\|/reservas\|/destacados\|/usuarios\|/categorias\|/condiciones\|/regiones\|/comunas" apps/dashboard/app/components/MenuDefault.vue || echo "0 Spanish route refs remaining"</automated>
  </verify>
  <done>Zero Spanish route path strings remain in MenuDefault.vue. All NuxtLink `to` props and isRouteActive() calls use English paths. The openMenu keys ('ads', 'reservations', 'featured') match the new route prefixes.</done>
</task>

<task type="auto">
  <name>Task 2: Update DropdownUser, DropdownSales, DropdownPendings, StatisticsDefault</name>
  <files>
    apps/dashboard/app/components/DropdownUser.vue,
    apps/dashboard/app/components/DropdownSales.vue,
    apps/dashboard/app/components/DropdownPendings.vue,
    apps/dashboard/app/components/StatisticsDefault.vue
  </files>
  <action>
Update all four header/stats components with English routes.

**DropdownUser.vue** — 3 NuxtLink `to` props:
- `/cuenta/perfil` → `/account/profile`
- `/cuenta/perfil/editar` → `/account/profile/edit`
- `/cuenta/cambiar-contrasena` → `/account/change-password`

**DropdownSales.vue** — 2 route refs:
- `to="/ordenes"` → `to="/orders"`
- `:to="\`/ordenes/${order.id}\`"` → `:to="\`/orders/${order.id}\`"`

**DropdownPendings.vue** — 2 route refs:
- `to="/anuncios/pendientes"` → `to="/ads/pending"`
- `:to="\`/anuncios/${ad.id}\`"` → `:to="\`/ads/${ad.id}\`"`

**StatisticsDefault.vue** — 10 route refs in the `link` prop objects:
- `to: '/anuncios/pendientes'` → `to: '/ads/pending'`
- `to: '/anuncios/activos'` → `to: '/ads/active'`
- `to: '/anuncios/expirados'` → `to: '/ads/expired'`
- `to: '/anuncios/rechazados'` → `to: '/ads/rejected'`
- `to: '/reservas/usadas'` → `to: '/reservations/used'`
- `to: '/reservas/libres'` → `to: '/reservations/free'`
- `to: '/destacados/usados'` → `to: '/featured/used'`
- `to: '/destacados/libres'` → `to: '/featured/free'`
- `to: '/ordenes'` → `to: '/orders'`
- `to: '/usuarios'` → `to: '/users'`
- `to: '/categorias'` → `to: '/categories'`
- `to: '/condiciones'` → `to: '/conditions'`
- `to: '/regiones'` → `to: '/regions'`
- `to: '/comunas'` → `to: '/communes'`

NOTE on StatisticsDefault.vue: The `counts` object uses Spanish API field names (`reservasUsadas`, `destacadosLibres`, `ordenes`, `categorias`, `condiciones`, `regiones`, `comunas`) that match the Strapi API response. These are data keys, NOT route paths — do NOT rename them.
  </action>
  <verify>
    <automated>grep -c "/cuenta\|/anuncios\|/ordenes\|/reservas\|/destacados\|/usuarios\|/categorias\|/condiciones\|/regiones\|/comunas" apps/dashboard/app/components/DropdownUser.vue apps/dashboard/app/components/DropdownSales.vue apps/dashboard/app/components/DropdownPendings.vue apps/dashboard/app/components/StatisticsDefault.vue || echo "0 Spanish route refs remaining"</automated>
  </verify>
  <done>Zero Spanish route path strings in all four components. DropdownUser uses /account/* paths. DropdownSales/Pendings use /orders and /ads/* paths. StatisticsDefault card links use English paths. API data field names (reservasUsadas, etc.) remain unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify no Spanish route paths remain in any of the 5 files:

```bash
grep -rn "/anuncios\|/ordenes\|/reservas\|/destacados\|/usuarios\|/categorias\|/condiciones\|/regiones\|/comunas\|/cuenta" \
  apps/dashboard/app/components/MenuDefault.vue \
  apps/dashboard/app/components/DropdownUser.vue \
  apps/dashboard/app/components/DropdownSales.vue \
  apps/dashboard/app/components/DropdownPendings.vue \
  apps/dashboard/app/components/StatisticsDefault.vue
```

Expected: zero matches (all route paths now use English).
</verification>

<success_criteria>
- MenuDefault.vue: All 20+ NuxtLink `to` props, isRouteActive() calls, and openMenu key strings use English paths
- DropdownUser.vue: All 3 account links use /account/* paths
- DropdownSales.vue: All order links use /orders
- DropdownPendings.vue: All pending links use /ads/pending and /ads/[id]
- StatisticsDefault.vue: All 14 card link `to` values use English paths; API field names unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-links-redirects-build-verification/15-01-SUMMARY.md`
</output>
