---
phase: 04-component-consolidation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/dashboard/app/pages/anuncios/pendientes.vue
  - apps/dashboard/app/pages/anuncios/activos.vue
  - apps/dashboard/app/pages/anuncios/abandonados.vue
  - apps/dashboard/app/pages/anuncios/baneados.vue
  - apps/dashboard/app/pages/anuncios/rechazados.vue
  - apps/dashboard/app/pages/anuncios/expirados.vue
  - apps/dashboard/app/components/AdsAbandoned.vue
  - apps/dashboard/app/components/AdsActives.vue
  - apps/dashboard/app/components/AdsArchived.vue
  - apps/dashboard/app/components/AdsBanned.vue
  - apps/dashboard/app/components/AdsPendings.vue
  - apps/dashboard/app/components/AdsRejected.vue
autonomous: false
requirements: [COMP-02, COMP-03]
gap_closure: true

must_haves:
  truths:
    - "Every ads list page renders <AdsTable> with the correct endpoint and section props — no page imports an Ads* specific component"
    - "The six original Ads* component files are deleted from the codebase"
    - "Every operator workflow (search, filter, paginate, view) works identically through AdsTable on all six pages"
  artifacts:
    - path: "apps/dashboard/app/pages/anuncios/pendientes.vue"
      provides: "Migrated page using AdsTable"
      contains: "AdsTable"
    - path: "apps/dashboard/app/pages/anuncios/activos.vue"
      provides: "Migrated page using AdsTable with show-web-link"
      contains: "show-web-link"
    - path: "apps/dashboard/app/pages/anuncios/abandonados.vue"
      provides: "Migrated page using AdsTable"
      contains: "AdsTable"
    - path: "apps/dashboard/app/pages/anuncios/baneados.vue"
      provides: "Migrated page using AdsTable"
      contains: "AdsTable"
    - path: "apps/dashboard/app/pages/anuncios/rechazados.vue"
      provides: "Migrated page using AdsTable"
      contains: "AdsTable"
    - path: "apps/dashboard/app/pages/anuncios/expirados.vue"
      provides: "Migrated page using AdsTable"
      contains: "AdsTable"
  key_links:
    - from: "apps/dashboard/app/pages/anuncios/pendientes.vue"
      to: "apps/dashboard/app/components/AdsTable.vue"
      via: "import AdsTable + <AdsTable endpoint='ads/pendings' section='adsPendings'>"
      pattern: "AdsTable"
    - from: "apps/dashboard/app/pages/anuncios/activos.vue"
      to: "apps/dashboard/app/components/AdsTable.vue"
      via: "import AdsTable + <AdsTable endpoint='ads/actives' section='adsActives' :show-web-link='true'>"
      pattern: "show-web-link"
---

<objective>
Migrate all six ads list pages to use AdsTable.vue and delete the six now-redundant Ads* specific components.

Purpose: This is the final wiring step for COMP-02 and COMP-03. AdsTable.vue already exists (COMP-01 complete). The six page files still import and render their old dedicated components. After this plan, AdsTable has six consumers and the six Ads* files are gone.

Output: Six updated page files + zero Ads* component files remaining.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-consolidation/04-01-SUMMARY.md

<interfaces>
<!-- AdsTable.vue props — extracted from apps/dashboard/app/components/AdsTable.vue -->
```typescript
interface Props {
  endpoint: string;       // Strapi endpoint, e.g. "ads/pendings"
  section: SettingsSection; // Settings store key, e.g. "adsPendings"
  emptyMessage?: string;  // Default: "No se encontraron anuncios"
  showWebLink?: boolean;  // Default: false — shows ExternalLink icon in activos only
}
```

<!-- Page-to-AdsTable mapping (from VERIFICATION.md) -->
| Page file            | endpoint           | section        | showWebLink |
|----------------------|--------------------|----------------|-------------|
| pendientes.vue       | ads/pendings       | adsPendings    | (omit)      |
| activos.vue          | ads/actives        | adsActives     | :show-web-link="true" |
| abandonados.vue      | ads/abandoneds     | adsAbandoned   | (omit)      |
| baneados.vue         | ads/banneds        | adsBanned      | (omit)      |
| rechazados.vue       | ads/rejecteds      | adsRejected    | (omit)      |
| expirados.vue        | ads/archiveds      | adsArchived    | (omit)      |

<!-- Current page structure (identical for all six) -->
```vue
<template>
  <div>
    <HeroDefault title="Pendientes" :breadcrumbs="breadcrumbs" />
    <AdsPendings />   <!-- Replace this line with <AdsTable ...> -->
  </div>
</template>

<script setup lang="ts">
import HeroDefault from "@/components/HeroDefault.vue";
import AdsPendings from "@/components/AdsPendings.vue";  <!-- Replace this import -->

definePageMeta({ layout: "dashboard" });

const breadcrumbs = [...];  <!-- Keep this unchanged -->
</script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate six ads pages to AdsTable</name>
  <files>
    apps/dashboard/app/pages/anuncios/pendientes.vue,
    apps/dashboard/app/pages/anuncios/activos.vue,
    apps/dashboard/app/pages/anuncios/abandonados.vue,
    apps/dashboard/app/pages/anuncios/baneados.vue,
    apps/dashboard/app/pages/anuncios/rechazados.vue,
    apps/dashboard/app/pages/anuncios/expirados.vue
  </files>
  <action>
For each of the six page files, make exactly two changes:
1. Replace the old Ads* component import with: `import AdsTable from "@/components/AdsTable.vue";`
2. Replace `<AdsPendings />` (or the equivalent old component tag) with the correct `<AdsTable>` call.

The `<HeroDefault>`, `definePageMeta`, and `breadcrumbs` constant must remain unchanged.

Apply these exact replacements:

**pendientes.vue**
- Remove: `import AdsPendings from "@/components/AdsPendings.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsPendings />`
- Add: `<AdsTable endpoint="ads/pendings" section="adsPendings" />`

**activos.vue**
- Remove: `import AdsActives from "@/components/AdsActives.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsActives />`
- Add: `<AdsTable endpoint="ads/actives" section="adsActives" :show-web-link="true" />`

**abandonados.vue**
- Remove: `import AdsAbandoned from "@/components/AdsAbandoned.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsAbandoned />`
- Add: `<AdsTable endpoint="ads/abandoneds" section="adsAbandoned" />`

**baneados.vue**
- Remove: `import AdsBanned from "@/components/AdsBanned.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsBanned />`
- Add: `<AdsTable endpoint="ads/banneds" section="adsBanned" />`

**rechazados.vue**
- Remove: `import AdsRejected from "@/components/AdsRejected.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsRejected />`
- Add: `<AdsTable endpoint="ads/rejecteds" section="adsRejected" />`

**expirados.vue**
- Remove: `import AdsArchived from "@/components/AdsArchived.vue";`
- Add: `import AdsTable from "@/components/AdsTable.vue";`
- Remove: `<AdsArchived />`
- Add: `<AdsTable endpoint="ads/archiveds" section="adsArchived" />`
  </action>
  <verify>
    <automated>grep -rn "AdsPendings\|AdsActives\|AdsAbandoned\|AdsBanned\|AdsRejected\|AdsArchived" /home/gabriel/Code/waldo-project/apps/dashboard/app/pages/anuncios/ | grep -v "//.*import" | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: no old Ads* imports in pages" || echo "FAIL: old Ads* still present in pages"</automated>
  </verify>
  <done>All six page files import AdsTable (not their old dedicated component); each page renders the correct AdsTable with endpoint and section props; activos.vue includes :show-web-link="true".</done>
</task>

<task type="auto">
  <name>Task 2: Delete six original Ads* component files</name>
  <files>
    apps/dashboard/app/components/AdsAbandoned.vue,
    apps/dashboard/app/components/AdsActives.vue,
    apps/dashboard/app/components/AdsArchived.vue,
    apps/dashboard/app/components/AdsBanned.vue,
    apps/dashboard/app/components/AdsPendings.vue,
    apps/dashboard/app/components/AdsRejected.vue
  </files>
  <action>
Delete all six original Ads* component files. These files are now replaced entirely by AdsTable.vue.

Files to delete:
- apps/dashboard/app/components/AdsAbandoned.vue
- apps/dashboard/app/components/AdsActives.vue
- apps/dashboard/app/components/AdsArchived.vue
- apps/dashboard/app/components/AdsBanned.vue
- apps/dashboard/app/components/AdsPendings.vue
- apps/dashboard/app/components/AdsRejected.vue

Use the Bash tool to run: `rm` on each file, or delete them one by one using the file system. Do NOT delete AdsTable.vue.

After deletion, verify no remaining file in apps/dashboard/app/ still imports from these deleted paths (the grep in Task 1 already confirmed pages are migrated; this is a final safety check).
  </action>
  <verify>
    <automated>ls /home/gabriel/Code/waldo-project/apps/dashboard/app/components/Ads*.vue 2>/dev/null | grep -v AdsTable.vue | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: only AdsTable.vue remains" || echo "FAIL: old Ads* files still exist"</automated>
  </verify>
  <done>The six files (AdsAbandoned.vue, AdsActives.vue, AdsArchived.vue, AdsBanned.vue, AdsPendings.vue, AdsRejected.vue) no longer exist in apps/dashboard/app/components/. AdsTable.vue is the only Ads*.vue component remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify operator workflows via AdsTable on all six pages</name>
  <what-built>All six ads list pages now render via AdsTable.vue. The six dedicated Ads* components are deleted. This checkpoint confirms behavioral parity before the phase closes.</what-built>
  <how-to-verify>
Start the dashboard dev server (`cd apps/dashboard && npm run dev` or equivalent).

For each of the six ads views, open it in the browser and verify:

1. **Pendientes** (`/anuncios/pendientes`): Page loads, table shows pending ads.
2. **Activos** (`/anuncios/activos`): Page loads, table shows active ads. Confirm the external-link icon appears on rows that have a slug, and clicking it opens `${websiteUrl}/anuncios/${slug}` in a new tab.
3. **Abandonados** (`/anuncios/abandonados`): Page loads, table shows abandoned ads.
4. **Baneados** (`/anuncios/baneados`): Page loads, table shows banned ads.
5. **Rechazados** (`/anuncios/rechazados`): Page loads, table shows rejected ads.
6. **Expirados** (`/anuncios/expirados`): Page loads, table shows expired ads.

Cross-section isolation check: Change the page or filter in Pendientes. Navigate to Activos. Confirm Activos shows its own independent pagination state (page and filter are NOT affected by what you did in Pendientes).
  </how-to-verify>
  <resume-signal>Type "approved" if all six pages work correctly, or describe any issue found.</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "AdsPendings\|AdsActives\|AdsAbandoned\|AdsBanned\|AdsRejected\|AdsArchived" apps/dashboard/app/pages/` returns no results
- `ls apps/dashboard/app/components/Ads*.vue` returns only `AdsTable.vue`
- Each page file contains the correct `endpoint` and `section` values per the mapping table
- `activos.vue` contains `:show-web-link="true"`
</verification>

<success_criteria>
All six ads list pages render via AdsTable. The six original Ads* files are deleted. Human verification confirms operator workflows function identically.
</success_criteria>

<output>
After completion, create .planning/phases/04-component-consolidation/04-02-SUMMARY.md
</output>
