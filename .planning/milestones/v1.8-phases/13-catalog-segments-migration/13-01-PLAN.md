---
phase: 13-catalog-segments-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/pages/categories/index.vue
  - apps/dashboard/app/pages/categories/new.vue
  - apps/dashboard/app/pages/categories/[id]/index.vue
  - apps/dashboard/app/pages/categories/[id]/edit.vue
  - apps/dashboard/app/pages/regions/index.vue
  - apps/dashboard/app/pages/regions/new.vue
  - apps/dashboard/app/pages/regions/[id]/index.vue
  - apps/dashboard/app/pages/regions/[id]/edit.vue
autonomous: true
requirements:
  - URL-03
  - URL-09

must_haves:
  truths:
    - "Navigating to /categories loads the categories list page"
    - "Navigating to /categories/new loads the new category form"
    - "Navigating to /categories/[id] loads the category detail page"
    - "Navigating to /categories/[id]/edit loads the category edit form"
    - "Navigating to /regions loads the regions list page"
    - "Navigating to /regions/new loads the new region form"
    - "Navigating to /regions/[id] loads the region detail page"
    - "Navigating to /regions/[id]/edit loads the region edit form"
    - "No file in apps/dashboard/app/pages/categories/ contains a /categorias/ route reference"
    - "No file in apps/dashboard/app/pages/regions/ contains a /regiones/ route reference"
  artifacts:
    - path: "apps/dashboard/app/pages/categories/index.vue"
      provides: "Categories list page at /categories"
    - path: "apps/dashboard/app/pages/categories/new.vue"
      provides: "New category form at /categories/new"
    - path: "apps/dashboard/app/pages/categories/[id]/index.vue"
      provides: "Category detail page at /categories/[id]"
    - path: "apps/dashboard/app/pages/categories/[id]/edit.vue"
      provides: "Category edit form at /categories/[id]/edit"
    - path: "apps/dashboard/app/pages/regions/index.vue"
      provides: "Regions list page at /regions"
    - path: "apps/dashboard/app/pages/regions/new.vue"
      provides: "New region form at /regions/new"
    - path: "apps/dashboard/app/pages/regions/[id]/index.vue"
      provides: "Region detail page at /regions/[id]"
    - path: "apps/dashboard/app/pages/regions/[id]/edit.vue"
      provides: "Region edit form at /regions/[id]/edit"
  key_links:
    - from: "apps/dashboard/app/pages/categories/[id]/index.vue"
      to: "/categories/[id]/edit"
      via: "NuxtLink :to binding in template"
      pattern: "categorias.*editar|categories.*edit"
    - from: "apps/dashboard/app/pages/regions/[id]/index.vue"
      to: "/regions/[id]/edit"
      via: "NuxtLink :to binding in template"
      pattern: "regiones.*editar|regions.*edit"
---

<objective>
Rename the `categorias` and `regiones` page directories to `categories` and `regions` respectively. Translate the `editar.vue` sub-page filename to `edit.vue` in each. Update every internal route reference within those files from `/categorias/...` → `/categories/...` and `/regiones/...` → `/regions/...`.

Purpose: Fulfils URL-03 and URL-09 — the `/categories` and `/regions` route trees become canonical English paths.
Output: New `apps/dashboard/app/pages/categories/` and `apps/dashboard/app/pages/regions/` directories with correctly named and correctly wired `.vue` files. The old `categorias/` and `regiones/` directories are deleted.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ads-migration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename categorias and regiones directories and files</name>
  <files>
    apps/dashboard/app/pages/categories/index.vue
    apps/dashboard/app/pages/categories/new.vue
    apps/dashboard/app/pages/categories/[id]/index.vue
    apps/dashboard/app/pages/categories/[id]/edit.vue
    apps/dashboard/app/pages/regions/index.vue
    apps/dashboard/app/pages/regions/new.vue
    apps/dashboard/app/pages/regions/[id]/index.vue
    apps/dashboard/app/pages/regions/[id]/edit.vue
  </files>
  <action>
    Use `git mv` to rename so Git tracks renames cleanly (same pattern as Phase 12 ads migration):

    ```bash
    # Categories
    git mv apps/dashboard/app/pages/categorias apps/dashboard/app/pages/categories
    git mv "apps/dashboard/app/pages/categories/[id]/editar.vue" "apps/dashboard/app/pages/categories/[id]/edit.vue"

    # Regions
    git mv apps/dashboard/app/pages/regiones apps/dashboard/app/pages/regions
    git mv "apps/dashboard/app/pages/regions/[id]/editar.vue" "apps/dashboard/app/pages/regions/[id]/edit.vue"
    ```

    The `index.vue`, `new.vue`, and `[id]/index.vue` filenames are already correct — they move with the directory.

    After the renames, verify:
    ```
    apps/dashboard/app/pages/categories/
      index.vue
      new.vue
      [id]/
        index.vue
        edit.vue

    apps/dashboard/app/pages/regions/
      index.vue
      new.vue
      [id]/
        index.vue
        edit.vue
    ```
  </action>
  <verify>ls "apps/dashboard/app/pages/categories/[id]/" shows edit.vue (not editar.vue); ls "apps/dashboard/app/pages/regions/[id]/" shows edit.vue (not editar.vue); neither apps/dashboard/app/pages/categorias/ nor apps/dashboard/app/pages/regiones/ exists</verify>
  <done>All 8 files are in categories/ and regions/ with correct English names. Old categorias/ and regiones/ directories are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update internal route references in categories and regions pages</name>
  <files>
    apps/dashboard/app/pages/categories/index.vue
    apps/dashboard/app/pages/categories/new.vue
    apps/dashboard/app/pages/categories/[id]/index.vue
    apps/dashboard/app/pages/categories/[id]/edit.vue
    apps/dashboard/app/pages/regions/index.vue
    apps/dashboard/app/pages/regions/new.vue
    apps/dashboard/app/pages/regions/[id]/index.vue
    apps/dashboard/app/pages/regions/[id]/edit.vue
  </files>
  <action>
    Update every Spanish route string inside the 8 renamed files to its English equivalent.

    **categories/index.vue** — update NuxtLink:
    - `to="/categorias/new"` → `to="/categories/new"`

    **categories/new.vue** — update breadcrumb:
    - `{ label: "Categorías", to: "/categorias" }` → `{ label: "Categories", to: "/categories" }`

    **categories/[id]/index.vue** — update NuxtLink and breadcrumb:
    - `:to="\`/categorias/${route.params.id}/editar\`"` → `:to="\`/categories/${route.params.id}/edit\`"`
    - `{ label: "Categorías", to: "/categorias" }` → `{ label: "Categories", to: "/categories" }`

    **categories/[id]/edit.vue** — update breadcrumbs:
    - `{ label: "Categorías", to: "/categorias" }` → `{ label: "Categories", to: "/categories" }`
    - `{ label: category.value.name, to: \`/categorias/${route.params.id}\` }` → `{ label: category.value.name, to: \`/categories/${route.params.id}\` }`

    **regions/index.vue** — update NuxtLink:
    - `to="/regiones/new"` → `to="/regions/new"`

    **regions/new.vue** — update breadcrumb:
    - `{ label: "Regiones", to: "/regiones" }` → `{ label: "Regions", to: "/regions" }`

    **regions/[id]/index.vue** — update NuxtLink and breadcrumb:
    - `:to="\`/regiones/${route.params.id}/editar\`"` → `:to="\`/regions/${route.params.id}/edit\`"`
    - `{ label: "Regiones", to: "/regiones" }` → `{ label: "Regions", to: "/regions" }`

    **regions/[id]/edit.vue** — update breadcrumbs:
    - `{ label: "Regiones", to: "/regiones" }` → `{ label: "Regions", to: "/regions" }`
    - `{ label: region.value.name, to: \`/regiones/${route.params.id}\` }` → `{ label: region.value.name, to: \`/regions/${route.params.id}\` }`

    Do NOT change API call strings (e.g., `strapi.find("categories", ...)` — these are already English Strapi collection names), Spanish UI labels, or anything outside these breadcrumb/route path strings.

    After edits, confirm no Spanish route strings remain:
    ```bash
    grep -r "/categorias/" apps/dashboard/app/pages/categories/
    grep -r "/regiones/" apps/dashboard/app/pages/regions/
    grep -r "editar" apps/dashboard/app/pages/categories/ apps/dashboard/app/pages/regions/
    ```
    Expected: no output from any command.
  </action>
  <verify>
    <automated>grep -r "/categorias/" apps/dashboard/app/pages/categories/ && echo "FAIL" || echo "PASS categories"; grep -r "/regiones/" apps/dashboard/app/pages/regions/ && echo "FAIL" || echo "PASS regions"</automated>
  </verify>
  <done>
    - All 8 files reference /categories/... and /regions/... routes only.
    - No /categorias/ or /regiones/ string present in their respective directories.
    - No /editar route references remain (replaced with /edit).
    - categories/[id]/index.vue NuxtLink points to /categories/[id]/edit.
    - regions/[id]/index.vue NuxtLink points to /regions/[id]/edit.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls apps/dashboard/app/pages/categories/` — shows index.vue, new.vue, [id]/
2. `ls "apps/dashboard/app/pages/categories/[id]/"` — shows index.vue, edit.vue
3. `ls apps/dashboard/app/pages/regions/` — shows index.vue, new.vue, [id]/
4. `ls "apps/dashboard/app/pages/regions/[id]/"` — shows index.vue, edit.vue
5. `ls apps/dashboard/app/pages/categorias/` — returns "No such file or directory"
6. `ls apps/dashboard/app/pages/regiones/` — returns "No such file or directory"
7. `grep -r "/categorias/" apps/dashboard/app/pages/categories/` — no output
8. `grep -r "/regiones/" apps/dashboard/app/pages/regions/` — no output
9. `git status` — shows renames tracked by Git (not deletions + additions)
</verification>

<success_criteria>
- `apps/dashboard/app/pages/categories/` exists with index.vue, new.vue, [id]/index.vue, [id]/edit.vue
- `apps/dashboard/app/pages/regions/` exists with index.vue, new.vue, [id]/index.vue, [id]/edit.vue
- `apps/dashboard/app/pages/categorias/` does not exist
- `apps/dashboard/app/pages/regiones/` does not exist
- Zero `/categorias/` or `/regiones/` route strings inside the renamed directories
- Zero `editar` route path strings inside the renamed directories
- Git history shows renames (not file deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-catalog-segments-migration/13-01-SUMMARY.md` using the summary template.
</output>
