---
phase: 13-catalog-segments-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/pages/communes/index.vue
  - apps/dashboard/app/pages/communes/new.vue
  - apps/dashboard/app/pages/communes/[id]/index.vue
  - apps/dashboard/app/pages/communes/[id]/edit.vue
  - apps/dashboard/app/pages/conditions/index.vue
  - apps/dashboard/app/pages/conditions/new.vue
  - apps/dashboard/app/pages/conditions/[id]/index.vue
  - apps/dashboard/app/pages/conditions/[id]/edit.vue
autonomous: true
requirements:
  - URL-04
  - URL-05

must_haves:
  truths:
    - "Navigating to /communes loads the communes list page"
    - "Navigating to /communes/new loads the new commune form"
    - "Navigating to /communes/[id] loads the commune detail page"
    - "Navigating to /communes/[id]/edit loads the commune edit form"
    - "Navigating to /conditions loads the conditions list page"
    - "Navigating to /conditions/new loads the new condition form"
    - "Navigating to /conditions/[id] loads the condition detail page"
    - "Navigating to /conditions/[id]/edit loads the condition edit form"
    - "No file in apps/dashboard/app/pages/communes/ contains a /comunas/ route reference"
    - "No file in apps/dashboard/app/pages/conditions/ contains a /condiciones/ route reference"
  artifacts:
    - path: "apps/dashboard/app/pages/communes/index.vue"
      provides: "Communes list page at /communes"
    - path: "apps/dashboard/app/pages/communes/new.vue"
      provides: "New commune form at /communes/new"
    - path: "apps/dashboard/app/pages/communes/[id]/index.vue"
      provides: "Commune detail page at /communes/[id]"
    - path: "apps/dashboard/app/pages/communes/[id]/edit.vue"
      provides: "Commune edit form at /communes/[id]/edit"
    - path: "apps/dashboard/app/pages/conditions/index.vue"
      provides: "Conditions list page at /conditions"
    - path: "apps/dashboard/app/pages/conditions/new.vue"
      provides: "New condition form at /conditions/new"
    - path: "apps/dashboard/app/pages/conditions/[id]/index.vue"
      provides: "Condition detail page at /conditions/[id]"
    - path: "apps/dashboard/app/pages/conditions/[id]/edit.vue"
      provides: "Condition edit form at /conditions/[id]/edit"
  key_links:
    - from: "apps/dashboard/app/pages/communes/[id]/index.vue"
      to: "/communes/[id]/edit"
      via: "NuxtLink :to binding in template"
      pattern: "comunas.*editar|communes.*edit"
    - from: "apps/dashboard/app/pages/conditions/[id]/index.vue"
      to: "/conditions/[id]/edit"
      via: "NuxtLink :to binding in template"
      pattern: "condiciones.*editar|conditions.*edit"
---

<objective>
Rename the `comunas` and `condiciones` page directories to `communes` and `conditions` respectively. Translate the `editar.vue` sub-page filename to `edit.vue` in each. Update every internal route reference within those files from `/comunas/...` → `/communes/...` and `/condiciones/...` → `/conditions/...`.

Purpose: Fulfils URL-04 and URL-05 — the `/communes` and `/conditions` route trees become canonical English paths.
Output: New `apps/dashboard/app/pages/communes/` and `apps/dashboard/app/pages/conditions/` directories with correctly named and correctly wired `.vue` files. The old `comunas/` and `condiciones/` directories are deleted.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ads-migration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename comunas and condiciones directories and files</name>
  <files>
    apps/dashboard/app/pages/communes/index.vue
    apps/dashboard/app/pages/communes/new.vue
    apps/dashboard/app/pages/communes/[id]/index.vue
    apps/dashboard/app/pages/communes/[id]/edit.vue
    apps/dashboard/app/pages/conditions/index.vue
    apps/dashboard/app/pages/conditions/new.vue
    apps/dashboard/app/pages/conditions/[id]/index.vue
    apps/dashboard/app/pages/conditions/[id]/edit.vue
  </files>
  <action>
    Use `git mv` to rename so Git tracks renames cleanly (same pattern as Phase 12 ads migration):

    ```bash
    # Communes
    git mv apps/dashboard/app/pages/comunas apps/dashboard/app/pages/communes
    git mv "apps/dashboard/app/pages/communes/[id]/editar.vue" "apps/dashboard/app/pages/communes/[id]/edit.vue"

    # Conditions
    git mv apps/dashboard/app/pages/condiciones apps/dashboard/app/pages/conditions
    git mv "apps/dashboard/app/pages/conditions/[id]/editar.vue" "apps/dashboard/app/pages/conditions/[id]/edit.vue"
    ```

    The `index.vue`, `new.vue`, and `[id]/index.vue` filenames are already correct — they move with the directory.

    After the renames, verify:
    ```
    apps/dashboard/app/pages/communes/
      index.vue
      new.vue
      [id]/
        index.vue
        edit.vue

    apps/dashboard/app/pages/conditions/
      index.vue
      new.vue
      [id]/
        index.vue
        edit.vue
    ```
  </action>
  <verify>ls "apps/dashboard/app/pages/communes/[id]/" shows edit.vue (not editar.vue); ls "apps/dashboard/app/pages/conditions/[id]/" shows edit.vue (not editar.vue); neither apps/dashboard/app/pages/comunas/ nor apps/dashboard/app/pages/condiciones/ exists</verify>
  <done>All 8 files are in communes/ and conditions/ with correct English names. Old comunas/ and condiciones/ directories are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update internal route references in communes and conditions pages</name>
  <files>
    apps/dashboard/app/pages/communes/index.vue
    apps/dashboard/app/pages/communes/new.vue
    apps/dashboard/app/pages/communes/[id]/index.vue
    apps/dashboard/app/pages/communes/[id]/edit.vue
    apps/dashboard/app/pages/conditions/index.vue
    apps/dashboard/app/pages/conditions/new.vue
    apps/dashboard/app/pages/conditions/[id]/index.vue
    apps/dashboard/app/pages/conditions/[id]/edit.vue
  </files>
  <action>
    Update every Spanish route string inside the 8 renamed files to its English equivalent.

    **communes/index.vue** — update NuxtLink:
    - `to="/comunas/new"` → `to="/communes/new"`

    **communes/new.vue** — update breadcrumb:
    - `{ label: "Comunas", to: "/comunas" }` → `{ label: "Communes", to: "/communes" }`

    **communes/[id]/index.vue** — update NuxtLink and breadcrumb:
    - `:to="\`/comunas/${route.params.id}/editar\`"` → `:to="\`/communes/${route.params.id}/edit\`"`
    - `{ label: "Comunas", to: "/comunas" }` → `{ label: "Communes", to: "/communes" }`

    **communes/[id]/edit.vue** — update breadcrumbs:
    - `{ label: "Comunas", to: "/comunas" }` → `{ label: "Communes", to: "/communes" }`
    - `{ label: commune.value.name, to: \`/comunas/${route.params.id}\` }` → `{ label: commune.value.name, to: \`/communes/${route.params.id}\` }`

    **conditions/index.vue** — update NuxtLink:
    - `to="/condiciones/new"` → `to="/conditions/new"`

    **conditions/new.vue** — update breadcrumb:
    - `{ label: "Condiciones", to: "/condiciones" }` → `{ label: "Conditions", to: "/conditions" }`

    **conditions/[id]/index.vue** — update NuxtLink and breadcrumb:
    - `:to="\`/condiciones/${route.params.id}/editar\`"` → `:to="\`/conditions/${route.params.id}/edit\`"`
    - `{ label: "Condiciones", to: "/condiciones" }` → `{ label: "Conditions", to: "/conditions" }`

    **conditions/[id]/edit.vue** — update breadcrumbs:
    - `{ label: "Condiciones", to: "/condiciones" }` → `{ label: "Conditions", to: "/conditions" }`
    - `{ label: condition.value.name, to: \`/condiciones/${route.params.id}\` }` → `{ label: condition.value.name, to: \`/conditions/${route.params.id}\` }`

    Do NOT change API call strings (Strapi collection names like "communes", "conditions" are already English), Spanish UI labels, or anything outside breadcrumb/route path strings.

    After edits, confirm no Spanish route strings remain:
    ```bash
    grep -r "/comunas/" apps/dashboard/app/pages/communes/
    grep -r "/condiciones/" apps/dashboard/app/pages/conditions/
    grep -r "editar" apps/dashboard/app/pages/communes/ apps/dashboard/app/pages/conditions/
    ```
    Expected: no output from any command.
  </action>
  <verify>
    <automated>grep -r "/comunas/" apps/dashboard/app/pages/communes/ && echo "FAIL" || echo "PASS communes"; grep -r "/condiciones/" apps/dashboard/app/pages/conditions/ && echo "FAIL" || echo "PASS conditions"</automated>
  </verify>
  <done>
    - All 8 files reference /communes/... and /conditions/... routes only.
    - No /comunas/ or /condiciones/ string present in their respective directories.
    - No /editar route references remain (replaced with /edit).
    - communes/[id]/index.vue NuxtLink points to /communes/[id]/edit.
    - conditions/[id]/index.vue NuxtLink points to /conditions/[id]/edit.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls apps/dashboard/app/pages/communes/` — shows index.vue, new.vue, [id]/
2. `ls "apps/dashboard/app/pages/communes/[id]/"` — shows index.vue, edit.vue
3. `ls apps/dashboard/app/pages/conditions/` — shows index.vue, new.vue, [id]/
4. `ls "apps/dashboard/app/pages/conditions/[id]/"` — shows index.vue, edit.vue
5. `ls apps/dashboard/app/pages/comunas/` — returns "No such file or directory"
6. `ls apps/dashboard/app/pages/condiciones/` — returns "No such file or directory"
7. `grep -r "/comunas/" apps/dashboard/app/pages/communes/` — no output
8. `grep -r "/condiciones/" apps/dashboard/app/pages/conditions/` — no output
9. `git status` — shows renames tracked by Git (not deletions + additions)
</verification>

<success_criteria>
- `apps/dashboard/app/pages/communes/` exists with index.vue, new.vue, [id]/index.vue, [id]/edit.vue
- `apps/dashboard/app/pages/conditions/` exists with index.vue, new.vue, [id]/index.vue, [id]/edit.vue
- `apps/dashboard/app/pages/comunas/` does not exist
- `apps/dashboard/app/pages/condiciones/` does not exist
- Zero `/comunas/` or `/condiciones/` route strings inside the renamed directories
- Zero `editar` route path strings inside the renamed directories
- Git history shows renames (not file deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-catalog-segments-migration/13-02-SUMMARY.md` using the summary template.
</output>
