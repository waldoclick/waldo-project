---
phase: 42-ad-creation-url-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/anunciar/resumen.vue
  - apps/website/app/components/FormCreateThree.vue
autonomous: true
requirements:
  - ROUTE-05
  - QUAL-02

must_haves:
  truths:
    - "Clicking Back on resumen.vue navigates to /anunciar/galeria-de-imagenes (not /anunciar?step=5)"
    - "FormCreateThree.vue no longer renders raw user data in the DOM"
  artifacts:
    - path: "apps/website/app/pages/anunciar/resumen.vue"
      provides: "Fixed back button navigation"
      contains: "galeria-de-imagenes"
    - path: "apps/website/app/components/FormCreateThree.vue"
      provides: "Debug leak removed"
  key_links:
    - from: "resumen.vue BarAnnouncement @back"
      to: "router.push('/anunciar/galeria-de-imagenes')"
      via: "inline handler"
      pattern: "galeria-de-imagenes"
---

<objective>
Fix two isolated issues in existing files: the `resumen.vue` back button hardcoded to the old `?step=5` URL, and the `FormCreateThree.vue` debug `<pre>` tag leaking raw user data into the rendered HTML.

Purpose: These are standalone fixes that can run in parallel with Plan 01 (Wave 1). No dependencies on the new step pages.

Output: `resumen.vue` back button uses new route; `FormCreateThree.vue` debug output removed.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/website/app/pages/anunciar/resumen.vue
@apps/website/app/components/FormCreateThree.vue
</context>

<interfaces>
<!-- Exact lines to change. No exploration needed. -->

In resumen.vue, line 20 (current):
```html
@back="router.push('/anunciar?step=5')"
```
Change to:
```html
@back="router.push('/anunciar/galeria-de-imagenes')"
```

In FormCreateThree.vue, line 136 (current — debug leak):
```html
    <pre>{{ user.value }}</pre>
```
Remove this line entirely. (Line 2 already has a commented-out `<!-- <pre>{{ user }}</pre> -->` — leave that commented line alone.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix resumen.vue back button</name>
  <files>apps/website/app/pages/anunciar/resumen.vue</files>
  <action>
In `apps/website/app/pages/anunciar/resumen.vue`, find line 20:

```html
@back="router.push('/anunciar?step=5')"
```

Replace with:

```html
@back="router.push('/anunciar/galeria-de-imagenes')"
```

This is a one-line change. No other modifications to this file.
  </action>
  <verify>
    <automated>grep "galeria-de-imagenes" apps/website/app/pages/anunciar/resumen.vue && ! grep "step=5" apps/website/app/pages/anunciar/resumen.vue</automated>
  </verify>
  <done>`resumen.vue` references `/anunciar/galeria-de-imagenes` for back navigation; `?step=5` is gone</done>
</task>

<task type="auto">
  <name>Task 2: Remove FormCreateThree.vue debug leak</name>
  <files>apps/website/app/components/FormCreateThree.vue</files>
  <action>
In `apps/website/app/components/FormCreateThree.vue`, remove line 136:

```html
    <pre>{{ user.value }}</pre>
```

This line leaks raw user PII into rendered HTML. Delete only this line. Do not touch line 2 which contains the already-commented `<!-- <pre>{{ user }}</pre> -->`.
  </action>
  <verify>
    <automated>! grep -n "user\.value" apps/website/app/components/FormCreateThree.vue | grep "<pre>"</automated>
  </verify>
  <done>No `<pre>{{ user.value }}</pre>` remains in FormCreateThree.vue; the commented-out debug line on line 2 is untouched</done>
</task>

</tasks>

<verification>
- `grep -r "step=5" apps/website/app/pages/` returns no matches
- `grep "user\.value" apps/website/app/components/FormCreateThree.vue` returns no `<pre>` tags
</verification>

<success_criteria>
- `resumen.vue` back button navigates to `/anunciar/galeria-de-imagenes`
- `FormCreateThree.vue` contains no live `<pre>` debug output
- No other changes to either file
</success_criteria>

<output>
After completion, create `.planning/phases/42-ad-creation-url-refactor/42-02-SUMMARY.md`
</output>
