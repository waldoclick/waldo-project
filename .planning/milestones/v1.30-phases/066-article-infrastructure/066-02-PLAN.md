---
phase: 066-article-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/scss/components/_article.scss
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_filter.scss
  - apps/website/app/scss/components/_related.scss
  - apps/website/app/scss/components/_card.scss
  - apps/website/app/scss/app.scss
autonomous: true
requirements: [BLOG-21, BLOG-22, BLOG-23, BLOG-24, BLOG-25, BLOG-26]

must_haves:
  truths:
    - "_article.scss exists with article--archive and article--single BEM blocks"
    - "_hero.scss has hero--articles and hero--article modifier blocks added (existing blocks preserved)"
    - "_filter.scss has filter--articles block added (existing filter--announcement preserved)"
    - "_related.scss has related--articles block added (existing related--ads preserved)"
    - "_card.scss has card--article block added (existing blocks preserved)"
    - "app.scss includes @use 'components/article' and website builds without SCSS errors"
  artifacts:
    - path: "apps/website/app/scss/components/_article.scss"
      provides: "BEM scaffold for article listing and detail page layouts"
      contains: "article--archive, article--single"
    - path: "apps/website/app/scss/components/_hero.scss"
      provides: "Extended hero with blog-specific modifier blocks"
      contains: "hero--articles, hero--article"
    - path: "apps/website/app/scss/components/_filter.scss"
      provides: "Extended filter with blog filter block"
      contains: "filter--articles"
    - path: "apps/website/app/scss/components/_related.scss"
      provides: "Extended related with articles block"
      contains: "related--articles"
    - path: "apps/website/app/scss/components/_card.scss"
      provides: "Extended card with article card block"
      contains: "card--article"
    - path: "apps/website/app/scss/app.scss"
      provides: "App stylesheet with article component imported"
      contains: "@use \"components/article\""
  key_links:
    - from: "apps/website/app/scss/app.scss"
      to: "apps/website/app/scss/components/_article.scss"
      via: "@use \"components/article\""
      pattern: "@use.*components/article"
---

<objective>
Create the complete SCSS scaffolding for all blog components — one new file plus targeted additions to four existing files — so that Phase 067 and 068 components can use `.article--archive`, `.article--single`, `.hero--articles`, `.hero--article`, `.filter--articles`, `.related--articles`, and `.card--article` classes without needing to add any SCSS themselves.

Purpose: Separating SCSS infrastructure from component implementation prevents blocking (Phase 067 can use classes immediately) and keeps component files clean.
Output: 5 SCSS files modified + 1 created + `app.scss` updated. All blocks are structural scaffolds with the layout properties that components will rely on — not full visual designs.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Reference files — read these to understand existing patterns before editing -->
@apps/website/app/scss/app.scss
@apps/website/app/scss/components/_announcement.scss
@apps/website/app/scss/components/_hero.scss
@apps/website/app/scss/components/_filter.scss
@apps/website/app/scss/components/_related.scss
@apps/website/app/scss/components/_card.scss
@apps/website/app/scss/abstracts/_variables.scss
@apps/website/app/scss/abstracts/_mixins.scss
</context>

<interfaces>
<!-- BEM rules from AGENTS.md — mandatory for all SCSS work -->
<!--
  - A block modifier (block--modifier) is its OWN namespace
  - All children prefixed with the modifier: article--archive__list, article--archive__filter
  - Never use hyphenated words as BEM element names — single nouns only
    - Correct: article--archive__list, article--single__body, article--single__sidebar
    - Wrong: article--archive__article-list, article--single__body-text
  - Do not add box-shadow or transform: scale
  - No scale transforms on hover
  - SCSS nesting must mirror HTML hierarchy exactly
-->

<!-- Existing reference — announcement--archive is the direct mirror for article--archive -->
<!-- _announcement.scss excerpt:
.announcement {
  &--archive {
    &__filter { margin-bottom: 30px; }
    &__list { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 20px; ... }
    &__paginate { margin-top: 40px; }
  }
  &--single {
    &__container { @extend .container; display: flex; gap: 30px; ... }
    &__body { flex: 1; ... }
    &__body__description { ... }
    &__sidebar { width: 350px; flex-shrink: 0; ... }
    &__sidebar__share { ... }
  }
}
-->

<!-- Existing related--ads is the direct mirror for related--articles -->
<!-- _related.scss:
.related {
  &--ads { background-color: rgba(240,240,240,0.3); }
}
-->

<!-- card--announcement is the reference for card--article styling approach -->
<!-- _hero.scss existing modifiers: --fake, --profile, --home, --results, --announcement, --default, --categories, --ad -->
<!-- _filter.scss existing modifiers: --announcement -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create _article.scss with archive and single BEM blocks</name>
  <files>apps/website/app/scss/components/_article.scss</files>
  <action>
Create `apps/website/app/scss/components/_article.scss` as a NEW file. Mirror the structure of `_announcement.scss` exactly.

File header — same imports as all other component files:
```scss
@use "../abstracts/mixins" as *;
@use "../abstracts/variables" as *;
@use "../base/container";
```

`.article` block with two modifier blocks:

**`article--archive`** (mirrors `announcement--archive`):
- `&__filter` — `margin-bottom: 30px;`
- `&__list` — `display: grid; width: 100%; gap: 20px;` with 4-column default → 3-col at screen-large → 2-col at screen-medium → 1-col at screen-small
- `&__paginate` — `margin-top: 40px;`

**`article--single`** (mirrors `announcement--single` but WITHOUT seller/price sidebar sections — article sidebar has categories list + share only):
- `&__container` — `@extend .container; display: flex; gap: 30px;` with `flex-direction: column-reverse` at screen-large
- `&__body` — `flex: 1; display: flex; flex-direction: column; gap: 30px;`
- `&__body__description` — `display: flex; flex-direction: column; gap: 10px;`
- `&__body__description__text` — `font-size: 16px; line-height: 26px; letter-spacing: 0.25px; color: $charcoal;`
- `&__sidebar` — `width: 350px; flex-shrink: 0; display: flex; flex-direction: column; gap: 30px;` → at screen-large: `flex-direction: column; width: auto;`
- `&__sidebar__categories` — `padding: 20px; border-radius: 4px; border: 1px solid $gainsboro;`
- `&__sidebar__share` — `padding: 20px; border-radius: 4px; border: 1px solid $gainsboro;`

BEM rules: all children prefixed with `article--archive__` or `article--single__`. Use only single-noun element names (no hyphens in element names). SCSS nesting mirrors HTML hierarchy.
  </action>
  <verify>
    <automated>grep -c "article--archive\|article--single" apps/website/app/scss/components/_article.scss</automated>
  </verify>
  <done>
- `_article.scss` exists with `.article { &--archive { ... } &--single { ... } }` BEM structure
- Both modifier blocks contain at minimum: filter, list, paginate (archive) and container, body, sidebar (single)
- No hyphenated BEM element names
- File compiles without SCSS errors (verified in Task 3)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend _hero, _filter, _related, _card with blog modifier blocks</name>
  <files>
    apps/website/app/scss/components/_hero.scss
    apps/website/app/scss/components/_filter.scss
    apps/website/app/scss/components/_related.scss
    apps/website/app/scss/components/_card.scss
  </files>
  <action>
Add new modifier blocks to four existing SCSS files. Append each new block INSIDE the parent block selector — before the closing `}` of `.hero`, `.filter`, `.related`, `.card`. Do not modify any existing selectors.

**_hero.scss** — add inside `.hero { }` before closing brace:

```scss
  &--articles {
    background-color: $white_smoke;

    &__container {
      @extend .container;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    &__breadcrumbs {
      display: flex;
    }

    &__title {
      margin-bottom: 0;
    }
  }

  &--article {
    background-color: $white_smoke;

    &__container {
      @extend .container;
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    &__breadcrumbs {
      display: flex;
    }

    &__title {
      margin-bottom: 0;
    }
  }
```

**_filter.scss** — add inside `.filter { }` before closing brace:

```scss
  &--articles {
    padding-bottom: 0;

    &__container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 14px;
      @extend .container;
    }

    &__selectors {
      display: flex;
      justify-content: space-around;
      align-items: center;
    }

    &__selector {
      display: flex;
      flex-direction: column;
      margin-right: 20px;

      label {
        font-size: 13px;
        display: block;
        margin-bottom: 1px;
      }
    }

    &__order {
      display: flex;
      align-items: center;

      label {
        font-size: 13px;
        display: block;
        margin-right: 10px;
      }
    }

    &__select {
      appearance: none;
      padding: 10px;
      border: 1px solid $platinum;
      border-radius: 4px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      padding-right: 40px;
      background-image: url("/images/chevron-down.svg");
      background-position: center right 15px;
      background-repeat: no-repeat;
    }
  }
```

**_related.scss** — add inside `.related { }` before closing brace:

```scss
  &--articles {
    background-color: rgba(240, 240, 240, 0.3);

    &__container {
      @extend .container;
    }

    &__head {
      margin-bottom: 50px;
      display: flex;
      flex-direction: column;
      gap: 10px;

      &--center {
        align-items: center;
        text-align: center;
      }
    }

    &__title {
      font-style: normal;
      font-weight: bold;
      font-size: 32px;
      display: flex;
      color: $charcoal;
      margin: 0;
      line-height: 1.2;
      max-width: 770px;
    }

    &__text {
      max-width: 770px;
    }

    &__grid {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr 1fr;
      gap: 20px;

      @include screen-large {
        grid-template-columns: 1fr 1fr;
      }

      @include screen-small {
        grid-template-columns: 1fr;
      }
    }
  }
```

**_card.scss** — add inside `.card { }` before closing brace:

```scss
  &--article {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 5px;

    &__image {
      overflow: hidden;
      position: relative;
      display: block;
      border-radius: 4px;

      img {
        width: 100%;
        object-fit: cover;
        float: left;
        aspect-ratio: 4 / 2;
      }
    }

    &__info {
      display: flex;
      flex-direction: column;
      gap: 5px;

      a {
        color: $charcoal;
      }

      &__category {
        font-style: normal;
        font-weight: 600;
        font-size: 12px;
        letter-spacing: 0.25px;
        text-decoration-line: underline;
        color: $charcoal;
      }

      &__title {
        font-style: normal;
        font-weight: bold;
        font-size: 14px;
        line-height: 1.4;
        letter-spacing: 0.25px;
        color: $charcoal;
        overflow: hidden;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
      }

      &__excerpt {
        font-style: normal;
        font-weight: normal;
        font-size: 13px;
        line-height: 1.5;
        letter-spacing: 0.25px;
        color: $charcoal;
        overflow: hidden;
        display: -webkit-box;
        -webkit-line-clamp: 3;
        -webkit-box-orient: vertical;
      }

      &__date {
        font-size: 12px;
        color: $davys_grey;
        letter-spacing: 0.25px;
      }

      &__link {
        font-style: normal;
        font-weight: 600;
        font-size: 12px;
        line-height: 24px;
        letter-spacing: 0.5px;
        text-decoration: underline;
        color: $dodger_blue;
      }
    }
  }
```

BEM rules: all child elements prefixed with their modifier name (e.g., `card--article__image`, not `card__image`). Single-noun element names only. No box-shadow, no transform: scale.
  </action>
  <verify>
    <automated>grep -l "articles\|article" apps/website/app/scss/components/_hero.scss apps/website/app/scss/components/_filter.scss apps/website/app/scss/components/_related.scss apps/website/app/scss/components/_card.scss | wc -l</automated>
  </verify>
  <done>
- `_hero.scss` contains `hero--articles` and `hero--article` blocks (existing blocks untouched)
- `_filter.scss` contains `filter--articles` block (existing `filter--announcement` untouched)
- `_related.scss` contains `related--articles` block (existing `related--ads` untouched)
- `_card.scss` contains `card--article` block (existing blocks untouched)
- All new child elements use their modifier as prefix (e.g., `card--article__info`, not `card__info`)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add article import to app.scss and verify build</name>
  <files>apps/website/app/scss/app.scss</files>
  <action>
Add `@use "components/article";` to `apps/website/app/scss/app.scss`.

Placement: After `@use "components/announcement";` (line 26) — place article immediately after announcement to keep blog-related styles grouped together.

The line to add:
```scss
@use "components/article";
```

After editing app.scss, verify the website SCSS builds without errors by running the Nuxt build (or typecheck, which also validates SCSS):
```bash
cd apps/website && yarn nuxt typecheck 2>&1 | tail -5
```

If SCSS errors appear, debug by checking the failing file's BEM nesting syntax.
  </action>
  <verify>
    <automated>grep -n "components/article" apps/website/app/scss/app.scss && cd apps/website && yarn nuxt typecheck 2>&1 | grep -iE "scss|sass|error" | head -10</automated>
  </verify>
  <done>
- `app.scss` contains `@use "components/article";`
- No SCSS compilation errors from the website build
- All 6 new SCSS blocks importable: `article--archive`, `article--single`, `hero--articles`, `hero--article`, `filter--articles`, `related--articles`, `card--article`
  </done>
</task>

</tasks>

<verification>
Final checks after all three tasks:
1. `ls apps/website/app/scss/components/_article.scss` — file exists
2. `grep -c "article--archive\|article--single" apps/website/app/scss/components/_article.scss` — returns ≥ 2
3. `grep "hero--articles\|hero--article" apps/website/app/scss/components/_hero.scss` — both present
4. `grep "filter--articles" apps/website/app/scss/components/_filter.scss` — present
5. `grep "related--articles" apps/website/app/scss/components/_related.scss` — present
6. `grep "card--article" apps/website/app/scss/components/_card.scss` — present
7. `grep "components/article" apps/website/app/scss/app.scss` — present
8. `cd apps/website && yarn nuxt typecheck 2>&1 | grep -c error` — returns 0
</verification>

<success_criteria>
- `_article.scss` created with `article--archive` (4-col grid, filter, paginate) and `article--single` (flex container, body, sidebar) BEM blocks
- `_hero.scss` extended with `hero--articles` and `hero--article` (both: white bg, container, breadcrumbs, title)
- `_filter.scss` extended with `filter--articles` (mirrors `filter--announcement` layout exactly)
- `_related.scss` extended with `related--articles` (mirrors `related--ads` structure exactly)
- `_card.scss` extended with `card--article` (image, info block with category, title, excerpt, date, link)
- `app.scss` includes `@use "components/article"` after announcement import
- Website SCSS builds without errors — all 6 BEM blocks are importable
</success_criteria>

<output>
After completion, create `.planning/phases/066-article-infrastructure/066-02-SUMMARY.md`
</output>
