---
phase: 073-tavily-search-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/search/controllers/search.ts
  - apps/strapi/src/api/search/routes/search.ts
autonomous: true
requirements:
  - BACK-01

must_haves:
  truths:
    - "POST /api/search/tavily with { query } returns { news: [{ title, link, snippet, date, source }] }"
    - "Controller delegates all Tavily HTTP calls to TavilyService — no direct fetch() in controller"
    - "Missing query returns 400 Bad Request without crashing Strapi"
    - "Tavily API error is caught and surfaced as a 500 ApplicationError without crashing Strapi"
  artifacts:
    - path: "apps/strapi/src/api/search/controllers/search.ts"
      provides: "Koa controller with tavily() handler"
      exports: ["default"]
    - path: "apps/strapi/src/api/search/routes/search.ts"
      provides: "Custom Strapi route registering POST /search/tavily"
      exports: ["default"]
  key_links:
    - from: "apps/strapi/src/api/search/controllers/search.ts"
      to: "apps/strapi/src/services/tavily/index.ts"
      via: "import { searchNews } from '../../../services/tavily'"
      pattern: "searchNews"
    - from: "apps/strapi/src/api/search/routes/search.ts"
      to: "apps/strapi/src/api/search/controllers/search.ts"
      via: "handler: 'search.tavily'"
      pattern: "search\\.tavily"
---

<objective>
Register a `POST /api/search/tavily` endpoint in Strapi by creating a custom controller and route file that follow the same pattern as `apps/strapi/src/api/ia/`.

Purpose: Expose TavilyService (already implemented) to the dashboard via a typed HTTP endpoint so Phase 074 can call it from the LightBoxArticles component.
Output: Two files — `search.ts` controller and `search.ts` route — that together make `POST /api/search/tavily` return `{ news: [...] }`.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<!-- Reference implementations to follow exactly -->
@apps/strapi/src/api/ia/controllers/ia.ts
@apps/strapi/src/api/ia/routes/ia.ts
</context>

<interfaces>
<!-- Existing contracts the executor needs — no codebase exploration required. -->

From apps/strapi/src/services/tavily/index.ts:
```typescript
export const searchNews = (query: string, num?: number) =>
  tavilyService.searchNews({ query, num });
// Returns: Promise<TavilySearchResponse>
```

From apps/strapi/src/services/tavily/tavily.types.ts:
```typescript
export interface TavilySearchRequest { query: string; num?: number; }
export interface TavilyNewsResult { title: string; link: string; snippet: string; date: string; source: string; imageUrl?: string; }
export interface TavilySearchResponse { news: TavilyNewsResult[]; }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create search controller</name>
  <files>apps/strapi/src/api/search/controllers/search.ts</files>
  <action>
    Create the directory structure `apps/strapi/src/api/search/controllers/` and write `search.ts`.

    Follow the EXACT same pattern as `apps/strapi/src/api/ia/controllers/ia.ts`:
    - Import `Context` from `"koa"`
    - Import `errors` from `"@strapi/utils"`, destructure `ApplicationError`
    - Import `searchNews` from `"../../../services/tavily"` (NOT from a relative path to the service file — use the index)
    - Export default object with a single async method `tavily(ctx: Context): Promise<void>`

    Handler logic:
    1. Cast `ctx.request.body` as `{ query?: string; num?: number }`
    2. Trim `body?.query` — if falsy, call `ctx.badRequest("Missing required field: query")` and `return`
    3. In `try`, call `const result = await searchNews(query, body.num)`
    4. Set `ctx.body = result` (result is already `{ news: [...] }` — do NOT wrap again)
    5. In `catch`, extract message with `error instanceof Error ? error.message : String(error)`, log with `strapi.log.error(\`[search/tavily] Tavily API error: \${message}\`)`, then `throw new ApplicationError(...)`

    Do NOT add extra error handling, middleware, or auth logic — Strapi handles that at the framework level.
  </action>
  <verify>
    <automated>cd apps/strapi && npx tsc --noEmit 2>&1 | grep -E "search/controllers" || echo "TypeScript OK"</automated>
  </verify>
  <done>File exists at apps/strapi/src/api/search/controllers/search.ts with no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Create search route</name>
  <files>apps/strapi/src/api/search/routes/search.ts</files>
  <action>
    Create the directory `apps/strapi/src/api/search/routes/` and write `search.ts`.

    Follow the EXACT same pattern as `apps/strapi/src/api/ia/routes/ia.ts`:

    ```typescript
    export default {
      routes: [
        {
          method: "POST",
          path: "/search/tavily",
          handler: "search.tavily",
          config: {
            policies: [],
          },
        },
      ],
    };
    ```

    Key details:
    - `path` must be `/search/tavily` (Strapi prepends `/api`, so the full URL becomes `POST /api/search/tavily`)
    - `handler` must be `"search.tavily"` — format is `"{controller-filename}.{method}"`
    - No auth middleware in `config.policies` — access control is managed by Strapi's admin panel
    - This file registers the route; Strapi auto-discovers it because it lives under `src/api/search/routes/`
  </action>
  <verify>
    <automated>cd apps/strapi && npx tsc --noEmit 2>&1 | grep -E "search/routes" || echo "TypeScript OK"</automated>
  </verify>
  <done>File exists at apps/strapi/src/api/search/routes/search.ts, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
After both tasks are complete, verify the full endpoint is wired correctly:

1. Run TypeScript check across strapi app: `cd apps/strapi && npx tsc --noEmit`
2. Confirm directory structure:
   ```
   apps/strapi/src/api/search/
     controllers/search.ts
     routes/search.ts
   ```
3. Confirm handler string matches: `handler: "search.tavily"` in routes matches `tavily(ctx)` method in controller
4. Confirm import path: `"../../../services/tavily"` resolves to `apps/strapi/src/services/tavily/index.ts`
</verification>

<success_criteria>
- `apps/strapi/src/api/search/controllers/search.ts` exists and exports default controller object with `tavily` method
- `apps/strapi/src/api/search/routes/search.ts` exists and registers `POST /search/tavily → search.tavily`
- `npx tsc --noEmit` in `apps/strapi` passes with no new errors
- Controller imports `searchNews` from services/tavily (no direct `fetch` calls)
- `ctx.body = result` sets the response — `result` is already `{ news: [...] }`, not wrapped further
</success_criteria>

<output>
After completion, create `.planning/phases/073-tavily-search-backend/073-01-SUMMARY.md`
</output>
