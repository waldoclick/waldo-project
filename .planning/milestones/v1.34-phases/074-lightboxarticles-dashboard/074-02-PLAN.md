---
phase: 074-lightboxarticles-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 074-01
files_modified:
  - apps/dashboard/app/pages/articles/index.vue
autonomous: true
requirements:
  - INT-01

must_haves:
  truths:
    - "articles/index.vue shows a 'Generar artículo' button with class `btn btn--announcement` and `Wand2` icon, placed alongside the existing 'Agregar artículo' NuxtLink"
    - "Pressing 'Generar artículo' opens the LightBoxArticles modal"
    - "Closing the modal (via backdrop, X button, or @close emit) hides it"
  artifacts:
    - path: "apps/dashboard/app/pages/articles/index.vue"
      provides: "articles index page with LightBoxArticles trigger button"
      contains: "LightBoxArticles"
  key_links:
    - from: "articles/index.vue"
      to: "LightBoxArticles.vue"
      via: "<LightBoxArticles :is-open='isLightboxOpen' @close='isLightboxOpen = false' />"
      pattern: "LightBoxArticles"
    - from: "btn--announcement button"
      to: "isLightboxOpen ref"
      via: "@click='isLightboxOpen = true'"
      pattern: "isLightboxOpen"
---

<objective>
Wire `LightBoxArticles` into `pages/articles/index.vue` — add the "Generar artículo" button
that opens the modal, and mount the component on the page.

Purpose: Completes the full user-facing feature — the button in the page header that triggers
the 3-step article generation flow.

Output:
- `apps/dashboard/app/pages/articles/index.vue` — updated with button + LightBoxArticles mount
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/074-lightboxarticles-dashboard/074-01-SUMMARY.md

<interfaces>
<!-- Current articles/index.vue (to be modified): -->
From apps/dashboard/app/pages/articles/index.vue:
```vue
<template>
  <div>
    <HeroDefault title="Artículos" :breadcrumbs="breadcrumbs">
      <template #actions>
        <NuxtLink class="btn btn--primary" to="/articles/new">
          Agregar artículo
        </NuxtLink>
      </template>
    </HeroDefault>
    <ArticlesDefault />
  </div>
</template>

<script setup lang="ts">
import HeroDefault from "@/components/HeroDefault.vue";
import ArticlesDefault from "@/components/ArticlesDefault.vue";

definePageMeta({
  layout: "dashboard",
});

const breadcrumbs = [{ label: "Artículos" }];
</script>
```

From apps/dashboard/app/components/LightBoxArticles.vue (created in plan 01):
```typescript
// Props: isOpen: boolean
// Emits: close
// Usage: <LightBoxArticles :is-open="bool" @close="handler" />
```

Button class for this case (from _button.scss):
```scss
.btn--announcement {
  background: $light_peach;
  border: 2px solid $charcoal;
  color: $charcoal;
  display: flex;
  gap: 5px;  // ← icon + text side by side
}
```

Icon import pattern (lucide-vue-next):
```typescript
import { Wand2 } from "lucide-vue-next";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LightBoxArticles button and modal to articles index page</name>
  <files>apps/dashboard/app/pages/articles/index.vue</files>
  <action>
Update `apps/dashboard/app/pages/articles/index.vue` to:

1. Add a `isLightboxOpen` ref initialized to `false`
2. Add a "Generar artículo" button with classes `btn btn--announcement` and a `Wand2` icon (size=16) in the `#actions` slot of HeroDefault, placed BEFORE the existing "Agregar artículo" NuxtLink
3. Mount `<LightBoxArticles :is-open="isLightboxOpen" @close="isLightboxOpen = false" />` at the bottom of the template (outside `<div>` wrapper is fine, but inside `<template>` — use a Fragment pattern if needed, or just inside the root `<div>`)
4. Import `LightBoxArticles` from `"@/components/LightBoxArticles.vue"` and `Wand2` from `"lucide-vue-next"`

**Final template result:**
```vue
<template>
  <div>
    <HeroDefault title="Artículos" :breadcrumbs="breadcrumbs">
      <template #actions>
        <button class="btn btn--announcement" type="button" @click="isLightboxOpen = true">
          <Wand2 :size="16" />
          Generar artículo
        </button>
        <NuxtLink class="btn btn--primary" to="/articles/new">
          Agregar artículo
        </NuxtLink>
      </template>
    </HeroDefault>
    <ArticlesDefault />
    <LightBoxArticles :is-open="isLightboxOpen" @close="isLightboxOpen = false" />
  </div>
</template>

<script setup lang="ts">
import { ref } from "vue";
import { Wand2 } from "lucide-vue-next";
import HeroDefault from "@/components/HeroDefault.vue";
import ArticlesDefault from "@/components/ArticlesDefault.vue";
import LightBoxArticles from "@/components/LightBoxArticles.vue";

definePageMeta({
  layout: "dashboard",
});

const breadcrumbs = [{ label: "Artículos" }];
const isLightboxOpen = ref(false);
</script>
```

No CSS changes needed — `btn--announcement` already exists in `_button.scss`.
  </action>
  <verify>
    <automated>yarn workspace @waldo/dashboard typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `pages/articles/index.vue` has `btn--announcement` button with `Wand2` icon
    - `LightBoxArticles` is mounted with `:is-open` + `@close` binding
    - TypeScript typecheck passes with zero new errors
    - Button appears before the "Agregar artículo" link in the actions slot
  </done>
</task>

</tasks>

<verification>
After this plan:

1. `yarn workspace @waldo/dashboard typecheck` passes with zero new errors
2. Navigate to `/articles` in the dashboard — see "Generar artículo" button (yellow/peach, Wand2 icon) next to "Agregar artículo"
3. Click button → LightBoxArticles modal opens at Step 1
4. Click X or backdrop → modal closes
5. The `btn--announcement` styling (from existing SCSS) renders correctly
</verification>

<success_criteria>
- TypeScript strict typecheck passes for the modified page
- "Generar artículo" button renders with correct BEM class `btn btn--announcement` and `Wand2` icon
- LightBoxArticles modal is mounted and responds to open/close
- INT-01 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/074-lightboxarticles-dashboard/074-02-SUMMARY.md`
</output>
