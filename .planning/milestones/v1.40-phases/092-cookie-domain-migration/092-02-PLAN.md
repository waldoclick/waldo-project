---
phase: 092-cookie-domain-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/.env.example
  - apps/dashboard/.env.example
autonomous: false
requirements: [SAFE-03]

must_haves:
  truths:
    - "Both .env.example files document COOKIE_DOMAIN with the production value .waldo.click"
    - "Developers cloning the repo know exactly which env var to set and what the production value looks like"
    - "Human verifies the shared-session flow works end-to-end in a browser"
  artifacts:
    - path: "apps/website/.env.example"
      provides: "COOKIE_DOMAIN env var documentation"
      contains: "COOKIE_DOMAIN=.waldo.click"
    - path: "apps/dashboard/.env.example"
      provides: "COOKIE_DOMAIN env var documentation"
      contains: "COOKIE_DOMAIN=.waldo.click"
  key_links:
    - from: "apps/website/.env.example"
      to: "apps/website/nuxt.config.ts strapi.cookie conditional"
      via: "COOKIE_DOMAIN env var"
      pattern: "COOKIE_DOMAIN"
---

<objective>
Add `COOKIE_DOMAIN` documentation to both `.env.example` files and verify the complete shared-session flow with a human checkpoint.

Purpose: Satisfy SAFE-03 (env var documented) and verify all SESS-01–06 success criteria in a real browser session.
Output: Both .env.example files updated. Human confirms login/logout works across both subdomains.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Current .env.example structure (both apps) -->
<!-- website .env.example ends with GOOGLE_CLIENT_ID line — add COOKIE_DOMAIN after the App section -->
<!-- dashboard .env.example has the same structure -->

From STATE.md v1.40 Key Facts:
- Production COOKIE_DOMAIN=.waldo.click
- Staging COOKIE_DOMAIN=.waldoclick.dev
- Local dev: unset (comment showing that omitting it keeps host-only behavior)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add COOKIE_DOMAIN to both .env.example files</name>
  <files>apps/website/.env.example, apps/dashboard/.env.example</files>
  <action>
In BOTH .env.example files, add a `COOKIE_DOMAIN` entry in the `# App` section, after `SESSION_MAX_AGE` and before the empty line that follows it.

Add this block:
```
# Cookie domain for shared session across subdomains (leave unset for local dev — host-only cookie)
# COOKIE_DOMAIN=.waldo.click  # production
# COOKIE_DOMAIN=.waldoclick.dev  # staging
```

The lines are commented out because local dev should NOT set this variable (host-only cookie is correct for localhost). The comments show developers the production and staging values they need to configure in their deployment environments.

Do not change any other lines in either file.
  </action>
  <verify>
    <automated>grep -n "COOKIE_DOMAIN" apps/website/.env.example apps/dashboard/.env.example</automated>
  </verify>
  <done>Both .env.example files contain the COOKIE_DOMAIN comment block. grep confirms presence in both files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify local login/logout regression + readiness for staging deploy</name>
  <files>none (verification only)</files>
  <action>Human verifies login/logout flows on both apps locally to confirm no regression before staging deploy.</action>
  <verify>Human approval via checkpoint gate</verify>
  <done>User types "approved" or describes issues found</done>
  <what-built>
Plan 01 added COOKIE_DOMAIN conditional domain spread to both nuxt.config.ts files and old-cookie cleanup to both useLogout composables. Plan 02 documented COOKIE_DOMAIN in both .env.example files.

To test the shared-session flow you need both apps running against the same Strapi instance with the same waldo_jwt cookie name. Local testing without a shared parent domain is limited — the shared session only activates when COOKIE_DOMAIN is set to a parent domain.

**What you CAN verify locally:**
  </what-built>
  <how-to-verify>
1. Start both apps locally (without COOKIE_DOMAIN set)
2. Log in on website (http://localhost:3000) — confirm login works normally ✓
3. Log in on dashboard (http://localhost:3001) — confirm login works normally ✓
4. Log out on website — confirm you're redirected to "/" and the session is gone ✓
5. Log out on dashboard — confirm you're redirected to "/auth/login" and the session is gone ✓
6. Confirm no console errors in either app during login/logout flows ✓

**To fully verify SESS-01–04 (cross-subdomain):**
Set `COOKIE_DOMAIN=.waldo.click` in both apps' .env files and test against staging/production domains OR set up local /etc/hosts entries for two subdomains pointing to 127.0.0.1 with a shared parent domain.

**Success criteria for this checkpoint:**
- Local login/logout works on both apps without errors (confirms no regression)
- You're satisfied with the implementation and comfortable deploying to staging/production for full cross-subdomain verification
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
```bash
grep -n "COOKIE_DOMAIN" apps/website/.env.example apps/dashboard/.env.example
```
Expected: Both files show 3 COOKIE_DOMAIN comment lines each.
</verification>

<success_criteria>
- Both .env.example files contain COOKIE_DOMAIN documentation with production value
- Human has verified local login/logout works without regression
- Phase 092 all 8 requirements (SESS-01–06, SAFE-02, SAFE-03) are implemented
</success_criteria>

<output>
After completion, create `.planning/phases/092-cookie-domain-migration/092-02-SUMMARY.md`
</output>
