---
phase: 076-dashboard-gift-lightbox
plan: "02"
type: execute
wave: 2
depends_on: ["076-01"]
files_modified:
  - apps/dashboard/app/pages/reservations/[id].vue
  - apps/dashboard/app/pages/featured/[id].vue
autonomous: true
requirements: [GIFT-01, GIFT-02]

must_haves:
  truths:
    - "A 'Regalar Reservas' button appears on the ad-reservation detail page (/reservations/[id])"
    - "A 'Regalar Reservas Destacadas' button appears on the featured-reservation detail page (/featured/[id])"
    - "Clicking the button opens the LightboxGift with the correct endpoint and label"
    - "The lightbox closes when the gift succeeds"
  artifacts:
    - path: "apps/dashboard/app/pages/reservations/[id].vue"
      provides: "Gift button + LightboxGift wired with endpoint='ad-reservations'"
      contains: "LightboxGift"
    - path: "apps/dashboard/app/pages/featured/[id].vue"
      provides: "Gift button + LightboxGift wired with endpoint='ad-featured-reservations'"
      contains: "LightboxGift"
  key_links:
    - from: "apps/dashboard/app/pages/reservations/[id].vue"
      to: "LightboxGift"
      via: "<LightboxGift :is-open='giftOpen' endpoint='ad-reservations' label='reservas' @close='giftOpen = false' @gifted='giftOpen = false' />"
      pattern: "LightboxGift"
    - from: "apps/dashboard/app/pages/featured/[id].vue"
      to: "LightboxGift"
      via: "<LightboxGift :is-open='giftOpen' endpoint='ad-featured-reservations' label='reservas destacadas' @close='giftOpen = false' @gifted='giftOpen = false' />"
      pattern: "LightboxGift"
---

<objective>
Wire LightboxGift.vue into the two reservation detail pages — `reservations/[id].vue` and `featured/[id].vue` — by adding a "Regalar Reservas" button and the LightboxGift component instance to each page.

Purpose: Completes the end-to-end gift flow — admin sees the button on a detail page, clicks it, fills the form, confirms, and the gift endpoint is called.
Output: Both detail pages updated with button + LightboxGift wiring.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/076-dashboard-gift-lightbox/076-01-SUMMARY.md

<interfaces>
<!-- LightboxGift.vue props (created in plan 01) -->
```typescript
Props:
  isOpen: boolean        // controls visibility
  endpoint: string       // 'ad-reservations' | 'ad-featured-reservations'
  label: string          // used in Swal text: e.g. 'reservas' | 'reservas destacadas'

Emits:
  'close'   // user cancelled or closed
  'gifted'  // gift succeeded
```

<!-- BoxContent sidebar slot — existing pattern in both pages -->
Both pages use:
```html
<template #sidebar>
  <BoxInformation title="Detalles" :columns="1">
    <CardInfo ... />
  </BoxInformation>
</template>
```

Add the button ABOVE BoxContent (or inside sidebar below the existing BoxInformation), following the same pattern as LightBoxArticles pages that add action buttons. Use a simple `<button class="btn btn--primary">` pattern — no new component needed.

The button placement: add a new `<template #sidebar>` action group OR place it below BoxContent as a standalone button row. Best pattern: add it inside the sidebar slot, below the existing BoxInformation, as a standalone `<div class="box-content__action">` or simply a `<button>` element.

Concrete approach: Place a `<div style="padding: 0 20px 20px;">` wrapping a `<button class="btn btn--primary" @click="giftOpen = true">` as the last child inside `<BoxContent>`'s sidebar slot, after the existing `<BoxInformation>`. This is the least invasive pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire LightboxGift into reservations/[id].vue</name>
  <files>apps/dashboard/app/pages/reservations/[id].vue</files>
  <action>
Update `apps/dashboard/app/pages/reservations/[id].vue`:

1. **Add `giftOpen` state** in the `<script setup>` block (already has `ref` imported):
   ```typescript
   const giftOpen = ref(false)
   ```

2. **Add button in sidebar slot** — after the closing `</BoxInformation>` tag inside `<template #sidebar>`, add:
   ```html
   <div style="padding: 16px 0 0;">
     <button class="btn btn--primary" type="button" @click="giftOpen = true">
       Regalar Reservas
     </button>
   </div>
   ```

3. **Add LightboxGift instance** — after the closing `</BoxContent>` tag (still inside the root `<div>`), add:
   ```html
   <LightboxGift
     :is-open="giftOpen"
     endpoint="ad-reservations"
     label="reservas"
     @close="giftOpen = false"
     @gifted="giftOpen = false"
   />
   ```

LightboxGift is auto-imported by Nuxt (component in `components/` directory) — no explicit import needed.

Do NOT change the breadcrumbs, title, useAsyncData, or any existing data-fetching logic.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | grep -E "reservations.*id|error TS" | head -20 || echo "typecheck ok"</automated>
  </verify>
  <done>reservations/[id].vue has a "Regalar Reservas" button that sets giftOpen=true and a LightboxGift instance wired to the ad-reservations endpoint</done>
</task>

<task type="auto">
  <name>Task 2: Wire LightboxGift into featured/[id].vue</name>
  <files>apps/dashboard/app/pages/featured/[id].vue</files>
  <action>
Update `apps/dashboard/app/pages/featured/[id].vue` — same pattern as Task 1 but for featured reservations:

1. **Add `giftOpen` state** in the `<script setup>` block (already has `ref` imported):
   ```typescript
   const giftOpen = ref(false)
   ```

2. **Add button in sidebar slot** — after the closing `</BoxInformation>` tag inside `<template #sidebar>`, add:
   ```html
   <div style="padding: 16px 0 0;">
     <button class="btn btn--primary" type="button" @click="giftOpen = true">
       Regalar Reservas Destacadas
     </button>
   </div>
   ```

3. **Add LightboxGift instance** — after the closing `</BoxContent>` tag (still inside the root `<div>`), add:
   ```html
   <LightboxGift
     :is-open="giftOpen"
     endpoint="ad-featured-reservations"
     label="reservas destacadas"
     @close="giftOpen = false"
     @gifted="giftOpen = false"
   />
   ```

Do NOT change the breadcrumbs, title, useAsyncData, or any existing data-fetching logic.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | grep -E "featured.*id|error TS" | head -20 || echo "typecheck ok"</automated>
  </verify>
  <done>featured/[id].vue has a "Regalar Reservas Destacadas" button that sets giftOpen=true and a LightboxGift instance wired to the ad-featured-reservations endpoint</done>
</task>

</tasks>

<verification>
- `grep -r "LightboxGift" apps/dashboard/app/pages/` returns both pages
- `grep "giftOpen" apps/dashboard/app/pages/reservations/\[id\].vue` shows the ref and button handler
- `grep "giftOpen" apps/dashboard/app/pages/featured/\[id\].vue` shows the ref and button handler
- `cd apps/dashboard && yarn nuxt typecheck` passes with no new TS errors
</verification>

<success_criteria>
Both detail pages have a gift button and a wired LightboxGift component. TypeScript check passes. No existing data-fetching or layout logic was changed.
</success_criteria>

<output>
After completion, create `.planning/phases/076-dashboard-gift-lightbox/076-02-SUMMARY.md`
</output>
