---
phase: 077-strapi-2-step-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/services/mjml/templates/verification-code.mjml
autonomous: true
requirements: [VSTEP-08]

must_haves:
  truths:
    - "Email template renders a prominent 6-digit code visually distinguishable from surrounding text"
    - "Template communicates a 5-minute expiry in Spanish"
    - "Branding is consistent with existing templates (same base layout, logo, footer)"
    - "Template accepts { name, code } as Nunjucks variables"
  artifacts:
    - path: "apps/strapi/src/services/mjml/templates/verification-code.mjml"
      provides: "MJML email template for 2-step login code"
      contains: "{{ code }}"
  key_links:
    - from: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      to: "apps/strapi/src/services/mjml/templates/verification-code.mjml"
      via: "sendMjmlEmail(strapi, 'verification-code', email, subject, { name, code })"
      pattern: "verification-code"
---

<objective>
Create the `verification-code.mjml` email template in Spanish — sent every time a user completes the first step of login. The 6-digit code must be large and prominent so the user can read it at a glance.

Purpose: Plan 03 (auth controllers) calls `sendMjmlEmail(strapi, 'verification-code', ...)` — that template must exist at render time.
Output: `apps/strapi/src/services/mjml/templates/verification-code.mjml`
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- MJML template patterns — from existing templates in this project -->

All templates extend `layouts/base.mjml` using Nunjucks block syntax:
```
{% extends "layouts/base.mjml" %} {% block content %}
  <mj-text ...>...</mj-text>
{% endblock %}
```

`sendMjmlEmail` signature (apps/strapi/src/services/mjml/send-email.ts):
```typescript
export async function sendMjmlEmail(
  strapi: any,
  template: string,       // "verification-code" (no .mjml extension)
  to: string | string[],
  subject: string,
  data: Record<string, any>
)
```

`data` object available in Nunjucks as template variables.
Automatic variables injected by sendMjmlEmail: `year`, `frontendUrl`, `appUrl`.

From apps/strapi/src/services/mjml/templates/gift-reservation.mjml (reference):
```
{% extends "layouts/base.mjml" %} {% block content %}
<mj-text font-size="16px"> Hola {{ name }}, </mj-text>
<mj-text>
  ¡Tienes un regalo! ...
</mj-text>
<mj-text> Saludos,<br />El equipo de <b>Waldo.click®</b> </mj-text>
{% endblock %}
```

Variables this template will receive:
- `{{ name }}` — user's first name (for personalized greeting)
- `{{ code }}` — 6-digit numeric string, e.g. "482031"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verification-code.mjml email template</name>
  <files>apps/strapi/src/services/mjml/templates/verification-code.mjml</files>
  <action>
Create `apps/strapi/src/services/mjml/templates/verification-code.mjml`.

Requirements:
- Extends `layouts/base.mjml` using the `{% extends %}` / `{% block content %}` pattern (same as all other templates)
- All copy in Spanish
- Personalized greeting: "Hola {{ name }},"
- Explain the user attempted to log in and needs to enter the code
- Display `{{ code }}` in a large, visually prominent style — use `font-size: 32px; font-weight: bold; letter-spacing: 8px; text-align: center` via inline MJML attribute or a wrapping `<mj-text>` with those attributes
- State clearly that the code expires in **5 minutes**
- If the user did not attempt to log in, advise them to ignore the email
- Close with standard "El equipo de Waldo.click®" sign-off

Example copy (exact wording can be adjusted for naturalness):
```
Hola {{ name }},

Has iniciado sesión en Waldo.click®. Para completar el acceso, ingresa el siguiente código de verificación:

[código grande: {{ code }}]

Este código es válido por 5 minutos.

Si no intentaste iniciar sesión, ignora este correo. Tu cuenta sigue segura.

Saludos,
El equipo de Waldo.click®
```

The code block should use a separate `<mj-text>` element with `font-size="32px" font-weight="bold" letter-spacing="8px" align="center" padding="20px 0"` to make it unmistakably visible.
  </action>
  <verify>
    <automated>test -f apps/strapi/src/services/mjml/templates/verification-code.mjml && grep -q "{{ code }}" apps/strapi/src/services/mjml/templates/verification-code.mjml && grep -q "5 minutos" apps/strapi/src/services/mjml/templates/verification-code.mjml && echo "OK: template exists with code variable and expiry notice"</automated>
  </verify>
  <done>File exists, contains `{{ code }}` Nunjucks variable, references 5-minute expiry in Spanish, and follows the `{% extends "layouts/base.mjml" %}` pattern</done>
</task>

</tasks>

<verification>
- `apps/strapi/src/services/mjml/templates/verification-code.mjml` exists
- Contains `{{ code }}` and `{{ name }}` Nunjucks variables
- Contains "5 minutos" (or equivalent expiry notice)
- Extends base layout (not standalone MJML)
</verification>

<success_criteria>
When `sendMjmlEmail(strapi, 'verification-code', email, 'Tu código de verificación', { name: 'Juan', code: '482031' })` is called in Plan 03, the Nunjucks renderer finds the template and produces valid HTML without throwing.
</success_criteria>

<output>
After completion, create `.planning/phases/077-strapi-2-step-backend/077-02-SUMMARY.md`
</output>
