---
phase: 090-migrate-all-get-callers
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/stores/ads.store.ts
  - apps/website/app/stores/related.store.ts
  - apps/website/app/stores/articles.store.ts
  - apps/website/app/stores/categories.store.ts
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "ads.store.ts has no strapi.find() calls — loadAds, loadAdBySlug, loadAdById use useApiClient"
    - "related.store.ts has no strapi.find() calls — loadRelatedAds uses useApiClient"
    - "articles.store.ts has no strapi.find() calls — loadArticles uses useApiClient"
    - "categories.store.ts has no strapi.find() calls — loadCategories, loadCategory, loadCategoryBySlug use useApiClient"
    - "client = useApiClient() is at setup root level in setup stores, at action level in Options API stores"
    - "Response .data removal only where strapi SDK was silently adding a wrapper — response shape from useApiClient is raw JSON body (same as Strapi response)"
  artifacts:
    - path: "apps/website/app/stores/ads.store.ts"
      provides: "Ad collection and single-ad fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/related.store.ts"
      provides: "Related ads fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/articles.store.ts"
      provides: "Article list fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/categories.store.ts"
      provides: "Category list and single-category fetching via useApiClient"
      contains: "useApiClient"
  key_links:
    - from: "ads.store.ts loadAds"
      to: "useApiClient"
      via: "client('/api/ads', { method: 'GET', params })"
      pattern: "client\\('/api/ads'"
    - from: "categories.store.ts loadCategories"
      to: "useApiClient"
      via: "client('/api/categories', { method: 'GET', params })"
      pattern: "client\\('/api/categories'"
---

<objective>
Migrate 4 content stores from `strapi.find()` to `useApiClient`. These stores handle ads, related ads, articles, and categories — the core content of the website. The ads.store.ts is a setup store with `strapi` at root level. categories.store.ts is also a setup store with `strapi` inside actions. related.store.ts and articles.store.ts are setup stores with `strapi` at root level.

Purpose: Removes strapi SDK dependency from the primary content stores, satisfying API-01.
Output: 4 modified store files.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-migrate-all-get-callers/090-VALIDATION.md

<interfaces>
<!-- Calling convention (from 089-01-SUMMARY.md) -->
```typescript
// Setup stores: client at root level
const client = useApiClient()  // at store root, same level as other const refs

// Options API stores: client at top of each action
const client = useApiClient()  // inside each action that needs it

// All calls:
const response = await client('/api/endpoint', {
  method: 'GET',
  params: { ... } as unknown as Record<string, unknown>
})
// response is raw Strapi JSON body: { data: T[], meta: { pagination: {...} } }
// existing casts (as unknown as StrapiResponse<T>) remain valid
```

ads.store.ts current pattern (3 find calls):
- loadAds: `strapi.find("ads", params)` — params has filters, pagination, sort, populate
- loadAdBySlug: `strapi.find("ads", { filters, populate })` — returns array, takes [0]
- loadAdById: `strapi.find("ads", { filters, populate })` — returns array, takes [0]
- All cast to `StrapiResponse<Ad>`, access `.data` and `.meta.pagination`

related.store.ts current pattern (1 find call):
- loadRelatedAds(id): `strapi.find(`related/ads/${id}`, { populate: "*" })`
- Casts to `AdResponse`

articles.store.ts current pattern (1 find call):
- loadArticles: `strapi.find("articles", params)` — same structure as ads
- Casts to `ArticleResponse`

categories.store.ts current pattern (3 find calls, strapi INSIDE actions):
- loadCategories: `strapi.find("categories", { pagination, populate, sort })` inside action
- loadCategory: `strapi.find("categories", { filters: { id: { $eq: id } }, populate })` inside action
- loadCategoryBySlug: `strapi.find("categories", { filters: { slug: { $eq: slug } }, populate })` inside action
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ads.store.ts and related.store.ts</name>
  <files>
    apps/website/app/stores/ads.store.ts
    apps/website/app/stores/related.store.ts
  </files>
  <action>
**ads.store.ts** — setup store, `strapi` currently at root level:

1. Replace `const strapi = useStrapi()` with `const client = useApiClient()` at root level.
2. `loadAds(filtersParams, paginationParams, sortParams)`:
   - Replace `await strapi.find("ads", params)` → `await client('/api/ads', { method: 'GET', params: params as unknown as Record<string, unknown> })`.
   - The existing `params` variable already contains `{ filters, pagination, sort, populate }` — pass it as `params` in the options. Cast to keep TypeScript happy.
   - `const typedResponse = response as unknown as StrapiResponse<Ad>` — unchanged.
3. `loadAdBySlug(slug)`:
   - Replace `await strapi.find("ads", { filters: {...}, populate: {...} } as unknown as Record<string, unknown>)` → `await client('/api/ads', { method: 'GET', params: { filters: { slug: { $eq: slug }, active: { $eq: true }, remaining_days: { $gt: 0 } }, populate: { commune: { populate: '*' }, user: { populate: '*' }, category: true, condition: true, gallery: true } } as unknown as Record<string, unknown> })`.
   - `const typedResponse = response as unknown as StrapiResponse<Ad>` — unchanged.
4. `loadAdById(id)`:
   - Replace `await strapi.find("ads", { filters: { id: { $eq: id } }, populate: {...} } as unknown as Record<string, unknown>)` → `await client('/api/ads', { method: 'GET', params: { filters: { id: { $eq: id } }, populate: { commune: { populate: '*' }, user: true, category: true, condition: true, gallery: true } } as unknown as Record<string, unknown> })`.
   - `const typedResponse = response as unknown as StrapiResponse<Ad>` — unchanged.

**related.store.ts** — setup store, `strapi` at root level:

1. Replace `const strapi = useStrapi()` with `const client = useApiClient()` at root level.
2. `loadRelatedAds(id)`:
   - Replace `await strapi.find(`related/ads/${id}`, { populate: "*" })` → `await client(`/api/related/ads/${id}`, { method: 'GET', params: { populate: '*' } as unknown as Record<string, unknown> })`.
   - `const typedResponse = response as unknown as AdResponse` — unchanged.
  </action>
  <verify>
    <automated>grep -n "strapi" apps/website/app/stores/ads.store.ts apps/website/app/stores/related.store.ts || echo "PASS: no strapi references"</automated>
  </verify>
  <done>ads.store.ts and related.store.ts have zero `strapi` references; all find calls use client('/api/...').</done>
</task>

<task type="auto">
  <name>Task 2: Migrate articles.store.ts and categories.store.ts</name>
  <files>
    apps/website/app/stores/articles.store.ts
    apps/website/app/stores/categories.store.ts
  </files>
  <action>
**articles.store.ts** — setup store, `strapi` at root level:

1. Replace `const strapi = useStrapi()` with `const client = useApiClient()` at root level.
2. `loadArticles(filtersParams, paginationParams, sortParams)`:
   - The existing `params` variable contains `{ filters, pagination, sort, populate: '*' }`.
   - Replace `await strapi.find("articles", params)` → `await client('/api/articles', { method: 'GET', params: params as unknown as Record<string, unknown> })`.
   - `const typedResponse = response as unknown as ArticleResponse` — unchanged.

**categories.store.ts** — setup store, `strapi` is called INSIDE each action (not at root level). For setup stores, `useApiClient()` must be at root level:

1. Add `const client = useApiClient()` at root level of the setup function (alongside `const categories = ref<Category[]>(...)` etc.). Do NOT add it inside each action.
2. `loadCategories()`:
   - Remove `const strapi = useStrapi()` inside the action.
   - Replace `await strapi.find("categories", { pagination: { page: 1, pageSize: 1000 }, ... })` → `await client('/api/categories', { method: 'GET', params: { pagination: { page: 1, pageSize: 1000 }, populate: '*', sort: ['name:asc'] } as unknown as Record<string, unknown> })`.
   - Response cast and shape extraction remain unchanged.
3. `loadCategory(id)` (if it exists):
   - Remove `const strapi = useStrapi()` inside the action.
   - Replace `strapi.find("categories", { filters: { id: { $eq: id } }, ... })` → `client('/api/categories', { method: 'GET', params: { filters: { id: { $eq: id } }, populate: '*' } as unknown as Record<string, unknown> })`.
4. `loadCategoryBySlug(slug)` (if it exists):
   - Remove `const strapi = useStrapi()` inside the action.
   - Replace `strapi.find("categories", { filters: { slug: { $eq: slug } }, ... })` → `client('/api/categories', { method: 'GET', params: { filters: { slug: { $eq: slug } }, populate: '*' } as unknown as Record<string, unknown> })`.
  </action>
  <verify>
    <automated>grep -n "strapi" apps/website/app/stores/articles.store.ts apps/website/app/stores/categories.store.ts || echo "PASS: no strapi references"</automated>
  </verify>
  <done>articles.store.ts and categories.store.ts have zero `strapi` references; all find calls use client('/api/...').</done>
</task>

<task type="auto">
  <name>Task 3: Verify wave 1 content stores</name>
  <files></files>
  <action>Run grep verification across all 4 stores in this plan to confirm no strapi.find/findOne remain.</action>
  <verify>
    <automated>grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/ads.store.ts apps/website/app/stores/related.store.ts apps/website/app/stores/articles.store.ts apps/website/app/stores/categories.store.ts && echo "FAIL" || echo "PASS: no strapi calls in content stores"</automated>
  </verify>
  <done>Zero strapi.find/findOne references in all 4 content stores.</done>
</task>

</tasks>

<verification>
After all tasks:
`grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/ads.store.ts apps/website/app/stores/related.store.ts apps/website/app/stores/articles.store.ts apps/website/app/stores/categories.store.ts` → exits 1 (no matches)
</verification>

<success_criteria>
- ads.store.ts: loadAds, loadAdBySlug, loadAdById use client('/api/ads')
- related.store.ts: loadRelatedAds uses client('/api/related/ads/${id}')
- articles.store.ts: loadArticles uses client('/api/articles')
- categories.store.ts: loadCategories (and loadCategory/loadCategoryBySlug if present) use client('/api/categories')
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/090-migrate-all-get-callers/090-02-SUMMARY.md`
</output>
