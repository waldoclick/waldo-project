# Requirements Archive: v1.14 GTM Module: Dashboard

**Shipped:** 2026-03-07
**Milestone Goal:** Install and configure `@saslavik/nuxt-gtm` in `apps/dashboard`, mirroring the v1.13 website implementation.

## Requirements

| Requirement | Phase | Plan | Status |
|-------------|-------|------|--------|
| GTM-DASH-01 | 34 | 34-01 | complete |
| GTM-DASH-02 | 34 | 34-01 | complete |
| GTM-DASH-03 | 34 | 34-01 | complete |

### GTM-DASH-01: Install `@saslavik/nuxt-gtm` module ✓
- `@saslavik/nuxt-gtm@0.1.3` in `apps/dashboard/package.json` devDependencies
- Module listed in `modules[]` in `apps/dashboard/nuxt.config.ts`

### GTM-DASH-02: Configure module — GTM ID, enableRouterSync, remove legacy field ✓
- `gtm: { id, enableRouterSync: true, debug: false }` block at top level of `nuxt.config.ts`
- `runtimeConfig.public.gtm: { id }` replaces flat `gtmId` field
- No file in `apps/dashboard` references `gtmId`

### GTM-DASH-03: TypeScript typecheck passes with zero errors ✓
- Zero new TypeScript errors introduced by GTM migration
- Pre-existing `formatDate` errors (54, unrelated) deferred to future phase — logged in `deferred-items.md`
