---
phase: 079-website-verify-flow-mjml-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/services/mjml/templates/verification-code.mjml
autonomous: true
requirements:
  - PWDR-04
must_haves:
  truths:
    - "The verification email received by the user reads '15 minutos' (not '5 minutos') for the code expiry"
  artifacts:
    - path: "apps/strapi/src/services/mjml/templates/verification-code.mjml"
      provides: "Verification code email template with correct 15-minute expiry copy"
      contains: "15 minutos"
  key_links:
    - from: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      to: "apps/strapi/src/services/mjml/templates/verification-code.mjml"
      via: "sendMjmlEmail(strapi, 'verification-code', ...)"
      pattern: "sendMjmlEmail.*verification-code"
---

<objective>
Fix the "5 minutos" copy error in `verification-code.mjml` — the template incorrectly states the code is valid for 5 minutes when the actual server-side expiry is 15 minutes.

Purpose: PWDR-04 requires the verification email to display the correct 15-minute expiry. The mismatch erodes user trust and causes unnecessary re-requests when users think the code has expired.
Output: `verification-code.mjml` updated with correct "15 minutos" copy.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key reference for the executor. -->

From apps/strapi/src/extensions/users-permissions/controllers/authController.ts:
```typescript
const CODE_EXPIRY_MS = 15 * 60 * 1000; // 15 minutes
```
Server-side expiry is definitively 15 minutes.

Current broken line in verification-code.mjml (line 16):
```mjml
<mj-text> Este código es válido por <b>5 minutos</b>. </mj-text>
```

Correct value: `15 minutos`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix "5 minutos" → "15 minutos" in verification-code.mjml</name>
  <files>apps/strapi/src/services/mjml/templates/verification-code.mjml</files>
  <action>
Open `apps/strapi/src/services/mjml/templates/verification-code.mjml` and change line 16:

Old:
```
<mj-text> Este código es válido por <b>5 minutos</b>. </mj-text>
```

New:
```
<mj-text> Este código es válido por <b>15 minutos</b>. </mj-text>
```

This is a pure copy fix — one character change. Do NOT modify any other part of the template (HTML structure, Jinja2 variables, layout inheritance).
  </action>
  <verify>
    <automated>grep "15 minutos" apps/strapi/src/services/mjml/templates/verification-code.mjml && ! grep "5 minutos" apps/strapi/src/services/mjml/templates/verification-code.mjml</automated>
  </verify>
  <done>
- `verification-code.mjml` contains "15 minutos"
- `verification-code.mjml` does NOT contain "5 minutos"
- No other lines in the template were changed
  </done>
</task>

</tasks>

<verification>
- `grep "15 minutos" apps/strapi/src/services/mjml/templates/verification-code.mjml` returns the corrected line
- `grep "5 minutos" apps/strapi/src/services/mjml/templates/verification-code.mjml` returns nothing
</verification>

<success_criteria>
The verification email template correctly states "15 minutos" — matching the actual server-side `CODE_EXPIRY_MS = 15 * 60 * 1000` constant.
</success_criteria>

<output>
After completion, create `.planning/phases/079-website-verify-flow-mjml-fix/079-02-SUMMARY.md`
</output>
