---
phase: 079-website-verify-flow-mjml-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/scss/components/_verify-code.scss
  - apps/website/app/scss/app.scss
autonomous: true
requirements:
  - PWDR-04
must_haves:
  truths:
    - "The /login/verificar page renders the verify-code form without missing styles"
    - "The form--verify block and its elements have the same spacing as the dashboard equivalent"
  artifacts:
    - path: "apps/website/app/scss/components/_verify-code.scss"
      provides: "BEM styles for .form--verify modifier used by FormVerifyCode.vue"
      contains: ".form--verify"
    - path: "apps/website/app/scss/app.scss"
      provides: "Import registration for verify-code styles"
      contains: "@use \"components/verify-code\""
  key_links:
    - from: "apps/website/app/components/FormVerifyCode.vue"
      to: "apps/website/app/scss/components/_verify-code.scss"
      via: "class=\"form form--verify\""
      pattern: "form--verify"
---

<objective>
Add the missing `_verify-code.scss` stylesheet for the website's FormVerifyCode.vue component and register it in `app.scss`.

Purpose: `FormVerifyCode.vue` uses `class="form form--verify"` but the website has no `_verify-code.scss` — the `form--verify` modifier has no styles. The dashboard has this file; the website does not.
Output: `_verify-code.scss` created, imported in `app.scss`.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/078-dashboard-verify-flow/078-02-SUMMARY.md
</context>

<interfaces>
<!-- Key references for the executor. No codebase exploration needed. -->

From apps/website/app/components/FormVerifyCode.vue (root element):
```html
<div class="form form--verify">
```

From apps/dashboard/app/scss/components/_verify-code.scss (reference implementation):
```scss
@use "../abstracts/mixins" as *;
@use "../abstracts/variables" as *;

.form--verify {
  &__resend {
    margin-top: 15px;
  }
}
```

From apps/website/app/scss/app.scss (tail of imports — add after "checkout"):
```scss
@use "components/checkout";
```
(Add `@use "components/verify-code";` at the very end of the file)

From apps/website/app/scss/abstracts/_variables.scss:
- $charcoal, $cultured, $davys_grey, $dodger_blue, etc. — use sparingly, no colors needed for this component

AGENTS.md BEM rules:
- Block `form`, modifier `verify` → `.form--verify`
- All elements under the modifier namespace: `.form--verify__resend`
- Do NOT use compound block names like `form-verify`
- Do NOT add `box-shadow` or `transform: scale`
- SCSS nesting must mirror HTML hierarchy exactly
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create _verify-code.scss and register in app.scss</name>
  <files>apps/website/app/scss/components/_verify-code.scss, apps/website/app/scss/app.scss</files>
  <action>
Create `apps/website/app/scss/components/_verify-code.scss`:

```scss
@use "../abstracts/mixins" as *;
@use "../abstracts/variables" as *;

.form--verify {
  // Modifier-specific layout: stack the resend wrapper below the verify button

  &__resend {
    margin-top: 15px;
  }
}
```

Then open `apps/website/app/scss/app.scss` and add `@use "components/verify-code";` at the very end of the file (after the last existing `@use` line).

Note: `FormVerifyCode.vue` uses `class="form form--verify"` as the root element. The `form` base class provides the foundation; `form--verify` is the modifier namespace. The `__resend` element does not exist in the website's FormVerifyCode.vue but the modifier block must still be defined for consistent BEM namespacing.

Do NOT add colors, shadows, or transforms — the base `form` styles already provide all necessary styling.
  </action>
  <verify>
    <automated>cd apps/website && yarn nuxt typecheck 2>&1 | tail -5; cd ../.. && ls apps/website/app/scss/components/_verify-code.scss && grep -c "verify-code" apps/website/app/scss/app.scss</automated>
  </verify>
  <done>
- `apps/website/app/scss/components/_verify-code.scss` exists with `.form--verify` block
- `apps/website/app/scss/app.scss` contains `@use "components/verify-code"`
- `yarn nuxt typecheck` passes (no new errors introduced)
  </done>
</task>

</tasks>

<verification>
- `_verify-code.scss` exists and contains `.form--verify`
- `app.scss` imports it via `@use "components/verify-code"`
- No TypeScript errors introduced
- BEM naming follows AGENTS.md convention (block `form`, modifier `verify`)
</verification>

<success_criteria>
The website's `/login/verificar` page renders without broken `form--verify` styles. FormVerifyCode.vue has its SCSS module registered in the global stylesheet.
</success_criteria>

<output>
After completion, create `.planning/phases/079-website-verify-flow-mjml-fix/079-01-SUMMARY.md`
</output>
