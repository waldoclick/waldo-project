---
phase: 05-type-safety
plan: 04
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
files_modified:
  - apps/dashboard/nuxt.config.ts
autonomous: false
requirements:
  - TYPE-03

must_haves:
  truths:
    - "typeCheck: true is set in nuxt.config.ts"
    - "npm run build completes without TypeScript errors in the dashboard"
    - "All type errors surface and are fixed before the plan is marked complete"
  artifacts:
    - path: "apps/dashboard/nuxt.config.ts"
      provides: "Build config with type checking enabled"
      contains: "typeCheck: true"
  key_links:
    - from: "apps/dashboard/nuxt.config.ts"
      to: "apps/dashboard/app/types/*.ts"
      via: "typeCheck triggers tsc over all app/ files"
      pattern: "typeCheck: true"
---

<objective>
Enable typeCheck in nuxt.config.ts and verify that npm run build passes without TypeScript errors in the dashboard.

Purpose: TYPE-03 requires the build to enforce type correctness. This plan flips the flag and surfaces any remaining type errors introduced by Plans 01-03, fixing them before declaring the phase complete.

Output: nuxt.config.ts with typeCheck: true; a clean build.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-type-safety/05-02-SUMMARY.md
@.planning/phases/05-type-safety/05-03-SUMMARY.md

<!-- Current nuxt.config.ts typescript block (line 334-338 approximately) -->
<interfaces>
Current state in apps/dashboard/nuxt.config.ts:
```typescript
typescript: {
  strict: true,
  typeCheck: false, // Disabled by default, enable when ready
},
```

Target state:
```typescript
typescript: {
  strict: true,
  typeCheck: true,
},
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable typeCheck and run build</name>
  <files>apps/dashboard/nuxt.config.ts</files>
  <action>
1. Open `apps/dashboard/nuxt.config.ts` and locate the `typescript` block (around line 334).

2. Change `typeCheck: false` to `typeCheck: true`. Remove the inline comment `// Disabled by default, enable when ready` since the flag is now active:
   ```typescript
   typescript: {
     strict: true,
     typeCheck: true,
   },
   ```

3. Run the build from the dashboard directory to surface type errors:
   ```bash
   cd /home/gabriel/Code/waldo-project/apps/dashboard && npm run build 2>&1 | head -150
   ```

4. If the build fails with TypeScript errors, fix each error by:
   - Adding missing type annotations
   - Replacing remaining `any` with correct types from `@/types/*` or narrower inline types
   - Adding null checks where TypeScript strict mode requires them
   - Do NOT disable strict mode or add `// @ts-ignore` comments

5. Re-run the build after each fix until it passes cleanly.

**Common errors to expect and how to fix them:**
- `Type 'X' is not assignable to type 'Y'` in Strapi response handling — add a type assertion `as Y` after verifying the shape matches
- `Object is possibly null` — add `?.` optional chaining or a null guard
- `Property 'X' does not exist on type 'Ad'` — add the missing field to the interface in `app/types/ad.ts` (or the relevant type file)
- `Parameter 'x' implicitly has an 'any' type` — add an explicit type annotation

The nuxt.config.ts change is the only permanent file change required by this task. Error fixes may touch type files or component files as needed.
  </action>
  <verify>
    <automated>grep "typeCheck: true" /home/gabriel/Code/waldo-project/apps/dashboard/nuxt.config.ts && echo "PASS: typeCheck enabled"</automated>
  </verify>
  <done>nuxt.config.ts has typeCheck: true. The build command runs and all TypeScript errors are resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm clean build with type checking active</name>
  <what-built>
    - typeCheck: true enabled in nuxt.config.ts
    - npm run build executed with type checking active
    - Any TypeScript errors surfaced during build were fixed
  </what-built>
  <how-to-verify>
    1. From `apps/dashboard/`: run `npm run build`
    2. Confirm the build completes with exit code 0 (no TypeScript errors in output)
    3. Confirm `grep "typeCheck" apps/dashboard/nuxt.config.ts` shows `typeCheck: true`
    4. Confirm no `// @ts-ignore` or `@ts-expect-error` directives were added as shortcuts
  </how-to-verify>
  <action>Human verifies build passes with typeCheck active.</action>
  <verify>Build exits 0 with no type errors.</verify>
  <done>Build passes clean with typeCheck: true. Phase 5 complete.</done>
  <resume-signal>Type "approved" if build passes clean. Describe any remaining errors if not.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 complete when all criteria are true:
1. `ls apps/dashboard/app/types/` shows ad.ts, user.ts, order.ts, category.ts, pack.ts
2. `grep -rn "interface Ad {" apps/dashboard/app/components/AdsTable.vue` — no output (inline deleted)
3. `grep "typeCheck: true" apps/dashboard/nuxt.config.ts` — returns match
4. `cd apps/dashboard && npm run build` — exits 0
</verification>

<success_criteria>
typeCheck: true is active in nuxt.config.ts. npm run build completes without TypeScript errors. The dashboard build enforces type correctness for all domain entities.
</success_criteria>

<output>
After completion, create `.planning/phases/05-type-safety/05-04-SUMMARY.md`
</output>
