---
phase: 05-type-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/dashboard/app/components/UsersDefault.vue
  - apps/dashboard/app/components/OrdersDefault.vue
  - apps/dashboard/app/components/CategoriesDefault.vue
  - apps/dashboard/app/components/PacksDefault.vue
  - apps/dashboard/app/pages/usuarios/[id].vue
  - apps/dashboard/app/pages/ordenes/[id].vue
autonomous: true
requirements:
  - TYPE-02

must_haves:
  truths:
    - "UsersDefault.vue, OrdersDefault.vue, CategoriesDefault.vue, PacksDefault.vue each import their domain type from app/types/"
    - "No inline User, Order, Category, or Pack interface exists in any of these component files"
    - "usuarios/[id].vue and ordenes/[id].vue replace ref<any> and parameter any with typed equivalents"
  artifacts:
    - path: "apps/dashboard/app/components/UsersDefault.vue"
      provides: "User list component using shared User type"
      contains: "import type { User } from '@/types/user'"
    - path: "apps/dashboard/app/components/OrdersDefault.vue"
      provides: "Orders list component using shared Order type"
      contains: "import type { Order, OrdersListResponse } from '@/types/order'"
    - path: "apps/dashboard/app/components/CategoriesDefault.vue"
      provides: "Categories list component using shared Category type"
      contains: "import type { Category } from '@/types/category'"
    - path: "apps/dashboard/app/components/PacksDefault.vue"
      provides: "Packs list component using shared Pack type"
      contains: "import type { Pack } from '@/types/pack'"
  key_links:
    - from: "apps/dashboard/app/types/user.ts"
      to: "apps/dashboard/app/components/UsersDefault.vue"
      via: "import { User } from '@/types/user'"
      pattern: "import.*User.*from.*@/types/user"
    - from: "apps/dashboard/app/types/order.ts"
      to: "apps/dashboard/app/components/OrdersDefault.vue"
      via: "import { Order, OrdersListResponse } from '@/types/order'"
      pattern: "import.*Order.*from.*@/types/order"
---

<objective>
Update UsersDefault, OrdersDefault, CategoriesDefault, PacksDefault, and the user/order detail pages to import their domain types from app/types/, replacing all inline interfaces and : any for domain fields.

Purpose: Completes TYPE-02 for the User, Order, Category, and Pack domains. Runs in parallel with Plan 02 since different files are touched.

Output: Four list components and two detail pages using shared types — no inline domain interfaces remain.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-type-safety/05-01-SUMMARY.md

<!-- Shared types created by Plan 01 — use these exactly -->
<interfaces>
From apps/dashboard/app/types/user.ts:
```typescript
export interface UserRole { name: string; id?: number; }
export interface UserRelation { name?: string; id?: number; }
export interface User {
  id: number; username: string; email: string;
  firstname?: string; lastname?: string;
  rut?: string; phone?: string; birthdate?: string;
  address?: string; address_number?: string; postal_code?: string;
  pro?: boolean; is_company?: boolean;
  createdAt: string; updatedAt?: string;
  role?: UserRole; region?: UserRelation; commune?: UserRelation;
}
```

From apps/dashboard/app/types/order.ts:
```typescript
export interface OrderUser extends Pick<User, "username"|"email"|"firstname"|"lastname"|"phone"> {}
export interface OrderAd { id: number | string; name: string; slug?: string; }
export interface Order {
  id: number; documentId?: string; buy_order?: string;
  amount: number | string; payment_method: string; is_invoice: boolean;
  items?: string; payment_response?: string;
  document_details?: string; document_response?: string;
  createdAt: string; updatedAt?: string;
  user?: OrderUser; ad?: OrderAd | null;
}
export interface OrdersListResponse {
  data: Order[];
  meta: { pagination: { page: number; pageCount: number; total: number; }; };
}
```

From apps/dashboard/app/types/category.ts:
```typescript
export interface Category {
  id: number; name: string; color?: string; createdAt?: string; updatedAt: string;
}
```

From apps/dashboard/app/types/pack.ts:
```typescript
export interface Pack {
  id: number; name: string; text?: string;
  total_days: number; total_ads: number; total_features: number;
  price: number; description?: string; createdAt: string; updatedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline interfaces in the four list components</name>
  <files>
    apps/dashboard/app/components/UsersDefault.vue
    apps/dashboard/app/components/OrdersDefault.vue
    apps/dashboard/app/components/CategoriesDefault.vue
    apps/dashboard/app/components/PacksDefault.vue
  </files>
  <action>
For each file, make targeted changes in the `<script setup lang="ts">` block only:

**UsersDefault.vue:**
1. Add: `import type { User } from "@/types/user";`
2. Delete the inline `interface User { ... }` block (lines 97–105)
3. Replace `const searchParams: any = {` with `const searchParams: Record<string, unknown> = {`
4. The `allUsers` ref is already typed as `ref<User[]>([])` — this now correctly uses the imported type

**OrdersDefault.vue:**
1. Add: `import type { Order, OrdersListResponse } from "@/types/order";`
2. Delete the inline `interface Order { ... }` block (lines 88–98)
3. Delete the inline `interface OrdersListResponse { ... }` block (lines 100–103)
4. Replace `sort: sortParam.value as any` with `sort: sortParam.value as string`
5. The `useAsyncData` return type annotation `async (): Promise<OrdersListResponse>` already uses the now-imported interface — no change needed there

**CategoriesDefault.vue:**
1. Add: `import type { Category } from "@/types/category";`
2. Delete the inline `interface Category { ... }` block (lines 109–114)
3. Replace `const searchParams: any = {` with `const searchParams: Record<string, unknown> = {`
4. The `allCategories` ref is already typed as `ref<Category[]>([])` — this now correctly uses the imported type

**PacksDefault.vue:**
1. Add: `import type { Pack } from "@/types/pack";`
2. Delete the inline `interface Pack { ... }` block (lines 99–110)
3. Replace `const searchParams: any = {` with `const searchParams: Record<string, unknown> = {`
4. The `allPacks` ref is already typed as `ref<Pack[]>([])` — this now correctly uses the imported type

Do NOT change templates or styles. Only modify the script block for each file.
  </action>
  <verify>
    <automated>grep -rn "import type.*from.*@/types/" /home/gabriel/Code/waldo-project/apps/dashboard/app/components/UsersDefault.vue /home/gabriel/Code/waldo-project/apps/dashboard/app/components/OrdersDefault.vue /home/gabriel/Code/waldo-project/apps/dashboard/app/components/CategoriesDefault.vue /home/gabriel/Code/waldo-project/apps/dashboard/app/components/PacksDefault.vue && echo "PASS: all four components have shared type imports"</automated>
  </verify>
  <done>All four list components import their domain type from app/types/. Inline interface blocks are deleted. No : any remains for domain fields (searchParams uses Record&lt;string, unknown&gt;).</done>
</task>

<task type="auto">
  <name>Task 2: Replace any in user and order detail pages</name>
  <files>
    apps/dashboard/app/pages/usuarios/[id].vue
    apps/dashboard/app/pages/ordenes/[id].vue
  </files>
  <action>
**usuarios/[id].vue:**
1. Add import: `import type { User, UserRelation } from "@/types/user";`
2. Find `const item = ref<any>(null)` and replace with: `const item = ref<User | null>(null);`
3. Find `const getRelationName = (relation?: any)` and replace parameter type:
   ```typescript
   const getRelationName = (relation?: UserRelation) => {
   ```
4. Find `const normalizeUser = (response: any)` (or equivalent normalization function) and type it:
   ```typescript
   const normalizeUser = (response: unknown): User | null => {
     if (!response) return null;
     if (typeof response === 'object' && response !== null) {
       if ('data' in response) return (response as { data: User }).data;
       if ('id' in response) return response as User;
     }
     return null;
   };
   ```
   If the function already has type narrowing logic, keep the logic and just update the signature.

**ordenes/[id].vue:**
1. Add import: `import type { Order } from "@/types/order";`
2. Find `const order = ref<any>(null)` and replace with: `const order = ref<Order | null>(null);`
3. Find `const formatFullName = (user: any)` and replace parameter type:
   ```typescript
   const formatFullName = (user: { firstname?: string; lastname?: string } | null | undefined) => {
   ```
   (Using a minimal inline type here is correct — this is a utility function, not a domain binding.)
4. Find `const normalizeOrder = (response: any)` and type it:
   ```typescript
   const normalizeOrder = (response: unknown): Order | null => {
     if (!response) return null;
     if (typeof response === 'object' && response !== null) {
       if ('data' in response) return (response as { data: Order }).data;
       if ('order' in response) return (response as { order: Order }).order;
       if ('id' in response) return response as Order;
     }
     return null;
   };
   ```

Do NOT change templates, styles, or component logic flow. Only update types in the script block.
  </action>
  <verify>
    <automated>grep -n "ref<any>" /home/gabriel/Code/waldo-project/apps/dashboard/app/pages/usuarios/[id].vue /home/gabriel/Code/waldo-project/apps/dashboard/app/pages/ordenes/[id].vue && echo "FAIL: ref<any> still present" || echo "PASS: no ref<any> in detail pages"</automated>
  </verify>
  <done>usuarios/[id].vue uses ref&lt;User | null&gt; and typed getRelationName/normalizeUser. ordenes/[id].vue uses ref&lt;Order | null&gt; and typed normalizeOrder/formatFullName. No ref&lt;any&gt; remains in either file.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep -rn "interface User {" apps/dashboard/app/components/UsersDefault.vue` — no output
- `grep -rn "interface Order {" apps/dashboard/app/components/OrdersDefault.vue` — no output
- `grep -rn "interface Category {" apps/dashboard/app/components/CategoriesDefault.vue` — no output
- `grep -rn "interface Pack {" apps/dashboard/app/components/PacksDefault.vue` — no output
- `grep -rn "ref<any>" apps/dashboard/app/pages/usuarios/[id].vue apps/dashboard/app/pages/ordenes/[id].vue` — no output
</verification>

<success_criteria>
All four list components and both detail pages use shared types from app/types/. No inline domain interfaces exist in these files. No ref&lt;any&gt; for domain data. The : any usages that remain are only in non-domain utility contexts (Strapi params typed as Record&lt;string, unknown&gt;).
</success_criteria>

<output>
After completion, create `.planning/phases/05-type-safety/05-03-SUMMARY.md`
</output>
