---
phase: 07-publico-vistas-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/por-que-waldo.vue
  - apps/website/app/components/MenuDefault.vue
  - apps/website/app/components/HighlightsDefault.vue
  - apps/website/app/components/HowtoDefault.vue
  - apps/website/app/components/PacksDefault.vue
  - apps/website/app/components/FaqDefault.vue
  - apps/website/app/scss/components/_why.scss
  - apps/website/app/scss/components/_highlights.scss
  - apps/website/app/scss/components/_howto.scss
  - apps/website/app/scss/components/_packs.scss
  - apps/website/app/scss/components/_faq.scss
  - apps/website/app/scss/components/_accordion.scss
  - apps/website/app/scss/app.scss
autonomous: true
requirements: [PUB-WHY]
must_haves:
  truths:
    - "/por-que-waldo renders as its own route to the 'Por qué Waldo' mockup screen (hero + cómo funciona + vendedores + packs + dark CTA + FAQ), logged-OUT, desktop 1440 + mobile 390"
    - "The page OWNS and restyles the Highlights/Howto/Packs/Faq section components + the accordion to the mockup (these no longer live on Home; this plan is the sole owner for the phase)"
    - "The header/footer nav 'Por qué Waldo' link points to /por-que-waldo (not the old /#por-que-waldo anchor)"
    - "Page hero clears the fixed/overlay headroom header (~128px top padding) — no content hidden behind the bar"
    - "Colors/typography use phase-04 tokens; no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/pages/por-que-waldo.vue"
      provides: "Por qué Waldo route, modeled on index.vue (closest sibling), single data load"
      contains: "useAsyncData"
    - path: "apps/website/app/scss/components/_accordion.scss"
      provides: "Restyled accordion (amber-tint chevron pill, $line borders) — OWNER for the whole phase (07-03/04/05 reuse it)"
    - path: "apps/website/app/components/MenuDefault.vue"
      provides: "Nav link updated to /por-que-waldo"
      contains: "/por-que-waldo"
  key_links:
    - from: "apps/website/app/pages/por-que-waldo.vue"
      to: "restyled section components (Highlights/Howto/Packs/Faq)"
      via: "component composition"
      pattern: "<HowtoDefault|<PacksDefault|<FaqDefault"
    - from: "apps/website/app/components/MenuDefault.vue"
      to: "/por-que-waldo route"
      via: "NuxtLink to"
      pattern: "to=\"/por-que-waldo\""
---

<objective>
Create the dedicated "Por qué Waldo" public page as its own route (`/por-que-waldo`) matching the UPDATED mockup screen (`apps/design/index.dc.html`, `data-screen-label="Por qué Waldo"`, currently lines ~441–633), RESTYLE the explanatory/commercial section components it composes, and update the nav link to point at the new route.

Purpose: The updated mockup promotes "Por qué Waldo" from a home anchor to a full standalone screen (own hero + "para comprar"/cómo funciona + vendedores + packs + dark CTA + FAQ). These Highlights/Howto/Packs/Faq bands + the accordion USED to be on Home; in the updated mockup they live ONLY here, so THIS PLAN now OWNS their restyle (07-01 no longer touches them). Today the nav links to `/#por-que-waldo` (a home anchor) and no page exists.
Output: `pages/por-que-waldo.vue` (composition only, single data load) + restyled Highlights/Howto/Packs/Faq components + restyled accordion (owner) + a thin `_why.scss` for the hero/CTA bands + nav link updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — re-locate with `grep -n 'data-screen-label' index.dc.html`; "Por qué Waldo" screen
# currently spans the data-screen-label div (~441) through the FAQ section (~633). Line numbers shift between drops.
@apps/design/index.dc.html
# Closest sibling to MODEL the new page on (CLAUDE.md: replicate closest existing equivalent):
@apps/website/app/pages/index.vue
# Components RESTYLED + reused here (this plan OWNS their restyle — do NOT defer to 07-01):
@apps/website/app/components/HighlightsDefault.vue
@apps/website/app/components/HowtoDefault.vue
@apps/website/app/components/PacksDefault.vue
@apps/website/app/components/FaqDefault.vue
@apps/website/app/components/MenuDefault.vue

<interfaces>
<!-- phase-04 tokens (do not modify/invent): $ink #26252b, $ink2 #56535f, $muted #8a8794,
     $amber #f7c97e, $amber_hover #efb85c, $amber_tint #fef8ec, $tag #a9772e, $cream #f6f4f1, $line #ece9e4.

  "Por qué Waldo" mockup view (index.dc.html ~441–633), in order:
  - HERO band (~442–453): $cream, amber radial glow, centered eyebrow tag, h1 "Conectamos a quien
    compra y vende activos industriales" (amber underline on "compra y vende"), $ink2 intro, 2 CTAs
    ("Explorar anuncios" amber + "Anunciar ahora" outline). padding-top 128px (clears headroom header).
  - "Para comprar" eyebrow + "Encuentra el equipo que necesitas" header (~455–464).
  - CÓMO FUNCIONA — 3 numbered cards (~465–498): big ghost number, amber-gradient icon tile, "Paso 0N"
    eyebrow ($tag), Poppins title, $ink2 body.  → HighlightsDefault (RESTYLE here).
  - VENDEDORES — 3-step (~500–531): steps-01/02/03 svgs, amber 30px numbered badges, centered, amber
    "Anunciar ahora" CTA.  → HowtoDefault (RESTYLE here).
  - PACKS grid (~533–578): 3 cards, middle "★ Recomendado" with 2px amber border + badge + translateY,
    "Ahorra N%" chips, check rows, amber CTA on recommended / outline on others.  → PacksDefault (RESTYLE).
  - dark "Empieza ahora" CTA band (~580–595): $ink panel, amber glow, 2 CTAs.
  - FAQ (~597–633): centered, h2 Poppins 800, accordion list.  → FaqDefault + accordion (RESTYLE here).

  ACCORDION (`.accordion`, `_accordion.scss`) — THIS PLAN IS THE OWNER for the phase. Restyle to the
  mockup accordion: item = $line border 4px radius, header Poppins 600 ~17px $ink, right-side ~30px square
  pill with amber-tint background + chevron that ROTATES on open (rotate allowed per CLAUDE.md; scale banned).
  07-03/04/05 REUSE this restyle and must NOT re-touch _accordion.scss.

  FaqDefault CONTRACT for 07-03/04/05: REMOVE the internal default-title fallback
  `const title = props.title ?? "Preguntas Frecuentes";` (FaqDefault.vue ~line 57). The heading must render
  ONLY when `title` is explicitly provided, so the help-center pages (07-03/04/05) can omit `title` and let
  the about layout supply the single h1. Make `title`/`text` purely opt-in (no heading/intro markup when absent).
  On THIS page, pass FaqDefault an explicit `:title`/`:text` so its heading renders.

  Current nav (MenuDefault.vue ~line 9): <NuxtLink to="/#por-que-waldo" ...> → change to /por-que-waldo.
  Register any NEW scss partial in app.scss (e.g. _why.scss if created; _highlights/_howto/_packs/_faq/
  _accordion already registered). Reference apps/design/screenshots for packs look if a packs PNG exists. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the explanatory + commercial section components + accordion (OWNER for the phase)</name>
  <files>apps/website/app/components/HighlightsDefault.vue, apps/website/app/components/HowtoDefault.vue, apps/website/app/components/PacksDefault.vue, apps/website/app/components/FaqDefault.vue, apps/website/app/scss/components/_highlights.scss, apps/website/app/scss/components/_howto.scss, apps/website/app/scss/components/_packs.scss, apps/website/app/scss/components/_faq.scss, apps/website/app/scss/components/_accordion.scss</files>
  <action>
    Restyle the section components to the "Por qué Waldo" mockup. These render ONLY on this screen now (they were removed from Home in 07-01), so restyle them here and verify them on /por-que-waldo.
    - HIGHLIGHTS (`.highlights--default`) → "cómo funciona" cards (mockup ~465–498): numbered card with big ghost "0N", amber-gradient icon tile, "Paso 0N" $tag eyebrow, Poppins title, $ink2 body. Per CLAUDE.md keep transitions simple (the mockup's hover translateY/shadow is decorative — do NOT add box-shadow/scale; use borders/background to convey the card).
    - HOWTO (`.howto--default`) → "vendedores" 3-step (mockup ~500–531): steps-01/02/03 svgs, amber 30px numbered badges, centered layout, amber "Anunciar ahora" CTA.
    - PACKS (`.packs--default`) → packs grid (mockup ~533–578): 3 cards, "Ahorra N%" chips, middle "★ Recomendado" with 2px amber border + badge (translateY allowed; no scale), check rows, amber CTA on recommended / outline CTA on others.
    - FAQ (`.faq--default`, FaqDefault.vue): restyle to the centered FAQ block (mockup ~597–633): h2 Poppins 800, accordion list. CRITICAL CONTRACT: REMOVE the internal default-title fallback `const title = props.title ?? "Preguntas Frecuentes";` — the heading renders ONLY when `title` is explicitly provided (so 07-03/04/05 can omit it and let the about layout supply the single h1). Make `title`/`text` purely opt-in (no heading/intro markup when the prop is absent).
    - ACCORDION (`.accordion`, `_accordion.scss`): THIS PLAN IS THE OWNER for the phase. Restyle to the mockup accordion: item = $line border 4px radius, header Poppins 600 ~17px $ink, right-side ~30px square pill with amber-tint background + chevron that rotates on open (rotate allowed; scale banned). 07-03/04/05 REUSE this and must NOT re-touch _accordion.scss.
    Tokens only; BEM strict (`.highlights--default__*`, `.howto--default__*`, `.packs--default__*`, `.faq--default__*`, `.accordion__*`; no standalone hyphenated classes); no existing-variable edits (add new only if a mockup color is absent).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "Highlights|Howto|Packs|Faq" || echo "no section type errors"</automated>
    ! grep -n 'props.title ?? ' apps/website/app/components/FaqDefault.vue  →  default-title fallback removed
    (Visual verification happens in Task 2 once the page composes these components.)
  </verify>
  <done>Highlights/Howto/Packs/FAQ + accordion restyled to the mockup; FaqDefault default-title fallback removed (heading opt-in); accordion restyle final (owner); tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create /por-que-waldo page (model on index.vue) composing the restyled sections + hero/CTA bands</name>
  <files>apps/website/app/pages/por-que-waldo.vue, apps/website/app/scss/components/_why.scss, apps/website/app/scss/app.scss</files>
  <action>
    Create `pages/por-que-waldo.vue` modeled EXACTLY on `index.vue` structure (imports, useAsyncData pattern, $setSEO/$setStructuredData). Compose, in mockup order (~441–633):
      hero band (`.why--default__hero`) → HighlightsDefault (cómo funciona, with the "para comprar" eyebrow header) → HowtoDefault (vendedores) → PacksDefault → dark CTA band (`.why--default__cta`) → FaqDefault (pass explicit `:title`/`:text`).
    The hero and dark-CTA bands are NOT covered by existing section components. Pages are composition-only per CLAUDE.md, so implement them as a small BEM block `.why--default` in a dedicated component or as elements rendered via `_why.scss` classes (read the closest existing hero/CTA component first — prefer reuse over new SCSS; HeroDefault may fit the hero). Do NOT put raw BEM/HTML sections in the page file.
    DATA: single useAsyncData (key "why-data") loading only what the composed sections need (packs via ad-packs — copy the loader shape from the OLD index.vue / pages that load ad-packs; faqs via faqsStore.loadFaqs) with Promise.all + object default. Categories/featured ads are NOT shown here — do not load them. No bare-await+useAsyncData. Hero top padding ~128px to clear the headroom header (06-02).
    Register `_why.scss` in app.scss only if you create it. Tokens only; BEM strict (`.why--default__*`); no existing-variable edits; new variable only if a mockup color is absent.
    SEO: $setSEO title/description for "Por qué Waldo" + appropriate structured data (WebPage).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "por-que-waldo" || echo "no por-que-waldo type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/por-que-waldo.vue  →  MUST equal 1
    grep "default: () =>" apps/website/app/pages/por-que-waldo.vue  →  present
    VISUAL LOOP (logged-OUT, dev server on :3000; live mockup on :3002): screenshot http://localhost:3000/por-que-waldo full-page at 1440x900 and 390x844 → /tmp/waldo-shots/07-02-why-{desktop,mobile}.png → Read each → compare element-by-element vs apps/design/index.dc.html "Por qué Waldo" screen (~441–633): hero glow/eyebrow/h1/CTAs, cómo-funciona cards, vendedores steps, packs grid (recomendado), dark CTA, FAQ accordion → fix → repeat until match.
  </verify>
  <done>/por-que-waldo exists, composition-only, single useAsyncData with default, composes the (Task 1) restyled sections + hero/CTA bands, hero clears the header; matches the mockup desktop+mobile; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Point nav link to /por-que-waldo</name>
  <files>apps/website/app/components/MenuDefault.vue</files>
  <action>
    In MenuDefault.vue change the "Por qué Waldo" link `to="/#por-que-waldo"` → `to="/por-que-waldo"` (keep the title attribute and label). Grep the codebase for any other `/#por-que-waldo` reference (footer menu, etc.) and update consistently to `/por-que-waldo`. Do NOT change the Spanish label text.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && ! grep -rn "/#por-que-waldo" apps/website/app && echo "no stale anchor links remain"</automated>
    VISUAL LOOP (logged-OUT, :3000): confirm clicking the header "Por qué Waldo" nav navigates to /por-que-waldo.
  </verify>
  <done>Nav link points to /por-que-waldo (no stale anchors anywhere in app/).</done>
</task>

</tasks>

<verification>
- /por-que-waldo is a real route, composition-only, single useAsyncData with default.
- Highlights/Howto/Packs/Faq + accordion restyled here (owner); FaqDefault default-title fallback removed.
- No `/#por-que-waldo` references remain.
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT, vs the "Por qué Waldo" mockup screen.
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server (compare vs :3002 live mockup); no full test suite.
</verification>

<success_criteria>
"Por qué Waldo" is its own route matching the updated mockup, with the Highlights/Howto/Packs/Faq sections + accordion restyled here (owner) and the nav updated to point at it; hero clears the headroom header.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-02-SUMMARY.md`.
</output>
