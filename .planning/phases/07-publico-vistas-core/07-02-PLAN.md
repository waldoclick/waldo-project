---
phase: 07-publico-vistas-core
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/website/app/pages/por-que-waldo.vue
  - apps/website/app/components/MenuDefault.vue
  - apps/website/app/scss/components/_why.scss
  - apps/website/app/scss/app.scss
autonomous: true
requirements: [PUB-WHY]
must_haves:
  truths:
    - "/por-que-waldo renders as its own route to the 'Por qué Waldo' mockup screen, logged-OUT, desktop 1440 + mobile 390"
    - "The page reuses the Home section components restyled in 07-01 (Highlights/Howto/Packs/Faq) — composition only, no duplicated section SCSS"
    - "The header/footer nav 'Por qué Waldo' link points to /por-que-waldo (not the old /#por-que-waldo anchor)"
    - "Page hero clears the fixed/overlay headroom header (~130px top padding)"
  artifacts:
    - path: "apps/website/app/pages/por-que-waldo.vue"
      provides: "Por qué Waldo route, modeled on index.vue (closest sibling)"
      contains: "useAsyncData"
    - path: "apps/website/app/components/MenuDefault.vue"
      provides: "Nav link updated to /por-que-waldo"
      contains: "/por-que-waldo"
  key_links:
    - from: "apps/website/app/pages/por-que-waldo.vue"
      to: "07-01 restyled section components"
      via: "component composition"
      pattern: "<HowtoDefault|<PacksDefault|<FaqDefault"
    - from: "apps/website/app/components/MenuDefault.vue"
      to: "/por-que-waldo route"
      via: "NuxtLink to"
      pattern: "to=\"/por-que-waldo\""
---

<objective>
Create the dedicated "Por qué Waldo" public page as its own route (`/por-que-waldo`) matching the mockup screen (`design/index.dc.html` lines 408–563), reusing the Home section components restyled in 07-01, and update the nav link to point at the new route.

Purpose: The mockup promotes "Por qué Waldo" from a home anchor to a full standalone screen (own hero + cómo funciona + vendedores + packs + dark CTA + FAQ). Today the nav links to `/#por-que-waldo` (a home anchor) and no page exists.
Output: `pages/por-que-waldo.vue` (composition only, single data load) + nav link updated + a thin page-level `_why.scss` for the hero/CTA bands the shared components don't cover.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@design/index.dc.html
# Closest sibling to MODEL the new page on (CLAUDE.md: replicate closest existing equivalent):
@apps/website/app/pages/index.vue
# Components reused (restyled in 07-01 — do NOT re-restyle here):
@apps/website/app/components/HighlightsDefault.vue
@apps/website/app/components/HowtoDefault.vue
@apps/website/app/components/PacksDefault.vue
@apps/website/app/components/FaqDefault.vue
@apps/website/app/components/MenuDefault.vue

<interfaces>
<!-- "Por qué Waldo" mockup view = index.dc.html lines 408–563:
  - hero band ($cream, amber radial glow, eyebrow tag, h1 "Conectamos a quien compra y vende...", 2 CTAs)
  - "Para comprar" eyebrow + "Cómo funciona" 3 cards   → HighlightsDefault (restyled 07-01)
  - "Vendedores" 3-step (steps-01/02/03)               → HowtoDefault (restyled 07-01)
  - PACKS grid                                         → PacksDefault (restyled 07-01)
  - dark "Empieza ahora" CTA band
  - FAQ                                                → FaqDefault (restyled 07-01)
phase-04 tokens: $ink #26252b, $ink2 #56535f, $muted #8a8794, $amber #f7c97e, $amber_hover #efb85c, $tag #a9772e, $cream #f6f4f1, $line #ece9e4.
Current nav (MenuDefault.vue line 9): <NuxtLink to="/#por-que-waldo" ...> -> change to /por-que-waldo. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /por-que-waldo page (model on index.vue) reusing restyled sections + hero/CTA bands</name>
  <files>apps/website/app/pages/por-que-waldo.vue, apps/website/app/scss/components/_why.scss, apps/website/app/scss/app.scss</files>
  <action>
    Create `pages/por-que-waldo.vue` modeled EXACTLY on `index.vue` structure (imports, useAsyncData pattern, $setSEO/$setStructuredData). Compose, in mockup order:
      hero band (`.why--default__hero`) → HighlightsDefault (cómo funciona) → HowtoDefault (vendedores) → PacksDefault → dark CTA band (`.why--default__cta`) → FaqDefault.
    The hero and dark-CTA bands are NOT covered by existing section components — implement them as a small new BEM block `.why--default` with its markup in the page is NOT allowed (pages are composition-only per CLAUDE.md). Instead: put the hero + dark CTA as elements of `.why--default` and render them via a thin inline section element list that maps to `_why.scss` classes — keep the page lean. (If a closer existing hero/CTA component exists, reuse it; HeroFake is empty and HeroDefault may fit — read the closest sibling first and prefer reuse over new SCSS.)
    DATA: single useAsyncData (key "why-data") loading only what the composed sections need (packs via ad-packs, faqs via faqsStore) with Promise.all + object default. Categories are NOT shown on this screen — do not load them. No bare-await+useAsyncData. Hero top padding ~130px to clear the headroom header (06-02).
    Register `_why.scss` in app.scss only if you create it. Tokens only; BEM strict (`.why--default__*`, no standalone hyphenated classes); no existing-variable edits; new variable only if the mockup needs an absent color.
    SEO: $setSEO title/description for "Por qué Waldo" + appropriate structured data (WebPage).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "por-que-waldo" || echo "no por-que-waldo type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/por-que-waldo.vue  →  MUST equal 1
    grep "default: () =>" apps/website/app/pages/por-que-waldo.vue  →  present
  </verify>
  <done>/por-que-waldo exists, composition-only, single useAsyncData with default, reuses 07-01 section components, hero clears the header; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Point nav link to /por-que-waldo + visual-verify the page</name>
  <files>apps/website/app/components/MenuDefault.vue</files>
  <action>
    In MenuDefault.vue change the "Por qué Waldo" link `to="/#por-que-waldo"` → `to="/por-que-waldo"` (keep the title attribute and label). Grep the codebase for any other `/#por-que-waldo` reference (footer menu, etc.) and update consistently to `/por-que-waldo`. Do NOT change the Spanish label text.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && ! grep -rn "/#por-que-waldo" apps/website/app && echo "no stale anchor links remain"</automated>
    VISUAL LOOP (logged-OUT, dev server on :3000): screenshot http://localhost:3000/por-que-waldo full-page at 1440x900 and 390x844 → save /tmp/waldo-shots/07-02-why-{desktop,mobile}.png → Read each → compare element-by-element vs design/index.dc.html "Por qué Waldo" screen (lines 408–563): hero glow/eyebrow/h1/CTAs, cómo-funciona cards, vendedores steps, packs grid, dark CTA, FAQ → fix → repeat until match. Confirm clicking the header "Por qué Waldo" nav navigates to /por-que-waldo.
  </verify>
  <done>Nav link points to /por-que-waldo (no stale anchors anywhere in app/); the page matches the mockup on desktop+mobile, logged-OUT.</done>
</task>

</tasks>

<verification>
- /por-que-waldo is a real route, composition-only, single useAsyncData with default.
- Section components are reused (not re-restyled) — _highlights/_howto/_packs/_faq/_accordion are NOT in this plan's files_modified.
- No `/#por-que-waldo` references remain.
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT.
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server; no full test suite.
</verification>

<success_criteria>
"Por qué Waldo" is its own route matching the mockup, reusing Home's restyled sections, with the nav updated and the hero clearing the headroom header.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-02-SUMMARY.md`.
</output>
