---
phase: 07-publico-vistas-core
plan: 05
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - apps/website/app/pages/condiciones-de-uso.vue
  - apps/website/app/components/TermsDefault.vue
  - apps/website/app/scss/components/_terms.scss
autonomous: true
requirements: [PUB-TERMS]
must_haves:
  truths:
    - "Condiciones de uso renders inside the help-center layout (07-03) with breadcrumb header + sticky sidebar (Condiciones active) + accordion panel"
    - "The page shows a single h1 (from the layout header) — the duplicate component h1/intro is removed"
    - "The accordion uses the restyle shipped in 07-02 (no re-styling of _accordion.scss here)"
  artifacts:
    - path: "apps/website/app/pages/condiciones-de-uso.vue"
      provides: "Terms page wrapped in <NuxtLayout name=\"about\" active=\"cond\">"
      contains: "NuxtLayout"
    - path: "apps/website/app/components/TermsDefault.vue"
      provides: "Content-panel component without duplicate h1/intro"
  key_links:
    - from: "apps/website/app/pages/condiciones-de-uso.vue"
      to: "apps/website/app/layouts/about.vue (07-03)"
      via: "<NuxtLayout name=\"about\" :title :intro active=\"cond\">"
      pattern: "NuxtLayout"
---

<objective>
Restyle "Condiciones de uso" to the mockup by reusing the help-center layout built in 07-03 — pass title/intro/active, strip the duplicate component heading, and present the terms as the restyled accordion in the content panel.

Purpose: Terms is a pure consumer of the 07-03 layout + 07-02 accordion. Mirror exactly what 07-04 did for Políticas (same pattern, terms content).
Output: condiciones-de-uso.vue using NuxtLayout; TermsDefault rendering only the accordion content (no own h1/intro); panel matches the mockup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@apps/design/index.dc.html
@apps/design/screenshots/legal-faq.png

# UPDATE these (closest existing equivalents):
@apps/website/app/pages/condiciones-de-uso.vue
@apps/website/app/components/TermsDefault.vue
# Sibling pattern just applied (model on it):
@apps/website/app/pages/politicas-de-privacidad.vue

<interfaces>
<!-- Reuse the 07-03 contract:
   <NuxtLayout name="about" title="Condiciones de uso" intro="..." active="cond">
     <TermsDefault :terms="terms || []" />
   </NuxtLayout>
   about.vue renders breadcrumb header (Waldo > Condiciones de uso) + h1 + intro + sticky sidebar
   (Condiciones highlighted) + content slot. TermsDefault must DROP its own <h1> + hero intro block.
   Accordion visual already restyled in 07-02 — DO NOT touch _accordion.scss.
   Current data load (1 useAsyncData "terms" with default) is already optimal — keep as-is. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Condiciones into the help-center layout + strip duplicate heading + fit panel</name>
  <files>apps/website/app/pages/condiciones-de-uso.vue, apps/website/app/components/TermsDefault.vue, apps/website/app/scss/components/_terms.scss</files>
  <action>
    Page: replace `definePageMeta({ layout: "about" })` with
      `<NuxtLayout name="about" title="Condiciones de uso" intro="<short intro per mockup tone>" active="cond"><TermsDefault :terms="terms || []" /></NuxtLayout>`.
    Add `definePageMeta({ layout: false })` to the page script (REQUIRED — an explicit <NuxtLayout> wrapper double-wraps without it). Keep the existing single useAsyncData ("terms", has default) and $setSEO/$setStructuredData unchanged. API already optimal (1 load) — note it.
    TermsDefault: remove the duplicate `<h1 class="terms--default__title">` and the `terms--default__hero` intro block (now in the layout header). Render only `<AccordionDefault :questions="terms" />` in the content panel. `_terms.scss` becomes a thin panel wrapper (spacing only); do NOT restyle/duplicate accordion rules (owned by 07-02 _accordion.scss). BEM strict; tokens only; no existing-variable edits; delete rules left dead by removing the hero/title.
    Model the change EXACTLY on the 07-04 Políticas change (same structure, terms data).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "condiciones-de-uso|TermsDefault" || echo "no terms type errors"</automated>
    grep "NuxtLayout" apps/website/app/pages/condiciones-de-uso.vue  →  present
    grep "layout: false" apps/website/app/pages/condiciones-de-uso.vue  →  present
    ! grep -n "terms--default__title\|terms--default__hero" apps/website/app/components/TermsDefault.vue  →  removed
    VISUAL LOOP (logged-OUT, :3000; live mockup on :3002): screenshot http://localhost:3000/condiciones-de-uso at 1440 + 390 → /tmp/waldo-shots/07-05-cond-{desktop,mobile}.png → Read → compare vs the mockup legal accordion screen + apps/design/screenshots/legal-faq.png (breadcrumb header, sidebar "Condiciones" active, accordion) → fix → repeat until match. Confirm exactly one h1.
  </verify>
  <done>Condiciones page uses NuxtLayout (active="cond"), single h1, accordion panel matches the mockup on desktop+mobile; no accordion re-styling; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Page wrapped in <NuxtLayout name="about" active="cond">; one h1 only.
- `_accordion.scss` NOT in diff (owned by 07-02).
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT.
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server; no full test suite.
</verification>

<success_criteria>
Condiciones de uso matches the mockup by reusing the 07-03 layout and 07-02 accordion, with the duplicate heading removed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-05-SUMMARY.md`.
</output>
