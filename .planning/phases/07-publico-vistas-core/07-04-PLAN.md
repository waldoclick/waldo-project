---
phase: 07-publico-vistas-core
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - apps/website/app/pages/politicas-de-privacidad.vue
  - apps/website/app/components/PoliciesDefault.vue
  - apps/website/app/scss/components/_policies.scss
autonomous: true
requirements: [PUB-PRIV]
must_haves:
  truths:
    - "Políticas de privacidad renders inside the help-center layout (07-03) with breadcrumb header + sticky sidebar (Políticas active) + accordion panel"
    - "The page shows a single h1 (from the layout header) — the duplicate component h1/intro is removed"
    - "The accordion uses the restyle shipped in 07-02 (no re-styling of _accordion.scss here)"
  artifacts:
    - path: "apps/website/app/pages/politicas-de-privacidad.vue"
      provides: "Privacy page wrapped in <NuxtLayout name=\"about\" active=\"priv\">"
      contains: "NuxtLayout"
    - path: "apps/website/app/components/PoliciesDefault.vue"
      provides: "Content-panel component without duplicate h1/intro"
  key_links:
    - from: "apps/website/app/pages/politicas-de-privacidad.vue"
      to: "apps/website/app/layouts/about.vue (07-03)"
      via: "<NuxtLayout name=\"about\" :title :intro active=\"priv\">"
      pattern: "NuxtLayout"
---

<objective>
Restyle "Políticas de privacidad" to the mockup by reusing the help-center layout built in 07-03 — pass title/intro/active, strip the duplicate component heading, and present the policies as the restyled accordion in the content panel.

Purpose: Privacy is a pure consumer of the 07-03 layout + 07-02 accordion. Minimal, additive: wire the page, remove duplication, fit the panel.
Output: politicas-de-privacidad.vue using NuxtLayout; PoliciesDefault rendering only the accordion content (no own h1/intro); panel matches the mockup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@apps/design/index.dc.html
@apps/design/screenshots/legal-faq.png

# UPDATE these (closest existing equivalents):
@apps/website/app/pages/politicas-de-privacidad.vue
@apps/website/app/components/PoliciesDefault.vue

<interfaces>
<!-- Reuse the 07-03 contract:
   <NuxtLayout name="about" title="Políticas de privacidad" intro="..." active="priv">
     <PoliciesDefault :policies="policies || []" />
   </NuxtLayout>
   about.vue renders breadcrumb header (Waldo > Políticas de privacidad) + h1 + intro + sticky sidebar
   (Políticas highlighted) + content slot. PoliciesDefault must DROP its own <h1> + hero intro block.
   Accordion visual already restyled in 07-02 — DO NOT touch _accordion.scss.
   Current data load (1 useAsyncData "policies" with default) is already optimal — keep as-is.
   Tokens: $ink #26252b, $ink2 #56535f, $muted #8a8794, $tag #a9772e, $cream #f6f4f1, $line #ece9e4. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Políticas into the help-center layout + strip duplicate heading + fit panel</name>
  <files>apps/website/app/pages/politicas-de-privacidad.vue, apps/website/app/components/PoliciesDefault.vue, apps/website/app/scss/components/_policies.scss</files>
  <action>
    Page: replace `definePageMeta({ layout: "about" })` with the explicit wrapper
      `<NuxtLayout name="about" title="Políticas de privacidad" intro="<short intro per mockup tone>" active="priv"><PoliciesDefault :policies="policies || []" /></NuxtLayout>`.
    Add `definePageMeta({ layout: false })` to the page script (REQUIRED — an explicit <NuxtLayout> wrapper double-wraps without it). Keep the existing single useAsyncData ("policies", has default) and $setSEO/$setStructuredData unchanged. API is already optimal (1 load) — note it, do not invent extra optimization.
    PoliciesDefault: remove the duplicate `<h1 class="policies--default__title">` and the `policies--default__hero` intro block (those now live in the layout header). Render only the accordion (`<AccordionDefault :questions="policies" />`) inside the content panel. `_policies.scss` becomes a thin panel wrapper (spacing only); the accordion styling comes from `_accordion.scss` (07-02) — do NOT restyle or duplicate accordion rules here. BEM strict; tokens only; no existing-variable edits; delete any rules left dead by removing the hero/title.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "politicas-de-privacidad|PoliciesDefault" || echo "no privacy type errors"</automated>
    grep "NuxtLayout" apps/website/app/pages/politicas-de-privacidad.vue  →  present
    grep "layout: false" apps/website/app/pages/politicas-de-privacidad.vue  →  present
    ! grep -n "policies--default__title\|policies--default__hero" apps/website/app/components/PoliciesDefault.vue  →  removed
    VISUAL LOOP (logged-OUT, :3000; live mockup on :3002): screenshot http://localhost:3000/politicas-de-privacidad at 1440 + 390 → /tmp/waldo-shots/07-04-priv-{desktop,mobile}.png → Read → compare vs the mockup legal accordion screen + apps/design/screenshots/legal-faq.png (same layout: breadcrumb header, sidebar "Políticas" active, accordion) → fix → repeat until match. Confirm exactly one h1 on the page.
  </verify>
  <done>Privacy page uses NuxtLayout (active="priv"), single h1, accordion panel matches the mockup on desktop+mobile; no accordion re-styling; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Page wrapped in <NuxtLayout name="about" active="priv">; one h1 only.
- `_accordion.scss` NOT in diff (owned by 07-02).
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT.
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server; no full test suite.
</verification>

<success_criteria>
Políticas de privacidad matches the mockup by reusing the 07-03 layout and 07-02 accordion, with the duplicate heading removed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-04-SUMMARY.md`.
</output>
