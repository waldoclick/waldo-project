---
phase: 07-publico-vistas-core
plan: 06
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - apps/website/app/pages/contacto/index.vue
  - apps/website/app/components/ContactDefault.vue
  - apps/website/app/components/FormContact.vue
  - apps/website/app/components/AddressDefault.vue
  - apps/website/app/scss/components/_contact.scss
  - apps/website/app/scss/components/_address.scss
autonomous: true
requirements: [PUB-CONTACT]
must_haves:
  truths:
    - "Contacto renders inside the help-center layout (07-03) with breadcrumb header + sticky sidebar (Contáctanos active) + form panel"
    - "The contact form matches the mockup: Correo, Nombre, Empresa (opcional), Teléfono CL+56 (opcional), Mensaje with char counter, amber submit — fields on $cream with amber focus"
    - "The contact block (correo + redes sociales) renders below the form per the mockup"
    - "Submit behavior is unchanged (vee-validate + existing apiClient submit + redirect to /contacto/gracias)"
  artifacts:
    - path: "apps/website/app/pages/contacto/index.vue"
      provides: "Contact page wrapped in <NuxtLayout name=\"about\" active=\"cont\">"
      contains: "NuxtLayout"
    - path: "apps/website/app/components/FormContact.vue"
      provides: "Contact form restyled to the mockup (cream fields, amber focus, amber submit)"
  key_links:
    - from: "apps/website/app/pages/contacto/index.vue"
      to: "apps/website/app/layouts/about.vue (07-03)"
      via: "<NuxtLayout name=\"about\" :title :intro active=\"cont\">"
      pattern: "NuxtLayout"
    - from: "apps/website/app/components/FormContact.vue"
      to: "existing submit handler (apiClient + /contacto/gracias)"
      via: "handleSubmit — behavior preserved"
      pattern: "handleSubmit"
---

<objective>
Restyle the Contacto page to the mockup by reusing the help-center layout built in 07-03 and styling the existing contact form + contact block to match (`apps/design/index.dc.html` lines 1168–1218). The form already has all the right fields (email, name, company, phone via InputPhone, message+counter) — this is visual + layout wiring, NOT a form rewrite.

Purpose: Contacto is the last consumer of the 07-03 layout. Preserve submit behavior; restyle fields to the mockup ($cream fields, amber focus, amber submit) and lay out the form + contact block per the mockup.
Output: contacto/index.vue using NuxtLayout (active="cont"); ContactDefault stripped of its own header; FormContact + AddressDefault restyled to the mockup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@apps/design/index.dc.html
@apps/design/screenshots/legal-contacto.png

# UPDATE these (closest existing equivalents — the form already exists with InputPhone from quick-task 124):
@apps/website/app/pages/contacto/index.vue
@apps/website/app/components/ContactDefault.vue
@apps/website/app/components/FormContact.vue
@apps/website/app/components/AddressDefault.vue

<interfaces>
<!-- Reuse the 07-03 contract:
   <NuxtLayout name="about" title="Escríbenos tus dudas" intro="..." active="cont">
     <ContactDefault />
   </NuxtLayout>
   about.vue renders breadcrumb header (Waldo > Escríbenos tus dudas) + h1 + intro + sticky sidebar
   (Contáctanos highlighted) + content slot. ContactDefault must DROP its own title/description header.

   Mockup contact form (lines 1170–1199): fields max-width ~600px, each field = label (13px 600 $ink)
   + input on $cream bg, $line border, focus = amber border + #fff bg. Fields:
     Correo electrónico (email) · Nombre · Empresa (opcional) · Teléfono [CL +56 select + number] (opcional)
     · Mensaje (textarea, 300 max, "N caracteres restantes") · amber submit "Enviar".
   Contact block (lines 1202–1216): card with Correo electrónico (mailto, amber mail icon) + Redes
   sociales (38px square icon buttons). AddressDefault already holds correo + redes — restyle it to this.

   Tokens: $ink #26252b, $ink2 #56535f, $muted #8a8794, $amber #f7c97e, $tag #a9772e, $cream #f6f4f1, $line #ece9e4.
   InputPhone (CL +56 select) already exists from quick-task 124 — reuse, do not rebuild.
   PRESERVE submit: existing handleSubmit + apiClient + redirect to /contacto/gracias must be unchanged. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Contacto into the help-center layout + strip ContactDefault header</name>
  <files>apps/website/app/pages/contacto/index.vue, apps/website/app/components/ContactDefault.vue, apps/website/app/scss/components/_contact.scss</files>
  <action>
    Page: replace `definePageMeta({ layout: "about" })` with
      `<NuxtLayout name="about" title="Escríbenos tus dudas" intro="<short intro per mockup tone>" active="cont"><ContactDefault /></NuxtLayout>`.
    Add `definePageMeta({ layout: false })` to the page script (REQUIRED — an explicit <NuxtLayout> wrapper double-wraps without it). Keep $setSEO/$setStructuredData unchanged. Contacto has no page data load and needs none — note that the API target is "no fetch" (correct; do not add one).
    ContactDefault: remove the `contact--default__header` (h1 "Escríbenos tus dudas" + description) — the title/intro now live in the layout header. Keep the form + address slots. Restyle `.contact--default` panel to the mockup: form column max-width ~600px, contact block below. BEM strict under `.contact--default__*`; tokens only; no existing-variable edits; remove rules left dead by deleting the header.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "contacto/index|ContactDefault" || echo "no contact page type errors"</automated>
    grep "NuxtLayout" apps/website/app/pages/contacto/index.vue  →  present
    grep "layout: false" apps/website/app/pages/contacto/index.vue  →  present
    ! grep -n "contact--default__header" apps/website/app/components/ContactDefault.vue  →  removed
  </verify>
  <done>Contacto page uses NuxtLayout (active="cont"); ContactDefault has no own header (single h1 from layout); panel laid out per mockup; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the contact form + contact block to the mockup (behavior preserved)</name>
  <files>apps/website/app/components/FormContact.vue, apps/website/app/components/AddressDefault.vue, apps/website/app/scss/components/_contact.scss, apps/website/app/scss/components/_address.scss</files>
  <action>
    FORM (FormContact.vue): restyle inputs to the mockup (lines 1172–1199) — each field: label 13px/600 $ink, control on $cream bg with $line border, focus → amber border + #fff bg, 4px radius, 13–15px padding. Fields stay exactly as today (Correo, Nombre, Empresa opcional, Teléfono via InputPhone CL+56, Mensaje textarea with the existing 300-char counter shown as "N caracteres restantes"). Submit = amber button "Enviar" (amber_hover on hover), self-aligned start. CRITICAL: do NOT change submit logic, yup schema, vee-validate wiring, apiClient call, or the /contacto/gracias redirect — visual only. Replace generic `form-group`/`form-control` classes with the contact BEM scope (`.contact--default__form__*`) OR style them in `_contact.scss` if shared primitives must stay — prefer the form's own BEM scope; no standalone hyphenated classes. Ensure InputPhone select matches the mockup (CL +56 on $cream, chevron). Per CLAUDE.md form rules: do not use form__label float on non-input groups.
    CONTACT BLOCK (AddressDefault.vue): restyle to the mockup card (lines 1202–1216) — white card, $line border, "Correo electrónico" eyebrow + mailto link with amber mail icon, "Redes sociales" eyebrow + 38px square social icon buttons on $cream. Keep existing real links (Facebook/Instagram/LinkedIn). BEM under `.address--default__*`; tokens only.
    No existing-variable edits; no scale/box-shadow; remove any dead rules.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "FormContact|AddressDefault" || echo "no form type errors"</automated>
    grep "handleSubmit" apps/website/app/components/FormContact.vue  →  present (submit logic preserved)
    VISUAL LOOP (logged-OUT, :3000; live mockup on :3002): screenshot http://localhost:3000/contacto at 1440 + 390 → /tmp/waldo-shots/07-06-contacto-{desktop,mobile}.png → Read → compare element-by-element vs apps/design/index.dc.html lines 1168–1218 AND apps/design/screenshots/legal-contacto.png (breadcrumb header, sidebar "Contáctanos" active, cream fields + amber focus, CL+56 phone, char counter, amber submit, contact card with correo + redes) → fix → repeat until match. Focus an input and confirm amber focus border + #fff bg.
  </verify>
  <done>Contact form + contact block match legal-contacto.png on desktop+mobile, logged-OUT; submit behavior unchanged; tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Page wrapped in <NuxtLayout name="about" active="cont">; one h1 only; ContactDefault header removed.
- Submit logic (handleSubmit/apiClient/redirect) unchanged — grep confirms handleSubmit present; no schema edits.
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT, vs legal-contacto.png.
- InputPhone (CL +56) reused, not rebuilt.
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server; no full test suite.
</verification>

<success_criteria>
Contacto matches the mockup by reusing the 07-03 layout, with the contact form and contact block restyled and submit behavior preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-06-SUMMARY.md`.
</output>
