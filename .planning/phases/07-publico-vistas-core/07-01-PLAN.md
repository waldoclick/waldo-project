---
phase: 07-publico-vistas-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/index.vue
  - apps/website/app/components/HeroHome.vue
  - apps/website/app/components/CardCategory.vue
  - apps/website/app/components/CategoryArchive.vue
  - apps/website/app/components/AdArchive.vue
  - apps/website/app/components/CardAnnouncement.vue
  - apps/website/app/components/ArticleArchive.vue
  - apps/website/app/components/CardArticle.vue
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_category.scss
  - apps/website/app/scss/components/_card.scss
  - apps/website/app/scss/components/_announcement.scss
  - apps/website/app/scss/components/_article.scss
autonomous: true
requirements: [PUB-HOME]
must_haves:
  truths:
    - "Home renders to the UPDATED mockup look (hero + featured-card carousel, avisos destacados grid, categorías grid, dark 'vender' CTA band, blog grid) logged-OUT on desktop 1440 and mobile 390"
    - "The hero shows a floating featured-ad card with a working carousel (prev/next buttons + dots cycling through the featured ads) beside the inline hero search + trust badges"
    - "Hero clears the fixed/overlay headroom header (~104px+ top padding) — no content hidden behind the bar"
    - "Home loads its page data with a single useAsyncData (one network wave): categories + featured ads + latest articles"
    - "Colors/typography use phase-04 tokens ($ink, $amber, $cream, $line, $muted, $tag); no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/pages/index.vue"
      provides: "Home composition (hero/destacados/categorías/CTA-vender/blog) + single Promise.all data load"
      contains: "useAsyncData"
    - path: "apps/website/app/components/HeroHome.vue"
      provides: "Hero with inline search, trust badges, and featured-card carousel (prev/next/dots state)"
      contains: "carousel"
    - path: "apps/website/app/scss/components/_hero.scss"
      provides: "Restyled .hero--home to the updated mockup (split layout + floating card)"
  key_links:
    - from: "apps/website/app/pages/index.vue"
      to: "section components (HeroHome / AdArchive / CategoryArchive / ArticleArchive)"
      via: "component composition only (no BEM/HTML in the page)"
      pattern: "<HeroHome"
    - from: "apps/website/app/components/HeroHome.vue"
      to: "featured ads array"
      via: "carousel index state cycling the floating card"
      pattern: "currentIndex|activeIndex|carousel"
    - from: "section component SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$cream"
---

<objective>
Restyle the public Home page (`pages/index.vue`) to the UPDATED v1.47 mockup (`apps/design/index.dc.html` home view, lines ~247–437) and consolidate its data fetching into a single load.

Purpose: Home is the highest-traffic public page and the visual anchor of the rebrand. The updated mockup RESTRUCTURED the home: it now leads with a hero that pairs an inline search with a FLOATING featured-ad card CAROUSEL, then shows an "Avisos destacados" ad grid, the "Explora por categoría" grid, a dark "¿Tienes equipos para vender?" CTA band, and a "Blog Waldo" article grid. The old Highlights/Howto/Packs/FAQ bands are NO LONGER on Home — they moved to the "Por qué Waldo" screen (now owned by 07-02).
Output: Restyled home sections (hero+carousel, destacados, categorías, dark CTA, blog) matching the mockup, on desktop + mobile, logged-OUT, with one useAsyncData instead of three.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth (read the relevant screens, do NOT copy inline styles — translate to BEM + phase-04 tokens)
# UPDATED mockup; re-locate screens with `grep -n 'data-screen-label' index.dc.html` (line numbers shift between drops).
# Home view = the markup BEFORE the first data-screen-label ("Por qué Waldo") — currently lines ~247–437.
@apps/design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@apps/design/screenshots/hero.png
@apps/design/screenshots/hero-carousel.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/index.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- UPDATED HOME STRUCTURE (apps/design/index.dc.html, home view ~247–437):
  1. HERO (~247–316): $cream bg + amber radial glow. LEFT = eyebrow tag, Poppins 800 h1 with amber
     underline on "Anuncia"/"busca", $ink2 subhead, inline search (input + amber "Buscar" button on #fff
     card with $line border), trust badges row ("Publica 3 avisos gratis", "13 categorías industriales",
     "Equipos nuevos y usados" with amber check icons). RIGHT = a FLOATING featured-ad card (amber
     rotated shadow plate behind it, white card, "Destacado" amber badge, photo, category chip, condition,
     title, location/meta, price + "Ver aviso") AND below it static carousel CONTROLS: prev/next round
     buttons + a row of dots. The card cycles through the featured ads (heroMulti gate when >1).
  2. AVISOS DESTACADOS (~318–335): #fff band, eyebrow "Destacados" + h2 "Avisos destacados" + intro +
     "Ver todos los anuncios" link; a 4-col grid of ad cards (AdCard in mockup → CardAnnouncement here).
  3. CATEGORÍAS (~337–399): $cream band, centered h2 "Explora por categoría" + intro; grid of category
     tiles (icon chip with per-industry color + name + "N anuncios ›").  → CategoryArchive + CardCategory.
  4. CTA VENDER (~401–416): #fff band wrapping a dark ($ink) rounded panel with amber radial glow:
     h2 "¿Tienes equipos para vender?" + copy + "Anunciar ahora" (amber) and "Cómo funciona" (outline) CTAs.
  5. BLOG (~418–435): $cream band, eyebrow "Blog Waldo" + h2 + intro + "Ver todos los artículos" link;
     3-col grid of article cards (BlogCard in mockup → CardArticle here).  → ArticleArchive + CardArticle.

  Reusable existing components (UPDATE-not-create):
   - HeroHome.vue + _hero.scss          → hero + carousel
   - AdArchive.vue + CardAnnouncement.vue (+ _announcement.scss / _card.scss) → avisos destacados grid
   - CategoryArchive.vue + CardCategory.vue + _category.scss → categorías grid (already close)
   - ArticleArchive.vue + CardArticle.vue (+ _article.scss) → blog grid
   The dark "vender" CTA band has NO existing component — implement it as elements of an existing home
   block (e.g. `.hero--home__cta` or a thin home-scoped block) per BEM; pages stay composition-only.

  Current home data load (index.vue) fires THREE useAsyncData ("categories", "home-packs",
  "featured-faqs"). packs/faqs MOVE OFF HOME (they belong to Por qué Waldo / 07-02). The new home load
  needs: categories (filterStore.loadFilterCategories), featured ads (adsStore.loadAds — see
  pages/anuncios/index.vue for the loader + sort "featured"), latest articles (articlesStore.loadArticles).

  STALE PNG WARNING: apps/design/screenshots/headroom.png shows an OLD home with a "Busca equipo /
  Filtra y elige / Contacta y coordina" 3-step band before "Explora por categoría" — that band is NOT
  in the current source home (it moved to Por qué Waldo as "Cómo funciona"). Use headroom.png ONLY to
  judge header clearance, NOT the home section order. Likewise apps/design/screenshots/cat-cards.png is
  the LISTING/category-archive page (sidebar + ad cards), NOT the home category grid — do not use it for
  Home. There is no dedicated PNG for the destacados/CTA/blog bands — verify those against index.dc.html.
  STALE PNG WARNING (hero search): apps/design/screenshots/hero.png renders the hero search as
  [input] | "Todas las categorías ▾" dropdown | [Buscar] AND an empty "Foto del equipo" placeholder.
  Both are STALE — the current source (index.dc.html ~260-267) and the newer hero-carousel.png show a
  single input + amber "Buscar" button with NO category dropdown, and a real featured-ad photo. The HTML
  is authoritative: do NOT rebuild a category dropdown in the hero search. Use hero.png only for the
  overall left/right split + trust-badge look; trust the HTML for the search markup. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate home data fetching to a single useAsyncData (categories + featured ads + latest articles)</name>
  <files>apps/website/app/pages/index.vue</files>
  <action>
    CURRENT (anti-pattern): index.vue fires THREE separate useAsyncData keys — "categories", "home-packs", "featured-faqs" — and the home no longer shows packs or FAQs.
    TARGET: ONE useAsyncData with key "home-data" whose callback runs `Promise.all([...])` over the loaders the NEW home needs and returns `{ categories, featuredAds, articles }`. Provide `default: () => ({ categories: [], featuredAds: [], articles: [] })` to eliminate T|undefined.
      - categories: filterStore.loadFilterCategories() → filterStore.filterCategories (keep existing store cache guard).
      - featuredAds: adsStore.loadAds({}, { page: 1, pageSize: ~8 }, ["featured"-style sort) — copy the loader/sort shape from pages/anuncios/index.vue (do NOT invent a new endpoint). Return the ads array for the destacados grid.
      - articles: articlesStore.loadArticles(...) → the latest ~3 published articles for the blog grid (reuse the store; respect its cache guard).
    Remove the packs (ad-packs) and faqs loaders from the home (they belong to 07-02 Por qué Waldo). Derive `categories`/`featuredAds`/`articles` as computed off the single `data` ref. Do NOT pair a bare `await storeAction()` with useAsyncData (no double-fetch). Keep $setSEO unchanged; update $setStructuredData only if needed (still WebSite/Organization — no FAQ schema on home). The page stays composition-only (no HTML sections / BEM in the page). Compose, in mockup order: HeaderDefault → HeroHome (pass `:featured-ads`) → AdArchive (destacados, pass the featured ads) → CategoryArchive (pass categories) → [dark vender CTA, rendered via HeroHome's CTA element or a thin home block] → ArticleArchive (blog, pass articles) → FooterDefault.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "index.vue" || echo "no index.vue type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/index.vue  →  MUST equal 1
    grep "default: () =>" apps/website/app/pages/index.vue  →  present (object default)
    ! grep -E "home-packs|featured-faqs|ad-packs|faqsStore" apps/website/app/pages/index.vue  →  packs/faqs loaders removed from home
  </verify>
  <done>index.vue has exactly one useAsyncData (key "home-data") with Promise.all over categories/featuredAds/articles and an object default; packs/faqs loaders removed; no bare-await+useAsyncData; composition-only; vue-tsc clean for index.vue.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the hero + build the featured-card carousel (BEM, phase-04 tokens)</name>
  <files>apps/website/app/components/HeroHome.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Translate the mockup HERO (index.dc.html home view ~247–316) to the existing `.hero--home` BEM block. Two-column split feel: LEFT content + RIGHT floating featured-card carousel.
    LEFT: eyebrow tag chip (amber-tint bg, $tag text, box icon), Poppins 800 headline with an amber underline accent on key words ("Anuncia", "busca") via background-image linear-gradient (NOT box-shadow), $ink2 subhead (max ~480px), inline SEARCH on #fff with $line border (search icon + input + amber "Buscar" button → wire it to the existing search/navigate behavior already in HeroHome, do not break it), then a trust-badges row ("Publica 3 avisos gratis" / "13 categorías industriales" / "Equipos nuevos y usados") each with an amber check icon.
    RIGHT — CAROUSEL (NET-NEW BEHAVIOR, the biggest build item): accept a `featuredAds` prop (array). Maintain a reactive `currentIndex` (ref). Render ONE floating card at a time bound to `featuredAds[currentIndex]`: amber rotated shadow plate behind (transform: rotate(2.5deg) is allowed — rotate carve-out; scale banned), white card with padding, photo (aspect 16/11) with a "Destacado" amber badge, a row with category chip (color dot + name) + condition, Poppins 700 title, location/meta line, a $line divider, then price (Poppins 800) + "Ver aviso" pill linking to the ad. BELOW the card render static CONTROLS only when `featuredAds.length > 1`: a prev round button, a row of dot buttons (active dot = amber/wider), a next round button. prev/next/dot click handlers update `currentIndex` (wrap-around). Keep transitions simple (opacity/translate, NO scale). The card+plate may keep the gentle `floaty` animation if a keyframe exists or is added in _hero.scss (no scale-on-hover).
    Tokens only ($ink/$ink2/$amber/$amber_hover/$cream/$line/$muted/$tag). CRITICAL: hero top padding must clear the fixed/overlay headroom header shipped in 06-02 (~104px+; mockup uses 104px). Verify against apps/design/screenshots/headroom.png that nothing hides behind the bar (use it for clearance ONLY — its section order is stale).
    BEM strict: every child under `.hero--home__*` (e.g. `__search`, `__badges`, `__card`, `__card__media`, `__controls`, `__dot`); no standalone hyphenated classes. Do NOT modify existing SCSS variables; add a NEW variable in _variables.scss only if the mockup needs a color absent from the palette.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroHome" || echo "no hero type errors"</automated>
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another; the live mockup is on http://localhost:3002):
      Use Playwright to screenshot http://localhost:3000/ at 1440x900 and 390x844 → save to /tmp/waldo-shots/07-01-hero-{desktop,mobile}.png → Read each PNG → compare element-by-element against apps/design/index.dc.html home hero AND apps/design/screenshots/hero.png + hero-carousel.png → fix SCSS/markup → re-screenshot → repeat until it matches (colors, spacing, type scale, floating card + plate, no content behind header). Click prev/next and a dot; re-screenshot to confirm the card actually changes (carousel works).
  </verify>
  <done>Hero matches the mockup + hero.png/hero-carousel.png on desktop and mobile, logged-OUT; the featured-card carousel cycles via prev/next/dots; hero clears the headroom header; only phase-04 (or newly-added) tokens used; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle avisos destacados + categorías + dark vender CTA + blog grid</name>
  <files>apps/website/app/components/CategoryArchive.vue, apps/website/app/components/CardCategory.vue, apps/website/app/components/AdArchive.vue, apps/website/app/components/CardAnnouncement.vue, apps/website/app/components/ArticleArchive.vue, apps/website/app/components/CardArticle.vue, apps/website/app/scss/components/_category.scss, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_article.scss, apps/website/app/scss/components/_card.scss</files>
  <action>
    Restyle the four remaining home bands to the updated mockup. Reuse existing components (UPDATE-not-create); push visual rules to the matching SCSS partials.
    - AVISOS DESTACADOS (~318–335): section header (eyebrow "Destacados" with amber star, h2 "Avisos destacados" Poppins 800, $ink2 intro, "Ver todos los anuncios →" outline link) over a #fff band; below it the ad-card grid via AdArchive/CardAnnouncement. Restyle CardAnnouncement to the mockup AdCard look (category chip with color dot, condition, title, location/year, price + "+ IVA", "Inicia sesión para ver al anunciante" gate when logged-out) — keep its existing logged-in/out behavior. The section header wrapper lives wherever the page composes it; keep BEM (`.announcement--archive__*` for the grid; header elements under the home/announcement namespace).
    - CATEGORÍAS (~337–399): centered h2 "Explora por categoría" + intro; CardCategory tiles (icon chip with per-industry color background, name, "N anuncios ›"). CategoryArchive/CardCategory already exist — update copy/markup minimally and restyle in _category.scss/_card.scss. Hover per CLAUDE.md: NO box-shadow/scale ADD beyond what already ships — keep transitions simple ($line border, color shifts).
    - CTA VENDER (~401–416): the dark rounded panel ($ink bg, amber radial glow, h2 "¿Tienes equipos para vender?", $ink2-on-dark copy, "Anunciar ahora" amber CTA + "Cómo funciona" outline CTA). No existing component — implement as a thin home-scoped BEM block (e.g. `.hero--home__cta` reused, or a dedicated element). Pages stay composition-only; the markup belongs in a component, not the page.
    - BLOG (~418–435): section header (eyebrow "Blog Waldo" amber dot, h2 + intro, "Ver todos los artículos →" link) over a $cream band; 3-col article grid via ArticleArchive/CardArticle. Restyle CardArticle to the mockup BlogCard look.
    Tokens only; BEM strict (no standalone hyphenated classes); no existing-variable edits (add new only if a mockup color is absent). No scale/box-shadow added per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CategoryArchive|CardCategory|AdArchive|CardAnnouncement|ArticleArchive|CardArticle" || echo "no section type errors"</automated>
    VISUAL LOOP (logged-OUT, :3000): screenshot http://localhost:3000/ full-page at 1440 and 390 → save /tmp/waldo-shots/07-01-sections-{desktop,mobile}.png → Read → compare the destacados / categorías / dark-CTA / blog bands element-by-element vs apps/design/index.dc.html home view ~318–435 (no dedicated PNG for these — use the source HTML + live mockup on :3002) → fix → repeat until match. Confirm section ORDER matches the source (hero → destacados → categorías → CTA → blog) and that the dropped Highlights/Howto/Packs/FAQ bands are NOT present.
  </verify>
  <done>Avisos destacados, categorías, dark vender CTA, and blog bands match the updated mockup on desktop+mobile, logged-OUT, in the correct order; no Highlights/Howto/Packs/FAQ on home; tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Single useAsyncData on index.vue (grep == 1) with object default; no double-fetch; packs/faqs loaders removed.
- Full home page screenshot-Read-compare passes at 1440 and 390, logged-OUT, against apps/design/index.dc.html home view + hero.png/hero-carousel.png. Section order = hero → destacados → categorías → dark CTA → blog.
- Hero featured-card carousel works (prev/next/dots cycle the card) and the hero clears the 06-02 headroom header (no overlap).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (only additions, if any).
- vue-tsc --noEmit exits 0 for all touched components.
- One heavy op at a time; NEVER run the full Vitest suite; reuse the running :3000 dev server; compare against :3002 live mockup.
</verification>

<success_criteria>
Home looks like the UPDATED mockup (hero + featured-card carousel, avisos destacados, categorías, dark vender CTA, blog) on desktop + mobile logged-OUT, loads with one data wave, uses only phase-04/new tokens, and modifies no existing SCSS variable.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-01-SUMMARY.md`.
</output>
