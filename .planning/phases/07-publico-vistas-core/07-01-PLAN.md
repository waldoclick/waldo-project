---
phase: 07-publico-vistas-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/index.vue
  - apps/website/app/components/HeroHome.vue
  - apps/website/app/components/CategoryArchive.vue
  - apps/website/app/components/HighlightsDefault.vue
  - apps/website/app/components/HowtoDefault.vue
  - apps/website/app/components/PacksDefault.vue
  - apps/website/app/components/FaqDefault.vue
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_category.scss
  - apps/website/app/scss/components/_highlights.scss
  - apps/website/app/scss/components/_howto.scss
  - apps/website/app/scss/components/_packs.scss
  - apps/website/app/scss/components/_faq.scss
  - apps/website/app/scss/components/_accordion.scss
autonomous: true
requirements: [PUB-HOME]
must_haves:
  truths:
    - "Home renders to the mockup look (hero, categories, explanatory sections, packs, FAQ) logged-OUT on desktop 1440 and mobile 390"
    - "Hero clears the fixed/overlay headroom header (~130px top padding) — no content hidden behind the bar"
    - "Home loads its page data with a single useAsyncData (one network wave), not three"
    - "Colors/typography use phase-04 tokens ($ink, $amber, $cream, $line, $muted, $tag); no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/pages/index.vue"
      provides: "Home composition + single Promise.all data load"
      contains: "useAsyncData"
    - path: "apps/website/app/scss/components/_hero.scss"
      provides: "Restyled .hero--home to mockup"
    - path: "apps/website/app/scss/components/_accordion.scss"
      provides: "Restyled accordion (amber chevron pill, $line borders) — OWNER for the whole phase"
  key_links:
    - from: "apps/website/app/pages/index.vue"
      to: "section components (HeroHome/CategoryArchive/Highlights/Howto/Packs/Faq)"
      via: "component composition only (no BEM/HTML in the page)"
      pattern: "<HeroHome"
    - from: "section component SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$cream"
---

<objective>
Restyle the public Home page (`pages/index.vue`) to the v1.47 mockup (`design/index.dc.html`, home view lines ~214–405) and consolidate its data fetching into a single load.

Purpose: Home is the highest-traffic public page and the visual anchor of the rebrand. It also OWNS the shared `_accordion.scss` restyle for the whole phase (FAQ on home, plus 07-03/04/05 reuse it).
Output: Restyled home sections (hero, categories, explanatory cards, packs, FAQ) matching the mockup, on desktop + mobile, logged-OUT, with one useAsyncData instead of three.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth (read the relevant screens, do NOT copy inline styles — translate to BEM + phase-04 tokens)
@design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/hero.png
@design/screenshots/hero-carousel.png
@design/screenshots/cat-cards.png
@design/screenshots/headroom.png
@design/screenshots/index-menu-open.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/index.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- Current home data load (index.vue) fires THREE useAsyncData: "categories", "home-packs", "featured-faqs".
     filterStore.loadFilterCategories(), client("ad-packs"), faqsStore.loadFaqs() are independent. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate home data fetching to a single useAsyncData (Promise.all)</name>
  <files>apps/website/app/pages/index.vue</files>
  <action>
    CURRENT (anti-pattern): index.vue fires THREE separate useAsyncData keys — "categories" (filterStore.loadFilterCategories), "home-packs" (client ad-packs), "featured-faqs" (faqsStore.loadFaqs). Three SSR data waves for one page.
    TARGET: ONE useAsyncData with key "home-data" whose callback runs `Promise.all([...])` over the three loaders, returning `{ categories, packs, faqs }`. Provide `default: () => ({ categories: [], packs: [], faqs: [] })` to eliminate T|undefined. Keep the existing store cache guards (filterStore/faqsStore already guard re-fetch). Derive `categories`/`packs`/`faqs` as computed off the single `data` ref. Do NOT pair a bare `await storeAction()` with useAsyncData (no double-fetch). Keep $setSEO / $setStructuredData unchanged. The page stays composition-only (no HTML sections / BEM in the page).
    Preserve the `useApiClient()` instantiation at setup scope (it is captured inside the callback).
    COMPOSITION NOTE (pairs with Task 3): in the home template, FaqDefault MUST be passed an explicit `:title` (e.g. "Preguntas frecuentes") so the home FAQ band keeps its heading. Task 3 removes FaqDefault's internal default-title fallback, so any usage that wants a heading must pass `title` explicitly. Pass the home FAQ `:title` (and `:text` as today) here.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "index.vue" || echo "no index.vue type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/index.vue  →  MUST equal 1
    grep "default: () =>" apps/website/app/pages/index.vue  →  present (object default)
  </verify>
  <done>index.vue has exactly one useAsyncData (key "home-data") with Promise.all and an object default; categories/packs/faqs are computed off it; no bare-await+useAsyncData; vue-tsc clean for index.vue.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle hero + categories to the mockup (BEM, phase-04 tokens)</name>
  <files>apps/website/app/components/HeroHome.vue, apps/website/app/components/CategoryArchive.vue, apps/website/app/scss/components/_hero.scss, apps/website/app/scss/components/_category.scss</files>
  <action>
    Translate the mockup HERO (index.dc.html lines ~215–284) and CATEGORÍAS (lines ~305–367) to the existing `.hero--home` and `.category--archive` BEM blocks. Update markup in the .vue components only where structure requires it; push all visual rules to `_hero.scss` / `_category.scss`.
    HERO: split layout feel per mockup, Poppins 800 headline with amber underline accent on key words, $ink2 subhead, search field on $cream with $line border, "13 categorías" trust line. CRITICAL: top padding must clear the fixed/overlay headroom header shipped in 06-02 — carry ~130px top padding (mockup uses 104–128px; account for the 74px bar). Verify against design/screenshots/headroom.png that nothing hides behind the bar.
    CATEGORÍAS: section header (h2 Poppins 800 + $ink2 paragraph) over the mockup background; category cards per design/screenshots/cat-cards.png (each industry its color dot, $line borders, hover per CLAUDE.md = NO scale/box-shadow add — keep transitions simple).
    Tokens only ($ink/$amber/$cream/$line/$muted/$tag). BEM strict: every child under `.hero--home__*` / `.category--archive__*`; no standalone hyphenated classes. Do NOT modify existing SCSS variables; if the mockup needs a color absent from the palette, add a NEW variable in _variables.scss (do not change existing).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroHome|CategoryArchive" || echo "no hero/category type errors"</automated>
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another):
      Use Playwright (recipe in plan-context) to screenshot http://localhost:3000/ at 1440x900 and 390x844 → save to /tmp/waldo-shots/07-01-hero-{desktop,mobile}.png → Read each PNG → compare element-by-element against design/index.dc.html hero/categorías screens AND design/screenshots/hero.png + cat-cards.png + headroom.png → fix SCSS → re-screenshot → repeat until it matches (colors, spacing, type scale, no content behind header).
  </verify>
  <done>Hero + categorías match the mockup and reference PNGs on desktop and mobile, logged-OUT; hero clears the headroom header; only phase-04 (or newly-added) tokens used; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle explanatory sections + packs + FAQ (OWNS _accordion.scss for the phase)</name>
  <files>apps/website/app/components/HighlightsDefault.vue, apps/website/app/components/HowtoDefault.vue, apps/website/app/components/PacksDefault.vue, apps/website/app/components/FaqDefault.vue, apps/website/app/scss/components/_highlights.scss, apps/website/app/scss/components/_howto.scss, apps/website/app/scss/components/_packs.scss, apps/website/app/scss/components/_faq.scss, apps/website/app/scss/components/_accordion.scss</files>
  <action>
    Restyle the home explanatory + commercial sections to the mockup. These same blocks are reused by Por qué Waldo (07-02), so restyle the COMPONENTS once here — 07-02 only composes them.
    - HIGHLIGHTS (`.highlights--default`) → "cómo funciona" cards (mockup lines ~434–465): numbered card, amber gradient icon tile, $tag eyebrow, Poppins title, $ink2 body. NO box-shadow/scale add (CLAUDE.md) — translate the look with borders/background only.
    - HOWTO (`.howto--default`) → "vendedores" 3-step (mockup lines ~469–498): steps-01/02/03 svgs, amber numbered badges, centered, amber "Anunciar ahora" CTA.
    - PACKS (`.packs--default`) → packs grid (mockup lines ~502–544): 3 cards, middle "Recomendado" with 2px amber border + badge, check rows, amber CTA on recommended / outline CTA on others. Ref design/screenshots/packs-light.png.
    - FAQ (`.faq--default`, FaqDefault.vue): restyle to the centered FAQ block (mockup lines ~565–...): h2 Poppins 800, accordion list. CRITICAL CONTRACT for 07-03: REMOVE the internal default-title fallback `const title = props.title ?? "Preguntas Frecuentes";` — the heading must render ONLY when `title` is explicitly provided (so the FAQ page in 07-03 can omit `title` and let the layout supply the single h1). Make `title`/`text` purely opt-in: no heading/intro markup when the prop is absent. (Home passes an explicit `:title` per Task 1; the FAQ page will omit it.)
    - ACCORDION (`.accordion`, `_accordion.scss`): THIS PLAN IS THE OWNER for the phase. Restyle to the mockup accordion (lines ~1108–1127): item = $line border 4px radius, header Poppins 600 17px $ink, right-side 30px square pill with amber-tint background + chevron that rotates (rotate allowed per CLAUDE.md carve-out; scale banned). 07-03/04/05 REUSE this restyle and must NOT re-touch _accordion.scss.
    Tokens only; BEM strict; no existing-variable edits.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "Highlights|Howto|Packs|Faq" || echo "no section type errors"</automated>
    VISUAL LOOP (logged-OUT): screenshot http://localhost:3000/ full-page at 1440 and 390 → save /tmp/waldo-shots/07-01-sections-{desktop,mobile}.png → Read → compare the highlights/howto/packs/FAQ bands vs the mockup screens AND design/screenshots/packs-light.png → fix → repeat until match. Confirm accordion open/closed states (chevron rotation, amber pill) match the mockup.
  </verify>
  <done>Highlights/Howto/Packs/FAQ + accordion match the mockup and packs-light.png on desktop+mobile, logged-OUT; accordion restyle final (owner); tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Single useAsyncData on index.vue (grep == 1) with object default; no double-fetch.
- Full home page screenshot-Read-compare passes at 1440 and 390, logged-OUT, against design/index.dc.html home screen + hero.png/cat-cards.png/headroom.png/packs-light.png.
- Hero clears the 06-02 headroom header (no overlap).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (only additions, if any).
- vue-tsc --noEmit exits 0 for all touched components.
- One heavy op at a time; NEVER run the full Vitest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
Home looks like the mockup (desktop + mobile, logged-OUT), loads with one data wave, uses only phase-04/new tokens, modifies no existing SCSS variable, and the accordion restyle is finalized for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-01-SUMMARY.md`.
</output>
