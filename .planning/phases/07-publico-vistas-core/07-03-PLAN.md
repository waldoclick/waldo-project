---
phase: 07-publico-vistas-core
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - apps/website/app/layouts/about.vue
  - apps/website/app/components/MenuAbout.vue
  - apps/website/app/components/SitemapDefault.vue
  - apps/website/app/pages/preguntas-frecuentes.vue
  - apps/website/app/pages/sitemap.vue
  - apps/website/app/scss/components/_layout.scss
  - apps/website/app/scss/components/_menu.scss
  - apps/website/app/scss/components/_sitemap.scss
autonomous: true
requirements: [PUB-FAQ]
must_haves:
  truths:
    - "The about layout renders a help-center header (breadcrumb 'Waldo > {título}' + big h1 + intro over $cream) driven by props the page supplies"
    - "The sidebar (MenuAbout) is sticky with the active item highlighted, matching the mockup; it lists FAQ · Políticas · Condiciones · Contáctanos · Mapa del sitio"
    - "The about layout renders the closing dark 'Empieza ahora' CTA band (shared by all help-center views) below the content panel"
    - "Preguntas frecuentes renders the (07-02) restyled accordion in the content panel with NO duplicate h1/intro (title/intro now live in the layout header)"
    - "Mapa del sitio renders the 3-column panel (Explorar / Tu cuenta / Información) inside the help-center layout"
  artifacts:
    - path: "apps/website/app/layouts/about.vue"
      provides: "Help-center layout: breadcrumb header (title/intro props) + sticky sidebar + content slot + closing CTA band"
      contains: "defineProps"
    - path: "apps/website/app/scss/components/_menu.scss"
      provides: "Restyled .menu--about sticky sidebar with active state — OWNER"
      contains: "menu--about"
    - path: "apps/website/app/pages/preguntas-frecuentes.vue"
      provides: "FAQ page passing title/intro/active to the layout via NuxtLayout"
      contains: "NuxtLayout"
  key_links:
    - from: "apps/website/app/pages/preguntas-frecuentes.vue"
      to: "apps/website/app/layouts/about.vue"
      via: "<NuxtLayout name=\"about\" :title :intro :active> wrapper"
      pattern: "NuxtLayout"
    - from: "apps/website/app/layouts/about.vue"
      to: "breadcrumb header + MenuAbout sidebar"
      via: "props -> header markup; active prop -> MenuAbout"
      pattern: ":active|:title"
---

<objective>
Build the shared "help-center" layout used by all four legal/help views (FAQ, Políticas, Condiciones, Contacto, Mapa). Restyle the existing `about` layout to the mockup (`apps/design/index.dc.html` "Vista pública" lines 1104–1254): a $cream breadcrumb header (Waldo > {título} + big h1 + intro), a sticky sidebar with active-state highlighting, the content panel, and a closing dark "Empieza ahora" CTA band. Wire the FAQ page + Mapa del sitio as the first consumers.

Purpose: This is the SPINE for 07-04/05/06. It defines the title/intro contract (header lives in the layout, not in each content component) and owns `_layout.scss` + `_menu.scss`. The accordion VISUAL was already restyled in 07-02 (owner of FaqDefault.vue + _faq.scss + _accordion.scss); this plan depends on 07-02 so the FAQ page renders the finished accordion. It does NOT touch FaqDefault.vue, _faq.scss, or _accordion.scss.
Output: Restyled `about` layout + MenuAbout sidebar + breadcrumb header driven by page props + closing CTA band; FAQ page + sitemap migrated to pass title/intro and render in the panel with no duplicate headings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — re-locate with `grep -n 'data-screen-label' index.dc.html`; "Vista pública" (legal/help)
# screen currently spans ~1104–1272 (line numbers shift between drops).
@apps/design/index.dc.html
@apps/design/screenshots/legal-faq.png
@apps/design/screenshots/02-legal-mapa.png

# Depends on 07-02 — read its SUMMARY to see the final FaqDefault props + accordion restyle:
@.planning/phases/07-publico-vistas-core/07-02-SUMMARY.md

# UPDATE these (closest existing equivalents — the about layout ALREADY implements the sidebar+content concept):
@apps/website/app/layouts/about.vue
@apps/website/app/components/MenuAbout.vue
@apps/website/app/components/HeroFake.vue
@apps/website/app/components/SitemapDefault.vue
@apps/website/app/pages/preguntas-frecuentes.vue
@apps/website/app/pages/sitemap.vue

<interfaces>
<!-- DECISION (interface-first — 07-04/05/06 build against this):
  The breadcrumb + h1 + intro live in the LAYOUT, not in each content component.
  Mechanism: each page replaces `definePageMeta({ layout: "about" })` with an explicit wrapper:
     <NuxtLayout name="about" :title="..." :intro="..." :active="'faq'|'priv'|'cond'|'cont'|'mapa'">
       <ContentComponent ... />
     </NuxtLayout>
  about.vue: defineProps<{ title: string; intro?: string; active: string }>(); renders the $cream
  breadcrumb header (Waldo > {title}, h1 = {title}, p = {intro}) then the gsplit (sticky sidebar +
  <slot/> content panel), then a closing dark "Empieza ahora" CTA band (see below). MenuAbout receives
  `active` and highlights the matching item. Content components must STRIP their own <h1>/intro so the
  title is not rendered twice.

  Mockup tokens: $ink #26252b, $ink2 #56535f, $muted #8a8794, $amber #f7c97e, $tag #a9772e,
  $cream #f6f4f1, $line #ece9e4. Header padding-top must clear the headroom header (mockup 108px).
  Sidebar: position sticky; active item = amber-tint background + $ink text + colored icon.

  NEW in the updated mockup — closing CTA band (lines ~1256–1271): a $cream section wrapping a dark
  ($ink) rounded panel with amber radial glow: h2 "Empieza ahora" (Poppins 800, #fff) + copy
  ("Explora los anuncios disponibles o publica tu equipo. Tus primeros 3 avisos son gratis.") +
  "Explorar anuncios" (amber) and "Anunciar ahora" (outline) CTAs. It sits below the content panel and is
  shared by ALL help-center views, so it belongs in the LAYOUT (not per page). Implement under
  `.layout--about__cta*` BEM in _layout.scss (no scale/box-shadow; rotate/glow OK).

  CRITICAL Nuxt gotcha: an explicit `<NuxtLayout name="about">` wrapper REQUIRES the page to set
  `definePageMeta({ layout: false })` — otherwise Nuxt also applies the default layout and you get
  a double-wrap. Every consumer page (FAQ, Mapa, and 07-04/05/06) MUST use `layout: false` + wrapper.
  (This pattern is novel in the repo — grep confirms no existing <NuxtLayout> in pages.)

  OWNED ELSEWHERE — DO NOT TOUCH HERE: FaqDefault.vue, _faq.scss, _accordion.scss (all 07-02).
  After 07-02, FaqDefault renders a heading ONLY when `title` is explicitly passed (the default-title
  fallback was removed). So the FAQ page simply OMITS `title`/`text` and the layout supplies the single h1.
  _sitemap.scss already exists and is already registered in app.scss (no app.scss edit needed).
  MISLABELED PNG WARNING (mapa): the bare apps/design/screenshots/legal-mapa.png is actually a HOME-HERO
  render (CAT 320 featured card), NOT the Mapa del sitio screen. The REAL mapa render is
  apps/design/screenshots/02-legal-mapa.png (breadcrumb "Waldo > Mapa del sitio" + sidebar Mapa active +
  3-col Explorar/Tu cuenta/Información panel). For Mapa, treat index.dc.html lines ~1220-1251 as the
  authoritative reference and use 02-legal-mapa.png for the render (NOT bare legal-mapa.png).
  HeroFake is shared by ~11 other pages (gracias/error) — DO NOT delete the HeroFake component or its
  `hero--fake` block in _hero.scss (owned by 07-02). Only stop importing/rendering HeroFake inside
  layouts/about.vue (replace it with the breadcrumb header). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the about layout into the help-center shell (header props + sticky sidebar + closing CTA)</name>
  <files>apps/website/app/layouts/about.vue, apps/website/app/components/MenuAbout.vue, apps/website/app/scss/components/_layout.scss, apps/website/app/scss/components/_menu.scss</files>
  <action>
    Restyle `layouts/about.vue` to the mockup "Vista pública" shell (header lines 1104–1118; body 1120–1254):
    - Add `defineProps<{ title: string; intro?: string; active: string }>()`.
    - In `layouts/about.vue`, remove the `<HeroFake />` usage + its import and replace it with a $cream breadcrumb header band: breadcrumb nav (`Waldo` link → chevron → `{title}` current, $muted/$ink), an h1 (Poppins 800, clamp 32–46px, $ink) = `{title}`, and a `{intro}` paragraph ($ink2, max 680px). Carry top padding ~108px+ to clear the fixed headroom header (06-02). DO NOT delete the HeroFake component or its `hero--fake` SCSS — it is used by ~11 other pages (gracias/error). Only this layout stops using it. Implement header rules under `.layout--about__header*` BEM in `_layout.scss`.
    - Body: the `.layout--about__container` becomes the gsplit (sticky sidebar 248px + content panel `minmax(0,1fr)`), matching the mockup grid/gap. Sidebar `position: sticky; top: ~96px`.
    - Pass `active` to `<MenuAbout :active="active" />`.
    - CLOSING CTA (NEW, lines ~1256–1271): below the content panel render a $cream section wrapping a dark ($ink) rounded panel with amber radial glow: h2 "Empieza ahora" (#fff) + copy + "Explorar anuncios" (amber) and "Anunciar ahora" (outline) CTAs. Shared by all help-center views, so it lives in the layout (after `<slot/>`, before the footer). Implement under `.layout--about__cta*` BEM in _layout.scss; tokens only; no scale/box-shadow (rotate/glow allowed).
    - MenuAbout (`_menu.scss` `.menu--about`, THIS PLAN IS OWNER): restyle to the mockup sidebar items (icon + label, 5 links FAQ/Políticas/Condiciones/Contáctanos/Mapa). Add an `active` prop; the matching item gets amber-tint background + $ink label + $tag icon stroke; others $ink2. Keep existing Lucide icons. BEM strict under `.menu--about__*`; no standalone hyphenated classes.
    Tokens only; do not modify existing SCSS variables (add new only if the mockup needs an absent color). Keep all the Lightbox imports in the layout intact.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "about.vue|MenuAbout" || echo "no layout/menu type errors"</automated>
    grep "defineProps" apps/website/app/layouts/about.vue  →  present (title/intro/active)
    ! grep -n "HeroFake" apps/website/app/layouts/about.vue  →  no HeroFake left in the about layout (component itself stays for other pages)
  </verify>
  <done>about layout renders a props-driven breadcrumb header + sticky sidebar with active state + closing "Empieza ahora" CTA band; MenuAbout restyled (owner); about layout no longer uses HeroFake (component kept for other pages); tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Preguntas frecuentes into the layout (NuxtLayout props, omit duplicate title)</name>
  <files>apps/website/app/pages/preguntas-frecuentes.vue</files>
  <action>
    Migrate `pages/preguntas-frecuentes.vue` from `definePageMeta({ layout: "about" })` to the explicit wrapper:
      `<NuxtLayout name="about" title="Preguntas frecuentes" intro="...mockup intro..." active="faq"><FaqDefault :faqs="faqs || []" /></NuxtLayout>`
    Add `definePageMeta({ layout: false })` to the page script (REQUIRED — without it Nuxt also applies the default layout and double-wraps).
    OMIT FaqDefault's `title`/`text` props here so the layout header is the single h1 (after 07-02, FaqDefault renders a heading ONLY when `title` is passed; do NOT edit FaqDefault.vue or _faq.scss — owned by 07-02). The accordion shown is the 07-02 restyle.
    Keep the existing single useAsyncData (key "faqs") — already optimal (1 load); add `default: () => []` if absent. Keep $setSEO/$setStructuredData. This task edits ONLY the page file.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "preguntas-frecuentes" || echo "no faq page type errors"</automated>
    grep "NuxtLayout" apps/website/app/pages/preguntas-frecuentes.vue  →  present
    grep "layout: false" apps/website/app/pages/preguntas-frecuentes.vue  →  present
    VISUAL LOOP (logged-OUT, :3000; live mockup on :3002): screenshot http://localhost:3000/preguntas-frecuentes at 1440 + 390 → /tmp/waldo-shots/07-03-faq-{desktop,mobile}.png → Read → compare vs apps/design/index.dc.html lines 1104–1166 AND apps/design/screenshots/legal-faq.png (breadcrumb header, sticky sidebar w/ FAQ active, restyled accordion list, single h1) → fix the layout/page (NOT the accordion) → repeat until match.
  </verify>
  <done>FAQ page uses NuxtLayout with title/intro/active="faq", renders a single h1 (no duplicate), the 07-02 accordion in the panel; matches legal-faq.png on desktop+mobile; FaqDefault.vue/_faq.scss untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Mapa del sitio — 3-column panel inside the help-center layout</name>
  <files>apps/website/app/pages/sitemap.vue, apps/website/app/components/SitemapDefault.vue, apps/website/app/scss/components/_sitemap.scss</files>
  <action>
    Migrate `pages/sitemap.vue` to the NuxtLayout wrapper: `<NuxtLayout name="about" title="Mapa del sitio" intro="..." active="mapa"><SitemapDefault :blocks="sitemapBlocks" /></NuxtLayout>`, and set `definePageMeta({ layout: false })` (REQUIRED to avoid double-wrap). Keep the existing single useAsyncData ("sitemap-data", already Promise.all) and the block-building logic; add `default` if missing. Strip the duplicate `<h1>` from SitemapDefault (title now in layout header).
    Restyle SitemapDefault to the mockup mapa panel (lines 1222–1250): 3-column grid (Explorar / Tu cuenta / Información style — keep the existing dynamic blocks: Páginas principales + Categorías + Comunas), each column with an amber-dot eyebrow heading over a $line underline, link rows with right chevron and $line row separators, $ink2 -> $ink hover. Reference apps/design/screenshots/02-legal-mapa.png (the bare legal-mapa.png is a mislabeled home-hero render; 02-legal-mapa.png is the real mapa screen). `_sitemap.scss` already exists and is registered in app.scss — edit it in place (no app.scss change). BEM under `.sitemap--default__*`; tokens only; no existing-variable edits; no scale/box-shadow.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "sitemap|SitemapDefault" || echo "no sitemap type errors"</automated>
    VISUAL LOOP (logged-OUT, :3000; live mockup on :3002): screenshot http://localhost:3000/sitemap at 1440 + 390 → /tmp/waldo-shots/07-03-mapa-{desktop,mobile}.png → Read → compare vs apps/design/index.dc.html lines 1222-1250 AND apps/design/screenshots/02-legal-mapa.png (the REAL mapa render; bare legal-mapa.png is mislabeled home-hero) — breadcrumb header, sidebar Mapa active, 3-col link panel → fix → repeat until match.
  </verify>
  <done>Mapa del sitio renders in the help-center layout with sidebar "Mapa" active and a 3-column link panel matching 02-legal-mapa.png (the real mapa render); single h1; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- about layout exposes title/intro/active props; breadcrumb header + sticky sidebar + closing "Empieza ahora" CTA band match the mockup.
- FAQ + Mapa pages use `<NuxtLayout name="about" ...>` and render a single h1 (no duplicate title).
- FaqDefault.vue, _faq.scss, _accordion.scss are NOT in this plan's diff (owned by 07-02).
- _sitemap.scss edited in place; app.scss NOT modified (sitemap already registered).
- Screenshot-Read-compare passes at 1440 + 390, logged-OUT, vs legal-faq.png + 02-legal-mapa.png (bare legal-mapa.png is a mislabeled home-hero render).
- `git diff _variables.scss` shows no existing-variable changes.
- vue-tsc exits 0; reuse running :3000 server (compare vs :3002 live mockup); one heavy op at a time; no full test suite.
</verification>

<success_criteria>
The shared help-center layout is built and consumed by FAQ + Mapa: props-driven breadcrumb header, sticky active sidebar, content panel, closing CTA band, single h1 — ready for 07-04/05/06 to reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/07-publico-vistas-core/07-03-SUMMARY.md`.
</output>
