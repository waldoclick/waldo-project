---
phase: 069-strapi-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/article/content-types/article/schema.json
  - apps/website/app/types/article.d.ts
autonomous: true
requirements:
  - ARTC-01

must_haves:
  truths:
    - "`source_url` field is defined in the Article Strapi schema as a string, optional"
    - "Strapi Content Manager shows `source_url` as an editable field for Article entries"
    - "`GET /api/articles/:id` response includes `source_url` (string or null)"
    - "The website's `Article` TypeScript interface includes `source_url: string | null`"
  artifacts:
    - path: "apps/strapi/src/api/article/content-types/article/schema.json"
      provides: "Article content type definition with source_url attribute"
      contains: "source_url"
    - path: "apps/website/app/types/article.d.ts"
      provides: "TypeScript interface for Article consumed by website pages"
      contains: "source_url: string | null"
  key_links:
    - from: "apps/strapi/src/api/article/content-types/article/schema.json"
      to: "Strapi API response"
      via: "Strapi core serializer — field in schema → field in response"
      pattern: "\"source_url\""
    - from: "apps/website/app/types/article.d.ts"
      to: "apps/website/app/pages/noticias/[slug].vue"
      via: "Article interface import"
      pattern: "source_url"
---

<objective>
Add the `source_url` field to the Article content type in Strapi and to the website's TypeScript interface.

Purpose: Phase 070 (Dashboard Form & Detail) requires `source_url` to exist in the schema before form fields and detail view can consume it. This phase creates that foundation.
Output: Updated schema.json + article.d.ts. No controller/service changes needed — Strapi's core serializer includes all schema fields automatically.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add source_url attribute to Article schema.json</name>
  <files>apps/strapi/src/api/article/content-types/article/schema.json</files>
  <action>
    Add a `source_url` attribute to the `attributes` object in `schema.json`, following the same pattern as `seo_title` and `seo_description`.

    Insert after the `seo_description` attribute (before the closing `}` of `attributes`):

    ```json
    "source_url": {
      "type": "string",
      "required": false
    }
    ```

    Do NOT add `unique`, `maxLength`, or any other constraints — keep it minimal like the other optional string fields. Do NOT modify `options.draftAndPublish` (already `true`).
  </action>
  <verify>
    <automated>node -e "const s = require('./apps/strapi/src/api/article/content-types/article/schema.json'); console.assert(s.attributes.source_url && s.attributes.source_url.type === 'string', 'source_url missing'); console.log('OK')"</automated>
  </verify>
  <done>`schema.json` contains `"source_url": { "type": "string", "required": false }` in its `attributes`</done>
</task>

<task type="auto">
  <name>Task 2: Add source_url to Article TypeScript interface (website)</name>
  <files>apps/website/app/types/article.d.ts</files>
  <action>
    Add `source_url: string | null;` to the `Article` interface in `apps/website/app/types/article.d.ts`.

    Insert it after `seo_description: string | null;` and before `publishedAt: string | null;`, to keep related fields grouped:

    ```typescript
    seo_title: string | null;
    seo_description: string | null;
    source_url: string | null;
    publishedAt: string | null;
    ```

    The field is nullable because Strapi returns `null` for optional string fields that have not been set (not `undefined`). Do NOT change any other field or import.
  </action>
  <verify>
    <automated>yarn --cwd apps/website nuxt typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>`Article` interface includes `source_url: string | null` and `yarn nuxt typecheck` passes without new errors</done>
</task>

</tasks>

<verification>
After both tasks complete, confirm:

1. `node -e "const s = require('./apps/strapi/src/api/article/content-types/article/schema.json'); console.log(JSON.stringify(s.attributes.source_url))"` prints `{"type":"string","required":false}`
2. `grep source_url apps/website/app/types/article.d.ts` finds `source_url: string | null;`
3. `yarn --cwd apps/website nuxt typecheck` exits without new TypeScript errors
</verification>

<success_criteria>
- `source_url` attribute exists in Article `schema.json` as `{ "type": "string", "required": false }`
- `Article` TypeScript interface in website includes `source_url: string | null`
- TypeScript check passes (`nuxt typecheck`) — no regressions
- Phase 070 can safely add `source_url` form fields and detail view without schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/069-strapi-schema/069-01-SUMMARY.md`
</output>
