---
phase: 114-fix-codacy-best-practice-warnings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/cron/subscription-charge.cron.ts
  - apps/strapi/src/cron/bbdd-backup.cron.ts
  - apps/strapi/src/cron/verification-code-cleanup.cron.ts
  - apps/strapi/src/cron/ad-free-reservation-restore.cron.ts
  - apps/strapi/src/api/payment/controllers/payment.ts
  - apps/strapi/src/api/payment/utils/suscription.utils.ts
  - apps/strapi/src/api/payment/services/pro.service.ts
  - apps/strapi/src/api/article/content-types/article/lifecycles.ts
  - apps/strapi/src/api/commune/content-types/commune/lifecycles.ts
  - apps/strapi/src/api/region/content-types/region/lifecycles.ts
  - apps/strapi/src/api/category/content-types/category/lifecycles.ts
  - apps/strapi/src/api/ad/content-types/ad/lifecycles.ts
  - apps/strapi/src/api/condition/content-types/condition/lifecycles.ts
  - apps/strapi/src/api/contact/services/contact.service.ts
  - apps/strapi/src/api/related/types/ad.types.ts
  - apps/strapi/src/api/cron-runner/controllers/cron-runner.ts
  - apps/strapi/src/api/ad-reservation/controllers/ad-reservation.ts
  - apps/strapi/src/services/mjml/send-email.ts
  - apps/strapi/src/services/mjml/test.ts
  - apps/strapi/src/services/weather/http-client.ts
  - apps/strapi/src/extensions/users-permissions/controllers/authController.ts
autonomous: true
requirements: [CBP-03]

must_haves:
  truths:
    - "Zero `any` type annotations remain in strapi source files"
    - "Zero `Function` type usage remains in strapi source files"
    - "TypeScript compiles clean for strapi"
  artifacts:
    - path: "apps/strapi/src/cron/subscription-charge.cron.ts"
      provides: "EntityService calls without as Function"
      contains: "as unknown as Record<string, unknown>"
    - path: "apps/strapi/src/api/payment/controllers/payment.ts"
      provides: "controllerWrapper with typed handler param"
      contains: "(ctx: Context) => Promise<void>"
    - path: "apps/strapi/src/api/article/content-types/article/lifecycles.ts"
      provides: "Lifecycle events typed with Event"
      contains: "Event"
  key_links:
    - from: "apps/strapi/src/api/article/content-types/article/lifecycles.ts"
      to: "@strapi/database"
      via: "import type { Event }"
      pattern: "import type.*Event.*from.*@strapi/database"
    - from: "apps/strapi/src/api/contact/services/contact.service.ts"
      to: "@strapi/strapi"
      via: "import type { Core }"
      pattern: "import type.*Core.*from.*@strapi/strapi"
---

<objective>
Fix all Codacy best-practice `any` and `Function` type warnings in the Strapi app (~30 violations across ~20 files).

Purpose: Eliminate `any` and `Function` types from Strapi source so Codacy stops flagging violations. Pure type annotation changes with zero runtime behavior impact.
Output: All Strapi source files free of `any` and `Function` type annotations, TypeScript compiles clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-fix-codacy-best-practice-warnings-replace-any-with-unknown-function-type-and-require-statements-across-monorepo/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix lifecycle events, catch blocks, Core.Strapi params, and contact service in Strapi</name>
  <files>
    apps/strapi/src/api/article/content-types/article/lifecycles.ts,
    apps/strapi/src/api/commune/content-types/commune/lifecycles.ts,
    apps/strapi/src/api/region/content-types/region/lifecycles.ts,
    apps/strapi/src/api/category/content-types/category/lifecycles.ts,
    apps/strapi/src/api/ad/content-types/ad/lifecycles.ts,
    apps/strapi/src/api/condition/content-types/condition/lifecycles.ts,
    apps/strapi/src/cron/verification-code-cleanup.cron.ts,
    apps/strapi/src/api/payment/services/pro.service.ts,
    apps/strapi/src/api/cron-runner/controllers/cron-runner.ts,
    apps/strapi/src/services/weather/http-client.ts,
    apps/strapi/src/api/contact/services/contact.service.ts,
    apps/strapi/src/services/mjml/send-email.ts,
    apps/strapi/src/services/mjml/test.ts
  </files>
  <read_first>
    apps/strapi/src/api/article/content-types/article/lifecycles.ts,
    apps/strapi/src/api/commune/content-types/commune/lifecycles.ts,
    apps/strapi/src/api/region/content-types/region/lifecycles.ts,
    apps/strapi/src/api/category/content-types/category/lifecycles.ts,
    apps/strapi/src/api/ad/content-types/ad/lifecycles.ts,
    apps/strapi/src/api/condition/content-types/condition/lifecycles.ts,
    apps/strapi/src/cron/verification-code-cleanup.cron.ts,
    apps/strapi/src/api/payment/services/pro.service.ts,
    apps/strapi/src/api/cron-runner/controllers/cron-runner.ts,
    apps/strapi/src/services/weather/http-client.ts,
    apps/strapi/src/api/contact/services/contact.service.ts,
    apps/strapi/src/services/mjml/send-email.ts,
    apps/strapi/src/services/mjml/test.ts,
    apps/strapi/src/middlewares/protect-user-fields.ts
  </read_first>
  <action>
**Pattern 5 — Lifecycle events (event: any -> Event from @strapi/database):**
For ALL 6 lifecycle files, add this import at top:
```typescript
import type { Event } from "@strapi/database/dist/lifecycles";
```
Then change every `(event: any)` parameter to `(event: Event)`:
- `article/lifecycles.ts`: 2 occurrences (beforeCreate, beforeUpdate or similar)
- `commune/lifecycles.ts`: 1 occurrence
- `region/lifecycles.ts`: 1 occurrence
- `category/lifecycles.ts`: 1 occurrence
- `ad/lifecycles.ts`: 1 occurrence
- `condition/lifecycles.ts`: 2 occurrences

**Pattern 1 — catch blocks (error: any -> error: unknown):**
- `verification-code-cleanup.cron.ts`: Change `catch (error: any)` to `catch (error: unknown)`. Replace `error.message` with `error instanceof Error ? error.message : String(error)`.
- `pro.service.ts`: 3 catch blocks — change all from `catch (error: any)` to `catch (error: unknown)`. Narrow with `error instanceof Error ? error.message : String(error)`.
- `cron-runner.ts`: Same pattern.
- `http-client.ts`: Same pattern.

**Pattern 4 — strapi: any -> Core.Strapi:**
Add import where not present:
```typescript
import type { Core } from "@strapi/strapi";
```
Reference: `apps/strapi/src/middlewares/protect-user-fields.ts` already uses this pattern.

- `contact.service.ts`: Change `constructor(private readonly strapi: any)` to `constructor(private readonly strapi: Core.Strapi)`. Also change `ctx: any` in `getClientIp` to `ctx: Context` — import `Context` from `"koa"` (same pattern as payment controller).
- `send-email.ts`: Change `strapi: any` constructor param to `Core.Strapi`. Also define `EmailOptions` interface:
```typescript
interface EmailOptions {
  to: string | string[];
  from: string;
  subject: string;
  text: string;
  html: string;
  replyTo?: string;
}
```
Change `emailOptions: any` to `emailOptions: EmailOptions`.
- `test.ts`: Change `strapi: any` to `Core.Strapi`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn ": any\|as any" apps/strapi/src/api/article/content-types/article/lifecycles.ts apps/strapi/src/api/commune/content-types/commune/lifecycles.ts apps/strapi/src/api/region/content-types/region/lifecycles.ts apps/strapi/src/api/category/content-types/category/lifecycles.ts apps/strapi/src/api/ad/content-types/ad/lifecycles.ts apps/strapi/src/api/condition/content-types/condition/lifecycles.ts` returns empty
    - `grep -rn ": any" apps/strapi/src/cron/verification-code-cleanup.cron.ts apps/strapi/src/api/payment/services/pro.service.ts apps/strapi/src/api/cron-runner/controllers/cron-runner.ts apps/strapi/src/services/weather/http-client.ts` returns empty
    - `grep -rn ": any" apps/strapi/src/api/contact/services/contact.service.ts apps/strapi/src/services/mjml/send-email.ts apps/strapi/src/services/mjml/test.ts` returns empty
    - `cd apps/strapi && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All lifecycle events use Event type, catch blocks use unknown, strapi params use Core.Strapi, contact service uses Koa Context, email options typed. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix subscription-charge cron, payment controller, backup cron, reservation controller, ad.types, and authController in Strapi</name>
  <files>
    apps/strapi/src/cron/subscription-charge.cron.ts,
    apps/strapi/src/cron/bbdd-backup.cron.ts,
    apps/strapi/src/cron/ad-free-reservation-restore.cron.ts,
    apps/strapi/src/api/payment/controllers/payment.ts,
    apps/strapi/src/api/payment/utils/suscription.utils.ts,
    apps/strapi/src/api/related/types/ad.types.ts,
    apps/strapi/src/api/ad-reservation/controllers/ad-reservation.ts,
    apps/strapi/src/extensions/users-permissions/controllers/authController.ts
  </files>
  <read_first>
    apps/strapi/src/cron/subscription-charge.cron.ts,
    apps/strapi/src/cron/bbdd-backup.cron.ts,
    apps/strapi/src/cron/ad-free-reservation-restore.cron.ts,
    apps/strapi/src/api/payment/controllers/payment.ts,
    apps/strapi/src/api/payment/utils/suscription.utils.ts,
    apps/strapi/src/api/related/types/ad.types.ts,
    apps/strapi/src/api/ad-reservation/controllers/ad-reservation.ts,
    apps/strapi/src/extensions/users-permissions/controllers/authController.ts
  </read_first>
  <action>
**Pattern 7 — subscription-charge.cron.ts (as Function -> typed filter casts):**
All 6 `as Function` casts on entityService methods must be removed. Instead, remove `as Function` from the entityService call and cast the `filters` parameter using the established pattern:
```typescript
filters: { ... } as unknown as Record<string, unknown>,
```
This pattern is already used at line 71 of this same file. Apply it to all 6 remaining occurrences. The result type cast (`as FailedPaymentRecord[]` etc.) at the end of the expression can stay — those are return type assertions, not `Function` casts.

Also fix `error: any` catch blocks in this file (if any) per Pattern 1.

**Pattern 8 — payment controller (handler: Function -> typed callable):**
- `apps/strapi/src/api/payment/controllers/payment.ts`: Change `handler: Function` to `handler: (ctx: Context) => Promise<void>`. The `Context` import from `"koa"` should already exist in this file.

**Pattern 13 — bbdd-backup.cron.ts (config: any -> DatabaseConnectionConfig):**
Define interface at top of file:
```typescript
interface DatabaseConnectionConfig {
  host?: string;
  port?: number;
  database?: string;
  user?: string;
  password?: string;
  filename?: string;
  schema?: string;
}
```
Replace all `config: any` params in `buildMySQLBackupCommand`, `buildPostgresBackupCommand`, `buildSQLiteBackupCommand` with `config: DatabaseConnectionConfig`.
Replace `connection: any` with `connection: DatabaseConnectionConfig`.

**Pattern 20 — suscription.utils.ts:**
Define interface at top of file:
```typescript
interface ProviderSubscriptionData {
  subscriptionId?: string;
  customerId?: string;
  planId?: string;
  status?: number;
  next_invoice_date?: string;
  [key: string]: unknown;
}
```
Replace `providerSubscriptionData: any` with `providerSubscriptionData: ProviderSubscriptionData`.
Replace `let savedSubscription: any` with `let savedSubscription: Record<string, unknown> | null = null` (or appropriate type based on what it holds).
Fix `error: any` catch blocks per Pattern 1.

**Pattern 12 — ad.types.ts ([key: string]: any):**
- `apps/strapi/src/api/related/types/ad.types.ts`: If the interface has typed properties plus `[key: string]: any`, remove the index signature. If it only has the index signature, change to `Record<string, unknown>`.

**ad-free-reservation-restore.cron.ts — ) as any[]:**
Define an interface for the reservation record with known properties (id, documentId, etc.) and cast to that instead of `any[]`. Read the file to determine the exact shape of the records returned by entityService.findMany.

**ad-reservation controller — record as any, user as any:**
Read the file. Replace `record as any` with proper type assertion or narrow the type. Replace `user as any` with the correct user type (likely from Strapi context). If no specific type fits, use `as unknown as Record<string, unknown>` and access properties with explicit casts.

**Pattern 22 — authController.ts JSDoc {Function}:**
Replace JSDoc `{Function}` annotations with `{(ctx: import("koa").Context) => Promise<void>}` or simply `{(...args: unknown[]) => Promise<unknown>}`. These are comments only — verify Codacy actually flags them (low confidence). Make the change defensively since it's zero risk.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "as Function" apps/strapi/src/cron/subscription-charge.cron.ts` returns empty
    - `grep -n "handler: Function" apps/strapi/src/api/payment/controllers/payment.ts` returns empty
    - `grep -rn ": any\|as any" apps/strapi/src/cron/bbdd-backup.cron.ts` returns empty
    - `grep -rn ": any\|as any" apps/strapi/src/api/payment/utils/suscription.utils.ts` returns empty
    - `grep -rn ": any" apps/strapi/src/api/related/types/ad.types.ts` returns empty
    - `grep -rn "as any" apps/strapi/src/cron/ad-free-reservation-restore.cron.ts` returns empty
    - `grep -rn "as any" apps/strapi/src/api/ad-reservation/controllers/ad-reservation.ts` returns empty
    - `cd apps/strapi && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All `as Function` casts replaced with typed filter casts, payment controller uses typed callable, backup cron has DatabaseConnectionConfig, subscription utils typed, ad.types index signature fixed, reservation records typed. Zero `any`/`Function` annotations remain in Strapi. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `grep -rn ": any\b\|as any\|: any\[\]\|as Function\|: Function" apps/strapi/src/ --include="*.ts" | grep -v node_modules | grep -v __tests__ | grep -v "\.test\."` returns zero results
- `cd apps/strapi && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
Zero `any` and `Function` type annotations in Strapi source files (excluding test files). TypeScript compilation passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/114-fix-codacy-best-practice-warnings-replace-any-with-unknown-function-type-and-require-statements-across-monorepo/114-03-SUMMARY.md`
</output>
