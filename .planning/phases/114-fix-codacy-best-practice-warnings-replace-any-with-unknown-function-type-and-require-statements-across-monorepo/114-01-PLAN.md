---
phase: 114-fix-codacy-best-practice-warnings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/FormForgotPassword.vue
  - apps/website/app/components/CardHighlight.vue
  - apps/website/app/components/AccountOrders.vue
  - apps/website/app/components/CardShortcut.vue
  - apps/website/app/components/FormResetPassword.vue
  - apps/website/app/components/CardProfileAd.vue
  - apps/website/app/components/FormContact.vue
  - apps/website/app/components/AccountAnnouncements.vue
  - apps/website/app/components/FormPassword.vue
  - apps/website/app/components/FormRegister.vue
  - apps/website/app/types/user.d.ts
  - apps/website/app/plugins/site-health.client.ts
  - apps/website/app/plugins/gtm.client.ts
  - apps/website/app/plugins/dompurify.client.ts
  - apps/website/app/composables/useSanitize.ts
  - apps/website/app/composables/useIcons.ts
  - apps/website/app/pages/login/google.vue
  - apps/website/app/pages/login/facebook.vue
  - apps/website/app/pages/cuenta/mis-anuncios.vue
  - apps/website/nuxt.config.ts
  - apps/website/server/api/dev-login.post.ts
autonomous: true
requirements: [CBP-01]

must_haves:
  truths:
    - "Zero `any` type annotations remain in website app source files"
    - "Zero `as any` casts remain in website app source files"
    - "TypeScript typecheck passes clean for website app"
  artifacts:
    - path: "apps/website/app/components/FormForgotPassword.vue"
      provides: "Form submit with Record<string, unknown>"
      contains: "values: Record<string, unknown>"
    - path: "apps/website/app/components/CardHighlight.vue"
      provides: "Icon prop typed as Component"
      contains: "Component"
    - path: "apps/website/app/plugins/gtm.client.ts"
      provides: "gtag args typed as unknown[]"
      contains: "args: unknown[]"
  key_links:
    - from: "apps/website/app/components/CardHighlight.vue"
      to: "vue"
      via: "import type { Component }"
      pattern: "import type.*Component.*from.*vue"
---

<objective>
Fix all Codacy best-practice `any` type warnings in the website app (~22 violations across ~20 files).

Purpose: Eliminate `any` types from the website app so Codacy stops flagging best-practice violations. Pure type annotation changes with zero runtime behavior impact.
Output: All website source files free of `any` type annotations, TypeScript compiles clean.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-fix-codacy-best-practice-warnings-replace-any-with-unknown-function-type-and-require-statements-across-monorepo/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix catch blocks, form handlers, plugin params, and DOMPurify casts in website</name>
  <files>
    apps/website/app/components/FormForgotPassword.vue,
    apps/website/app/components/FormResetPassword.vue,
    apps/website/app/components/FormContact.vue,
    apps/website/app/components/FormPassword.vue,
    apps/website/app/components/FormRegister.vue,
    apps/website/app/plugins/site-health.client.ts,
    apps/website/app/plugins/gtm.client.ts,
    apps/website/app/plugins/dompurify.client.ts,
    apps/website/app/composables/useSanitize.ts,
    apps/website/app/pages/login/google.vue,
    apps/website/app/pages/login/facebook.vue,
    apps/website/server/api/dev-login.post.ts,
    apps/website/nuxt.config.ts
  </files>
  <read_first>
    apps/website/app/components/FormForgotPassword.vue,
    apps/website/app/components/FormResetPassword.vue,
    apps/website/app/components/FormContact.vue,
    apps/website/app/components/FormPassword.vue,
    apps/website/app/components/FormRegister.vue,
    apps/website/app/plugins/site-health.client.ts,
    apps/website/app/plugins/gtm.client.ts,
    apps/website/app/plugins/dompurify.client.ts,
    apps/website/app/composables/useSanitize.ts,
    apps/website/app/pages/login/google.vue,
    apps/website/app/pages/login/facebook.vue,
    apps/website/server/api/dev-login.post.ts,
    apps/website/nuxt.config.ts,
    apps/website/app/composables/useOrderById.ts
  </read_first>
  <action>
Apply these specific fixes per research patterns:

**Pattern 1 — catch blocks (error: any -> error: unknown):**
- `apps/website/server/api/dev-login.post.ts`: Change `catch (error: any)` to `catch (error: unknown)`. Replace `error.message` with `error instanceof Error ? error.message : String(error)`.
- `apps/website/app/pages/login/google.vue`: Same pattern — `catch (error: any)` to `catch (error: unknown)`, narrow with `error instanceof Error ? error.message : String(error)`.
- `apps/website/app/pages/login/facebook.vue`: Same pattern.
- `apps/website/app/components/FormPassword.vue`: Same pattern for any `catch (error: any)` block.
- `apps/website/app/components/FormRegister.vue`: Replace `error as any` casts with `error as unknown` and narrow with `instanceof Error`.

**Reference pattern (already in codebase):**
```typescript
// apps/website/app/composables/useOrderById.ts
} catch (error: unknown) {
  const msg = error instanceof Error ? error.message : String(error);
}
```

**Pattern 2 — vee-validate form handlers (values: any -> Record<string, unknown>):**
- `FormForgotPassword.vue`: Change `(values: any)` to `(values: Record<string, unknown>)`. At each property access, cast: `values.email as string`.
- `FormResetPassword.vue`: Same — `(values: any)` to `(values: Record<string, unknown>)`. Cast `values.password as string`, `values.passwordConfirmation as string`.
- `FormContact.vue`: Same — `(values: any)` to `(values: Record<string, unknown>)`. Cast each accessed property as its expected type.
- `FormPassword.vue`: Same for both submit handlers if two exist.

**Pattern 3 — defineNuxtPlugin (nuxtApp: any -> remove type):**
- `apps/website/app/plugins/site-health.client.ts`: Remove `: any` from `(nuxtApp: any)` — `defineNuxtPlugin` already types it correctly. Result: `defineNuxtPlugin((nuxtApp) => {`.

**Pattern 11 — gtag args (any[] -> unknown[]):**
- `apps/website/app/plugins/gtm.client.ts`: Change `(...args: any[])` to `(...args: unknown[])`.

**Pattern 17 — nuxt.config.ts (as any -> proper cast):**
- `apps/website/nuxt.config.ts`: Replace `} as any,` with `} as Record<string, unknown>,` or remove the cast if the structure matches the expected type. If removal causes TS errors, use `// @ts-expect-error -- Nuxt head config type mismatch` on the line above.

**Pattern 18/19 — DOMPurify window casts:**
- `apps/website/app/plugins/dompurify.client.ts`: Replace `(window as any).DOMPurify` with `(window as unknown as Record<string, unknown>).DOMPurify` or check if `window.d.ts` already declares `DOMPurify` on `Window` — if so, use `window.DOMPurify` directly. Read `apps/website/app/types/window.d.ts` to check.
- `apps/website/app/composables/useSanitize.ts`: Same fix for `(window as any).DOMPurify` references.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && npx nuxt typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn ": any\|as any\|any\[\]" apps/website/app/components/FormForgotPassword.vue apps/website/app/components/FormResetPassword.vue apps/website/app/components/FormContact.vue apps/website/app/components/FormPassword.vue apps/website/app/components/FormRegister.vue` returns empty
    - `grep -rn ": any\|as any" apps/website/app/plugins/site-health.client.ts apps/website/app/plugins/gtm.client.ts apps/website/app/plugins/dompurify.client.ts` returns empty
    - `grep -rn "as any" apps/website/app/composables/useSanitize.ts` returns empty
    - `grep -rn ": any" apps/website/app/pages/login/google.vue apps/website/app/pages/login/facebook.vue apps/website/server/api/dev-login.post.ts` returns empty
    - `grep -n "as any" apps/website/nuxt.config.ts` returns empty
    - `cd apps/website && npx nuxt typecheck` exits 0
  </acceptance_criteria>
  <done>All catch blocks, form handlers, plugin params, DOMPurify casts, and nuxt.config in website use proper types instead of `any`. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix icon props, index signatures, user types, and composables in website</name>
  <files>
    apps/website/app/components/CardHighlight.vue,
    apps/website/app/components/CardShortcut.vue,
    apps/website/app/components/AccountOrders.vue,
    apps/website/app/components/AccountAnnouncements.vue,
    apps/website/app/components/CardProfileAd.vue,
    apps/website/app/types/user.d.ts,
    apps/website/app/composables/useIcons.ts,
    apps/website/app/pages/cuenta/mis-anuncios.vue,
    apps/website/app/pages/anuncios/[slug].vue
  </files>
  <read_first>
    apps/website/app/components/CardHighlight.vue,
    apps/website/app/components/CardShortcut.vue,
    apps/website/app/components/AccountOrders.vue,
    apps/website/app/components/AccountAnnouncements.vue,
    apps/website/app/components/CardProfileAd.vue,
    apps/website/app/types/user.d.ts,
    apps/website/app/composables/useIcons.ts,
    apps/website/app/pages/cuenta/mis-anuncios.vue,
    apps/website/app/pages/anuncios/[slug].vue
  </read_first>
  <action>
Apply these specific fixes:

**Pattern 6 — icon props (any -> Component from vue):**
- `CardHighlight.vue`: Change `icon: any` to `icon: Component`. Add `import type { Component } from "vue";` at top of script setup.
- `CardShortcut.vue`: Change `iconComponent: any` to `iconComponent: Component`. Add `import type { Component } from "vue";`.
- `AccountAnnouncements.vue`: Change `icon: any` prop to `icon: Component`. Add `import type { Component } from "vue";`.
- `apps/website/app/pages/cuenta/mis-anuncios.vue`: Change `icon: any` to `icon: Component`. Add `import type { Component } from "vue";`.

**Pattern 12 — index signatures ([key: string]: any -> remove or narrow):**
- `AccountOrders.vue`: The `Order` interface has `[key: string]: any` plus typed properties (id, status, total, amount, is_invoice, createdAt, updatedAt). **Remove the `[key: string]: any` line entirely** — the typed properties are sufficient.
- `AccountAnnouncements.vue`: The `Announcement` interface has `id: number` and `[key: string]: any`. Replace with `Record<string, unknown> & { id: number }` or define all known properties and remove the index signature.

**Pattern for useIcons.ts:**
- `apps/website/app/composables/useIcons.ts`: Change `{ [key: string]: any }` return type to `Record<string, Component>`. Add `import type { Component } from "vue";`.

**Pattern 23 — user.d.ts (any[] -> Record<string, unknown>[]):**
- `apps/website/app/types/user.d.ts`: Change `ads?: any[]` to `ads?: Record<string, unknown>[]` and `orders?: any[]` to `orders?: Record<string, unknown>[]`.

**CardProfileAd.vue — response: any:**
- Read the file to identify the exact `any` usage. Replace `response: any` with the appropriate type — likely the Strapi response shape or a defined ad type. Use `Record<string, unknown>` if no specific type exists.

**[slug].vue (anuncios) — check actual violation:**
- Read the file and find any `any` annotations. Fix per the applicable pattern (likely catch block or response type).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && npx nuxt typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn ": any\|as any\|any\[\]" apps/website/app/components/CardHighlight.vue apps/website/app/components/CardShortcut.vue apps/website/app/components/AccountOrders.vue apps/website/app/components/AccountAnnouncements.vue apps/website/app/components/CardProfileAd.vue` returns empty
    - `grep -rn "any" apps/website/app/types/user.d.ts` returns no `any` type annotations (may match "company" etc. — check only `: any` pattern)
    - `grep -n ": any\|any\}" apps/website/app/composables/useIcons.ts` returns empty
    - `grep -n ": any" apps/website/app/pages/cuenta/mis-anuncios.vue` returns empty
    - `cd apps/website && npx nuxt typecheck` exits 0
  </acceptance_criteria>
  <done>All icon props use `Component` from vue, index signatures removed or narrowed, user.d.ts uses `Record<string, unknown>[]`, useIcons returns `Record<string, Component>`. Zero `any` annotations remain in website app. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `grep -rn ": any\b\|as any\|: any\[\]\|any\}" apps/website/app/ apps/website/server/ --include="*.ts" --include="*.vue" | grep -v node_modules | grep -v ".nuxt"` returns zero results
- `grep -n "as any" apps/website/nuxt.config.ts` returns zero results
- `cd apps/website && npx nuxt typecheck` exits 0
</verification>

<success_criteria>
Zero `any` type annotations in website app source files. TypeScript compilation passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/114-fix-codacy-best-practice-warnings-replace-any-with-unknown-function-type-and-require-statements-across-monorepo/114-01-SUMMARY.md`
</output>
