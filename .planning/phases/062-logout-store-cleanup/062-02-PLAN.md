---
phase: 062-logout-store-cleanup
plan: 02
type: execute
wave: 2
depends_on: [062-01]
files_modified:
  - apps/website/app/components/MenuUser.vue
  - apps/website/app/components/MobileBar.vue
  - apps/website/app/components/SidebarAccount.vue
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "MenuUser.vue calls useLogout().logout() — no inline useStrapiAuth logout code remains"
    - "MobileBar.vue calls useLogout().logout() — no inline useStrapiAuth logout code remains"
    - "SidebarAccount.vue calls useLogout().logout() — no inline useStrapiAuth logout code remains"
    - "nuxt typecheck passes with zero errors across all of apps/website"
  artifacts:
    - path: "apps/website/app/components/MenuUser.vue"
      provides: "Component using useLogout()"
      contains: "useLogout"
    - path: "apps/website/app/components/MobileBar.vue"
      provides: "Component using useLogout()"
      contains: "useLogout"
    - path: "apps/website/app/components/SidebarAccount.vue"
      provides: "Component using useLogout()"
      contains: "useLogout"
  key_links:
    - from: "MenuUser.vue / MobileBar.vue / SidebarAccount.vue"
      to: "apps/website/app/composables/useLogout.ts"
      via: "useLogout() composable call"
      pattern: "useLogout"
---

<objective>
Replace the three identical inline logout implementations in the three entry-point components with a single call to `useLogout().logout()`. This eliminates code duplication and ensures every logout path goes through the centralized store cleanup sequence.

Purpose: With the `useLogout` composable from Plan 01 now handling store resets, the components only need to call it. This satisfies QUAL-01 (centralized, no duplication) and connects all logout entry points to the cleanup pipeline.

Output: Three updated components with no inline logout logic, clean typecheck.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/062-logout-store-cleanup/062-01-SUMMARY.md

@apps/website/app/components/MenuUser.vue
@apps/website/app/components/MobileBar.vue
@apps/website/app/components/SidebarAccount.vue
</context>

<interfaces>
<!-- What useLogout exports (created by Plan 01). No exploration needed. -->

From apps/website/app/composables/useLogout.ts:
```typescript
export const useLogout = () => {
  const logout = async (): Promise<void> => { ... };
  return { logout };
};
```

Current logout pattern in each component (to be replaced):
```typescript
// MenuUser.vue:108 and SidebarAccount.vue:123
const { logout } = useStrapiAuth();
// ... inside handler:
await logout();
router.push("/");   // or navigateTo("/")

// MobileBar.vue:188 — also closes mobile menu first
const { logout } = useStrapiAuth();
// ... inside handler:
appStore.closeMobileMenu();
await logout();
router.push("/");
```

After refactor, each component handler becomes:
```typescript
// All three components:
const { logout } = useLogout();
// ... inside handler:
await logout();
// navigateTo("/") is handled inside useLogout — do NOT add router.push here
```

For MobileBar: `appStore.closeMobileMenu()` call BEFORE `await logout()` is PRESERVED — `useAppStore.$reset()` in the composable already closes the menu, but the explicit close-before-logout is a no-op and can be left or removed. Prefer REMOVING it since $reset() handles it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline logout in MenuUser.vue, MobileBar.vue, SidebarAccount.vue</name>
  <files>
    apps/website/app/components/MenuUser.vue,
    apps/website/app/components/MobileBar.vue,
    apps/website/app/components/SidebarAccount.vue
  </files>
  <action>
    In each component, make the following surgical changes:

    **All three components:**
    1. Remove the line: `const { logout } = useStrapiAuth();`
    2. Add: `const { logout } = useLogout();`
    3. Remove the `router.push("/")` (or `navigateTo("/")`) call that follows `await logout()` — navigation is now inside the composable
    4. Keep `await logout()` as-is — it now calls the composable's logout, which handles everything

    **MobileBar.vue specifically:**
    - Also remove `appStore.closeMobileMenu()` from the logout handler — `useAppStore.$reset()` inside `useLogout` resets `isMobileMenuOpen` to false, making the explicit close redundant
    - If `appStore` is no longer used anywhere else in the component after removing the closeMobileMenu call, remove the `const appStore = useAppStore()` line too. Read the component carefully — `appStore` may be used for other things (e.g., toggling the menu); only remove the line if it becomes completely unused.

    **Do NOT** change any other component logic. Do NOT change template markup. Do NOT change event handler names. Do NOT add try/catch. Do NOT import `useLogout` manually — it is auto-imported by Nuxt from the `composables/` directory.

    Verify after each file edit that the handler function still has a proper `async` declaration wrapping `await logout()`.
  </action>
  <verify>
    <automated>grep -rn "useStrapiAuth" apps/website/app/components/MenuUser.vue apps/website/app/components/MobileBar.vue apps/website/app/components/SidebarAccount.vue</automated>
  </verify>
  <done>
    Zero occurrences of `useStrapiAuth` in the three components. Each component has `const { logout } = useLogout()` and calls `await logout()` in its handler.
  </done>
</task>

<task type="auto">
  <name>Task 2: TypeScript typecheck — zero errors</name>
  <files></files>
  <action>
    Run the TypeScript typecheck for the website app and fix any errors that arise from the changes in this phase.

    Command: `yarn workspace apps/website typecheck`

    Common errors to expect and fix:
    - If `router` is now unused after removing `router.push("/")` in a component → remove the `const router = useRouter()` line (only if router is not used elsewhere in that component)
    - If `useStrapiAuth` import is now unused → it's auto-imported, no explicit import to remove
    - If `useAppStore` is unused in MobileBar after removing `closeMobileMenu()` → remove `const appStore = useAppStore()` if and only if appStore has no other uses in the component

    If typecheck passes on first run, no changes needed. If errors exist, fix them minimally — do not refactor unrelated code.
  </action>
  <verify>
    <automated>yarn workspace apps/website typecheck 2>&1 | grep -c "error TS" || echo "0 errors"</automated>
  </verify>
  <done>
    `yarn workspace apps/website typecheck` exits with zero TypeScript errors. Output shows "Found 0 errors" or equivalent.
  </done>
</task>

</tasks>

<verification>
Final checks after both tasks:
1. `grep -rn "useStrapiAuth" apps/website/app/components/MenuUser.vue apps/website/app/components/MobileBar.vue apps/website/app/components/SidebarAccount.vue` → zero results
2. `grep -rn "useLogout" apps/website/app/components/MenuUser.vue apps/website/app/components/MobileBar.vue apps/website/app/components/SidebarAccount.vue` → 3 results (one per file)
3. `yarn workspace apps/website typecheck` → zero errors
4. `yarn workspace apps/website vitest run` → all tests pass (including useLogout.test.ts from Plan 01)
</verification>

<success_criteria>
- All three logout entry points use `useLogout()` — zero inline `useStrapiAuth` logout calls remain in the three components
- No duplicated logout logic anywhere in the codebase
- `yarn workspace apps/website typecheck` passes with zero errors
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/062-logout-store-cleanup/062-02-SUMMARY.md`
</output>
