---
phase: 06-cierre-cuenta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/MenuUser.vue
  - apps/website/app/scss/components/_menu.scss
autonomous: true
requirements: [HDR-02]
must_haves:
  truths:
    - "The user-menu trigger shows avatar/initials + 'Hola' + first name + chevron"
    - "The open dropdown shows a header row (avatar + name + close), grouped links, and 'Cerrar sesión'"
    - "'Cerrar sesión' still calls useLogout with the existing confirmation Swal"
    - "Click-outside still closes the dropdown"
  artifacts:
    - path: "apps/website/app/components/MenuUser.vue"
      provides: "Restyled dropdown matching UserMenu.dc.html, logout behavior unchanged"
    - path: "apps/website/app/scss/components/_menu.scss"
      provides: "menu--user tokens matching the mockup"
      contains: "menu--user"
  key_links:
    - from: "apps/website/app/components/MenuUser.vue"
      to: "useLogout"
      via: "handleLogout confirmation then logout()"
      pattern: "useLogout|handleLogout"
---

<objective>
Restyle the user dropdown (`MenuUser.vue` + `_menu.scss`) to the `design/UserMenu.dc.html` mockup: a white pill trigger (avatar + "Hola" + first name + chevron) and a 256px dropdown card (header row with avatar/name/close, grouped link sections, and a "Cerrar sesión" row with a logout icon). **Reuse the existing component** — keep `useLogout`, the confirmation Swal, click-outside, and manager link; only restyle and re-structure the markup to the mockup.

Purpose: HDR-02 — close the menu visual gap.
Output: Restyled MenuUser dropdown; logout behavior unchanged.

NOTE: visual front — orchestrator runs pixel verification; do NOT instruct screenshots. Encode exact token values.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cierre-cuenta/06-CONTEXT.md

# Mockup
@design/UserMenu.dc.html

# Files to edit
@apps/website/app/components/MenuUser.vue
@apps/website/app/scss/components/_menu.scss

<interfaces>
<!-- Phase-04 tokens (do NOT modify _variables.scss): -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;  $amber: #f7c97e;  $cream: #f6f4f1;  $line: #ece9e4;

<!-- UserMenu.dc.html target values: -->
trigger:   display:flex; align-items:center; gap:10px; background:#fff; border:1px solid $line;
           border-radius:4px; padding:5px 12px 5px 5px; hover background:$cream
avatar:    34px circle, background:$amber, color:$ink, font-weight:700, font-size:13px (initials fallback)
greeting:  "Hola" 11px $muted weight 500 ; name 14px weight 600 $ink
chevron:   16px, rotates 180deg when open (transition .25s)
dropdown:  position:absolute; top:calc(100% + 12px); right:0; width:256px; background:#fff;
           border:1px solid $line; border-radius:4px; overflow:hidden; z-index:60;
           box-shadow:0 18px 38px -16px rgba(20,18,30,.22)
header row: avatar 36px + "Hola"/name + close button; padding:15px 16px; border-bottom:1px $line
link:      display:block; padding:9px 12px; border-radius:4px; font-size:14.5px; weight 500; color:$ink2;
           hover background:$cream; color:$ink. Groups separated by border-top:1px $line; padding:6px wrapper
logout:    flex row, gap:9px, lucide log-out icon 16px, font-size:14.5px weight 600 color:$ink, hover $cream
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle MenuUser trigger + dropdown to UserMenu.dc.html, preserving logout/click-outside/manager link</name>
  <read_first>
    - apps/website/app/components/MenuUser.vue (current: button trigger with AvatarDefault + greeting; nav with manager link, account links, packs/faq/policy links, logout button; handleLogout with Swal + useLogout; click-outside via handleClickOutside)
    - design/UserMenu.dc.html (target structure: trigger pill, dropdown card with header row + grouped links + logout)
    - apps/website/app/scss/components/_menu.scss (current menu--user block)
  </read_first>
  <files>apps/website/app/components/MenuUser.vue, apps/website/app/scss/components/_menu.scss</files>
  <action>
    Re-structure the template and restyle `_menu.scss` to the mockup. PRESERVE behavior:
    - Keep `useLogout`, `handleLogout` (the Swal confirmation "¿Estás seguro de cerrar sesión?" → `logout()`) — do not change logout logic.
    - Keep the manager link (`v-if="isManager"` → dashboard/site toggle) as its own group.
    - Keep `handleClickOutside` / `onMounted` / `onUnmounted` click-outside, and the `isOpen` toggle.
    - Keep all existing NuxtLink destinations and their Spanish labels (Mi cuenta, Mis anuncios, Mi perfil, Cambiar contraseña, Comprar packs, Preguntas frecuentes, Políticas de privacidad).

    Template structure (BEM under `.menu--user`):
    - Trigger button `menu--user__button`: avatar (`menu--user__button__avatar` — keep AvatarDefault), greeting block `menu--user__button__greetings` ("Hola" + `__name` first name), and a chevron element `menu--user__button__chevron` (replace the current Menu/X icon-in-circle toggle with the mockup's down-chevron that rotates when open). Use a lucide `ChevronDown` (`import { ChevronDown as IconChevron } from "lucide-vue-next"`) and rotate via a `:class` / `is-open` modifier (transform in SCSS, allowed for rotation — NOT scale).
    - Dropdown `menu--user__menu` (keep `:class="{ 'is-open': isOpen }"`): add a header row `menu--user__menu__head` (avatar 36px + greeting + close button `menu--user__menu__head__close` wired to `menuOpen`/close, lucide `X`). Keep the existing `<ul class="menu--user__menu__links">` groups; the last group is the logout row — give the logout button a lucide `LogOut` icon (`import { LogOut as IconLogout }`).
    - Remove the obsolete commented-out PRO `<li>` blocks (dead markup) while editing — do not leave commented code.

    SCSS (`.menu--user` in `_menu.scss`): apply the `<interfaces>` target values (trigger pill, avatar amber circle, dropdown card 256px with the exact box-shadow, header row, link hover, group separators, logout row). Use tokens only. No box-shadow on hover of links; the dropdown card's drop shadow from the mockup is permitted (it is a static elevation, not a hover scale). Keep transitions simple (chevron rotate + dropdown open).

    Constraints: BEM block--modifier__element under `.menu--user`; no standalone hyphenated classes; English identifiers; Spanish UI text preserved; no static inline styles; no unused imports (if AvatarDefault/IconMenu/IconX are replaced, remove unused ones).
  </action>
  <verify>
    <automated>cd apps/website && npx nuxt typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Logout preserved: `grep -c "useLogout\|handleLogout" apps/website/app/components/MenuUser.vue` ≥ 2 AND `grep -c "cerrar sesión\|Cerrar sesión" apps/website/app/components/MenuUser.vue` ≥ 1
    - Click-outside preserved: `grep -c "handleClickOutside" apps/website/app/components/MenuUser.vue` ≥ 2
    - Manager link preserved: `grep -c "isManager" apps/website/app/components/MenuUser.vue` ≥ 1
    - Chevron + logout icons present: `grep -c "ChevronDown\|LogOut" apps/website/app/components/MenuUser.vue` ≥ 2
    - Dropdown width + shadow: `grep -c "256px" apps/website/app/scss/components/_menu.scss` ≥ 1 AND `grep -c "box-shadow" apps/website/app/scss/components/_menu.scss` ≥ 1
    - No commented-out PRO `<li>` blocks remain: `grep -c "PRO</b>" apps/website/app/components/MenuUser.vue` returns 0
    - No unused imports (IconMenu/IconX removed if no longer used); no static inline `style="..."`
  </acceptance_criteria>
  <done>
    MenuUser renders the mockup trigger pill (avatar + Hola + name + rotating chevron) and the 256px dropdown (header row + grouped links + logout row with icon). Logout confirmation + click-outside + manager link all still work. typecheck clean, no unused imports.
  </done>
</task>

</tasks>

<verification>
- `npx nuxt typecheck` passes (no new MenuUser errors).
- Logout flow unchanged (Swal confirm → useLogout).
- Visual pixel verification deferred to the orchestrator.
</verification>

<success_criteria>
The user dropdown matches `UserMenu.dc.html` (trigger pill, 256px card with header row, grouped links, logout row) while logout, click-outside, and the manager link keep working.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cierre-cuenta/06-03-SUMMARY.md`
</output>
