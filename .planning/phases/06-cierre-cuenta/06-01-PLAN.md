---
phase: 06-cierre-cuenta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/index.ts
autonomous: true
requirements: [STAT-PERM]
must_haves:
  truths:
    - "On every Strapi boot, the Authenticated role holds the four stats action permissions"
    - "GET /api/ads/:documentId/stats returns 200 (not 403) for the ad owner"
    - "GET /api/ads/me/views-total returns 200 (not 403) for an authenticated user"
    - "Re-running bootstrap creates no duplicate permission rows (idempotent)"
  artifacts:
    - path: "apps/strapi/src/index.ts"
      provides: "Idempotent permission grant in bootstrap() for the Authenticated role"
      contains: "plugin::users-permissions.role"
  key_links:
    - from: "apps/strapi/src/index.ts bootstrap()"
      to: "plugin::users-permissions.permission rows"
      via: "create only when action UID not already present on the authenticated role"
      pattern: "plugin::users-permissions.permission"
---

<objective>
Grant the Strapi **Authenticated** role the permissions for the stats endpoints so the account stats UI stops receiving 403s. The project has **no permission seeder**, so add an idempotent grant inside the existing `bootstrap()` function that runs on every start (guarded against duplicates).

Purpose: STAT-PERM — `ad-view.stats` and `ad-view.panelViewsTotal` currently return 403 because the Authenticated role lacks the permission. This unblocks the Panel KPI and the per-ad stats modal (consumed by plan 06-05).
Output: Authenticated role holds the four action UIDs after boot; stats endpoints respond 200 for owners.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cierre-cuenta/06-CONTEXT.md

# Files to edit / model after
@apps/strapi/src/index.ts

<interfaces>
<!-- In-repo idiom for querying the role (apps/strapi/src/extensions/users-permissions/controllers/userController.ts:243):
     strapi.db.query("plugin::users-permissions.role").findMany({ where: { type: { $in: [...] } } })
     Use the SAME query model UIDs below. -->

Role model UID:       plugin::users-permissions.role        (authenticated role: { type: "authenticated" })
Permission model UID: plugin::users-permissions.permission
  VERIFIED from the plugin source (@strapi/plugin-users-permissions@5.41.1
  server/content-types/permission/index.js): the permission content-type has ONLY two
  attributes — `action: string` (required) and `role: relation manyToOne`. There is NO
  `enabled` field in v5. The plugin's own role.js creates rows with EXACTLY:
      strapi.db.query('plugin::users-permissions.permission').create({ data: { action: actionID, role: role.id } })
  Copy that create shape verbatim — do NOT add `enabled`.

Action UIDs to grant (Strapi v5 format `api::<api>.<controller>.<action>`, derived from the custom route handlers):
  - api::ad-view.ad-view.stats              (GET /api/ads/:documentId/stats)
  - api::ad-view.ad-view.panelViewsTotal    (GET /api/ads/me/views-total)
  - api::ad-contact.ad-contact.recordContact (POST /api/ads/:documentId/contact — route is auth:false; grant kept for fidelity, see note)
  - api::ad-contact.ad-contact.contactsTotal (GET /api/ads/me/contacts-total — route added by plan 06-04; grant ahead so it never 403s)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotent stats-permission grant to bootstrap()</name>
  <read_first>
    - apps/strapi/src/index.ts (the existing bootstrap function — append after the sort_priority backfill, inside the same try/catch style)
    - apps/strapi/src/extensions/users-permissions/controllers/userController.ts lines 240-246 (the exact `strapi.db.query("plugin::users-permissions.role")` idiom this project uses)
  </read_first>
  <files>apps/strapi/src/index.ts</files>
  <action>
    In `bootstrap({ strapi })`, AFTER the existing `recalculateSortPriorities()` block, add a new guarded block that grants the Authenticated role the stats permissions. Implementation:

    1. Define a const array of the four action UIDs (copy verbatim from the `<interfaces>` block above):
       `api::ad-view.ad-view.stats`, `api::ad-view.ad-view.panelViewsTotal`,
       `api::ad-contact.ad-contact.recordContact`, `api::ad-contact.ad-contact.contactsTotal`.

    2. Resolve the authenticated role using the in-repo idiom:
       `const role = await strapi.db.query("plugin::users-permissions.role").findOne({ where: { type: "authenticated" }, populate: ["permissions"] });`
       If `!role`, log a warning and return (do not throw).

    3. Build a `Set<string>` of existing action UIDs from `role.permissions` (cast each row as `{ action: string }`). This Set IS the idempotency guard.

    4. For each target UID NOT already in the Set, create the permission row:
       `await strapi.db.query("plugin::users-permissions.permission").create({ data: { action: uid, role: role.id } });`
       (NO `enabled` field — it does not exist on the v5 permission content-type; including it would fail the create.)
       Count how many were created.

    5. `console.log` a summary, e.g. `stats permissions grant: N created, M already present`.

    6. Wrap the whole block in try/catch; on error `console.error("Error granting stats permissions:", error)` — never throw (must not crash boot), matching the existing backfill block's style.

    Constraints:
    - TypeScript, no `any` — cast query results as needed (e.g. `as Array<{ action: string }>`), documented inline.
    - Do NOT modify the seeders block or the sort_priority block.
    - Do NOT flip any route's `auth` config. The grant is additive only.
    - English identifiers and comments.

    NOTE (record in SUMMARY): `recordContact` route is `auth:false`, so its permission is a no-op for access — it is granted only for D-05 fidelity. The real 403→200 flips are `stats`, `panelViewsTotal`, and (after 06-04) `contactsTotal`.
  </action>
  <verify>
    <automated>cd apps/strapi && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "plugin::users-permissions.permission" apps/strapi/src/index.ts` ≥ 1
    - `grep -c "type: \"authenticated\"" apps/strapi/src/index.ts` ≥ 1
    - All four UIDs present: `grep -c "api::ad-view.ad-view.stats\|api::ad-view.ad-view.panelViewsTotal\|api::ad-contact.ad-contact.recordContact\|api::ad-contact.ad-contact.contactsTotal" apps/strapi/src/index.ts` returns 4
    - The create uses `{ action: ..., role: role.id }` with NO `enabled` key: `grep -c "enabled" apps/strapi/src/index.ts` returns 0
    - The grant block is inside `bootstrap` and wrapped in try/catch (no throw)
  </acceptance_criteria>
  <done>
    Bootstrap grants the four action UIDs to the Authenticated role on each start, skipping any already present. tsc clean. After a Strapi restart, `GET /api/ads/me/views-total` and `GET /api/ads/:documentId/stats` return 200 for an authenticated owner instead of 403.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in apps/strapi with no new errors.
- After restart (`pnpm dev` / Strapi boot), the boot log shows the "stats permissions grant" summary line.
- Manual confirm (orchestrator, optional): with a valid `waldo_jwt`, `GET /api/ads/me/views-total` returns 200.
</verification>

<success_criteria>
The Authenticated role holds `api::ad-view.ad-view.stats`, `api::ad-view.ad-view.panelViewsTotal`, `api::ad-contact.ad-contact.recordContact`, and `api::ad-contact.ad-contact.contactsTotal` after every boot, with no duplicate rows on re-run.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cierre-cuenta/06-01-SUMMARY.md`
</output>
