---
phase: 06-cierre-cuenta
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-04"]
files_modified:
  - apps/website/app/components/CardProfileAd.vue
  - apps/website/app/scss/components/_card.scss
  - apps/website/app/components/AccountMain.vue
  - apps/website/app/stores/user.store.ts
autonomous: true
requirements: [ACT-01, STAT-UI]
must_haves:
  truths:
    - "No CardProfileAd action button is a no-op (handleDeactivate bug fixed; every action calls a real handler)"
    - "Per real status, the card renders the correct primary action + overflow menu items from the mockup"
    - "Active/expiring cards show 'N vistas · N contactos' from real per-ad stats"
    - "Panel 'Contactos recibidos' KPI shows the real contacts-total (not 0)"
  artifacts:
    - path: "apps/website/app/components/CardProfileAd.vue"
      provides: "Wired per-status actions + real vistas·contactos meta"
      contains: "handleDeactivate()"
    - path: "apps/website/app/stores/user.store.ts"
      provides: "loadContactsTotal() store action"
      contains: "loadContactsTotal"
    - path: "apps/website/app/components/AccountMain.vue"
      provides: "Panel contacts KPI wired to loadContactsTotal"
      contains: "loadContactsTotal"
  key_links:
    - from: "apps/website/app/components/CardProfileAd.vue handleDeactivate"
      to: "appStore.openDeactivateLightbox"
      via: "@click handleDeactivate() (with parens)"
      pattern: "handleDeactivate\\(\\)"
    - from: "apps/website/app/components/CardProfileAd.vue"
      to: "userStore.loadAdStats"
      via: "lazy on-mount stats load for active/expiring cards"
      pattern: "loadAdStats"
    - from: "apps/website/app/components/AccountMain.vue"
      to: "userStore.loadContactsTotal"
      via: "useAsyncData panel aggregate"
      pattern: "loadContactsTotal"
---

<objective>
Close the **account card** gaps (ACT-01 + STAT-UI UI half), all in one owner of `CardProfileAd.vue` / `_card.scss`:
1. Fix the real no-op bug `@click="handleDeactivate; toggleMenu()"` → `handleDeactivate()` and wire every per-status action from the mockup (no dead buttons).
2. Show real **"N vistas · N contactos"** meta on active/expiring cards via the existing `loadAdStats` (lazy per-card; gated to published/expiring; reuse, no batch endpoint).
3. Wire the Panel **"Contactos recibidos"** KPI to the new `GET /ads/me/contacts-total` (06-04) via a `loadContactsTotal` store action.

Depends on 06-01 (stats permission → stats endpoint returns 200, not 403) and 06-04 (`contacts-total` endpoint + real contact events). Wave 2.

Purpose: ACT-01 (no no-ops) + STAT-UI (card meta + real Panel contacts).
Output: functional, mockup-accurate ad cards with real stats meta; Panel contacts KPI shows real data.

NOTE: card meta is a visual front — orchestrator runs pixel verification; do NOT instruct screenshots. Encode exact mockup values.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cierre-cuenta/06-CONTEXT.md

# Mockup actions per status + meta
@design/account.dc.html

# Files to edit
@apps/website/app/components/CardProfileAd.vue
@apps/website/app/scss/components/_card.scss
@apps/website/app/components/AccountMain.vue
@apps/website/app/stores/user.store.ts

<interfaces>
<!-- Store (user.store.ts) — already present: -->
loadAdStats(documentId: string, days = 14): Promise<{ total, series, contacts, conversion, avgPerDay }>  // total=views, contacts=contacts
loadPanelViewsTotal(): Promise<{ total }>
deactivateAd(adDocumentId, reason?): Promise<...>
// ADD: loadContactsTotal(): Promise<{ total }> → GET "ads/me/contacts-total" (mirror loadPanelViewsTotal exactly)

<!-- app.store.ts: openDeactivateLightbox(adDocumentId: string) → opens the "dar de baja" lightbox -->

<!-- STATUS MAPPING — real enum (props.ad.status) vs mockup labels. Use the REAL enum, never invent 'activo'/'vendido'/'baja': -->
published (+ remaining_days>7)   = "Activo"        → meta: "N vistas · N contactos"
published (+ remaining_days<=7)  = "Vence en N días" (expiring) → meta: "N vistas · N contactos"
review                            = "En revisión"
expired                           = "Expirado"
rejected                          = "Rechazado"
banned                            = "Baneado"

<!-- BACKEND REALITY (confirmed): ad has /deactivate (dar de baja) + republish is a frontend store-fill → /anunciar.
     There is NO mark-as-sold endpoint and NO feature-existing-ad endpoint. -->

<!-- Per-status ACTIONS to render (adapt mockup actionModelFor to the real enum + real handlers): -->
published/expiring:
  - primary: if NOT featured → "Destacar" (amber); if featured → "Estadísticas" (secondary, opens StatsAdModal)
  - overflow menu: "Estadísticas" (if not already primary, opens modal), "Ver anuncio" (link to /anuncios/:slug),
    "Marcar como vendido", "Dar de baja" (danger → handleDeactivate())
expired:  primary "Republicar" (handleRepublish) ; menu: "Ver anuncio"
rejected: primary "Ver motivo" (handleRejectedClick Swal)
banned:   primary "Ver motivo" (handleBannedClick Swal)
review:   primary "Previsualizar" (link to /anuncios/:slug) — keep existing

<!-- NO-BACKEND fallbacks (D-03: never a dead button) — pick the explicit behavior now: -->
"Destacar"           → navigate to the packs/featured purchase flow (nuxt-link / navigateTo "/packs"), OR a Swal informativo
                        explaining featuring is purchased there. Choose navigateTo("/packs") (concrete, no dead handler).
"Marcar como vendido" → Swal informativo (es): explain it isn't available yet and suggest "Dar de baja".
                        (No backend; must be an informative Swal, never a no-op.) Spanish copy in the Swal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loadContactsTotal store action + wire Panel "Contactos recibidos" KPI</name>
  <read_first>
    - apps/website/app/stores/user.store.ts (loadPanelViewsTotal — copy structure for loadContactsTotal; add to the returned object)
    - apps/website/app/components/AccountMain.vue (useAsyncData "account-panel" Promise.all; current totalContacts derives from contact_count which is always 0)
  </read_first>
  <files>apps/website/app/stores/user.store.ts, apps/website/app/components/AccountMain.vue</files>
  <action>
    1. STORE: add `loadContactsTotal(): Promise<{ total: number }>` mirroring `loadPanelViewsTotal` exactly but hitting `client("ads/me/contacts-total", { method: "GET" })`, same try/catch → `{ total: 0 }` fallback, same `{ data: PanelViewsTotalResult }` cast shape. Export it in the store's return object.

    2. PANEL (AccountMain.vue): in the `useAsyncData("account-panel")` `Promise.all`, add `userStore.loadContactsTotal()` and return its `.total` as `totalContacts` in the data object + default. Replace the current `totalContacts` computed (the `contact_count` reduce that always yields 0) with `computed(() => panel.value.totalContacts)`. Keep the rest of the panel logic intact.

    Constraints: TypeScript no `any` (store-style casts); useAsyncData with `default` provided (existing); English identifiers; Spanish UI untouched; no unused imports.
  </action>
  <verify>
    <automated>cd apps/website && npx nuxt typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loadContactsTotal" apps/website/app/stores/user.store.ts` ≥ 2 (def + export)
    - `grep -c "ads/me/contacts-total" apps/website/app/stores/user.store.ts` ≥ 1
    - `grep -c "loadContactsTotal" apps/website/app/components/AccountMain.vue` ≥ 1
    - `grep -c "contact_count" apps/website/app/components/AccountMain.vue` returns 0 (old derivation removed)
  </acceptance_criteria>
  <done>
    Panel "Contactos recibidos" reads the real contacts-total aggregate; store exposes loadContactsTotal; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix handleDeactivate no-op + wire all per-status card actions to the mockup</name>
  <read_first>
    - apps/website/app/components/CardProfileAd.vue (template actions block lines ~40-127; the bug `@click="handleDeactivate; toggleMenu()"` line ~74; existing handlers handleRepublish, handleRejectedClick, handleBannedClick, handleDeactivate, handleOpenStats)
    - design/account.dc.html lines 724-763 (actionModelFor — per-status primary + menu)
    - apps/website/app/scss/components/_card.scss lines 10-160 (card--profileAd block + actions sub-tree)
  </read_first>
  <files>apps/website/app/components/CardProfileAd.vue, apps/website/app/scss/components/_card.scss</files>
  <action>
    Make every action real and match the mockup's per-status model (using the REAL enum + handlers from `<interfaces>`):

    1. FIX THE BUG: line ~74 `@click="handleDeactivate; toggleMenu()"` → `@click="handleDeactivate(); toggleMenu()"` (add parens — currently the function is referenced, never called).

    2. Per-status actions (`published`/expiring, `expired`, `rejected`, `banned`, `review`) — extend the existing `<template v-if>` branches so each renders the mockup's primary + overflow menu:
       - published/expiring: primary = "Destacar" (amber, `card--profileAd__actions__primary`) when `!ad.featured` else "Estadísticas" (secondary). Overflow menu items: "Estadísticas" (when not primary → `handleOpenStats()`), "Ver anuncio" (link to `/anuncios/:slug`, target _blank), "Marcar como vendido" (→ Swal informativo), "Dar de baja" (danger → `handleDeactivate()`). Keep the existing dropdown structure (`__overflow`, `__menu-btn`, `__backdrop`, `__dropdown`, `__dropdown__item`, `--danger`).
       - "Destacar": `navigateTo("/packs")` (concrete navigation — NOT a dead handler). Add a `handleFeature` that calls `navigateTo("/packs")`, or use a nuxt-link; either is fine, but it MUST navigate.
       - "Marcar como vendido": add `handleMarkSold` → `Swal.fire({ ... })` informativo in Spanish (e.g. title "Marcar como vendido", text explaining it's not available yet and to use "Dar de baja", confirmButtonText "Entendido"). NEVER a no-op.
       - expired: primary "Republicar" → `handleRepublish` (exists); menu "Ver anuncio".
       - rejected/banned: primary "Ver motivo" → existing handleRejectedClick / handleBannedClick.
       - review: keep "Previsualizar" link.
       Do NOT introduce a new `status` value (`'sold'`, `'baja'`) — there is no such enum and no backend for it.

    3. SCSS: ensure `card--profileAd__actions__*` classes exist for primary (amber), secondary (white/border), menu-btn (38px), dropdown + items + `--danger` (per mockup lines 729-744 / 185-198). Reuse existing classes; add only missing element styles under the `card--profileAd` namespace. Tokens only ($amber, $ink, $line, $cream, $error). No box-shadow on hover, no scale.

    4. Remove any now-unused imports; add needed lucide icons (e.g. `Star`, `CircleCheck`, `CircleOff`) as used. Keep StatsAdModal wiring.

    Constraints: BEM under `.card--profileAd`; no standalone hyphenated classes; English identifiers; Spanish UI text; no static inline styles (data-bound `:style` only for category color, already present); no unused vars/imports.
  </action>
  <verify>
    <automated>cd apps/website && npx nuxt typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Bug fixed: `grep -c "handleDeactivate; toggleMenu" apps/website/app/components/CardProfileAd.vue` returns 0 AND `grep -c "handleDeactivate(); toggleMenu\|handleDeactivate()" apps/website/app/components/CardProfileAd.vue` ≥ 1
    - No-backend actions are real: `grep -c "handleMarkSold\|Marcar como vendido" apps/website/app/components/CardProfileAd.vue` ≥ 1 AND `grep -c "navigateTo(\"/packs\")\|/packs" apps/website/app/components/CardProfileAd.vue` ≥ 1
    - No invented status values: `grep -c "'sold'\|'baja'\|'vendido'\|'activo'" apps/website/app/components/CardProfileAd.vue` returns 0
    - Every menu/button has a handler (no bare ` @click="" ` or function-without-call): manual grep `grep -nE '@click="[a-zA-Z]+[^("]' apps/website/app/components/CardProfileAd.vue` returns only calls with `()` (no reference-only handlers)
    - Actions SCSS present: `grep -c "card--profileAd__actions__primary\|card--profileAd__actions__dropdown" apps/website/app/scss/components/_card.scss` ≥ 1
    - No unused imports (typecheck/eslint clean)
  </acceptance_criteria>
  <done>
    Every card action calls a real handler; the handleDeactivate parens bug is fixed; Destacar navigates to /packs; Marcar como vendido shows a Spanish informativo Swal; per-status primary+menu match the mockup using the real enum. typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Show real "N vistas · N contactos" meta on active/expiring cards</name>
  <read_first>
    - apps/website/app/components/CardProfileAd.vue (statusMessage computed → __meta__right span line ~36; script setup; props.ad.documentId; userStore.loadAdStats already imported via store)
    - apps/website/app/stores/user.store.ts (loadAdStats(documentId, days) → { total, contacts, ... })
    - design/account.dc.html line 738 (metaFor: active/expiring → 'N vistas · N contactos')
    - apps/website/app/scss/components/_card.scss (card--profileAd__body__meta sub-tree)
  </read_first>
  <files>apps/website/app/components/CardProfileAd.vue</files>
  <action>
    For active/expiring cards (status `published`), replace the "Expira en N días" message in the meta-right slot with the real **"N vistas · N contactos"** (mockup metaFor). Implementation (lazy per-card, reuse loadAdStats — no batch endpoint):

    1. In `<script setup>`, add reactive `views` and `contacts` refs (default 0) and an async loader that, ONLY when `props.ad?.status === "published"` and `props.ad?.documentId`, calls `useUserStore().loadAdStats(props.ad.documentId)` and sets `views.value = res.total; contacts.value = res.contacts`. Trigger it on mount client-side (`onMounted` guarded by `import.meta.client`, or `watch` per CLAUDE.md component data-loading rules — use the pattern consistent with sibling components; loadAdStats already swallows errors → 0 fallback).
    2. Add a computed `metaRight` that returns:
       - published → `\`${views.value} vistas · ${contacts.value} contactos\``
       - else → the existing `statusMessage` value (keep expired/review/rejected/banned text unchanged).
    3. Bind `__meta__right` span to `metaRight` instead of `statusMessage`.

    Efficiency: one stats call per visible published card on mount (no N+1 inside a loop, no polling). Do NOT build a batch endpoint (scope). Stats endpoint is permission-gated and returns 200 after 06-01.

    Constraints: TypeScript no `any`; no static inline styles; English identifiers; Spanish UI text ("vistas", "contactos"); no unused imports.
  </action>
  <verify>
    <automated>cd apps/website && npx nuxt typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vistas · " apps/website/app/components/CardProfileAd.vue` ≥ 1 (uses the · separator) OR `grep -c "vistas\b" CardProfileAd.vue` ≥ 1 AND `grep -c "contactos" CardProfileAd.vue` ≥ 1
    - `grep -c "loadAdStats" apps/website/app/components/CardProfileAd.vue` ≥ 1
    - meta-right bound to the new computed: `grep -c "metaRight" apps/website/app/components/CardProfileAd.vue` ≥ 2
    - stats load gated to published: `grep -c "published" apps/website/app/components/CardProfileAd.vue` ≥ 1
    - no per-frame / loop stats calls (single load on mount/watch)
  </acceptance_criteria>
  <done>
    Active/expiring cards display real "N vistas · N contactos" from loadAdStats (lazy, one call per card on mount); other statuses keep their existing meta text. typecheck clean.
  </done>
</task>

</tasks>

<verification>
- `npx nuxt typecheck` passes (no new errors in CardProfileAd, AccountMain, user.store).
- No no-op buttons remain in CardProfileAd (handleDeactivate fixed; every action calls a handler).
- After 06-01 + 06-04 + Strapi restart: Panel "Contactos recibidos" and card meta show real numbers.
- Visual pixel verification (card meta) deferred to the orchestrator.
</verification>

<success_criteria>
Cards are fully functional and mockup-accurate: no no-ops, correct per-status actions, real "N vistas · N contactos" meta; the Panel "Contactos recibidos" KPI shows real contacts-total data.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cierre-cuenta/06-05-SUMMARY.md`
</output>
