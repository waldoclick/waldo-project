---
phase: 13-catalog-segments-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/pages/orders/index.vue
  - apps/dashboard/app/pages/orders/[id].vue
  - apps/dashboard/app/pages/users/index.vue
  - apps/dashboard/app/pages/users/[id].vue
autonomous: true
requirements:
  - URL-08
  - URL-11

must_haves:
  truths:
    - "Navigating to /orders loads the orders list page"
    - "Navigating to /orders/[id] loads the order detail page"
    - "Navigating to /users loads the users list page"
    - "Navigating to /users/[id] loads the user detail page"
    - "No file in apps/dashboard/app/pages/orders/ contains a /ordenes/ route reference"
    - "No file in apps/dashboard/app/pages/users/ contains a /usuarios/ route reference"
  artifacts:
    - path: "apps/dashboard/app/pages/orders/index.vue"
      provides: "Orders list page at /orders"
    - path: "apps/dashboard/app/pages/orders/[id].vue"
      provides: "Order detail page at /orders/[id]"
    - path: "apps/dashboard/app/pages/users/index.vue"
      provides: "Users list page at /users"
    - path: "apps/dashboard/app/pages/users/[id].vue"
      provides: "User detail page at /users/[id]"
  key_links:
    - from: "apps/dashboard/app/pages/orders/[id].vue"
      to: "/orders"
      via: "breadcrumb { label: 'Orders', to: '/orders' }"
      pattern: "ordenes|orders"
    - from: "apps/dashboard/app/pages/users/[id].vue"
      to: "/users"
      via: "breadcrumb { label: 'Users', to: '/users' }"
      pattern: "usuarios|users"
---

<objective>
Rename the `ordenes` and `usuarios` page directories to `orders` and `users` respectively. Update every internal route reference within those files from `/ordenes/...` → `/orders/...` and `/usuarios/...` → `/users/...`.

Purpose: Fulfils URL-08 and URL-11 — the `/orders` and `/users` route trees become canonical English paths.
Output: New `apps/dashboard/app/pages/orders/` and `apps/dashboard/app/pages/users/` directories with correctly named and correctly wired `.vue` files. The old `ordenes/` and `usuarios/` directories are deleted.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ads-migration/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename ordenes and usuarios directories</name>
  <files>
    apps/dashboard/app/pages/orders/index.vue
    apps/dashboard/app/pages/orders/[id].vue
    apps/dashboard/app/pages/users/index.vue
    apps/dashboard/app/pages/users/[id].vue
  </files>
  <action>
    Use `git mv` to rename so Git tracks renames cleanly (same pattern as Phase 12 ads migration):

    ```bash
    # Orders
    git mv apps/dashboard/app/pages/ordenes apps/dashboard/app/pages/orders

    # Users
    git mv apps/dashboard/app/pages/usuarios apps/dashboard/app/pages/users
    ```

    Both directories contain only `index.vue` and `[id].vue` — no sub-directories, no filename translations needed. The files move as-is.

    After the renames, verify:
    ```
    apps/dashboard/app/pages/orders/
      index.vue
      [id].vue

    apps/dashboard/app/pages/users/
      index.vue
      [id].vue
    ```
  </action>
  <verify>ls apps/dashboard/app/pages/orders/ shows index.vue and [id].vue; ls apps/dashboard/app/pages/users/ shows index.vue and [id].vue; neither apps/dashboard/app/pages/ordenes/ nor apps/dashboard/app/pages/usuarios/ exists</verify>
  <done>All 4 files are in orders/ and users/ directories. Old ordenes/ and usuarios/ directories are gone from the filesystem.</done>
</task>

<task type="auto">
  <name>Task 2: Update internal route references in orders and users pages</name>
  <files>
    apps/dashboard/app/pages/orders/index.vue
    apps/dashboard/app/pages/orders/[id].vue
    apps/dashboard/app/pages/users/index.vue
    apps/dashboard/app/pages/users/[id].vue
  </files>
  <action>
    Update every Spanish route string inside the 4 renamed files to its English equivalent.

    **orders/index.vue** — inspect for any `/ordenes/` references and update if present.

    **orders/[id].vue** — update breadcrumb:
    - `{ label: "Órdenes", to: "/ordenes" }` → `{ label: "Orders", to: "/orders" }`

    **users/index.vue** — inspect for any `/usuarios/` references and update if present.

    **users/[id].vue** — update breadcrumb:
    - `{ label: "Usuarios", to: "/usuarios" }` → `{ label: "Users", to: "/users" }`

    Do NOT change API call strings (Strapi collection names), Spanish UI labels, or anything outside route path strings.

    After edits, confirm no Spanish route strings remain:
    ```bash
    grep -r "/ordenes/" apps/dashboard/app/pages/orders/
    grep -r "/usuarios/" apps/dashboard/app/pages/users/
    ```
    Expected: no output from either command.
  </action>
  <verify>
    <automated>grep -r "/ordenes/" apps/dashboard/app/pages/orders/ && echo "FAIL" || echo "PASS orders"; grep -r "/usuarios/" apps/dashboard/app/pages/users/ && echo "FAIL" || echo "PASS users"</automated>
  </verify>
  <done>
    - orders/[id].vue breadcrumb references /orders.
    - users/[id].vue breadcrumb references /users.
    - No /ordenes/ or /usuarios/ string present in their respective directories.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls apps/dashboard/app/pages/orders/` — shows index.vue, [id].vue
2. `ls apps/dashboard/app/pages/users/` — shows index.vue, [id].vue
3. `ls apps/dashboard/app/pages/ordenes/` — returns "No such file or directory"
4. `ls apps/dashboard/app/pages/usuarios/` — returns "No such file or directory"
5. `grep -r "/ordenes/" apps/dashboard/app/pages/orders/` — no output
6. `grep -r "/usuarios/" apps/dashboard/app/pages/users/` — no output
7. `git status` — shows renames tracked by Git (not deletions + additions)
</verification>

<success_criteria>
- `apps/dashboard/app/pages/orders/` exists with index.vue, [id].vue
- `apps/dashboard/app/pages/users/` exists with index.vue, [id].vue
- `apps/dashboard/app/pages/ordenes/` does not exist
- `apps/dashboard/app/pages/usuarios/` does not exist
- Zero `/ordenes/` or `/usuarios/` route strings inside the renamed directories
- Git history shows renames (not file deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-catalog-segments-migration/13-03-SUMMARY.md` using the summary template.
</output>
