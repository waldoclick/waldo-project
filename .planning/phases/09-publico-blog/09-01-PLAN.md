---
phase: 09-publico-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/blog/index.vue
  - apps/website/app/components/HeroArticles.vue
  - apps/website/app/components/FilterArticles.vue
  - apps/website/app/components/ArticleArchive.vue
  - apps/website/app/components/CardArticle.vue
  - apps/website/app/scss/components/_articles.scss
  - apps/website/app/scss/components/_card.scss
autonomous: true
requirements: [PUB-BLOG]
must_haves:
  truths:
    - "Blog listing (/blog) renders to the mockup look (cream breadcrumb hero, toolbar, 3-col card grid, pager) logged-OUT on desktop 1440 and mobile 390"
    - "Hero clears the fixed/overlay headroom header (~130px top padding) — no content hidden behind the bar"
    - "Each blog card matches BlogCard.dc.html (16/9 media, category pill with color dot, Poppins title, 2-line excerpt, date + read-time meta) and does NOT fire its own endpoint (no N+1)"
    - "/blog loads its page data in a single useAsyncData (one network wave), keeping its unique key + object default"
    - "Colors/typography use phase-04 tokens ($ink, $ink2, $muted, $amber, $tag, $cream, $line); no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/components/CardArticle.vue"
      provides: "Restyled blog card matching BlogCard.dc.html — OWNER for the phase (09-02 reuses it)"
    - path: "apps/website/app/scss/components/_card.scss"
      provides: "Restyled .card--article block (line ~922)"
      contains: "card--article"
    - path: "apps/website/app/scss/components/_articles.scss"
      provides: "Restyled .hero--articles + .filter--articles + .article--archive to mockup"
    - path: "apps/website/app/pages/blog/index.vue"
      provides: "Listing composition + single useAsyncData data load"
      contains: "useAsyncData"
  key_links:
    - from: "apps/website/app/pages/blog/index.vue"
      to: "section components (HeroArticles/FilterArticles/ArticleArchive)"
      via: "component composition only (no BEM/HTML in the page)"
      pattern: "<ArticleArchive"
    - from: "apps/website/app/components/ArticleArchive.vue"
      to: "apps/website/app/components/CardArticle.vue"
      via: "v-for grid render"
      pattern: "<CardArticle"
    - from: "card SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$cream|\\$tag"
---

<objective>
Restyle the public Blog listing page (`pages/blog/index.vue`) to the v1.47 mockup ("Blog" screen, `design/index.dc.html` lines ~1239–1312, plus `design/BlogCard.dc.html`): cream breadcrumb hero with "Blog Waldo" tag + h1 + intro, a filter toolbar, a 3-column blog-card grid, and a minimal pager.

Purpose: Blog listing is Fase 3 of the public rebrand. It OWNS the blog card restyle (`CardArticle.vue` + the `.card--article` block in `_card.scss`) for the whole phase — 09-02 reuses the restyled card in its "Sigue leyendo" band and must not re-touch it.
Output: Restyled listing (hero, toolbar, card grid, pager) matching the mockup + reference screenshots, on desktop 1440 + mobile 390, logged-OUT, loading data in a single wave.

SCOPE — VISUAL RESTYLE ONLY (this is the "Rediseño visual" milestone, precedent = phase 07):
- IN: restyle existing markup/SCSS of the hero, the existing category + order selects, the card, the grid and the pager to the mockup look.
- OUT (defer to user approval — do NOT build): free-text article search input, custom multi-select category dropdown with checkboxes, and "Limpiar filtros". These are NEW behaviors the current page does not have. Keep the existing native `<select>` controls (category + order) and restyle them toward the mockup toolbar look. Surface these deferrals in the SUMMARY.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — read the relevant screens, do NOT copy inline styles. Translate to BEM + phase-04 tokens:
@design/index.dc.html
@design/BlogCard.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/blog.png
@design/screenshots/blog-grid.png
@design/screenshots/blog-list.png
@design/screenshots/blog-p2.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/blog/index.vue
@apps/website/app/components/CardArticle.vue
@apps/website/app/components/HeroArticles.vue
@apps/website/app/components/FilterArticles.vue
@apps/website/app/components/ArticleArchive.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- CardArticle.vue IS the BlogCard.dc.html equivalent — restyle it; do NOT create a new BlogCard component (CLAUDE.md create-only-if-no-equivalent rule). -->
<!-- BlogCard category color: mockup derives a per-category hue via oklch (HUE map). Do NOT hand-roll oklch in SCSS.
     Use a simple amber-tinted pill (rgba(247,201,126,.28) background + $tag text + a small color dot) consistent with the phase tokens — the category pill in blog.png / blog-grid.png reads as a single amber-family treatment. -->
<!-- Current /blog data load: ONE useAsyncData (key blog-{category}-{page}-{order}) running categoriesStore.loadCategories() then articlesStore.loadArticles() sequentially, with a related-articles fallback only when the result is empty. This is already a single wave with default — NO N+1 (CardArticle uses transformUrl, a pure URL helper, not an endpoint). Keep this; do not manufacture a refactor. -->

<!-- .card--article block lives in _card.scss at line ~922. .hero--articles / .filter--articles / .article--archive live in _articles.scss. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the blog card (CardArticle.vue + .card--article) to BlogCard.dc.html — OWNER for the phase</name>
  <files>apps/website/app/components/CardArticle.vue, apps/website/app/scss/components/_card.scss</files>
  <action>
    Translate `design/BlogCard.dc.html` to the existing `.card card--article` BEM block. THIS PLAN IS THE OWNER of the card restyle for the whole phase — 09-02 reuses it in "Sigue leyendo" and must NOT re-touch CardArticle/_card.scss.
    Update markup in CardArticle.vue only where structure requires it; push all visual rules to the `.card--article` block in `_card.scss` (line ~922).
    Per BlogCard.dc.html: white card, $line 1px border, 4px radius, column flex, full height.
    - MEDIA: 16/9 ratio top block. When a cover exists, `object-fit:cover` fills it; when absent, the existing empty-state image / placeholder. Category pill is absolutely positioned top-left (top/left 14px): amber-tinted background (rgba(247,201,126,.28)), $tag text, 700, 11px, uppercase, .05em tracking, 4px radius, with a small 7px color dot — keep it a single amber-family treatment (do NOT hand-roll oklch hues in SCSS).
    - BODY: padding 16px 18px 18px, gap 9px. h3 Poppins 700 18px $ink, -.3px tracking, line-height 1.3. Excerpt $ink2 14px, line-height 1.55, clamped to 2 lines (-webkit-line-clamp:2). Meta row pushed to bottom (margin-top:auto, padding-top 12px): date $muted 12.5px, a 3px dot separator, then a clock icon + "N min de lectura" read-time label.
    - READ-TIME: the mockup shows "N min de lectura". If the Article type has no read-time field, derive a simple estimate from body length (e.g. ~200 words/min, min 1) in the component as a computed — keep it a pure local helper, no endpoint. Keep the existing date Intl formatting.
    - The whole card is a link to `/blog/${article.slug}` (preserve current NuxtLink navigation). Keep `useImageProxy`/`transformUrl` for the cover — do NOT add any endpoint call (no N+1).
    CLAUDE.md: BEM strict (every child under `.card--article__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform add (mockup's hover lift is banned here — keep transitions simple, e.g. border-color only). Tokens only; do NOT modify existing SCSS variables (add a NEW variable only if the mockup needs a color absent from the palette).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CardArticle" || echo "no CardArticle type errors"</automated>
    grep -n "card--article" apps/website/app/scss/components/_card.scss → block present and restyled.
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another): screenshot http://localhost:3000/blog at 1440x900 → save /tmp/waldo-shots/09-01-card-desktop.png → Read PNG → compare a single card element-by-element vs design/BlogCard.dc.html AND design/screenshots/blog-grid.png (media ratio, category pill, title type, 2-line excerpt clamp, date + read-time meta) → fix SCSS → re-screenshot → repeat until it matches.
  </verify>
  <done>CardArticle renders to BlogCard.dc.html on desktop + mobile, logged-OUT; no endpoint per card; no box-shadow/scale; tokens only; vue-tsc clean. Card restyle is final (owner for the phase).</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the listing hero + toolbar + grid + pager (HeroArticles, FilterArticles, ArticleArchive, _articles.scss)</name>
  <files>apps/website/app/components/HeroArticles.vue, apps/website/app/components/FilterArticles.vue, apps/website/app/components/ArticleArchive.vue, apps/website/app/scss/components/_articles.scss</files>
  <action>
    Translate the "Blog" screen (`design/index.dc.html` lines ~1239–1312) to the existing `.hero--articles`, `.filter--articles` and `.article--archive` BEM blocks. Markup changes in the .vue files only where structure requires; push all visual rules to `_articles.scss`.
    - HERO (`.hero--articles`, lines ~1240–1252): section on $cream with $line bottom border and the soft amber radial glow (top-right). Inner max-width 1200px. Breadcrumb row (Waldo › Blog) in $muted with a chevron + bold $ink current. Then a "Blog Waldo" tag pill (rgba(247,201,126,.28) bg, $tag text, book icon). Then h1 Poppins 800 ~clamp(32px,3.6vw,46px) $ink "Activos industriales, explicados", then an $ink2 17px intro paragraph (max-width 680px). Keep BreadcrumbsDefault if it already renders the Waldo › Blog crumb to spec; otherwise restyle within the block. CRITICAL: top padding must clear the 06-02 fixed/overlay headroom header — mockup uses 108px; carry ~130px (account for the ~74px bar) so nothing hides behind it. Verify against blog.png that the tag/h1 are fully visible below the header.
    - TOOLBAR (`.filter--articles`, mockup lines ~1255–1280): a single horizontal row above the grid. Restyle the EXISTING category `<select>` and order `<select>` as the mockup's white pill controls (white bg, $line border, 4px radius, 12–14px padding, Poppins 600 14px $ink, hover border $ink). Do NOT build the free-text search input, the custom checkbox dropdown, or "Limpiar filtros" — those are deferred (note in SUMMARY). Keep the existing v-model + router.push query behavior unchanged.
    - GRID (`.article--archive__list`): 3 columns desktop (repeat(3,minmax(0,1fr)), gap 24px), collapsing to 2 then 1 on smaller widths; max-width 1200px container, padding to mockup (40px top, 88px bottom). It renders CardArticle (restyled in Task 1) — do not touch the card here.
    - PAGER (`.article--archive__paginate`): restyle the existing vue-awesome-paginate output to the mockup's minimal numbered buttons (square, $line border, active = $ink fill / white text) per blog-grid.png. Keep the existing onClickHandler / router query behavior. Do not replace the pagination library.
    BEM strict; tokens only; no existing-variable edits.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroArticles|FilterArticles|ArticleArchive" || echo "no listing type errors"</automated>
    VISUAL LOOP (logged-OUT): screenshot http://localhost:3000/blog full-page at 1440x900 and 390x844 → save /tmp/waldo-shots/09-01-listing-{desktop,mobile}.png → Read each PNG → compare hero (glow, tag, h1, intro, header clearance), toolbar pills, 3-col grid, and pager element-by-element vs design/index.dc.html "Blog" screen AND blog.png + blog-grid.png + blog-list.png + blog-p2.png → fix SCSS → re-screenshot → repeat until match. Confirm desktop 3 cols and mobile single column; confirm nothing hides behind the fixed header.
  </verify>
  <done>Listing hero, toolbar, 3-col grid and pager match the mockup + reference PNGs on desktop and mobile, logged-OUT; hero clears the headroom header; existing select/pager behavior preserved; deferrals (search/multiselect/clear) noted; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm single-wave data load + composition wiring on the listing page</name>
  <files>apps/website/app/pages/blog/index.vue</files>
  <action>
    Audit and (only if needed) tidy the data load on `pages/blog/index.vue`. CURRENT: ONE useAsyncData (key `blog-{category}-{page}-{order}`) that runs categoriesStore.loadCategories() then articlesStore.loadArticles() sequentially inside the callback, with an object `default`, watching category/page/order. This already satisfies the requirement: a single network wave, unique key, default, no double-fetch, no N+1 (CardArticle does not call an endpoint). Do NOT manufacture a refactor.
    Confirm and keep:
    - Exactly one useAsyncData on the page (grep == 1).
    - The object `default: () => ({...})` is present (eliminates T|undefined).
    - No bare `await storeAction()` paired with useAsyncData (no double-fetch).
    - The page stays composition-only: it imports and arranges HeaderDefault / HeroArticles / FilterArticles / ArticleArchive / FooterDefault (and the existing empty-state MessageDefault + RelatedArticles fallback) — no HTML sections or BEM classes in the page itself.
    - $setSEO / $setStructuredData logic unchanged.
    Only edit if the audit finds a violation (e.g. a stray second useAsyncData or a double-fetch). If the two independent loads (categories + articles) can be safely parallelized with Promise.all without breaking the empty-state fallback, you MAY do so; otherwise leave the sequential load as-is. Document current→target honestly in the SUMMARY (requirement already largely met).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "blog/index.vue|pages/blog" || echo "no blog index type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/blog/index.vue → MUST equal 1.
    grep "default: () =>" apps/website/app/pages/blog/index.vue → present.
  </verify>
  <done>blog/index.vue has exactly one useAsyncData with object default, no double-fetch, composition-only; vue-tsc clean; current→target data-fetch state documented for the SUMMARY.</done>
</task>

</tasks>

<verification>
- Single useAsyncData on blog/index.vue (grep == 1) with object default; no double-fetch; no N+1 (card has no endpoint).
- Full /blog screenshot-Read-compare passes at 1440 and 390, logged-OUT, against design/index.dc.html "Blog" screen + blog.png/blog-grid.png/blog-list.png/blog-p2.png + BlogCard.dc.html.
- Hero clears the 06-02 headroom header (no overlap).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only, if any).
- vue-tsc --noEmit grep clean for all touched components.
- One heavy op at a time; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
/blog looks like the mockup (desktop + mobile, logged-OUT): cream breadcrumb hero clearing the header, restyled toolbar, 3-col card grid with BlogCard-matching cards, minimal pager. Loads in one data wave, uses only phase-04/new tokens, modifies no existing SCSS variable, and the CardArticle restyle is finalized for 09-02. Net-new behaviors (search, multi-select, clear filters) are deferred and surfaced for user approval.
</success_criteria>

<output>
After completion, create `.planning/phases/09-publico-blog/09-01-SUMMARY.md`. In it, explicitly list the deferred net-new behaviors (free-text search, multi-select category dropdown, "Limpiar filtros") and the current→target data-fetch state (already single-wave) for user review.
</output>
