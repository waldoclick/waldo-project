---
phase: 09-publico-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/pages/blog/index.vue
  - apps/website/app/components/HeroArticles.vue
  - apps/website/app/components/FilterArticles.vue
  - apps/website/app/components/ArticleArchive.vue
  - apps/website/app/components/CardArticle.vue
  - apps/website/app/scss/components/_articles.scss
  - apps/website/app/scss/components/_card.scss
autonomous: true
requirements: [PUB-BLOG, PUB-BLOG-FILTERS]
must_haves:
  truths:
    - "Blog listing (/blog) renders to the mockup look (cream breadcrumb hero, toolbar, 3-col card grid, pager) logged-OUT on desktop 1440 and mobile 390"
    - "Hero clears the fixed/overlay headroom header (~130px top padding) — no content hidden behind the bar"
    - "Each blog card matches BlogCard.dc.html (16/9 media, category pill with color dot, Poppins title, 2-line excerpt, date + read-time meta) and does NOT fire its own endpoint (no N+1)"
    - "The toolbar implements the mockup: a free-text search input, a category multi-select dropdown (checkboxes), and a 'Limpiar filtros' control"
    - "Search + multiselect + pagination filter CLIENT-SIDE over a single loaded set — the search covers ALL published articles (not just the visible page), and no per-keystroke endpoint is fired (no N+1)"
    - "/blog loads its full article set in a single useAsyncData (one network wave), keeping its unique key + object default"
    - "Colors/typography use phase-04 tokens ($ink, $ink2, $muted, $amber, $tag, $cream, $line); no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/components/CardArticle.vue"
      provides: "Restyled blog card matching BlogCard.dc.html — OWNER for the phase (09-02 reuses it)"
    - path: "apps/website/app/components/FilterArticles.vue"
      provides: "Mockup toolbar: free-text search input + category multi-select (checkbox dropdown) + 'Limpiar filtros'; emits filter state to the page/archive"
    - path: "apps/website/app/scss/components/_card.scss"
      provides: "Restyled .card--article block (line ~922)"
      contains: "card--article"
    - path: "apps/website/app/scss/components/_articles.scss"
      provides: "Restyled .hero--articles + .filter--articles (search/dropdown/clear) + .article--archive to mockup"
    - path: "apps/website/app/pages/blog/index.vue"
      provides: "Listing composition + single useAsyncData full-set load + client-side filter/search/paginate orchestration"
      contains: "useAsyncData"
  key_links:
    - from: "apps/website/app/pages/blog/index.vue"
      to: "section components (HeroArticles/FilterArticles/ArticleArchive)"
      via: "component composition only (no BEM/HTML in the page)"
      pattern: "<ArticleArchive"
    - from: "apps/website/app/components/FilterArticles.vue"
      to: "apps/website/app/pages/blog/index.vue"
      via: "emits search query + selected category slugs (client-side filter state)"
      pattern: "defineEmits|update:"
    - from: "apps/website/app/components/ArticleArchive.vue"
      to: "apps/website/app/components/CardArticle.vue"
      via: "v-for grid render of the client-side filtered+paged slice"
      pattern: "<CardArticle"
    - from: "card SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$cream|\\$tag"
---

<objective>
Migrate the public Blog listing page (`pages/blog/index.vue`) to the v1.47 mockup ("Blog" screen, `design/index.dc.html` lines ~1239–1312, plus `design/BlogCard.dc.html`) with FULL FIDELITY: cream breadcrumb hero with "Blog Waldo" tag + h1 + intro, a filter toolbar (free-text search + category multi-select dropdown + "Limpiar filtros"), a 3-column blog-card grid, and a minimal pager.

Purpose: Blog listing is Fase 3 of the public rebrand. It OWNS the blog card restyle (`CardArticle.vue` + the `.card--article` block in `_card.scss`) for the whole phase — 09-02 reuses the restyled card in its "Sigue leyendo" band and must not re-touch it.
Output: Mockup-faithful listing (hero, toolbar with working search/multiselect/clear, card grid, pager) matching the mockup + reference screenshots, on desktop 1440 + mobile 390, logged-OUT, loading the full article set in a single wave and filtering/paginating client-side.

SCOPE — FULL MOCKUP FIDELITY (the user chose to build the net-new behaviors, not just restyle):
- IN: restyle the hero, the card, the grid and the pager to the mockup look; AND build the mockup toolbar behaviors — a free-text article search input, a custom category multi-select dropdown with checkboxes, and a "Limpiar filtros" control.
- FILTERING ARCHITECTURE (DECIDED — client-side over a single loaded set): the mockup toolbar (`blogQuery`, `blogDropItems` checkboxes, `blogPaged`, `blogPager`, `blogRange`, `blogClearFilters`) is unambiguously client-side filter+search+paginate over one loaded set. The blog is editorial (bounded article count, tens not thousands), so load ALL published articles in ONE query (generous pageSize, e.g. 100) and do search + category-multiselect + pagination entirely in the page/components. This makes free-text search cover EVERY article (a server-page-only filter would silently search just 12 — broken) and fires NO per-keystroke endpoint (no N+1).
- ORDER SELECT REMOVED: the mockup toolbar (lines ~1255–1280) has only category-multiselect + search + "Limpiar filtros" — there is NO order/sort select. Drop the existing "Ordenar por" select; default sort stays newest-first (client-side `createdAt:desc`).
- SEO TRADE-OFF (DECIDED): client-side multiselect no longer drives `route.query.category`, so the per-category SEO title falls back to a single generic blog SEO ("Blog — Waldo.click®"). This is an accepted trade-off of the mockup's client-side toolbar; keep the structured-data Blog object generic. Note it in the SUMMARY.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — read the relevant screens, do NOT copy inline styles. Translate to BEM + phase-04 tokens:
@design/index.dc.html
@design/BlogCard.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/blog.png
@design/screenshots/blog-grid.png
@design/screenshots/blog-list.png
@design/screenshots/blog-p2.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/blog/index.vue
@apps/website/app/components/CardArticle.vue
@apps/website/app/components/HeroArticles.vue
@apps/website/app/components/FilterArticles.vue
@apps/website/app/components/ArticleArchive.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- CardArticle.vue IS the BlogCard.dc.html equivalent — restyle it; do NOT create a new BlogCard component (CLAUDE.md create-only-if-no-equivalent rule). -->
<!-- BlogCard category color: mockup derives a per-category hue via oklch (HUE map). Do NOT hand-roll oklch in SCSS.
     Use a simple amber-tinted pill (rgba(247,201,126,.28) background + $tag text + a small color dot) consistent with the phase tokens — the category pill in blog.png / blog-grid.png reads as a single amber-family treatment. -->

<!-- CURRENT /blog data load (apps/website/app/pages/blog/index.vue): ONE useAsyncData (key blog-{category}-{page}-{order}) running categoriesStore.loadCategories() then articlesStore.loadArticles({page, pageSize:12}) with server-side category filter + order, plus a related-articles empty-state fallback. articlesStore.loadArticles(filters, {page,pageSize}, sort) is the store action; categoriesStore.loadCategories() returns the category list. NEW shape: load ALL articles once (pageSize ~100, sort createdAt:desc) + categories, then filter/search/paginate client-side. Keep ONE useAsyncData with default. The Article type exposes: title, header (excerpt), slug, categories[] ({name, slug}), createdAt, cover/gallery. -->

<!-- .card--article block lives in _card.scss at line ~922. .hero--articles / .filter--articles / .article--archive live in _articles.scss. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the blog card (CardArticle.vue + .card--article) to BlogCard.dc.html — OWNER for the phase</name>
  <files>apps/website/app/components/CardArticle.vue, apps/website/app/scss/components/_card.scss</files>
  <action>
    Translate `design/BlogCard.dc.html` to the existing `.card card--article` BEM block. THIS PLAN IS THE OWNER of the card restyle for the whole phase — 09-02 reuses it in "Sigue leyendo" and must NOT re-touch CardArticle/_card.scss.
    Update markup in CardArticle.vue only where structure requires it; push all visual rules to the `.card--article` block in `_card.scss` (line ~922).
    Per BlogCard.dc.html: white card, $line 1px border, 4px radius, column flex, full height.
    - MEDIA: 16/9 ratio top block. When a cover exists, `object-fit:cover` fills it; when absent, the existing empty-state image / placeholder. Category pill is absolutely positioned top-left (top/left 14px): amber-tinted background (rgba(247,201,126,.28)), $tag text, 700, 11px, uppercase, .05em tracking, 4px radius, with a small 7px color dot — keep it a single amber-family treatment (do NOT hand-roll oklch hues in SCSS).
    - BODY: padding 16px 18px 18px, gap 9px. h3 Poppins 700 18px $ink, -.3px tracking, line-height 1.3. Excerpt $ink2 14px, line-height 1.55, clamped to 2 lines (-webkit-line-clamp:2). Meta row pushed to bottom (margin-top:auto, padding-top 12px): date $muted 12.5px, a 3px dot separator, then a clock icon + "N min de lectura" read-time label.
    - READ-TIME: the mockup shows "N min de lectura". If the Article type has no read-time field, derive a simple estimate from body length (e.g. ~200 words/min, min 1) in the component as a computed — keep it a pure local helper, no endpoint. Keep the existing date Intl formatting.
    - The whole card is a link to `/blog/${article.slug}` (preserve current NuxtLink navigation). Keep `useImageProxy`/`transformUrl` for the cover — do NOT add any endpoint call (no N+1).
    CLAUDE.md: BEM strict (every child under `.card--article__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform add (mockup's hover lift is banned here — keep transitions simple, e.g. border-color only). Tokens only; do NOT modify existing SCSS variables (add a NEW variable only if the mockup needs a color absent from the palette).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CardArticle" || echo "no CardArticle type errors"</automated>
    grep -n "card--article" apps/website/app/scss/components/_card.scss → block present and restyled.
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another; Playwright via playwright-core, chromium at /root/.cache/ms-playwright/chromium-1208/chrome-linux64/chrome): screenshot http://localhost:3000/blog at 1440x900 → save /tmp/waldo-shots/09-01-card-desktop.png → Read PNG → compare a single card element-by-element vs design/BlogCard.dc.html AND design/screenshots/blog-grid.png (media ratio, category pill, title type, 2-line excerpt clamp, date + read-time meta) → fix SCSS → re-screenshot → repeat until it matches.
  </verify>
  <done>CardArticle renders to BlogCard.dc.html on desktop + mobile, logged-OUT; no endpoint per card; no box-shadow/scale; tokens only; vue-tsc clean. Card restyle is final (owner for the phase).</done>
</task>

<task type="auto">
  <name>Task 2: Build the mockup toolbar — free-text search + category multi-select dropdown + "Limpiar filtros" (FilterArticles)</name>
  <files>apps/website/app/components/FilterArticles.vue, apps/website/app/scss/components/_articles.scss</files>
  <action>
    Rebuild the EXISTING `FilterArticles.vue` into the mockup toolbar (`design/index.dc.html` lines ~1255–1280) — UPDATE the existing component, do NOT create a new one. The toolbar is a single horizontal row above the grid in the `.filter--articles` BEM block. Markup changes in the .vue file as the new controls require; push all visual rules to `_articles.scss`.
    REMOVE the current "Ordenar por" order `<select>` entirely (the mockup has no order control). Replace the native category `<select>` with a custom multi-select dropdown.
    Build three controls:
    1. CATEGORY MULTI-SELECT DROPDOWN (mockup lines ~1256–1272): a white pill button (white bg, $line border, 4px radius, 12–14px padding, Poppins 600 14px $ink, hover border $ink) with a list icon + a dynamic label ("Todas las categorías" when none selected, the category name when 1, "N categorías" when many) + a chevron. On click it toggles an absolutely-positioned panel (white, $line border, 8px radius, 6px padding, min-width 210px, z-index 200) listing every category as a checkbox row: a square check box (ticked = amber/$ink fill with a check glyph, unticked = $line border) + the category name, hover bg $cream. Clicking a row toggles that category in the selected set. Close the panel on outside-click (use a document click listener added on mount / removed on unmount, or a `@click` backdrop — no new standalone hyphenated classes; keep everything under `.filter--articles__*`). Selected category slugs are component state.
    2. FREE-TEXT SEARCH (mockup lines ~1273–1276): a white pill input wrapper (flex 0 1 380px, white bg, $line border, 4px radius, 12–15px padding, focus-within border $amber) with a search (magnifier) icon + an `<input>` placeholder "Buscar artículos…", bound to a `query` ref. Enforce the existing project maxlength convention on search inputs (cap the text length, e.g. maxlength 80, consistent with the app's other search inputs — confirm the cap used elsewhere and match it).
    3. "LIMPIAR FILTROS" (mockup lines ~1277–1279): a borderless underlined link ($muted 13.5px, hover $ink) shown ONLY when there is an active filter (search text non-empty OR ≥1 category selected). Clicking it clears the search text and the selected categories.
    EMIT filter state to the parent page so the page can filter client-side: keep `:categories` as an input prop (list to render the checkboxes), and emit the current search string + selected category slug array (e.g. `defineEmits(['update:query','update:selected'])` or a single `update:filters` payload — pick one, used consistently by Task 3). Do NOT push to `router.push` query anymore (the toolbar is now client-side state, not URL-driven). BEM strict (every child under `.filter--articles__*`); no standalone hyphenated classes; NO box-shadow except the dropdown panel's subtle drop shadow IS in the mockup — but CLAUDE.md bans adding box-shadow, so render the panel with a $line border only (no shadow) for parity with the project rules; tokens only; no existing-variable edits; no unused vars/props (remove the dropped order logic cleanly).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "FilterArticles" || echo "no FilterArticles type errors"</automated>
    grep -n "Buscar artículos\|Limpiar filtros" apps/website/app/components/FilterArticles.vue → search + clear present.
    grep -c "select" apps/website/app/components/FilterArticles.vue → order/category native selects removed (0, or only inside unrelated text).
    VISUAL LOOP (logged-OUT, reuse :3000): screenshot http://localhost:3000/blog at 1440x900 → save /tmp/waldo-shots/09-01-toolbar-desktop.png; then via Playwright click the category dropdown button and screenshot the OPEN panel → save /tmp/waldo-shots/09-01-toolbar-open.png → Read both PNGs → compare the search pill, the dropdown button + open checkbox panel, and the "Limpiar filtros" link element-by-element vs design/index.dc.html "Blog" toolbar AND blog.png/blog-list.png → fix → re-screenshot → repeat until match.
  </verify>
  <done>FilterArticles renders the mockup toolbar (search input + category checkbox dropdown + conditional "Limpiar filtros"), emits search/selected state to the page, no native selects, no router.push; matches the mockup on desktop and mobile, logged-OUT; tokens only; no unused vars; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Single full-set load + client-side filter/search/paginate wiring (index.vue, HeroArticles, ArticleArchive, _articles.scss)</name>
  <files>apps/website/app/pages/blog/index.vue, apps/website/app/components/HeroArticles.vue, apps/website/app/components/ArticleArchive.vue, apps/website/app/scss/components/_articles.scss</files>
  <action>
    Wire the client-side filtering and restyle the hero + grid + pager.
    1) DATA LOAD (index.vue): change the single useAsyncData to load the FULL article set once — `articlesStore.loadArticles({}, { page: 1, pageSize: 100 }, ['createdAt:desc'])` plus `categoriesStore.loadCategories()` — into the object `default`. Use a stable key (e.g. `blog-all`); the per-keystroke/per-page filtering is client-side, NOT in the key. Keep exactly ONE useAsyncData with object default, no bare-await+useAsyncData double-fetch. The page stays composition-only (imports/arranges HeaderDefault / HeroArticles / FilterArticles / ArticleArchive / FooterDefault) — no HTML/BEM in the page. Drop the now-unused `route.query` category/page/order watching from the data key (the toolbar no longer writes the URL); keep `$setSEO`/`$setStructuredData` but simplify to a single generic blog SEO (no per-category title) per the DECIDED trade-off.
    2) CLIENT-SIDE STATE (index.vue): hold reactive `searchQuery` (string) and `selectedSlugs` (string[]) updated from FilterArticles' emits, plus a `currentPage` ref. Compute `filteredArticles` = the full set filtered by: title/header `$containsi`-style case-insensitive match on searchQuery (when non-empty) AND category membership (article.categories some slug ∈ selectedSlugs, when selectedSlugs non-empty). Compute the paged slice (pageSize 12) + a derived pagination object ({page, pageSize, pageCount, total}) from `filteredArticles.length`. Reset `currentPage` to 1 whenever searchQuery or selectedSlugs changes (mirrors the mockup + the existing 260406-r0g pagination-reset fix). Pass the paged slice + derived pagination to ArticleArchive, and the category list to FilterArticles.
    3) EMPTY STATE: when `filteredArticles.length === 0`, show the mockup "Sin resultados" card (lines ~1282–1289) with a "Limpiar filtros" button — reuse the existing MessageDefault if it can be styled to match, otherwise render the mockup empty card within the archive block; wire its clear button to reset the filters. Keep the existing RelatedArticles empty-state fallback ONLY if there are genuinely zero articles loaded at all (not merely zero after filtering) — or drop it if the "Sin resultados" card now covers the empty-filter case (decide and note in SUMMARY; do NOT bake band chrome here).
    4) HERO (`.hero--articles`, mockup lines ~1240–1252): section on $cream with $line bottom border and the soft amber radial glow (top-right). Inner max-width 1200px. Breadcrumb row (Waldo › Blog) in $muted with chevron + bold $ink current. Then a "Blog Waldo" tag pill (rgba(247,201,126,.28) bg, $tag text, book icon). Then h1 Poppins 800 ~clamp(32px,3.6vw,46px) $ink "Activos industriales, explicados", then an $ink2 17px intro paragraph (max-width 680px). Keep BreadcrumbsDefault if it renders Waldo › Blog to spec. CRITICAL: top padding must clear the 06-02 fixed/overlay headroom header — mockup uses 108px; carry ~130px (account for the ~74px bar) so nothing hides behind it. Verify against blog.png.
    5) GRID + PAGER (`.article--archive`): 3 columns desktop (repeat(3,minmax(0,1fr)), gap 24px) → 2 → 1 on smaller widths; max-width 1200px container, padding to mockup (40px top, 88px bottom). It renders CardArticle (restyled in Task 1) — do not touch the card. Restyle the pager to the mockup's minimal numbered buttons (square, $line border, active = $ink fill / white text) per blog-grid.png, plus the optional results-range label (`blogRange`, lines ~1300–1301). The pager now drives `currentPage` client-side (NOT router query); the existing vue-awesome-paginate output may be replaced with simple numbered buttons since pagination is client-side — keep it minimal and accessible.
    BEM strict; tokens only; no existing-variable edits; no unused vars (remove dropped route-query/order code).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "blog/index.vue|pages/blog|HeroArticles|ArticleArchive" || echo "no listing type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/blog/index.vue → MUST equal 1.
    grep "default: () =>" apps/website/app/pages/blog/index.vue → present.
    VISUAL LOOP (logged-OUT, reuse :3000): screenshot http://localhost:3000/blog full-page at 1440x900 and 390x844 → save /tmp/waldo-shots/09-01-listing-{desktop,mobile}.png → Read each PNG → compare hero (glow, tag, h1, intro, header clearance), toolbar, 3-col grid, pager element-by-element vs design/index.dc.html "Blog" screen AND blog.png + blog-grid.png + blog-list.png + blog-p2.png → fix → re-screenshot → repeat until match. Then via Playwright: type a query in the search input → screenshot → confirm the grid filters to matching articles and the count/pager updates (the filter works over the full set, not one page); select a category checkbox → confirm the grid narrows; click "Limpiar filtros" → confirm full grid returns. Confirm desktop 3 cols / mobile single column / nothing hides behind the fixed header.
  </verify>
  <done>blog/index.vue has exactly one useAsyncData (full set) with object default, no double-fetch; search + multiselect + pager filter the full set client-side (search works globally, page resets on filter change); hero/grid/pager match the mockup + reference PNGs on desktop and mobile clearing the header; tokens only; no unused vars; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Single useAsyncData on blog/index.vue (grep == 1) with object default loading the full article set; no double-fetch; no N+1 (filtering is client-side, card has no endpoint).
- Toolbar implements the mockup: free-text search (covering ALL articles), category checkbox multi-select, conditional "Limpiar filtros"; order select removed.
- Full /blog screenshot-Read-compare passes at 1440 and 390, logged-OUT, against design/index.dc.html "Blog" screen + blog.png/blog-grid.png/blog-list.png/blog-p2.png + BlogCard.dc.html, AND the interactive search/filter/clear loop verified live via Playwright.
- Hero clears the 06-02 headroom header (no overlap).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only, if any).
- vue-tsc --noEmit grep clean for all touched components.
- One heavy op at a time; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
/blog looks AND behaves like the mockup (desktop + mobile, logged-OUT): cream breadcrumb hero clearing the header, a working toolbar (free-text search + category checkbox dropdown + "Limpiar filtros"), a 3-col card grid with BlogCard-matching cards, and a minimal client-side pager. Loads the full article set in one data wave, filters/searches/paginates client-side (search covers every article, no N+1), uses only phase-04/new tokens, modifies no existing SCSS variable, and finalizes the CardArticle restyle for 09-02. The order select is removed (not in the mockup); per-category SEO is intentionally generalized.
</success_criteria>

<output>
After completion, create `.planning/phases/09-publico-blog/09-01-SUMMARY.md`. In it, document: the client-side filtering architecture (single full-set load + in-page search/multiselect/paginate), the removed order select, the generalized blog SEO trade-off, and the current→target data-fetch state (was paginated server load → now single full-set load filtered client-side; still one wave, no N+1) for user review.
</output>
