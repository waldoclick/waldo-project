---
phase: 09-publico-blog
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/strapi/src/api/ad/routes/00-ad-custom.ts
  - apps/strapi/src/api/ad/controllers/ad.ts
  - apps/website/app/stores/ads.store.ts
  - apps/website/app/pages/blog/[slug].vue
  - apps/website/app/components/HeroArticle.vue
  - apps/website/app/components/ArticleSingle.vue
  - apps/website/app/components/RelatedArticles.vue
  - apps/website/app/scss/components/_article.scss
autonomous: true
requirements: [PUB-ARTICLE, PUB-ARTICLE-GATE, PUB-ARTICLE-FEATURED]
must_haves:
  truths:
    - "Blog article interior (/blog/:slug) renders to the mockup look (breadcrumb + back link, article header with category pill / Poppins h1 / excerpt / author·date·read-time meta, styled body, sidebar, footer 'Volver al blog' + share, 'Sigue leyendo' related band) logged-OUT on desktop 1440 and mobile 390"
    - "Header/breadcrumb clears the fixed/overlay headroom header (~130px top padding) — no content hidden behind the bar"
    - "The article is laid out as the mockup 2-column grid (article column max-width 720px + 308px sticky sidebar, align-items:start) on desktop, collapsing to a single column on mobile — the article header (pill/h1/excerpt/meta) is the FIRST child of the left grid column, a sibling of the sidebar"
    - "The body has a working read-more GATE: it is clamped to a partial height with a fade overlay + 'Leer más' control that expands the full body on click (pure client state — the full body is in the DOM, no data change)"
    - "The sidebar 'Destacados en Waldo' block lists REAL featured ads with prices, loaded from a new GET /ads/featured endpoint (single small query, no N+1) — each row shows media, category, title, price and links to the ad"
    - "'Sigue leyendo' reuses the restyled CardArticle from 09-01 (this plan does NOT touch CardArticle/_card.scss)"
    - "/blog/:slug loads article + related + featured ads in a single useAsyncData (one wave, Promise.all) with object default; colors/type use phase-04 tokens; no existing SCSS variable is modified"
  artifacts:
    - path: "apps/strapi/src/api/ad/routes/00-ad-custom.ts"
      provides: "New public route GET /ads/featured (auth:false) declared before the :id wildcard"
      contains: "ads/featured"
    - path: "apps/strapi/src/api/ad/controllers/ad.ts"
      provides: "New featured(ctx) action delegating to activeAds with featured-first sort + small pageSize"
      contains: "async featured"
    - path: "apps/website/app/stores/ads.store.ts"
      provides: "loadFeaturedAds() action hitting GET /ads/featured, returning a small Ad[]"
      contains: "ads/featured"
    - path: "apps/website/app/scss/components/_article.scss"
      provides: "Restyled .hero--article (breadcrumb/back bar) + .article--single (2-col grid, header, body type scale, read-more gate, destacados sidebar) to mockup"
      contains: "article--single"
    - path: "apps/website/app/components/ArticleSingle.vue"
      provides: "Article 2-col grid: header + cover + gated body + footer (left) and sticky sidebar with real Destacados ads + static promo (right)"
    - path: "apps/website/app/pages/blog/[slug].vue"
      provides: "Article composition + single useAsyncData loading article + related + featured ads (Promise.all)"
      contains: "useAsyncData"
  key_links:
    - from: "apps/website/app/pages/blog/[slug].vue"
      to: "GET /ads/featured (via ads.store loadFeaturedAds)"
      via: "Promise.all inside the existing useAsyncData (one wave)"
      pattern: "loadFeaturedAds|ads/featured"
    - from: "apps/website/app/components/ArticleSingle.vue"
      to: "featured ads passed as a prop"
      via: "v-for over featuredAds in the Destacados sidebar block"
      pattern: ":featured-ads|featuredAds"
    - from: "apps/website/app/components/RelatedArticles.vue"
      to: "apps/website/app/components/CardArticle.vue"
      via: "v-for grid render (reuses 09-01 restyle)"
      pattern: "<CardArticle"
    - from: "apps/strapi/src/api/ad/controllers/ad.ts"
      to: "activeAds service"
      via: "delegation with featured-first sort (reuses existing query, no duplication)"
      pattern: "activeAds"
---

<objective>
Migrate the public Blog article interior (`pages/blog/[slug].vue`) to the v1.47 mockup ("Artículo" screen, `design/index.dc.html` lines ~1317–1415) with FULL FIDELITY: a full-width breadcrumb (Waldo › Blog › Categoría) + "Volver al blog" back bar above a 2-column grid; the left column holds the article header (category pill, Poppins 800 h1, excerpt, author · date · read-time meta), a styled cover, a GATED body (read-more clamp + fade + "Leer más"), and a footer row; the right column is a sticky sidebar showing REAL "Destacados en Waldo" ads with prices + a static dark promo; below the grid, a cream "Sigue leyendo" related band.

Purpose: the article interior is the second view of Fase 3 (blog) of the public rebrand. It REUSES the CardArticle restyle finalized in 09-01 for its "Sigue leyendo" band, and introduces a new public backend endpoint for the Destacados ads.
Output: Mockup-faithful article interior (breadcrumb/back bar, 2-col grid, gated body, real featured-ads sidebar, related band) on desktop 1440 + mobile 390, logged-OUT, in a single data wave.

MOCKUP STRUCTURE (read carefully — it drives the component split):
- lines ~1318–1327: breadcrumb + "Volver al blog" — a full-width row ABOVE the grid.
- line ~1328: `gsplit` grid — `grid-template-columns: minmax(0,1fr) 308px; gap:56px; align-items:start`.
- lines ~1329–1376: the LEFT grid column is `<article max-width:720px>` containing, in order: HEADER (pill → h1 → excerpt → meta) → COVER → BODY (with the read-more gate, lines ~1345–1365) → FOOTER ACTIONS.
- lines ~1379–1402: the RIGHT grid column is `<aside sticky top:86px>` (Destacados ads card + dark promo).
=> The article header is the FIRST child of the left column and a SIBLING of the sidebar; with align-items:start the sidebar top-aligns with the category pill. The header lives in ArticleSingle (which owns the grid), NOT in a separate full-width section. HeroArticle is reduced to the breadcrumb/back bar only.

SCOPE — FULL MOCKUP FIDELITY (the user chose to build the net-new behaviors, not just restyle):
- IN (restyle): breadcrumb/back bar, header, body, 2-column layout, footer actions, related band.
- IN (NEW — read-more gate, lines ~1345–1365): a body content gate. DECIDED implementation — the body is rendered via `parseMarkdown(article.body)` v-html (a single HTML blob that cannot be cleanly split into `aPreview`/`aRest` like the mockup's pre-split blocks). So implement the gate as a COLLAPSED CONTAINER: wrap the prose in an element with a clamped `max-height` (e.g. ~420px) + `overflow:hidden`, a gradient fade overlay at the bottom (linear-gradient transparent→#fff), and a "Leer más en el artículo" amber button (+ a small read-label below) that, on click, sets a `expanded` ref removing the clamp/fade to reveal the full body. Pure client state; do NOT split the markdown. Short articles whose body fits under the clamp should NOT show the gate (measure or guard on a min length).
- IN (NEW — Destacados sidebar, lines ~1379–1394): the "Destacados en Waldo" block lists REAL ads with prices. This needs backend: a new public endpoint. DECIDED design below (Task 1). The static dark "¿Vendes equipos? → Publicar aviso" promo (lines ~1397–1401) is also in scope as a static link.
- Share: keep the existing ShareDefault, restyled into the footer "Compartir" button placement (the mockup's copy-link micro-state is cosmetic — keep ShareDefault's behavior; do not build a new copy-link mechanic).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — read the relevant screen, do NOT copy inline styles. Translate to BEM + phase-04 tokens:
@design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/01-blog-article.png
@design/screenshots/02-blog-article.png
@design/screenshots/article-container.png
@design/screenshots/article-sticky-share.png
@design/screenshots/article-buy.png
@design/screenshots/article-sell.png
@design/screenshots/article-redesign.png
# Read-more GATE refs — BUILD the gate to match these (clamp + fade + "Leer más"):
@design/screenshots/01-article-gate.png
@design/screenshots/02-article-gate.png
@design/screenshots/article-readmore.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/blog/[slug].vue
@apps/website/app/components/HeroArticle.vue
@apps/website/app/components/ArticleSingle.vue
@apps/website/app/components/RelatedArticles.vue
@apps/website/app/stores/ads.store.ts

# Backend — the new endpoint reuses these (UPDATE, do NOT duplicate query logic):
@apps/strapi/src/api/ad/routes/00-ad-custom.ts
@apps/strapi/src/api/ad/controllers/ad.ts
@apps/strapi/src/api/ad/services/ad.ts
@apps/strapi/src/api/ad/services/sanitize-ad.ts

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DEPENDS ON 09-01: CardArticle.vue + the .card--article block in _card.scss are restyled by 09-01.
     RelatedArticles ("Sigue leyendo") renders <CardArticle>; this plan MUST NOT touch CardArticle.vue or _card.scss. -->

<!-- COMPONENT SPLIT (per mockup structure, see <objective>):
     - HeroArticle = breadcrumb (Waldo › Blog › {Categoría}) + "Volver al blog" back link ONLY — a full-width row above the grid.
     - ArticleSingle = owns the 2-col grid. LEFT column: header (pill/h1/excerpt/meta) → cover → GATED body → footer actions. RIGHT column: sticky sidebar (real Destacados ads + static promo).
       Exactly ONE <h1> renders across the article view — it lives in ArticleSingle (the article header), NOT in HeroArticle. -->

<!-- BACKEND — FEATURED ADS ENDPOINT (Task 1). The existing public catalog already returns featured-sorted ads:
     - api::ad.ad service `activeAds(options, isManager, userId)` filters active/non-banned/non-rejected/remaining_days>0 and runs ONE findMany + count (no N+1). It populates gallery/category/user/price and sanitizes via sanitizeAdForPublic for public callers.
     - sanitizeAdForPublic returns: name, slug, price, currency, gallery, category (singular), commune, etc. — exactly what the sidebar row needs (media, category, title=name, priceLabel).
     - Ads carry `sort_priority` (0 = featured PRO, 1 = featured normal, 2 = not featured) and `ad_featured_reservation`.
     DECIDED: featured-FIRST-then-fill (not strict-only) so the sidebar is never empty: sort `sort_priority:asc, createdAt:desc`, pageSize 4 (small). This surfaces real featured ads first and tops up with recent active ads. New route GET /ads/featured (auth:false), new controller action `featured(ctx)` delegating to `strapi.service("api::ad.ad").activeAds({ sort: ["sort_priority:asc","createdAt:desc"], pageSize: 4, page: 1 }, true, null)` (isManager=true,userId=null to bypass user filtering, same as `catalog`). Single small query, no N+1.
     SUBTRACTIVE ALTERNATIVE (acknowledge per CLAUDE.md UPDATE-not-create): GET /ads/catalog?sort=sort_priority:asc&pageSize=4 returns the same data with ZERO backend change. We add a thin dedicated /ads/featured route anyway for a clean, semantic contract the website store can call without leaking catalog params — it reuses activeAds, duplicating no query logic. -->

<!-- WEBSITE consume: ads.store.ts already calls `ads/catalog` via useApiClient and maps StrapiResponse<Ad>. Add a sibling `loadFeaturedAds()` that GETs `ads/featured` and returns Ad[] (small). The public ad fields are name/slug/price/currency/gallery/category — the sidebar shows name as title, formats price (reuse the existing price-format util the announcement cards use), and links to /anuncios/{slug} (the public ad route — confirm the exact path the announcement card uses). -->

<!-- Current /blog/:slug data load: ONE useAsyncData (key article-{slug}) with object default + 404 via showError, running up to 3 sequential loadArticles (article → category-related → recent top-up). KEEP one wave: add the featured-ads fetch via Promise.all so the article fetch and the featured fetch run together (the related top-up depends on the article result, so structure as: await article; then Promise.all([related/top-up logic, loadFeaturedAds()])). The article body is rendered via parseMarkdown(article.body) v-html in ArticleSingle — keep that; render the FULL body inside the read-more gate container. -->

<!-- .hero--article + .article--single live in _article.scss (~125 lines). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NEW backend endpoint GET /ads/featured (route + controller) + website loadFeaturedAds() store action</name>
  <files>apps/strapi/src/api/ad/routes/00-ad-custom.ts, apps/strapi/src/api/ad/controllers/ad.ts, apps/website/app/stores/ads.store.ts</files>
  <action>
    INTERFACE-FIRST: define the Destacados contract before ArticleSingle consumes it.
    1) ROUTE (apps/strapi/src/api/ad/routes/00-ad-custom.ts): add a new entry `{ method: "GET", path: "/ads/featured", handler: "ad.featured", config: { auth: false } }`. Place it alongside the other static `/ads/*` routes (this custom file loads BEFORE ad.ts so the static path is matched before any `:id` wildcard — same pattern as `/ads/catalog`). Mark `auth: false` so logged-OUT visitors get the sidebar (consistent with the public blog being viewed logged-OUT).
    2) CONTROLLER (apps/strapi/src/api/ad/controllers/ad.ts): add an async `featured(ctx)` action modeled on the existing `catalog(ctx)` action (read it first — copy its structure). It delegates to the EXISTING service with featured-first sort and a small page size, bypassing user filtering exactly like catalog (isManager=true, userId=null):
       `const result = await strapi.service("api::ad.ad").activeAds({ sort: ["sort_priority:asc", "createdAt:desc"], page: 1, pageSize: 4 }, true, null); return result;`
       Wrap in try/catch with `ctx.throw(500, error)` like the sibling actions. Do NOT write a new query — reuse `activeAds` (it already does ONE findMany + count, populates gallery/category/price, sanitizes for public). This guarantees a single small query, no N+1.
    3) STORE (apps/website/app/stores/ads.store.ts): add a `loadFeaturedAds()` action modeled on the existing `loadAds` (read it first). It GETs `ads/featured` via the existing `client` (useApiClient), maps `response.data` as `Ad[]`, and RETURNS the array (do not necessarily mutate the shared `ads` ref — return a local small list so the article page can hold it in pageData without clobbering listing state). Type it `Promise<Ad[]>`. No new endpoint params needed (the backend fixes sort+pageSize). Keep CLAUDE.md Strapi cast patterns (`response as unknown as StrapiResponse<Ad>`). No unused vars.
    CLAUDE.md: SOLID/thin-controller (delegate to service); English; no `any`; do not modify `activeAds` itself. The persist audit comment on the ads store stays valid (no state shape change if loadFeaturedAds returns locally).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && node ../../node_modules/.bin/tsc --noEmit 2>&1 | grep -E "ad.ts|00-ad-custom" || echo "no strapi ad type errors"</automated>
    grep -n "ads/featured" apps/strapi/src/api/ad/routes/00-ad-custom.ts → route present with auth:false.
    grep -n "async featured" apps/strapi/src/api/ad/controllers/ad.ts → action present, delegates to activeAds.
    grep -n "ads/featured\|loadFeaturedAds" apps/website/app/stores/ads.store.ts → store action present.
    cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ads.store" || echo "no ads.store type errors".
    LIVE CONTRACT CHECK (dev server already on :3000 — do NOT start another; Strapi must be running for this): curl the website proxy `http://localhost:3000/api/ads/featured` (or the direct Strapi route if that is how the proxy maps) and confirm a JSON `{ data: [...], meta: { pagination: { pageSize: 4 } } }` shape with each item exposing name, slug, price, gallery, category — NOT a 404/500. If Strapi is not running locally, document the exact curl + expected shape in the SUMMARY for manual confirmation. If the curl 404s, restart Strapi — newly added routes may NOT hot-reload in dev (cf. the 129-06 decision: Strapi route config changes require a restart to take effect). (This is an automated/curl check — NOT the visual loop.)
  </verify>
  <done>GET /ads/featured (auth:false) returns up to 4 featured-first active ads via the reused activeAds service (single query, no N+1); loadFeaturedAds() returns a typed Ad[]; strapi tsc + vue-tsc clean; no duplicated query logic; the subtractive catalog alternative is noted in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce HeroArticle to the breadcrumb + back bar (full-width, above the grid)</name>
  <files>apps/website/app/components/HeroArticle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    Per the mockup, the breadcrumb + "Volver al blog" is a full-width row ABOVE the 2-col grid (lines ~1318–1327). The article HEADER (pill/h1/excerpt/meta) is NOT here — it lives in the left grid column and is built by Task 3 (ArticleSingle). So REDUCE HeroArticle to only the breadcrumb/back bar; remove any title/date markup it currently renders so the article view has exactly one h1 (in ArticleSingle).
    Translate to the existing `.hero--article` BEM block. Markup changes in HeroArticle.vue only where structure requires; push visual rules to `_article.scss`.
    - BREADCRUMB/BACK BAR (mockup lines ~1318–1327): a max-width 1200px row, space-between, wrap. LEFT: breadcrumb (Waldo › Blog › {Categoría}) in $muted 13.5px with chevron icons + bold $ink current category. RIGHT: a "Volver al blog" back link (left-arrow icon + $ink2 600 13.5px, hover $ink) → /blog. Keep/restyle the existing BreadcrumbsDefault if it renders the 3-crumb path to spec; otherwise restyle within the block. HeroArticle already receives title/category/publishedAt props — keep what the breadcrumb needs (categoryName/categorySlug for the 3rd crumb), drop the now-unused ones (no unused props/vars per CLAUDE.md).
    - CRITICAL: top padding clears the 06-02 fixed/overlay header — mockup uses 94px; carry ~130px (account for the ~74px bar) so the breadcrumb is fully visible below the header.
    BEM strict (every child under `.hero--article__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform; tokens only; do NOT modify existing SCSS variables.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroArticle" || echo "no HeroArticle type errors"</automated>
    grep -c "<h1" apps/website/app/components/HeroArticle.vue → MUST equal 0 (HeroArticle no longer holds the h1).
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another; Playwright via playwright-core, chromium at /root/.cache/ms-playwright/chromium-1208/chrome-linux64/chrome): open a real article URL (pick an existing slug from http://localhost:3000/blog), screenshot at 1440x900 → save /tmp/waldo-shots/09-02-bar-desktop.png → Read PNG → compare the breadcrumb (Waldo › Blog › Categoría) + "Volver al blog" back link vs design/index.dc.html "Artículo" screen AND 01-blog-article.png → fix → re-screenshot → repeat until match. Confirm the bar clears the fixed header.
  </verify>
  <done>HeroArticle renders only the breadcrumb/back bar (no h1, no unused props) matching the mockup top bar on desktop and mobile, logged-OUT; clears the headroom header; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Build the 2-col grid in ArticleSingle — header + cover + GATED body + footer (left) and Destacados ads + promo sidebar (right)</name>
  <files>apps/website/app/components/ArticleSingle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    ArticleSingle OWNS the 2-col grid (mockup lines ~1328–1403). Translate to the existing `.article--single` BEM block. Markup changes in ArticleSingle.vue only where structure requires; push visual rules to `_article.scss`. ArticleSingle already receives `:article`; ADD a `:featured-ads` prop (Ad[]) for the Destacados sidebar (passed from the page, loaded in Task 1's store action — see Task 4 wiring).
    - GRID: max-width 1200px, CSS grid `grid-template-columns: minmax(0,1fr) 308px; gap:56px; align-items:start`. LEFT column = `.article--single__main` (content max-width 720px): header → cover → gated body → footer actions. RIGHT column = `.article--single__sidebar` (sticky top ~86px). Collapses to a single column on mobile (sidebar below, sticky disabled).
    - HEADER (FIRST child of the left column, mockup lines ~1329–1339): category pill (cream bg, $line border, uppercase 700 11.5px, .05em tracking, 4px radius, 7px color dot); then the SINGLE h1 Poppins 800 clamp(30px,3.2vw,40px) $ink (-.6px tracking, line-height 1.14) = article.title; then the excerpt ($ink2 18.5px, line-height 1.55) = article.header; then a meta row with bottom $line border: a 30px amber avatar circle with "W" + author name ($ink2 600), 3px dot separators, the date ($muted, Intl es-CL from createdAt), and a clock icon + "N min de lectura" read-time. READ-TIME: derive from body length (≈200 words/min, min 1) as a local computed — no endpoint. This is the ONLY h1 in the article view.
    - COVER (mockup lines ~1340–1344): when the article has a cover/gallery image, a 16/9 rounded (10px) bordered ($line) media block before the body. Reuse the existing GalleryDefault if present, restyled to this frame; otherwise render the cover. Hide the block when there is no image.
    - GATED BODY (mockup lines ~1345–1365) — BUILD THE READ-MORE GATE: render the existing `parseMarkdown(article.body)` v-html inside a body element (e.g. `.article--single__main__body`), restyled to the mockup prose: paragraphs $ink2 17px line-height 1.72; h2 Poppins 700 22px $ink (-.3px, line-height 1.3, margin-top 24px); scope prose rules under the body class (e.g. `:is(p,h2)`). DO NOT split the markdown. Instead wrap the body in a gate container with a clamped `max-height` (~420px) + `overflow:hidden` when collapsed, a bottom gradient fade overlay (transparent→#fff), and a centered "Leer más en el artículo" amber button (Poppins 700 15px, $ink text, $amber bg, hover $amber_hover) + a small $muted read-label below (lines ~1357–1364). Hold an `expanded` ref (default false); clicking "Leer más" sets it true, removing the clamp/fade and hiding the button to reveal the full body. GUARD: if the body is short (fits under the clamp), do not render the gate (e.g. render expanded by default for short bodies, or measure height on mount). Pure client state; no data change.
    - FOOTER ACTIONS (mockup lines ~1367–1376): a top-bordered row — "¿Te sirvió este artículo?" + the existing ShareDefault (restyled as a cream/$line bordered "Compartir" button), and a "Ver más artículos" outline link back to /blog.
    - SIDEBAR (right column) — DESTACADOS ADS (mockup lines ~1379–1394): a white $line bordered card (10px radius). Header: a "Destacados en Waldo" eyebrow ($tag 700 11px uppercase .08em + amber dot). Then v-for over the `featuredAds` prop (real ads from /ads/featured): each row is a link to the public ad (/anuncios/{ad.slug} — confirm the exact public ad route the announcement card uses) with a 62px rounded media thumb (ad.gallery[0] via the existing image proxy/transform util), a category eyebrow (ad.category.name + dot), a 2-line-clamped Poppins 600 title (ad.name), and a Poppins 700 price (format ad.price with the existing price-format util used by the announcement cards). Top-border separator between rows (`border-top:1px solid $line`), hover bg $cream. Footer "Ver todos los avisos" outline link → /anuncios. When featuredAds is empty, hide the Destacados card gracefully. NO N+1 — the ads arrive pre-loaded as a prop (single /ads/featured call in Task 4).
    - SIDEBAR — STATIC PROMO (mockup lines ~1397–1401): a dark ($ink #26252B bg, 10px radius) card: h4 Poppins 800 white "¿Vendes equipos?", a #C9C5CC subline, and an amber "Publicar aviso" button linking to the publish flow (use the same route the public header's "Anunciar ahora" CTA uses — confirm it).
    BEM strict (every child under `.article--single__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform; tokens only; no existing-variable edits; no unused vars.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ArticleSingle" || echo "no ArticleSingle type errors"</automated>
    grep -c "<h1" apps/website/app/components/ArticleSingle.vue → MUST equal 1 (the only h1 in the article view).
    grep -n "article--single" apps/website/app/scss/components/_article.scss → 2-col grid (align-items:start) + header + prose + gate + destacados sidebar rules present.
    grep -n "Leer más\|featuredAds\|Destacados" apps/website/app/components/ArticleSingle.vue → gate + featured sidebar present.
    VISUAL LOOP (logged-OUT, reuse :3000): screenshot the article URL full-page at 1440x900 and 390x844 → save /tmp/waldo-shots/09-02-body-{desktop,mobile}.png → Read each PNG → compare the 2-col layout (sidebar top-aligns with the category pill), header, cover frame, the GATED body (clamp + fade + "Leer más" visible in the collapsed state), footer actions, the Destacados ads card (real ads, media, category, title, price), and the dark promo vs design/index.dc.html "Artículo" screen AND 02-blog-article.png + article-container.png + article-sell.png + article-buy.png + article-sticky-share.png + 01-article-gate.png/02-article-gate.png/article-readmore.png → fix → re-screenshot → repeat until match. Then via Playwright click "Leer más" → screenshot → confirm the full body expands and the fade/button disappear. Confirm sidebar sticky on desktop / stacked on mobile.
  </verify>
  <done>ArticleSingle renders the 2-col grid with the header (single h1) first/top-aligned with the sticky sidebar, plus cover, a WORKING read-more gate over the full body, footer actions, a Destacados sidebar listing REAL featured ads with prices (from the prop), and the static dark promo — matching the mockup + reference PNGs on desktop and mobile, logged-OUT; tokens only; no unused vars; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: 'Sigue leyendo' related band (RelatedArticles) + wire single-wave load (article + related + featured) on the page</name>
  <files>apps/website/app/components/RelatedArticles.vue, apps/website/app/pages/blog/[slug].vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    1) RELATED BAND (mockup lines ~1405–1414): restyle the existing RelatedArticles section into the mockup "Sigue leyendo" band — full-width on $cream with a $line top border, padding 56px 0 72px, inner max-width 1200px, an h2 Poppins 800 26px $ink "Sigue leyendo", and a 3-col grid (gap 24px) of the restyled CardArticle (from 09-01). On the article page, pass `title="Sigue leyendo"`.
       SHARED-COMPONENT CAUTION: RelatedArticles is ALSO used by 09-01's listing as an empty-state fallback. Do NOT bake the cream-band chrome (cream bg + top border) into the shared component's base class — apply it via a MODIFIER used only on the article page (e.g. a `variant="band"` prop → root class `related--articles--band`), so the base `related--articles` stays neutral. The grid/h2 styling can be shared; the cream background + top border + 56/72 padding are modifier-only. RelatedArticles renders `<CardArticle>` — DO NOT touch CardArticle.vue or _card.scss (owned by 09-01). BEM strict; NO box-shadow/scale; tokens only.
    2) DATA LOAD WIRING on `pages/blog/[slug].vue`: KEEP exactly ONE useAsyncData (key `article-{slug}`) with object default + 404 via showError. ADD the featured ads to pageData WITHOUT adding a second wave: structure the callback as — load the article by slug first (needed for related-category filter + 404); then run `Promise.all([<related/top-up logic>, useAdsStore().loadFeaturedAds()])` so the related articles and the featured ads load concurrently (one wave). Extend the `ArticlePageData` interface with `featuredAds: Ad[]` and the object default with `featuredAds: []`. Pass `:featured-ads="pageData.featuredAds"` to ArticleSingle and `title="Sigue leyendo"` + the band modifier prop to RelatedArticles. Instantiate `useAdsStore()` at setup scope (per the 110-01 decision) — call its action inside the callback. Keep the page composition-only (imports/arranges HeaderDefault/HeroArticle/ArticleSingle/RelatedArticles/FooterDefault — no HTML/BEM in the page), and keep `$setSEO`/`$setStructuredData` + the article_view analytics watchers + the 404 behavior unchanged.
    CLAUDE.md: no double-fetch (no bare `await storeAction()` paired with useAsyncData outside the callback); no unused vars; types via the Ad type import.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RelatedArticles|blog/\\[slug\\]|pages/blog" || echo "no related/page type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/blog/[slug].vue → MUST equal 1.
    grep "default: () =>" apps/website/app/pages/blog/[slug].vue → present.
    grep -n "loadFeaturedAds\|featuredAds\|Promise.all" apps/website/app/pages/blog/[slug].vue → featured fetch wired via Promise.all (one wave).
    grep -n ":featured-ads\|title=\"Sigue leyendo\"" apps/website/app/pages/blog/[slug].vue → props passed.
    VISUAL LOOP (logged-OUT, reuse :3000): screenshot the article URL focused on the bottom band at 1440 and 390 → save /tmp/waldo-shots/09-02-related-{desktop,mobile}.png → Read → compare the "Sigue leyendo" band (cream bg, top border, h2, 3-col grid reusing the restyled card) vs design/index.dc.html "Artículo" screen → fix → repeat until match. Also confirm the /blog listing empty-state fallback (RelatedArticles WITHOUT the band modifier) is NOT cream-banded.
  </verify>
  <done>"Sigue leyendo" band matches the mockup (cream, top border, h2, 3-col grid of the 09-01-restyled card) on desktop and mobile via an article-page-only modifier (listing fallback unaffected); CardArticle/_card.scss untouched; [slug].vue has exactly one useAsyncData with object default loading article + related + featured ads (Promise.all, one wave) and unchanged 404/SEO/analytics; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- New backend: GET /ads/featured (auth:false) declared before the :id wildcard, delegates to activeAds (featured-first sort, pageSize 4), single query no N+1; strapi tsc clean.
- Single useAsyncData on blog/[slug].vue (grep == 1) with object default loading article + related + featured ads via Promise.all (one wave); 404/SEO/analytics preserved.
- Exactly one h1 in the article view (0 in HeroArticle, 1 in ArticleSingle).
- The article header is the first child of the left grid column; the sidebar top-aligns with it (align-items:start).
- Read-more gate works: collapsed clamp + fade + "Leer más" expands the full body (Playwright-verified).
- Destacados sidebar shows REAL featured ads with prices (media/category/title/price) and links to ads; hides gracefully when empty.
- Full /blog/:slug screenshot-Read-compare passes at 1440 and 390, logged-OUT, against design/index.dc.html "Artículo" screen + the reference PNGs (incl. the gate refs).
- "Sigue leyendo" reuses the 09-01 CardArticle restyle (CardArticle.vue + _card.scss NOT in this plan's diff); cream band is a modifier-only treatment (listing fallback unaffected).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only, if any).
- vue-tsc --noEmit grep clean for all touched website components; strapi tsc clean for the ad route/controller.
- One heavy op at a time; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
/blog/:slug looks AND behaves like the mockup "Artículo" screen (desktop + mobile, logged-OUT): a full-width breadcrumb/back bar clearing the header, then a 2-col grid whose left column holds the article header (pill/h1/excerpt/meta) top-aligned with a sticky sidebar that lists REAL "Destacados en Waldo" ads with prices (from the new /ads/featured endpoint) + a static dark promo, followed by styled cover + a body with a WORKING read-more gate (clamp + fade + "Leer más") + footer actions, and a cream "Sigue leyendo" band reusing the 09-01 card. The new endpoint reuses the activeAds service (no duplicated query, single small call no N+1), and the page loads article + related + featured in one data wave. Uses only phase-04/new tokens, modifies no existing SCSS variable, does not touch the 09-01-owned card, and does not restyle the shared listing fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/09-publico-blog/09-02-SUMMARY.md`. In it, document: the new GET /ads/featured endpoint (route + controller delegating to activeAds, featured-first sort, pageSize 4) and the acknowledged subtractive catalog alternative; the read-more gate implementation (CSS clamp + fade + toggle over v-html, not a markdown split); which public ad route the Destacados rows link to and the price-format util reused; and the current→target data-fetch state (article + related + featured in one wave via Promise.all) for user review.
</output>
