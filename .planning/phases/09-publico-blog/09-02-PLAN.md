---
phase: 09-publico-blog
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/website/app/pages/blog/[slug].vue
  - apps/website/app/components/HeroArticle.vue
  - apps/website/app/components/ArticleSingle.vue
  - apps/website/app/components/RelatedArticles.vue
  - apps/website/app/scss/components/_article.scss
autonomous: true
requirements: [PUB-ARTICLE]
must_haves:
  truths:
    - "Blog article interior (/blog/:slug) renders to the mockup look (breadcrumb + back link, article header with category pill / Poppins h1 / excerpt / author·date·read-time meta, styled body, sidebar, footer 'Volver al blog' + share, 'Sigue leyendo' related band) logged-OUT on desktop 1440 and mobile 390"
    - "Header/breadcrumb clears the fixed/overlay headroom header (~130px top padding) — no content hidden behind the bar"
    - "The article is laid out as the mockup 2-column grid (article column max-width 720px + 308px sticky sidebar, align-items:start) on desktop, collapsing to a single column on mobile — the article header (pill/h1/excerpt/meta) is the FIRST child of the left grid column, a sibling of the sidebar (so the sidebar top-aligns with the header)"
    - "'Sigue leyendo' reuses the restyled CardArticle from 09-01 (this plan does NOT touch CardArticle/_card.scss)"
    - "/blog/:slug loads its data in a single useAsyncData (one wave) with object default; colors/type use phase-04 tokens; no existing SCSS variable is modified"
  artifacts:
    - path: "apps/website/app/scss/components/_article.scss"
      provides: "Restyled .hero--article (breadcrumb/back bar) + .article--single (2-col grid, header, body type scale, sidebar) to mockup"
      contains: "article--single"
    - path: "apps/website/app/components/ArticleSingle.vue"
      provides: "Article 2-col grid: header (pill/h1/excerpt/meta) + cover + body + footer in the left column, sticky sidebar in the right"
    - path: "apps/website/app/pages/blog/[slug].vue"
      provides: "Article composition + single useAsyncData data load"
      contains: "useAsyncData"
  key_links:
    - from: "apps/website/app/pages/blog/[slug].vue"
      to: "section components (HeroArticle/ArticleSingle/RelatedArticles)"
      via: "component composition only (no BEM/HTML in the page)"
      pattern: "<ArticleSingle"
    - from: "apps/website/app/components/RelatedArticles.vue"
      to: "apps/website/app/components/CardArticle.vue"
      via: "v-for grid render (reuses 09-01 restyle)"
      pattern: "<CardArticle"
    - from: "article SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$cream|\\$line"
---

<objective>
Restyle the public Blog article interior (`pages/blog/[slug].vue`) to the v1.47 mockup ("Artículo" screen, `design/index.dc.html` lines ~1317–1415): a full-width breadcrumb (Waldo › Blog › Categoría) + "Volver al blog" back bar above a 2-column grid; the left column holds the article header (category pill, Poppins 800 h1, excerpt, author · date · read-time meta row with bottom border), a styled cover + body, and a footer row ("¿Te sirvió este artículo?" + share + "Ver más artículos"); the right column is a sticky sidebar shell; below the grid, a cream "Sigue leyendo" related band.

Purpose: the article interior is the second view of Fase 3 (blog) of the public rebrand. It REUSES the CardArticle restyle finalized in 09-01 for its "Sigue leyendo" band.
Output: Restyled article interior matching the mockup + reference screenshots, on desktop 1440 + mobile 390, logged-OUT, in a single data wave.

MOCKUP STRUCTURE (read carefully — it drives the component split):
- lines ~1318–1327: breadcrumb + "Volver al blog" — a full-width row ABOVE the grid.
- line ~1328: `gsplit` grid — `grid-template-columns: minmax(0,1fr) 308px; gap:56px; align-items:start`.
- lines ~1329–1377: the LEFT grid column is `<article max-width:720px>` containing, in order: HEADER (pill → h1 → excerpt → meta) → COVER → BODY → FOOTER ACTIONS.
- lines ~1379–1402: the RIGHT grid column is `<aside sticky top:86px>` (Destacados card + dark promo).
=> The article header is the FIRST child of the left column and a SIBLING of the sidebar. With align-items:start the sidebar's top edge aligns with the category pill, not the body. The header therefore lives in ArticleSingle (which owns the grid), NOT in a separate full-width section above the grid. HeroArticle is reduced to the breadcrumb/back bar only.

SCOPE — VISUAL RESTYLE ONLY (this is the "Rediseño visual" milestone, precedent = phase 07):
- IN: restyle existing markup/SCSS of the breadcrumb/back bar, header, body, 2-column layout, sidebar shell, footer actions and the related band to the mockup look. Render the FULL styled article body.
- OUT (defer to user approval — do NOT build):
  1. The read-more CONTENT GATE (mockup `aPreview`/`aRest`/`aExpanded` — fade overlay + "Leer más en el artículo"). Render the full body instead; defer the gate.
  2. The "Destacados en Waldo" sidebar block that lists ADS WITH PRICES (mockup lines ~1381–1394). That pulls a different domain (ads) into a blog page — out of scope for a visual phase and would add a new data source. Instead, keep the existing sidebar content (Categorías + Share) restyled to the mockup card shell, OR fill the slot with a recent-articles mini-list built from data ALREADY loaded (the relatedArticles already in pageData) — do NOT add the ads endpoint.
  3. Copy-link share mechanic — keep the existing ShareDefault; just restyle its placement.
  Surface these deferrals in the SUMMARY.
- The static dark "¿Vendes equipos? → Publicar aviso" promo card (mockup lines ~1397–1401) is trivial, on-brand and in scope — add it to the sidebar shell as a static link to the publish flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — read the relevant screen, do NOT copy inline styles. Translate to BEM + phase-04 tokens:
@design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/01-blog-article.png
@design/screenshots/02-blog-article.png
@design/screenshots/article-container.png
@design/screenshots/article-sticky-share.png
@design/screenshots/article-buy.png
@design/screenshots/article-sell.png
@design/screenshots/article-redesign.png
# Article GATE refs — these show the deferred read-more gate; do NOT build it, render the full body:
@design/screenshots/01-article-gate.png
@design/screenshots/02-article-gate.png
@design/screenshots/article-readmore.png

# Current page + components (UPDATE these — closest existing equivalents, do NOT create new):
@apps/website/app/pages/blog/[slug].vue
@apps/website/app/components/HeroArticle.vue
@apps/website/app/components/ArticleSingle.vue
@apps/website/app/components/RelatedArticles.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DEPENDS ON 09-01: CardArticle.vue + the .card--article block in _card.scss are restyled by 09-01.
     RelatedArticles ("Sigue leyendo") renders <CardArticle>; this plan MUST NOT touch CardArticle.vue or _card.scss. -->

<!-- COMPONENT SPLIT (per mockup structure, see <objective>):
     - HeroArticle = breadcrumb (Waldo › Blog › {Categoría}) + "Volver al blog" back link ONLY — a full-width row above the grid.
     - ArticleSingle = owns the 2-col grid. LEFT column: header (pill/h1/excerpt/meta) → cover → body → footer actions. RIGHT column: sticky sidebar.
       ArticleSingle already receives :article (title, header/excerpt, categories, createdAt) so the header needs NO new props.
       Exactly ONE <h1> renders across the article view — it lives in ArticleSingle (the article header), NOT in HeroArticle. -->

<!-- Current /blog/:slug data load: ONE useAsyncData (key article-{slug}) that runs up to THREE SEQUENTIAL loadArticles inside the callback: (1) the article by slug, (2) same-category related, (3) recent top-up only when related < 3. Has object default + 404 via showError. This is one wave; the only honest optimization is collapsing the related/top-up logic (optional). Do NOT add the ads endpoint. The article body is rendered via parseMarkdown(article.body) v-html in ArticleSingle — keep that; render the FULL body (no gate). -->

<!-- .hero--article + .article--single live in _article.scss (125 lines). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce HeroArticle to the breadcrumb + back bar (full-width, above the grid)</name>
  <files>apps/website/app/components/HeroArticle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    Per the mockup, the breadcrumb + "Volver al blog" is a full-width row ABOVE the 2-col grid (lines ~1318–1327). The article HEADER (pill/h1/excerpt/meta) is NOT here — it lives in the left grid column and is built by Task 2 (ArticleSingle). So REDUCE HeroArticle to only the breadcrumb/back bar; remove any title/date markup it currently renders so the article view has exactly one h1 (in ArticleSingle).
    Translate to the existing `.hero--article` BEM block. Markup changes in HeroArticle.vue only where structure requires; push visual rules to `_article.scss`.
    - BREADCRUMB/BACK BAR (mockup lines ~1318–1327): a max-width 1200px row, space-between, wrap. LEFT: breadcrumb (Waldo › Blog › {Categoría}) in $muted 13.5px with chevron icons + bold $ink current category. RIGHT: a "Volver al blog" back link (left-arrow icon + $ink2 600 13.5px, hover $ink) → /blog. Keep/restyle the existing BreadcrumbsDefault if it renders the 3-crumb path to spec; otherwise restyle within the block. HeroArticle already receives title/category/publishedAt props — keep what the breadcrumb needs (categoryName/categorySlug for the 3rd crumb), drop the now-unused ones if any (no unused props/vars per CLAUDE.md).
    - CRITICAL: top padding clears the 06-02 fixed/overlay header — mockup uses 94px; carry ~130px (account for the ~74px bar) so the breadcrumb is fully visible below the header.
    BEM strict (every child under `.hero--article__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform; tokens only; do NOT modify existing SCSS variables.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroArticle" || echo "no HeroArticle type errors"</automated>
    grep -c "<h1" apps/website/app/components/HeroArticle.vue → MUST equal 0 (HeroArticle no longer holds the h1).
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another): open a real article URL (pick an existing slug from http://localhost:3000/blog), screenshot at 1440x900 → save /tmp/waldo-shots/09-02-bar-desktop.png → Read PNG → compare the breadcrumb (Waldo › Blog › Categoría) + "Volver al blog" back link vs design/index.dc.html "Artículo" screen AND 01-blog-article.png → fix → re-screenshot → repeat until match. Confirm the bar clears the fixed header.
  </verify>
  <done>HeroArticle renders only the breadcrumb/back bar (no h1, no unused props) matching the mockup top bar on desktop and mobile, logged-OUT; clears the headroom header; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the 2-col grid in ArticleSingle — header + cover + body + footer (left) and sticky sidebar (right)</name>
  <files>apps/website/app/components/ArticleSingle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    ArticleSingle OWNS the 2-col grid (mockup lines ~1328–1403). Translate to the existing `.article--single` BEM block. Markup changes in ArticleSingle.vue only where structure requires; push visual rules to `_article.scss`. ArticleSingle already receives `:article` (title, header/excerpt, categories, createdAt) so the header needs NO new props.
    - GRID: max-width 1200px, CSS grid `grid-template-columns: minmax(0,1fr) 308px; gap:56px; align-items:start`. LEFT column = `.article--single__main` (content max-width 720px): header → cover → body → footer actions. RIGHT column = `.article--single__sidebar` (sticky). Collapses to a single column on mobile (sidebar below, sticky disabled).
    - HEADER (FIRST child of the left column, mockup lines ~1329–1339): category pill (cream bg, $line border, uppercase 700 11.5px, .05em tracking, 4px radius, 7px color dot); then the SINGLE h1 Poppins 800 clamp(30px,3.2vw,40px) $ink (-.6px tracking, line-height 1.14) = article.title; then the excerpt ($ink2 18.5px, line-height 1.55) = article.header; then a meta row with bottom $line border: a 30px amber avatar circle with "W" + author name ($ink2 600), 3px dot separators, the date ($muted, Intl es-CL from createdAt), and a clock icon + "N min de lectura" read-time. READ-TIME: derive a simple estimate from body length (≈200 words/min, min 1) as a local computed — no endpoint. This is the ONLY h1 in the article view.
    - COVER (mockup lines ~1340–1344): when the article has a cover/gallery image, a 16/9 rounded (10px) bordered ($line) media block before the body. Reuse the existing GalleryDefault if present, restyled to this frame; otherwise render the cover. Hide the block when there is no image.
    - BODY (mockup lines ~1345–1356): the existing `parseMarkdown(article.body)` v-html, restyled to the mockup prose: paragraphs $ink2 17px line-height 1.72; h2 Poppins 700 22px $ink (-.3px, line-height 1.3, margin-top 24px). Scope prose rules under the body element class (e.g. `.article--single__main__body :is(p,h2)`). RENDER THE FULL BODY — do NOT build the read-more gate (fade + "Leer más"); deferred (note in SUMMARY).
    - FOOTER ACTIONS (mockup lines ~1367–1376): a top-bordered row — "¿Te sirvió este artículo?" + the existing ShareDefault (restyled as a cream/$line bordered button), and a "Ver más artículos" outline link back to /blog.
    - SIDEBAR (right column, mockup lines ~1379–1402): sticky (top ~86px). For the "Destacados" slot do NOT pull the ads endpoint — instead keep the existing Categorías + Share content restyled into a white $line bordered card (10px radius), OR render a recent-articles mini-list from the relatedArticles already in pageData (whichever reads cleaner vs article-sell.png) — note the choice in SUMMARY. THEN add the static dark promo card (#26252b bg, 10px radius): h4 Poppins 800 white "¿Vendes equipos?", a #C9C5CC subline, and an amber "Publicar aviso" button linking to the publish flow (use the same route the public header's "Anunciar ahora" CTA uses). This static promo is in scope.
    BEM strict (every child under `.article--single__*`; no standalone hyphenated classes); NO box-shadow / NO scale-transform; tokens only; no existing-variable edits.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ArticleSingle" || echo "no ArticleSingle type errors"</automated>
    grep -c "<h1" apps/website/app/components/ArticleSingle.vue → MUST equal 1 (the only h1 in the article view).
    grep -n "article--single" apps/website/app/scss/components/_article.scss → 2-col grid (align-items:start) + header + prose + sidebar rules present.
    VISUAL LOOP (logged-OUT): screenshot the article URL full-page at 1440x900 and 390x844 → save /tmp/waldo-shots/09-02-body-{desktop,mobile}.png → Read each PNG → compare the 2-col layout (sidebar top-aligns with the category pill), header (pill/h1/excerpt/meta), cover frame, body prose (p/h2 scale), footer actions, sidebar card + dark promo vs design/index.dc.html "Artículo" screen AND 02-blog-article.png + article-container.png + article-sell.png + article-buy.png + article-sticky-share.png → fix → re-screenshot → repeat until match. Confirm full body renders (no gate); sidebar sticky on desktop / stacked on mobile.
  </verify>
  <done>ArticleSingle renders the 2-col grid with the header (the single h1) as the first left-column child top-aligned with the sticky sidebar, plus cover, full body, footer actions, and sidebar (restyled content card + static dark promo) — matching the mockup + reference PNGs on desktop and mobile, logged-OUT; gate + ads sidebar deferred; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: 'Sigue leyendo' related band (RelatedArticles) + confirm single-wave data load on the page</name>
  <files>apps/website/app/components/RelatedArticles.vue, apps/website/app/pages/blog/[slug].vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    1) RELATED BAND (mockup lines ~1405–1414): restyle the existing RelatedArticles section into the mockup "Sigue leyendo" band — full-width on $cream with a $line top border, padding 56px 0 72px, inner max-width 1200px, an h2 Poppins 800 26px $ink "Sigue leyendo", and a 3-col grid (gap 24px) of the restyled CardArticle (from 09-01). On the article page, pass `title="Sigue leyendo"` so the heading matches.
       SHARED-COMPONENT CAUTION: RelatedArticles is ALSO used by 09-01's listing as its empty-state "Artículos recientes" fallback. Do NOT bake the cream-band chrome (cream bg + top border) into the shared component's base class — the listing fallback would silently inherit it. Apply the band chrome via a MODIFIER used only on the article page (e.g. add a `variant="band"` prop → root class `related--articles--band`, or wrap the section in an article-page-scoped class), so the base `related--articles` stays neutral. The grid/h2 styling can be shared; the cream background + top border + 56/72 padding are the modifier-only part. RelatedArticles already renders `<CardArticle>` — DO NOT touch CardArticle.vue or _card.scss (owned by 09-01). BEM strict; NO box-shadow/scale; tokens only.
    2) DATA LOAD AUDIT on `pages/blog/[slug].vue`: CURRENT is ONE useAsyncData (key `article-{slug}`) with object default + 404 via showError, running up to 3 sequential loadArticles (article → category-related → recent top-up). This is already a single wave. Confirm and keep: exactly one useAsyncData (grep == 1), object default present, no bare-await+useAsyncData double-fetch, page composition-only (imports/arranges HeaderDefault/HeroArticle/ArticleSingle/RelatedArticles/FooterDefault — no HTML/BEM in the page), $setSEO/$setStructuredData + the article_view analytics watchers unchanged. Pass `title="Sigue leyendo"` (and the band modifier prop) to RelatedArticles from the page. Optionally collapse the related/top-up branch for clarity, but do NOT add the ads endpoint and do NOT change the 404 behavior. Document current→target honestly in the SUMMARY (already single-wave).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "RelatedArticles|blog/\\[slug\\]|pages/blog" || echo "no related/page type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/blog/[slug].vue → MUST equal 1.
    grep "default: () =>" apps/website/app/pages/blog/[slug].vue → present.
    VISUAL LOOP (logged-OUT): screenshot the article URL focused on the bottom band at 1440 and 390 → save /tmp/waldo-shots/09-02-related-{desktop,mobile}.png → Read → compare the "Sigue leyendo" band (cream bg, top border, h2, 3-col grid reusing the restyled card) vs design/index.dc.html "Artículo" screen → fix → repeat until match. Also confirm the /blog listing empty-state fallback (RelatedArticles without the band modifier) is NOT cream-banded.
  </verify>
  <done>"Sigue leyendo" band matches the mockup (cream, top border, h2, 3-col grid of the 09-01-restyled card) on desktop and mobile, logged-OUT, via an article-page-only modifier (listing fallback unaffected); CardArticle/_card.scss untouched; [slug].vue has exactly one useAsyncData with object default and unchanged 404/SEO/analytics; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Single useAsyncData on blog/[slug].vue (grep == 1) with object default; no double-fetch; 404 behavior preserved; no ads endpoint added.
- Exactly one h1 in the article view (0 in HeroArticle, 1 in ArticleSingle).
- The article header is the first child of the left grid column and the sidebar top-aligns with it (align-items:start), per the mockup.
- Full /blog/:slug screenshot-Read-compare passes at 1440 and 390, logged-OUT, against design/index.dc.html "Artículo" screen + 01/02-blog-article.png + article-container.png + article-sell.png + article-buy.png + article-sticky-share.png.
- Breadcrumb clears the 06-02 headroom header (no overlap).
- "Sigue leyendo" reuses the 09-01 CardArticle restyle; CardArticle.vue and _card.scss are NOT in this plan's diff; the cream band is a modifier-only treatment (listing fallback unaffected).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only, if any).
- vue-tsc --noEmit grep clean for all touched components.
- One heavy op at a time; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
/blog/:slug looks like the mockup "Artículo" screen (desktop + mobile, logged-OUT): a full-width breadcrumb/back bar clearing the header, then a 2-col grid whose left column holds the article header (pill/h1/excerpt/meta) top-aligned with a sticky sidebar (restyled content card + static dark promo), followed by styled cover + full body + footer actions, and a cream "Sigue leyendo" band reusing the 09-01 card. Loads in one data wave, uses only phase-04/new tokens, modifies no existing SCSS variable, does not touch the 09-01-owned card, and does not restyle the shared listing fallback. The read-more gate and the ads "Destacados" sidebar are deferred and surfaced for user approval.
</success_criteria>

<output>
After completion, create `.planning/phases/09-publico-blog/09-02-SUMMARY.md`. In it, explicitly list the deferred net-new behaviors (read-more content gate; "Destacados en Waldo" ads-with-prices sidebar; copy-link share mechanic), which sidebar fallback was chosen, and the current→target data-fetch state (already single-wave) for user review.
</output>
