---
phase: 120-refactor-pro-subscription-model
plan: 04
type: execute
wave: 3
depends_on: ["120-02", "120-03"]
files_modified:
  - apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts
  - apps/strapi/tests/cron/subscription-charge.cron.test.ts
  - apps/strapi/tests/middlewares/protect-user-fields.test.ts
  - apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts
autonomous: true
requirements: [SUB-CRON-01, SUB-CANCEL-01, SUB-PROTECT-01, SUB-CHARGE-01, SUB-CHARGE-02, SUB-MIGRATE-01]

must_haves:
  truths:
    - "All existing tests pass after migration changes"
    - "Test assertions verify subscription-pro is the source of card data"
    - "TypeScript compiles clean across strapi app"
    - "Bootstrap migration test passes: active/cancelled users get subscription-pro records, users without tbk_user are skipped, idempotency prevents duplicates"
  artifacts:
    - path: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      provides: "Updated cron tests with subscription_pro populated"
    - path: "apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts"
      provides: "Updated cancellation tests querying subscription-pro"
    - path: "apps/strapi/tests/middlewares/protect-user-fields.test.ts"
      provides: "Updated middleware tests with card fields still protected"
    - path: "apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts"
      provides: "Bootstrap migration unit test covering idempotency and edge cases"
      contains: "migrateSubscriptionPro"
  key_links:
    - from: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      to: "apps/strapi/src/cron/subscription-charge.cron.ts"
      via: "jest mock assertions"
      pattern: "subscription_pro"
    - from: "apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts"
      to: "apps/strapi/src/bootstrap/migrate-subscription-pro.ts"
      via: "jest mock assertions"
      pattern: "migrateSubscriptionPro"
---

<objective>
Update existing test suites to reflect the subscription-pro migration, create a bootstrap migration test, and ensure all tests pass with TypeScript compiling clean.

Purpose: Validates that the migration is correct and prevents regression. Migration test covers idempotency and edge cases.
Output: Updated test files, new migration test, full green test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-RESEARCH.md
@.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-01-SUMMARY.md
@.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-02-SUMMARY.md
@.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cron and cancellation test suites for subscription-pro</name>
  <files>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts
  </files>
  <read_first>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts,
    apps/strapi/src/cron/subscription-charge.cron.ts,
    apps/strapi/src/api/payment/services/pro-cancellation.service.ts
  </read_first>
  <action>
**A. Update subscription-charge.cron.test.ts:**

1. Find all mock user objects that provide `tbk_user` at the top level. Move `tbk_user` into a nested `subscription_pro` object:
   - Before: `{ id: 1, documentId: "abc", tbk_user: "tbk123", pro_expires_at: "..." }`
   - After: `{ id: 1, documentId: "abc", pro_expires_at: "...", subscription_pro: { tbk_user: "tbk123" } }`

2. Update any mock `findMany` return values to include `subscription_pro` in the populated user objects.

3. If there are assertions checking that `authorizeCharge` was called with `user.tbk_user`, update them to verify the tbk_user value still arrives correctly (the cron extracts it from subscription_pro and passes it down).

4. Add a test case (or update existing) that verifies: when `subscription_pro` is null or `subscription_pro.tbk_user` is falsy, the user is skipped (logger.warn is called).

5. Update the strapi mock's entityService.findMany to return users with `subscription_pro` populated when the call includes `populate: ["subscription_pro"]`.

**B. Update pro-cancellation.service.test.ts:**

1. The current test mocks `strapi.entityService.findOne("plugin::users-permissions.user", ...)` returning `{ tbk_user: "..." }`. Replace this with a mock for `strapi.db.query("api::subscription-pro.subscription-pro").findOne(...)` returning `{ id: 1, tbk_user: "..." }`.

2. Add mock for `strapi.entityService.update` being called with `"api::subscription-pro.subscription-pro"` to clear tbk_user.

3. Update the "User has no active inscription" test case to mock `strapi.db.query("api::subscription-pro.subscription-pro").findOne(...)` returning null.

4. Verify the user update still sets `pro_status: "cancelled"` and `tbk_user: null` on the user entity (dual-write).

5. Ensure `strapi.db.query` mock is set up with a factory that returns different query objects for different UIDs.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && yarn jest --testPathPattern="subscription-charge|pro-cancellation" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `yarn jest --testPathPattern="subscription-charge"` exits 0 (all tests pass)
    - `yarn jest --testPathPattern="pro-cancellation"` exits 0 (all tests pass)
    - Test file contains `subscription_pro` in mock user objects (cron test)
    - Test file contains `api::subscription-pro.subscription-pro` in mock setup (cancellation test)
    - At least one test verifies skipping users with missing subscription_pro tbk_user
  </acceptance_criteria>
  <done>Cron and cancellation tests updated for subscription-pro, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Create bootstrap migration test and update middleware test</name>
  <files>
    apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts,
    apps/strapi/tests/middlewares/protect-user-fields.test.ts
  </files>
  <read_first>
    apps/strapi/tests/middlewares/protect-user-fields.test.ts,
    apps/strapi/src/middlewares/protect-user-fields.ts,
    apps/strapi/src/bootstrap/migrate-subscription-pro.ts
  </read_first>
  <action>
**A. Create bootstrap migration test (`apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts`):**

Create a comprehensive test for `migrateSubscriptionPro()` covering SUB-MIGRATE-01:

1. **Active users with tbk_user get subscription-pro records:** Mock `strapi.db.query("plugin::users-permissions.user").findMany` to return 2 users (one active, one cancelled) with `tbk_user` set. Mock `strapi.db.query("api::subscription-pro.subscription-pro").findOne` to return null (no existing record). Assert `strapi.entityService.create` is called twice with correct data mapping: `user.pro_card_type` -> `card_type`, `user.pro_card_last4` -> `card_last4`, etc.

2. **Users without tbk_user are skipped:** The query filters with `tbk_user: { $notNull: true }`, so these users never appear in results. Verify the findMany `where` clause includes `$notNull`.

3. **Idempotency — skip users who already have a subscription-pro record:** Mock `strapi.db.query("api::subscription-pro.subscription-pro").findOne` to return an existing record for one user. Assert `strapi.entityService.create` is NOT called for that user.

4. **Empty result set — no users to migrate:** Mock findMany to return empty array. Assert create is never called, logger.info reports "no PRO users to migrate".

Follow the AAA pattern (Arrange, Act, Assert). Mock strapi globally as done in other test files.

**B. Update protect-user-fields.test.ts:**

1. Verify that `tbk_user`, `pro_card_type`, `pro_card_last4`, `pro_inscription_token` ARE still stripped from request body (they remain protected since fields still exist on user schema).

2. Add/update a test that sends a body with `{ pro_status: "active", pro_expires_at: "2026-05-01" }` and verifies these ARE still stripped.

3. If there is a test that checks the complete list of PROTECTED_USER_FIELDS, update it to match the current list (unchanged):
   `["pro_status", "pro_expires_at", "tbk_user", "pro_card_type", "pro_card_last4", "pro_inscription_token", "username", "avatar", "cover", "role", "provider", "confirmed", "blocked"]`

**C. Full verification sweep:**

After updating the tests, run the full Strapi test suite to confirm no regressions:
```bash
cd apps/strapi && yarn jest --no-coverage
```

Also run TypeScript check:
```bash
cd apps/strapi && npx tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && yarn jest --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `yarn jest --testPathPattern="migrate-subscription-pro"` exits 0
    - Migration test covers: active users migrated, cancelled users migrated, idempotency skip, empty set
    - `yarn jest --testPathPattern="protect-user-fields"` exits 0
    - Middleware test asserts `tbk_user` IS still stripped (field remains protected)
    - `yarn jest --no-coverage` exits 0 (full suite green)
    - `npx tsc --noEmit` exits 0 (TypeScript compiles clean)
  </acceptance_criteria>
  <done>All tests pass including new migration test, TypeScript compiles clean, migration is verified complete</done>
</task>

</tasks>

<verification>
- `cd apps/strapi && yarn jest --no-coverage` — full suite green
- `cd apps/strapi && npx tsc --noEmit` — TypeScript compiles
- `grep -r "user\.tbk_user" apps/strapi/src/cron/ apps/strapi/src/api/payment/services/pro-cancellation.service.ts` — returns 0 matches (no direct user.tbk_user reads)
- `test -f apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts` — migration test exists
</verification>

<success_criteria>
- All Strapi tests pass (jest exits 0)
- TypeScript compiles without errors
- No direct `user.tbk_user` reads remain in cron or cancellation service
- protect-user-fields test validates card fields are STILL protected
- Bootstrap migration test covers idempotency, active/cancelled users, and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/120-refactor-pro-subscription-model-subscription-pro-collection-type-move-card-data-out-of-user-fix-charge-before-activate-order-calendar-billing/120-04-SUMMARY.md`
</output>
