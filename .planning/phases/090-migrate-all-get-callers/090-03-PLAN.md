---
phase: 090-migrate-all-get-callers
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/stores/me.store.ts
  - apps/website/app/stores/user.store.ts
  - apps/website/app/stores/indicator.store.ts
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "me.store.ts has no strapi.find() calls — loadMe uses useApiClient"
    - "user.store.ts has no strapi.find() calls — loadUsers, loadUser, loadUserAds, loadUserOrders, loadUserAdCounts use useApiClient"
    - "indicator.store.ts has no strapi.find() or strapi.findOne() calls — fetchIndicators, fetchIndicator, convertCurrency use useApiClient"
    - "user.store.ts already has client = useApiClient() at root level (from phase 088) — no duplicate instantiation added"
    - "indicator.store.ts findOne pattern: client('/api/indicators/${code}', { method: 'GET' }) returns raw body — no .data wrapper"
  artifacts:
    - path: "apps/website/app/stores/me.store.ts"
      provides: "Current user fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/user.store.ts"
      provides: "User list, user ads, orders, and ad counts via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/indicator.store.ts"
      provides: "Economic indicators and currency conversion via useApiClient"
      contains: "useApiClient"
  key_links:
    - from: "me.store.ts loadMe"
      to: "useApiClient"
      via: "client('/api/users/me', { method: 'GET', params })"
      pattern: "client\\('/api/users/me'"
    - from: "indicator.store.ts fetchIndicator"
      to: "useApiClient"
      via: "client(`/api/indicators/${code}`, { method: 'GET' })"
      pattern: "client\\(`/api/indicators/"
---

<objective>
Migrate 3 user-and-business stores from `strapi.find()`/`strapi.findOne()` to `useApiClient`. These stores handle current user profile, user-scoped data (ads, orders, counts), and economic indicators.

Special cases:
- `user.store.ts` ALREADY has `const client = useApiClient()` at root level (from phase 088 mutation migration). The GET calls must use the same root-level `client` variable — do NOT add another `useApiClient()` call.
- `indicator.store.ts` uses `strapi.findOne()` for `fetchIndicator` — this becomes `client('/api/indicators/${code}', { method: 'GET' })` returning raw body instead of `{ data: T }`.

Purpose: Completes store-level migration, satisfying API-01 and API-02.
Output: 3 modified store files.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-migrate-all-get-callers/090-VALIDATION.md

<interfaces>
<!-- Critical patterns for this plan -->

user.store.ts existing structure (after phase 088):
```typescript
export const useUserStore = defineStore("user", () => {
  const strapi = useStrapi()      // <-- STILL HERE for GET calls
  const client = useApiClient()   // <-- ALREADY HERE from phase 088 for PUT calls
  // ... both exist currently
})
```
Goal: remove `strapi = useStrapi()`, use existing `client` for all GET calls.

me.store.ts existing structure:
```typescript
export const useMeStore = defineStore("me", () => {
  const strapi = useStrapi()      // <-- replace this
  const apiClient = useApiClient() // <-- ALREADY HERE from phase 088 (named apiClient not client)
})
```
Goal: remove `strapi = useStrapi()`, use existing `apiClient` for GET calls.

indicator.store.ts — findOne pattern:
```typescript
// Before:
const response = await strapi.findOne<StrapiData<Indicator>>(`indicators/${code}`)
// strapi.findOne returns { data: T, meta: {} } — so caller does response.data or response (needs investigation)

// After:
const response = await client(`/api/indicators/${code}`, { method: 'GET' })
// useApiClient returns raw body — for findOne endpoints this is { data: T, meta: {} }
// BUT: current code returns `response` directly (not response.data)
// So the migration is: replace strapi.findOne call with client call, keep same return value
```

Key: `strapi.findOne` in @nuxtjs/strapi v2 returns the response body directly (already the raw JSON),
same as what `useApiClient` returns. The existing return statements need no change.

Indicator store's StrapiData/StrapiResponse type imports (from @nuxtjs/strapi) can remain — they
describe the SHAPE of the response body, not SDK wrappers. They're valid TypeScript types regardless.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate me.store.ts</name>
  <files>apps/website/app/stores/me.store.ts</files>
  <action>
me.store.ts has TWO composables at root level: `strapi = useStrapi()` and `apiClient = useApiClient()` (the latter from phase 088 for `saveUsername` PUT call).

1. Remove `const strapi = useStrapi()` (the apiClient line stays — it's used for saveUsername).
2. `loadMe()`:
   - Replace `await strapi.find("users/me", { populate: { commune: { populate: "region" } } })` → `await apiClient('/api/users/me', { method: 'GET', params: { populate: { commune: { populate: 'region' } } } as unknown as Record<string, unknown> })`.
   - `me.value = response as unknown as User` — unchanged (raw body IS the User object for `/api/users/me`).
   - Note: The current code does `response as unknown as User` directly (no `.data` wrapper) — this stays the same since `useApiClient` also returns raw body.
  </action>
  <verify>
    <automated>grep -n "strapi" apps/website/app/stores/me.store.ts || echo "PASS: no strapi references"</automated>
  </verify>
  <done>me.store.ts has zero `strapi` references; loadMe uses apiClient('/api/users/me').</done>
</task>

<task type="auto">
  <name>Task 2: Migrate user.store.ts GET calls</name>
  <files>apps/website/app/stores/user.store.ts</files>
  <action>
user.store.ts has BOTH `strapi = useStrapi()` and `client = useApiClient()` at root level (client added in phase 088 for PUT calls). Remove `strapi = useStrapi()` and migrate all 5 GET calls to use the existing root-level `client`:

1. `loadUsers()`:
   - Replace `await strapi.find("users", { pagination: { pageSize: 20, page: 1 }, populate: "*" })` → `await client('/api/users', { method: 'GET', params: { pagination: { pageSize: 20, page: 1 }, populate: '*' } as unknown as Record<string, unknown> })`.
   - `users.value = response.data as unknown as User[]` — BUT: `useApiClient` returns raw body. For `/api/users` Strapi returns a raw array (not `{ data: [], meta: {} }`). Check current code: `response.data as unknown as User[]`. After migration, `response` IS already the array (or `{ data: [] }` structure). Keep cast but if `response.data` was the array, now `response` may be the array directly. Use: `users.value = (response as unknown as { data: User[] }).data ?? (response as unknown as User[])` — OR keep original cast and test. SAFER: keep `response.data as unknown as User[]` pattern unchanged since Strapi v5 collection endpoints return `{ data: [], meta: {} }`.

2. `loadUser(slug)`:
   - Replace `await strapi.find("users", { filters: { username: { $eq: slug } }, populate: "*" })` → `await client('/api/users', { method: 'GET', params: { filters: { username: { $eq: slug } }, populate: '*' } as unknown as Record<string, unknown> })`.
   - `const list = (response.data ?? response) as unknown as User[]` — this fallback already handles both response shapes; keep unchanged.

3. `loadUserAds(filters, pagination, sort)`:
   - Replace `await strapi.find("ads/me", { filters, pagination, sort, populate: "*" })` → `await client('/api/ads/me', { method: 'GET', params: { filters, pagination, sort, populate: '*' } as unknown as Record<string, unknown> })`.
   - `ads.value = response.data as unknown as Ad[]` — unchanged.

4. `loadUserOrders(filters, pagination, sort)`:
   - Replace `await strapi.find("orders/me", { filters, pagination, sort, populate: "*" })` → `await client('/api/orders/me', { method: 'GET', params: { filters, pagination, sort, populate: '*' } as unknown as Record<string, unknown> })`.
   - `return response` — unchanged.

5. `loadUserAdCounts()`:
   - Replace `await strapi.find("ads/me/counts", {})` → `await client('/api/ads/me/counts', { method: 'GET' })`.
   - `return response as unknown as { published: number; review: number; expired: number; rejected: number; banned: number }` — unchanged.

After all replacements, remove `const strapi = useStrapi()` from root level.
  </action>
  <verify>
    <automated>grep -n "strapi\.find\|useStrapi" apps/website/app/stores/user.store.ts || echo "PASS: no strapi.find/useStrapi references"</automated>
  </verify>
  <done>user.store.ts has zero `strapi.find`/`useStrapi` references; all 5 GET actions use the existing root-level `client`.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate indicator.store.ts</name>
  <files>apps/website/app/stores/indicator.store.ts</files>
  <action>
indicator.store.ts is a setup store with `strapi = useStrapi()` at root level. It has THREE strapi calls (2x find, 1x findOne).

1. Replace `const strapi = useStrapi()` with `const client = useApiClient()` at root level.
2. `fetchIndicators()`:
   - Replace `await strapi.find<{ data: Indicator[]; meta: { timestamp: string } }>("indicators")` → `await client('/api/indicators', { method: 'GET' })`.
   - Remove the TypeScript generic (useApiClient uses `<T = unknown>`) — cast instead: `const response = await client('/api/indicators', { method: 'GET' }) as { data: Indicator[]; meta: { timestamp: string } }`.
   - `indicators.value = response.data as unknown as Indicator[]` — unchanged (raw body from Strapi IS `{ data: [], meta: {} }`).
   - `return response` — unchanged.
3. `fetchIndicator(code)`:
   - Replace `await strapi.findOne<StrapiData<Indicator>>(`indicators/${code}`)` → `await client(`/api/indicators/${code}`, { method: 'GET' })`.
   - The current code: `return response` — `strapi.findOne` returns the raw response body (same as useApiClient). No change to return.
   - Cast: `return response as unknown as StrapiData<Indicator>` if needed for TypeScript.
4. `convertCurrency({ amount, from, to })`:
   - Replace `await strapi.find<StrapiResponse<ConvertResponse>>("indicators/convert", { amount, from, to } as unknown as Record<string, unknown>)` → `await client('/api/indicators/convert', { method: 'GET', params: { amount, from, to } as unknown as Record<string, unknown> })`.
   - `return response` — unchanged.
   - Cast: `return response as unknown as StrapiResponse<ConvertResponse>` if TypeScript complains.

Keep the `StrapiData`, `StrapiResponse` type imports from `@nuxtjs/strapi` — they describe response shapes, not SDK behavior, so they remain valid.
  </action>
  <verify>
    <automated>grep -n "strapi\.find\|strapi\.findOne\|useStrapi" apps/website/app/stores/indicator.store.ts || echo "PASS: no strapi calls"</automated>
  </verify>
  <done>indicator.store.ts has zero `strapi.find`/`strapi.findOne`/`useStrapi` references; all 3 fetch functions use client('/api/indicators').</done>
</task>

</tasks>

<verification>
After all tasks:
`grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/me.store.ts apps/website/app/stores/user.store.ts apps/website/app/stores/indicator.store.ts` → exits 1 (no matches)
</verification>

<success_criteria>
- me.store.ts: loadMe uses apiClient('/api/users/me')
- user.store.ts: loadUsers, loadUser, loadUserAds, loadUserOrders, loadUserAdCounts use client('/api/...')
- indicator.store.ts: fetchIndicators uses client('/api/indicators'), fetchIndicator uses client('/api/indicators/${code}'), convertCurrency uses client('/api/indicators/convert')
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/090-migrate-all-get-callers/090-03-SUMMARY.md`
</output>
