---
phase: 090-migrate-all-get-callers
plan: "06"
type: execute
wave: 2
depends_on:
  - "090-01"
  - "090-02"
  - "090-03"
  - "090-04"
  - "090-05"
files_modified: []
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-06

must_haves:
  truths:
    - "Zero strapi.find() or strapi.findOne() calls remain in any store, composable, page, or component in apps/website"
    - "nuxt typecheck exits 0 with zero TypeScript errors (or only the same pre-existing errors as the baseline)"
    - "All 12 stores, 3 composables, 4 pages, and 1 component have been migrated"
  artifacts:
    - path: "apps/website/app/stores/"
      provides: "All stores using useApiClient for GET calls"
      contains: "useApiClient"
    - path: "apps/website/app/composables/useApiClient.ts"
      provides: "Single entry point for all API calls"
  key_links:
    - from: "all store/composable/page GET calls"
      to: "useApiClient"
      via: "client('/api/...', { method: 'GET', params: {...} })"
      pattern: "strapi\\.find|strapi\\.findOne"
---

<objective>
Final validation gate: confirm all strapi.find/findOne calls have been eliminated across the entire apps/website codebase, and TypeScript typechecks passes with zero new errors. This plan acts as the quality checkpoint for the full migration.

Purpose: Satisfies API-06 (typeCheck: true passes after full migration). Provides definitive proof of requirement completion.
Output: No files created — validation-only plan.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-migrate-all-get-callers/090-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global strapi.find/findOne grep gate</name>
  <files></files>
  <action>
Run a comprehensive grep across ALL apps/website source files to confirm no strapi.find or strapi.findOne calls remain:

```bash
grep -rn "strapi\.find\|strapi\.findOne" \
  apps/website/app/stores/ \
  apps/website/app/composables/ \
  apps/website/app/pages/ \
  apps/website/app/components/
```

Expected result: No matches (exit code 1). If matches are found, fix them before proceeding.

Also check that `useStrapi()` is no longer called for data fetching (it may still exist in auth helpers — that's OK):
```bash
grep -rn "const strapi = useStrapi\|useStrapi()" \
  apps/website/app/stores/ \
  apps/website/app/composables/ \
  apps/website/app/pages/ \
  apps/website/app/components/
```

Acceptable remaining `useStrapi` calls: auth-related helpers (fetchUser, login, logout, register) — these are NOT data-fetching and are explicitly out of scope per REQUIREMENTS.md Out of Scope section.
  </action>
  <verify>
    <automated>grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/ apps/website/app/composables/ apps/website/app/pages/ apps/website/app/components/ && echo "FAIL: strapi calls remain" || echo "PASS: zero strapi.find/findOne calls"</automated>
  </verify>
  <done>Zero strapi.find/findOne calls in stores, composables, pages, and components.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript typecheck</name>
  <files></files>
  <action>
Run `nuxt typecheck` to verify TypeScript compiles cleanly after all migrations:

```bash
yarn workspace waldo-website nuxt typecheck
```

Expected: exits 0.

If new TypeScript errors appear (not pre-existing baseline), fix them:
- Most common issue: response type mismatch where `response.data` is no longer typed
- Fix: add explicit cast `as { data: T }` or `as unknown as T`
- All response casts in the migrated files should already handle this

Pre-existing errors (from phase 089 baseline — DO NOT fix these, they are out of scope):
- None known — typecheck was passing at phase 089 completion
  </action>
  <verify>
    <automated>yarn workspace waldo-website nuxt typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>nuxt typecheck exits 0. Zero TypeScript errors in the migrated files.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Browser smoke test — all pages load correctly</name>
  <files></files>
  <action>
    Human confirms that the full migration works end-to-end in the browser. Start the dev server and test key pages.
  </action>
  <verify>
    1. Start the dev server: `yarn workspace waldo-website dev`
    2. Visit http://localhost:3000/ — home page loads, categories show, packs show, FAQs show
    3. Visit http://localhost:3000/packs — packs page loads correctly
    4. Visit http://localhost:3000/anuncios — ads listing loads (uses ads.store, filter.store, regions.store)
    5. Log in and visit http://localhost:3000/cuenta — profile page loads (me.store, user.store)
    6. Check browser console — zero API errors, zero runtime errors
  </verify>
  <done>All key pages load without errors. No console warnings about strapi SDK or response shape mismatches. Type "approved" to confirm.</done>
</task>

</tasks>

<verification>
Complete migration proof:
1. `grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/` → exits 1 (zero matches in any .ts/.vue file in stores/, composables/, pages/, components/)
2. `yarn workspace waldo-website nuxt typecheck` → exits 0
3. Human verification: all key pages load in browser without errors
</verification>

<success_criteria>
- API-01: All 12 stores have zero strapi.find() calls ✓
- API-02: All strapi.findOne() calls replaced ✓ (indicator.store, useOrderById, anunciar/gracias.vue)
- API-03: All 3 composables use useApiClient ✓
- API-04: All 4 pages and FormProfile.vue use useApiClient or have dead imports removed ✓
- API-06: nuxt typecheck exits 0 ✓
</success_criteria>

<output>
After completion, create `.planning/phases/090-migrate-all-get-callers/090-06-SUMMARY.md`
</output>
