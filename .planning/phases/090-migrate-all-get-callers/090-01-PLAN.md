---
phase: 090-migrate-all-get-callers
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/stores/filter.store.ts
  - apps/website/app/stores/regions.store.ts
  - apps/website/app/stores/communes.store.ts
  - apps/website/app/stores/conditions.store.ts
  - apps/website/app/stores/faqs.store.ts
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "filter.store.ts has no strapi.find() calls — loadFilterCommunes and loadFilterCategories use useApiClient"
    - "regions.store.ts has no strapi.find() calls — loadRegions uses useApiClient"
    - "communes.store.ts has no strapi.find() calls — loadCommunes uses useApiClient"
    - "conditions.store.ts has no strapi.find() calls — loadConditions and loadConditionById use useApiClient"
    - "faqs.store.ts has no strapi.find() calls — loadFaqs uses useApiClient"
    - "Each migrated store instantiates client = useApiClient() at the store root level (not inside action functions)"
    - "Response access uses raw body — .data wrapper removed wherever strapi SDK added it"
  artifacts:
    - path: "apps/website/app/stores/filter.store.ts"
      provides: "Commune and category filter data fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/regions.store.ts"
      provides: "Region list fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/communes.store.ts"
      provides: "Commune list fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/conditions.store.ts"
      provides: "Condition list and single-condition fetching via useApiClient"
      contains: "useApiClient"
    - path: "apps/website/app/stores/faqs.store.ts"
      provides: "FAQ list fetching via useApiClient"
      contains: "useApiClient"
  key_links:
    - from: "each store action"
      to: "useApiClient()"
      via: "const client = useApiClient() at store root level"
      pattern: "client\\(url,\\s*\\{\\s*method:\\s*'GET'"
---

<objective>
Migrate 5 reference-data stores from `strapi.find()` to `useApiClient`. These are the simplest stores — each makes a single GET call with no SDK-specific generics. They all follow the same pattern: `strapi.find(endpoint, params)` → `client(url, { method: 'GET', params })` with the response being the raw body (no `.data` wrapper needed since these stores already cast `response as unknown as SomeType`).

Purpose: Removes strapi SDK dependency from filter, regions, communes, conditions, and faqs stores, satisfying API-01.
Output: 5 modified store files with `strapi.find()` replaced by `useApiClient`.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/090-migrate-all-get-callers/090-VALIDATION.md

@.planning/phases/089-get-support-in-useapiclient/089-01-SUMMARY.md
@.planning/phases/088-mutation-calls-use-api-client/088-01-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from codebase. Executor needs no further exploration. -->

From apps/website/app/composables/useApiClient.ts:
```typescript
export function useApiClient(): (url: string, options?: {
  method?: string;
  params?: Record<string, unknown>;
  body?: unknown;
  headers?: Record<string, string>;
}) => Promise<unknown>
```

Calling convention (confirmed by 089 test suite):
```typescript
const client = useApiClient()  // at store root level, not inside actions
const response = await client('endpoint', { method: 'GET', params: { ... } })
// response IS the raw body — no .data wrapper
// cast: response as unknown as YourType
```

SDK pattern being replaced:
```typescript
const strapi = useStrapi()
const response = await strapi.find('endpoint', params)
// response from strapi.find IS already wrapped: { data: T[], meta: {...} }
// so existing code does: response as unknown as StrapiResponse<T> then uses .data
```

Key insight: After migration, `client(url, { method: 'GET', params })` returns the SAME structure
as `strapi.find()` — a raw JSON body from Strapi which IS `{ data: T[], meta: {...} }` for
collection endpoints. The casts (`as unknown as SomeType`) remain valid. No response shape changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate filter.store.ts</name>
  <files>apps/website/app/stores/filter.store.ts</files>
  <action>
Replace `strapi.find()` with `useApiClient` in both `loadFilterCommunes` and `loadFilterCategories`.

Current pattern (lines 47 and 80):
```typescript
const strapi = useStrapi()
const response = await strapi.find("filter/communes", {})
```

Migration steps:
1. Add `const client = useApiClient()` at the store root, BEFORE the action functions (same level as other `const` declarations at top of the `() =>` function body). Remove `const strapi = useStrapi()`.
2. In `loadFilterCommunes`: replace `strapi.find("filter/communes", {})` with `client('/api/filter/communes', { method: 'GET' })`.
3. In `loadFilterCategories`: replace `strapi.find("filter/categories", {})` with `client('/api/filter/categories', { method: 'GET' })`.
4. Response casts (`as unknown as FilterResponse<FilterCommune>` and `as unknown as FilterResponse<FilterCategory>`) remain unchanged — raw body matches the expected shape.
5. Remove any unused `useStrapi` call from the file.

Note: `client` must be instantiated at store root level (composable rules — must be in setup context, not inside async functions).
  </action>
  <verify>
    <automated>grep -n "strapi" apps/website/app/stores/filter.store.ts || echo "No strapi references — PASS"</automated>
  </verify>
  <done>filter.store.ts has zero `strapi` references; both loadFilterCommunes and loadFilterCategories use `client('/api/filter/communes', ...)` and `client('/api/filter/categories', ...)` respectively.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate regions, communes, conditions, faqs stores</name>
  <files>
    apps/website/app/stores/regions.store.ts
    apps/website/app/stores/communes.store.ts
    apps/website/app/stores/conditions.store.ts
    apps/website/app/stores/faqs.store.ts
  </files>
  <action>
Apply the same migration pattern to the four remaining stores. These use the Options API (defineStore with `state/getters/actions`), so `useApiClient()` must be called at the top of each action that uses it (Options API stores don't have a setup root, so per-action instantiation is acceptable here — same composable rules apply in setup context, but actions in Options API stores are regular methods, not setup context).

**regions.store.ts** — Options API store:
- In `loadRegions()` action: add `const client = useApiClient()` at the top of the action body, replace `const strapi = useStrapi()` and `strapi.find("regions", { ... })` with `await client('/api/regions', { method: 'GET', params: { pagination: { page: 1, pageSize: 1000 } } as unknown as Record<string, unknown> })`.
- Cast: `response as unknown as RegionResponse` remains unchanged.

**communes.store.ts** — Options API store:
- In `loadCommunes()` action: add `const client = useApiClient()` at top, replace `const strapi = useStrapi()` and `strapi.find("communes", { ... })` with `await client('/api/communes', { method: 'GET', params: { pagination: { page: 1, pageSize: 1000 }, populate: '*', sort: ['name:asc'] } as unknown as Record<string, unknown> })`.
- Cast: `response as unknown as CommuneResponse` remains unchanged.

**conditions.store.ts** — Options API store, TWO actions:
- `loadConditions()`: replace `strapi.find("conditions", { pagination, populate })` → `client('/api/conditions', { method: 'GET', params: { pagination: { page: 1, pageSize: 1000 }, populate: '*' } as unknown as Record<string, unknown> })`.
- `loadConditionById(id)`: replace `strapi.find("conditions", { filters: { id: { $eq: id } }, populate })` → `client('/api/conditions', { method: 'GET', params: { filters: { id: { $eq: id } }, populate: '*' } as unknown as Record<string, unknown> })`.
- Both have `const strapi = useStrapi()` inside the action — replace each with `const client = useApiClient()`.

**faqs.store.ts** — setup store, but `strapi` is at root level:
- Move `const client = useApiClient()` to root level (replacing `const strapi = useStrapi()`).
- In `loadFaqs()`: replace `strapi.find("faqs", { ... })` → `client('/api/faqs', { method: 'GET', params: { pagination: { pageSize: PAGE_SIZE, page: 1 }, populate: '*', sort: ['createdAt:asc'] } as unknown as Record<string, unknown> })`.
- Cast: `response as unknown as FAQResponse` remains unchanged.

For all stores: Strapi endpoint URL format is `/api/{collection}` — `useApiClient` calls `useStrapiClient()` which prepends the Strapi base URL, so the path must start with `/api/`.
  </action>
  <verify>
    <automated>grep -rn "strapi\.find\|strapi\.findOne\|useStrapi" apps/website/app/stores/regions.store.ts apps/website/app/stores/communes.store.ts apps/website/app/stores/conditions.store.ts apps/website/app/stores/faqs.store.ts || echo "No strapi calls — PASS"</automated>
  </verify>
  <done>All four stores have zero `strapi.find`/`useStrapi` references; each uses `useApiClient` with correct `/api/` prefixed URLs.</done>
</task>

<task type="auto">
  <name>Task 3: Verify wave 1 stores — typecheck + grep gate</name>
  <files></files>
  <action>
Run verification to confirm all 5 stores in this plan are clean:
1. Grep for any remaining strapi.find/findOne in the 5 files
2. Run typecheck to catch any type errors introduced by the migration
  </action>
  <verify>
    <automated>grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/filter.store.ts apps/website/app/stores/regions.store.ts apps/website/app/stores/communes.store.ts apps/website/app/stores/conditions.store.ts apps/website/app/stores/faqs.store.ts && echo "FAIL: strapi calls remain" || echo "PASS: no strapi calls"</automated>
  </verify>
  <done>Zero strapi.find/findOne references in all 5 stores. TypeScript typecheck exits 0 (or same pre-existing errors as baseline — no new errors introduced by this plan).</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rn "strapi\.find\|strapi\.findOne" apps/website/app/stores/filter.store.ts apps/website/app/stores/regions.store.ts apps/website/app/stores/communes.store.ts apps/website/app/stores/conditions.store.ts apps/website/app/stores/faqs.store.ts` → exits 1 (no matches)
2. `yarn workspace waldo-website nuxt typecheck` → exits 0 (or same pre-existing errors, no new ones)
</verification>

<success_criteria>
- filter.store.ts: loadFilterCommunes + loadFilterCategories use client('/api/filter/communes') and client('/api/filter/categories')
- regions.store.ts: loadRegions uses client('/api/regions')
- communes.store.ts: loadCommunes uses client('/api/communes')
- conditions.store.ts: loadConditions + loadConditionById use client('/api/conditions')
- faqs.store.ts: loadFaqs uses client('/api/faqs')
- Zero new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/090-migrate-all-get-callers/090-01-SUMMARY.md`
</output>
