---
phase: 08-publico-anuncios-perfil
plan: 02
type: execute
wave: 2
depends_on: ["08-01", "08-04"]
files_modified:
  - apps/website/app/components/AdSingle.vue
  - apps/website/app/components/HeroAnnouncement.vue
  - apps/website/app/components/GalleryDefault.vue
  - apps/website/app/components/ReminderDefault.vue
  - apps/website/app/components/ShareDefault.vue
  - apps/website/app/scss/components/_announcement.scss
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_gallery.scss
  - apps/website/app/scss/components/_reminder.scss
  - apps/website/app/scss/components/_share.scss
autonomous: true
requirements: [PUB-AD-DETAIL, PUB-AD-CONTACT]
must_haves:
  truths:
    - "The ad interior (/anuncios/[slug]) renders to the 'Detalle de producto' mockup: breadcrumb hero with title + meta, left column (gallery + Acerca + Ubicación + Especificación técnica), sticky right column (precio card + vendedor/contacto card + compartir card), logged-OUT and logged-IN, on desktop 1440 and mobile 390"
    - "Logged-OUT shows the 'Datos de contacto protegidos' gate card (lock icon, iniciar sesión / crear cuenta CTAs, NO contact data); logged-IN shows the seller card with email + phone shown OBFUSCATED (the masked values from the payload — the real values are NOT in the payload) and a per-row eye/copy button that REVEALS the real value by fetching the matching per-channel reveal endpoint"
    - "Clicking the email eye button calls GET /ads/:documentId/reveal/email (loading spinner → check), un-masks the row with the real email and copies it; clicking the phone eye button calls GET /ads/:documentId/reveal/phone the same way — each reveal is its OWN single-channel request (08-04 endpoints), and the endpoint records the ad-contact event server-side"
    - "The WhatsApp button fetches GET /ads/:documentId/reveal/whatsapp then opens wa.me/<digits>; the Call button fetches GET /ads/:documentId/reveal/phone then opens tel:<phone> — both live INSIDE the logged-IN block (never shown logged-OUT), use the real value returned by the reveal endpoint (NOT a payload value), and the endpoint records the contact"
    - "The ad detail still loads with a single ad request (ads/slug/:slug embeds gallery/seller/category/commune/condition with the seller contact ALREADY OBFUSCATED) plus the existing related-ads load for active ads only; the reveals are tiny per-channel on-demand calls (only when the user clicks), no per-field prefetch, no N+1 on load"
    - "Related ads reuse the CardAnnouncement restyled in 08-01; colors/typography use phase-04 tokens; no existing SCSS variable is modified; hero clears the headroom header"
  artifacts:
    - path: "apps/website/app/components/AdSingle.vue"
      provides: "Ad detail body + sticky sidebar (precio / vendedor-contacto with masked email+phone, per-channel eye-reveal + WhatsApp + Call via the 08-04 reveal endpoints / compartir) restyled to mockup"
    - path: "apps/website/app/scss/components/_announcement.scss"
      provides: "Restyled .announcement--single section ONLY (08-01 owns --archive — do not collide)"
    - path: "apps/website/app/components/HeroAnnouncement.vue"
      provides: "Restyled detail hero (breadcrumb + title + meta + volver)"
  key_links:
    - from: "apps/website/app/pages/anuncios/[slug].vue"
      to: "apps/website/app/components/AdSingle.vue"
      via: ":all=ad :access=access (single useAsyncData ad-${slug})"
      pattern: "<AdSingle"
    - from: "apps/website/app/components/AdSingle.vue"
      to: "logged-in gate"
      via: "isLoggedIn computed off useSessionUser"
      pattern: "isLoggedIn"
    - from: "apps/website/app/components/AdSingle.vue eye/copy + WhatsApp + Call"
      to: "GET /ads/:documentId/reveal/:channel (08-04)"
      via: "apiClient fetch per channel → real value → unmask / copy / wa.me / tel:"
      pattern: "reveal/"
---

<objective>
Restyle the public ad interior (`pages/anuncios/[slug].vue` via `HeroAnnouncement` + `AdSingle` + `RelatedAds`) to the v1.47 "Detalle de producto" mockup (`apps/design/index.dc.html` lines ~767-966) at FULL FIDELITY, AND implement the user-approved obfuscated-contact + per-channel reveal model. The contact GATE (logged-OUT vs logged-IN) is preserved. Logged-IN: email/phone are rendered OBFUSCATED (the masked strings already in the payload from 08-04) and a per-row eye/copy button reveals the REAL value by calling the matching single-channel reveal endpoint; WhatsApp and Call buttons reveal-then-open. The real values NEVER ship in the payload — they arrive only on the user's click.

Purpose: the ad interior is the conversion surface AND a PII/anti-scraping surface. Masked-by-default + per-channel reveal exposes the real contact only to an authenticated human at the moment they contact, killing bulk scraping.
Output: Restyled detail hero, gallery + content (Acerca/Ubicación/Especificación), and the sticky sidebar (precio card; vendedor/contacto card with the logged-OUT gate, the logged-IN masked email/phone + eye-reveal + WhatsApp + Call wired to the 08-04 reveal endpoints; compartir card) matching the mockup on desktop + mobile, logged-OUT and logged-IN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — "Detalle de producto" screen; translate inline styles to BEM + phase-04 tokens:
@apps/design/index.dc.html
# Rendered references — NOTE: the "Detalle de producto" screen has NO dedicated rendered PNG in screenshots/
# (the article-*.png files are the BLOG "Artículo" screen, NOT the ad detail — do NOT compare against them).
# The AUTHORITATIVE reference for this view is the DC SOURCE apps/design/index.dc.html lines 767–966 (read in full).
# Only headroom.png is a valid rendered reference here (header-overlap clearance):
@apps/design/screenshots/headroom.png

# Current page + components to UPDATE (closest existing equivalents — do NOT create new):
@apps/website/app/pages/anuncios/[slug].vue
@apps/website/app/components/AdSingle.vue
@apps/website/app/components/HeroAnnouncement.vue
@apps/website/app/components/GalleryDefault.vue
@apps/website/app/components/ReminderDefault.vue
@apps/website/app/components/ShareDefault.vue
@apps/website/app/components/CardInfo.vue

<interfaces>
<!-- phase-04 tokens — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DATA PATH (already optimized — keep it):
  [slug].vue → ONE useAsyncData "ad-${slug}" → adsStore.loadAdBySlug → GET ads/slug/:slug
    (returns { data: ad, access } with gallery/user/category/commune/condition embedded — single request, no N+1)
    + indicatorStore.convertCurrency (price in alt currency) + relatedStore.loadRelatedAds(ad.id) ONLY when ad.status === "active".
  Do NOT add extra per-field/per-spec requests. The price-conversion + related calls already exist; keep them. -->

<!-- CONTACT DATA — CHANGED in 08-04 (read this carefully — supersedes the old "data already loaded" model):
  getUserFromAll = all.user. sanitizeAdForPublic (08-04) now returns user.email / user.phone / user.whatsapp OBFUSCATED
  (e.g. "g•••••@•••••.cl", "+56 9 •••• ••78") PLUS booleans user.has_email / user.has_phone / user.has_whatsapp.
  The REAL email/phone/whatsapp are NOT in the payload. To get a real value you MUST call the 08-04 per-channel reveal endpoint:
    GET ads/${documentId}/reveal/email     → { data: { channel:"email",    value: "<real email>" } }
    GET ads/${documentId}/reveal/phone     → { data: { channel:"phone",    value: "<real phone>" } }
    GET ads/${documentId}/reveal/whatsapp  → { data: { channel:"whatsapp", value: "<real whatsapp>" } }
  These require a logged-in viewer (the proxy injects the JWT) and RECORD the ad-contact event server-side — so the frontend does NOT
  also POST ads/:documentId/contact for a reveal (the endpoint already records it). The old handleContact() did TWO things: (1) contactSeller(type)
  GA4 analytics [client-side — KEEP IT] and (2) the POST ads/:documentId/contact [SUPERSEDED — the reveal endpoint records server-side now].
  So on every reveal/WhatsApp/Call click STILL call contactSeller(type) for GA4, but DROP the duplicate POST (no double-count). Do NOT remove
  the useAdAnalytics import / contactSeller — only the POST path goes. Use which channels to RENDER off has_email/has_phone/has_whatsapp. -->

<!-- CONTACT GATE (behavioral contract — preserve the gate, implement masked+reveal inside logged-IN):
  AdSingle: isLoggedIn = !!useSessionUser().value
    logged-IN  → seller CardInfo + masked Email/Phone rows with eye-reveal + WhatsApp + Call (all via 08-04 reveal endpoints)
    logged-OUT → gate ONLY (no contact actions, no masked values, ever) -->

<!-- SCSS OWNERSHIP: edit ONLY the .announcement--single section of _announcement.scss (lines ~65-224).
  The .announcement--archive section and .card--announcement in _card.scss are owned by 08-01 — DO NOT touch them.
  RelatedAds renders CardAnnouncement (already restyled in 08-01) — do not re-style the card.
  SHARED FILE CAVEAT: _hero.scss is also listed by 08-03 (.hero--profile). Scope strictly to &--announcement here; merge cleanly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the detail hero (HeroAnnouncement) to the mockup breadcrumb header</name>
  <files>apps/website/app/components/HeroAnnouncement.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Translate the "Detalle de producto" hero (`apps/design/index.dc.html` lines ~770-793) to the existing `.hero--announcement` block. Edit ONLY the `&--announcement` block in `_hero.scss`.
    Layout per mockup: a light hero on a subtle category-tinted bg with a bottom $line border; breadcrumb (Waldo › Anuncios › {categoría} › {título}) in $muted with chevron separators and $ink current crumb; Poppins 800 $ink title (clamp ~30-44px, tight letter-spacing); a meta row under the title with a category pill + "Publicado {fecha}" (clock icon) and — IF the page already provides it — a views count. SCOPE GUARD: only render meta the component already receives (name, category, user, dates). Do NOT invent a new views-count fetch — if the current props don't include a view count, omit it rather than add a request.
    Keep the existing props (`name`, `category`, `user`) and the component's current data flow. CRITICAL: carry ~104-130px top padding so the hero clears the fixed/overlay headroom header (06-02) — verify vs apps/design/screenshots/headroom.png.
    Tokens only; BEM strict (children under `.hero--announcement__*`); no existing-variable edits; no box-shadow/scale; lucide icons imported without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroAnnouncement" || echo "no hero type errors"</automated>
    VISUAL LOOP (dev server already on :3000): screenshot a real ad detail URL (find one slug from http://localhost:3000/anuncios) at 1440 and 390 → save /tmp/waldo-shots/08-02-hero-{desktop,mobile}.png → Read → compare breadcrumb + title + meta + headroom clearance vs the mockup + headroom.png → fix → repeat until match.
  </verify>
  <done>Detail hero matches the mockup breadcrumb header, clears the headroom header, Poppins title + meta styled from existing props only (no new fetch); tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the detail body (gallery + Acerca + Ubicación + Especificación) to the mockup left column</name>
  <files>apps/website/app/components/AdSingle.vue, apps/website/app/components/GalleryDefault.vue, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_gallery.scss</files>
  <action>
    Restyle the LEFT column of `.announcement--single` to `apps/design/index.dc.html` lines ~800-853. Edit ONLY the `.announcement--single` section of `_announcement.scss` (lines ~65-224) and `_gallery.scss`.
    - GALLERY (`.announcement--single__body__gallery` / GalleryDefault): main image in a 16/10 framed box (1px $line border, 4px radius, $cream bg) with the photos-count badge bottom-left and the condition chip top-right per mockup; thumbnails row below. Keep GalleryDefault's existing lightbox behavior — restyle only.
    - ACERCA (`__body__description`): h2 Poppins 800 $ink 24px "Acerca de este producto" + body $ink2 1.65 line-height, max-width ~640px. Keep the existing `v-html="sanitizeRich(...)"`.
    - UBICACIÓN + ESPECIFICACIÓN (`__body__specs`): section title Poppins 800 24px + a 2-col label/value grid (label 12px $muted 600 uppercase-ish; value 15.5px $ink 700; "No especificado" italic $muted when empty). These currently render via `<CardInfo>` rows — restyle the spec table to the mockup's 2-col grid. Keep the existing CardInfo usage and the Dirección → Google Maps link; if the mockup's flat label/value grid is cleaner than CardInfo here, prefer adjusting the `__specs__table` SCSS over rewriting all CardInfo usages (restyle-first, minimal markup change).
    Tokens only; BEM strict (children under `.announcement--single__body__*`); no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AdSingle|GalleryDefault" || echo "no body type errors"</automated>
    VISUAL LOOP: screenshot the ad detail URL at 1440 and 390 → save /tmp/waldo-shots/08-02-body-{desktop,mobile}.png → Read → compare gallery frame + badges, Acerca, Ubicación grid, Especificación grid vs the AUTHORITATIVE DC source apps/design/index.dc.html "Detalle de producto" left column (lines ~800-853) → fix → repeat until match. (No ad-detail rendered PNG exists — the article-*.png are blog screenshots; do NOT use them.)
  </verify>
  <done>Detail left column (gallery, Acerca, Ubicación, Especificación) matches the DC source "Detalle de producto" left column (apps/design/index.dc.html ~800-853) on desktop+mobile; gallery lightbox behavior unchanged; only the --single section + gallery SCSS touched; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the sticky sidebar + wire masked contact eye-reveal + WhatsApp/Call via the 08-04 reveal endpoints</name>
  <files>apps/website/app/components/AdSingle.vue, apps/website/app/components/ReminderDefault.vue, apps/website/app/components/ShareDefault.vue, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_reminder.scss, apps/website/app/scss/components/_share.scss</files>
  <action>
    Restyle the sticky RIGHT column of `.announcement--single__sidebar` to `apps/design/index.dc.html` lines ~857-951, AND wire the obfuscated-contact + per-channel reveal model against the 08-04 endpoints. Sidebar = sticky (top ~94px), column gap ~16px, three cards (#fff, 1px $line, 4px radius).
    - PRECIO card (`__sidebar__info__top`): "PRECIO" eyebrow ($muted, uppercase 12px) + Poppins 800 $ink 30px price + "+ IVA" $muted; keep the existing converted-price + tooltip block, restyled.
    - VENDEDOR / CONTACTO card (`__sidebar__info`): PRESERVE the gate (logged-IN `getUserFromAll && isLoggedIn` vs logged-OUT `v-else`).
      • logged-IN: seller row (amber initials avatar, name, "Ver perfil y todos sus avisos" link → /{username}), then PRO/Verificado chips per mockup (use existing user fields: pro_status, and a verified flag if present — omit Verificado if no data, render "Sin verificar" outline pill otherwise).
        OBFUSCATED EMAIL / PHONE ROWS + EYE-REVEAL (per the 08-04 model — NOT display-side masking):
          - Render the Email row only when `getUserFromAll.has_email`, the Phone row only when `getUserFromAll.has_phone`.
          - Each row shows the MASKED value straight from the payload (`getUserFromAll.email` / `getUserFromAll.phone` are already obfuscated by 08-04). Add local refs per channel: `emailRevealed`/`phoneRevealed` (false), `emailReal`/`phoneReal` (""), `emailLoading`/`phoneLoading` (false), `emailCopied`/`phoneCopied` (false).
          - The eye/copy button (36px square, $line border; icon states: eye → spinner while loading → check briefly after copy → copy icon) on @click MUST, for that channel: set loading true; call `apiClient(\`ads/\${documentId}/reveal/\${channel}\`)` (channel = "email"|"phone"); on success store `.value` into the real ref, set revealed=true, `navigator.clipboard.writeText(realValue)`, show the check for ~1.5s; on error toast/swallow; finally loading=false. The endpoint records the ad-contact server-side — do NOT also POST ads/:documentId/contact. Once revealed, the row shows the REAL value (mockup `dEmailRevealed`/`dPhoneRevealed` look).
          - Keep the mockup helper line "Datos protegidos contra robots · se revelan al copiarlos o al contactar." under the phone row ($muted, lock icon).
        WHATSAPP + CALL buttons (mockup lines ~924-925), INSIDE the logged-IN block only:
          - WhatsApp: amber full-width "Escribir por WhatsApp" (render only when `getUserFromAll.has_whatsapp`). On click: fetch `ads/${documentId}/reveal/whatsapp`, strip the returned value to digits, then `window.open(\`https://wa.me/\${digits}\`, "_blank", "noopener,noreferrer")`. Show the button's loading spinner while fetching (mockup `dWaLoading`).
          - Call: outline full-width "Llamar ahora" (render only when `getUserFromAll.has_phone`). On click: fetch `ads/${documentId}/reveal/phone`, then `window.location.href = \`tel:\${value}\``. If the phone was already revealed (phoneReal set), skip the refetch and use it.
          - On every reveal/WhatsApp/Call click, STILL call the existing `contactSeller(type)` (GA4 analytics — client-side, KEEP the useAdAnalytics import). The reveal endpoint records the ad-contact server-side, so DROP only the duplicate `apiClient(... /contact, POST)` call from the old handleContact (no double-count). Use the established apiClient (useApiClient) pattern already in AdSingle. Remove the old handleContact wrapper ONLY after re-wiring contactSeller into the new click handlers; do NOT remove contactSeller itself (CLAUDE.md: no unused vars, but contactSeller stays USED).
      • logged-OUT (`v-else`): restyle ReminderDefault to the mockup "Datos de contacto protegidos" gate (centered lock icon in a $cream circle, Poppins title, $ink2 copy, amber "Iniciar sesión" CTA → /login, outline "Crear cuenta gratis" CTA → /registro). The mockup (DC line ~935) ALSO shows an optional verified line under the CTAs — "Anunciante verificado por Waldo" ($green check, 12.5px) — render it only when the seller has a verified flag, omit otherwise (no new fetch). Pure restyle — same links/behavior; NO contact data, NO masked values, NO reveal here.
    - COMPARTIR card (`__sidebar__share` → ShareDefault): restyle ShareDefault to the mockup compartir card (label + FB/X/WhatsApp icon buttons + copy-link button on $cream tiles). Keep existing share behavior.
    Tokens only; BEM strict (children under `.announcement--single__sidebar__*`, `.reminder--default__*`, `.share--default__*`); no existing-variable edits; no box-shadow/scale; lucide icons imported without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AdSingle|ReminderDefault|ShareDefault" || echo "no sidebar type errors"</automated>
    grep -E "reveal/" apps/website/app/components/AdSingle.vue  →  MUST match (per-channel reveal calls present)
    grep -c "reveal/" apps/website/app/components/AdSingle.vue  →  MUST be ≥ 3 (email + phone + whatsapp reveals; Call may reuse phone)
    grep -E "ads/.*?/contact\b" apps/website/app/components/AdSingle.vue  →  MUST NOT match (the old POST contact path is removed — reveal records server-side)
    grep -c "contactSeller" apps/website/app/components/AdSingle.vue  →  MUST be ≥ 2 (GA4 contactSeller still called on reveal/WhatsApp/Call — only the duplicate POST was dropped, GA4 must NOT regress)
    VISUAL LOOP — BOTH AUTH STATES:
      (a) logged-OUT: screenshot ad detail at 1440 and 390 → /tmp/waldo-shots/08-02-side-out-{desktop,mobile}.png → Read → compare precio card + "Datos de contacto protegidos" gate + compartir vs the DC source apps/design/index.dc.html sidebar (precio ~857-866, gate ~928-936, compartir ~939-951) → confirm NO email/phone/WhatsApp/Call and NO masked values shown logged-OUT → fix → repeat.
      (b) logged-IN: inject the waldo_jwt cookie (token in plan-context) in Playwright, reload, screenshot the MASKED initial state → /tmp/waldo-shots/08-02-side-in-{desktop,mobile}.png → Read → compare seller row + MASKED email/phone (bullets) + eye buttons + WhatsApp (amber) + Call (outline) vs the DC source apps/design/index.dc.html logged-IN contact card (~870-926) → then CLICK the email eye button, wait for the reveal, screenshot the REVEALED state → confirm the masked value is replaced by the real email and the check icon shows → fix → repeat until both states match.
  </verify>
  <done>Sticky sidebar matches the mockup in BOTH auth states; logged-IN email/phone render MASKED (payload values) with a per-channel eye-reveal that fetches the real value from the matching SEPARATE endpoint GET /ads/:documentId/reveal/email|phone (08-04) and copies it; WhatsApp + Call reveal-then-open via GET /ads/:documentId/reveal/whatsapp|phone (inside logged-IN only); GA4 contactSeller(type) still fires on every reveal/WhatsApp/Call click (no analytics regression) while the duplicate POST is dropped (reveal records server-side); logged-OUT shows the gate with NO contact data; tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Ad detail screenshot-Read-compare passes at 1440 and 390, logged-OUT AND logged-IN (including the eye-reveal interaction), vs apps/design/index.dc.html "Detalle de producto" DC source (lines 767–966 — the authoritative reference; there is no ad-detail rendered PNG, the article-*.png are blog screenshots).
- Logged-OUT gate ("Datos de contacto protegidos") shows NO email/phone/WhatsApp/Call and NO masked values; logged-IN shows masked email/phone that reveal the REAL value on the eye click via the per-channel 08-04 endpoint.
- Reveal/WhatsApp/Call route through GET /ads/:documentId/reveal/:channel (08-04) which records the ad-contact server-side — the old client POST ads/:documentId/contact is removed (no double-count, no parallel path).
- The real email/phone/whatsapp are NOT in the ad payload — they arrive only on the user's click; WhatsApp deep-link is wa.me/<digits>, Call is tel:<phone>; rows/buttons render only when has_email/has_phone/has_whatsapp.
- [slug].vue keeps its single useAsyncData "ad-${slug}" (ads/slug/:slug embeds everything, seller contact obfuscated); related load stays active-only; reveals are on-demand single-channel calls (no N+1 on load).
- Related ads render the 08-01 CardAnnouncement (not re-styled here).
- Only the `.announcement--single` section of _announcement.scss is modified (the `--archive` section + _card.scss .card--announcement belong to 08-01).
- Hero clears the 06-02 headroom header; `git diff` of _variables.scss shows NO existing-variable change (additions only); no unused imports.
- vue-tsc --noEmit exits 0; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server; one heavy op at a time.
</verification>

<success_criteria>
The ad interior looks AND behaves like the "Detalle de producto" mockup on desktop + mobile in both auth states: restyled hero/body/sidebar, the logged-IN contact block with email/phone OBFUSCATED in the payload and revealed per-channel on click via the 08-04 reveal endpoints (which record the contact server-side), plus WhatsApp (wa.me) and Call (tel:) buttons that reveal-then-open. The real contact never ships in the payload. Reuses the 08-01 card for related ads, uses only phase-04/new tokens, and modifies no existing SCSS variable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-publico-anuncios-perfil/08-02-SUMMARY.md`.
</output>
