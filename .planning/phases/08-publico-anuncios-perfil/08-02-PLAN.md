---
phase: 08-publico-anuncios-perfil
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/website/app/components/AdSingle.vue
  - apps/website/app/components/HeroAnnouncement.vue
  - apps/website/app/components/GalleryDefault.vue
  - apps/website/app/components/ReminderDefault.vue
  - apps/website/app/components/ShareDefault.vue
  - apps/website/app/scss/components/_announcement.scss
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_gallery.scss
  - apps/website/app/scss/components/_reminder.scss
  - apps/website/app/scss/components/_share.scss
autonomous: true
requirements: [PUB-AD-DETAIL, PUB-AD-CONTACT]
must_haves:
  truths:
    - "The ad interior (/anuncios/[slug]) renders to the 'Detalle de producto' mockup: breadcrumb hero with title + meta, left column (gallery + Acerca + Ubicación + Especificación técnica), sticky right column (precio card + vendedor/contacto card + compartir card), logged-OUT and logged-IN, on desktop 1440 and mobile 390"
    - "Logged-OUT shows the 'Datos de contacto protegidos' gate card (lock icon, iniciar sesión / crear cuenta CTAs); logged-IN shows the seller card with email + phone MASKED by default, revealed (and copied) on click of the copy button, plus WhatsApp and Call deep-link buttons"
    - "Email and phone are masked on first render logged-IN and revealed on the SAME click that copies + records the ad-contact event (06-04 handleContact) — masking is display-side only, the data is already loaded via ads/slug/:slug and the existing handleContact POST is preserved"
    - "The WhatsApp button opens wa.me/<seller-digits> and the Call button opens tel:<seller-phone>, both built from the seller phone already in the payload; both live INSIDE the logged-IN block (never shown logged-OUT) and fire handleContact('phone') so the contact is recorded"
    - "The ad detail loads with a single ad request (ads/slug/:slug embeds gallery/seller/category/commune/condition) plus the existing related-ads load for active ads only; no per-spec or per-field request, NO new endpoint for reveal/masking (no N+1)"
    - "Related ads reuse the CardAnnouncement restyled in 08-01; colors/typography use phase-04 tokens; no existing SCSS variable is modified; hero clears the headroom header"
  artifacts:
    - path: "apps/website/app/components/AdSingle.vue"
      provides: "Ad detail body + sticky sidebar (precio / vendedor-contacto with masking+reveal+WhatsApp+Call / compartir) restyled to mockup"
    - path: "apps/website/app/scss/components/_announcement.scss"
      provides: "Restyled .announcement--single section ONLY (08-01 owns --archive — do not collide)"
    - path: "apps/website/app/components/HeroAnnouncement.vue"
      provides: "Restyled detail hero (breadcrumb + title + meta + volver)"
  key_links:
    - from: "apps/website/app/pages/anuncios/[slug].vue"
      to: "apps/website/app/components/AdSingle.vue"
      via: ":all=ad :access=access (single useAsyncData ad-${slug})"
      pattern: "<AdSingle"
    - from: "apps/website/app/components/AdSingle.vue"
      to: "logged-in gate"
      via: "isLoggedIn computed off useSessionUser"
      pattern: "isLoggedIn"
    - from: "apps/website/app/components/AdSingle.vue reveal/copy button"
      to: "handleContact ad-contact POST"
      via: "@click reveals masked value AND calls handleContact (records contact)"
      pattern: "handleContact"
    - from: "apps/website/app/components/AdSingle.vue WhatsApp/Call buttons"
      to: "wa.me / tel: deep-links built from seller phone"
      via: ":href computed from getUserFromAll.phone, inside logged-IN block"
      pattern: "wa\\.me|tel:"
---

<objective>
Restyle the public ad interior (`pages/anuncios/[slug].vue` via `HeroAnnouncement` + `AdSingle` + `RelatedAds`) to the v1.47 "Detalle de producto" mockup (`design/index.dc.html` lines ~733-928) at FULL FIDELITY. The contact GATE (logged-OUT vs logged-IN) is preserved, and the logged-IN contact block gains the mockup's net-new behaviors: email/phone MASKING + reveal-on-copy, plus WhatsApp (wa.me) and Call (tel:) deep-link buttons. All of this is display-side over data already loaded — NO new endpoint.

Purpose: the ad interior is the conversion surface (price + contact). It must look AND behave like the mockup (masked-then-revealed contact, one-tap WhatsApp/Call) while keeping its single-load data path and its proven contact gate.
Output: Restyled detail hero, gallery + content (Acerca/Ubicación/Especificación), and the sticky sidebar (precio card; vendedor/contacto card with the logged-OUT gate, the logged-IN masked email/phone + reveal-on-copy + WhatsApp + Call; compartir card) matching the mockup on desktop + mobile, logged-OUT and logged-IN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — "Detalle de producto" screen; translate inline styles to BEM + phase-04 tokens:
@design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/article-container.png
@design/screenshots/article-redesign.png
@design/screenshots/article-sticky-share.png
@design/screenshots/article-sell.png
@design/screenshots/article-buy.png
@design/screenshots/headroom.png

# Current page + components to UPDATE (closest existing equivalents — do NOT create new):
@apps/website/app/pages/anuncios/[slug].vue
@apps/website/app/components/AdSingle.vue
@apps/website/app/components/HeroAnnouncement.vue
@apps/website/app/components/GalleryDefault.vue
@apps/website/app/components/ReminderDefault.vue
@apps/website/app/components/ShareDefault.vue
@apps/website/app/components/CardInfo.vue

<interfaces>
<!-- phase-04 tokens — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DATA PATH (already optimized — keep it):
  [slug].vue → ONE useAsyncData "ad-${slug}" → adsStore.loadAdBySlug → GET ads/slug/:slug
    (returns { data: ad, access } with gallery/user/category/commune/condition embedded — single request, no N+1)
    + indicatorStore.convertCurrency (price in alt currency) + relatedStore.loadRelatedAds(ad.id) ONLY when ad.status === "active".
  Do NOT add extra per-field/per-spec requests. The price-conversion + related calls already exist; keep them.
  CONTACT DATA: getUserFromAll = all.user. sanitizeAdForPublic (Strapi) exposes user.email + user.phone to LOGGED-IN viewers
  and nulls them logged-OUT. So masking/reveal/WhatsApp/Call are PURE FRONTEND over data already present — NO endpoint, NO N+1. -->

<!-- CONTACT GATE (behavioral contract — preserve the gate, ADD the mockup contact UX inside logged-IN):
  AdSingle: isLoggedIn = !!useSessionUser().value
    logged-IN  → seller CardInfo + Email/Phone rows (NOW masked-by-default) + WhatsApp + Call + handleContact() records ad-contact (06-04)
    logged-OUT → <ReminderDefault/> gate ONLY (no contact actions ever shown logged-OUT)
  handleContact('email'|'phone') already fires contactSeller(GA4) + POST ads/:documentId/contact (06-04). Reuse it for reveal-on-copy
  and for the WhatsApp/Call buttons — do NOT add a second/parallel contact path. -->

<!-- SCSS OWNERSHIP: edit ONLY the .announcement--single section of _announcement.scss (lines ~65-224).
  The .announcement--archive section (lines ~5-35) and .card--announcement in _card.scss are owned by 08-01 — DO NOT touch them.
  RelatedAds renders CardAnnouncement (already restyled in 08-01) — do not re-style the card. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the detail hero (HeroAnnouncement) to the mockup breadcrumb header</name>
  <files>apps/website/app/components/HeroAnnouncement.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Translate the "Detalle de producto" hero (`design/index.dc.html` lines ~737-761) to the existing `.hero--announcement` block. Edit ONLY the `&--announcement` block in `_hero.scss`.
    Layout per mockup: a light hero on a subtle category-tinted bg with a bottom $line border; breadcrumb (Waldo › Anuncios › {categoría} › {título}) in $muted with chevron separators and $ink current crumb; Poppins 800 $ink title (clamp ~30-44px, tight letter-spacing); a meta row under the title with a category pill + "Publicado {fecha}" (clock icon) and — IF the page already provides it — a views count. SCOPE GUARD: only render meta the component already receives (name, category, user, dates). Do NOT invent a new views-count fetch — a hero "N vistas" line is an OPEN QUESTION (see PLANNING return); if the current props don't include a view count, omit it rather than add a request.
    Keep the existing props (`name`, `category`, `user`) and the component's current data flow. CRITICAL: carry ~104-130px top padding so the hero clears the fixed/overlay headroom header (06-02) — verify vs design/screenshots/headroom.png.
    Tokens only; BEM strict (children under `.hero--announcement__*`); no existing-variable edits; no box-shadow/scale; lucide icons imported without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroAnnouncement" || echo "no hero type errors"</automated>
    VISUAL LOOP (dev server already on :3000): screenshot a real ad detail URL (find one slug from http://localhost:3000/anuncios) at 1440 and 390 → save /tmp/waldo-shots/08-02-hero-{desktop,mobile}.png → Read → compare breadcrumb + title + meta + headroom clearance vs the mockup + headroom.png → fix → repeat until match.
  </verify>
  <done>Detail hero matches the mockup breadcrumb header, clears the headroom header, Poppins title + meta styled from existing props only (no new fetch); tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the detail body (gallery + Acerca + Ubicación + Especificación) to the mockup left column</name>
  <files>apps/website/app/components/AdSingle.vue, apps/website/app/components/GalleryDefault.vue, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_gallery.scss</files>
  <action>
    Restyle the LEFT column of `.announcement--single` to `design/index.dc.html` lines ~764-820. Edit ONLY the `.announcement--single` section of `_announcement.scss` (lines ~65-224) and `_gallery.scss`.
    - GALLERY (`.announcement--single__body__gallery` / GalleryDefault): main image in a 16/10 framed box (1px $line border, 4px radius, $cream bg) with the photos-count badge bottom-left and the condition chip top-right per mockup; thumbnails row below. Keep GalleryDefault's existing lightbox behavior — restyle only.
    - ACERCA (`__body__description`): h2 Poppins 800 $ink 24px "Acerca de este producto" + body $ink2 1.65 line-height, max-width ~640px. Keep the existing `v-html="sanitizeRich(...)"`.
    - UBICACIÓN + ESPECIFICACIÓN (`__body__specs`): section title Poppins 800 24px + a 2-col label/value grid (label 12px $muted 600 uppercase-ish; value 15.5px $ink 700; "No especificado" italic $muted when empty). These currently render via `<CardInfo>` rows — restyle the spec table to the mockup's 2-col grid. Keep the existing CardInfo usage and the Dirección → Google Maps link; if the mockup's flat label/value grid is cleaner than CardInfo here, prefer adjusting the `__specs__table` SCSS over rewriting all CardInfo usages (restyle-first, minimal markup change).
    Tokens only; BEM strict (children under `.announcement--single__body__*`); no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AdSingle|GalleryDefault" || echo "no body type errors"</automated>
    VISUAL LOOP: screenshot the ad detail URL at 1440 and 390 → save /tmp/waldo-shots/08-02-body-{desktop,mobile}.png → Read → compare gallery frame + badges, Acerca, Ubicación grid, Especificación grid vs design/index.dc.html detail screen + design/screenshots/article-container.png + article-redesign.png → fix → repeat until match.
  </verify>
  <done>Detail left column (gallery, Acerca, Ubicación, Especificación) matches the mockup + article-container.png on desktop+mobile; gallery lightbox behavior unchanged; only the --single section + gallery SCSS touched; tokens only; vue-tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Restyle the sticky sidebar + add masked contact reveal-on-copy + WhatsApp/Call deep-links</name>
  <files>apps/website/app/components/AdSingle.vue, apps/website/app/components/ReminderDefault.vue, apps/website/app/components/ShareDefault.vue, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_reminder.scss, apps/website/app/scss/components/_share.scss</files>
  <action>
    Restyle the sticky RIGHT column of `.announcement--single__sidebar` to `design/index.dc.html` lines ~822-913, AND implement the mockup's net-new contact behaviors (masking/reveal + WhatsApp + Call). Sidebar = sticky (top ~94px), column gap ~16px, three cards (#fff, 1px $line, 4px radius).
    - PRECIO card (`__sidebar__info__top`): "PRECIO" eyebrow ($muted, uppercase 12px) + Poppins 800 $ink 30px price + "+ IVA" $muted; keep the existing converted-price + tooltip block, restyled.
    - VENDEDOR / CONTACTO card (`__sidebar__info`): PRESERVE the gate (logged-IN `getUserFromAll && isLoggedIn` vs logged-OUT `v-else`).
      • logged-IN: seller row (amber initials avatar, name, "Ver perfil y todos sus avisos" link → /{username}).
        NET-NEW — EMAIL/PHONE MASKING + REVEAL-ON-COPY:
          - Email and Phone rows render the value MASKED by default. Add local refs `emailRevealed`/`phoneRevealed` (false initially). Build a masked display computed:
            email → keep first char + domain, e.g. `j••••@gmail.com`; phone → keep last 2-3 digits, e.g. `+56 9 •••• ••12`. Keep it simple (a small inline mask helper in <script setup>; do NOT add a util file unless one already exists).
          - The copy button (36px square, $line border, mockup eye→check→copy icon states) on @click MUST: (1) set the row's revealed ref true, (2) `navigator.clipboard.writeText(realValue)`, (3) call the EXISTING `handleContact('email'|'phone')` so the GA4 + ad-contact POST (06-04) fire exactly as today. Once revealed, show the full value (mockup `dEmailRevealed`/`dPhoneRevealed` style) and a check icon briefly. Reuse the current `handleContact` wiring — do NOT add a parallel contact path or a new endpoint.
          - Keep the mockup's "Datos protegidos contra robots · se revelan al copiarlos o al contactar." helper line under the phone row ($muted, lock icon).
        NET-NEW — WHATSAPP + CALL buttons (mockup lines ~886-887), INSIDE the logged-IN block only (never logged-OUT):
          - WhatsApp: amber full-width button "Escribir por WhatsApp"; `:href` = `https://wa.me/${digits}` where `digits` = `getUserFromAll.phone` stripped to digits (computed `waHref`). On click also call `handleContact('phone')`. Use `<a target="_blank" rel="noopener noreferrer">` styled as the button (or a button that opens the link) — pick the existing app pattern. Do NOT hand-roll +56 country logic; the stored phone is already in international form — just strip non-digits.
          - Call: outline full-width button "Llamar ahora"; `:href` = `tel:${getUserFromAll.phone}` (computed `telHref`); on click also `handleContact('phone')`.
          - Guard both: only render when `getUserFromAll?.phone` exists.
      • logged-OUT (`v-else` → ReminderDefault): restyle ReminderDefault to the mockup "Datos de contacto protegidos" gate (centered lock icon in a $cream circle, Poppins title, $ink2 copy, amber "Iniciar sesión" CTA → /login, outline "Crear cuenta gratis" CTA → /registro). Pure restyle — same links/behavior; NO contact actions here.
    - COMPARTIR card (`__sidebar__share` → ShareDefault): restyle ShareDefault to the mockup compartir card (label + FB/X/WhatsApp icon buttons + copy-link button on $cream tiles). Keep existing share behavior.
    Tokens only; BEM strict (children under `.announcement--single__sidebar__*`, `.reminder--default__*`, `.share--default__*`); no existing-variable edits; no box-shadow/scale; lucide icons imported without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AdSingle|ReminderDefault|ShareDefault" || echo "no sidebar type errors"</automated>
    grep -E "wa\.me|tel:" apps/website/app/components/AdSingle.vue  →  MUST match (WhatsApp + Call deep-links present)
    grep -c "handleContact" apps/website/app/components/AdSingle.vue  →  MUST be ≥ 3 (reveal-copy email + reveal-copy phone + WhatsApp/Call all record contact)
    VISUAL LOOP — BOTH AUTH STATES:
      (a) logged-OUT: screenshot ad detail at 1440 and 390 → /tmp/waldo-shots/08-02-side-out-{desktop,mobile}.png → Read → compare precio card + "Datos de contacto protegidos" gate + compartir vs design/screenshots/article-buy.png + article-sticky-share.png + the mockup detail screen → confirm NO email/phone/WhatsApp/Call shown logged-OUT → fix → repeat.
      (b) logged-IN: inject the waldo_jwt cookie (token in plan-context) in Playwright, reload, screenshot the masked initial state → /tmp/waldo-shots/08-02-side-in-{desktop,mobile}.png → Read → compare seller row + MASKED email/phone + copy buttons + WhatsApp (amber) + Call (outline) vs design/screenshots/article-sell.png + the mockup → then click a copy button, screenshot the REVEALED state → confirm value un-masks and check icon shows → fix → repeat until both states match.
  </verify>
  <done>Sticky sidebar matches the mockup in BOTH auth states; logged-IN email/phone are masked then reveal-on-copy (calling the existing handleContact ad-contact POST), WhatsApp (wa.me) + Call (tel:) deep-links render inside the logged-IN block only and also record contact; logged-OUT shows the gate with NO contact data; no new endpoint added; tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Ad detail screenshot-Read-compare passes at 1440 and 390, logged-OUT AND logged-IN, vs design/index.dc.html "Detalle de producto" + article-*.png references.
- Logged-OUT gate ("Datos de contacto protegidos") shows NO email/phone/WhatsApp/Call; logged-IN shows masked email/phone that reveal-on-copy + WhatsApp + Call.
- Reveal/copy and WhatsApp/Call all route through the existing handleContact (GA4 + ad-contact POST 06-04) — no parallel contact path, no new endpoint.
- WhatsApp deep-link is wa.me/<digits> (non-digits stripped from seller phone), Call is tel:<phone>; both only render when seller phone exists.
- [slug].vue keeps its single useAsyncData "ad-${slug}" (ads/slug/:slug embeds everything); related load stays active-only; no per-field/per-spec N+1; no extra requests added for masking/reveal.
- Related ads render the 08-01 CardAnnouncement (not re-styled here).
- Only the `.announcement--single` section of _announcement.scss is modified (the `--archive` section + _card.scss .card--announcement belong to 08-01).
- Hero clears the 06-02 headroom header.
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only).
- vue-tsc --noEmit exits 0; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server; one heavy op at a time.
</verification>

<success_criteria>
The ad interior looks AND behaves like the "Detalle de producto" mockup on desktop + mobile in both auth states: restyled hero/body/sidebar, the logged-IN contact block with masked email/phone that reveal-on-copy plus WhatsApp (wa.me) and Call (tel:) deep-links — all over data already loaded (no new endpoint, no N+1), reusing the existing handleContact ad-contact recording. Reuses the 08-01 card for related ads, uses only phase-04/new tokens, and modifies no existing SCSS variable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-publico-anuncios-perfil/08-02-SUMMARY.md`.
</output>
