---
phase: 08-publico-anuncios-perfil
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/CardAnnouncement.vue
  - apps/website/app/components/AdArchive.vue
  - apps/website/app/components/FilterResults.vue
  - apps/website/app/components/HeroResults.vue
  - apps/website/app/scss/components/_card.scss
  - apps/website/app/scss/components/_announcement.scss
  - apps/website/app/scss/components/_filter.scss
  - apps/website/app/scss/components/_hero.scss
autonomous: true
requirements: [PUB-ADS]
must_haves:
  truths:
    - "The /anuncios listing renders to the mockup look (toolbar with result count + sort, responsive card grid, sin-resultados empty state) logged-OUT on desktop 1440 and mobile 390"
    - "The shared ad card (CardAnnouncement) matches AdCard.dc.html: 4/3 image, category pill with color dot, image-count badge, Destacado badge, Poppins title, price + IVA, and the logged-OUT 'Inicia sesión para ver al anunciante' footer with lock icon"
    - "CardAnnouncement supports a sold VARIANT (sold + sold-when props per AdCard.dc.html sold state): 'Vendido' badge top-left ($ink bg, check icon), title in $ink2, price strikethrough $muted, 'Vendido {soldWhen}' footer, and NO meta/Destacado/seller/login-prompt — this variant is consumed by 08-03's Vendidos tab and must be FINAL here"
    - "The listing loads its data without per-card requests: a single paginated ads/catalog call returns the page (no N+1); the category lookup stays a separate stable-key load on purpose (it must not refetch on page/order/commune change)"
    - "Colors/typography use phase-04 tokens ($ink, $ink2, $muted, $amber, $cream, $line, $tag); no existing SCSS variable is modified; hero clears the fixed/overlay headroom header"
  artifacts:
    - path: "apps/website/app/components/CardAnnouncement.vue"
      provides: "Shared ad card restyled to AdCard.dc.html INCLUDING the sold variant (sold + sold-when props) — OWNER for the whole phase (08-02/08-03 reuse, must not re-touch)"
    - path: "apps/website/app/scss/components/_card.scss"
      provides: "Restyled .card--announcement block + its sold modifier/state (OWNER for the phase)"
    - path: "apps/website/app/scss/components/_announcement.scss"
      provides: "Restyled .announcement--archive grid only (08-02 owns the --single section, do not collide)"
  key_links:
    - from: "apps/website/app/components/AdArchive.vue"
      to: "apps/website/app/components/CardAnnouncement.vue"
      via: "v-for over ads, one card per ad (no fetch in card)"
      pattern: "<CardAnnouncement"
    - from: "apps/website/app/components/CardAnnouncement.vue SCSS"
      to: "phase-04 tokens"
      via: "@use abstracts/variables"
      pattern: "\\$amber|\\$ink|\\$line"
---

<objective>
Restyle the public ad listing (`pages/anuncios/index.vue` via its section components) to the v1.47 mockup: the shared ad card (`AdCard.dc.html`), the results toolbar/grid, and the results hero. This plan OWNS the shared `CardAnnouncement` restyle + its `.card--announcement` SCSS for the entire phase — 08-02 (ad detail relacionados) and 08-03 (seller profile grid + Vendidos tab) reuse the same card and must NOT re-touch it. Because 08-03's Vendidos tab is now approved, this plan ALSO finalizes the card's sold variant here (so 08-03 only passes props, never edits the card).

Purpose: /anuncios is the primary discovery surface and the card is reused by all three Fase-2 views (active + sold), so it is the wave backbone — restyle it once here, sold variant included.
Output: Restyled listing (hero + toolbar + card grid + empty state) matching `design/index.dc.html` "lista de anuncios" screen and `design/screenshots/anuncios-grid.png`, on desktop + mobile, logged-OUT, with the existing single-load data path preserved; plus the card's sold variant ready for 08-03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — read the relevant screens, translate inline styles to BEM + phase-04 tokens (do NOT copy inline CSS):
@design/AdCard.dc.html
@design/index.dc.html
# Rendered references — Read these PNGs and compare element-by-element:
@design/screenshots/anuncios-grid.png
@design/screenshots/01-all-cards.png
@design/screenshots/pager.png
@design/screenshots/headroom.png

# Current page + components to UPDATE (closest existing equivalents — do NOT create new):
@apps/website/app/pages/anuncios/index.vue
@apps/website/app/components/CardAnnouncement.vue
@apps/website/app/components/AdArchive.vue
@apps/website/app/components/FilterResults.vue
@apps/website/app/components/HeroResults.vue

<interfaces>
<!-- phase-04 tokens already in apps/website/app/scss/abstracts/_variables.scss — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- SOLD VARIANT (from design/AdCard.dc.html sold state — read that screen):
  sold badge: top-left, bg $ink (#26252B), #fff text, check icon, "Vendido"
  title: $ink2 (#56535F) instead of $ink
  price: $muted (#8A8794), text-decoration line-through
  footer: when soldWhen present → "Vendido {soldWhen}" row, top border 1px $line, $muted 12.5px
  when sold: NO meta line, NO Destacado badge, NO seller row, NO login prompt
  Add `sold` (boolean) + `soldWhen`/`sold-when` (string) props to CardAnnouncement; default false/"" so active cards are unaffected. -->

<!-- DATA PATH (already optimized — do NOT introduce an N+1 nor merge the two loads):
  index.vue fires:
    (a) useAsyncData "category-${slug}"  → categoriesStore.loadCategory (ONLY when ?category= is present; key stable per slug)
    (b) useAsyncData "adsData-..."        → adsStore.loadAds → GET ads/catalog (single paginated call, populate:"*", pageSize 20)
  loadAds returns the full page array in one request; CardAnnouncement renders from that array and fires NO request of its own (no N+1).
  Keep (a) and (b) SEPARATE: (a) is intentionally keyed by slug so it does NOT refetch on page/order/commune/search change. Merging them would regress to a category refetch on every filter change. -->

<!-- SCSS OWNERSHIP for this phase:
  .card--announcement  → _card.scss (line ~612)         ← THIS PLAN owns it (08-02/08-03 reuse) — INCLUDING the sold state
  .announcement--archive → _announcement.scss (lines ~5-35)  ← THIS PLAN owns it
  .announcement--single  → _announcement.scss (lines ~65-224) ← 08-02 owns it (do NOT touch here)
  CardAnnouncement.vue currently has NO dedicated .card--announcement rules in _card.scss beyond a small block — read _card.scss ~612 before editing. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the shared ad card (CardAnnouncement) to AdCard.dc.html + sold variant — PHASE OWNER</name>
  <files>apps/website/app/components/CardAnnouncement.vue, apps/website/app/scss/components/_card.scss</files>
  <action>
    Translate `design/AdCard.dc.html` (both the default AND the sold state) to the existing `.card--announcement` BEM block. Update markup in CardAnnouncement.vue ONLY where structure requires (e.g. category pill needs a color-dot span; logged-OUT footer needs a lock icon; sold variant needs a "Vendido" badge + sold footer); push all visual rules to `_card.scss` under `.card &--announcement`.
    Card anatomy per the mockup (DEFAULT/active):
    - Outer: #fff, 1px $line border, 4px radius, overflow hidden, Poppins. CLAUDE.md: NO box-shadow / NO scale transform on hover — keep transitions simple (the mockup's translateY/shadow hover is BANNED; use a simple border/background change or none).
    - Image: aspect-ratio 4/3, $cream placeholder bg; keep the existing NuxtImg + NuxtLink to `/anuncios/${all.slug}`. Keep the existing image-count badge (top-left, IconImage + count) and Destacado badge (top-right, amber bg #f7c97e, $ink text, star icon) — restyle to the mockup pill look.
    - Body (padding ~14px 15px 16px, column gap 6px): category pill = uppercase 11px, $tag-ish color on a tint bg with a 7px color dot (reuse the existing `--announcement-category-color` custom property the component already sets onMounted); title h3 Poppins 600 ~16.5px $ink; price row = Poppins 700 $ink + "+ IVA" in $muted.
    - Seller/login footer (border-top 1px $line, margin-top): logged-OUT → "Inicia sesión para ver al anunciante" with a lock icon (lucide), $muted; logged-IN + PRO seller → "Por: {firstname}" link as today. Preserve the EXACT current conditional logic (the `user`, `route.params.slug !== getUser.username`, `pro_status === 'active'` guards) — this is a restyle, not a behavior change.
    SOLD VARIANT (NET-NEW — required by 08-03 Vendidos tab, owned here): add `sold` (Boolean, default false) + `sold-when`/`soldWhen` (String, default "") props. When `sold`:
    - render a "Vendido" badge top-left over the image ($ink #26252B bg, #fff text, check icon, lucide, ~11.5px 700) — per AdCard.dc.html line ~19;
    - title color → $ink2; price → $muted with `text-decoration: line-through`;
    - HIDE the meta line, the Destacado badge, the seller row AND the login prompt (mockup: `notSold` / `!sold` guards — line ~31/35/39/75-76);
    - when `soldWhen` present, render a "Vendido {soldWhen}" footer (top border 1px $line, $muted 12.5px) — line ~39.
    Drive these via a `.card--announcement--sold` modifier (or `is-sold` state class) under the SAME block; BEM strict, no standalone hyphenated classes. Active cards (sold=false) must be byte-for-byte unchanged in behavior.
    Tokens only. Import lucide icons WITHOUT alias on the import line so grep detects literals. Do NOT modify existing SCSS variables; if a mockup color is absent from the palette, add a NEW variable in _variables.scss.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "CardAnnouncement" || echo "no CardAnnouncement type errors"</automated>
    grep -E "sold|soldWhen|Vendido" apps/website/app/components/CardAnnouncement.vue  →  MUST match (sold prop + variant present)
    VISUAL LOOP (logged-OUT, dev server already on :3000 — do NOT start another): Playwright (recipe in plan-context) screenshot http://localhost:3000/anuncios at 1440x900 and 390x844 → save /tmp/waldo-shots/08-01-card-{desktop,mobile}.png → Read each PNG → compare a card element-by-element against design/AdCard.dc.html AND design/screenshots/01-all-cards.png (pill, dot, badges, title, price, lock footer) → fix SCSS → re-screenshot → repeat until match. The sold variant has no live route on /anuncios — verify its rendering in 08-03's Vendidos screenshots; here ensure the props exist + active cards are unregressed.
  </verify>
  <done>CardAnnouncement matches AdCard.dc.html + 01-all-cards.png logged-OUT on desktop+mobile (active state) AND exposes a working sold variant (sold + sold-when props → Vendido badge, $ink2 title, strikethrough price, sold footer, hidden meta/seller/login) per the AdCard sold state; existing seller/login conditional logic unchanged for active cards; no box-shadow/scale added; tokens only; vue-tsc clean. This card (active + sold) is now FINAL for the phase (owner).</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the results toolbar + grid (AdArchive) to the mockup</name>
  <files>apps/website/app/components/AdArchive.vue, apps/website/app/components/FilterResults.vue, apps/website/app/scss/components/_announcement.scss, apps/website/app/scss/components/_filter.scss</files>
  <action>
    Translate the listing toolbar + grid from `design/index.dc.html` (lines ~700-728) to the existing `.announcement--archive` and `.filter--announcement` blocks. Edit ONLY the `.announcement--archive` section of `_announcement.scss` (lines ~5-35) — do NOT touch the `.announcement--single` section below it (owned by 08-02).
    - GRID: `.announcement--archive__list` → responsive grid matching the mockup (auto-fill minmax ~224px on desktop, collapsing to 2 then 1 col per the existing breakpoint mixins, gap ~22px). Keep the existing `v-for` over `ads` rendering one `<CardAnnouncement>` each (no fetch in the loop).
    - TOOLBAR: the mockup shows a top toolbar = "Filtros" button (outline, $line border, filter icon) + "{N} anuncios" count ($ink2, bold $ink number) on the left, and an "Ordenar por" select (outline, chevron) on the right. Map this onto the existing FilterResults.vue (which already renders filter selectors + order). Restyle FilterResults to the mockup toolbar look; do NOT change its filtering BEHAVIOR (same query params: category/commune/order/s). If a control in the mockup has no current equivalent (e.g. a dedicated "Filtros" toggle button), prefer restyling what exists over adding new behavior — restyle-first.
    - EMPTY STATE: the page already renders MessageDefault when ads.length === 0; restyle it (or its container) toward the mockup "Sin resultados" card ($cream bg, dashed $line border, search icon, "Limpiar filtros" amber button) without changing the page's empty-state logic. Keep MessageDefault's existing button → /anuncios.
    Tokens only; BEM strict (children under `.announcement--archive__*` / `.filter--announcement__*`); no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "AdArchive|FilterResults" || echo "no toolbar/grid type errors"</automated>
    VISUAL LOOP (logged-OUT): screenshot http://localhost:3000/anuncios full-page at 1440 and 390 → save /tmp/waldo-shots/08-01-grid-{desktop,mobile}.png → Read → compare toolbar (count + sort), grid columns/gap, and empty state vs design/index.dc.html listing screen + design/screenshots/anuncios-grid.png + pager.png → fix → repeat until match. Confirm pagination still renders (vue-awesome-paginate) and the grid is 3-4 cols desktop / 1 col mobile.
  </verify>
  <done>Listing toolbar, card grid, empty state and pager match the mockup + anuncios-grid.png on desktop+mobile, logged-OUT; filtering behavior (query params) unchanged; only the --archive section of _announcement.scss touched; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the results hero (HeroResults) to the mockup + verify data path unchanged</name>
  <files>apps/website/app/components/HeroResults.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Restyle the `.hero--results` block (category/search results header) to the v1.47 look used by the listing screen in `design/index.dc.html`: breadcrumb (Waldo › Anuncios › {categoría}) in $muted with chevron separators, Poppins 800 $ink title, and the category color accent where the mockup uses it. CRITICAL: carry ~130px top padding so the hero clears the fixed/overlay headroom header (06-02) — verify against design/screenshots/headroom.png that the breadcrumb is not hidden behind the bar. Edit ONLY the `.hero--results` block in `_hero.scss` (it is ~492 lines with many hero variants — scope your changes to `&--results`).
    DATA PATH (verify, do NOT change): confirm index.vue still uses the TWO separate useAsyncData (category-${slug} + adsData-...) and that loadAds remains a single ads/catalog call. Do NOT merge them and do NOT add a populate change — the split is intentional (category must not refetch on filter change) and populate:"*" is the established read shape. Add a one-line code comment in index.vue ONLY if one is missing explaining why the two loads stay separate (an explanatory comment already exists for the removed watch — match its style).
    Tokens only; BEM strict; no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroResults|anuncios/index" || echo "no hero/index type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/anuncios/index.vue  →  MUST equal 2 (unchanged — the split is intentional)
    VISUAL LOOP (logged-OUT): screenshot http://localhost:3000/anuncios?category=mineria (a real category slug) at 1440 and 390 → save /tmp/waldo-shots/08-01-hero-{desktop,mobile}.png → Read → compare breadcrumb + title + headroom clearance vs the mockup + headroom.png → fix → repeat until match.
  </verify>
  <done>Results hero matches the mockup, clears the headroom header (no overlap), breadcrumb + Poppins title styled; index.vue still has exactly 2 useAsyncData (data path unchanged, no N+1, no populate change); tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- CardAnnouncement matches AdCard.dc.html (active AND sold variants — this card is the phase owner; 08-02/08-03 must not re-touch it or _card.scss .card--announcement).
- The sold variant (sold + sold-when props) renders per the AdCard sold state and active cards are unregressed.
- Listing screenshot-Read-compare passes at 1440 and 390, logged-OUT, vs design/index.dc.html listing screen + anuncios-grid.png/01-all-cards.png/pager.png.
- index.vue retains its TWO intentional useAsyncData (category + ads); loadAds stays a single ads/catalog paginated call; no per-card request (no N+1); no populate narrowing.
- Only the `.announcement--archive` section of _announcement.scss is modified (the `--single` section is reserved for 08-02).
- Hero clears the 06-02 headroom header (no overlap).
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (only additions, if any).
- vue-tsc --noEmit exits 0 for all touched components.
- One heavy op at a time; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server.
</verification>

<success_criteria>
The /anuncios listing (shared card, toolbar, grid, empty state, hero) looks like the mockup on desktop + mobile logged-OUT, keeps its existing single-load data path (no N+1, no regression to the category split), uses only phase-04/new tokens, modifies no existing SCSS variable, and the CardAnnouncement restyle — active state AND the sold variant consumed by 08-03 — is finalized for downstream plans.
</success_criteria>

<output>
After completion, create `.planning/phases/08-publico-anuncios-perfil/08-01-SUMMARY.md`.
</output>
