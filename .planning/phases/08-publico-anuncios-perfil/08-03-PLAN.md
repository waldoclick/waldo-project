---
phase: 08-publico-anuncios-perfil
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/website/app/components/HeroProfile.vue
  - apps/website/app/components/ProfileDefault.vue
  - apps/website/app/components/SidebarProfile.vue
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_profile.scss
  - apps/website/app/scss/components/_sidebar.scss
autonomous: true
requirements: [PUB-PROFILE]
must_haves:
  truths:
    - "The public seller profile (/{username}) renders to the 'Perfil del anunciante' mockup: contained cover + circular avatar, identity header (name + PRO badge + verified/sin-verificar chip), meta row (@username · ubicación · miembro desde · tipo), metrics strip, category chips, and the active-ads grid — logged-OUT and logged-IN, desktop 1440 and mobile 390"
    - "BOTH variants render from one page: PRO sellers (pro_status === 'active') show the cover + avatar + bio; básica (standard) sellers show the contained header without cover personalization and the 'Perfil estándar' note — driven by pro_status, no separate route"
    - "The profile loads with a single ads request (adsStore.loadAds for the user's active ads) plus the user load; cards render from that array (no per-card request, no N+1); the existing active-only query is preserved"
    - "The ad grid reuses the CardAnnouncement restyled in 08-01; colors/typography use phase-04 tokens; no existing SCSS variable is modified; header clears the headroom"
  artifacts:
    - path: "apps/website/app/components/HeroProfile.vue"
      provides: "Restyled contained cover + circular avatar (PRO) / plain header (básica) to mockup"
    - path: "apps/website/app/components/ProfileDefault.vue"
      provides: "Restyled identity header + metrics + chips + ad grid to mockup"
    - path: "apps/website/app/components/SidebarProfile.vue"
      provides: "Restyled seller identity/contact block to mockup"
  key_links:
    - from: "apps/website/app/pages/[slug].vue"
      to: "apps/website/app/components/ProfileDefault.vue"
      via: ":user :ads :pagination (single useAsyncData adsData-${slug})"
      pattern: "<ProfileDefault"
    - from: "apps/website/app/components/ProfileDefault.vue"
      to: "apps/website/app/components/CardAnnouncement.vue"
      via: "v-for over ads (no fetch in card)"
      pattern: "<CardAnnouncement"
---

<objective>
Restyle the public seller profile (`pages/[slug].vue` via `HeroProfile` + `ProfileDefault` + `SidebarProfile`) to the v1.47 "Perfil del anunciante" mockup (`design/index.dc.html` lines ~933-1062). ONE plan covers BOTH variants — the page renders the PRO layout (cover + avatar + bio) vs the básica layout (contained header + "Perfil estándar" note) based on the seller's `pro_status`, with no separate route.

Purpose: the seller profile is the trust/credibility surface and the PRO upsell payoff. It must look like the mockup for both PRO and standard sellers while keeping its single-load data path.
Output: Restyled cover/identity header (PRO and básica), metrics strip, category chips, contact identity, and the active-ads grid matching the mockup on desktop + mobile, logged-OUT and logged-IN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — "Perfil del anunciante" screen; translate inline styles to BEM + phase-04 tokens:
@design/index.dc.html
# Rendered references — Read these PNGs. BÁSICA vs PRO variants:
@design/screenshots/profile-basic.png
@design/screenshots/profile-basic-contained.png
@design/screenshots/profile-pro.png
@design/screenshots/profile-pro2.png
@design/screenshots/p-pro-final.png
@design/screenshots/01-p-pro-inline.png
@design/screenshots/profile-contained.png
@design/screenshots/profile-gate.png
@design/screenshots/headroom.png

# Current page + components to UPDATE (closest existing equivalents — do NOT create new):
@apps/website/app/pages/[slug].vue
@apps/website/app/components/HeroProfile.vue
@apps/website/app/components/ProfileDefault.vue
@apps/website/app/components/SidebarProfile.vue

<interfaces>
<!-- phase-04 tokens — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DATA PATH (already single-load — keep it):
  [slug].vue → ONE useAsyncData "adsData-${slug}":
    userStore.loadUser(username) (populate:"*" → user + avatar/cover/commune)
    + adsStore.loadAds({ active:{$eq:true}, remaining_days:{$gt:0}, user:{username} }, {pageSize:12}, ["createdAt:desc"])
  Returns { user, ads, pagination } — one user request + one paginated ads request; cards render from the array (no N+1).
  Do NOT add per-card or per-metric requests. -->

<!-- VARIANT SWITCH:
  PRO    = user.pro_status === 'active'  → cover + circular avatar + bio + personalized header
  BÁSICA = otherwise                     → contained header, no cover personalization, "Perfil estándar · este anunciante no ha personalizado su página" note
  SidebarProfile already gates a `pro` class via (isPro || !isPublic). ProfileDefault already shows MemoPro/MemoDefault for the OWNER only — keep that owner-upsell logic; it is separate from the public PRO/básica visual variant. -->

<!-- SCSS OWNERSHIP: edit _hero.scss (.hero--profile block only), _profile.scss, _sidebar.scss (.sidebar--profile block only).
  The ad grid renders CardAnnouncement (restyled in 08-01) — DO NOT re-style the card or touch _card.scss / the --archive|--single sections of _announcement.scss. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the profile cover + avatar (HeroProfile) — PRO vs básica</name>
  <files>apps/website/app/components/HeroProfile.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Translate the profile cover/portada from `design/index.dc.html` lines ~937-946 to the existing `.hero--profile` block. Edit ONLY the `&--profile` block in `_hero.scss`.
    Per mockup: a CONTAINED cover (max-width ~1200px, rounded ~14px, 1px $line border) — PRO sellers show their cover image / a tinted deco; básica sellers show a plain contained band (no personalized cover). A circular avatar (104px, 4px #fff border) overlaps the cover bottom-left (margin-top ~-54px). Above the cover, a "Volver al marketplace" back link ($ink2, arrow-left icon).
    Drive PRO vs básica off `user.pro_status === 'active'` (and/or the existing avatar/cover presence) — keep the current HeroProfile props/flow; this is a restyle. CLAUDE.md NO box-shadow rule: the mockup's avatar shadow is BANNED — use the border + ring look without box-shadow (or a flat ring). CRITICAL: top padding ~96px so the back link/cover clears the fixed/overlay headroom header (06-02) — verify vs headroom.png.
    Tokens only; BEM strict (children under `.hero--profile__*`); no existing-variable edits; no box-shadow/scale; lucide icons without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroProfile" || echo "no hero type errors"</automated>
    VISUAL LOOP (dev server already on :3000): find a PRO seller username and a básica seller username from the data (or use the same user toggling pro_status if only one is available — note it in SUMMARY). Screenshot http://localhost:3000/{username} at 1440 and 390 for BOTH variants → save /tmp/waldo-shots/08-03-cover-{pro,basic}-{desktop,mobile}.png → Read → compare contained cover + avatar overlap + back link + headroom clearance vs design/screenshots/profile-pro.png + profile-basic-contained.png + headroom.png → fix → repeat until both match.
  </verify>
  <done>Profile cover + avatar match the mockup for PRO and básica on desktop+mobile; back link present; clears headroom; no box-shadow; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the identity header + metrics + category chips (ProfileDefault / SidebarProfile)</name>
  <files>apps/website/app/components/ProfileDefault.vue, apps/website/app/components/SidebarProfile.vue, apps/website/app/scss/components/_profile.scss, apps/website/app/scss/components/_sidebar.scss</files>
  <action>
    Restyle the identity block from `design/index.dc.html` lines ~947-1000 onto the existing profile components. Edit `_profile.scss` and the `.sidebar--profile` block of `_sidebar.scss`.
    - IDENTITY HEADER: name (Poppins 800 $ink 28px) inline with a PRO badge ($ink pill, amber star, shown when pro_status === 'active') and a verified ($magic-mint-ish green pill) / "Sin verificar" ($cream outline pill) chip. Meta row: @username (when present) · ubicación · región (map-pin icon) · "Miembro desde {año}" (calendar icon) · tipo (Empresa/Particular pill). Use ONLY data the page already provides (user fields). SCOPE GUARD: a logged-IN contact action column (WhatsApp/Call) and a logged-OUT "Inicia sesión para contactar" button appear in the mockup — render the logged-OUT "Inicia sesión para contactar" link (it maps to existing auth state, → /login) but treat the WhatsApp/Call buttons as an OPEN QUESTION (net-new) — do NOT implement WhatsApp/Call here.
    - BIO: PRO → bio paragraph if present, else "aún no agregó una descripción" italic; básica → the "Perfil estándar · este anunciante no ha personalizado su página" note ($cream pill, info icon). Drive off pro_status + bio presence.
    - METRICS STRIP: a bordered ($line, ~10px radius) inline group of metric cells (e.g. avisos publicados / activos / etc.) — use the counts the page already has (publishedAdsCount / pagination.total). Do NOT add a metrics endpoint; if a mockup metric has no current data source, omit that cell rather than fetch it (note omissions in SUMMARY).
    - CATEGORY CHIPS ("Opera en"): chips with a color dot per category — derive from the categories present in the loaded ads (no extra request).
    - SidebarProfile: the current sidebar (avatar/name/published count + contact CardInfos + logged-OUT ReminderDefault) — reconcile with the mockup. The mockup folds identity into the header rather than a left sidebar; restyle SidebarProfile's contact/identity block to the mockup chip/meta look and keep its logged-IN/logged-OUT contact gating. Restyle-first: do not delete the sidebar's data, restyle its presentation to match the mockup's identity/contact treatment.
    Tokens only; BEM strict (children under `.profile--default__*`, `.sidebar--profile__*`); no existing-variable edits; no box-shadow/scale; lucide icons without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ProfileDefault|SidebarProfile" || echo "no identity type errors"</automated>
    VISUAL LOOP — BOTH VARIANTS + BOTH AUTH STATES:
      logged-OUT: screenshot PRO + básica usernames at 1440 and 390 → /tmp/waldo-shots/08-03-id-out-{pro,basic}-{desktop,mobile}.png → Read → compare name/badges/meta/metrics/chips/bio vs design/screenshots/p-pro-final.png + 01-p-pro-inline.png + profile-basic.png → fix → repeat.
      logged-IN: inject waldo_jwt (token in plan-context), reload, screenshot → /tmp/waldo-shots/08-03-id-in-{pro,basic}-{desktop}.png → Read → confirm the contact/identity block matches and the logged-OUT "inicia sesión para contactar" is correctly hidden → fix → repeat until match.
  </verify>
  <done>Identity header (name/badges/meta), metrics strip, category chips, bio (PRO) / "Perfil estándar" note (básica), and the sidebar contact block match the mockup for both variants and both auth states; only existing data used (no new fetch); WhatsApp/Call not added; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the avisos grid + section chrome; verify single-load data path</name>
  <files>apps/website/app/components/ProfileDefault.vue, apps/website/app/scss/components/_profile.scss</files>
  <action>
    Restyle the avisos section from `design/index.dc.html` lines ~1003-1060 onto `ProfileDefault`'s ad list. Edit `_profile.scss` only.
    - SECTION: a $cream band with a 3-col grid (`profile--default__content__list` → 3 cols desktop, collapsing to 2/1 via existing breakpoint mixins, gap ~22px) of CardAnnouncement cards (already restyled in 08-01 — do NOT re-style the card; for profile cards the mockup passes hide-seller, which the card already handles via its existing seller-conditional logic).
    - The mockup shows tabs ("Avisos activos {N}" / "Vendidos {N}"), a per-profile search box, and category/sort selects above the grid. SCOPE GUARD — these are NET-NEW behavior the current page does NOT have (it loads active ads only, no sold query, no per-profile search). Implement ONLY the visual "Avisos activos" header/count using existing pagination.total; render the empty state ("Sin anuncios" → restyle the existing EmptyState to the mockup's "Sin resultados" card). Do NOT add the Vendidos tab, the per-profile search, or the category/sort selects — they require a new sold-ads backend query + new filtering behavior and are OPEN QUESTIONS (see PLANNING return). Leave a brief code comment noting the tabs/search are deferred so the next executor doesn't re-derive it.
    - PAGINATION: keep the existing vue-awesome-paginate; restyle its container toward the mockup pager (design/screenshots/pager.png) if trivial, else leave functional.
    DATA PATH (verify, do NOT change): confirm [slug].vue keeps ONE useAsyncData "adsData-${slug}" doing loadUser + loadAds (active-only) and that the grid renders from `ads` with no per-card request. Do NOT split or duplicate the load.
    Tokens only; BEM strict (children under `.profile--default__content__*`); no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ProfileDefault|pages/.slug." || echo "no grid type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/[slug].vue  →  MUST equal 1 (single-load, unchanged)
    VISUAL LOOP (logged-OUT): screenshot a profile with ads + a profile with no ads at 1440 and 390 → save /tmp/waldo-shots/08-03-grid-{has,empty}-{desktop,mobile}.png → Read → compare the "Avisos activos" header/count, 3-col card grid, empty state, and pager vs design/index.dc.html profile avisos section + design/screenshots/pager.png → fix → repeat until match.
  </verify>
  <done>Avisos grid (3-col, CardAnnouncement, "Avisos activos" count, empty state, pager) matches the mockup on desktop+mobile; Vendidos tab / per-profile search / sort selects NOT added (deferred, commented); [slug].vue keeps its single useAsyncData (no N+1); tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Profile screenshot-Read-compare passes at 1440 and 390 for BOTH variants (PRO + básica) and BOTH auth states, vs design/index.dc.html "Perfil del anunciante" + profile-pro/profile-basic references.
- PRO vs básica is driven by pro_status from one page (no separate route); the owner MemoPro/MemoDefault upsell logic is preserved.
- [slug].vue keeps its single useAsyncData (loadUser + active-only loadAds); cards render from the array; no per-card/per-metric request (no N+1); active-only query unchanged.
- The ad grid reuses the 08-01 CardAnnouncement (not re-styled here); _card.scss and the --archive/--single sections of _announcement.scss are NOT touched.
- Net-new features (Vendidos tab, per-profile search, sort/category selects, WhatsApp/Call buttons, email/phone masking) are NOT implemented — deferred as OPEN QUESTIONS.
- Header/cover clears the 06-02 headroom header.
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only).
- vue-tsc --noEmit exits 0; NEVER run the full Vitest/Jest suite; reuse the running :3000 dev server; one heavy op at a time.
</verification>

<success_criteria>
The public seller profile looks like the "Perfil del anunciante" mockup for both PRO and básica sellers, on desktop + mobile, logged-OUT and logged-IN; renders both variants from one pro_status-driven page; keeps its single-load active-ads data path; reuses the 08-01 card; defers all net-new behavior (tabs/search/contact buttons); uses only phase-04/new tokens; modifies no existing SCSS variable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-publico-anuncios-perfil/08-03-SUMMARY.md`.
</output>
