---
phase: 08-publico-anuncios-perfil
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/strapi/src/api/ad/services/ad.ts
  - apps/strapi/src/api/ad/controllers/ad.ts
  - apps/strapi/src/api/ad/routes/00-ad-custom.ts
  - apps/strapi/tests/api/ad/sold-ads.service.test.ts
  - apps/website/app/stores/ads.store.ts
  - apps/website/app/pages/[slug].vue
  - apps/website/app/components/HeroProfile.vue
  - apps/website/app/components/ProfileDefault.vue
  - apps/website/app/components/SidebarProfile.vue
  - apps/website/app/scss/components/_hero.scss
  - apps/website/app/scss/components/_profile.scss
  - apps/website/app/scss/components/_sidebar.scss
autonomous: true
requirements: [PUB-PROFILE, PUB-PROFILE-SOLD, PUB-PROFILE-FILTER]
must_haves:
  truths:
    - "The public seller profile (/{username}) renders to the 'Perfil del anunciante' mockup: contained cover + circular avatar, identity header (name + PRO badge + verified/sin-verificar chip), meta row (@username · ubicación · miembro desde · tipo), metrics strip, category chips, and the active-ads grid — logged-OUT and logged-IN, desktop 1440 and mobile 390"
    - "BOTH variants render from one page: PRO sellers (pro_status === 'active') show the cover + avatar + bio; básica (standard) sellers show the contained header without cover personalization and the 'Perfil estándar' note — driven by pro_status, no separate route"
    - "The avisos section shows two tabs ('Avisos activos {N}' / 'Vendidos {N}'); the Vendidos tab loads on first activation via a single paginated public request (GET ads/sold/:username) and renders the seller's sold/down ads using the 08-01 CardAnnouncement SOLD variant (:sold + :sold-when props — Vendido badge, strikethrough price; no per-card request, no N+1, no fetch on profile mount for the sold list)"
    - "The active tab has a per-profile search box + category select + sort select that filter the seller's active ads via the SAME single useAsyncData (server-side name/category/sort params on the existing ads/catalog load) — re-fetch on input is one paginated query, never N+1, and the page still has exactly one useAsyncData"
    - "The active-ads grid loads with a single ads request (adsStore.loadAds for the user's active ads) plus the user load; cards render from that array (no per-card request, no N+1); the existing active-only query is preserved"
    - "The ad grids reuse the CardAnnouncement restyled in 08-01; colors/typography use phase-04 tokens; no existing SCSS variable is modified; header clears the headroom"
  artifacts:
    - path: "apps/strapi/src/api/ad/services/ad.ts"
      provides: "New soldAds(options, isManager, userId) service method — 'was-live, now-down' ads for a seller, routed through getAdvertisements (batched view/contact counts, pagination, no N+1)"
    - path: "apps/strapi/src/api/ad/controllers/ad.ts"
      provides: "New soldByUsername(ctx) controller — public, role-bypass (true, null), sanitized, paginated; resolves :username from params"
    - path: "apps/strapi/src/api/ad/routes/00-ad-custom.ts"
      provides: "New GET /ads/sold/:username route (config.auth:false) declared before the /ads/:id wildcard"
    - path: "apps/strapi/tests/api/ad/sold-ads.service.test.ts"
      provides: "Jest AAA test of soldAds default filters (active:false, banned:false, rejected:false, draft:false, remaining_days:0, user.username scoping)"
    - path: "apps/website/app/components/HeroProfile.vue"
      provides: "Restyled contained cover + circular avatar (PRO) / plain header (básica) to mockup"
    - path: "apps/website/app/components/ProfileDefault.vue"
      provides: "Restyled identity header + metrics + chips + tabbed (Activos/Vendidos) ad grid + per-profile search/category/sort to mockup"
    - path: "apps/website/app/stores/ads.store.ts"
      provides: "New loadSoldAds(username, pagination) action calling GET ads/sold/:username (single paginated load, separate from active loadAds)"
  key_links:
    - from: "apps/website/app/pages/[slug].vue"
      to: "apps/website/app/components/ProfileDefault.vue"
      via: ":user :ads :pagination (single useAsyncData adsData-${slug} with name/category/sort filters)"
      pattern: "<ProfileDefault"
    - from: "apps/website/app/components/ProfileDefault.vue"
      to: "apps/website/app/components/CardAnnouncement.vue"
      via: "v-for over ads (active) and over sold ads (no fetch in card)"
      pattern: "<CardAnnouncement"
    - from: "apps/website/app/stores/ads.store.ts loadSoldAds"
      to: "GET ads/sold/:username"
      via: "single paginated client call on tab activation"
      pattern: "ads/sold/"
    - from: "apps/strapi/src/api/ad/controllers/ad.ts soldByUsername"
      to: "strapi.service('api::ad.ad').soldAds"
      via: "soldAds(options, true, null) scoped by username filter"
      pattern: "soldAds"
---

<objective>
Restyle the public seller profile (`pages/[slug].vue` via `HeroProfile` + `ProfileDefault` + `SidebarProfile`) to the v1.47 "Perfil del anunciante" mockup (`design/index.dc.html` lines ~933-1060) at FULL FIDELITY. ONE plan covers BOTH variants (PRO cover+avatar+bio vs básica contained header + "Perfil estándar" note, driven by `pro_status`, no separate route) AND the mockup's net-new avisos features: a **Vendidos tab** (backed by a new public, paginated, N+1-safe Strapi endpoint for the seller's sold/down ads) and a **per-profile search + category + sort** that filter the seller's active ads through the existing single useAsyncData (server-side params, one query, never N+1).

Purpose: the seller profile is the trust/credibility surface and the PRO upsell payoff. It must look AND behave like the mockup (tabbed Activos/Vendidos, searchable/filterable own-ads) for both PRO and standard sellers while keeping a single-load-per-view data path.
Output: Restyled cover/identity header (PRO and básica), metrics strip, category chips; the active-ads grid with working search/category/sort; the Vendidos tab with sold-ad cards; the new ads/sold/:username backend (service + controller + route + Jest test). Matching the mockup on desktop + mobile, logged-OUT and logged-IN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

# Source of truth — "Perfil del anunciante" screen; translate inline styles to BEM + phase-04 tokens:
@design/index.dc.html
# Rendered references — Read these PNGs. BÁSICA vs PRO variants:
@design/screenshots/profile-basic.png
@design/screenshots/profile-basic-contained.png
@design/screenshots/profile-pro.png
@design/screenshots/profile-pro2.png
@design/screenshots/p-pro-final.png
@design/screenshots/01-p-pro-inline.png
@design/screenshots/profile-contained.png
@design/screenshots/profile-gate.png
@design/screenshots/headroom.png

# Current page + components to UPDATE (closest existing equivalents — do NOT create new):
@apps/website/app/pages/[slug].vue
@apps/website/app/components/HeroProfile.vue
@apps/website/app/components/ProfileDefault.vue
@apps/website/app/components/SidebarProfile.vue
@apps/website/app/stores/ads.store.ts

# Backend to UPDATE (add soldAds following the archivedAds/catalog pattern):
@apps/strapi/src/api/ad/services/ad.ts
@apps/strapi/src/api/ad/controllers/ad.ts
@apps/strapi/src/api/ad/routes/00-ad-custom.ts

<interfaces>
<!-- phase-04 tokens — use these, do not modify, do not invent -->
$ink: #26252b;  $ink2: #56535f;  $muted: #8a8794;
$amber: #f7c97e;  $amber_hover: #efb85c;  $amber_tint: #fef8ec;  $tag: #a9772e;
$cream: #f6f4f1;  $line: #ece9e4;

<!-- DATA PATH (active ads — single useAsyncData, KEEP one load, now with search/category/sort params):
  [slug].vue → ONE useAsyncData "adsData-${slug}":
    userStore.loadUser(username) (populate:"*" → user + avatar/cover/commune)
    + adsStore.loadAds({ active:{$eq:true}, remaining_days:{$gt:0}, user:{username}, ...nameFilter, ...categoryFilter }, {pageSize:12, page}, [sortParam])
  Returns { user, ads, pagination }. Adding search (name:{$containsi}), category, and sort means re-running THIS SAME useAsyncData via its watch list — a re-fetch is ONE paginated query, NOT N+1, and the page keeps exactly ONE useAsyncData. Do NOT add a second useAsyncData. -->

<!-- VENDIDOS / SOLD ADS (NET-NEW backend, single paginated load on tab open — no N+1, no fetch on mount):
  There is NO 'sold' flag in the ad schema. deactivateAd sets active:false, remaining_days:0 (identical to expired/archived) and reason_for_deactivation is nullable — so "sold" and "expired" are indistinguishable in data. The HONEST, agreed definition of a seller's "Vendidos"/down ads is the archivedAds filter, scoped to the username and public-safe:
    { user:{ username:{$eq} }, active:false, banned:false, rejected:false, draft:false, remaining_days:0 }
  (draft:false is the one addition over owner-scoped archivedAds — it stops never-published drafts leaking on a PUBLIC surface.)
  This MUST route through the existing getAdvertisements() helper so pagination + the batched view/contact counts come for free (no N+1). New endpoint mirrors `catalog`: public (auth:false), role-bypass soldAds(options, true, null), sanitized. Load on tab activation only; the Vendidos count badge populates on first open (active count uses existing pagination.total).
  SOLD CARD VARIANT: the "Vendido" card look (badge, $ink2 title, strikethrough price, sold footer) is the sold variant of CardAnnouncement, ADDED + OWNED in 08-01 (sold + sold-when props). This plan only PASSES :sold="true" + a sold-when value — it does NOT style the card.
  SOLD-WHEN SOURCE: the ad schema has NO sold_at/sold field. deactivateAd updates the record, so `updatedAt` is the pragmatic "vendido {fecha}" date — format updatedAt (es-CL, e.g. "hace 2 meses" or "jun 2026"); if you prefer not to imply precision, omit soldWhen and render the card without the sold footer. State the choice in the SUMMARY. -->

<!-- DEFERRED / OUT OF SCOPE (do NOT implement; surface in SUMMARY):
  Profile WhatsApp/Call contact buttons (mockup lines ~963-973) are NET-NEW PII surface: the public /users endpoint (getUserDataWithFilters) STRIPS phone/email for all non-managers, so the profile payload has NO seller phone. Exposing it needs a new authenticated PII endpoint — a security decision NOT in <features_to_add> and NOT approved. Keep the logged-OUT "Inicia sesión para contactar" link (maps to existing auth state → /login); leave WhatsApp/Call DEFERRED with an explicit code comment and note it in the SUMMARY as an open user decision. -->

<!-- VARIANT SWITCH:
  PRO    = user.pro_status === 'active'  → cover + circular avatar + bio + personalized header
  BÁSICA = otherwise                     → contained header, no cover personalization, "Perfil estándar · este anunciante no ha personalizado su página" note
  SidebarProfile already gates a `pro` class via (isPro || !isPublic). ProfileDefault shows MemoPro/MemoDefault for the OWNER only — keep that owner-upsell logic; it is separate from the public PRO/básica visual variant. -->

<!-- SCSS OWNERSHIP: edit _hero.scss (.hero--profile block only), _profile.scss, _sidebar.scss (.sidebar--profile block only).
  The ad grids render CardAnnouncement (restyled in 08-01, INCLUDING its sold variant) — DO NOT re-style the card or touch _card.scss / the --archive|--single sections of _announcement.scss. The Vendidos cards pass :sold/:sold-when props only. -->

<!-- BACKEND OWNERSHIP: this plan is the ONLY 08-* plan touching apps/strapi — 08-01/08-02 are website-only there.
  SHARED FILE CAVEAT (wave 2 parallel): _hero.scss is listed by BOTH 08-02 (.hero--announcement block) and 08-03 (.hero--profile block). The blocks are disjoint, so an executor edits only its own &--block and merges cleanly — but since 08-02 and 08-03 can run in parallel (both wave 2, both depends_on:[08-01]), treat _hero.scss as a shared file: scope strictly to &--profile here and rebase/merge if 08-02 landed first. All OTHER files are exclusively owned per plan. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — soldAds service + public ads/sold/:username endpoint (+ Jest test)</name>
  <files>apps/strapi/src/api/ad/services/ad.ts, apps/strapi/src/api/ad/controllers/ad.ts, apps/strapi/src/api/ad/routes/00-ad-custom.ts, apps/strapi/tests/api/ad/sold-ads.service.test.ts</files>
  <action>
    Add a public, paginated, N+1-safe endpoint for a seller's "Vendidos"/down ads, mirroring the existing `catalog`/`archivedAds` pattern. Follow CLAUDE.md Strapi rules (SOLID, English, prefer documentId for writes — N/A here, this is read-only).
    - SERVICE (`services/ad.ts`): add `async soldAds(options, isManager = false, userId = null)` next to `archivedAds`. defaultFilters (the agreed "was-live, now-down" definition — do NOT re-derive):
        { active: { $eq: false }, banned: { $eq: false }, rejected: { $eq: false }, draft: { $eq: false }, remaining_days: { $eq: 0 } }
      Return `getAdvertisements(options, defaultFilters, "archived", undefined, isManager, userId)` — this reuses the batched view/contact aggregation + pagination + sanitize (no N+1). The per-username scope comes from the controller passing options.filters.user (see below), which getAdvertisements merges into the where clause.
    - CONTROLLER (`controllers/ad.ts`): add `async soldByUsername(ctx)` modeled on `catalog`. Read `ctx.params.username`; build options exactly like `catalog` (extract pagination.page/pageSize, delete the pagination object), then set `options.filters = { ...(options.filters||{}), user: { username: { $eq: username } } }`. Call `strapi.service("api::ad.ad").soldAds(options, true, null)` (public role-bypass like catalog) and return it. Wrap in try/catch → ctx.throw(500, error) like the siblings.
    - ROUTE (`routes/00-ad-custom.ts`): add `{ method: "GET", path: "/ads/sold/:username", handler: "ad.soldByUsername", config: { auth: false } }`. CRITICAL ordering: declare it BEFORE any `/ads/:id`-style wildcard so the static `sold` segment is not captured (the slug route already follows this convention — place it among the other static GETs near `/ads/catalog`).
    - TEST (`apps/strapi/tests/api/ad/sold-ads.service.test.ts`, AAA pattern, English, mock strapi.db.query): assert that calling soldAds for a username produces a findMany/count `where` containing active:false, banned:false, rejected:false, draft:false, remaining_days:0 (or $eq forms) AND the user.username scope. Mock `strapi.db.query("api::ad.ad").findMany/count` and the ad-view/ad-contact count services (return {}); assert the filter shape and that pagination is honored. Mirror an existing ad service test for harness setup (strapi.contentType stub, logtail mock with __esModule:true) — read a sibling test in apps/strapi/tests first.
    No new schema field, no migration, no write path. Never use `any` (CLAUDE.md). Do not relax any other route's auth.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && node ../../node_modules/.bin/jest tests/api/ad/sold-ads.service.test.ts --runTestsByPath 2>&1 | tail -20</automated>
    grep -n "sold/:username" apps/strapi/src/api/ad/routes/00-ad-custom.ts  →  route present with config.auth:false
    grep -n "soldAds\|soldByUsername" apps/strapi/src/api/ad/services/ad.ts apps/strapi/src/api/ad/controllers/ad.ts  →  service + controller present
  </verify>
  <done>GET /ads/sold/:username returns the seller's sold/down ads (active:false, banned:false, rejected:false, draft:false, remaining_days:0, scoped by username), public + paginated + sanitized through getAdvertisements (no N+1); the route is declared before any wildcard with auth:false; the Jest AAA test passes asserting the filter shape; no schema/migration; no `any`; no other route's auth changed.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the profile cover + avatar (HeroProfile) — PRO vs básica</name>
  <files>apps/website/app/components/HeroProfile.vue, apps/website/app/scss/components/_hero.scss</files>
  <action>
    Translate the profile cover/portada from `design/index.dc.html` lines ~937-946 to the existing `.hero--profile` block. Edit ONLY the `&--profile` block in `_hero.scss`.
    Per mockup: a CONTAINED cover (max-width ~1200px, rounded ~14px, 1px $line border) — PRO sellers show their cover image / a tinted deco; básica sellers show a plain contained band (no personalized cover). A circular avatar (104px, 4px #fff border) overlaps the cover bottom-left (margin-top ~-54px). Above the cover, a "Volver al marketplace" back link ($ink2, arrow-left icon).
    Drive PRO vs básica off `user.pro_status === 'active'` (and/or the existing avatar/cover presence) — keep the current HeroProfile props/flow; this is a restyle. CLAUDE.md NO box-shadow rule: the mockup's avatar shadow is BANNED — use the border + ring look without box-shadow (or a flat ring). CRITICAL: top padding ~96px so the back link/cover clears the fixed/overlay headroom header (06-02) — verify vs headroom.png.
    Tokens only; BEM strict (children under `.hero--profile__*`); no existing-variable edits; no box-shadow/scale; lucide icons without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HeroProfile" || echo "no hero type errors"</automated>
    VISUAL LOOP (dev server already on :3000): find a PRO seller username and a básica seller username from the data (or use the same user toggling pro_status if only one is available — note it in SUMMARY). Screenshot http://localhost:3000/{username} at 1440 and 390 for BOTH variants → save /tmp/waldo-shots/08-03-cover-{pro,basic}-{desktop,mobile}.png → Read → compare contained cover + avatar overlap + back link + headroom clearance vs design/screenshots/profile-pro.png + profile-basic-contained.png + headroom.png → fix → repeat until both match.
  </verify>
  <done>Profile cover + avatar match the mockup for PRO and básica on desktop+mobile; back link present; clears headroom; no box-shadow; tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle the identity header + metrics + chips (ProfileDefault / SidebarProfile)</name>
  <files>apps/website/app/components/ProfileDefault.vue, apps/website/app/components/SidebarProfile.vue, apps/website/app/scss/components/_profile.scss, apps/website/app/scss/components/_sidebar.scss</files>
  <action>
    Restyle the identity block from `design/index.dc.html` lines ~947-1000 onto the existing profile components. Edit `_profile.scss` and the `.sidebar--profile` block of `_sidebar.scss`.
    - IDENTITY HEADER: name (Poppins 800 $ink 28px) inline with a PRO badge ($ink pill, amber star, shown when pro_status === 'active') and a verified ($magic-mint-ish green pill) / "Sin verificar" ($cream outline pill) chip. Meta row: @username (when present) · ubicación · región (map-pin icon) · "Miembro desde {año}" (calendar icon) · tipo (Empresa/Particular pill). Use ONLY data the page already provides (user fields).
      DEFERRED — CONTACT ACTION COLUMN: the mockup's logged-IN WhatsApp/Call column (lines ~963-973) is NOT implemented — the public /users payload strips seller phone/email (PII), so the data is absent and exposing it is an unapproved security decision (see PLANNING return). Render ONLY the logged-OUT "Inicia sesión para contactar" link (maps to existing auth state → /login). Add a brief code comment: "WhatsApp/Call deferred — requires authenticated seller-phone PII endpoint, pending user approval." Do NOT add WhatsApp/Call or any phone fetch here.
    - BIO: PRO → bio paragraph if present, else "aún no agregó una descripción" italic; básica → the "Perfil estándar · este anunciante no ha personalizado su página" note ($cream pill, info icon). Drive off pro_status + bio presence.
    - METRICS STRIP: a bordered ($line, ~10px radius) inline group of metric cells — use the counts the page already has (publishedAdsCount / pagination.total). Do NOT add a metrics endpoint; if a mockup metric has no current data source, omit that cell rather than fetch it (note omissions in SUMMARY).
    - CATEGORY CHIPS ("Opera en"): chips with a color dot per category — derive from the categories present in the loaded ads (no extra request).
    - SidebarProfile: restyle its contact/identity block to the mockup chip/meta look and keep its logged-IN/logged-OUT gating. Restyle-first: do not delete the sidebar's data, restyle its presentation to match the mockup's identity/contact treatment.
    Tokens only; BEM strict (children under `.profile--default__*`, `.sidebar--profile__*`); no existing-variable edits; no box-shadow/scale; lucide icons without alias.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ProfileDefault|SidebarProfile" || echo "no identity type errors"</automated>
    VISUAL LOOP — BOTH VARIANTS + BOTH AUTH STATES:
      logged-OUT: screenshot PRO + básica usernames at 1440 and 390 → /tmp/waldo-shots/08-03-id-out-{pro,basic}-{desktop,mobile}.png → Read → compare name/badges/meta/metrics/chips/bio vs design/screenshots/p-pro-final.png + 01-p-pro-inline.png + profile-basic.png → confirm "Inicia sesión para contactar" present, WhatsApp/Call ABSENT → fix → repeat.
      logged-IN: inject waldo_jwt (token in plan-context), reload, screenshot → /tmp/waldo-shots/08-03-id-in-{pro,basic}-{desktop}.png → Read → confirm the contact/identity block matches and the logged-OUT "inicia sesión para contactar" is correctly hidden → fix → repeat until match.
  </verify>
  <done>Identity header (name/badges/meta), metrics strip, category chips, bio (PRO) / "Perfil estándar" note (básica), and the sidebar contact block match the mockup for both variants and both auth states; only existing data used (no new fetch); WhatsApp/Call deferred (commented, no phone fetch); tokens only; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Tabbed avisos grid (Activos/Vendidos) + per-profile search/category/sort; wire single-load + sold load</name>
  <files>apps/website/app/stores/ads.store.ts, apps/website/app/pages/[slug].vue, apps/website/app/components/ProfileDefault.vue, apps/website/app/scss/components/_profile.scss</files>
  <action>
    Restyle the avisos section from `design/index.dc.html` lines ~1003-1058 onto `ProfileDefault`'s ad list AND implement its net-new behaviors (tabs, search, category, sort, Vendidos). Edit `_profile.scss` for styling.
    - TABS (mockup ~1006-1009): "Avisos activos {N}" / "Vendidos {N}" tab bar (bottom $line border, active tab underline). Active count = existing `pagination.total`. Sold count populates on first Vendidos activation (show the tab without a number or with a lazy count until loaded; do NOT fetch sold ads on mount).
    - ACTIVE TAB toolbar (mockup ~1012-1029): a per-profile search box (search icon, placeholder "Buscar en los avisos de este anunciante…"), a category select (categories present in the loaded ads, or a fixed list), and a sort select (e.g. Recientes / Precio asc / Precio desc). These filter the SELLER'S ACTIVE ADS server-side through the EXISTING single useAsyncData:
        • In [slug].vue add reactive refs (search, category, sort) and add them to the useAsyncData `watch` list. Build the filtersParams to include `name: { $containsi: search }` (when non-empty) and `category: { slug: { $eq: category } }` (when chosen), and map the sort ref to sortParams (e.g. ["createdAt:desc"] / ["price:asc"] / ["price:desc"]). Reset `page` to 1 when search/category/sort change (mirror the existing pagination reset quick-fix 260406-r0g).
        • This is the SAME useAsyncData re-running — a single paginated ads/catalog query per change, NOT N+1. Keep exactly ONE useAsyncData in [slug].vue (the grep verify enforces this).
        • Empty result → restyle the existing EmptyState to the mockup "Sin resultados" card (mockup ~1031: white card, $line border, search-circle icon, "Sin resultados", subcopy).
    - VENDIDOS TAB (mockup ~1040-1047): on first activation, call a NEW `adsStore.loadSoldAds(username, { pageSize, page })` (add it to ads.store.ts) → GET ads/sold/:username (Task 1 endpoint). Render the returned ads with the 08-01 CardAnnouncement in the same 3-col grid, passing `:sold="true"` + `:sold-when="<formatted updatedAt>"` (the sold variant is owned by 08-01 — pass props, do NOT style the card). soldWhen = formatted `ad.updatedAt` (deactivateAd touches updatedAt) or omit if you choose no date (note in SUMMARY). Empty → "Aún no hay ventas registradas" card (mockup ~1041). Single paginated load on tab open; cards render from the array (no per-card request, no N+1). Keep loadSoldAds SEPARATE from active loadAds (own ref/state in the store or local component state — do NOT overwrite the active ads array used by the active tab; prefer local component state in ProfileDefault holding the sold ads + a separate paginated call, or a dedicated store ref `soldAds`/`soldPagination`).
    - GRID + PAGINATION: `profile--default__content__list` → 3-col grid (collapsing to 2/1 via existing breakpoint mixins, gap ~22px). Keep vue-awesome-paginate for the active tab; restyle its container toward the mockup pager (design/screenshots/pager.png) if trivial, else leave functional. (Sold tab pagination can be a simple paged load if more than one page; keep it single-query-per-page.)
    DATA PATH: [slug].vue keeps ONE useAsyncData for the active tab (now with search/category/sort params). The sold load is a separate user-triggered single paginated call on tab open — NOT a useAsyncData, NOT fired on mount.
    Tokens only; BEM strict (children under `.profile--default__content__*`); no existing-variable edits; no box-shadow/scale.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -E "ProfileDefault|pages/.slug.|ads.store" || echo "no grid type errors"</automated>
    grep -c "useAsyncData" apps/website/app/pages/[slug].vue  →  MUST equal 1 (single useAsyncData for active tab; search/category/sort re-run it, they do NOT add a second)
    grep -n "loadSoldAds\|ads/sold/" apps/website/app/stores/ads.store.ts  →  loadSoldAds present hitting ads/sold/
    VISUAL LOOP (logged-OUT):
      ACTIVE: screenshot a profile with ads at 1440 and 390 → /tmp/waldo-shots/08-03-grid-active-{desktop,mobile}.png → Read → compare tabs, search box, category+sort selects, 3-col grid, pager vs design/index.dc.html profile avisos section + pager.png. Type in the search box → screenshot → confirm the grid filters (fewer/zero cards) and the empty state renders the "Sin resultados" card. → fix → repeat.
      VENDIDOS: click the Vendidos tab → screenshot → /tmp/waldo-shots/08-03-grid-sold-{desktop,mobile}.png → Read → confirm the sold grid (or the "Aún no hay ventas registradas" empty card) renders from a single ads/sold/:username call (check the network tab / dev server log shows ONE request, no per-card requests) vs mockup ~1040-1047. → fix → repeat.
  </verify>
  <done>Avisos section matches the mockup: Activos/Vendidos tabs, per-profile search + category + sort filtering the active ads via the SAME single useAsyncData (page resets to 1 on change), and the Vendidos tab loading the seller's sold ads via one paginated ads/sold/:username call on first activation (no fetch on mount, no N+1). Active and sold grids reuse the 08-01 CardAnnouncement; [slug].vue keeps exactly 1 useAsyncData; empty states match the mockup; tokens only; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Profile screenshot-Read-compare passes at 1440 and 390 for BOTH variants (PRO + básica) and BOTH auth states, vs design/index.dc.html "Perfil del anunciante" + profile-pro/profile-basic references.
- PRO vs básica is driven by pro_status from one page (no separate route); the owner MemoPro/MemoDefault upsell logic is preserved.
- GET /ads/sold/:username is public (auth:false), paginated, sanitized, scoped to the username, defined "was-live, now-down" (active:false, banned:false, rejected:false, draft:false, remaining_days:0), routed through getAdvertisements (batched counts, no N+1); Jest AAA test passes; route declared before any wildcard; no schema change, no other route's auth touched.
- Active tab: [slug].vue keeps exactly ONE useAsyncData; search (name $containsi), category, and sort re-run that single query (page resets to 1); no per-card request, no N+1, no second useAsyncData.
- Vendidos tab: loads via one ads/sold/:username call on first activation only (not on mount); cards render from the returned array (no N+1); separate from the active ads state.
- Both grids reuse the 08-01 CardAnnouncement (not re-styled here); the Vendidos grid passes :sold/:sold-when to the 08-01-owned sold variant; _card.scss and the --archive/--single sections of _announcement.scss are NOT touched.
- DEFERRED (not implemented, surfaced in SUMMARY): profile WhatsApp/Call contact buttons — require a new authenticated seller-phone PII endpoint (public /users strips phone/email), a security decision not approved.
- Header/cover clears the 06-02 headroom header.
- `git diff apps/website/app/scss/abstracts/_variables.scss` shows NO modification to existing variables (additions only).
- vue-tsc --noEmit exits 0 for website; Jest passes for the new sold-ads test; NEVER run the full Vitest/Jest suite (run only the new test by path); reuse the running :3000 dev server; one heavy op at a time.
</verification>

<success_criteria>
The public seller profile looks AND behaves like the "Perfil del anunciante" mockup for both PRO and básica sellers, on desktop + mobile, logged-OUT and logged-IN: one pro_status-driven page, tabbed Activos/Vendidos avisos, per-profile search + category + sort filtering the active ads through a single useAsyncData, and a Vendidos tab backed by a new public, paginated, N+1-safe ads/sold/:username endpoint (with a Jest test). Reuses the 08-01 card; profile WhatsApp/Call deferred as an open security decision; uses only phase-04/new tokens; modifies no existing SCSS variable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-publico-anuncios-perfil/08-03-SUMMARY.md`.
</output>
