---
phase: 129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie
plan: 05
type: execute
wave: 2
depends_on: [129-01, 129-02, 129-03]
files_modified:
  - apps/website/app/components/FormVerifyCode.vue
  - apps/website/app/composables/useImage.ts
  - apps/website/app/components/UploadMedia.vue
  - apps/website/app/composables/useLogout.ts
  - apps/website/app/middleware/auth.ts
  - apps/website/app/middleware/onboarding-guard.global.ts
  - apps/website/app/middleware/dashboard-guard.global.ts
  - apps/website/tests/composables/useLogout.test.ts
  - apps/website/tests/middleware/dashboard-guard.test.ts
autonomous: true
requirements: [VERIFYCODE-NO-SETTOKEN, UPLOADS-NO-TOKEN, LOGOUT-SERVER-ROUTE, GUARDS-NO-TOKEN]
must_haves:
  truths:
    - "FormVerifyCode.vue no longer calls setToken and no longer cache-busts nuxt._cookies; it calls fetchUser after the server route set the cookie"
    - "useImage.uploadFiles and UploadMedia.vue no longer read a client token or build an Authorization header; the proxy injects it"
    - "useLogout calls POST /api/auth/logout (server clears httpOnly cookie); no document.cookie max-age=0"
    - "All three middleware guards drop the useStrapiToken guard and call fetchUser unconditionally (client) on null user"
    - "Zero useStrapiToken references remain in the website app"
  artifacts:
    - path: "apps/website/app/composables/useLogout.ts"
      provides: "server-side logout"
      contains: "/api/auth/logout"
    - path: "apps/website/app/middleware/auth.ts"
      provides: "user-state guard, no token read"
      contains: "useSessionUser"
  key_links:
    - from: "useLogout"
      to: "server/api/auth/logout.post.ts"
      via: "$fetch POST"
      pattern: "/api/auth/logout"
    - from: "FormVerifyCode.vue"
      to: "httpOnly cookie via verify-code route"
      via: "fetchUser after apiClient /auth/verify-code"
      pattern: "fetchUser"
---

<objective>
Migrate the remaining real-logic files: `FormVerifyCode.vue` (drop `setToken` + the `nuxt._cookies` cache-bust; the verify-code route set the cookie), the two upload paths (`useImage.uploadFiles`, `UploadMedia.vue` — drop the manual `Authorization: Bearer ${token.value}` header; the proxy injects it), `useLogout` (call the server logout route instead of `document.cookie` clearing + module logout), and the three middleware guards (drop the `useStrapiToken` guard which is unreadable under httpOnly — Pitfall 1).

NOTE — knowingly half-migrated until cutover: these now reference `useSessionUser`/`useSessionAuth`, but the `@nuxtjs/strapi` module remains the active session writer until plan 06 activates `session.ts`. Verify-code login, uploads, and logout are exercised end-to-end by plan 06 human-verify checkpoints. Acceptance here is grep/structure + unit tests.

Purpose: Eliminate every client token read/write outside the OAuth surface (plan 04) and the cutover sweep (plan 06).
Output: 7 migrated files + 2 updated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-CONTEXT.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-RESEARCH.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-01-PLAN.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-02-PLAN.md

<interfaces>
<!-- useSessionAuth() (plan 01): { fetchUser, logout, getProviderAuthenticationUrl } -->
<!-- /api/auth/logout (plan 02): POST, clears httpOnly cookie -->
<!-- Proxy (plan 02): injects Authorization from httpOnly cookie on ALL /api/* incl. uploads -->
<!-- Current guard shapes read: auth.ts, onboarding-guard.global.ts, dashboard-guard.global.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FormVerifyCode.vue + uploads — drop setToken, cache-bust, and manual Authorization</name>
  <files>apps/website/app/components/FormVerifyCode.vue, apps/website/app/composables/useImage.ts, apps/website/app/components/UploadMedia.vue</files>
  <read_first>
    - apps/website/app/components/FormVerifyCode.vue (lines 142-160 — setToken at 151, any nuxt._cookies cache-bust)
    - apps/website/app/composables/useImage.ts (lines 40-90 — token read at 49, Authorization header at 72)
    - apps/website/app/components/UploadMedia.vue (lines 130-150 — token at 136, Authorization at 146)
    - .planning/phases/129-*/129-RESEARCH.md sections "### FormVerifyCode.vue — Remove setToken + remove cache-bust", "### Upload Components — Remove Manual Authorization Headers", Pitfall 7
  </read_first>
  <action>
In `apps/website/app/components/FormVerifyCode.vue` (handleVerify):
- The `apiClient("/auth/verify-code", ...)` call now hits the dedicated Nitro route (plan 02), which set the httpOnly cookie and returns `{ user }` (no jwt).
- Change `const { setToken, fetchUser } = useStrapiAuth();` to `const { fetchUser } = useSessionAuth();`.
- Remove `setToken(responseRaw.jwt);`.
- Remove any `nuxt._cookies[...] = useCookie(...)` cache-bust line (Phase 109 artifact — httpOnly cookies never enter `nuxt._cookies`). If present, delete it and any now-unused `useNuxtApp`/`useCookie` import.
- Keep `await fetchUser();` and the existing manager-redirect + onboarding logic. Change `const loggedUser = useStrapiUser();` to `useSessionUser()` if it is used for the manager check (or leave for plan 06 sweep — but since you are already editing this file and removing useStrapiAuth, switch the user read to useSessionUser here for consistency).

In `apps/website/app/composables/useImage.ts` (`uploadFiles`):
- Remove `const token = useStrapiToken();` (line 49).
- Remove `Authorization: \`Bearer ${token.value}\`` from the `headers` object (line 72). Keep the `X-Recaptcha-Token` header logic.
- If `headers` becomes only-recaptcha, keep it as `const headers: Record<string, string> = {};` then conditionally add the recaptcha token. The `fetch(uploadUrl, { method, body, headers })` call stays; the proxy injects Authorization from the httpOnly cookie.

In `apps/website/app/components/UploadMedia.vue`:
- Remove `const token = useStrapiToken();` (line 136).
- Remove `Authorization: \`Bearer ${token.value}\`` from the fetch headers (line 146). Keep any reCAPTCHA header. The proxy injects Authorization.

No unused imports / no `_`-prefix (Codacy): remove `useStrapiToken` imports where now unused.
  </action>
  <verify>
    <automated>cd apps/website && ! grep -q "setToken" app/components/FormVerifyCode.vue && ! grep -q "nuxt._cookies" app/components/FormVerifyCode.vue && ! grep -q "useStrapiToken" app/composables/useImage.ts && ! grep -q 'Authorization: `Bearer ${token' app/composables/useImage.ts && ! grep -q "useStrapiToken" app/components/UploadMedia.vue && ! grep -q 'Authorization: `Bearer ${token' app/components/UploadMedia.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/components/FormVerifyCode.vue` does NOT contain `setToken` or `nuxt._cookies`; contains `useSessionAuth`
    - `app/composables/useImage.ts` does NOT contain `useStrapiToken` or `Authorization: ` + Bearer-token construction
    - `app/components/UploadMedia.vue` does NOT contain `useStrapiToken` or the Bearer-token Authorization header
  </acceptance_criteria>
  <done>verify-code drops setToken+cache-bust; both upload paths drop manual Authorization; proxy handles auth.</done>
</task>

<task type="auto">
  <name>Task 2: useLogout.ts — call server logout route; remove document.cookie clearing</name>
  <files>apps/website/app/composables/useLogout.ts, apps/website/tests/composables/useLogout.test.ts</files>
  <read_first>
    - apps/website/app/composables/useLogout.ts (entire file — strapiLogout at 17, document.cookie at 31)
    - apps/website/tests/composables/useLogout.test.ts (the test to update)
    - .planning/phases/129-*/129-RESEARCH.md "Files requiring LOGIC CHANGES" row for useLogout.ts
    - .planning/phases/129-*/129-02-PLAN.md Task 2 (logout route contract)
  </read_first>
  <action>
In `apps/website/app/composables/useLogout.ts`:
- Replace the import `import { useStrapiAuth, navigateTo } from "#imports";` with `import { navigateTo } from "#imports";` (drop useStrapiAuth).
- Remove `const { logout: strapiLogout } = useStrapiAuth();`.
- Remove the entire `if (import.meta.client) { ... document.cookie = waldo_jwt=...max-age=0... }` block (lines 26-32) — the client cannot clear an httpOnly cookie; the server route does it.
- Replace `await strapiLogout();` with `await $fetch("/api/auth/logout", { method: "POST" });`.
- Keep all the store `$reset()`/`reset()` calls, the `window.google?.accounts?.id?.disableAutoSelect()` line, and `await navigateTo("/");`.
- After this, the user state should also be cleared: add `useSessionUser().value = null;` before `navigateTo("/")` (the server cleared the cookie; clear the client state too). Alternatively call `useSessionAuth().logout()` which both POSTs and nulls the user — but to avoid a double POST, prefer the explicit `$fetch` + `useSessionUser().value = null`.

Update `apps/website/tests/composables/useLogout.test.ts`:
- Replace the `useStrapiAuth.logout` mock with a `$fetch` mock; assert `$fetch` is called with `"/api/auth/logout"` and `{ method: "POST" }`.
- Assert all store resets still fire and `navigateTo("/")` is called.

No unused imports (Codacy).
  </action>
  <verify>
    <automated>cd apps/website && grep -q "/api/auth/logout" app/composables/useLogout.ts && ! grep -q "document.cookie" app/composables/useLogout.ts && ! grep -q "useStrapiAuth" app/composables/useLogout.ts && pnpm vitest run tests/composables/useLogout.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `app/composables/useLogout.ts` contains `/api/auth/logout` and does NOT contain `document.cookie` or `useStrapiAuth`
    - `tests/composables/useLogout.test.ts` asserts the POST to `/api/auth/logout` and passes
  </acceptance_criteria>
  <done>Logout goes through the server route; client cookie hack removed; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Middleware guards — drop useStrapiToken guard, fetchUser unconditional on client</name>
  <files>apps/website/app/middleware/auth.ts, apps/website/app/middleware/onboarding-guard.global.ts, apps/website/app/middleware/dashboard-guard.global.ts, apps/website/tests/middleware/dashboard-guard.test.ts</files>
  <read_first>
    - apps/website/app/middleware/auth.ts (entire file)
    - apps/website/app/middleware/onboarding-guard.global.ts (entire file)
    - apps/website/app/middleware/dashboard-guard.global.ts (entire file)
    - apps/website/tests/middleware/dashboard-guard.test.ts (the test to update)
    - .planning/phases/129-*/129-RESEARCH.md section "### Simplified Middleware Guards", Pitfall 1
    - STATE.md decisions 125-01 (dashboard-guard SSR fail-open) and 125-01 (onboarding /dashboard exemption) — preserve these behaviors
  </read_first>
  <action>
General transformation for all three guards (Pitfall 1 — httpOnly token is unreadable client-side, so the `useStrapiToken()` gate would permanently block fetchUser). Replace:
```
const token = useStrapiToken();
if (!token.value) return ...;   // or: if (token.value) { fetchUser() }
...
const { fetchUser } = useStrapiAuth();
await fetchUser();
```
with an unconditional client-side fetch:
```
if (import.meta.client) {
  const { fetchUser } = useSessionAuth();
  await fetchUser(); // 401 = anonymous; useSessionAuth sets user=null, no side effect
}
```
And change every `useStrapiUser()` → `useSessionUser()` in these three files.

`auth.ts` specifically:
- Keep the SSR-skip `if (import.meta.server) return;` (now a performance optimization; update/trim the long stale comment about the @nuxtjs/strapi plugin since that no longer applies — keep it short and accurate).
- After the (client) fetchUser, the existing `if (!user.value) { useCookie("redirect",...); return navigateTo("/login"); }` stays.

`onboarding-guard.global.ts`:
- Keep `AUTH_EXEMPT_PATHS` and the early exempt return.
- Remove the `useStrapiToken` block; replace with the client fetchUser pattern. Preserve the SSR fail-open (`if (import.meta.server) return;`) so SSR doesn't block before hydration, AND preserve the `/onboarding` startsWith allowance and manager bypass and `isProfileComplete()` logic exactly.

`dashboard-guard.global.ts`:
- Keep the `if (!to.path.startsWith("/dashboard")) return;` gate.
- Remove the `useStrapiToken` block. Replace `if (!token.value) return navigateTo("/login");` semantics with: on null user, client-fetch; if still null → `return navigateTo("/login")`.
- Preserve the SSR fail-open `if (!roleName) return;` (D-03) and the manager-only enforcement.
- Keep `useSessionUser<User>()` typing (import type { User } stays).

Update `apps/website/tests/middleware/dashboard-guard.test.ts`:
- Remove `useStrapiToken` stubs. Stub `useSessionUser` and `useSessionAuth` (fetchUser). Keep the assertions: null user → redirect to `/login`; non-manager role → redirect to `/`; manager → allowed; `to.path` not starting with `/dashboard` → no-op.
  </action>
  <verify>
    <automated>cd apps/website && ! grep -rq "useStrapiToken" app/middleware/ && ! grep -rq "useStrapiUser\|useStrapiAuth" app/middleware/ && grep -q "useSessionUser" app/middleware/dashboard-guard.global.ts && pnpm vitest run tests/middleware/dashboard-guard.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "useStrapiToken" app/middleware/` returns nothing
    - `grep -r "useStrapiUser\|useStrapiAuth" app/middleware/` returns nothing
    - All three guards contain `useSessionUser` (and the two that fetch contain `useSessionAuth`)
    - `tests/middleware/dashboard-guard.test.ts` passes with user-based (not token-based) stubs
  </acceptance_criteria>
  <done>All three guards are token-free and user-state-based; dashboard-guard test green; SSR fail-open + exemptions preserved.</done>
</task>

</tasks>

<verification>
- `grep -rn "useStrapiToken" apps/website/app` returns nothing.
- `grep -rn "setToken" apps/website/app` returns nothing.
- `pnpm --filter website vitest run tests/composables/useLogout.test.ts tests/middleware/dashboard-guard.test.ts` green.
- (End-to-end verify-code/upload/logout exercised by plan 06 checkpoints.)
</verification>

<success_criteria>
Every client token read/write outside OAuth is gone: verify-code and uploads rely on the proxy + server cookie, logout uses the server route, and all three guards are user-state-based with no token gate. `useStrapiToken` is fully eliminated from the app; logout and dashboard-guard tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-05-SUMMARY.md`
</output>
