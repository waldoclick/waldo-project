---
phase: 129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie
plan: 04
type: execute
wave: 2
depends_on: [129-01, 129-02, 129-03]
files_modified:
  - apps/website/app/composables/useProviders.ts
  - apps/website/app/components/LoginWithGoogle.vue
  - apps/website/app/pages/login/google.vue
  - apps/website/app/pages/login/facebook.vue
  - apps/website/app/plugins/google-one-tap.client.ts
autonomous: true
requirements: [OAUTH-POPUP-NOJWT, OAUTH-EXCHANGE-ROUTES, ONETAP-NO-SETTOKEN]
must_haves:
  truths:
    - "loginWithPopup resolves on { type: google-oauth-success } and returns no jwt"
    - "LoginWithGoogle.vue no longer calls setToken; it calls fetchUser after popup success"
    - "login/google.vue and login/facebook.vue POST to /api/auth/{provider}/exchange instead of authenticateProvider"
    - "google-one-tap plugin no longer calls setToken; the server route set the cookie; it reloads"
    - "Zero setToken / authenticateProvider references remain in these 5 files"
  artifacts:
    - path: "apps/website/app/composables/useProviders.ts"
      provides: "popup flow with no jwt + getProviderAuthenticationUrl from useSessionAuth"
      contains: "useSessionAuth"
    - path: "apps/website/app/pages/login/google.vue"
      provides: "exchange-route call"
      contains: "/api/auth/google/exchange"
  key_links:
    - from: "login/google.vue"
      to: "server/api/auth/google/exchange.post.ts"
      via: "$fetch POST { access_token }"
      pattern: "/api/auth/google/exchange"
    - from: "LoginWithGoogle.vue"
      to: "httpOnly cookie via popup callback"
      via: "loginWithPopup then fetchUser"
      pattern: "loginWithPopup"
---

<objective>
Migrate the five Google/Facebook OAuth logic files to the httpOnly model: the popup no longer carries a jwt (the Nitro popup callback set the cookie); `LoginWithGoogle.vue` drops `setToken` and calls `fetchUser`; the redirect pages POST to the new `/api/auth/{provider}/exchange` routes (which set the cookie) instead of `authenticateProvider`; and the One Tap plugin drops `setToken` (its server route set the cookie).

NOTE — knowingly half-migrated until cutover: these files now use `useSessionAuth`/`useSessionUser`, but the `@nuxtjs/strapi` module remains the active session writer until plan 06 activates `session.ts`. End-to-end OAuth login is therefore only verified by the human-verify checkpoints in plan 06. Acceptance here is grep/structure-based.

Purpose: Remove all client-side token handling from the OAuth surface.
Output: 5 migrated files; zero setToken/authenticateProvider in them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-CONTEXT.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-RESEARCH.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-01-PLAN.md
@.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-02-PLAN.md

<interfaces>
<!-- New composable (plan 01): useSessionAuth() -> { fetchUser, logout, getProviderAuthenticationUrl } -->
<!-- New routes (plan 02): POST /api/auth/google/exchange and /api/auth/facebook/exchange -> { user }, set cookie -->
<!-- New popup callback (plan 02): broadcasts { type: "google-oauth-success" } with NO jwt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useProviders.ts + LoginWithGoogle.vue — popup returns no jwt, drop setToken</name>
  <files>apps/website/app/composables/useProviders.ts, apps/website/app/components/LoginWithGoogle.vue</files>
  <read_first>
    - apps/website/app/composables/useProviders.ts (entire file — popup BroadcastChannel)
    - apps/website/app/components/LoginWithGoogle.vue (entire file — setToken usage line 24)
    - .planning/phases/129-*/129-RESEARCH.md sections "### useProviders.ts changes", "### LoginWithGoogle.vue — BroadcastChannel redesign", and Pitfall 5
  </read_first>
  <action>
In `apps/website/app/composables/useProviders.ts`:
1. Change the destructure source: `const { getProviderAuthenticationUrl } = useSessionAuth();` (was `useStrapiAuth()`).
2. Change `loginWithPopup` return type from `Promise<{ jwt: string }>` to `Promise<void>`. In `onPayload`, on `google-oauth-success` resolve with no value: `finish(() => resolve());`. Remove the `jwt?` field from the local `msg` type and remove the `msg.jwt` usage. The `google-oauth-error` reject path stays unchanged.

In `apps/website/app/components/LoginWithGoogle.vue`:
1. Change `const { setToken, fetchUser } = useStrapiAuth();` (line 24) to `const { fetchUser } = useSessionAuth();`.
2. Change the handler body from:
```
const { jwt } = await loginWithPopup("google");
setToken(jwt);
await fetchUser();
```
to:
```
await loginWithPopup("google"); // cookie set server-side by the Nitro popup callback
await fetchUser();              // proxy injects Authorization from the httpOnly cookie
```
Leave the rest of the component (post-login navigation, any `useStrapiUser` read) as-is — the mechanical sweep in plan 06 renames any remaining `useStrapiUser`.

No unused imports / no `_`-prefix (Codacy): remove `setToken` cleanly.
  </action>
  <verify>
    <automated>cd apps/website && grep -q "useSessionAuth" app/composables/useProviders.ts && ! grep -q "msg.jwt" app/composables/useProviders.ts && grep -q "useSessionAuth" app/components/LoginWithGoogle.vue && ! grep -q "setToken" app/components/LoginWithGoogle.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/composables/useProviders.ts` contains `useSessionAuth` and does NOT contain `msg.jwt`
    - `loginWithPopup` return type is `Promise<void>` (no `{ jwt: string }`)
    - `app/components/LoginWithGoogle.vue` contains `useSessionAuth` and does NOT contain `setToken`
  </acceptance_criteria>
  <done>Popup resolves with no jwt; LoginWithGoogle drops setToken and calls fetchUser via useSessionAuth.</done>
</task>

<task type="auto">
  <name>Task 2: login/google.vue + login/facebook.vue — exchange routes replace authenticateProvider</name>
  <files>apps/website/app/pages/login/google.vue, apps/website/app/pages/login/facebook.vue</files>
  <read_first>
    - apps/website/app/pages/login/google.vue (entire file — authenticateProvider usage)
    - apps/website/app/pages/login/facebook.vue (entire file)
    - apps/website/app/types/user.ts (User type for the { user } response)
    - .planning/phases/129-*/129-RESEARCH.md section "### login/google.vue and login/facebook.vue — Exchange route replaces authenticateProvider"
    - .planning/phases/129-*/129-02-PLAN.md Task 3 (the exchange route contract: POST { access_token } -> { user })
  </read_first>
  <action>
In `apps/website/app/pages/login/google.vue`:
1. Remove `const { authenticateProvider } = useStrapiAuth();` (line 19).
2. Replace the `authenticateProvider("google", ...)` call with a POST to the exchange route:
```
const result = await $fetch<{ user: import("@/types/user").User }>(
  "/api/auth/google/exchange",
  { method: "POST", body: { access_token: String(route.query.access_token || "") } },
);
// Cookie is set server-side. Use result.user / fetchUser for the navigation logic.
```
Preserve the existing post-auth navigation block (meStore.reset(), isProfileComplete(), referer/onboarding redirects, login("google") analytics) — only the token-exchange line changes. Where the old code branched on `if (response)`, branch on `if (result?.user)`.

In `apps/website/app/pages/login/facebook.vue`:
- Same transformation, calling `/api/auth/facebook/exchange`. Preserve its existing navigation logic.
- NOTE: Facebook OAuth credentials may be inactive in Strapi (Research Open Question #3). The route wiring is mechanical parity; whether Facebook login completes end-to-end is verified by the plan 06 checkpoint. Do not block on it.

No unused imports (remove `authenticateProvider`/`useStrapiAuth` if now unused).
  </action>
  <verify>
    <automated>cd apps/website && grep -q "/api/auth/google/exchange" app/pages/login/google.vue && grep -q "/api/auth/facebook/exchange" app/pages/login/facebook.vue && ! grep -q "authenticateProvider" app/pages/login/google.vue && ! grep -q "authenticateProvider" app/pages/login/facebook.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/pages/login/google.vue` contains `/api/auth/google/exchange` and does NOT contain `authenticateProvider`
    - `app/pages/login/facebook.vue` contains `/api/auth/facebook/exchange` and does NOT contain `authenticateProvider`
    - Neither page contains `useStrapiAuth` after the change
  </acceptance_criteria>
  <done>Both redirect pages POST to the exchange routes; authenticateProvider removed.</done>
</task>

<task type="auto">
  <name>Task 3: google-one-tap.client.ts — drop setToken, rely on server cookie</name>
  <files>apps/website/app/plugins/google-one-tap.client.ts</files>
  <read_first>
    - apps/website/app/plugins/google-one-tap.client.ts (entire file — setToken at line 39, callback at line ~52)
    - .planning/phases/129-*/129-RESEARCH.md section "### Google One Tap Plugin — Remove setToken"
    - .planning/phases/129-*/129-02-PLAN.md Task 2 (google-one-tap.post.ts sets the cookie and returns { user })
  </read_first>
  <action>
In `apps/website/app/plugins/google-one-tap.client.ts`:
1. Remove `useStrapiAuth` from the `#imports` import block and remove `const { setToken } = useStrapiAuth();` (line ~39).
2. Change the auth-state guard `const user = useStrapiUser();` to `const user = useSessionUser();` (so the plugin checks the new state). Keep the `if (user.value) return;` guard.
3. Change the callback body from:
```
const result = await client<{ jwt: string; user: unknown }>(
  "auth/google-one-tap",
  { method: "POST", body: { credential: response.credential } },
);
setToken(result.jwt);
reloadNuxtApp();
```
to:
```
await client("auth/google-one-tap", {
  method: "POST",
  body: { credential: response.credential },
});
// The Nitro route /api/auth/google-one-tap already set the httpOnly cookie.
reloadNuxtApp();
```
The `client("auth/google-one-tap", { method: "POST" })` call routes through `useApiClient`, which injects `X-Recaptcha-Token`; it matches the dedicated Nitro route (plan 02) BEFORE the catch-all, where reCAPTCHA is re-verified.

No unused imports (Codacy): remove `useStrapiAuth` import; drop the now-unused `{ jwt: string; user: unknown }` generic.
  </action>
  <verify>
    <automated>cd apps/website && ! grep -q "setToken" app/plugins/google-one-tap.client.ts && ! grep -q "useStrapiAuth" app/plugins/google-one-tap.client.ts && grep -q "useSessionUser" app/plugins/google-one-tap.client.ts && grep -q "auth/google-one-tap" app/plugins/google-one-tap.client.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/plugins/google-one-tap.client.ts` does NOT contain `setToken` or `useStrapiAuth`
    - `app/plugins/google-one-tap.client.ts` contains `useSessionUser` and still POSTs to `auth/google-one-tap`
    - The callback no longer destructures `jwt` from the client response
  </acceptance_criteria>
  <done>One Tap plugin relies on the server-set cookie; setToken and useStrapiAuth removed.</done>
</task>

</tasks>

<verification>
- `grep -rn "setToken\|authenticateProvider" app/composables/useProviders.ts app/components/LoginWithGoogle.vue app/pages/login/google.vue app/pages/login/facebook.vue app/plugins/google-one-tap.client.ts` returns nothing.
- `grep -rn "/api/auth/google/exchange\|/api/auth/facebook/exchange" app/pages/login/` matches both pages.
- (End-to-end OAuth is exercised by plan 06 human-verify checkpoints — the module is still active here.)
</verification>

<success_criteria>
All OAuth client surface (popup, redirect exchange, One Tap) is free of client-side token handling: popup carries no jwt, redirect pages use exchange routes, One Tap relies on the server cookie. Zero setToken/authenticateProvider remain in the five files.
</success_criteria>

<output>
After completion, create `.planning/phases/129-eliminate-nuxtjs-strapi-and-centralize-session-auth-and-http-through-the-proxy-with-httponly-cookie/129-04-SUMMARY.md`
</output>
