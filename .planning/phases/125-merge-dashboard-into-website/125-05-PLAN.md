---
phase: 125-merge-dashboard-into-website
plan: 05
type: execute
wave: 5
depends_on: [125-04]
files_modified:
  - apps/dashboard/app/pages/
  - apps/website/app/pages/dashboard/
autonomous: true
requirements: [PAGE-01, PAGE-02]
must_haves:
  truths:
    - "All surviving dashboard pages live under apps/website/app/pages/dashboard/"
    - "Dashboard auth pages and dev.vue are dropped (not migrated)"
    - "Every migrated page uses definePageMeta({ layout: 'dashboard' })"
    - "Internal /auth/* links in migrated pages point to website's auth routes"
  artifacts:
    - path: "apps/website/app/pages/dashboard/index.vue"
      provides: "Dashboard home page"
    - path: "apps/website/app/pages/dashboard/ads/[id].vue"
      provides: "Dashboard ad detail page at /dashboard/ads/:id"
  key_links:
    - from: "apps/website/app/pages/dashboard"
      to: "layout: 'dashboard'"
      via: "definePageMeta"
      pattern: "layout.*dashboard"
---

<objective>
Move all 73 dashboard pages into apps/website/app/pages/dashboard/, dropping the 4 auth pages and dev.vue, ensuring every surviving page declares the dashboard layout and fixing internal auth links.

Purpose: D-08 — dashboard accessible at /dashboard/**. The Nuxt file-based router maps pages/dashboard/ads/[id].vue to /dashboard/ads/:id. Dashboard auth pages are dropped (D-01: website's login covers all users).
Output: ~68 pages under apps/website/app/pages/dashboard/, auth pages + dev.vue dropped, layout meta set, auth links fixed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md
@.planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md

<interfaces>
Pages to DROP (do NOT migrate — D-01, D-11):
  auth/login.vue, auth/forgot-password.vue, auth/reset-password.vue, auth/verify-code.vue, dev.vue

Internal link rewrites (RESEARCH.md Wave 1):
  /auth/login           → /login
  /auth/forgot-password → /recuperar-contrasena
  /auth/reset-password  → /restablecer-contrasena
  (any /auth/* dashboard-internal link → website equivalent)

All 73 dashboard pages ALREADY have definePageMeta — verify layout is set to 'dashboard'
(some may use the default layout implicitly; set layout: 'dashboard' explicitly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv dashboard pages tree into pages/dashboard/, drop auth + dev pages</name>
  <files>apps/dashboard/app/pages/, apps/website/app/pages/dashboard/</files>
  <read_first>
    - apps/dashboard/app/pages/ (full tree listing — source of all 73 pages)
    - apps/website/app/pages/ (confirm no existing dashboard/ subdir to clash)
    - .planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md (D-08 pages routing, D-01/D-11 drops)
  </read_first>
  <action>
    1. Create the target directory: `mkdir -p apps/website/app/pages/dashboard`
    2. Move the entire dashboard pages tree preserving subfolder structure, using git mv per top-level entry (CLAUDE.md mandates git mv for page moves):
       `git mv apps/dashboard/app/pages/<entry> apps/website/app/pages/dashboard/<entry>`
       for each top-level file/dir (ads/, orders/, users/, account/, maintenance/, integrations/, index.vue, etc.).
    3. DROP (delete via git rm, do not migrate): the auth pages and dev page. After moving, remove them from their new location:
       `git rm apps/website/app/pages/dashboard/auth/login.vue apps/website/app/pages/dashboard/auth/forgot-password.vue apps/website/app/pages/dashboard/auth/reset-password.vue apps/website/app/pages/dashboard/auth/verify-code.vue`
       `git rm apps/website/app/pages/dashboard/dev.vue`
       Remove the now-empty auth/ directory if nothing else remains in it.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "pages under dashboard:" && find apps/website/app/pages/dashboard -name "*.vue" | wc -l && echo "dropped auth/dev (must be 0):" && ls apps/website/app/pages/dashboard/auth/login.vue apps/website/app/pages/dashboard/dev.vue 2>/dev/null | wc -l && echo "remaining dashboard pages (must be 0):" && find apps/dashboard/app/pages -name "*.vue" 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find apps/website/app/pages/dashboard -name "*.vue" | wc -l` returns 68 (73 minus 5 dropped)
    - `ls apps/website/app/pages/dashboard/auth/login.vue 2>/dev/null` returns nothing
    - `ls apps/website/app/pages/dashboard/dev.vue 2>/dev/null` returns nothing
    - `find apps/dashboard/app/pages -name "*.vue" 2>/dev/null | wc -l` returns 0
    - `test -f apps/website/app/pages/dashboard/index.vue` succeeds
  </acceptance_criteria>
  <done>68 pages under apps/website/app/pages/dashboard/; 4 auth pages + dev.vue dropped; dashboard pages tree empty.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure dashboard layout meta + fix internal auth links</name>
  <files>apps/website/app/pages/dashboard/</files>
  <read_first>
    - apps/website/app/pages/dashboard/index.vue (sample migrated page — verify definePageMeta layout value)
    - apps/website/app/layouts/dashboard.vue (the layout these pages reference — from Plan 01)
  </read_first>
  <action>
    1. Verify every page under apps/website/app/pages/dashboard/ declares `definePageMeta({ layout: "dashboard" })`. For any page whose definePageMeta does not set layout (or sets a different one like "default"), add/correct `layout: "dashboard"`. All 73 originals had definePageMeta — most likely already correct; correct any that diverge.
    2. Fix internal auth links across the migrated pages (and any migrated dashboard components in apps/website/app/components that link to /auth/*):
       `grep -rln "/auth/login\|/auth/forgot-password\|/auth/reset-password\|/auth/verify-code" apps/website/app/pages/dashboard apps/website/app/components`
       Rewrite per mapping: /auth/login → /login, /auth/forgot-password → /recuperar-contrasena, /auth/reset-password → /restablecer-contrasena. Apply to NuxtLink `to`, navigateTo(), and any href.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "pages without dashboard layout (must be 0):" && grep -rL "layout:\s*[\"']dashboard[\"']" apps/website/app/pages/dashboard --include="*.vue" | wc -l && echo "stale /auth links (must be 0):" && grep -rl "/auth/login\|/auth/forgot-password\|/auth/reset-password\|/auth/verify-code" apps/website/app/pages/dashboard apps/website/app/components | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rL "layout:\s*[\"']dashboard[\"']" apps/website/app/pages/dashboard --include="*.vue" | wc -l` returns 0 (every page sets dashboard layout)
    - `grep -rl "/auth/login\|/auth/forgot-password\|/auth/reset-password\|/auth/verify-code" apps/website/app/pages/dashboard apps/website/app/components` returns nothing
    - `grep -rl "to=\"/login\"\|navigateTo(\"/login\")" apps/website/app/pages/dashboard | wc -l` returns >= 1 (rewrites landed where auth links existed)
  </acceptance_criteria>
  <done>Every migrated page uses the dashboard layout; all internal /auth/* links rewritten to website auth routes.</done>
</task>

</tasks>

<verification>
- `find apps/website/app/pages/dashboard -name "*.vue" | wc -l` returns 68
- `find apps/dashboard/app/pages -name "*.vue"` returns nothing
- No page under pages/dashboard lacks `layout: "dashboard"`
- No stale /auth/* links in migrated pages/components
- (Full typecheck is Plan 06 Task 2 — not gated here; pages may still have type errors from the move)
</verification>

<success_criteria>
- 68 dashboard pages under pages/dashboard/
- Auth pages + dev.vue dropped
- Dashboard layout meta on all pages
- Internal auth links fixed
</success_criteria>

<output>
After completion, create `.planning/phases/125-merge-dashboard-into-website/125-05-SUMMARY.md`
</output>
