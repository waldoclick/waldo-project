---
phase: 125-merge-dashboard-into-website
plan: 03
type: execute
wave: 3
depends_on: [125-02]
files_modified:
  - apps/dashboard/app/components/
  - apps/website/app/components/
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "The colliding dashboard components are renamed with Dashboard suffix before any copy"
    - "All internal references to renamed components are updated in dashboard pages/components/layout"
    - "Non-colliding dashboard components are copied into website without name collision"
    - "Only IconX.vue collides between the two icons/ folders (identical, kept)"
  artifacts:
    - path: "apps/website/app/components/HeroDashboard.vue"
      provides: "Renamed example of a collision-resolved component"
    - path: "apps/website/app/components/icons/iconBetterStack.vue"
      provides: "Copied dashboard-exclusive icon"
  key_links:
    - from: "apps/website/app/components"
      to: "no duplicate basenames except IconX.vue"
      via: "rename-before-copy"
      pattern: "Dashboard.vue"
---

<objective>
Migrate all 95 dashboard flat components plus the 3 dashboard-exclusive icons into website, resolving the name collisions by renaming dashboard versions with the Dashboard suffix BEFORE copying.

Purpose: Nuxt 4 auto-import is last-writer-wins with NO build error on collision (RESEARCH.md Pitfall 3). A colliding component copied under its original name silently shadows website's version. Rename-before-copy is the load-bearing ordering constraint of this plan.
Output: ~27 renamed components + ~67 copied non-colliding components + 3 copied icons in apps/website/app/components/, with all internal references updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md
@.planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md

<interfaces>
The collision list (D-05, locked — each colliding flat component gets `Dashboard` suffix):
AvatarDefault, BreadcrumbsDefault, CardInfo, FooterDefault, FormDev, FormForgotPassword,
FormLogin, FormPack, FormPassword, FormResetPassword, FormVerifyCode, GalleryDefault,
HeaderDefault, HeroDefault, IntroduceAuth, LightboxAdblock, LightboxCookies, LightboxRazon,
LoadingDefault, LogoWhite, MenuDefault, MenuMobile, PacksDefault, PictureDefault,
PoliciesDefault, SearchDefault, TermsDefault

(D-17 correction: the trailing "IconX" in CONTEXT.md's list refers to icons/IconX.vue, which is
byte-identical in both apps and is NOT renamed. The authoritative rename set is computed by diff
in Task 1 — do not trust a hardcoded count.)

Suffix rule: append `Dashboard` to the FULL PascalCase name.
  HeroDefault → HeroDashboard, HeaderDefault → HeaderDashboard, FormLogin → FormLoginDashboard,
  CardInfo → CardInfoDashboard, IntroduceAuth → IntroduceAuthDashboard, LogoWhite → LogoWhiteDashboard.

icons/ subfolder (RESEARCH.md lines 108-114):
- Copy dashboard's IconGtm.vue, iconBetterStack.vue, iconCloudflare.vue into website/components/icons/
- SKIP IconX.vue (byte-identical in both apps — website's kept)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute collision set and rename all colliding dashboard components</name>
  <files>apps/dashboard/app/components/</files>
  <read_first>
    - apps/dashboard/app/components/ (full flat listing — source files)
    - apps/website/app/components/ (full flat listing — collision targets)
    - .planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md (D-05 collision list, D-17 IconX correction)
  </read_first>
  <action>
    1. Compute the authoritative collision set with a diff (do not trust hardcoded counts):
       `comm -12 <(ls apps/dashboard/app/components/*.vue | xargs -n1 basename | sort) <(ls apps/website/app/components/*.vue | xargs -n1 basename | sort)`
       This lists the flat-file collisions. Cross-check against the D-05 list. Use the computed set as source of truth.

    2. For each colliding component, rename the dashboard file with the `Dashboard` suffix using `git mv` (CLAUDE.md mandates git mv for component renames). Example:
       `git mv apps/dashboard/app/components/HeroDefault.vue apps/dashboard/app/components/HeroDashboard.vue`
       Apply the suffix to the FULL PascalCase name per the mapping above (CardInfo → CardInfoDashboard).

    3. Update `defineOptions({ name: "..." })` inside each renamed file to match the new name if present.

    4. Update ALL internal references across apps/dashboard/app/ (pages, other components, the layout — note: dashboard.vue layout was already copied to website in Plan 01; update the website copy too if it references any renamed component) in the SAME commit. Find references with:
       `grep -rln "HeroDefault\|HeaderDefault\|CardInfo\|FormLogin" apps/dashboard/app apps/website/app/layouts/dashboard.vue`
       Rewrite each `<HeroDefault` → `<HeroDashboard`, closing tags, and explicit imports for every component in the collision set.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "Remaining flat collisions (must be empty):" && comm -12 <(ls apps/dashboard/app/components/*.vue 2>/dev/null | xargs -n1 basename | sort) <(ls apps/website/app/components/*.vue | xargs -n1 basename | sort)</automated>
  </verify>
  <acceptance_criteria>
    - `comm -12 <(ls apps/dashboard/app/components/*.vue | xargs -n1 basename | sort) <(ls apps/website/app/components/*.vue | xargs -n1 basename | sort)` returns NOTHING
    - `ls apps/dashboard/app/components/HeroDashboard.vue apps/dashboard/app/components/HeaderDashboard.vue apps/dashboard/app/components/FormLoginDashboard.vue` all exist
    - `grep -rl "<HeroDefault\b\|<HeaderDefault\b\|<FormLogin\b" apps/dashboard/app/pages apps/dashboard/app/components` returns nothing
    - `git -C /home/gab/Code/waldo-project status --short apps/dashboard/app/components | grep -c "^R"` returns >= 27
  </acceptance_criteria>
  <done>Every flat-file collision renamed with Dashboard suffix via git mv; all internal references updated; the comm -12 collision check returns empty.</done>
</task>

<task type="auto">
  <name>Task 2: Copy non-colliding components + dashboard-exclusive icons into website</name>
  <files>apps/website/app/components/</files>
  <read_first>
    - apps/dashboard/app/components/ (post-rename listing — everything here now copies cleanly)
    - apps/website/app/components/icons/ (current: IconWhatsApp.vue, IconX.vue)
    - apps/dashboard/app/components/icons/ (IconGtm.vue, IconX.vue, iconBetterStack.vue, iconCloudflare.vue)
  </read_first>
  <action>
    After Task 1, NO flat-file collisions remain, so moving is safe.
    1. Move ALL flat .vue files from apps/dashboard/app/components/ into apps/website/app/components/ using git mv (preserves history; dashboard dir is deleted in Plan 07):
       `git mv apps/dashboard/app/components/*.vue apps/website/app/components/`
       If glob fails on count, iterate per file.

    2. Icons subfolder — move ONLY the 3 dashboard-exclusive icons, skip IconX.vue:
       `git mv apps/dashboard/app/components/icons/IconGtm.vue apps/website/app/components/icons/`
       `git mv apps/dashboard/app/components/icons/iconBetterStack.vue apps/website/app/components/icons/`
       `git mv apps/dashboard/app/components/icons/iconCloudflare.vue apps/website/app/components/icons/`
       Leave apps/dashboard/app/components/icons/IconX.vue in place (identical to website's — removed with the directory in Plan 07).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "Final collision check (only IconX expected):" && comm -12 <(find apps/dashboard/app/components -name "*.vue" 2>/dev/null | xargs -n1 basename | sort -u) <(find apps/website/app/components -name "*.vue" | xargs -n1 basename | sort -u) && echo "--- website icons:" && ls apps/website/app/components/icons/</automated>
  </verify>
  <acceptance_criteria>
    - `comm -12 <(find apps/dashboard/app/components -name "*.vue" | xargs -n1 basename | sort -u) <(find apps/website/app/components -name "*.vue" | xargs -n1 basename | sort -u)` returns exactly `IconX.vue` (and nothing else)
    - `ls apps/dashboard/app/components/*.vue 2>/dev/null | wc -l` returns 0 (all flat components moved out)
    - `ls apps/website/app/components/icons/IconGtm.vue apps/website/app/components/icons/iconBetterStack.vue apps/website/app/components/icons/iconCloudflare.vue` all exist
    - `ls apps/website/app/components/HeroDashboard.vue apps/website/app/components/HeaderDashboard.vue` exist (renamed components landed in website)
  </acceptance_criteria>
  <done>All flat dashboard components moved into website with zero collisions (except identical IconX.vue); 3 dashboard-exclusive icons copied; IconX.vue not duplicated.</done>
</task>

</tasks>

<verification>
- `comm -12` of all component basenames between dashboard and website returns only `IconX.vue`
- `ls apps/dashboard/app/components/*.vue | wc -l` returns 0
- Renamed components (Dashboard suffix) present in apps/website/app/components/
- (Typecheck deferred: session-composable and type errors are fixed in Plans 04-07; do not run full typecheck as a gate here — component bodies still reference useSessionX)
</verification>

<success_criteria>
- All colliding components renamed with Dashboard suffix before copy (zero silent shadowing)
- All non-colliding components moved into website
- 3 dashboard-exclusive icons copied, IconX.vue skipped
- Internal references to renamed components updated
</success_criteria>

<output>
After completion, create `.planning/phases/125-merge-dashboard-into-website/125-03-SUMMARY.md`
</output>
