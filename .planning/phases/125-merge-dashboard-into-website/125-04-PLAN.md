---
phase: 125-merge-dashboard-into-website
plan: 04
type: execute
wave: 4
depends_on: [125-03]
files_modified:
  - apps/website/app/types/
  - apps/website/app/composables/useExportCsv.ts
  - apps/website/app/composables/useServices.ts
  - apps/website/app/composables/useSlugify.ts
  - apps/website/app/components/
  - apps/website/app/stores/search.store.ts
  - apps/website/app/stores/settings.store.ts
  - apps/website/nuxt.config.ts
autonomous: true
requirements: [SESS-01, SESS-02, COMPOSABLE-01, TYPE-02]
must_haves:
  truths:
    - "Dashboard-exclusive TypeScript types are copied into website before typecheck gates run"
    - "Zero useSessionUser/Token/Auth/Client references remain in the website app directory"
    - "All migrated dashboard components use useStrapiUser/Token/Auth + useApiClient"
    - "useExportCsv, useServices, useSlugify composables exist in website"
    - "The 4 dashboard session composables are NOT copied into website"
  artifacts:
    - path: "apps/website/app/types/order.ts"
      provides: "Dashboard-exclusive order type"
    - path: "apps/website/app/composables/useExportCsv.ts"
      provides: "CSV export composable"
    - path: "apps/website/app/composables/useServices.ts"
      provides: "Services list composable (reads websiteUrl)"
    - path: "apps/website/app/composables/useSlugify.ts"
      provides: "slugify wrapper composable"
  key_links:
    - from: "apps/website/app/components (migrated)"
      to: "useStrapiUser / useApiClient"
      via: "session composable replacement"
      pattern: "useStrapiUser"
---

<objective>
Copy the dashboard-exclusive TypeScript types and the 3 dashboard-exclusive composables into website, then replace every dashboard session-composable call (useSessionUser/Token/Auth/Client) across the merged components and stores with website's @nuxtjs/strapi equivalents.

Purpose: D-04 — website's session system wins; the 4 custom session composables are dropped, not copied. Types must land BEFORE the Plan 06 typecheck gate: 10 migrated pages/components import dashboard-exclusive types (order, subscription-pro, subscription-payment, better-stack) — without them, typecheck throws "Cannot find module @/types/order". Types are zero-dependency, so they copy cleanly here.
Output: 4 dashboard-exclusive types + 3 composables in website; all useSessionX references swapped to Strapi equivalents; runtimeConfig.public.websiteUrl added for useServices.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md
@.planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md

<interfaces>
Dashboard-exclusive types to copy (apps/dashboard/app/types → apps/website/app/types) — VERIFIED exclusive:
  better-stack.ts, order.ts, subscription-payment.ts, subscription-pro.ts
  Do NOT copy: user.ts (website's user.d.ts wins — RESEARCH.md Pitfall 2), app.d.ts, plugins.d.ts,
  strapi.d.ts, ad.ts, category.ts, pack.ts (website already has equivalents).

Session replacement mapping (D-04):
  useSessionUser<User>()  → useStrapiUser<User>()
  useSessionToken()       → useStrapiToken()
  useSessionAuth()        → useStrapiAuth()
  useSessionClient()      → useApiClient()   (website's existing composable)

DROP (do NOT copy into website): useSessionUser.ts, useSessionToken.ts, useSessionAuth.ts,
useSessionClient.ts, and dashboard's session.ts plugin.

COPY into website (no website equivalent): useExportCsv.ts, useServices.ts, useSlugify.ts.

These already exist in website — website's version WINS, dashboard's is dropped:
useApiClient.ts, useImage.ts, useLogger.ts, useLogout.ts, useRut.ts, useSanitize.ts, useSweetAlert2.ts

qs caveat (RESEARCH.md Pitfall 4): useSessionClient explicitly qs.stringify'd params. When swapping
to useApiClient, components passing deeply-nested filter params may serialize differently. For any
component built around qs bracket notation, keep an explicit qs.stringify before passing params.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy dashboard-exclusive types into website</name>
  <files>apps/website/app/types/</files>
  <read_first>
    - apps/dashboard/app/types/ (source — order.ts, subscription-payment.ts, subscription-pro.ts, better-stack.ts)
    - apps/website/app/types/ (existing — do NOT overwrite; user.d.ts wins)
    - .planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md (Pitfall 2 User shape)
  </read_first>
  <action>
    Confirm the exclusive set by diff:
      `comm -23 <(ls apps/dashboard/app/types | sed 's/\.ts$//;s/\.d$//' | sort) <(ls apps/website/app/types | sed 's/\.d\.ts$//;s/\.ts$//' | sort)`
    The verified exclusives are: better-stack, order, subscription-payment, subscription-pro.
    Move each via git mv into apps/website/app/types/:
      `git mv apps/dashboard/app/types/order.ts apps/website/app/types/`
      `git mv apps/dashboard/app/types/subscription-payment.ts apps/website/app/types/`
      `git mv apps/dashboard/app/types/subscription-pro.ts apps/website/app/types/`
      `git mv apps/dashboard/app/types/better-stack.ts apps/website/app/types/`
    Do NOT copy dashboard's user.ts (website's user.d.ts wins). Do NOT copy ad.ts, category.ts, pack.ts, app.d.ts, plugins.d.ts, strapi.d.ts (website has equivalents).
    If a copied type re-imports another dashboard type that is NOT exclusive (e.g. imports User), update the import to resolve against website's type (auto-provided / website's path).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && ls apps/website/app/types/order.ts apps/website/app/types/subscription-payment.ts apps/website/app/types/subscription-pro.ts apps/website/app/types/better-stack.ts && echo "user.ts not copied:" && ls apps/website/app/types/user.ts 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/website/app/types/order.ts apps/website/app/types/subscription-payment.ts apps/website/app/types/subscription-pro.ts apps/website/app/types/better-stack.ts` all exist
    - `ls apps/website/app/types/user.ts 2>/dev/null` returns nothing (dashboard user.ts NOT copied)
    - `grep -rl "@/types/order\|@/types/subscription-pro\|@/types/subscription-payment\|@/types/better-stack" apps/website/app/pages/dashboard apps/website/app/components | wc -l` returns >= 1 (consumers present and types now resolvable)
  </acceptance_criteria>
  <done>The 4 dashboard-exclusive types moved into website; user.ts not copied; consuming pages/components can now resolve them.</done>
</task>

<task type="auto">
  <name>Task 2: Copy useExportCsv, useServices, useSlugify + add websiteUrl runtimeConfig</name>
  <files>apps/website/app/composables/useExportCsv.ts, apps/website/app/composables/useServices.ts, apps/website/app/composables/useSlugify.ts, apps/website/nuxt.config.ts</files>
  <read_first>
    - apps/dashboard/app/composables/useExportCsv.ts (source — uses useApiClient, useSettingsStore, useSweetAlert2)
    - apps/dashboard/app/composables/useServices.ts (source — reads config.websiteUrl, default https://waldo.click)
    - apps/dashboard/app/composables/useSlugify.ts (source — wraps slugify package)
    - apps/website/nuxt.config.ts (runtimeConfig.public at ~line 345 — websiteUrl is NOT present, must add)
  </read_first>
  <action>
    1. Move the 3 composables via git mv (history preservation):
       `git mv apps/dashboard/app/composables/useExportCsv.ts apps/website/app/composables/`
       `git mv apps/dashboard/app/composables/useServices.ts apps/website/app/composables/`
       `git mv apps/dashboard/app/composables/useSlugify.ts apps/website/app/composables/`
    2. In each moved composable, swap any useSessionX → useStrapiX/useApiClient per the mapping. useExportCsv already uses useApiClient (no session swap needed there) — verify. Remove any unused imports (CLAUDE.md).
    3. useServices reads `config.websiteUrl`. Add to apps/website/nuxt.config.ts runtimeConfig.public:
       `websiteUrl: process.env.WEBSITE_URL || "https://waldo.click",`
       (placed alongside the existing public keys like dashboardUrl at ~line 348).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && ls apps/website/app/composables/useExportCsv.ts apps/website/app/composables/useServices.ts apps/website/app/composables/useSlugify.ts && grep "websiteUrl" apps/website/nuxt.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/website/app/composables/useExportCsv.ts && test -f apps/website/app/composables/useServices.ts && test -f apps/website/app/composables/useSlugify.ts` succeeds
    - `ls apps/dashboard/app/composables/useExportCsv.ts 2>/dev/null` returns nothing (moved out)
    - `grep "websiteUrl:" apps/website/nuxt.config.ts` matches
    - `grep -c "useSession" apps/website/app/composables/useExportCsv.ts apps/website/app/composables/useServices.ts apps/website/app/composables/useSlugify.ts` returns 0
  </acceptance_criteria>
  <done>3 dashboard-exclusive composables moved into website with session calls swapped; websiteUrl added to runtimeConfig.public.</done>
</task>

<task type="auto">
  <name>Task 3: Replace all useSessionX references across migrated components and stores</name>
  <files>apps/website/app/components/, apps/website/app/stores/search.store.ts, apps/website/app/stores/settings.store.ts, apps/website/app/layouts/dashboard.vue</files>
  <read_first>
    - apps/website/app/composables/useApiClient.ts (target replacement for useSessionClient — confirm signature)
    - apps/dashboard/app/composables/useSessionClient.ts (source — note qs.stringify usage to preserve where needed)
    - apps/website/app/components/ (migrated dashboard components — grep for useSessionX usage)
  </read_first>
  <action>
    Find every remaining useSessionX reference in the website app dir (post component-migration):
      `grep -rln "useSessionUser\|useSessionToken\|useSessionAuth\|useSessionClient" apps/website/app`
    For each file, apply the mapping (D-04):
      `useSessionUser<User>()` → `useStrapiUser<User>()`
      `useSessionToken()` → `useStrapiToken()`
      `useSessionAuth()` → `useStrapiAuth()`
      `useSessionClient()` → `useApiClient()`
    Update any explicit import lines (e.g. `import { useSessionClient } from "#imports"` → use the Strapi equivalent or rely on auto-import). Remove now-unused imports (CLAUDE.md — no unused imports).
    qs caveat (RESEARCH.md Pitfall 4): for any component that passed deeply-nested `filters` params through useSessionClient with explicit qs serialization, keep the qs.stringify call and pass the serialized string — do not assume useApiClient serializes identically. Flag such components in the SUMMARY for the manual smoke test.
    Replace the dashboard User type import: any `import type { User } from "@/types/user"` that resolved to dashboard's type now resolves to website's type (auto-provided). Remove the explicit import if it causes a shape mismatch on role.type (RESEARCH.md Pitfall 2); website's User auto-import covers it.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "Remaining useSessionX (must be 0):" && grep -rc "useSessionUser\|useSessionToken\|useSessionAuth\|useSessionClient" apps/website/app | grep -v ":0" | head; echo "exit-marker"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "useSessionUser\|useSessionToken\|useSessionAuth\|useSessionClient" apps/website/app` returns NOTHING
    - `grep -rl "useStrapiUser\|useStrapiToken\|useStrapiAuth\|useApiClient" apps/website/app/components | wc -l` returns > 0 (replacements landed)
    - `ls apps/website/app/composables/useSession*.ts 2>/dev/null | wc -l` returns 0 (session composables NOT in website)
  </acceptance_criteria>
  <done>Zero useSessionX references remain anywhere in apps/website/app; all swapped to Strapi composables/useApiClient; the 4 session composables not present in website.</done>
</task>

</tasks>

<verification>
- `ls apps/website/app/types/order.ts apps/website/app/types/subscription-pro.ts apps/website/app/types/subscription-payment.ts apps/website/app/types/better-stack.ts` all exist
- `grep -rl "useSessionUser\|useSessionToken\|useSessionAuth\|useSessionClient" apps/website/app` returns nothing
- `ls apps/website/app/composables/useSession*.ts 2>/dev/null` returns nothing
- 3 dashboard-exclusive composables present in website
- (Full typecheck remediation is Plan 06 Task 2 — type errors from the move are fixed there; types now resolve)
</verification>

<success_criteria>
- 4 dashboard-exclusive types copied into website (before Plan 06 typecheck gate)
- useExportCsv, useServices, useSlugify in website
- Zero useSessionX references in website app dir
- websiteUrl in runtimeConfig.public
- qs-serialization-sensitive components flagged for manual smoke test in SUMMARY
</success_criteria>

<output>
After completion, create `.planning/phases/125-merge-dashboard-into-website/125-04-SUMMARY.md`
</output>
