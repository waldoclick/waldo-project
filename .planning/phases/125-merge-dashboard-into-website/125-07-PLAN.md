---
phase: 125-merge-dashboard-into-website
plan: 07
type: execute
wave: 7
depends_on: [125-06]
files_modified:
  - apps/website/app/components/MenuUser.vue
  - apps/strapi/src/extensions/users-permissions/controllers/authController.ts
  - apps/strapi/src/api/ad/content-types/ad/lifecycles.ts
  - apps/strapi/src/api/payment/services/ad.service.ts
  - apps/strapi/src/api/payment/services/free-ad.service.ts
  - apps/strapi/config/middlewares.ts
  - apps/website/app/components/FormForgotPasswordDashboard.vue
  - turbo.json
  - package.json
autonomous: true
requirements: [RUNTIME-01, CLEANUP-01]
must_haves:
  truths:
    - "MenuUser.vue links to /dashboard via internal NuxtLink (not external dashboardUrl href)"
    - "Strapi email/reset URLs point to FRONTEND_URL with /dashboard/ prefix where applicable"
    - "apps/dashboard removed from turbo.json and root package.json workspaces"
    - "apps/dashboard directory deleted"
    - "Final website typecheck + test suite + build all green"
  artifacts:
    - path: "apps/website/app/components/MenuUser.vue"
      provides: "Internal dashboard link"
      contains: "to=\"/dashboard\""
  key_links:
    - from: "apps/website/app/components/MenuUser.vue"
      to: "/dashboard"
      via: "NuxtLink internal"
      pattern: "to=\"/dashboard\""
    - from: "apps/strapi (email builders)"
      to: "FRONTEND_URL/dashboard/ads"
      via: "DASHBOARD_URL replacement"
      pattern: "FRONTEND_URL"
---

<objective>
Complete the merge: convert MenuUser's external dashboard link to an internal NuxtLink, update Strapi's DASHBOARD_URL-based email/reset URLs to the merged /dashboard/ paths, remove the dashboard workspace, delete apps/dashboard, and run the final verification gate.

Purpose: RUNTIME-01 — Strapi email/CORS references to the standalone dashboard host must be updated or admin emails 404 (RESEARCH.md Pitfall 5). CLEANUP-01 — remove the workspace and delete the app. This plan intentionally touches Strapi: it is URL plumbing (not endpoint/business-logic change), consciously expanding the "Strapi untouched" non-goal for the 5 DASHBOARD_URL references. (Dashboard-exclusive types were copied in Plan 04 — no type work here.)
Output: MenuUser internal link, Strapi URLs fixed, dashboard workspace removed and directory deleted, final gate green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-merge-dashboard-into-website/125-CONTEXT.md
@.planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md

<interfaces>
Strapi DASHBOARD_URL references (5 files — verified):
  apps/strapi/src/extensions/users-permissions/controllers/authController.ts — reset URL builder
  apps/strapi/src/api/ad/content-types/ad/lifecycles.ts — DASHBOARD_URL/ads/${id} email link
  apps/strapi/src/api/payment/services/ad.service.ts — DASHBOARD_URL/ads/${id} email
  apps/strapi/src/api/payment/services/free-ad.service.ts — DASHBOARD_URL/ads/${id} email
  apps/strapi/config/middlewares.ts — CORS allowedOrigins (host only, no path)

FRONTEND_URL is the website host. Email/ad links become `${FRONTEND_URL}/dashboard/ads/${id}`.
Password reset: migrated FormForgotPasswordDashboard.vue must send context: "website" so reset URL
points to FRONTEND_URL/restablecer-contrasena (RESEARCH.md Open Q #1 recommendation — single-line change).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MenuUser internal link + FormForgotPasswordDashboard context</name>
  <files>apps/website/app/components/MenuUser.vue, apps/website/app/components/FormForgotPasswordDashboard.vue</files>
  <read_first>
    - apps/website/app/components/MenuUser.vue (line ~33 — external :href="config.public.dashboardUrl")
    - apps/website/app/components/FormForgotPasswordDashboard.vue (migrated in Plan 03 — find the context: "dashboard" body field)
  </read_first>
  <action>
    1. MenuUser.vue: replace the external dashboard link (line ~33) `<a :href="config.public.dashboardUrl" ...>` with an internal `<NuxtLink to="/dashboard" ...>`. Remove the now-unused `config.public.dashboardUrl` reference in that component (and the `config` / useRuntimeConfig declaration if unused after — CLAUDE.md no-unused).
    2. FormForgotPasswordDashboard.vue: change the request body `context: "dashboard"` to `context: "website"` (RESEARCH.md Open Q #1) so Strapi builds the reset URL at FRONTEND_URL/restablecer-contrasena.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep "to=\"/dashboard\"" apps/website/app/components/MenuUser.vue && echo "---" && grep "context.*website" apps/website/app/components/FormForgotPasswordDashboard.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep "to=\"/dashboard\"" apps/website/app/components/MenuUser.vue` matches
    - `grep "config.public.dashboardUrl" apps/website/app/components/MenuUser.vue` returns nothing
    - `grep "context: \"website\"\|context: 'website'" apps/website/app/components/FormForgotPasswordDashboard.vue` matches
    - `grep "context: \"dashboard\"\|context: 'dashboard'" apps/website/app/components/FormForgotPasswordDashboard.vue` returns nothing
  </acceptance_criteria>
  <done>MenuUser uses internal /dashboard NuxtLink with no dashboardUrl reference; FormForgotPasswordDashboard sends context: "website".</done>
</task>

<task type="auto">
  <name>Task 2: Update Strapi DASHBOARD_URL email/reset URLs to /dashboard/ paths</name>
  <files>apps/strapi/src/extensions/users-permissions/controllers/authController.ts, apps/strapi/src/api/ad/content-types/ad/lifecycles.ts, apps/strapi/src/api/payment/services/ad.service.ts, apps/strapi/src/api/payment/services/free-ad.service.ts, apps/strapi/config/middlewares.ts</files>
  <read_first>
    - apps/strapi/src/extensions/users-permissions/controllers/authController.ts (find DASHBOARD_URL + context: "dashboard" branch)
    - apps/strapi/src/api/ad/content-types/ad/lifecycles.ts (DASHBOARD_URL/ads/${id} link)
    - apps/strapi/src/api/payment/services/ad.service.ts (DASHBOARD_URL/ads link)
    - apps/strapi/src/api/payment/services/free-ad.service.ts (DASHBOARD_URL/ads link)
    - apps/strapi/config/middlewares.ts (CORS allowedOrigins with DASHBOARD_URL)
    - .planning/phases/125-merge-dashboard-into-website/125-RESEARCH.md (Runtime State Inventory + Pitfall 5)
  </read_first>
  <action>
    For each ad-link email builder (lifecycles.ts, ad.service.ts, free-ad.service.ts): replace
    `${process.env.DASHBOARD_URL}/ads/${id}` (and similar) with `${process.env.FRONTEND_URL}/dashboard/ads/${id}`.
    The post-merge dashboard ad detail page is at /dashboard/ads/:id, so the /dashboard/ prefix is required.

    authController.ts: the `context === "dashboard"` branch builds the reset URL at DASHBOARD_URL/auth/reset-password. Since the migrated form now sends context: "website" (Task 1), the "dashboard" branch is effectively dead — but to be safe, change the "dashboard" branch to build `${process.env.FRONTEND_URL}/restablecer-contrasena` (same as the website branch) so any residual context:"dashboard" callers still resolve correctly. Follow SOLID/DI — do not introduce new instantiation; just change the URL string.

    middlewares.ts CORS: DASHBOARD_URL was an allowed origin for the standalone dashboard host. After merge it is the same host as FRONTEND_URL. Remove the DASHBOARD_URL entry from allowedOrigins if it duplicates FRONTEND_URL; keep FRONTEND_URL. Do not break existing allowed origins.

    Confirm the edited Strapi files compile with `yarn workspace waldo-strapi build`. Do NOT run the full Strapi test suite as a gate — verify only that the build succeeds.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "remaining ad-link DASHBOARD_URL (must be 0 in email builders):" && grep -rc "DASHBOARD_URL}/ads\|DASHBOARD_URL + \"/ads" apps/strapi/src/api/ad apps/strapi/src/api/payment | grep -v ":0"; echo "frontend prefix present:" && grep -rl "FRONTEND_URL}/dashboard/ads\|FRONTEND_URL + \"/dashboard/ads" apps/strapi/src | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "DASHBOARD_URL}/ads\|DASHBOARD_URL + \"/ads\"" apps/strapi/src/api/ad apps/strapi/src/api/payment` returns nothing (no ad email points at old host)
    - `grep -rl "FRONTEND_URL}/dashboard/ads\|FRONTEND_URL + \"/dashboard/ads\"" apps/strapi/src` returns >= 1 file
    - `grep "FRONTEND_URL}/restablecer-contrasena\|FRONTEND_URL + \"/restablecer" apps/strapi/src/extensions/users-permissions/controllers/authController.ts` matches
    - `yarn workspace waldo-strapi build 2>&1 | grep -iE "error TS|compilation failed" | wc -l` returns 0 (Strapi builds clean)
  </acceptance_criteria>
  <done>All ad email links use FRONTEND_URL/dashboard/ads/; reset URL points to FRONTEND_URL/restablecer-contrasena; CORS deduplicated; Strapi builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Remove dashboard workspace, delete apps/dashboard, final verification gate</name>
  <files>turbo.json, package.json, apps/dashboard/</files>
  <read_first>
    - package.json (root — workspaces array with "apps/dashboard")
    - turbo.json (waldo-dashboard#build, waldo-dashboard#dev tasks, and any task deps referencing waldo-dashboard)
    - apps/dashboard/ (confirm only non-migrated remnants remain: nuxt.config, plugins, dropped middleware, session composables, IconX.vue, etc.)
  </read_first>
  <action>
    1. Confirm everything intended to migrate is already in website (types in Plan 04; pages, components, composables, stores, layout, SCSS done in Plans 01-06). Remaining in apps/dashboard should be ONLY non-migrated items (nuxt.config.ts, dropped plugins, session.ts plugin, useSession*.ts, dev.global.ts, guard.global.ts, guest.ts, icons/IconX.vue, ecosystem.config.cjs, package.json, and the non-exclusive types user.ts/ad.ts/category.ts/pack.ts/app.d.ts/plugins.d.ts/strapi.d.ts). These are intentionally NOT migrated (D-04, D-11, RESEARCH.md Pitfall 2 + anti-patterns).
    2. Remove "apps/dashboard" from the root package.json `workspaces` array.
    3. Remove all waldo-dashboard entries from turbo.json: the `waldo-dashboard#build` / `waldo-dashboard#dev` task overrides and any `dependsOn` referencing waldo-dashboard.
    4. Delete the directory: `git rm -r apps/dashboard`.
    5. Reinstall to refresh the workspace lockfile: `yarn install` from root.
    6. Final gate: `yarn workspace waldo-website nuxi typecheck` (must pass) and `yarn workspace waldo-website test --run` (must pass) and `yarn workspace waldo-website build` (must pass).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && echo "dashboard dir gone:" && ls apps/dashboard 2>/dev/null | wc -l && echo "workspace ref gone:" && grep -c "apps/dashboard" package.json && echo "turbo ref gone:" && grep -c "waldo-dashboard" turbo.json && echo "--- final typecheck:" && yarn workspace waldo-website nuxi typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ls apps/dashboard 2>/dev/null | wc -l` returns 0 (directory deleted)
    - `grep -c "apps/dashboard" package.json` returns 0
    - `grep -c "waldo-dashboard" turbo.json` returns 0
    - `yarn workspace waldo-website nuxi typecheck` exits 0
    - `yarn workspace waldo-website test --run` all green (including AUTH/GUARD tests from Plan 01)
    - `yarn workspace waldo-website build` succeeds
  </acceptance_criteria>
  <done>apps/dashboard deleted; workspace and turbo references removed; final typecheck + test + build all green.</done>
</task>

</tasks>

<verification>
- `ls apps/dashboard` returns nothing
- `grep "apps/dashboard" package.json` and `grep "waldo-dashboard" turbo.json` return nothing
- MenuUser links internally to /dashboard
- Strapi ad emails + reset URL use FRONTEND_URL/dashboard paths
- `yarn workspace waldo-website nuxi typecheck && yarn workspace waldo-website test --run && yarn workspace waldo-website build` all pass
</verification>

<success_criteria>
- MenuUser internal /dashboard link
- Strapi DASHBOARD_URL references updated (5 files)
- apps/dashboard removed from workspace + deleted
- Final website typecheck + test + build green
</success_criteria>

<output>
After completion, create `.planning/phases/125-merge-dashboard-into-website/125-07-SUMMARY.md`
</output>
