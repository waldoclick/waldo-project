---
phase: 03-validacion-ia-de-campos-de-texto-libre-en-el-registro-boolean-por-campo-fail-open
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/strapi/src/extensions/users-permissions/controllers/authController.ts
  - apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts
  - apps/website/app/components/FormRegister.vue
autonomous: true
requirements: []

must_haves:
  truths:
    - "On local registration, the AI validates only free-text fields: firstname, lastname, address, and (only when is_company) business_name, business_type, business_address (D-01, D-02, D-03)"
    - "If any validated field returns explicit false, registration is rejected with a field-specific Spanish message and the built-in register controller is NEVER called (D-05)"
    - "If the AI service fails in any way, registration proceeds normally — the built-in register controller IS called (D-07 fail-open, proven end-to-end)"
    - "password, email, rut, phone, postal_code, birthdate, region/commune (and business equivalents) are never sent to the AI (D-03)"
    - "The Spanish rejection message surfaces to the user in FormRegister.vue"
  artifacts:
    - path: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      provides: "AI validation gate inside registerUserLocal before built-in register"
      contains: "validateFields"
    - path: "apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts"
      provides: "controller tests incl. end-to-end fail-open proof"
      contains: "validateFields"
  key_links:
    - from: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      to: "apps/strapi/src/services/field-validation"
      via: "import { validateFields } from '../../../services/field-validation'"
      pattern: "field-validation"
    - from: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      to: "ctx.badRequest"
      via: "field-specific Spanish message on explicit false"
      pattern: "ctx.badRequest"
---

<objective>
Wire the `field-validation` service (plan 01) into the local registration controller so free-text fields are AI-validated before the user is created. On an explicit `false`, reject with a field-specific Spanish message; on any AI failure, registration proceeds (fail-open). Confirm the existing FormRegister.vue error handling already surfaces the message (and tweak only if needed).

Purpose: Block obviously-fake free-text input (e.g. `akhsdgKAJHSDGH` as a name) at registration without ever letting AI downtime block real users.

Output: AI gate in `registerUserLocal`, a Spanish field→message map, controller tests including an end-to-end fail-open proof, and verified frontend message surfacing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-validacion-ia-de-campos-de-texto-libre-en-el-registro-boolean-por-campo-fail-open/03-CONTEXT.md
@.planning/phases/03-validacion-ia-de-campos-de-texto-libre-en-el-registro-boolean-por-campo-fail-open/03-01-SUMMARY.md

<interfaces>
<!-- From plan 01 (must be complete before this plan runs). -->

From apps/strapi/src/services/field-validation/index.ts:
```typescript
export type FieldMap = Record<string, string>;
export type ValidationResult = Record<string, boolean>;
export function validateFields(fields: FieldMap): Promise<ValidationResult>;
// NEVER throws. Returns all-true on any AI failure. Only an explicit false blocks.
```

<!-- The hook point. registerUserLocal already validates fields then calls ctx.badRequest BEFORE registerController. -->
From apps/strapi/src/extensions/users-permissions/controllers/authController.ts (registerUserLocal, abridged):
```typescript
export const registerUserLocal = (registerController) => async (ctx) => {
  try {
    const { is_company, firstname, lastname, email, rut, password, username,
      accepted_age_confirmation, accepted_terms, accepted_usage_terms,
      address, business_name, business_type, business_address /* present in body */ } = ctx.request.body;
    // ...required-fields check -> ctx.badRequest("All fields are required")
    // ...RESERVED_USERNAMES check -> ctx.badRequest("Username not available")
    // ...validatePasswordStrength -> ctx.badRequest(passwordError)
    // <<< INSERT AI VALIDATION GATE HERE (after password check, before ensureUniqueUsername) >>>
    const uniqueUsername = await ensureUniqueUsername(username);
    // ...build forwardBody, ctx.request.body = forwardBody
    await registerController(ctx);  // <-- user is created HERE
    // ...
  } catch (error) { ctx.badRequest("Error during registration:", { error }); }
};
```

<!-- Frontend already extracts and Swal-displays Strapi badRequest messages. -->
From apps/website/app/components/FormRegister.vue handleSubmit catch (lines ~418-436):
```typescript
const strapiMessage = err.error?.details?.error?.message || err.error?.message || "";
if (strapiMessage === "Email or Username are already taken") { Swal.fire("Error", "El correo electrónico ya está en uso.", "error"); }
else { Swal.fire("Error", strapiMessage || "Ocurrió un error durante el registro.", "error"); }
```
`ctx.badRequest("El apellido no parece válido")` yields `{ error: { status:400, name:"BadRequest", message:"El apellido no parece válido" } }`, read by `err.error?.message`, and falls into the `else` branch → Swal shows the message verbatim.

Controller test harness to mirror:
apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts — buildCtx helper, strapiQueryMock, global.strapi stub, jest.mock for services, AAA.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert AI validation gate into registerUserLocal with Spanish field→message map</name>
  <files>apps/strapi/src/extensions/users-permissions/controllers/authController.ts</files>
  <read_first>
    - apps/strapi/src/extensions/users-permissions/controllers/authController.ts (registerUserLocal, lines ~142-270)
    - apps/strapi/src/services/field-validation/index.ts (validateFields contract)
    - apps/strapi/config/plugins.ts (register.allowedFields — confirms address/business_* arrive in body)
    - .planning/phases/03-validacion-ia-de-campos-de-texto-libre-en-el-registro-boolean-por-campo-fail-open/03-CONTEXT.md (D-01..D-05, D-07)
  </read_first>
  <behavior>
    Inside registerUserLocal, after validatePasswordStrength and before ensureUniqueUsername:
    - Test A (explicit false rejects, register NOT called): validateFields mocked to return `{ lastname: false }` → controller returns badRequest("El apellido no parece válido") and `registerController` mock is NOT called.
    - Test B (all true proceeds): validateFields mocked to return all-true → `registerController` mock IS called once.
    - Test C (business fields validated only when is_company): is_company=true with business_name/business_type/business_address present → the object passed to validateFields includes business_name/business_type/business_address; with is_company=false → it does NOT include any business_* key and includes only firstname/lastname/address.
    - Test D (excluded fields never sent): the object passed to validateFields never contains password, email, rut, phone, postal_code, birthdate, region, commune.
    - Test E (END-TO-END FAIL-OPEN): do NOT mock validateFields — mock the ai-provider `generate` to reject; use the REAL field-validation service and REAL controller; assert `registerController` IS called (registration proceeds despite AI failure).
  </behavior>
  <action>
    1. Add import at top of authController.ts: `import { validateFields } from "../../../services/field-validation";`
    2. Destructure the free-text fields from `ctx.request.body` (they are already on the body per plugins.ts allowedFields): `address`, `business_name`, `business_type`, `business_address` (firstname/lastname already destructured).
    3. Define a module-level Spanish message map (UPPER_SNAKE_CASE constant), exact strings:
       - firstname → "El nombre no parece válido"
       - lastname → "El apellido no parece válido"
       - address → "La dirección no parece válida"
       - business_name → "La razón social no parece válida"
       - business_type → "El giro no parece válido"
       - business_address → "La dirección de la empresa no parece válida"
       (Note: for is_company, firstname is "Razón Social" and lastname is "Giro" in the UI, but D-01 evaluates the field keys firstname/lastname directly; keep the natural-person messages for firstname/lastname — Claude's-discretion text. Acceptable.)
    4. Build the fields-to-validate object: always include `firstname`, `lastname`, and `address` IF present/non-empty. Add `business_name`, `business_type`, `business_address` ONLY when `is_company === true` and the value is present/non-empty. Skip empty/undefined values (don't ask the AI to judge a blank). Build as `Record<string,string>`.
    5. Call `const validation = await validateFields(fieldsToValidate);`
    6. Find the first key whose value is strictly `=== false`: `const failedField = Object.keys(validation).find((k) => validation[k] === false);` If found: `return ctx.badRequest(REJECTION_MESSAGES[failedField] ?? "Algunos datos no parecen válidos");`
    7. Otherwise continue to `ensureUniqueUsername` unchanged.
    No `any`. English code comments. Do NOT touch registerUserAuth (Google OAuth) — out of scope.
  </action>
  <verify>
    <automated>cd apps/strapi && npx jest tests/extensions/users-permissions/controllers/authController.test.ts 2>&1 | grep -E "Tests:.*passed" && grep -q "validateFields" src/extensions/users-permissions/controllers/authController.ts && grep -q "El apellido no parece válido" src/extensions/users-permissions/controllers/authController.ts && grep -q "field-validation" src/extensions/users-permissions/controllers/authController.ts</automated>
  </verify>
  <done>registerUserLocal calls validateFields with only the free-text fields (business_* gated on is_company), rejects on explicit false with the correct Spanish message before calling the built-in register, and proceeds when the AI fails. registerUserAuth untouched. All controller tests (A-E incl. end-to-end fail-open) pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add controller tests for the AI gate (incl. end-to-end fail-open) to authController.test.ts</name>
  <files>apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts</files>
  <read_first>
    - apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts (buildCtx, strapiQueryMock, mock patterns)
    - apps/strapi/src/extensions/users-permissions/controllers/authController.ts (the gate added in Task 1)
    - apps/strapi/tests/services/ai-provider/ai-provider.service.test.ts (how to mock ai-provider's generate for the end-to-end test)
  </read_first>
  <action>
    Add a `describe("registerUserLocal AI validation gate")` block to the existing authController.test.ts (do not break existing tests).

    For Tests A-D: `jest.mock("../../../../src/services/field-validation", () => ({ validateFields: jest.fn() }))`, import the mocked `validateFields`, and drive its resolved value per case. Use the existing buildCtx helper; pass a `registerController` jest.fn() into `registerUserLocal(registerController)`. Assert on `registerController` call count and on `ctx.badRequest` message.
      - Test A: validateFields → `{ lastname: false }`; assert badRequest called with "El apellido no parece válido" AND registerController NOT called.
      - Test B: validateFields → `{ firstname:true, lastname:true, address:true }`; assert registerController called once.
      - Test C: inspect `validateFields.mock.calls[0][0]` — with is_company=true+business_* present it contains business_name/business_type/business_address; with is_company=false it contains none of them.
      - Test D: assert `validateFields.mock.calls[0][0]` has no password/email/rut/phone/postal_code/birthdate/region/commune keys.

    For Test E (END-TO-END FAIL-OPEN — the deep_work_rules requirement): do NOT mock field-validation. Instead `jest.mock("../../../../src/services/ai-provider", () => ({ generate: jest.fn().mockRejectedValue(new Error("AI down")) }))` (and `generateArticleDraft` if imported elsewhere). Use the REAL `validateFields` from field-validation and the REAL `registerUserLocal`. Provide a valid registration body and a `registerController` jest.fn(); assert `registerController` WAS called — proving an AI failure does not block registration. (If module-level jest.mock conflicts arise between Tests A-D and Test E, isolate Test E in its own describe with `jest.isolateModules` / `jest.resetModules` + a separate import, or place it in a sibling test file `authController.register-failopen.test.ts` under the same tests dir — mirror the source path.)
  </action>
  <verify>
    <automated>cd apps/strapi && npx jest tests/extensions/users-permissions/controllers/authController 2>&1 | grep -E "Tests:.*passed" && (grep -rq "mockRejectedValue" tests/extensions/users-permissions/controllers/ && grep -rq "registerController" tests/extensions/users-permissions/controllers/)</automated>
  </verify>
  <done>Controller test suite passes including the end-to-end fail-open test that injects a rejecting ai-provider, runs the real validation service through the real controller, and asserts registration still proceeds.</done>
</task>

<task type="auto">
  <name>Task 3: Verify FormRegister.vue surfaces the Spanish rejection message (tweak only if needed)</name>
  <files>apps/website/app/components/FormRegister.vue</files>
  <read_first>
    - apps/website/app/components/FormRegister.vue (handleSubmit catch, lines ~418-436)
  </read_first>
  <action>
    The existing catch block reads `err.error?.details?.error?.message || err.error?.message` and shows it via `Swal.fire("Error", strapiMessage || "...", "error")` in the `else` branch. `ctx.badRequest("El apellido no parece válido")` produces `{ error: { message: "El apellido no parece válido" } }`, which `err.error?.message` reads and the `else` branch displays verbatim.

    1. Confirm by inspection that the AI rejection message (a plain badRequest message, NOT equal to "Email or Username are already taken") flows into the `else` branch and is shown verbatim.
    2. If and only if it does NOT surface verbatim, make the MINIMAL change so the backend message is displayed (do not rebuild the handler, do not add new files — CLAUDE.md). Most likely NO code change is required.
    3. Add a one-line code comment above the `else` branch noting it also surfaces AI free-text validation rejections from registerUserLocal (so the behavior is discoverable). This is the only expected edit.
  </action>
  <verify>
    <automated>cd apps/website && grep -q "err.error?.message" app/components/FormRegister.vue && grep -q "strapiMessage" app/components/FormRegister.vue</automated>
  </verify>
  <done>FormRegister.vue's catch surfaces the backend Spanish rejection message verbatim (confirmed); a clarifying comment documents that AI free-text rejections land here. No rebuild, no new files.</done>
</task>

</tasks>

<verification>
- registerUserLocal sends only free-text fields to validateFields; business_* gated on is_company.
- Explicit false → field-specific Spanish badRequest, built-in register not called.
- AI failure → registration proceeds (end-to-end test green).
- FormRegister.vue shows the backend message.
- `npx jest tests/extensions/users-permissions/controllers/` green; existing register/login/verify tests still pass.
</verification>

<success_criteria>
- AI free-text validation is live in the local registration flow, server-side, before user creation (D-08).
- Only an explicit AI false blocks registration, with a correct Spanish per-field message (D-04, D-05).
- Fail-open proven end-to-end by a test that injects an AI failure and asserts registration proceeds (D-07).
- Excluded fields (password/email/rut/phone/postal_code/birthdate/region/commune) are never sent to the AI (D-03).
- Google OAuth register (registerUserAuth) untouched; no UX changes beyond surfacing the message.
</success_criteria>

<output>
After completion, create `.planning/phases/03-validacion-ia-de-campos-de-texto-libre-en-el-registro-boolean-por-campo-fail-open/03-02-SUMMARY.md`
</output>
