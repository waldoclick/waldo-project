---
phase: 121-clean-subscription-data-model
plan: 03
type: execute
wave: 3
depends_on: ["121-01", "121-02"]
files_modified:
  - apps/strapi/tests/cron/subscription-charge.cron.test.ts
  - apps/strapi/tests/middlewares/protect-user-fields.test.ts
  - apps/strapi/tests/api/payment/payment-pro-response.test.ts
  - apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts
autonomous: true
requirements:
  - SUB-MODEL-121-01
  - SUB-MODEL-121-02
  - SUB-MODEL-121-03
  - SUB-MODEL-121-04

must_haves:
  truths:
    - "All tests pass with `yarn workspace @waldo/strapi jest --no-coverage`"
    - "Zero occurrences of pro_expires_at in entire apps/strapi/src and apps/strapi/tests trees"
    - "Cron tests assert period_end in subscription-payment create/update calls"
    - "Cron tests no longer assert pro_expires_at on user updates"
    - "proResponse tests are implemented (no longer .todo stubs)"
    - "protect-user-fields test references 9 protected fields (was 13, minus 4 card fields already dropped by migration)"
  artifacts:
    - path: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      provides: "Updated cron test suite for period_end-based billing"
    - path: "apps/strapi/tests/middlewares/protect-user-fields.test.ts"
      provides: "Updated middleware test without pro_expires_at"
    - path: "apps/strapi/tests/api/payment/payment-pro-response.test.ts"
      provides: "Implemented proResponse tests asserting subscription-payment creation with period_end"
    - path: "apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts"
      provides: "Updated cancellation test without pro_expires_at references"
  key_links:
    - from: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      to: "apps/strapi/src/cron/subscription-charge.cron.ts"
      via: "Jest mock assertions"
      pattern: "period_end"
---

<objective>
Update all test suites to match the Phase 121 code changes: cron tests for period_end-based billing, proResponse test implementation, protect-user-fields test update, and cancellation test cleanup. Final verification sweep to confirm zero pro_expires_at references remain.

Purpose: Tests must validate the new data model where period_end lives on subscription-payment and pro_expires_at no longer exists.
Output: All 4 test files updated, full suite green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-clean-subscription-data-model-move-pro-expires-at-out-of-user-into-subscription-payment-as-period-end/121-RESEARCH.md
@.planning/phases/121-clean-subscription-data-model-move-pro-expires-at-out-of-user-into-subscription-payment-as-period-end/121-01-SUMMARY.md
@.planning/phases/121-clean-subscription-data-model-move-pro-expires-at-out-of-user-into-subscription-payment-as-period-end/121-02-SUMMARY.md

<interfaces>
<!-- The executor must read the ACTUAL updated source files to understand the new code before writing tests -->
<!-- Key changes from plans 01 and 02: -->

After Plan 01:
- subscription-payment schema now has `period_end: { type: "date", required: true }`
- user schema no longer has `pro_expires_at`
- PROTECTED_USER_FIELDS: ["pro_status", "username", "avatar", "cover", "role", "provider", "confirmed", "blocked"] (9 items)
- proResponse creates subscription-payment with period_end via subPaymentCreate

After Plan 02:
- Cron Step 1: queries subscription-payment by period_end (not users by pro_expires_at)
- Cron Step 4: queries subscription-payment for cancelled users by period_end (not users by pro_expires_at), deduplicates with Set
- chargeUser: takes periodEnd param, writes period_end on all 4 data payloads, no user.pro_expires_at update
- ProUser: no pro_expires_at field
- DuePaymentRecord: new interface with period_end
- FailedPaymentRecord: now has period_end field
- CancelledUser interface: deleted

Test mock chain changes (CRITICAL):
- Step 1 findMany now queries subscription-payment (was users) — mock chain order changes
- Step 1 no longer has per-user idempotency findMany — one fewer mock call per user
- Step 4 findMany now queries subscription-payment (was users) — mock chain order changes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cron test suite for period_end-based billing</name>
  <files>apps/strapi/tests/cron/subscription-charge.cron.test.ts</files>
  <read_first>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts,
    apps/strapi/src/cron/subscription-charge.cron.ts
  </read_first>
  <action>
IMPORTANT: Read the ACTUAL updated cron source first (from Plan 02 output) to understand the exact findMany call sequence before rewriting mocks. The mock chain order is determined by the source code execution order.

**A. Update makeUser helper** — Remove `pro_expires_at` from the factory:
```typescript
const makeUser = (
  overrides: Partial<{
    id: number;
    documentId: string;
    subscription_pro: { tbk_user: string; pending_invoice?: boolean } | null;
  }> = {}
) => ({
  id: 42,
  documentId: "abc123xyz456789012345678",
  subscription_pro: { tbk_user: "tbk-user-stored-token" },
  ...overrides,
});
```

**B. Rewrite CHRG-01 test** — Step 1 now returns subscription-payment records (not users):
- Mock `mockFindMany` call 1: returns `[{ id: 1, period_end: "2026-02-20", user: makeUser() }]` (due payments from subscription-payment)
- Mock `mockFindMany` call 2: returns `[]` (retry candidates — Step 2)
- Mock `mockFindMany` call 3: returns `[]` (deactivation candidates — Step 3)
- Mock `mockFindMany` call 4: returns `[]` (Step 4 cancelled payments)
- NOTE: No idempotency check findMany — that's gone
- Assert: `mockFindMany` first call is on `api::subscription-payment.subscription-payment` with `period_end: { $lte: today }` filter
- Assert: `mockAuthorizeCharge` called with user.documentId, user.subscription_pro.tbk_user

**C. Rewrite CHRG-01b tests** — User with no tbk_user. Same mock pattern but with null subscription_pro in the user nested inside the payment record.

**D. Rewrite CHRG-02 test** — Successful charge creates approved subscription-payment with period_end:
- Assert: `mockCreate` called with `period_end: expect.any(String)` in data
- Assert: `mockUpdate` is NOT called with `plugin::users-permissions.user` and `pro_expires_at` — instead assert `mockUpdate` is NOT called at all for user update (chargeUser no longer updates user on success)
- NEW ASSERTION: `expect(mockCreate).toHaveBeenCalledWith("api::subscription-payment.subscription-payment", expect.objectContaining({ data: expect.objectContaining({ period_end: expect.any(String) }) }))`

**E. Remove CHRG-05 idempotency test** — The separate idempotency check no longer exists. The query itself is the guard. Delete this entire describe block.

**F. Rewrite CHRG-03 (first failure) test** — Failed record must include period_end:
- Assert: `mockCreate` data includes `period_end: expect.any(String)`

**G. Rewrite CHRG-03 (retry) test** — Retry success updates record with period_end:
- FailedPaymentRecord mock must include `period_end: "2026-03-01"`
- Assert: `mockUpdate` for subscription-payment includes `period_end: expect.any(String)`
- Remove assertion that `mockUpdate` is called with `plugin::users-permissions.user` and `pro_expires_at`

**H. Rewrite CHRG-03 (deactivation) test** — Exhausted subscription deactivation:
- Assert: user update contains only `pro_status: "inactive"` — NOT `pro_expires_at: null`, NOT `tbk_user: null`
- The `tbk_user: null` was removed from the cron deactivation path because it's handled by subscription-pro separately

**I. Rewrite CANC-04 Step 4 tests:**
- Step 4 mock now returns subscription-payment records: `[{ id: 100, user: { id: 55, documentId: "cancelleduserid12345678" } }]`
- Assert: `mockFindMany` is called with `api::subscription-payment.subscription-payment` and filters `{ status: "approved", period_end: { $lte: today }, user: { pro_status: { $eq: "cancelled" } } }`
- Assert: user update contains only `pro_status: "inactive"` — NOT `pro_expires_at: null`, NOT `tbk_user: null`
- "skips cancelled users whose pro_expires_at is in the future" test remains valid — just rename to "Step 4 returns empty when no expired cancelled payments exist"

**J. Rewrite chargeUser order+Facto tests:**
- Remove assertions on `pro_expires_at` in user update
- The "extends pro_expires_at even when order creation fails" test should be renamed to "creates subscription-payment with period_end even when order creation fails"
- Assert: `mockCreate` data includes `period_end: expect.any(String)` instead of asserting `mockUpdate` with `pro_expires_at`

**K. Final sweep:** `grep "pro_expires_at" apps/strapi/tests/cron/subscription-charge.cron.test.ts` must return 0 matches.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace @waldo/strapi jest tests/cron/subscription-charge.cron.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @waldo/strapi jest tests/cron/subscription-charge.cron.test.ts --no-coverage` passes all tests
    - `grep "pro_expires_at" apps/strapi/tests/cron/subscription-charge.cron.test.ts` returns 0 matches
    - `grep "period_end" apps/strapi/tests/cron/subscription-charge.cron.test.ts` returns matches in: DuePaymentRecord mocks, FailedPaymentRecord mocks, mockCreate assertions, mockUpdate assertions
    - No test asserts `pro_expires_at` on any mockUpdate or mockCreate call
    - Step 1 test asserts findMany on `api::subscription-payment.subscription-payment` (not `plugin::users-permissions.user`)
    - Step 4 test asserts findMany on `api::subscription-payment.subscription-payment` with cancelled filter
    - CHRG-05 idempotency describe block is deleted
  </acceptance_criteria>
  <done>
    - All cron tests pass
    - Zero pro_expires_at references in cron test file
    - Tests validate period_end in all subscription-payment create/update assertions
    - Mock chains match the new cron execution order
  </done>
</task>

<task type="auto">
  <name>Task 2: Update middleware + proResponse + cancellation tests, final verification sweep</name>
  <files>
    apps/strapi/tests/middlewares/protect-user-fields.test.ts,
    apps/strapi/tests/api/payment/payment-pro-response.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts
  </files>
  <read_first>
    apps/strapi/tests/middlewares/protect-user-fields.test.ts,
    apps/strapi/tests/api/payment/payment-pro-response.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts,
    apps/strapi/src/middlewares/protect-user-fields.ts,
    apps/strapi/src/api/payment/controllers/payment.ts,
    apps/strapi/src/api/payment/services/pro-cancellation.service.ts
  </read_first>
  <action>
**A. protect-user-fields.test.ts:**

1. **Test 2 (line 42):** Remove `pro_expires_at: "2026-01-01"` from the test input data and remove the assertion `expect(ctx.request.body.data).not.toHaveProperty("pro_expires_at")`. The test description should be updated: "strips pro_status, tbk_user from body.data, keeps lastname" (remove mention of pro_expires_at).

2. **Test 9 (line 178):** Update the "all 13 protected fields" test:
   - Change description to "strips all 9 protected fields: pro_status, username, avatar, cover, role, provider, confirmed, blocked" (note: tbk_user, pro_card_type, pro_card_last4, pro_inscription_token, pro_pending_invoice were already removed from the middleware by Phase 120 migration — they may still be in the test. Read the actual middleware to verify the current PROTECTED_USER_FIELDS list and match the test exactly)
   - Remove `pro_expires_at: "2026-05-01"` from the test input data
   - Remove `"pro_expires_at"` from the `protectedFields` array in the assertion
   - Update the count in the description to match actual PROTECTED_USER_FIELDS length (should be 9 after removing pro_expires_at)
   - IMPORTANT: Read the ACTUAL middleware file to get the exact current PROTECTED_USER_FIELDS list before updating the test. The middleware may still have card fields like `tbk_user`, `pro_card_type` etc. that were not yet removed — or they may have been removed in Phase 120. Match reality.

3. **Test 8 (line 156):** If this test references card enrollment fields that are still in PROTECTED_USER_FIELDS, keep it. If those fields were already removed from the middleware, this test may need cleanup. Read the actual middleware first.

**B. payment-pro-response.test.ts — IMPLEMENT the .todo tests:**

This file currently has only `.todo` stubs. Implement all 8 tests. The test must mock:
- `strapi.db.query("api::subscription-pro.subscription-pro").findOne` — returns subPro record
- `strapi.entityService.update` — for subscription-pro card data update and user activation
- `strapi.entityService.create` — for subscription-payment creation
- `OneclickService.finishInscription` — returns inscription result
- `OneclickService.authorizeCharge` — returns charge result
- `orderUtils.createAdOrder` — for order creation
- `generalUtils.generateFactoDocument` — for Facto document
- `documentDetails` — for user document details
- `computeSortPriority` — for ad sort priority

Structure the test file following the AAA pattern (Arrange, Act, Assert) and the existing test patterns in the codebase.

Key test assertions for Phase 121:
- **"should create first subscription-payment with period_end":** Assert `mockCreate` is called with `"api::subscription-payment.subscription-payment"` and data containing `period_end: expect.any(String)`, `period_start: expect.any(String)`, `status: "approved"`, `amount`, `charge_attempts: 1`
- **"should set pro_status to active only after charge succeeds":** Assert `mockUpdate` called with `plugin::users-permissions.user` and `{ data: { pro_status: "active" } }` — and NOT containing `pro_expires_at`
- **"should NOT activate user when charge fails":** Assert `mockUpdate` NOT called with `plugin::users-permissions.user`
- **"should redirect to /pro/error?reason=charge-failed":** Assert `ctx.redirect` called with charge-failed URL
- **"should redirect to /pro/error?reason=rejected":** Assert `ctx.redirect` with rejected URL
- **"should redirect to /pro/error?reason=cancelled":** Assert `ctx.redirect` with cancelled URL

The test must import PaymentController from `../../../src/api/payment/controllers/payment` and call `proResponse` through the controller, passing a mock Koa context.

IMPORTANT: Read the actual proResponse method in payment.ts (after Plan 01 changes) to understand the exact mock sequence needed. The method calls: finishInscription -> db.query.findOne -> authorizeCharge -> entityService.update (subPro) -> entityService.create (subscription-payment) -> entityService.update (user) -> createAdOrder.

**C. pro-cancellation.service.test.ts:**

1. Find the test "does NOT change pro_expires_at" (line 116) and update it:
   - Rename to "does NOT include pro_expires_at in user update (field no longer exists)"
   - The assertion `expect(updateData).not.toHaveProperty("pro_expires_at")` is still valid — keep it
   - But check if there are any other references to `pro_expires_at` in comments or test descriptions and update them

2. Run `grep "pro_expires_at" apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts` and update all occurrences to remove or update references.

**D. Final verification sweep:**

After all test updates:
1. `grep -r "pro_expires_at" apps/strapi/src/` — must return 0 matches
2. `grep -r "pro_expires_at" apps/strapi/tests/` — must return 0 matches (or only in negative assertions like `not.toHaveProperty`)
3. `yarn workspace @waldo/strapi jest --no-coverage` — full suite must pass
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace @waldo/strapi jest --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace @waldo/strapi jest --no-coverage` passes all tests (zero failures)
    - `grep -r "pro_expires_at" apps/strapi/src/` returns 0 matches
    - `grep -r "pro_expires_at" apps/strapi/tests/` returns 0 matches OR only in `not.toHaveProperty("pro_expires_at")` negative assertions
    - payment-pro-response.test.ts has zero `.todo` tests — all 8+ tests implemented
    - payment-pro-response.test.ts asserts subscription-payment creation with `period_end`
    - payment-pro-response.test.ts asserts user update does NOT contain `pro_expires_at`
    - protect-user-fields.test.ts Test 9 matches actual PROTECTED_USER_FIELDS count (9 fields)
  </acceptance_criteria>
  <done>
    - All 4 test files updated
    - Full Strapi test suite passes
    - Zero pro_expires_at in source code
    - Zero pro_expires_at in tests (except negative assertions)
    - proResponse tests fully implemented (no more .todo stubs)
    - Phase 121 data model migration is complete and verified
  </done>
</task>

</tasks>

<verification>
Final phase verification:
1. `grep -r "pro_expires_at" apps/strapi/src/` — 0 matches
2. `grep -r "pro_expires_at" apps/strapi/tests/` — 0 matches (or negative assertions only)
3. `grep "period_end" apps/strapi/src/api/subscription-payment/content-types/subscription-payment/schema.json` — confirms field exists
4. `yarn workspace @waldo/strapi jest --no-coverage` — all tests pass
5. `grep "period_end" apps/strapi/src/cron/subscription-charge.cron.ts` — multiple matches
6. `grep "period_end" apps/strapi/src/api/payment/controllers/payment.ts` — at least 1 match (proResponse)
</verification>

<success_criteria>
- All Strapi tests pass
- Zero pro_expires_at references in source and test code (except negative test assertions)
- proResponse tests fully implemented with period_end assertions
- Cron tests validate period_end-based billing flow
- protect-user-fields test matches actual middleware field count
</success_criteria>

<output>
After completion, create `.planning/phases/121-clean-subscription-data-model-move-pro-expires-at-out-of-user-into-subscription-payment-as-period-end/121-03-SUMMARY.md`
</output>
