---
phase: 52-ad-draft-decoupling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/ad/services/ad.ts
  - apps/strapi/src/api/ad/controllers/ad.ts
  - apps/strapi/src/api/ad/routes/00-ad-custom.ts
autonomous: true
requirements:
  - BACK-03
  - BACK-04
  - BACK-05

must_haves:
  truths:
    - "`computeAdStatus()` returns `'draft'` for any ad where `draft === true`, checked before all other status conditions"
    - "The `AdStatus` type includes `'draft'` and no longer includes `'abandoned'`"
    - "The service has `draftAds()` method with `draft: true` filter; `abandonedAds()` is renamed to `draftAds()`"
    - "The controller has `drafts()` handler; `abandoneds()` is renamed to `drafts()`"
    - "The route `GET /ads/abandoneds` is replaced by `GET /ads/drafts`"
  artifacts:
    - path: "apps/strapi/src/api/ad/services/ad.ts"
      provides: "Updated computeAdStatus + draftAds service method"
      contains: '"draft"'
    - path: "apps/strapi/src/api/ad/controllers/ad.ts"
      provides: "drafts() controller handler"
      contains: "drafts"
    - path: "apps/strapi/src/api/ad/routes/00-ad-custom.ts"
      provides: "/ads/drafts route"
      contains: "/ads/drafts"
  key_links:
    - from: "apps/strapi/src/api/ad/controllers/ad.ts"
      to: "apps/strapi/src/api/ad/services/ad.ts"
      via: "strapi.service('api::ad.ad').draftAds(options)"
      pattern: "draftAds"
    - from: "apps/strapi/src/api/ad/routes/00-ad-custom.ts"
      to: "apps/strapi/src/api/ad/controllers/ad.ts"
      via: "handler: 'ad.drafts'"
      pattern: "ad\\.drafts"
---

<objective>
Update the ad service, controller, and routes to replace the `abandoned` concept with `draft`. This includes: updating `computeAdStatus()` to evaluate `draft: true` first, renaming `abandonedAds()` to `draftAds()`, renaming the `abandoneds` controller/route to `drafts`, and removing `"abandoned"` from the `AdStatus` union type.

Purpose: Makes the backend correctly classify and serve draft ads. The new `GET /ads/drafts` endpoint serves the dashboard "Borradores" section (Plan 04).
Output: Updated `ad.ts` service, `ad.ts` controller, `00-ad-custom.ts` routes.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current AdStatus type (apps/strapi/src/api/ad/services/ad.ts lines 17-24) -->
```typescript
type AdStatus =
  | "active"
  | "pending"
  | "archived"
  | "banned"
  | "rejected"
  | "abandoned"   // ← REMOVE this
  | "unknown";
// Add "draft" BEFORE "active" in the union
```

<!-- Current computeAdStatus logic (ad.ts lines 39-97) -->
// First check: add BEFORE the `if (adObj.rejected)` block:
// if (adObj.draft === true) return "draft";

<!-- Current abandonedAds method (ad.ts lines 448-457) -->
// Rename method to draftAds
// Change filter: { draft: { $eq: true } }
// (Remove active/banned/rejected/ad_reservation conditions — draft field is the source of truth now)
// Change getAdvertisements status arg: "draft"

<!-- Current abandoneds controller (ad.ts lines 250-276) -->
// Rename method: abandoneds → drafts
// Change service call: .draftAds(options)
// Update JSDoc comment to reflect drafts

<!-- Current route (00-ad-custom.ts lines 42-46) -->
// Change path: "/ads/abandoneds" → "/ads/drafts"
// Change handler: "ad.abandoneds" → "ad.drafts"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update AdStatus type and computeAdStatus in ad.ts service</name>
  <files>apps/strapi/src/api/ad/services/ad.ts</files>
  <behavior>
    - computeAdStatus({ draft: true }) → "draft"
    - computeAdStatus({ draft: true, active: true }) → "draft" (draft checked first)
    - computeAdStatus({ active: true, banned: false, rejected: false, remaining_days: 5 }) → "active" (unchanged)
    - computeAdStatus({ active: false, banned: false, rejected: false, ad_reservation: null, is_paid: true, remaining_days: 5 }) → "unknown" (the old abandoned condition no longer returns "abandoned")
    - AdStatus type has "draft", does NOT have "abandoned"
  </behavior>
  <action>
    1. Update the `AdStatus` union type: add `"draft"` before `"active"`, remove `"abandoned"`:
       ```typescript
       type AdStatus =
         | "draft"
         | "active"
         | "pending"
         | "archived"
         | "banned"
         | "rejected"
         | "unknown";
       ```

    2. In `computeAdStatus()`, add the draft check as the FIRST condition (before the `rejected` check):
       ```typescript
       if (adObj.draft === true) {
         return "draft";
       }
       ```

    3. Rename `abandonedAds()` method to `draftAds()`. Replace the entire method body with:
       ```typescript
       async draftAds(options: AdQueryOptions = {}) {
         const defaultFilters = {
           draft: { $eq: true },
         };
         return getAdvertisements(options, defaultFilters, "draft");
       }
       ```
       The `draft` field is now the authoritative source of truth — remove the old `active/banned/rejected/ad_reservation` conditions.

    IMPORTANT: Do NOT remove the `abandoned` branch from `computeAdStatus()` immediately. The old abandoned branch (`if active=false, no reservation, is_paid, remaining_days>0 → "abandoned"`) should now fall through to `"unknown"` since we removed `"abandoned"` from AdStatus. This happens naturally by removing the return statement — but since the branch was there, just leave the check absent (it was the last named branch before `unknown` fallthrough). The function already falls through to `return "unknown"` at the end.

    Actually: the old `abandoned` block should be REMOVED entirely from `computeAdStatus()` since "abandoned" no longer exists as a valid status. Ads that would have been "abandoned" now have `draft: true` (after migration) and will be caught by the new first check.
  </action>
  <verify>
    <automated>cd apps/strapi && npx tsc --noEmit --project tsconfig.json 2>&1 | grep "ad/services/ad" | head -20; echo "TypeCheck exit: $?"</automated>
  </verify>
  <done>`AdStatus` has `"draft"` and lacks `"abandoned"`. `computeAdStatus` returns `"draft"` when `ad.draft === true` before any other check. `draftAds()` method exists with `draft: { $eq: true }` filter. `abandonedAds()` method is gone. Zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rename abandoneds controller handler and update route</name>
  <files>apps/strapi/src/api/ad/controllers/ad.ts, apps/strapi/src/api/ad/routes/00-ad-custom.ts</files>
  <action>
    In `apps/strapi/src/api/ad/controllers/ad.ts`:

    1. Rename the `abandoneds` method to `drafts`. Change the service call inside from `.abandonedAds(options)` to `.draftAds(options)`. Update the JSDoc `@route` comment to `GET /api/ads/drafts`. The rest of the method body (query extraction, pagination, options build) is IDENTICAL to other list handlers — no logic changes needed.

    In `apps/strapi/src/api/ad/routes/00-ad-custom.ts`:

    2. Find the route entry for `/ads/abandoneds` with handler `"ad.abandoneds"`. Change it to:
       ```typescript
       {
         method: "GET",
         path: "/ads/drafts",
         handler: "ad.drafts",
       },
       ```

    No other routes are changed.
  </action>
  <verify>
    <automated>cd apps/strapi && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "ad/controllers/ad|00-ad-custom" | head -20; node -e "const r = require('./src/api/ad/routes/00-ad-custom'); const hasDrafts = r.default.routes.some(x => x.path === '/ads/drafts' && x.handler === 'ad.drafts'); const noAbandoned = !r.default.routes.some(x => x.path === '/ads/abandoneds'); console.log('drafts route:', hasDrafts, 'no abandoned:', noAbandoned); if (!hasDrafts || !noAbandoned) process.exit(1);" 2>/dev/null || echo "Cannot require TS directly — TypeScript check is sufficient"</automated>
  </verify>
  <done>`ad.ts` controller has `drafts()` method calling `.draftAds()`. `abandoneds()` method is gone. Route file has `GET /ads/drafts` → `ad.drafts`. `GET /ads/abandoneds` route is removed. Zero TypeScript errors in these files.</done>
</task>

</tasks>

<verification>
1. `tsc --noEmit` in `apps/strapi` passes with zero errors for modified files
2. `AdStatus` union in `ad.ts` service: `"draft"` present, `"abandoned"` absent
3. `computeAdStatus({draft: true})` returns `"draft"` (first branch)
4. `draftAds()` method present in service factory with `draft: { $eq: true }` filter
5. Route file: `/ads/drafts` present, `/ads/abandoneds` absent
</verification>

<success_criteria>
- `abandoned` is completely removed from the ad service layer — no references remain in these three files
- `draft` status is the first check in `computeAdStatus()` — confirmed by reading the function
- Dashboard can call `GET /api/ads/drafts` and receive draft ads filtered by `draft: true`
</success_criteria>

<output>
After completion, create `.planning/phases/52-ad-draft-decoupling/52-02-SUMMARY.md`
</output>
