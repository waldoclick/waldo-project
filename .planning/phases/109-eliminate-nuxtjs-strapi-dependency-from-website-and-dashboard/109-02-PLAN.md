---
phase: 109-eliminate-nuxtjs-strapi-dependency-from-website-and-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - apps/dashboard/app/composables/useApiClient.ts
  - apps/dashboard/app/composables/useImage.ts
  - apps/dashboard/app/composables/useLogout.ts
  - apps/dashboard/app/middleware/guard.global.ts
  - apps/dashboard/app/middleware/guest.ts
  - apps/dashboard/app/plugins/sentry.ts
  - apps/dashboard/app/components/HeaderDefault.vue
  - apps/dashboard/app/components/HeroDashboard.vue
  - apps/dashboard/app/components/AvatarDefault.vue
  - apps/dashboard/app/components/DropdownUser.vue
  - apps/dashboard/app/components/FormEdit.vue
  - apps/dashboard/app/components/FormPassword.vue
  - apps/dashboard/app/components/FormVerifyCode.vue
  - apps/dashboard/app/components/FormLogin.vue
  - apps/dashboard/nuxt.config.ts
  - apps/dashboard/package.json
  - apps/dashboard/tests/composables/useApiClient.test.ts
  - apps/dashboard/tests/stubs/imports.stub.ts
autonomous: true
requirements:
  - REQ-109-03
  - REQ-109-04
  - REQ-109-05

must_haves:
  truths:
    - "Zero useStrapiUser/useStrapiToken/useStrapiAuth/useStrapiClient calls remain in dashboard app directory"
    - "useApiClient still injects X-Recaptcha-Token on POST/PUT/DELETE after switching to useSessionClient"
    - "TypeScript compiles clean after all renames"
    - "All 55+ existing tests still pass"
    - "@nuxtjs/strapi is no longer in modules array or package.json dependencies"
  artifacts:
    - path: "apps/dashboard/app/composables/useApiClient.ts"
      provides: "Uses useSessionClient instead of useStrapiClient"
      contains: "useSessionClient()"
    - path: "apps/dashboard/nuxt.config.ts"
      provides: "Module removed from modules array"
    - path: "apps/dashboard/package.json"
      provides: "@nuxtjs/strapi removed from dependencies"
  key_links:
    - from: "apps/dashboard/app/composables/useApiClient.ts"
      to: "useSessionClient"
      via: "import swap from useStrapiClient"
      pattern: "useSessionClient\\(\\)"
    - from: "apps/dashboard/app/middleware/guard.global.ts"
      to: "useSessionUser"
      via: "import swap from useStrapiUser"
      pattern: "useSessionUser\\(\\)"
    - from: "apps/dashboard/app/components/FormVerifyCode.vue"
      to: "useSessionAuth, useSessionUser"
      via: "import swap from useStrapiAuth, useStrapiUser"
      pattern: "useSessionAuth\\(\\)|useSessionUser\\(\\)"
    - from: "apps/dashboard/tests/composables/useApiClient.test.ts"
      to: "useSessionClient mock"
      via: "vi.mock swap from useStrapiClient"
      pattern: "useSessionClient"
---

<objective>
Replace all useStrapiX calls with useSessionX across the entire dashboard, update test mocks, remove @nuxtjs/strapi from modules and package.json.

Purpose: Complete the elimination of the @nuxtjs/strapi dependency from the dashboard. After this plan, the dashboard owns its entire session and HTTP layer with no external module dependency.

Output: All 13 consumer files migrated, test mocks updated, module removed from config, package removed from dependencies.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-eliminate-nuxtjs-strapi-dependency-from-website-and-dashboard/109-RESEARCH.md
@.planning/phases/109-eliminate-nuxtjs-strapi-dependency-from-website-and-dashboard/109-01-SUMMARY.md

@apps/dashboard/app/composables/useApiClient.ts
@apps/dashboard/app/composables/useImage.ts
@apps/dashboard/app/composables/useLogout.ts
@apps/dashboard/nuxt.config.ts
@apps/dashboard/tests/composables/useApiClient.test.ts
@apps/dashboard/tests/stubs/imports.stub.ts

<interfaces>
<!-- Session composables created in Plan 01 — use these directly -->

From apps/dashboard/app/composables/useSessionUser.ts:
```typescript
export const useSessionUser = <T = User>() => useState<T | null>("session_user", () => null);
```

From apps/dashboard/app/composables/useSessionToken.ts:
```typescript
export const useSessionToken = (): Ref<string | null>;
```

From apps/dashboard/app/composables/useSessionClient.ts:
```typescript
export const useSessionClient = () => async <T>(url: string, fetchOptions?: FetchOptions) => Promise<T>;
```

From apps/dashboard/app/composables/useSessionAuth.ts:
```typescript
export const useSessionAuth = () => ({ setToken, fetchUser, logout });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap all useStrapiX calls to useSessionX in consumer files</name>
  <files>
    apps/dashboard/app/composables/useApiClient.ts
    apps/dashboard/app/composables/useImage.ts
    apps/dashboard/app/composables/useLogout.ts
    apps/dashboard/app/middleware/guard.global.ts
    apps/dashboard/app/middleware/guest.ts
    apps/dashboard/app/plugins/sentry.ts
    apps/dashboard/app/components/HeaderDefault.vue
    apps/dashboard/app/components/HeroDashboard.vue
    apps/dashboard/app/components/AvatarDefault.vue
    apps/dashboard/app/components/DropdownUser.vue
    apps/dashboard/app/components/FormEdit.vue
    apps/dashboard/app/components/FormPassword.vue
    apps/dashboard/app/components/FormVerifyCode.vue
    apps/dashboard/app/components/FormLogin.vue
  </files>
  <read_first>
    apps/dashboard/app/composables/useApiClient.ts
    apps/dashboard/app/composables/useImage.ts
    apps/dashboard/app/composables/useLogout.ts
    apps/dashboard/app/middleware/guard.global.ts
    apps/dashboard/app/middleware/guest.ts
    apps/dashboard/app/plugins/sentry.ts
    apps/dashboard/app/components/HeaderDefault.vue
    apps/dashboard/app/components/HeroDashboard.vue
    apps/dashboard/app/components/AvatarDefault.vue
    apps/dashboard/app/components/DropdownUser.vue
    apps/dashboard/app/components/FormEdit.vue
    apps/dashboard/app/components/FormPassword.vue
    apps/dashboard/app/components/FormVerifyCode.vue
    apps/dashboard/app/components/FormLogin.vue
  </read_first>
  <action>
Perform these mechanical substitutions in each file. The new composables are auto-imported by Nuxt (they live in `composables/`), so explicit imports from `#imports` must be REMOVED for the replaced symbols. Other `#imports` symbols (like `navigateTo`, `useNuxtApp`, `useRuntimeConfig`) stay.

**useApiClient.ts (1 change):**
- Line 1: Change `import { useStrapiClient, useNuxtApp } from "#imports"` to `import { useNuxtApp } from "#imports"`
- Line 11: Change `const client = useStrapiClient()` to `const client = useSessionClient()`
- `useSessionClient` is auto-imported, no explicit import needed

**useImage.ts (1 change):**
- Line 1: Change `import { useRuntimeConfig, useStrapiToken } from "#imports"` to `import { useRuntimeConfig } from "#imports"`
- Line 52 (inside uploadFile): Change `const token = useStrapiToken()` to `const token = useSessionToken()`
- `useSessionToken` is auto-imported

**useLogout.ts (1 change):**
- Line 4: Change `import { useStrapiAuth, navigateTo } from "#imports"` to `import { navigateTo } from "#imports"`
- Line 11: Change `const { logout: strapiLogout } = useStrapiAuth()` to `const { logout: sessionLogout } = useSessionAuth()`
- Line 22: Change `await strapiLogout()` to `sessionLogout()` (useSessionAuth.logout is synchronous — it just sets token to null and user to null; no await needed)
- `useSessionAuth` is auto-imported

**guard.global.ts:**
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove `useStrapiUser` from any `#imports` import line
- Note: `useSessionUser<User>()` already returns `Ref<User | null>`, no cast needed

**guest.ts:**
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove `useStrapiUser` from any `#imports` import line

**sentry.ts:**
- Replace `useStrapiUser()` with `useSessionUser()`
- Remove `useStrapiUser` from any `#imports` import line

**HeaderDefault.vue:**
- Replace `useStrapiUser()` with `useSessionUser()`
- Remove from `#imports` if present

**HeroDashboard.vue:**
- Replace `useStrapiUser()` with `useSessionUser()`
- Remove from `#imports` if present

**AvatarDefault.vue:**
- Replace `useStrapiUser()` with `useSessionUser()`
- Remove from `#imports` if present

**DropdownUser.vue:**
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove from `#imports` if present

**FormEdit.vue (2 changes):**
- Replace `useStrapiAuth()` with `useSessionAuth()` (for `fetchUser`)
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove both from `#imports` if present

**FormPassword.vue:**
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove from `#imports` if present

**FormVerifyCode.vue (2 changes):**
- Replace `useStrapiAuth()` with `useSessionAuth()` (for `setToken`, `fetchUser`)
- Replace `useStrapiUser() as Ref<User | null>` with `useSessionUser<User>()`
- Remove both from `#imports` if present
- IMPORTANT: This file already has `delete nuxt._cookies[config.strapi.cookieName]` logic — do NOT change this line. The `useSessionToken` implementation caches using the same `nuxt._cookies[cookieName]` pattern.

**FormLogin.vue (1 change):**
- Replace `useStrapiAuth()` with `useSessionAuth()` (for `logout`)
- Remove from `#imports` if present

After all changes, run verification grep to confirm zero `useStrapiUser|useStrapiToken|useStrapiAuth|useStrapiClient` references remain in `apps/dashboard/app/`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -rn "useStrapiUser\|useStrapiToken\|useStrapiAuth\|useStrapiClient" apps/dashboard/app/ && echo "FAIL: stale references found" || echo "PASS: zero stale references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "useStrapiUser\|useStrapiToken\|useStrapiAuth\|useStrapiClient" apps/dashboard/app/` returns zero matches
    - `apps/dashboard/app/composables/useApiClient.ts` contains `useSessionClient()` and does NOT contain `useStrapiClient`
    - `apps/dashboard/app/composables/useImage.ts` contains `useSessionToken()` and does NOT contain `useStrapiToken`
    - `apps/dashboard/app/composables/useLogout.ts` contains `useSessionAuth()` and does NOT contain `useStrapiAuth`
    - `apps/dashboard/app/middleware/guard.global.ts` contains `useSessionUser<User>()`
    - `apps/dashboard/app/components/FormVerifyCode.vue` contains `useSessionAuth()` and `useSessionUser<User>()`
    - `apps/dashboard/app/components/FormVerifyCode.vue` still contains `delete nuxt._cookies[config.strapi.cookieName]` (unchanged)
  </acceptance_criteria>
  <done>All 14 consumer files have been migrated from useStrapiX to useSessionX with zero stale references remaining in apps/dashboard/app/.</done>
</task>

<task type="auto">
  <name>Task 2: Update test mocks and remove @nuxtjs/strapi from config and dependencies</name>
  <files>
    apps/dashboard/tests/composables/useApiClient.test.ts
    apps/dashboard/tests/stubs/imports.stub.ts
    apps/dashboard/nuxt.config.ts
    apps/dashboard/package.json
  </files>
  <read_first>
    apps/dashboard/tests/composables/useApiClient.test.ts
    apps/dashboard/tests/stubs/imports.stub.ts
    apps/dashboard/nuxt.config.ts
    apps/dashboard/package.json
  </read_first>
  <action>
**1. Update `tests/composables/useApiClient.test.ts`:**

The test currently mocks `useStrapiClient` from `#imports`. After Task 1, `useApiClient.ts` imports `useSessionClient` from the auto-imported composable (not from `#imports`). Update the mock:

BEFORE:
```typescript
vi.mock("#imports", () => ({
  useStrapiClient: () => mockClient,
  useNuxtApp: () => ({ $recaptcha: { execute: mockExecute } }),
}));
```

AFTER:
```typescript
vi.mock("#imports", () => ({
  useNuxtApp: () => ({ $recaptcha: { execute: mockExecute } }),
}));

vi.mock("~/app/composables/useSessionClient", () => ({
  useSessionClient: () => mockClient,
}));
```

The `mockClient` variable stays the same — it's a function that returns a resolved promise, which is exactly what `useSessionClient()` returns.

**2. Update `tests/stubs/imports.stub.ts`:**

Remove the stale `useStrapiAuth`, `useStrapiClient`, and `useStrapiUser` exports — they are no longer imported from `#imports` anywhere in the dashboard.

BEFORE:
```typescript
export const navigateTo = () => {};
export const useStrapiAuth = () => ({ logout: () => {} });
export const useStrapiClient = () => () => Promise.resolve({});
export const useNuxtApp = () => ({});
export const useStrapiUser = () => ({ value: null });
export const useRoute = () => ({ path: "/" });
export const defineNuxtPlugin = (fn: unknown) => fn;
export const useRuntimeConfig = () => ({ public: {} });
```

AFTER:
```typescript
export const navigateTo = () => {};
export const useNuxtApp = () => ({});
export const useRoute = () => ({ path: "/" });
export const defineNuxtPlugin = (fn: unknown) => fn;
export const useRuntimeConfig = () => ({ public: {} });
export const useState = <T>(key: string, init?: () => T) => ({ value: init ? init() : null });
export const useCookie = <T>(_name: string, _opts?: unknown) => ({ value: null as T | null });
```

The `useState` and `useCookie` stubs are added because the new session composables use these Nuxt built-ins (auto-imported via `#imports`), and tests that transitively import session composables need them resolved.

**3. Remove `@nuxtjs/strapi` from `nuxt.config.ts` modules array:**

In the `modules` array, remove the `"@nuxtjs/strapi"` entry. Keep everything else.

BEFORE:
```typescript
modules: [
    "nuxt-security",
    "@nuxt/test-utils/module",
    "@nuxt/eslint",
    "@nuxtjs/strapi",
    "@pinia/nuxt",
    ...
],
```

AFTER:
```typescript
modules: [
    "nuxt-security",
    "@nuxt/test-utils/module",
    "@nuxt/eslint",
    "@pinia/nuxt",
    ...
],
```

IMPORTANT: Do NOT remove the `strapi: { ... }` block from `nuxt.config.ts`. The custom composables read `config.strapi.url`, `config.strapi.prefix`, `config.strapi.cookieName`, `config.strapi.cookie`, and `config.strapi.auth` at runtime. The `strapi:` key is both a (now unused) module option AND a runtimeConfig declaration.

**4. Remove `@nuxtjs/strapi` from `package.json` dependencies:**

Run:
```bash
yarn workspace waldo-dashboard remove @nuxtjs/strapi
```

Then run `yarn install` from monorepo root to update lockfile.

**5. Run full test suite and typecheck:**
```bash
yarn workspace waldo-dashboard vitest run
yarn workspace waldo-dashboard nuxt typecheck
```
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace waldo-dashboard vitest run && grep -q "@nuxtjs/strapi" apps/dashboard/package.json && echo "FAIL: @nuxtjs/strapi still in package.json" || echo "PASS: @nuxtjs/strapi removed from package.json"</automated>
  </verify>
  <acceptance_criteria>
    - `yarn workspace waldo-dashboard vitest run` exits 0 (all tests pass including updated useApiClient.test.ts)
    - `apps/dashboard/tests/composables/useApiClient.test.ts` contains `vi.mock("~/app/composables/useSessionClient"` and does NOT contain `useStrapiClient`
    - `apps/dashboard/tests/stubs/imports.stub.ts` does NOT contain `useStrapiAuth`, `useStrapiClient`, or `useStrapiUser`
    - `apps/dashboard/tests/stubs/imports.stub.ts` contains `useState` and `useCookie` exports
    - `apps/dashboard/nuxt.config.ts` modules array does NOT contain `"@nuxtjs/strapi"`
    - `apps/dashboard/nuxt.config.ts` still contains the `strapi: {` config block (NOT removed)
    - `apps/dashboard/package.json` does NOT contain `"@nuxtjs/strapi"`
    - `grep -rn "useStrapiUser\|useStrapiToken\|useStrapiAuth\|useStrapiClient" apps/dashboard/` returns zero matches (including tests)
  </acceptance_criteria>
  <done>@nuxtjs/strapi completely removed from dashboard — zero references in app code, zero references in tests, removed from modules array and package.json. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard auth flow works end-to-end</name>
  <what-built>
    Complete elimination of @nuxtjs/strapi dependency from the dashboard. All session management (user state, JWT cookie, HTTP client, auth operations) now uses custom composables (useSessionUser, useSessionToken, useSessionClient, useSessionAuth) and a custom startup plugin (session.ts). The @nuxtjs/strapi module has been removed from nuxt.config.ts modules and package.json.
  </what-built>
  <how-to-verify>
    1. Start the dashboard dev server: `yarn workspace waldo-dashboard dev`
    2. Visit http://localhost:3001 — should redirect to /auth/login (guard middleware works with useSessionUser)
    3. Log in with valid credentials — should redirect to dashboard home
    4. Verify user name appears in header (HeaderDefault.vue uses useSessionUser)
    5. Verify user avatar appears (AvatarDefault.vue uses useSessionUser)
    6. Refresh the page — session should persist (session.ts plugin calls fetchUser from cookie)
    7. Navigate to /account/profile — user data should be pre-filled (FormEdit.vue uses useSessionUser)
    8. Click logout — should clear session and redirect to /auth/login
    9. Verify you cannot access /ads without being logged in (guard.global.ts)
    10. Check browser console — no errors related to useStrapiX or missing composables
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -rn "useStrapiUser\|useStrapiToken\|useStrapiAuth\|useStrapiClient" apps/dashboard/` — zero matches
2. `grep -q "@nuxtjs/strapi" apps/dashboard/package.json` — no match (removed)
3. `grep "@nuxtjs/strapi" apps/dashboard/nuxt.config.ts` — no match in modules array
4. `yarn workspace waldo-dashboard vitest run` — all tests pass
5. `yarn workspace waldo-dashboard nuxt typecheck` — compiles clean
6. Dashboard login/logout/session-persist flow works in browser
</verification>

<success_criteria>
- Zero useStrapiX references in entire apps/dashboard/ directory
- @nuxtjs/strapi removed from package.json dependencies
- @nuxtjs/strapi removed from nuxt.config.ts modules array
- strapi config block preserved in nuxt.config.ts (composables depend on it)
- All existing tests pass + useApiClient test updated with new mock
- TypeScript compiles clean
- Dashboard auth flow verified working in browser
</success_criteria>

<output>
After completion, create `.planning/phases/109-eliminate-nuxtjs-strapi-dependency-from-website-and-dashboard/109-02-SUMMARY.md`
</output>
