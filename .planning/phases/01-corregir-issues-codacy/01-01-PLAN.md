---
phase: 01-corregir-issues-codacy
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - apps/strapi/src/extensions/users-permissions/controllers/authController.ts
autonomous: true
requirements: [CODACY-FIX]

must_haves:
  truths:
    - "verifyCode coerces pendingToken to a scalar string before the where filter — an object {$ne:null} cannot inject a query operator."
    - "The reserved-username suffix is generated with crypto.randomBytes (server CSPRNG), not Math.random."
  artifacts:
    - path: "apps/strapi/src/extensions/users-permissions/controllers/authController.ts"
      provides: "scalar-coerced pendingToken lookup + CSPRNG username suffix"
      contains: "crypto.randomBytes"
  key_links:
    - from: "authController verifyCode"
      to: "strapi.db.query(VC_UID).findOne where.pendingToken"
      via: "String() coercion"
      pattern: "String\\(pendingToken\\)"
    - from: "authController reserved-username path"
      to: "crypto.randomBytes"
      via: "import node:crypto"
      pattern: "randomBytes"
---

<objective>
Fix the two genuine security reals that BOTH live in authController.ts. This plan owns the file end-to-end and produces TWO atomic commits (one per pattern bucket) — keeping CONTEXT's "atomic commit per bucket" while avoiding a file conflict with the insecure-random / NoSQL plans.

Purpose:
- NoSQL operator-injection: `findOne({ where: { pendingToken } })` receives an uncoerced value from `ctx.request.body`. Strapi v5 Query Engine interprets a nested object as `$`-operators, so `{$ne:null}` bypasses the truthy guard. Scalar coercion closes it.
- insecure-random: the reserved-username collision suffix uses `Math.random()` (flagged insecure-random). This is server Node → use `crypto.randomBytes`.

Output: authController.ts with `String(pendingToken)` coercion + `crypto.randomBytes` suffix; the Wave 0 pendingToken injection test goes GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corregir-issues-codacy/01-RESEARCH.md
</context>

<read_first>
- apps/strapi/src/extensions/users-permissions/controllers/authController.ts (read lines ~280-300 for the username-suffix block and ~385-400 for the verifyCode pendingToken block)
- apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts (the Wave 0 injection case + CHECK whether it stubs Math.random globally or pins the exact base36 suffix value)
- 01-RESEARCH.md disposition rows: "authController:396" (FIX) and "authController.ts:290" (insecure-random FIX, server) + the "Test-survives-refactor caveat" note in §3 Group 2
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coerce pendingToken to scalar (NoSQL injection fix) — COMMIT 1</name>
  <files>apps/strapi/src/extensions/users-permissions/controllers/authController.ts</files>
  <behavior>
    - verifyCode with body.pendingToken = {$ne:null} → no record matched (Wave 0 test goes GREEN).
    - verifyCode with a valid string pendingToken → unchanged behavior (existing tests stay green).
  </behavior>
  <action>
    In the verifyCode handler (~line 385-397): after destructuring `{ pendingToken, code }` and the existing `if (!pendingToken || !normalizedCode) return ctx.badRequest(...)` guard, coerce before the query:
      `const tokenValue = String(pendingToken);`
    and change the lookup to `.findOne({ where: { pendingToken: tokenValue } })`.
    Per RESEARCH this is the exact closing fix — do NOT rely on the truthy guard alone. Keep the existing badRequest guard. No `any`; no unused vars.
    COMMIT this change alone: `fix(strapi): coerce pendingToken to scalar in verifyCode (NoSQL operator-injection)`.
  </action>
  <verify>
    <automated>pnpm --filter strapi jest authController --silent 2>&1 | tail -20</automated>
  </verify>
  <done>`grep -c 'String(pendingToken)' apps/strapi/src/extensions/users-permissions/controllers/authController.ts` == 1; the Wave 0 pendingToken `$ne` injection test passes; all pre-existing authController tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Math.random username suffix with crypto.randomBytes — COMMIT 2</name>
  <files>apps/strapi/src/extensions/users-permissions/controllers/authController.ts</files>
  <action>
    Add `import { randomBytes } from "node:crypto";` (or reuse an existing crypto import if present at top of file). Replace the reserved-username suffix line (~290):
      OLD: `const suffix = Math.random().toString(36).slice(2, 6);`
      NEW: `const suffix = randomBytes(3).toString("hex").slice(0, 4);`
    Rationale (RESEARCH): server Node context → `crypto.randomBytes`, NOT `crypto.getRandomValues` (that is the client primitive used in 01-03 password.ts). The suffix is a username-collision breaker, low security impact, but applying the correct server primitive keeps the insecure-random rule active for real future hits.
    CAVEAT (RESEARCH §3 Group 2): BEFORE committing, confirm apps/strapi/tests/.../authController.test.ts does NOT globally stub Math.random nor pin the exact base36 suffix string. If it does, update that assertion to check suffix SHAPE (length 4, hex/alnum charset), not a fixed value. Do not weaken any other assertion.
    No `any`; no unused vars (if an existing crypto import already covers randomBytes, do not add a duplicate).
    COMMIT this change alone: `fix(strapi): use crypto.randomBytes for username suffix (insecure-random)`.
  </action>
  <verify>
    <automated>pnpm --filter strapi jest authController --silent 2>&1 | tail -20</automated>
  </verify>
  <done>`grep -c 'randomBytes' apps/strapi/src/extensions/users-permissions/controllers/authController.ts` >= 1; `grep -c 'Math.random' apps/strapi/src/extensions/users-permissions/controllers/authController.ts` == 0; all authController tests pass (including any shape-based suffix assertion).</done>
</task>

</tasks>

<verification>
- `grep -c 'Math.random' authController.ts` == 0.
- `grep -c 'String(pendingToken)' authController.ts` == 1.
- `grep -c 'randomBytes' authController.ts` >= 1.
- Two atomic commits, one per bucket.
- `pnpm --filter strapi jest authController` exits 0 (Wave 0 injection case now GREEN).
</verification>

<success_criteria>
- NoSQL pendingToken vector closed (scalar coercion); insecure-random suffix uses server CSPRNG.
- No `any` introduced; no unused imports/vars.
- Security buckets for these two findings will clear on the remote re-scan (verified in plan 06).
</success_criteria>

<output>
After completion, create `.planning/phases/01-corregir-issues-codacy/01-01-SUMMARY.md` noting both commits, the grep results, and whether the authController test needed a shape-based suffix assertion update.
</output>
