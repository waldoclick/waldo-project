---
phase: 01-corregir-issues-codacy
plan: 05
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - apps/website/app/composables/useProviders.ts
  - apps/strapi/src/middlewares/image-uploader.ts
autonomous: true
requirements: [CODACY-FIX]

must_haves:
  truths:
    - "redirectToProvider validates provider against ['google','facebook'] before navigating — a non-allowlisted value does not assign window.location.href."
    - "image-uploader asserts the resolved upload filepath is under the OS temp dir before fs read/write."
  artifacts:
    - path: "apps/website/app/composables/useProviders.ts"
      provides: "open-redirect allowlist guard"
      contains: "includes(provider)"
    - path: "apps/strapi/src/middlewares/image-uploader.ts"
      provides: "realpath confinement under os.tmpdir()"
      contains: "tmpdir"
  key_links:
    - from: "redirectToProvider"
      to: "window.location.href"
      via: "allowlist guard before assignment"
      pattern: "ALLOWED"
---

<objective>
Apply the 2 defensive hardenings classified VALIDATE in RESEARCH. Neither is exploitable today (open-redirect: all call sites pass literals; image-uploader: filepath is server-generated by the multipart parser), but hardening lets the corresponding Codacy findings be suppressed with a citable in-code guard, and closes the vector against future call sites.

Purpose (RESEARCH §3 Group 4):
- useProviders.ts:7 — open-redirect: validate `provider` against `['google','facebook']` before assigning `window.location.href`.
- image-uploader.ts — path-traversal (generic + non-literal-fs): assert `path.resolve(f.filepath)` is under `os.tmpdir()` before fs ops.

Output: both guards in place; the Wave 0 useProviders allowlist test goes GREEN.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corregir-issues-codacy/01-RESEARCH.md
</context>

<read_first>
- apps/website/app/composables/useProviders.ts (the `redirectToProvider` fn, line 6-8)
- apps/strapi/src/middlewares/image-uploader.ts (top of file + `convertToWebP` — `f.filepath` is read at fs.readFileSync/writeFileSync)
- apps/website/tests/composables/useProviders.test.ts (Wave 0 — the disallowed-provider test this turns GREEN)
- 01-RESEARCH.md disposition rows "useProviders.ts:7" (VALIDATE) and "image-uploader.ts" (VALIDATE)
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useProviders allowlist guard — COMMIT 1</name>
  <files>apps/website/app/composables/useProviders.ts</files>
  <behavior>
    - redirectToProvider('google') / ('facebook') → navigates as before.
    - redirectToProvider(anything else) → returns without assigning window.location.href (Wave 0 test GREEN).
  </behavior>
  <action>
    In useProviders, add a module-level allowlist and guard `redirectToProvider`:
      ```
      const ALLOWED_PROVIDERS = ["google", "facebook"] as const;
      const redirectToProvider = (provider: string): void => {
        if (!ALLOWED_PROVIDERS.includes(provider as (typeof ALLOWED_PROVIDERS)[number])) return;
        window.location.href = getProviderAuthenticationUrl(provider);
      };
      ```
    Do not touch loginWithPopup. No `any`; no unused vars.
    COMMIT: `fix(website): allowlist OAuth provider before redirect (open-redirect)`.
  </action>
  <verify>
    <automated>pnpm --filter website vitest run useProviders 2>&1 | tail -20</automated>
  </verify>
  <done>`grep -c 'ALLOWED_PROVIDERS' apps/website/app/composables/useProviders.ts` >= 1; the Wave 0 disallowed-provider test passes; the 'google' navigation test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: image-uploader realpath confinement under os.tmpdir() — COMMIT 2</name>
  <files>apps/strapi/src/middlewares/image-uploader.ts</files>
  <action>
    Add `import os from "os";` (alongside the existing path/fs imports). In `convertToWebP` (and any other fn that does fs.read/writeFileSync on `f.filepath`), before the fs ops add a confinement assertion:
      ```
      const resolved = path.resolve(f.filepath);
      if (!resolved.startsWith(path.resolve(os.tmpdir()) + path.sep)) {
        throw new Error("Upload filepath outside temp dir");
      }
      ```
    Use `resolved` for the subsequent fs.readFileSync/writeFileSync calls. This confines reads/writes to the OS temp dir where the multipart parser writes uploads (RESEARCH). Keep behavior identical for legitimate `/tmp/...` paths. No `any`; no unused vars.
    COMMIT: `fix(strapi): confine image-uploader fs ops under os.tmpdir() (path-traversal)`.
  </action>
  <verify>
    <automated>pnpm --filter strapi jest image-uploader 2>&1 | tail -20 || echo "no dedicated test — typecheck gate below"</automated>
  </verify>
  <done>`grep -c 'tmpdir' apps/strapi/src/middlewares/image-uploader.ts` >= 1; Strapi tsc passes; if an image-uploader test exists it passes, otherwise tsc is the gate (document in SUMMARY).</done>
</task>

</tasks>

<verification>
- `grep -c 'ALLOWED_PROVIDERS' useProviders.ts` >= 1; Wave 0 useProviders allowlist test GREEN.
- `grep -c 'tmpdir' image-uploader.ts` >= 1; Strapi tsc passes.
- Two atomic commits, one per file/bucket.
</verification>

<success_criteria>
- Open-redirect confined to an allowlist; image-uploader fs ops confined to the temp dir.
- These VALIDATE findings can now be suppressed citing the in-code guard (plan 06).
- No `any`; no unused vars.
</success_criteria>

<output>
After completion, create `.planning/phases/01-corregir-issues-codacy/01-05-SUMMARY.md` with the grep confirmations and the verification path used for image-uploader.
</output>
