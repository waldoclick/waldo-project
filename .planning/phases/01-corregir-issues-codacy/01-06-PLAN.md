---
phase: 01-corregir-issues-codacy
plan: 06
type: execute
wave: 2
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - .codacy.yaml
autonomous: false
requirements: [CODACY-TRIAGE, CODACY-SUPPRESS, CODACY-VERIFY]
user_setup:
  - service: codacy
    why: "Bulk-ignoring ~80 false-positive findings and verifying the remote re-scan requires a Codacy ACCOUNT API token with write scope. The project token is read-only (canary returned 401)."
    env_vars:
      - name: CODACY_ACCOUNT_API_TOKEN
        source: "Codacy → Account → Access Management → API tokens (account-level, NOT a project token)"
    dashboard_config:
      - task: "Confirm the account has admin/write access to gh/waldoclick/waldo-project so bulk-ignore is authorized"
        location: "Codacy → Organization waldoclick → repository waldo-project"

must_haves:
  truths:
    - "Every false-positive finding from the RESEARCH disposition table is marked Ignored on Codacy remote with reason 'False positive' and a per-issue justification."
    - "hashicorp-tf-password (9 hits) and the jest.setup.ts generic-api-key (1) are Ignored/excluded on remote — without blinding either rule's future detection."
    - "After the remote re-scan, the open issue count for this snapshot's findings is 0 (or Ignored-with-justification)."
  artifacts:
    - path: ".codacy.yaml"
      provides: "In-repo audit artifact: documented intent for the tf-password disposition + the existing jest.setup.ts/tests excludes (enforcement is remote, not this file)"
      contains: "hashicorp-tf-password"
  key_links:
    - from: "Codacy account API token"
      to: "POST /api/v3/analysis/.../issues/bulk-ignore"
      via: "≤100 issueIds per call, reason 'False positive'"
      pattern: "bulk-ignore"
---

<objective>
Suppression + final verification track. Mark the ~80 false positives as Ignored on Codacy remote (with per-issue justification) and confirm the remote re-scan shows the real fixes (Waves 0-1) cleared and the FPs Ignored. This plan is NOT autonomous — it is BLOCKED until the user provides a Codacy ACCOUNT API token (the project token is read-only; the canary bulk-ignore returned 401).

Purpose (RESEARCH F2/F3/OQ-1, §2, §4):
- Inline `nosemgrep` is IGNORED under SARIF (F2) and committed `.codacy.yaml` excludes are NOT honored remotely (F3). So the ONLY reliable suppression mechanism is the Codacy bulk-ignore API / dashboard Ignore action — which lives in Codacy, not git.
- In-repo audit trail = the disposition table in 01-RESEARCH.md + the justifications echoed here. The `.codacy.yaml` edit is documentary INTENT, not remote enforcement.

Output: all FPs Ignored on remote; remote re-scan green; phase verification complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corregir-issues-codacy/01-RESEARCH.md
@.planning/phases/01-corregir-issues-codacy/01-VALIDATION.md

# 01-RESEARCH.md §1 Disposition table = the authoritative per-issue SUPPRESS list with justifications.
# Org: gh/waldoclick, repo: waldo-project. Endpoint base: /api/v3/analysis/organizations/gh/waldoclick/repositories/waldo-project
</context>

<read_first>
- 01-RESEARCH.md §1 Disposition table (every SUPPRESS row + justification), §2 Suppression plan, §4 Verification, F1/F2/F3, OQ-1/OQ-2/OQ-3
- .codacy.yaml (root — already contains `ignore:` with jest.setup.ts and **/tests/**; schema has engines/exclude_paths/ignore only — NO per-rule disable stanza)
- 01-VALIDATION.md §"Manual-Only Verifications"
</read_first>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint A: Provide Codacy ACCOUNT API token (blocks remote suppression)</name>
  <action>Pause for the user to supply the account-level Codacy API token — Claude cannot self-issue this credential. Do not proceed to any bulk-ignore call until it is set.</action>
  <what-built>All autonomous fixes (Waves 0-1) are committed. This step needs a credential Claude cannot self-issue.</what-built>
  <how-to-verify>
    Provide a Codacy ACCOUNT API token (Codacy → Account → Access Management → API tokens — account-level, not a project token) with write access to gh/waldoclick/waldo-project. Set it as CODACY_ACCOUNT_API_TOKEN in the execution environment.
    Why required: the bulk-ignore endpoint and the ignoredIssues verification need account write scope. The project token returned 401 on the canary (RESEARCH).
  </how-to-verify>
  <resume-signal>Type "token set" once CODACY_ACCOUNT_API_TOKEN is available, or "skip remote" to defer the remote suppression to a follow-up and close the phase on the autonomous fixes only.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Trigger remote re-scan of the fix branch; capture the live open-issue set</name>
  <files></files>
  <action>
    Push the Wave 0-1 branch (already committed) so Codacy re-analyzes. Then fetch the LIVE open issues via the Codacy search API (issueIds rotate — do NOT reuse snapshot ids):
      `POST /api/v3/analysis/organizations/gh/waldoclick/repositories/waldo-project/issues/search` with the account token (header `api-token: $CODACY_ACCOUNT_API_TOKEN`).
    Record: (a) which of the 6 security reals + 9 any now show 0 (Waves 0-1 cleared them); (b) the live issueId for each remaining SUPPRESS finding, mapped to its RESEARCH disposition justification. Confirm the 3 already-resolved-at-HEAD issues (payment.test.ts unused-var bb82c170, the 2 v-html in 08e35269) auto-cleared — NO ACTION needed on those.
  </action>
  <verify>
    <automated>echo "Manual API verification — see SUMMARY for the live open-issue table"</automated>
  </verify>
  <done>A live issueId → justification map exists for every SUPPRESS finding; the 6 reals + 9 any confirmed cleared on remote; the 3 stale-resolved confirmed auto-cleared.</done>
</task>

<task type="auto">
  <name>Task 2: CANARY one bulk-ignore, then batch the rest (≤100/call)</name>
  <files></files>
  <action>
    CANARY (RESEARCH OQ-1 + advisor): issue ONE single-issue bulk-ignore first to confirm the account token actually has write scope (project token gave 401):
      `POST /api/v3/analysis/organizations/gh/waldoclick/repositories/waldo-project/issues/bulk-ignore`
      header `api-token: $CODACY_ACCOUNT_API_TOKEN`, body `{ "issueIds": ["<one FP id>"], "reason": "False positive", "comment": "<justification from RESEARCH disposition>" }`.
    If the canary 401s → STOP, the token lacks write scope; re-raise the checkpoint. If it succeeds → batch ALL remaining SUPPRESS findings in calls of ≤100 issueIds, each call body carrying `reason: "False positive"` and a per-issue `comment` drawn from the RESEARCH disposition justification.
    Covered by these bulk-ignore calls (RESEARCH §1 + §2):
      - NoSQL SUPPRESS rows (~33): scalar-coerced / server-derived values.
      - path-traversal SUPPRESS rows (weather, indicador, bbdd-backup, config/database.ts): fixed server dirs / server-generated segments.
      - v-html (12 current): each routes through useSanitize/sanitize-html.
      - SSRF (2), unsafe-dynamic-method (1), non-literal-regexp (1): fixed hosts / env-driven allowlist with throw / hardcoded constant.
      - hashicorp-tf-password (9): the "password" show/hide idiom — structural noise (see Task 3 for the in-repo audit note).
      - generic-api-key (jest.setup.ts, 1): Transbank sandbox key in a test setup file (path-level Ignore; do NOT disable the rule — OQ-2).
      - image-uploader (5) + useProviders (1): suppress AFTER plan 05 landed, citing the new realpath/allowlist guard.
    Do NOT use inline `nosemgrep` (F2 — ignored under SARIF). Do NOT bulk-ignore any of the 6 reals or 9 any (those are FIXED, must show 0 on their own).
  </action>
  <verify>
    <automated>echo "Manual API verification — see SUMMARY for bulk-ignore call log + canary result"</automated>
  </verify>
  <done>Canary succeeded; every SUPPRESS finding from the disposition table is Ignored on remote with reason 'False positive' + justification comment; reals/any NOT ignored.</done>
</task>

<task type="auto">
  <name>Task 3: In-repo audit artifact for the tf-password / generic-api-key dispositions</name>
  <files>.codacy.yaml</files>
  <action>
    The root `.codacy.yaml` schema (engines/exclude_paths/ignore) has NO per-rule disable stanza, and remote does NOT honor committed excludes anyway (F3) — so this edit is an IN-REPO AUDIT ARTIFACT documenting intent, not remote enforcement. Add a clearly-labeled comment block near the `ignore:` section recording:
      - `hashicorp-tf-password`: 100% structural noise (the "password" show/hide input-type idiom across 8 Vue forms + 1 email-template-name string in authController:562); disposition = repo-wide Ignore on Codacy remote (Task 2). Zero real hits → the only legitimate blanket suppression.
      - `detected-generic-api-key` on jest.setup.ts: Transbank SANDBOX key in a test setup file; jest.setup.ts is ALREADY in the `ignore:` list above; disposition = path-level Ignore (NOT a rule-wide disable — the rule retains genuine detection value, OQ-2).
    Do NOT invent a schema-invalid per-rule `disable:` key. Keep the existing `ignore:`/`exclude_paths:` entries intact. Commit: `docs(codacy): record tf-password + generic-api-key suppression rationale (audit trail)`.
  </action>
  <verify>
    <automated>grep -c 'hashicorp-tf-password' .codacy.yaml</automated>
  </verify>
  <done>`grep -c 'hashicorp-tf-password' .codacy.yaml` >= 1; the comment block documents both dispositions; no schema-invalid keys added; jest.setup.ts still present in `ignore:`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint B: Confirm remote shows 0 open with auditable ignores</name>
  <action>Pause for the user to confirm the Codacy dashboard reflects the expected end-state after the re-scan and bulk-ignore.</action>
  <what-built>Remote re-scan after Waves 0-1 fixes + bulk-ignore of all FPs.</what-built>
  <how-to-verify>
    1. Open the Codacy dashboard for gh/waldoclick/waldo-project on the fix branch.
    2. Confirm the open-issue count for the snapshot's 100 findings is 0 (reals fixed; FPs Ignored-with-justification).
    3. Spot-check 2-3 Ignored findings show reason "False positive" + the justification comment.
    4. Confirm hashicorp-tf-password and the generic-api-key finding are Ignored, and that neither rule is globally disabled (future hits would still surface).
    Verify the ignored set via `POST /api/v3/analysis/.../ignoredIssues/search`.
  </how-to-verify>
  <resume-signal>Type "verified" if open count is 0 and ignores carry justifications, or describe discrepancies.</resume-signal>
</task>

</tasks>

<verification>
- Remote open-issue count for the snapshot findings == 0 (reals fixed in Waves 0-1; FPs Ignored).
- Each Ignored finding carries reason "False positive" + a justification from the RESEARCH disposition table.
- hashicorp-tf-password + generic-api-key Ignored without globally disabling either rule.
- `.codacy.yaml` carries the in-repo audit comment block (documentary).
- The 6 reals + 9 any show 0 on their own (NOT via Ignore).
</verification>

<success_criteria>
- Codacy remote shows 0 open for this snapshot — real issues fixed, false positives Ignored with auditable justification.
- No rule with a real hit was blinded.
- In-repo audit trail = 01-RESEARCH.md disposition table + this plan's justifications + the .codacy.yaml comment block.
</success_criteria>

<output>
After completion, create `.planning/phases/01-corregir-issues-codacy/01-06-SUMMARY.md` with: the canary result, the bulk-ignore call log (issueIds per batch + reason), the live open-issue-count before/after, and the ignoredIssues verification output.
</output>
