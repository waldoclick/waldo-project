---
phase: 01-corregir-issues-codacy
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/strapi/tests/api/ad/services/ad.service.saveDraft.test.ts
  - apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts
  - apps/strapi/tests/api/payment/services/checkout.service.test.ts
  - apps/website/tests/composables/useProviders.test.ts
autonomous: true
requirements: [CODACY-FIX, CODACY-VERIFY]

must_haves:
  truths:
    - "A characterization test for ad.service saveDraft exists and is collected by Jest (new-vs-update branch + ownership)."
    - "An injection test case feeds ad_id={$gt:0} to saveDraft and is RED until the Wave 1 coercion fix lands."
    - "An injection test case feeds pendingToken={$ne:null} to authController verifyCode and is RED until the Wave 1 coercion fix lands."
    - "An injection test case feeds payload.pack={$ne:''} to checkout.service and is RED until the Wave 1 coercion fix lands."
    - "A Vitest test asserts redirectToProvider('evil://x') does NOT navigate (allowlist guard), RED until Wave 1 hardening."
  artifacts:
    - path: "apps/strapi/tests/api/ad/services/ad.service.saveDraft.test.ts"
      provides: "saveDraft characterization + ad_id injection case"
      contains: "saveDraft"
    - path: "apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts"
      provides: "pendingToken object-injection case (extension)"
      contains: "$ne"
    - path: "apps/strapi/tests/api/payment/services/checkout.service.test.ts"
      provides: "payload.pack object-injection case (extension)"
      contains: "$ne"
    - path: "apps/website/tests/composables/useProviders.test.ts"
      provides: "open-redirect allowlist guard test"
      contains: "redirectToProvider"
  key_links:
    - from: "ad.service.saveDraft.test.ts"
      to: "apps/strapi/src/api/ad/services/ad.ts saveDraft"
      via: "import + invocation"
      pattern: "saveDraft"
---

<objective>
Wave 0 regression gate. Create/extend the characterization and injection tests that GUARD the Wave 1 security fixes BEFORE any production code changes (per CONTEXT regression gate + VALIDATION Wave 0).

Purpose: The three NoSQL coercion fixes (ad.ts:1141, authController:396, checkout:112) and the useProviders allowlist hardening touch high-blast-radius auth/payment paths. The injection test cases written here are RED BY DESIGN — they assert the post-fix secure behavior and MUST fail until Wave 1 lands. This is the Nyquist guard that proves each fix actually closes the vector.

Output: 1 new Strapi test file (saveDraft) + 2 extended Strapi test files (authController, checkout) + 1 new Vitest test (useProviders).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-corregir-issues-codacy/01-RESEARCH.md
@.planning/phases/01-corregir-issues-codacy/01-VALIDATION.md

# CLAUDE.md test rules: tests live in root tests/ dirs (never co-located); Strapi uses Jest AAA, mock all external deps; Vitest for website.
</context>

<read_first>
- apps/strapi/src/api/ad/services/ad.ts (the saveDraft method around line 1140 — read the full method to mirror its mocks)
- apps/strapi/tests/api/payment/services/checkout.service.test.ts (existing — mock path pattern is `../../../../src/...`)
- apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts (existing — mirror its strapi.db.query mock dispatch)
- apps/website/tests/composables/useProviders.test.ts does NOT exist yet — model it on apps/website/tests/composables/useSessionAuth.test.ts (vi.stubGlobal pattern for Nuxt auto-imports)
- 01-RESEARCH.md §"Wave 0 gaps" and §"Group 1 — NoSQL operator-injection"
</read_first>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saveDraft characterization + ad_id injection test (NEW FILE)</name>
  <files>apps/strapi/tests/api/ad/services/ad.service.saveDraft.test.ts</files>
  <behavior>
    - Characterization (passes NOW): saveDraft({...no ad_id}, userId) takes the CREATE branch → calls strapi.db.query(...).create; saveDraft({ad_id: 42, ...}, userId) takes the UPDATE branch and re-checks ownership before update.
    - Injection (RED until Wave 1 fix): saveDraft({ ad_id: { $gt: 0 }, ...}, userId) MUST NOT reach an update with an operator object as the id. Assert the call coerces to a scalar — i.e. the where clause receives a NaN/numeric, not `{$gt:0}` — so no record is matched/updated. This assertion FAILS today because `ad.ad_id as number | undefined` passes the object through untouched.
  </behavior>
  <action>
    Create the NEW test file mirroring the AAA mock style of apps/strapi/tests/api/ad/services/ad.service.test.ts (mock paths use `../../../../src/...`; mock strapi.db.query with a per-UID mockImplementation dispatch as in 122-04 precedent). Import the ad service saveDraft method. Stub `strapi.db.query("api::ad.ad")` returning { create, findOne, update } jest.fns.
    Three tests:
      1. CREATE branch: payload without ad_id → expect create called, update NOT called.
      2. UPDATE branch: payload with numeric ad_id and matching owner → expect findOne/update called with where.id === that number.
      3. INJECTION (RED): payload with `ad_id: { $gt: 0 }` → expect that the value handed to the where filter is NOT the object `{$gt:0}` (e.g. expect Number-coerced/NaN, or expect update NOT called). Document inline: "RED until 01-02 applies Number(ad.ad_id)".
    Do not modify production code. No `any`; no `_`-prefixed unused vars.
  </action>
  <verify>
    <automated>pnpm --filter strapi jest ad.service.saveDraft --silent 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at apps/strapi/tests/api/ad/services/ad.service.saveDraft.test.ts; Jest collects 3 tests; the CREATE + UPDATE characterization tests pass; the injection test is RED (documented in SUMMARY as expected-red until 01-02). `grep -c 'saveDraft' apps/strapi/tests/api/ad/services/ad.service.saveDraft.test.ts` >= 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend authController + checkout tests with object-injection cases</name>
  <files>apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts, apps/strapi/tests/api/payment/services/checkout.service.test.ts</files>
  <behavior>
    - authController (RED until 01-01): verifyCode with `ctx.request.body = { pendingToken: { $ne: null }, code: '123456' }` MUST be rejected — assert the value reaching `findOne({ where: { pendingToken } })` is a scalar string (or the request is badRequest), so the `{$ne:null}` operator never matches a record.
    - checkout (RED until 01-02): processCheckout with `payload.pack = { $ne: '' }` MUST NOT match a pack — assert the where.name handed to `ad-pack findOne` is a scalar string, not the operator object.
  </behavior>
  <action>
    EXTEND the two existing test files (do not rewrite). In authController.test.ts add an `it('rejects pendingToken operator-injection object', ...)` that builds a ctx with `body.pendingToken = { $ne: null }` and asserts the where value passed to the verification-code findOne mock is `String`-typed (e.g. `expect(typeof capturedWhere.pendingToken).toBe('string')`) — RED today because pendingToken is uncoerced. In checkout.service.test.ts add an `it('rejects pack operator-injection object', ...)` asserting the captured `where.name` for the ad-pack findOne is a string when `payload.pack = { $ne: '' }` — RED today. Capture the where arg via the existing findOne mock (use `mockFn.mock.calls[0][0]`). No `any`; no unused vars.
  </action>
  <verify>
    <automated>pnpm --filter strapi jest authController checkout.service --silent 2>&1 | tail -25</automated>
  </verify>
  <done>Both files contain a new `$ne` injection test; `grep -c '\$ne' apps/strapi/tests/extensions/users-permissions/controllers/authController.test.ts` >= 1 and same for checkout.service.test.ts; the new cases are RED (documented expected-red until 01-01/01-02). Pre-existing tests in both files still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useProviders allowlist redirect test (NEW FILE)</name>
  <files>apps/website/tests/composables/useProviders.test.ts</files>
  <behavior>
    - RED until 01-05: redirectToProvider('google') sets window.location.href; redirectToProvider('evil://x') (or any value not in ['google','facebook']) MUST NOT set window.location.href.
  </behavior>
  <action>
    Create NEW Vitest file modeled on apps/website/tests/composables/useSessionAuth.test.ts. Use vi.stubGlobal to provide useSessionAuth() returning a `getProviderAuthenticationUrl` spy. Mock window.location.href via a settable stub. Tests: (1) `redirectToProvider('google')` → getProviderAuthenticationUrl called and href assigned; (2) `redirectToProvider('evil://x')` → href NOT assigned (assert the spy/href untouched). Test (2) is RED today because useProviders has no allowlist guard. No `any`; no unused vars.
  </action>
  <verify>
    <automated>pnpm --filter website vitest run useProviders 2>&1 | tail -20</automated>
  </verify>
  <done>File exists at apps/website/tests/composables/useProviders.test.ts; `grep -c 'redirectToProvider' apps/website/tests/composables/useProviders.test.ts` >= 1; the disallowed-provider test is RED (documented expected-red until 01-05).</done>
</task>

</tasks>

<verification>
- All 4 test files exist in root tests/ dirs (never co-located).
- Characterization/positive cases pass; injection/negative cases are RED by design (each documented in SUMMARY with the Wave 1 plan that turns it green).
- No production code modified in this plan.
</verification>

<success_criteria>
- `pnpm --filter strapi jest ad.service.saveDraft authController checkout.service` collects all targeted tests.
- `pnpm --filter website vitest run useProviders` collects the new test.
- The injection/allowlist cases are RED (expected) — this is the Nyquist guard, NOT a failure. Wave 1 plans (01, 02, 05) turn them green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-corregir-issues-codacy/01-00-SUMMARY.md` listing each test added and explicitly marking which cases are RED-by-design and which Wave 1 plan turns each green.
</output>
