---
phase: 05-audit-log-for-every-crud-operation-in-strapi
plan: 05
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - apps/strapi/src/api/payment/services/ad.service.ts
  - apps/strapi/src/api/payment/services/pack.service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every logger.info/warn/error call in ad.service.ts and pack.service.ts routes through the shared logAudit helper with the { actor, actor_type, data } envelope"
    - "Each call site keeps its original log level — no info/warn/error downgrade"
    - "Each call site's human-readable message string is preserved verbatim (including template-literal messages with no metadata object)"
    - "actor resolves to the site's existing local userId (never a Webpay reference); sites with no userId in scope use actor system"
    - "Zero business-logic, control-flow, amount, or order-identity changes — only how metadata is passed into the logger"
  artifacts:
    - path: "apps/strapi/src/api/payment/services/ad.service.ts"
      provides: "All 12 logger call sites reshaped to logAudit"
      contains: "logAudit"
    - path: "apps/strapi/src/api/payment/services/pack.service.ts"
      provides: "All 16 logger call sites reshaped to logAudit"
      contains: "logAudit"
  key_links:
    - from: "apps/strapi/src/api/payment/services/ad.service.ts"
      to: "apps/strapi/src/utils/audit-log/index.ts"
      via: "import logAudit* level functions"
      pattern: "utils/audit-log"
    - from: "apps/strapi/src/api/payment/services/pack.service.ts"
      to: "apps/strapi/src/utils/audit-log/index.ts"
      via: "import logAudit* level functions"
      pattern: "utils/audit-log"
---

<objective>
Homologate the logger call sites in the two heaviest payment service files — `ad.service.ts` (12 calls) and `pack.service.ts` (16 calls) — to route through the shared `logAudit` helper from 05-03, reshaping metadata into the `{ actor, actor_type, data }` envelope.

Purpose: Same as 05-04 — these are the payment/ad-creation flows the user flagged as "super importantes." RESHAPE-ONLY per CLAUDE.md Payment Rules: no logic, control flow, amounts, or order-identity may change; only how metadata reaches the logger.
Output: Both service files with all logger calls converted to `logAuditInfo`/`logAuditWarn`/`logAuditError`, levels and messages preserved, existing metadata under `data`, `actor` from local `userId` (or `"system"`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-CONTEXT.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-03-PLAN.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-04-PLAN.md

<interfaces>
<!-- Shared helper from 05-03. Both files import from 3 levels up (src/api/payment/services/ → src/utils): -->
```ts
import { logAuditInfo, logAuditWarn, logAuditError } from "../../../utils/audit-log";
// logAuditInfo(message, { actor: number | "system", actor_type, data?: Record<string, unknown> })
```

Both files currently import the raw logger: `import logger from "../../../utils/logtail";` (ad.service.ts line 4, pack.service.ts line 3). Remove it once every call is converted (CLAUDE.md: no unused imports). Only import the level functions actually used in each file.

Level counts (verified 2026-07-02):
- ad.service.ts: info=8, warn=0, error=4 (12 total) → import logAuditInfo + logAuditError only (no warn sites)
- pack.service.ts: info=8, warn=3, error=5 (16 total) → import all three

IMPORTANT metadata-shape note: several sites in these files use template-literal MESSAGE-ONLY calls with NO metadata object, e.g. `logger.info(\`Aviso ${ad.ad_id} modificado por el usuario ${userId}\`);` and `logger.info(\`Reserva ${i + 1}/${total_ads} creada\`, {...})`. For message-only calls, keep the message verbatim and OMIT `data` (helper defaults it). Do NOT restructure the template literal.

Actor resolution (CONTEXT.md — end-user payment flows, NOT admin):
- `userId` in scope → `actor: userId, actor_type: "plugin::users-permissions.user"`
- No user id in scope (e.g. email-send error handlers that only have an error) → `actor: "system", actor_type: "system"`. Do NOT invent a userId lookup.
- Never use a Webpay reference (buy_order, token) as actor — stays under `data`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape all 12 logger call sites in ad.service.ts to logAudit</name>
  <read_first>
    - apps/strapi/src/api/payment/services/ad.service.ts (read fully; note each logger.* call, level, message, metadata object, and whether userId is in scope at that site)
    - apps/strapi/src/utils/audit-log/index.ts (the helper)
    - apps/strapi/src/api/payment/services/pack.service.ts (do NOT edit here — read only to confirm no shared symbol collisions; edited in Task 2)
  </read_first>
  <files>apps/strapi/src/api/payment/services/ad.service.ts</files>
  <action>
Reshape EVERY `logger.info/warn/error` in ad.service.ts (8 info, 0 warn, 4 error) to `logAuditInfo`/`logAuditError`. Mechanical, per-site, reshape-only.

Rules (identical to 05-04):
- Keep level: `logger.info`→`logAuditInfo`, `logger.error`→`logAuditError`.
- Copy MSG verbatim (including template literals — do not restructure).
- `<DATA>` = existing metadata object minus the field that became `actor`; drop nothing else. Message-only calls (e.g. `logger.info(\`Aviso ${ad.ad_id} modificado por el usuario ${userId}\`)`) → omit `data`.
- actor: `userId` in scope → `actor: userId, actor_type: "plugin::users-permissions.user"`; email-error / no-userId sites (e.g. "Error sending ad creation emails:") → `actor: "system", actor_type: "system"`.

Anchor sites (re-locate by message text): "Creando reservas de anuncios del pack", `Reserva ${i + 1}/${total_ads} creada`, `Aviso ${ad.ad_id} modificado por el usuario ${userId}` (message-only), `Error al procesar aviso: ${error.message}` [error], "Error sending ad creation emails:" [error → actor system].

Add `import { logAuditInfo, logAuditError } from "../../../utils/audit-log";`. Remove `import logger from "../../../utils/logtail";` after conversion. Do NOT import logAuditWarn (no warn sites in this file — unused import would violate CLAUDE.md).

DO NOT touch any non-logger line: no changes to reservation counts, ad amounts, pack logic, order-identity, or control flow.

Commit `refactor(05-05): homologate ad.service.ts logger calls to shared logAudit helper`.
  </action>
  <verify>
    <automated>cd apps/strapi && test "$(grep -cE 'logger\.(info|warn|error)' src/api/payment/services/ad.service.ts)" = "0" && grep -q "utils/audit-log" src/api/payment/services/ad.service.ts && echo AD-SERVICE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "logger\.(info|warn|error)" apps/strapi/src/api/payment/services/ad.service.ts` returns 0
    - `grep -c "import logger from" apps/strapi/src/api/payment/services/ad.service.ts` returns 0 (unused import removed)
    - `grep -c "logAuditInfo" apps/strapi/src/api/payment/services/ad.service.ts` returns 8; `grep -c "logAuditError" ...` returns 4
    - `grep -c "logAuditWarn" apps/strapi/src/api/payment/services/ad.service.ts` returns 0 (no warn sites; not imported)
    - Message strings preserved: `grep -q "Error al procesar aviso" ...` and `grep -q "modificado por el usuario" ...` both match
    - "Error sending ad creation emails:" site uses `actor: "system"` (no userId in that catch scope)
    - Diff scope: `git diff apps/strapi/src/api/payment/services/ad.service.ts` — every changed hunk is a logger→logAudit line or the import lines; no amount/reservation/control-flow line altered
    - `cd apps/strapi && npx tsc --noEmit` shows no NEW errors vs the pre-change state
  </acceptance_criteria>
  <done>All 12 ad.service.ts logger calls reshaped to logAudit; levels/messages preserved; actor from userId or system; logger import removed; diff proven logger-only.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape all 16 logger call sites in pack.service.ts to logAudit</name>
  <read_first>
    - apps/strapi/src/api/payment/services/pack.service.ts (read fully; note each logger.* call, level, message, metadata object, userId availability)
    - apps/strapi/src/utils/audit-log/index.ts (the helper)
  </read_first>
  <files>apps/strapi/src/api/payment/services/pack.service.ts</files>
  <action>
Reshape EVERY `logger.info/warn/error` in pack.service.ts (8 info, 3 warn, 5 error) to the matching level function. Mechanical, per-site, reshape-only. Same rules as Task 1.

Key: this file HAS warn sites — `logger.warn`→`logAuditWarn` (preserve warn level; do NOT downgrade to info). Import all three: `import { logAuditInfo, logAuditWarn, logAuditError } from "../../../utils/audit-log";`.

actor: `userId` in scope → `actor: userId, actor_type: "plugin::users-permissions.user"`; Webpay transaction-response sites or error handlers with no userId → `actor: "system", actor_type: "system"`. Webpay references (buy_order, token, transaction data) stay under `data`, never as actor.

Message-only template-literal calls → omit `data`, keep message verbatim.

Remove `import logger from "../../../utils/logtail";` after conversion.

DO NOT touch any non-logger line: no changes to pack purchase amounts, Webpay transaction creation/response handling, Zoho sync logic, order-identity, or control flow.

Commit `refactor(05-05): homologate pack.service.ts logger calls to shared logAudit helper`.
  </action>
  <verify>
    <automated>cd apps/strapi && test "$(grep -cE 'logger\.(info|warn|error)' src/api/payment/services/pack.service.ts)" = "0" && test "$(grep -c logAuditWarn src/api/payment/services/pack.service.ts)" = "3" && echo PACK-SERVICE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "logger\.(info|warn|error)" apps/strapi/src/api/payment/services/pack.service.ts` returns 0
    - `grep -c "import logger from" apps/strapi/src/api/payment/services/pack.service.ts` returns 0
    - `grep -c "logAuditInfo" apps/strapi/src/api/payment/services/pack.service.ts` returns 8; `grep -c "logAuditWarn" ...` returns 3; `grep -c "logAuditError" ...` returns 5
    - All 3 warn sites are logAuditWarn (NOT downgraded to info) — this is the level-preservation gate for the file with warn sites
    - Webpay-reference fields (buy_order/token) never appear as the `actor` value (grep the transaction sites: actor is a userId or "system", not a Webpay field)
    - Diff scope: `git diff apps/strapi/src/api/payment/services/pack.service.ts` — every changed hunk is logger→logAudit or import lines; no pack-amount/Webpay-transaction/control-flow line altered
    - `cd apps/strapi && npx tsc --noEmit` shows no NEW errors vs pre-change state
  </acceptance_criteria>
  <done>All 16 pack.service.ts logger calls reshaped to logAudit; all 3 warn sites stay warn; messages preserved; actor from userId or system; logger import removed; diff proven logger-only.</done>
</task>

</tasks>

<verification>
- Both files: `grep -cE "logger\.(info|warn|error)"` returns 0; logAudit counts match (ad.service info 8/error 4; pack.service info 8/warn 3/error 5)
- `git diff` on both files shows only logger→logAudit call sites and import lines changed (reshape-only; payment rules respected)
- `cd apps/strapi && npx jest --maxWorkers=2` — no NEW failures vs the 05-01 baseline (ad.approve.zoho.test.ts, indicador.test.ts, general.utils.test.ts)
- `cd apps/strapi && npx tsc --noEmit` — no NEW errors
</verification>

<success_criteria>
- All ad.service.ts + pack.service.ts logger calls route through the shared logAudit helper
- Log levels and message strings preserved verbatim (pack.service's 3 warn sites stay warn)
- actor is the real local userId (or system); never a Webpay reference
- Zero business-logic/control-flow/amount/order-identity changes (diff is logger-only)
- No new Jest or tsc failures vs baseline
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-05-SUMMARY.md`
</output>
</content>
