---
phase: 05-audit-log-for-every-crud-operation-in-strapi
plan: 02
type: execute
wave: 3
depends_on: ["05-03", "05-04", "05-05", "05-06"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Audit log lines for create/update/delete appear in the local rotating log file (apps/strapi/logs/app-*.log) and/or Better Stack, NOT in a Strapi Content Manager collection (the DB table was removed in 05-03)"
    - "A create, update, and delete performed via the admin panel each produce one audit log line with actor_type admin::user and the acting admin's actor id"
    - "A create/update/delete performed via the public API with a user JWT produces log lines with actor_type plugin::users-permissions.user"
    - "Context-less writes (seeders on a fresh boot, the recalculateSortPriorities backfill) appear as actor_type system log lines"
    - "At least one homologated payment/ad log line (from 05-04/05/06) renders with the new { actor, actor_type, data } envelope and the correct actor (real userId, never a Webpay reference)"
  artifacts: []
  key_links: []
---

<objective>
Human verification that the reworked audit-log mechanism (logger-based, built in 05-03) and the homologated payment/ad logs (05-04/05/06) work end-to-end against a running Strapi instance — writing correctly-shaped audit lines to the existing Winston logger (local rotating file + Better Stack), with correct actor discrimination.

Purpose: The Jest suites prove handler + helper logic in isolation with mocks. This plan proves the real integration: the subscriber actually fires on real DB writes and emits a log line (not a DB row), actor discrimination is correct against real auth strategies, and the homologated payment logs carry the new envelope with the right actor. Per the CONTEXT.md PIVOT, the read path is Better Stack's log explorer plus the local `apps/strapi/logs/app-*.log` rotating file — NOT the Strapi Content Manager (the DB table was removed).
Output: Human sign-off (or a defect list to feed a gap-closure plan via `/gsd:plan-phase 5 --gaps`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-CONTEXT.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-03-PLAN.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-04-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of the logger-based audit mechanism + homologated payment logs against a running Strapi</name>
  <read_first>
    - .planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-03-SUMMARY.md (what the subscriber rework actually built — logger-based, DB table removed)
    - apps/strapi/src/subscribers/audit-log.subscriber.ts (the handler being exercised — actor mapping, logAudit call, message format `Audit ${action}: ${uid}`)
    - apps/strapi/src/utils/audit-log/index.ts (the shared helper — { actor, actor_type, data } envelope, three levels)
    - apps/strapi/src/utils/logtail/index.ts (confirm the DailyRotateFile transport writes to logs/app-%DATE%.log — this is the local file to tail)
    - apps/strapi/src/index.ts (confirm registerAuditLogSubscriber is registered at the top of bootstrap())
  </read_first>
  <files>N/A — verification only, no repo files modified</files>
  <action>
What was built: (05-03) the audit-log content-type DB table was REMOVED; the global lifecycle subscriber now writes one audit LOG LINE per create/update/delete via the shared `logAudit` helper (message format `Audit create|update|delete: <uid>`, envelope `{ actor, actor_type, data: { content_type_uid, record_id, record_document_id } }`), routed through the existing Winston logger (Better Stack + console + local `apps/strapi/logs/app-%DATE%.log` rotating file, 90-day retention). (05-04/05/06) the ~62 existing payment/ad logger calls were reshaped to the same `logAudit` envelope, preserving message text, log level, and metadata. Unit tests pass; this checkpoint confirms real running behavior and the log read path.

Start Strapi locally (from repo root): `pnpm --filter waldo-strapi dev` (or the project's standard command for the strapi app). Wait for boot to complete, then perform these checks. The read path is the LOG, not Content Manager.

1. Log read path (replaces the old Content Manager visibility check):
   - Identify today's log file: `ls -t apps/strapi/logs/app-*.log | head -1`.
   - Tail it live in a second terminal: `tail -f apps/strapi/logs/app-*.log | grep -E "Audit (create|update|delete):"`.
   - (Optional) Confirm the same lines appear in Better Stack's log explorer if reachable (external, already used daily) — this is a bonus, not required if the local file shows the lines.
   - Expected: NO "Audit Log" collection-type should exist in the Strapi admin Content Manager anymore (the DB table was removed in 05-03) — if it still appears, that is a defect (stale content-type not deleted).

2. System lines from boot (expected per scope):
   - On boot with seeders enabled (`APP_RUN_SEEDERS=true`) or the `recalculateSortPriorities` ad backfill, confirm the log contains `Audit create|update: ...` lines with `"actor_type":"system"` and `"actor":"system"`. Grep: `grep '"actor_type":"system"' apps/strapi/logs/app-*.log | head`.

3. Admin-panel write → admin::user line:
   - In Content Manager, create a new entry in any editable content-type (e.g. add a FAQ or a Term), then edit it, then delete it.
   - Expected in the log: three new lines — `Audit create: api::faq.faq` (or the chosen uid), `Audit update: ...`, `Audit delete: ...` — each with `"actor_type":"admin::user"` and `"actor":<your admin user id>`, and `data` containing `content_type_uid`, `record_id`, `record_document_id`.

4. Public-API user write → plugin::users-permissions.user line (if convenient):
   - Perform a public-API write authenticated with a normal user JWT (e.g. the website/dashboard flow that creates or updates an ad).
   - Expected: a log line with `"actor_type":"plugin::users-permissions.user"` and `"actor":<that user's id>`.

5. Homologated payment/ad log line (05-04/05/06 verification — CONTEXT.md flagged these as "super importantes"):
   - Trigger at least one homologated payment/ad log — e.g. save an ad draft (fires ad.ts "Borrador de anuncio creado") or run a checkout/pack flow.
   - Expected: the corresponding message (e.g. "Borrador de anuncio creado") now appears with the new envelope: top-level `"actor"` = the real user id (NOT a Webpay reference like buy_order/token_ws), `"actor_type":"plugin::users-permissions.user"`, and the original metadata (e.g. adId) nested under `"data"`. Confirm the log LEVEL is unchanged (an error-level payment log still shows as error, not info).

6. Business writes still succeed:
   - Confirm the create/update/delete in step 3 and the payment/ad action in step 5 all completed normally (no 500s, entries saved/removed, payment flow unaffected) — proving the audit hook and the reshaped logs do not break real writes.
  </action>
  <verify>
    <automated>MISSING — this requires a running Strapi instance, admin-panel + payment-flow interaction, and inspection of live log lines by a human; there is no CLI/API substitute for the real-actor-tagging and payment-flow checks. The human runs `tail -f apps/strapi/logs/app-*.log | grep "Audit"` as a semi-automated aid, but pass/fail is human-judged.</automated>
  </verify>
  <done>All checks pass: audit lines appear in the local log file / Better Stack (no Content Manager collection remains); admin writes tag admin::user; public-API user writes tag plugin::users-permissions.user; boot/seeder writes tag system; at least one homologated payment/ad log renders the new envelope with a real userId actor at the correct level; real writes unaffected.</done>
  <acceptance_criteria>
    NOTE: checkpoint:human-verify task — per task_breakdown, checkpoint tasks are the documented exception to the grep-verifiable acceptance_criteria rule; the criteria below are human-observable pass conditions, confirmed via the resume-signal.
    - Audit lines (`Audit create|update|delete: <uid>`) appear in apps/strapi/logs/app-*.log (and/or Better Stack); NO "Audit Log" collection-type remains in the Strapi admin Content Manager
    - Three consecutive admin-panel writes (create, update, delete) on one content-type produce three log lines with actor_type = admin::user, actor = acting admin id, and data.content_type_uid / data.record_id / data.record_document_id populated
    - A public-API write with a user JWT produces a line with actor_type = plugin::users-permissions.user (if exercised)
    - Boot/seeder/backfill writes appear as lines with "actor_type":"system" and "actor":"system"
    - At least one homologated payment/ad log line (e.g. "Borrador de anuncio creado") renders with the new { actor, actor_type, data } envelope, actor = real userId (never a Webpay reference), original log level preserved
    - All exercised business + payment writes complete normally (no 500s; entries saved/removed; payment flow unaffected)
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe which check failed (with the observed / missing log lines) so a gap-closure plan can be created via `/gsd:plan-phase 5 --gaps`.</resume-signal>
</task>

</tasks>

<verification>
Human confirms: audit lines appear in the local rotating log file / Better Stack (no Content Manager collection remains); admin writes tag admin::user; public-API user writes tag plugin::users-permissions.user; boot/seeder writes tag system; at least one homologated payment/ad log renders the new envelope with a real userId actor at the correct level; real writes unaffected.
</verification>

<success_criteria>
All verification checks pass and the user types "approved". Any failure is captured as a defect for `/gsd:plan-phase 5 --gaps`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-02-SUMMARY.md`
</output>
</content>
