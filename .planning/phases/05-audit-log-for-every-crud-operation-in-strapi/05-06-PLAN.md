---
phase: 05-audit-log-for-every-crud-operation-in-strapi
plan: 06
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - apps/strapi/src/api/ad/services/ad.ts
  - apps/strapi/src/api/payment/services/checkout.service.ts
  - apps/strapi/src/api/payment/services/free-ad.service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every logger.info/warn/error call in ad.ts, checkout.service.ts, and free-ad.service.ts routes through the shared logAudit helper with the { actor, actor_type, data } envelope"
    - "Each call site keeps its original log level — no info/warn/error downgrade"
    - "Each call site's human-readable message string is preserved verbatim"
    - "actor resolves to the site's existing local userId (never a Webpay/order reference); sites with no userId in scope use actor system"
    - "Zero business-logic, control-flow, amount, or order-identity changes — only how metadata is passed into the logger"
  artifacts:
    - path: "apps/strapi/src/api/ad/services/ad.ts"
      provides: "All 5 logger call sites reshaped to logAudit"
      contains: "logAudit"
    - path: "apps/strapi/src/api/payment/services/checkout.service.ts"
      provides: "All 5 logger call sites reshaped to logAudit"
      contains: "logAudit"
    - path: "apps/strapi/src/api/payment/services/free-ad.service.ts"
      provides: "The 1 logger call site reshaped to logAudit"
      contains: "logAudit"
  key_links:
    - from: "apps/strapi/src/api/ad/services/ad.ts"
      to: "apps/strapi/src/utils/audit-log/index.ts"
      via: "import logAudit* level functions"
      pattern: "utils/audit-log"
    - from: "apps/strapi/src/api/payment/services/checkout.service.ts"
      to: "apps/strapi/src/utils/audit-log/index.ts"
      via: "import logAudit* level functions"
      pattern: "utils/audit-log"
    - from: "apps/strapi/src/api/payment/services/free-ad.service.ts"
      to: "apps/strapi/src/utils/audit-log/index.ts"
      via: "import logAuditError"
      pattern: "utils/audit-log"
---

<objective>
Homologate the remaining logger call sites — `ad.ts` (5 calls), `checkout.service.ts` (5 calls), `free-ad.service.ts` (1 call) — to route through the shared `logAudit` helper from 05-03, reshaping metadata into the `{ actor, actor_type, data }` envelope.

Purpose: Completes the payment/ad-log homologation (with 05-04 and 05-05). RESHAPE-ONLY per CLAUDE.md Payment Rules — no logic, control flow, amounts, or order-identity change; only how metadata reaches the logger. `ad.ts` also contains the draft-creation flow and `recalculateSortPriorities` backfill; touch ONLY the logger lines.
Output: The three files with all logger calls converted to `logAuditInfo`/`logAuditWarn`/`logAuditError`, levels and messages preserved, existing metadata under `data`, `actor` from local `userId` (or `"system"`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-CONTEXT.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-03-PLAN.md
@.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-04-PLAN.md

<interfaces>
<!-- Shared helper from 05-03. All three files import from 3 levels up: -->
```ts
// ad.ts is at src/api/ad/services/ad.ts; checkout/free-ad at src/api/payment/services/ — both 3 levels up from src/utils
import { logAuditInfo, logAuditError } from "../../../utils/audit-log";
```

All three files currently import the raw logger: `import logger from "../../../utils/logtail";` (ad.ts line 14, checkout.service.ts line 5, free-ad.service.ts line 3). Remove it once every call is converted.

Level counts (verified 2026-07-02):
- ad.ts: info=3, warn=0, error=2 (5 total) → import logAuditInfo + logAuditError
- checkout.service.ts: info=1, warn=0, error=4 (5 total) → import logAuditInfo + logAuditError
- free-ad.service.ts: info=0, warn=0, error=1 (1 total) → import logAuditError only

Actor resolution (CONTEXT.md — end-user flows, NOT admin):
- `userId` in scope → `actor: userId, actor_type: "plugin::users-permissions.user"`
- No userId in scope → `actor: "system", actor_type: "system"`. Do NOT invent a lookup.
  - ad.ts specifics: draft-creation/update/error sites ("Borrador de anuncio creado/actualizado", "Error al guardar borrador de anuncio") all have `userId` in scope. The Zoho ad-approval sites ("Zoho contact not found for ad approval — skipping CRM sync", "Zoho sync failed for ad approval — approval flow unaffected") log `{ adId }` today; `userId` IS in scope in `approveAd` (it uses `actived_by: userId`) → use `actor: userId, actor_type: "plugin::users-permissions.user"`, keep `adId` (and `error`) under `data`.
  - checkout.service.ts specifics: "Documento Facto generado exitosamente (checkout)" and the Facto/Zoho error sites — use `userId` if in scope at that site, else `actor: "system"`. Per CONTEXT.md the "order-record-creation-failure" site ("Failed to create order record for checkout") has NO userId in scope → `actor: "system", actor_type: "system"`.
  - free-ad.service.ts: the single "Error sending free ad creation emails:" site — per CONTEXT.md keep `actor: "system", actor_type: "system"` (no userId in that error scope).
- Never use an order/Webpay reference as actor — stays under `data`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reshape the 5 logger call sites in ad.ts to logAudit</name>
  <read_first>
    - apps/strapi/src/api/ad/services/ad.ts (read the logger sites: draft flow ~1202/1236/1244 and Zoho approval ~682/695; confirm userId is in scope at each — note approveAd uses actived_by: userId)
    - apps/strapi/src/utils/audit-log/index.ts (the helper)
  </read_first>
  <files>apps/strapi/src/api/ad/services/ad.ts</files>
  <action>
Reshape EVERY `logger.info/error` in ad.ts (3 info, 2 error) to `logAuditInfo`/`logAuditError`. Mechanical, reshape-only.

Rules: keep level; copy MSG verbatim; `<DATA>` = existing metadata minus the field that became actor; drop nothing else.

Per-site actor:
- "Borrador de anuncio creado" (logs `{ userId, adId: newAd.id }`) → `actor: userId, actor_type: "plugin::users-permissions.user"`, `data: { adId: newAd.id }`.
- "Borrador de anuncio actualizado" (logs `{ userId, adId }`) → `actor: userId, ..., data: { adId }`.
- "Error al guardar borrador de anuncio" [error] (logs `{ userId, error }`) → `actor: userId, ..., data: { error: (error as Error).message }`.
- "Zoho contact not found for ad approval — skipping CRM sync" (logs `{ adId }`) → `actor: userId, actor_type: "plugin::users-permissions.user"`, `data: { adId }` (userId is in scope in approveAd).
- "Zoho sync failed for ad approval — approval flow unaffected" [error] (logs `{ adId, error }`) → `actor: userId, ..., data: { adId, error: zohoError.message }`.

Add `import { logAuditInfo, logAuditError } from "../../../utils/audit-log";`. Remove `import logger from "../../../utils/logtail";` after conversion.

DO NOT touch any non-logger line: leave `recalculateSortPriorities`, draft creation `strapi.db.query(...).create/update`, ownership checks, and the Zoho `Promise.resolve().then()` control flow exactly as they are — only the logger calls inside them change.

Commit `refactor(05-06): homologate ad.ts logger calls to shared logAudit helper`.
  </action>
  <verify>
    <automated>cd apps/strapi && test "$(grep -cE 'logger\.(info|warn|error)' src/api/ad/services/ad.ts)" = "0" && grep -q "utils/audit-log" src/api/ad/services/ad.ts && echo AD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "logger\.(info|warn|error)" apps/strapi/src/api/ad/services/ad.ts` returns 0
    - `grep -c "import logger from" apps/strapi/src/api/ad/services/ad.ts` returns 0
    - `grep -c "logAuditInfo(" apps/strapi/src/api/ad/services/ad.ts` returns 3; `grep -c "logAuditError(" ...` returns 2
    - Message strings preserved: `grep -q "Borrador de anuncio creado" ...`, `grep -q "Zoho sync failed for ad approval" ...` both match
    - Zoho approval sites use `actor: userId` (not adId as actor); adId stays under data
    - Diff scope (file boundary): `git diff --stat apps/strapi/src/api/ad/services/ad.ts` shows ONLY the target file(s) changed
    - Diff scope (mechanical no-logic-change gate): the following command MUST print nothing — it collapses re-indented preserved lines (metadata fields moving under `data:`) via `uniq -u`, then flags any surviving changed line touching a forbidden business surface (amounts, order-identity, Webpay refs, control flow, redirects):
      ```
      git diff -U0 -- apps/strapi/src/api/ad/services/ad.ts | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' \
        | sed -E 's/^[+-][[:space:]]*//' | sort | uniq -u \
        | grep -E '(ctx\.redirect|buy_order|buyOrder|TBK_|token_ws|\.documentId|\bamount\b|\bprice\b|\btotal\b|\bif\b|\belse\b|\breturn\b|await )'
      ```
      Rationale: preserved metadata fields (e.g. `amount: …`, `orderId: …documentId`, `tbkToken`) re-indent identically on the `-`/`+` sides and are dropped by `uniq -u`; a genuine logic edit (e.g. `amount = price * 2`, a redirect swapped to `buyOrder`) differs between sides and survives to be flagged. Zero output = reshape-only proven mechanically.
    - Diff scope (human residual): after the mechanical gate passes, a manual read of `git diff` confirms nothing subtle slipped through outside logger/data blocks (this prose check now backs up, not replaces, the mechanical gate above)
    - `cd apps/strapi && npx tsc --noEmit` shows no NEW errors vs pre-change state
  </acceptance_criteria>
  <done>All 5 ad.ts logger calls reshaped to logAudit; levels/messages preserved; actor is userId; adId under data; backfill/draft/Zoho control flow untouched; diff proven logger-only.</done>
</task>

<task type="auto">
  <name>Task 2: Reshape the 5 (checkout.service.ts) + 1 (free-ad.service.ts) logger call sites to logAudit</name>
  <read_first>
    - apps/strapi/src/api/payment/services/checkout.service.ts (read the 5 logger sites; confirm userId availability per site — note "Failed to create order record for checkout" has no userId)
    - apps/strapi/src/api/payment/services/free-ad.service.ts (the single "Error sending free ad creation emails:" site)
    - apps/strapi/src/utils/audit-log/index.ts (the helper)
  </read_first>
  <files>apps/strapi/src/api/payment/services/checkout.service.ts, apps/strapi/src/api/payment/services/free-ad.service.ts</files>
  <action>
Reshape checkout.service.ts (1 info, 4 error) and free-ad.service.ts (1 error). Mechanical, reshape-only. Same rules.

checkout.service.ts per-site actor:
- "Documento Facto generado exitosamente (checkout)" [info] and the Facto-error site — use `userId` if in scope at that site, else `actor: "system"`.
- "Failed to create order record for checkout" [error] — NO userId in scope → `actor: "system", actor_type: "system"`; keep existing metadata (error, order-related fields) under `data` (never as actor — order.documentId stays a data field, never the actor).
- "Zoho sync failed for checkout — payment unaffected" [error] — use `userId` if in scope else `actor: "system"`.
Import `import { logAuditInfo, logAuditError } from "../../../utils/audit-log";`. Remove the raw logger import.

free-ad.service.ts:
- "Error sending free ad creation emails:" [error] (logs `{ error }`) → `actor: "system", actor_type: "system"`, `data: { error }`.
Import `import { logAuditError } from "../../../utils/audit-log";` (error-only file — do NOT import logAuditInfo/Warn). Remove the raw logger import.

DO NOT touch any non-logger line: checkout order-record creation, Facto document generation, Zoho sync, and free-ad email-send control flow stay exactly as they are.

Commit `refactor(05-06): homologate checkout.service.ts and free-ad.service.ts logger calls to shared logAudit helper`.
  </action>
  <verify>
    <automated>cd apps/strapi && test "$(grep -cE 'logger\.(info|warn|error)' src/api/payment/services/checkout.service.ts)" = "0" && test "$(grep -cE 'logger\.(info|warn|error)' src/api/payment/services/free-ad.service.ts)" = "0" && echo CHECKOUT-FREEAD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "logger\.(info|warn|error)" apps/strapi/src/api/payment/services/checkout.service.ts` returns 0; same for free-ad.service.ts returns 0
    - Both files: `grep -c "import logger from"` returns 0 (unused imports removed)
    - checkout.service.ts: `grep -c "logAuditInfo(" ...` returns 1; `grep -c "logAuditError(" ...` returns 4
    - free-ad.service.ts: `grep -c "logAuditError(" ...` returns 1; `grep -c "logAuditInfo(\|logAuditWarn(" ...` returns 0
    - "Failed to create order record for checkout" site uses `actor: "system"`; free-ad email-error site uses `actor: "system"`
    - Message strings preserved: `grep -q "Failed to create order record for checkout" ...` and `grep -q "Error sending free ad creation emails" ...` both match
    - Diff scope (file boundary): `git diff --stat apps/strapi/src/api/payment/services/checkout.service.ts apps/strapi/src/api/payment/services/free-ad.service.ts` shows ONLY these two files changed
    - Diff scope (mechanical no-logic-change gate) — RUN ONCE PER FILE; each invocation MUST print nothing (collapses re-indented preserved `data:` lines via `uniq -u`, then flags any surviving changed line touching a forbidden business surface):
      ```
      for f in apps/strapi/src/api/payment/services/checkout.service.ts apps/strapi/src/api/payment/services/free-ad.service.ts; do \
        echo "== $f =="; git diff -U0 -- "$f" | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' \
          | sed -E 's/^[+-][[:space:]]*//' | sort | uniq -u \
          | grep -E '(ctx\.redirect|buy_order|buyOrder|TBK_|token_ws|\.documentId|\bamount\b|\bprice\b|\btotal\b|\bif\b|\belse\b|\breturn\b|await )'; \
      done
      ```
      Rationale: preserved metadata fields re-indenting under `data:` are dropped by `uniq -u` (identical on `-`/`+` sides); a genuine order-record/Facto/control-flow edit differs between sides and survives to be flagged. Zero output for BOTH files = reshape-only proven mechanically.
    - Diff scope (human residual): after the mechanical gate passes for both files, a manual read of `git diff` confirms nothing subtle slipped through outside logger/data blocks (backs up, not replaces, the mechanical gate)
    - `cd apps/strapi && npx tsc --noEmit` shows no NEW errors vs pre-change state
  </acceptance_criteria>
  <done>All 5 checkout.service.ts + 1 free-ad.service.ts logger calls reshaped to logAudit; levels/messages preserved; no-userId sites use system; imports cleaned; diff proven logger-only.</done>
</task>

</tasks>

<verification>
- All three files: `grep -cE "logger\.(info|warn|error)"` returns 0; logAudit counts match (ad.ts info 3/error 2; checkout info 1/error 4; free-ad error 1)
- `git diff` on all three files shows only logger→logAudit call sites and import lines changed (reshape-only; payment rules respected)
- `cd apps/strapi && npx jest --maxWorkers=2` — no NEW failures vs the 05-01 baseline (ad.approve.zoho.test.ts, indicador.test.ts, general.utils.test.ts)
- `cd apps/strapi && npx tsc --noEmit` — no NEW errors
- Existing payment/ad Jest tests already mock `utils/logtail` as `{ info, warn, error }` (verified 2026-07-02) and assert NOTHING on logger calls; the logAudit helper forwards through that same mocked logtail via a default import (identical to the current production import style), so these suites keep passing WITHOUT test edits. Confirm post-change: the reshaped module's own test file (e.g. payment.test.ts) shows no NEW failure vs the Task 1 baseline. Do NOT add a jest.mock of `utils/audit-log` to these production-flow tests unless a specific one starts failing.
</verification>

<success_criteria>
- All ad.ts + checkout.service.ts + free-ad.service.ts logger calls route through the shared logAudit helper
- Log levels and message strings preserved verbatim
- actor is the real local userId (or system for no-user sites); never an order/Webpay reference
- Zero business-logic/control-flow/amount/order-identity changes (diff is logger-only)
- No new Jest or tsc failures vs baseline
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-log-for-every-crud-operation-in-strapi/05-06-SUMMARY.md`
</output>
</content>
