---
phase: 05-rediseno-cuenta
plan: 09
type: execute
wave: 7
depends_on: ["05-08", "05-02", "05-03"]
files_modified:
  - apps/website/app/components/StatsAdModal.vue
  - apps/website/app/components/AccountMain.vue
  - apps/website/app/components/CardProfileAd.vue
  - apps/website/app/stores/user.store.ts
  - apps/website/app/scss/components/_account.scss
autonomous: true
requirements: [STAT-VIEW]
must_haves:
  truths:
    - "Opening 'Estadísticas' on an active ad shows a modal with total views, contacts, conversión, prom/día and a 14-day bar chart fed by GET /api/ads/:documentId/stats"
    - "The Panel KPIs 'Vistas totales' and 'Contactos recibidos' show real aggregated data from GET /api/ads/me/views-total (+ contacts), not placeholders"
    - "The Panel triage 'Ver estadísticas' action opens the same stats modal"
  artifacts:
    - path: "apps/website/app/components/StatsAdModal.vue"
      provides: "Per-ad stats modal with chart.js 14-day bar chart"
      contains: "ads/${"
    - path: "apps/website/app/stores/user.store.ts"
      provides: "loadAdStats(documentId) + loadPanelViewsTotal() store actions"
      contains: "loadAdStats"
  key_links:
    - from: "apps/website/app/components/StatsAdModal.vue"
      to: "GET /api/ads/:documentId/stats"
      via: "useApiClient fetch"
      pattern: "/stats"
    - from: "apps/website/app/components/AccountMain.vue"
      to: "GET /api/ads/me/views-total"
      via: "store loadPanelViewsTotal"
      pattern: "loadPanelViewsTotal"
    - from: "CardProfileAd.vue + AccountMain triage"
      to: "StatsAdModal.vue"
      via: "open-stats event / shared modal state"
      pattern: "StatsAdModal"
---

<objective>
Wire the statistics frontend (STAT-VIEW): create a per-ad stats modal matching `design/account.dc.html` lines 471-496 (3 KPI tiles + "Vistas en los últimos 14 días" bar chart with chart.js + vue-chartjs + average/día + info note), open it from the "Estadísticas" action on ad cards and the Panel triage "Ver estadísticas", and replace the Panel placeholder KPIs ("Vistas totales", "Contactos recibidos") with real data from the 05-08 aggregation endpoints.

Purpose: Final wave. Depends on 05-08 (endpoints), 05-02 (Panel/AccountMain), and 05-03 (CardProfileAd/announcements) because it edits those owned files. Uses already-installed chart.js + vue-chartjs (no new deps).
Output: New StatsAdModal.vue (chart.js Bar), store actions loadAdStats/loadPanelViewsTotal, AccountMain KPI/triage wiring, CardProfileAd "Estadísticas" trigger, and modal styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html
@.planning/phases/05-rediseno-cuenta/05-08-PLAN.md
@.planning/phases/05-rediseno-cuenta/05-02-PLAN.md
@.planning/phases/05-rediseno-cuenta/05-03-PLAN.md

<interfaces>
<!-- 05-08 endpoints (built first, behind the Nuxt proxy at /api/...): -->
<!-- GET /api/ads/:documentId/stats?days=14 → { total, series:number[14], contacts, conversion, avgPerDay } -->
<!-- GET /api/ads/me/views-total → { total } -->

<!-- HTTP client pattern in this app (CLAUDE.md + STATE.md): -->
<!-- useApiClient() is the single HTTP client; declare at setup scope, never inside callbacks. -->
<!-- Stores use a client = useApiClient() / useStrapiClient pattern — follow ads.store.ts loadAdBySlug (client(`ads/slug/${slug}`)). -->

<!-- chart.js + vue-chartjs are installed (see ChartSales.vue for the registration pattern): -->
<!-- import { Bar } from "vue-chartjs"; import { Chart as ChartJS, CategoryScale, LinearScale, BarElement, Tooltip } from "chart.js"; ChartJS.register(...) -->

<!-- Mockup modal (lines 471-496): header (title + status badge + cat), 3 KPI tiles (Vistas totales, Contactos, Conversión), -->
<!-- "Vistas en los últimos 14 días" + "Prom. X / día", the bar chart (last bar amber_hover, others light amber), info note, Cerrar button. -->

<!-- AccountMain.vue (from 05-02) has placeholder KPIs totalViews/totalContacts + triage rows with a TODO stats action. -->
<!-- CardProfileAd.vue (from 05-03) has an "Estadísticas" primary/menu action (currently a no-op placeholder). -->

<!-- Tokens: $ink $ink2 $muted $amber $amber_hover $cream $line $success $warning $error $white. SCSS: _account.scss (modifier account--stats / stats modal block). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add loadAdStats + loadPanelViewsTotal store actions (with a util for chart data shaping)</name>
  <files>apps/website/app/utils/stats.ts, apps/website/tests/utils/stats.test.ts, apps/website/app/stores/user.store.ts</files>
  <read_first>
    - apps/website/app/stores/user.store.ts (add the two actions following loadUserAds/loadUserOrders pattern + the client usage)
    - apps/website/app/stores/ads.store.ts loadAdBySlug (client(`ads/slug/${slug}`) HTTP pattern)
    - .planning/phases/05-rediseno-cuenta/05-08-PLAN.md (endpoint response shapes)
    - apps/website/app/utils/ (find an existing pure util + its test to mirror the test location: apps/website/tests/utils/)
  </read_first>
  <behavior>
    - Test 1: a pure util `buildStatsChartData(series: number[])` returns a chart.js `{ labels, datasets }` where labels.length === series.length and dataset.data === series (used by StatsAdModal). 100% unit coverage at creation (CLAUDE.md util rule).
    - Test 2: `buildStatsChartData` highlights the last bar (last backgroundColor differs from the rest) — matches the mockup last-bar emphasis.
  </behavior>
  <action>
    1. Add a pure util `apps/website/app/utils/stats.ts` exporting `buildStatsChartData(series: number[]): { labels: string[]; datasets: [{ data: number[]; backgroundColor: string[]; borderRadius: number }] }`. Labels = last N day short-dates (or index strings). backgroundColor array: all but the last = light amber (`#f2dbaa`), last = `$amber_hover` value `#efb85c` (these are chart colors, not SCSS — literals allowed in JS chart config; keep them consistent with tokens). Write its test in `apps/website/tests/utils/stats.test.ts` (Vitest) covering Test 1-2.
    2. Add to `user.store.ts`: `loadAdStats(documentId: string, days = 14)` → `client(`ads/${documentId}/stats?days=${days}`, { method:"GET" })` returning `{ total, series, contacts, conversion, avgPerDay }`; and `loadPanelViewsTotal()` → `client("ads/me/views-total", { method:"GET" })` returning `{ total }`. Follow the existing client-at-setup pattern; cast responses per the Strapi SDK cast rules. Export both from the store return.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vitest run tests/utils/stats.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "buildStatsChartData" apps/website/app/utils/stats.ts` matches
    - `test -f apps/website/tests/utils/stats.test.ts` (util test exists at the mirrored tests/ path)
    - `grep -E "loadAdStats|loadPanelViewsTotal" apps/website/app/stores/user.store.ts` matches both
    - `grep -E "ads/.*\\/stats|ads/me/views-total" apps/website/app/stores/user.store.ts` matches the endpoint paths
    - Vitest util test passes (verify command).
  </acceptance_criteria>
  <done>Two store actions + a tested chart-data util exist; Vitest green.</done>
</task>

<task type="auto">
  <name>Task 2: Create StatsAdModal.vue (chart.js 14-day bar) + add stats modal styles</name>
  <files>apps/website/app/components/StatsAdModal.vue, apps/website/app/scss/components/_account.scss</files>
  <read_first>
    - design/account.dc.html lines 471-496 (modal layout: header, 3 KPI tiles, chart, prom/día, info note, Cerrar)
    - apps/website/app/components/ChartSales.vue (vue-chartjs Bar + ChartJS.register pattern + options)
    - apps/website/app/utils/stats.ts (buildStatsChartData from Task 1)
    - apps/website/app/scss/components/_account.scss (add stats-modal styles; keep @use + other modifiers)
  </read_first>
  <action>
    Create `apps/website/app/components/StatsAdModal.vue`. Props: `open: boolean`, `ad: { documentId, name, category?, status? } | null`. Emits `close`. On open (watch `open`+`ad`, immediate), call `useUserStore().loadAdStats(ad.documentId)` and render. Layout matches the mockup (BEM block e.g. `stats` modifier `stats--ad`, or `account--stats`): backdrop + dialog; header (eyebrow "Estadísticas del anuncio" + title `ad.name` + status badge + category); a 3-tile KPI row (Vistas totales = total, Contactos = contacts, Conversión = `conversion%`); "Vistas en los últimos 14 días" + "Prom. {avgPerDay} / día"; a `<Bar :data="chartData" :options="chartOptions" />` where `chartData = buildStatsChartData(series)`; the cream info note (lucide `Info`, $tag); a "Cerrar" button emitting close. Register chart.js modules locally (CategoryScale, LinearScale, BarElement, Tooltip) per ChartSales. NO inline `style=` (chart.js config object in JS is fine; it's not a template style attribute). Add `.stats--ad` (or chosen block) styles to `_account.scss` using phase-04 tokens (backdrop rgba ink; dialog $white radius 14px; KPI tiles 1px $line; bars colored from the util; note $cream/$tag).
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-09-check.css 2>&1 | head -5 && test -f /tmp/05-09-check.css && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "StatsAdModal" || echo "no StatsAdModal type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "from \"vue-chartjs\"|Bar" apps/website/app/components/StatsAdModal.vue` matches (chart.js Bar used, no new dep)
    - `grep "loadAdStats" apps/website/app/components/StatsAdModal.vue` matches (fetches real stats)
    - `grep -E "Conversión|Prom\\.|14 días" apps/website/app/components/StatsAdModal.vue` matches the mockup copy
    - `grep -c "style=" apps/website/app/components/StatsAdModal.vue` returns 0 (template has no inline style attrs)
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY
    - SCSS compiles; vue-tsc clean for StatsAdModal.vue.
    - Final pixel fidelity verified visually against design/account.dc.html lines 471-496.
  </acceptance_criteria>
  <done>StatsAdModal renders real per-ad stats + 14-day chart matching the mockup; compiles; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the modal + real KPIs into AccountMain.vue and CardProfileAd.vue</name>
  <files>apps/website/app/components/AccountMain.vue, apps/website/app/components/CardProfileAd.vue</files>
  <read_first>
    - apps/website/app/components/AccountMain.vue (from 05-02 — has placeholder totalViews/totalContacts + triage TODO action)
    - apps/website/app/components/CardProfileAd.vue (from 05-03 — has the "Estadísticas" action placeholder)
    - apps/website/app/components/StatsAdModal.vue (Task 2 — props open/ad, emit close)
    - .planning/phases/05-rediseno-cuenta/05-02-PLAN.md and 05-03-PLAN.md (the TODO markers placed there)
  </read_first>
  <action>
    1. AccountMain.vue: replace the placeholder "Vistas totales" / "Contactos recibidos" KPI values with real data. Call `useUserStore().loadPanelViewsTotal()` for "Vistas totales"; derive "Contactos recibidos" from the same panel aggregation if exposed, else sum contacts available (best-available; if a dedicated panel-contacts total isn't in 05-08, keep contacts from the per-ad data the panel already loads — Claude's discretion, but the value must be real, not a static 0). Render `<StatsAdModal :open="statsOpen" :ad="statsAd" @close="statsOpen=false" />` and wire the triage "Ver estadísticas" action to set `statsAd`/`statsOpen`. Remove the `<!-- TODO 05-09 ... -->` markers.
    2. CardProfileAd.vue: wire its "Estadísticas" primary/menu action to open the modal — either emit an `open-stats` event consumed by AccountAnnouncements/parent that owns the shared StatsAdModal, OR include the modal per card (prefer a single shared modal lifted to the list/parent to avoid N modals). Implement the cleanest of the two; ensure clicking "Estadísticas" opens StatsAdModal for that ad's documentId. Remove the no-op placeholder.
    Keep all other behavior. NO inline styles.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "AccountMain|CardProfileAd|AccountAnnouncements" || echo "no type errors in wired files"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "loadPanelViewsTotal" apps/website/app/components/AccountMain.vue` matches (real "Vistas totales")
    - `grep "StatsAdModal" apps/website/app/components/AccountMain.vue` matches (modal mounted)
    - `grep -rL "TODO 05-09" apps/website/app/components/AccountMain.vue` (no leftover 05-09 TODO markers) — i.e. `grep -c "TODO 05-09" apps/website/app/components/AccountMain.vue` returns 0
    - `grep -E "StatsAdModal|open-stats" apps/website/app/components/CardProfileAd.vue` matches (stats action wired)
    - `grep -c "style=" apps/website/app/components/AccountMain.vue apps/website/app/components/CardProfileAd.vue` returns 0
    - vue-tsc clean for the wired files.
    - Final behavior verified visually: clicking "Estadísticas"/"Ver estadísticas" opens the modal with real data (orchestrator loop).
  </acceptance_criteria>
  <done>Panel KPIs show real aggregated data; "Estadísticas"/"Ver estadísticas" open the modal; placeholders removed; vue-tsc clean.</done>
</task>

</tasks>

<verification>
- Vitest util test green; app.scss compiles; vue-tsc clean for StatsAdModal/AccountMain/CardProfileAd.
- No new dependencies (chart.js + vue-chartjs already installed).
- Panel "Vistas totales"/"Contactos recibidos" are real; stats modal shows total/contacts/conversión/prom + 14-day chart.
- No inline styles; no token-value changes.
</verification>

<success_criteria>
The per-ad stats modal (lines 471-496) and the Panel KPIs render real event-sourced aggregated data via the 05-08 endpoints, opened from ad cards and Panel triage, using chart.js — matching the mockup.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-09-SUMMARY.md`
</output>
