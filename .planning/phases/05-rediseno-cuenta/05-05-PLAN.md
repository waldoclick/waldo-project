---
phase: 05-rediseno-cuenta
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - apps/website/app/components/AccountProfile.vue
  - apps/website/app/components/AccountEdit.vue
  - apps/website/app/scss/components/_account.scss
autonomous: true
requirements: [ACC-PROFILE]
must_haves:
  truths:
    - "Mi perfil (/cuenta/perfil) renders the Ver/Editar segmented toggle, the public-profile preview card, the completeness bar, the 'Información personal' grid, and the 'Identidad pública' cards — matching the mockup"
    - "Editar mode (/cuenta/perfil/editar) renders the account-type tabs + the FormProfile fields styled to the mockup"
    - "All profile read/edit behavior (FormProfile save, region/comuna, company toggle, validation) is unchanged (no regression)"
  artifacts:
    - path: "apps/website/app/components/AccountProfile.vue"
      provides: "Redesigned profile 'Ver' view (preview + completeness + info grid + identidad pública)"
      contains: "account--profile__preview"
    - path: "apps/website/app/components/AccountEdit.vue"
      provides: "Redesigned profile 'Editar' wrapper around FormProfile"
      contains: "account--edit"
    - path: "apps/website/app/scss/components/_account.scss"
      provides: "account--profile + account--edit styles using new tokens"
      contains: "account--profile__preview"
  key_links:
    - from: "apps/website/app/pages/cuenta/perfil/index.vue"
      to: "AccountProfile.vue"
      via: "component import (unchanged)"
      pattern: "AccountProfile"
    - from: "AccountEdit.vue"
      to: "FormProfile.vue"
      via: "form reused (not rebuilt)"
      pattern: "FormProfile"
---

<objective>
Restyle the Mi perfil views to match `design/account.dc.html` lines 260-419: the "Ver" mode (public-profile preview card with cover/avatar/name/badges, completeness bar, "Información personal" 2-col grid, and "Identidad pública" cards for username/avatar/cover) and the "Editar" mode (account-type Persona Natural / Empresa tabs wrapping the existing FormProfile fields). The Ver/Editar toggle maps to the existing two routes `/cuenta/perfil` (AccountProfile) and `/cuenta/perfil/editar` (AccountEdit).

Purpose: View 4. Shares `_account.scss` → runs after 05-04 (wave 5). Reuses AccountProfile + AccountEdit + FormProfile; restyle only, zero behavior regression.
Output: Restyled AccountProfile.vue + AccountEdit.vue + new `account--profile` / `account--edit` styles. FormProfile internals unchanged (its own styling may be lightly scoped if needed, Claude's discretion — but no logic change).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html
@.planning/phases/05-rediseno-cuenta/05-04-PLAN.md

<interfaces>
<!-- AccountProfile.vue (Ver mode) — existing computeds (keep): -->
const user = useSessionUser();
// getAddress, getBusinessAddress, formattedBirthdate, regionName, businessRegionName
// user fields: firstname, lastname, rut, commune?.name, region?.name, address, address_number, phone, email,
//   postal_code, birthdate, is_company, business_name/type/rut/commune/postal_code, pro_status, username, avatar, cover

<!-- It already renders CardInfo cards in a grid. The mockup "Información personal" grid (lines 319-325) is the same data, restyled. -->
<!-- "Identidad pública" cards (lines 327-341, proItems): Nombre de usuario (@username), Foto de perfil, Portada — -->
<!--   each with a CTA. Reuse the EXISTING routes as CTA targets (no new modal logic required): -->
<!--   Cambiar usuario → /cuenta/username ; Foto de perfil → /cuenta/avatar ; Portada → /cuenta/cover -->
<!--   (These pages already exist and stay working — D-10 zero regression. Building inline modals instead is Claude's discretion.) -->

<!-- AccountEdit.vue (Editar mode) just wraps <FormProfile />. The mockup edit card (lines 369-417) adds: -->
<!--   account-type segmented tabs (Persona Natural / Empresa) + the fields grid + save/cancel footer. -->
<!--   FormProfile ALREADY contains the fields + company toggle + region/comuna + save. Do NOT duplicate or rebuild that logic. -->
<!--   Restyle the wrapper + (optionally, scoped) the FormProfile field/button look to the mockup. -->

<!-- Ver/Editar toggle: AccountProfile shows a segmented control linking to /cuenta/perfil (Ver, active) and /cuenta/perfil/editar (Editar). -->
<!-- AccountEdit shows the same control with Editar active. Use <nuxt-link>; matches the existing two-route structure. -->

<!-- New phase-04 tokens: $ink $ink2 $muted $amber $amber_hover $cream $line $success $white. PRO badge uses $ink bg. -->
<!-- SCSS: scss/components/_account.scss, modifiers account--profile and account--edit. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AccountProfile.vue (Ver mode: preview + completeness + info grid + identidad pública)</name>
  <files>apps/website/app/components/AccountProfile.vue</files>
  <read_first>
    - apps/website/app/components/AccountProfile.vue (current — keep all computeds + CardInfo usage; restyle to mockup)
    - design/account.dc.html lines 260-366 (Ver mode: header + Ver/Editar toggle, preview card, completeness, info grid, identidad pública, PRO plan card)
    - design/account.dc.html lines 896-909 (personalFields + proItems derivation)
    - apps/website/app/components/AccountProfile.vue computeds (regionName, getAddress, formattedBirthdate)
    - apps/website/app/pages/cuenta/perfil/index.vue (DO NOT MODIFY)
  </read_first>
  <action>
    Restyle AccountProfile.vue to the mockup Ver mode (block `account`, modifier `account--profile`). Keep ALL script computeds + the `user` data + CardInfo (CardInfo may be reused inside the info grid, or replaced with a simple label/value div per mockup line 322 — prefer reusing CardInfo restyled). Structure:
    - Header: eyebrow "Cuenta" + h1 "Mi perfil" + intro; a Ver/Editar segmented control on the right (`account--profile__toggle` with two `<nuxt-link>`: "Ver" → /cuenta/perfil active, "Editar" → /cuenta/perfil/editar).
    - `account--profile__preview` card: cover band (amber gradient or user.cover), circular avatar (user.avatar or initials, $amber bg), name + PRO badge (if pro_status active, $ink badge with `Sparkles`) + Verificado badge ($success tint, `BadgeCheck`), meta (@username, comuna with `MapPin`, "Miembro desde {year}" with `Calendar`), and a tipo chip (Persona Natural / Empresa). If a public bio exists render it.
    - `account--profile__completeness` card: "Tu perfil está N% completo" + percentage + a progress bar ($success fill). Compute N from filled fields (Claude's discretion on the formula; a simple ratio of non-empty key fields).
    - `account--profile__info`: heading "Información personal" + an "Editar" link (→ /cuenta/perfil/editar), then a 2-col grid of label/value rows from the existing user fields (Nombres, Apellidos, RUT, Fecha de nacimiento, Teléfono, WhatsApp if present, Correo, Región, Comuna, Dirección, Código postal). Keep the company info block when `user.is_company`.
    - `account--profile__identity`: heading "Identidad pública" (+ PRO badge), a 3-card grid (Nombre de usuario / Foto de perfil / Portada), each with an icon tile, label, current-value text, and a CTA `<nuxt-link>` to the EXISTING route (/cuenta/username, /cuenta/avatar, /cuenta/cover). Guard PRO-only items with the existing `appConfig.features.pro` / pro_status as today.
    - Optional PRO plan/cancel card (mockup lines 344-364) — render if pro_status active; Claude's discretion on wiring the cancel action (may link to existing PRO management). Do not invent new backend.
    NO inline `style=`.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AccountProfile" || echo "no AccountProfile type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "account--profile__preview" apps/website/app/components/AccountProfile.vue` matches
    - `grep -E "account--profile__completeness|account--profile__identity" apps/website/app/components/AccountProfile.vue` matches both sections
    - `grep -E "/cuenta/username|/cuenta/avatar|/cuenta/cover" apps/website/app/components/AccountProfile.vue` matches (identidad pública CTAs reuse existing routes)
    - `grep -E "/cuenta/perfil/editar" apps/website/app/components/AccountProfile.vue` matches (Editar toggle/link)
    - `grep -c "style=" apps/website/app/components/AccountProfile.vue` returns 0
    - `git diff apps/website/app/pages/cuenta/perfil/index.vue` is EMPTY
    - Final pixel fidelity verified visually against design/account.dc.html lines 260-366.
  </acceptance_criteria>
  <done>Profile Ver mode matches the mockup; all computeds/data + existing routes preserved; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle AccountEdit.vue (Editar mode) + add account--profile/account--edit styles to _account.scss</name>
  <files>apps/website/app/components/AccountEdit.vue, apps/website/app/scss/components/_account.scss</files>
  <read_first>
    - apps/website/app/components/AccountEdit.vue (current — wraps FormProfile; restyle wrapper, keep FormProfile)
    - design/account.dc.html lines 369-417 (Editar card: account-type tabs, fields grid, save/cancel footer)
    - apps/website/app/components/FormProfile.vue (DO NOT change its logic — only scoped styling if needed)
    - apps/website/app/scss/components/_account.scss (add account--profile + account--edit rules; keep @use + other modifiers)
  </read_first>
  <action>
    1. Restyle AccountEdit.vue wrapper to the mockup edit card (modifier `account--edit`). Keep `<FormProfile />` as the single source of the form. Add: eyebrow "Cuenta" + h1 "Mi perfil" + the Ver/Editar toggle (Editar active, Ver → /cuenta/perfil). Wrap FormProfile in `account--edit__card` (1px $line, $white, radius 14px, padding 28px 30px). The account-type Persona Natural / Empresa tabs in the mockup are driven by FormProfile's existing company toggle — if FormProfile already exposes that, ensure it renders inside the card; do NOT duplicate the tab logic in AccountEdit. If FormProfile's save/cancel buttons exist, ensure they match the amber/secondary mockup footer (scoped styling only). NO inline styles.

    2. Add `.account--profile` and `.account--edit` rules to `_account.scss` mirroring the markup. Use ONLY phase-04 tokens. Concrete: profile toggle segmented control ($cream track, active $white pill with subtle shadow — shadow allowed here as it's the mockup segmented control, but per CLAUDE.md avoid box-shadow on elements generally; the segmented active pill uses `0 1px 2px rgba(38,37,43,.08)` per mockup line 890 — keep minimal); preview card (cover gradient band, avatar circle $amber, badges); completeness bar ($cream track, $success fill); info grid 2-col with row dividers $line; identity 3-card grid; edit card 1px $line/$white/radius 14px; field labels $muted; amber save button / outline cancel. Map all tints to $success/$amber/$line/$cream/$ink.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-05-check.css 2>&1 | head -5 && test -f /tmp/05-05-check.css && echo "SCSS compiles" && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "AccountEdit|FormProfile" || echo "no AccountEdit/FormProfile type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "account--profile__preview" apps/website/app/scss/components/_account.scss` matches
    - `grep -E "account--edit__card|account--edit" apps/website/app/scss/components/_account.scss` matches
    - `grep "FormProfile" apps/website/app/components/AccountEdit.vue` still matches (form reused, not rebuilt)
    - `grep -c "style=" apps/website/app/components/AccountEdit.vue` returns 0
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY
    - `git diff apps/website/app/components/FormProfile.vue` shows NO `<script>` logic changes (styling/markup-class only, if touched at all)
    - SCSS compiles; vue-tsc clean.
    - Final pixel fidelity verified visually against design/account.dc.html lines 369-417.
  </acceptance_criteria>
  <done>Profile Editar mode matches the mockup; FormProfile logic untouched; styles compile; vue-tsc clean; tokens unchanged.</done>
</task>

</tasks>

<verification>
- vue-tsc clean for AccountProfile.vue + AccountEdit.vue; app.scss compiles.
- perfil/index.vue + perfil/editar.vue pages untouched; FormProfile logic unchanged.
- Existing username/avatar/cover routes still reachable from identidad pública CTAs (zero regression).
- No inline styles; no token-value changes.
</verification>

<success_criteria>
Mi perfil (Ver + Editar) matches design/account.dc.html lines 260-419 in desktop and responsive, using phase-04 tokens, reusing AccountProfile/AccountEdit/FormProfile — zero behavior regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-05-SUMMARY.md`
</output>
