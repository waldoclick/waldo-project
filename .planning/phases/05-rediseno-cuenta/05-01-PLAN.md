---
phase: 05-rediseno-cuenta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/SidebarAccount.vue
  - apps/website/app/scss/components/_sidebar.scss
  - apps/website/app/scss/components/_layout.scss
  - apps/website/app/scss/app.scss
autonomous: true
requirements: [ACC-LAYOUT]
must_haves:
  truths:
    - "The account area renders a new sidebar layout matching design/account.dc.html (sticky 268px aside + main content)"
    - "Sidebar shows user name, public-profile link, 5 nav items (Panel, Mis anuncios, Mis órdenes, Mi perfil, Cambiar contraseña) with active indicator, and a credits/packs card"
    - "Logout still works exactly as before (Swal confirm → useLogout)"
  artifacts:
    - path: "apps/website/app/components/SidebarAccount.vue"
      provides: "Redesigned account sidebar (5 nav items + credits card)"
      contains: "sidebar--account__nav"
    - path: "apps/website/app/scss/components/_sidebar.scss"
      provides: "sidebar--account BEM styles using new tokens"
      contains: "sidebar--account"
    - path: "apps/website/app/scss/components/_layout.scss"
      provides: "layout--account container/sidebar/content grid styles"
      contains: "layout--account"
  key_links:
    - from: "apps/website/app/layouts/account.vue"
      to: "SidebarAccount.vue"
      via: "component import (unchanged)"
      pattern: "SidebarAccount"
    - from: "SidebarAccount.vue"
      to: "useLogout"
      via: "logout action"
      pattern: "useLogout"
---

<objective>
Redesign the account sidebar (`SidebarAccount.vue`) and its layout container styles to match the `design/account.dc.html` sidebar (lines 41-73): a sticky 268px-wide `aside` with the user's name + location, a "Ver mi perfil público" outline button, a 5-item nav with an amber active indicator bar, and a "Créditos disponibles / Comprar packs" card pinned to the bottom.

Purpose: Establishes the new account shell that every Wave-2+ view plan renders inside. This is a Wave-1 root with no dependency on the other view plans.
Output: Restyled `SidebarAccount.vue` + updated `_sidebar.scss` and `_layout.scss` partials, all using the phase-04 tokens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html

<interfaces>
<!-- Available phase-04 SCSS tokens (apps/website/app/scss/abstracts/_variables.scss) — USE THESE, DO NOT MODIFY their values -->
$ink: #26252b;        // primary text / headings
$ink2: #56535f;       // secondary text
$muted: #8a8794;      // labels, meta
$amber: #f7c97e;      // CTA background, active indicator
$amber_hover: #efb85c;// CTA hover, active bar
$cream: #f6f4f1;      // active nav background, surfaces
$line: #ece9e4;       // borders, dividers
$success: #3b9e63;    // "+3 gratis" accent
$white: #ffffff;

<!-- SCSS architecture in this repo: ONE partial per BEM block in scss/components/, each imported via app.scss @use. -->
<!-- "One SCSS per component" (CLAUDE.md) maps here to one block-partial. Edit _sidebar.scss for .sidebar, _layout.scss for .layout. -->

<!-- Composables consumed by SidebarAccount.vue (already present, keep): -->
const user = useSessionUser<User>();   // { firstname, lastname, username, commune?: { name, region } }
const { logout } = useLogout();
const appConfig = useAppConfiguration();  // appConfig.features.pro
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign SidebarAccount.vue markup to the mockup (5 nav items + credits card)</name>
  <files>apps/website/app/components/SidebarAccount.vue</files>
  <read_first>
    - apps/website/app/components/SidebarAccount.vue (current sidebar — keep useSessionUser, useLogout, getUbication, confirmation logic verbatim)
    - design/account.dc.html lines 41-73 (target sidebar: name, location with map-pin, "Ver mi perfil público" outline button, nav, credits card)
    - design/account.dc.html lines 698-720 (navDef: the canonical 5 nav items + lucide icons + active-style logic)
    - apps/website/app/layouts/account.vue (unchanged — confirms SidebarAccount import path)
  </read_first>
  <action>
    Rewrite the SidebarAccount.vue `<template>` to match the mockup. Keep the `<script setup>` data sources (useSessionUser, useLogout, getUbication computed, confirmation method, useRoute) — only the markup and the nav set change. Concrete structure (BEM block `sidebar`, modifier `sidebar--account`):

    - Root: `<div class="sidebar sidebar--account">`
    - Info group `sidebar--account__info`:
      - `sidebar--account__name` → `{{ user.firstname }} {{ user.lastname }}` (font-weight 700, 19px, color $ink)
      - `sidebar--account__location` → lucide `MapPin` (size 14) + `{{ getUbication }}` (13px, $ink2). Keep the existing `v-show="getUbication"` guard.
      - `sidebar--account__public` → `<nuxt-link :to="`/${user.username}`">` outline button with lucide `ExternalLink` (size 15) + text "Ver mi perfil público" (1px $line border, white bg, $cream hover).
    - Divider: `<div class="sidebar--account__divider"></div>` (1px $line).
    - Nav `sidebar--account__nav` (replace the old `<ul class="sidebar--account__menu">`). EXACTLY 5 items as `<nuxt-link>` with class `sidebar--account__nav__item` (and `sidebar--account__nav__item--active` when route matches), each rendering a lucide icon (size 18) + label:
      1. Panel → `/cuenta/` → icon `LayoutDashboard`
      2. Mis anuncios → `/cuenta/mis-anuncios/` → icon `Package`
      3. Mis órdenes → `/cuenta/mis-ordenes/` → icon `Receipt`
      4. Mi perfil → `/cuenta/perfil` → icon `UserRound`
      5. Cambiar contraseña → `/cuenta/cambiar-contrasena` → icon `Lock`
      Use `<nuxt-link>` active class via `exact-active-class`/`active-class` OR compute active from `route.path`. The active item gets a left amber bar (`sidebar--account__nav__item--active` ::before, 3px, $amber_hover) + $cream background per mockup lines 55-57.
      REMOVE the old username/avatar/cover PRO menu items and the "Cerrar sesión" nav item — these are NOT in the mockup sidebar (username/avatar/cover become modals inside Mi perfil view in plan 05-05; logout lives in the header UserMenu). Keep the `confirmation`/logout function in script ONLY if still referenced; if no longer referenced after removing the logout `<a>`, delete `useLogout`, `confirmation`, and `Swal` to satisfy the no-unused rule.
    - Credits card `sidebar--account__credits` (mockup lines 62-72): label "Créditos disponibles" + lucide `Ticket` (16, $amber_hover); a large number (placeholder `{{ credits }}` — wire to real data later, for now render `0` or a static value, mark with a `<!-- TODO 05-09 wire credits -->`) + "+3 gratis" in $success; a `<nuxt-link to="/packs" class="sidebar--account__credits__buy">` amber button "Comprar packs".

    Replace lucide imports accordingly: `MapPin, ExternalLink, LayoutDashboard, Package, Receipt, UserRound, Lock, Ticket` from `lucide-vue-next`. Remove now-unused icon imports (User, UserCircle, AtSign, Camera, Image, LogOut, ShoppingCart, IconPackage rename as needed). NO inline `style=` attributes anywhere — all styling via BEM classes in _sidebar.scss.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "SidebarAccount" || echo "no SidebarAccount type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sidebar--account__nav__item" apps/website/app/components/SidebarAccount.vue` returns >= 5
    - `grep -E "LayoutDashboard|Receipt|UserRound" apps/website/app/components/SidebarAccount.vue` matches all three new icons
    - `grep -c "style=" apps/website/app/components/SidebarAccount.vue` returns 0 (no inline styles; note: `v-show`/`:to` are not `style=`)
    - `grep -E "/cuenta/avatar|/cuenta/cover|/cuenta/username" apps/website/app/components/SidebarAccount.vue` returns nothing (PRO links removed from sidebar nav)
    - `grep "Comprar packs" apps/website/app/components/SidebarAccount.vue` matches (credits card present)
    - Final pixel fidelity verified visually against design/account.dc.html lines 41-73 by the orchestrator screenshot loop.
  </acceptance_criteria>
  <done>SidebarAccount.vue renders 5-item nav + credits card + public-profile button, no inline styles, vue-tsc clean for this file.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle _sidebar.scss and _layout.scss to the mockup using phase-04 tokens</name>
  <files>apps/website/app/scss/components/_sidebar.scss, apps/website/app/scss/components/_layout.scss, apps/website/app/scss/app.scss</files>
  <read_first>
    - apps/website/app/scss/components/_sidebar.scss (current .sidebar--account styles — rewrite the account modifier; keep @use lines)
    - apps/website/app/scss/components/_layout.scss (current .layout--account container — restyle grid/sidebar/content)
    - apps/website/app/scss/app.scss lines 46-50 (confirms _sidebar/_layout already @use'd — no new @use needed)
    - design/account.dc.html lines 39-73 (container: max-width 1200px, padding 0 32px, flex gap 40px; aside: width 268px, sticky top 70px)
    - apps/website/app/scss/abstracts/_variables.scss (tokens — read only, do not modify)
  </read_first>
  <action>
    Update `_sidebar.scss` `.sidebar--account` and `_layout.scss` `.layout--account` to match the mockup. SCSS nesting must mirror the HTML hierarchy from Task 1 exactly (siblings→siblings, children→nested). Use ONLY the phase-04 tokens ($ink, $ink2, $muted, $amber, $amber_hover, $cream, $line, $success, $white) — do NOT introduce raw hex except where the mockup uses a value with no token (document with a comment).

    `_layout.scss .layout--account`:
    - `&__container`: max-width 1200px, margin 0 auto, padding 0 32px, display flex, gap 40px, align-items flex-start.
    - `&__sidebar`: flex none, width 268px (the sticky behavior lives on the aside inside SidebarAccount or here — keep position:sticky; top: 70px; align-self flex-start; max-height calc(100vh - 70px); overflow-y auto per mockup line 42).
    - `&__content`: flex 1, min-width 0, padding 40px 0 96px (mockup line 76).
    - Add a responsive breakpoint (mobile): stack sidebar above content (flex-direction column) — reuse the existing media-query mixin/pattern already in the file if present.

    `_sidebar.scss .sidebar--account`:
    - `&__info`: flex column, gap 14px.
    - `&__name`: font-weight 700, font-size 19px, color $ink, letter-spacing -.3px.
    - `&__location`: inline-flex, gap 7px, font-size 13px, color $ink2 (icon color $muted).
    - `&__public`: flex center, gap 8px, 1px solid $line, background $white, border-radius 4px, padding 10px, font-size 13.5px, font-weight 600, color $ink; hover background $cream.
    - `&__divider`: height 1px, background $line.
    - `&__nav`: flex column, gap 3px.
    - `&__nav__item`: flex align-center, gap 12px, padding 11px 14px, border-radius 6px, font-size 14.5px, color $ink2, font-weight 500, position relative.
    - `&__nav__item--active`: background $cream, color $ink, font-weight 600; `&::before` { content ""; position absolute; left 0; top 8px; bottom 8px; width 3px; border-radius 0 3px 3px 0; background $amber_hover; }
    - `&__credits`: margin-top auto, 1px solid $line, background $white, border-radius 8px, padding 16px; nested label row ($muted 12.5px + amber ticket icon), big number ($ink 30px 800) + "+3 gratis" ($success 12.5px 600), and `&__buy` amber button (background $amber, hover $amber_hover, color $ink #26252b, padding 11px, border-radius 4px).
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-01-check.css 2>&1 | head -5 && test -f /tmp/05-01-check.css && echo "SCSS compiles"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\\\$amber_hover|\\\$cream|\\\$line|\\\$ink" apps/website/app/scss/components/_sidebar.scss` matches (new tokens used)
    - `grep "sidebar--account__nav__item--active" apps/website/app/scss/components/_sidebar.scss` matches (active state styled)
    - `grep "268px" apps/website/app/scss/components/_layout.scss` matches (sidebar width per mockup)
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY (existing variable values untouched)
    - SCSS compiles without errors (verify command).
    - Final pixel fidelity verified visually against design/account.dc.html lines 39-73.
  </acceptance_criteria>
  <done>_sidebar.scss and _layout.scss compile and visually match the mockup shell; _variables.scss unchanged.</done>
</task>

</tasks>

<verification>
- vue-tsc clean for SidebarAccount.vue; SCSS app.scss compiles.
- No inline styles in SidebarAccount.vue; no modified token values.
- Logout/auth behavior unchanged (verified by orchestrator interaction in the visual loop).
</verification>

<success_criteria>
The account sidebar and layout shell render identically to design/account.dc.html (lines 39-73) in desktop and responsive, using only phase-04 tokens, with the 5-item nav and credits card.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-01-SUMMARY.md`
</output>
