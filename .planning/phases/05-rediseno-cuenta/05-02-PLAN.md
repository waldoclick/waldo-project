---
phase: 05-rediseno-cuenta
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/website/app/components/AccountMain.vue
  - apps/website/app/scss/components/_account.scss
autonomous: true
requirements: [ACC-PANEL]
must_haves:
  truths:
    - "The Panel (/cuenta) renders the greeting header, 3 KPI cards (Vistas totales, Contactos recibidos, Anuncios activos), the 'Necesita tu atención' block, and the packs upsell — matching the mockup"
    - "KPI values and triage list come from existing user data (no behavior regression); real stats wiring of 'Vistas totales' is deferred to 05-09"
  artifacts:
    - path: "apps/website/app/components/AccountMain.vue"
      provides: "Redesigned Panel overview (KPIs + triage + upsell)"
      contains: "account--main__kpi"
    - path: "apps/website/app/scss/components/_account.scss"
      provides: "account--main panel styles using new tokens"
      contains: "account--main__kpi"
  key_links:
    - from: "apps/website/app/pages/cuenta/index.vue"
      to: "AccountMain.vue"
      via: "component import (unchanged)"
      pattern: "AccountMain"
    - from: "AccountMain.vue"
      to: "user ad/pack data"
      via: "existing composables (useUser/usePacks)"
      pattern: "usePacks"
---

<objective>
Redesign `AccountMain.vue` (the Panel at `/cuenta`) to match `design/account.dc.html` lines 78-139: a "Panel" eyebrow + "Hola, {firstname} 👋" heading + intro paragraph; a 3-column KPI grid (Vistas totales, Contactos recibidos, Anuncios activos); a "Necesita tu atención" section listing ads that are por-vencer/rechazados with an action; and the dark packs-upsell card.

Purpose: First view inside the new shell (depends on 05-01). Sets the `account--main` block styles that later view plans build alongside.
Output: Restyled AccountMain.vue + new `account--main` styles in `_account.scss`. The "Vistas totales" / "Contactos recibidos" KPI values use placeholders/best-available data now; 05-09 wires them to the real `/ads/me/views-total` aggregation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html
@.planning/phases/05-rediseno-cuenta/05-01-PLAN.md

<interfaces>
<!-- Existing AccountMain.vue composables (keep working): -->
const user = useSessionUser<User>();                 // user.firstname for greeting
const { getAdReservationsText, getFeaturedAdReservationsText } = useUser();
const { getPackBannerText } = usePacks();
const { packs, loadPacks } = usePacksList();          // onMounted(loadPacks)
const appConfig = useAppConfiguration();              // appConfig.features.pro

<!-- For KPI "Anuncios activos" and triage (por-vencer/rechazado), the data source is the user store ad counts -->
<!-- (apps/website/app/stores/user.store.ts): -->
const userStore = useUserStore();
userStore.loadUserAdCounts(): Promise<{ published:number; review:number; expired:number; rejected:number; banned:number }>;
userStore.loadUserAds(filter, { page, pageSize }, sort): Promise<{ data, meta }>;  // use filter "rejected" / "published" for triage

<!-- KPI mockup mapping (design/account.dc.html lines 771-775): -->
<!--   Vistas totales   → placeholder "0" now, icon "eye"    (05-09 wires /ads/me/views-total) -->
<!--   Contactos recibidos → placeholder "0" now, icon "phone" (05-09 wires) -->
<!--   Anuncios activos → counts.published, icon "package", sub "de N publicados" -->

<!-- New phase-04 tokens available: $ink $ink2 $muted $amber $amber_hover $cream $line $success $warning $error $white -->
<!-- SCSS lives in scss/components/_account.scss (.account block, modifier account--main). app.scss already @use's it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild AccountMain.vue Panel markup (header + 3 KPIs + triage + upsell)</name>
  <files>apps/website/app/components/AccountMain.vue</files>
  <read_first>
    - apps/website/app/components/AccountMain.vue (current — keep useUser/usePacks/usePacksList/appConfig logic; replace markup)
    - design/account.dc.html lines 78-139 (Panel: eyebrow, greeting, intro, KPI grid, "Necesita tu atención", upsell)
    - design/account.dc.html lines 766-788 (data derivation: activeAll, kpis, triage from porvencer/rechazado, MemoPro vs upsell)
    - apps/website/app/stores/user.store.ts (loadUserAdCounts, loadUserAds signatures)
    - apps/website/app/components/AccountMain.vue current MemoPro usage (the dark card maps to the upsell)
  </read_first>
  <action>
    Replace the AccountMain.vue template with the mockup Panel (BEM block `account`, modifier `account--main`). Concrete structure:

    - `account--main__eyebrow` → "Panel" (13px 600 $muted).
    - `account--main__greeting` (h1) → `Hola, {{ user?.firstname }} 👋` (30px 800 $ink).
    - `account--main__intro` (p) → "Este es el resumen de tu cuenta. Revisa lo que necesita tu atención y cómo rinden tus anuncios." (15px $ink2, max-width 560px).
    - `account--main__kpis` grid (3 cols, gap 18px). Each card `account--main__kpi`:
        - `account--main__kpi__label` ($muted 12.5px) + lucide icon (size 18, $muted) in a header row.
        - `account--main__kpi__value` ($ink 30px 800).
        - `account--main__kpi__sub` ($muted 12.5px).
      Three cards: (1) "Vistas totales" / value `{{ totalViews }}` (start `0`, `<!-- TODO 05-09 wire /ads/me/views-total -->`) / sub "en anuncios activos" / icon `Eye`; (2) "Contactos recibidos" / `{{ totalContacts }}` (start `0`) / sub "llamadas y mensajes" / icon `Phone`; (3) "Anuncios activos" / `{{ activeCount }}` / sub `de {{ totalCount }} publicados` / icon `Package`. Pull activeCount/totalCount from `loadUserAdCounts` (published + sum of all).
    - `account--main__attention` section (mockup lines 99-127): heading "Necesita tu atención" + a count badge; a list of `account--main__attention__row` items derived from por-vencer + rechazado ads. For each: a category icon tile, title, a status badge (Vence en N días / Rechazado), a note, and an action button ("Ver estadísticas" → opens stats modal later / "Ver motivo"). For THIS plan, render the rows from `loadUserAds("rejected", ...)` (and any por-vencer source available); the "Ver estadísticas" action can be a no-op placeholder `<!-- TODO 05-09 open stats modal -->`. If no items → render the "Todo al día" success card (mockup lines 122-127, lucide `CheckCheck`, $success tile).
    - `account--main__upsell` dark card (mockup lines 130-138): background $ink, "Ahorra hasta 98%" pill, h3 "Publica más, paga menos", paragraph, and a `<nuxt-link to="/packs">` amber "Comprar packs" button with `ArrowRight` icon. (If `appConfig.features.pro` keeps MemoPro relevant, you may render MemoPro above the upsell as today — Claude's discretion; the upsell card itself must match the mockup.)

    Script: keep existing composables; add `useUserStore()` at setup scope and load counts via `useAsyncData('account-panel-counts', ...)` OR the existing `onMounted` pattern already in the file (do NOT pair a bare await with useAsyncData — follow CLAUDE.md data-fetch rule). Compute `activeCount`, `totalCount`, and `triage` rows. Add lucide imports: `Eye, Phone, Package, CheckCheck, ArrowRight, ChartNoAxesColumn, CircleAlert` (plus per-category icons as needed). NO inline `style=`.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AccountMain" || echo "no AccountMain type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "account--main__kpi" apps/website/app/components/AccountMain.vue` returns >= 3 occurrences (3 KPI cards)
    - `grep -E "Vistas totales|Contactos recibidos|Anuncios activos" apps/website/app/components/AccountMain.vue` matches all three KPI labels
    - `grep "Necesita tu atención" apps/website/app/components/AccountMain.vue` matches (triage block)
    - `grep "account--main__upsell" apps/website/app/components/AccountMain.vue` matches (packs upsell)
    - `grep -c "style=" apps/website/app/components/AccountMain.vue` returns 0
    - vue-tsc clean for AccountMain.vue.
    - Final pixel fidelity verified visually against design/account.dc.html lines 78-139.
  </acceptance_criteria>
  <done>Panel renders greeting + 3 KPIs + triage/Todo-al-día + upsell, no inline styles, vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add account--main panel styles to _account.scss using phase-04 tokens</name>
  <files>apps/website/app/scss/components/_account.scss</files>
  <read_first>
    - apps/website/app/scss/components/_account.scss (existing .account--main — replace its rules to match the new markup; keep @use lines and other modifiers untouched)
    - design/account.dc.html lines 85-138 (KPI card 1px $line/12px radius/20px 22px; attention rows gradient/border; dark upsell card)
    - apps/website/app/scss/abstracts/_variables.scss (tokens — read only)
  </read_first>
  <action>
    Replace `.account--main` rules in `_account.scss` so they mirror the Task-1 markup hierarchy exactly (siblings→siblings, children→nested). Use ONLY phase-04 tokens. Concrete:
    - `&__eyebrow` 13px 600 $muted; `&__greeting` 30px 800 $ink letter-spacing -.5px; `&__intro` 15px $ink2 line-height 1.6 max-width 560px.
    - `&__kpis` display grid, grid-template-columns repeat(3,1fr), gap 18px, margin-top 32px (mobile: 1 col).
    - `&__kpi` 1px solid $line, background $white, border-radius 12px, padding 20px 22px; `__label` $muted 12.5px; `__value` $ink 30px 800 margin-top 16px; `__sub` $muted 12.5px margin-top 8px.
    - `&__attention` margin-top 48px; heading row ($ink 19px 700 + dark count badge background $ink color $white); `__row` flex gap 16px, border-radius 12px, padding 16px 18px (rejected variant: border #F1D3D0 background #FCF4F3 — these two have no token, add `// no token in palette — mockup literal` comment; por-vencer variant amber-tinted per mockup line 776). Status badges reuse tokens: activo→$success tint, vence→$warning tint, rechazado→$error tint.
    - `&__upsell` background $ink, border-radius 12px, padding 26px 28px, flex; pill (amber-tinted), h3 #fff 22px 800, paragraph rgba white, amber button (background $amber hover $amber_hover color #26252b).
    Keep existing `.account--main__shortcuts` / `__announcements` rules removable ONLY if the new markup no longer uses them (subtractive). Do not touch other `.account--*` modifier blocks.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-02-check.css 2>&1 | head -5 && test -f /tmp/05-02-check.css && echo "SCSS compiles"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "account--main__kpi" apps/website/app/scss/components/_account.scss` matches
    - `grep "account--main__upsell" apps/website/app/scss/components/_account.scss` matches
    - `grep -E "\\\$amber|\\\$cream|\\\$line|\\\$success|\\\$warning|\\\$error" apps/website/app/scss/components/_account.scss` matches (tokens used)
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY
    - SCSS compiles (verify command).
    - Final pixel fidelity verified visually against design/account.dc.html lines 85-138.
  </acceptance_criteria>
  <done>Panel styles compile and visually match the mockup; tokens unchanged.</done>
</task>

</tasks>

<verification>
- vue-tsc clean for AccountMain.vue; app.scss compiles.
- No inline styles; no token-value changes.
- KPI/triage data sourced from existing user-store methods (no new endpoint invented here).
</verification>

<success_criteria>
The Panel matches design/account.dc.html lines 78-139 in desktop and responsive, using phase-04 tokens, with 3 KPIs + triage + upsell. "Vistas totales"/"Contactos recibidos" placeholders are wired to real data in 05-09.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-02-SUMMARY.md`
</output>
