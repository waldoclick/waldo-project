---
phase: 05-rediseno-cuenta
plan: 06
type: execute
wave: 6
depends_on: ["05-05"]
files_modified:
  - apps/website/app/components/AccountPassword.vue
  - apps/website/app/scss/components/_account.scss
autonomous: true
requirements: [ACC-PASSWORD]
must_haves:
  truths:
    - "Cambiar contraseña (/cuenta/cambiar-contrasena) renders the header, the password card (current + new with generate + strength + repeat) and the Google-provider info note, matching the mockup"
    - "Password change behavior (FormPassword logic, strength meter, generate, validation, external-provider guard) is unchanged (no regression)"
  artifacts:
    - path: "apps/website/app/components/AccountPassword.vue"
      provides: "Redesigned Cambiar contraseña view"
      contains: "account--password__card"
    - path: "apps/website/app/scss/components/_account.scss"
      provides: "account--password styles using new tokens"
      contains: "account--password__card"
  key_links:
    - from: "apps/website/app/pages/cuenta/cambiar-contrasena.vue"
      to: "AccountPassword.vue"
      via: "component import (unchanged)"
      pattern: "AccountPassword"
    - from: "AccountPassword.vue"
      to: "FormPassword.vue"
      via: "form reused (not rebuilt)"
      pattern: "FormPassword"
---

<objective>
Restyle the Cambiar contraseña view (`AccountPassword.vue`, which wraps `FormPassword`) to match `design/account.dc.html` lines 421-465: header (eyebrow "Seguridad", h1, intro); a 560px password card containing the current/new/repeat fields with the "Generar segura" link, show/hide toggles, strength meter and match note; and the cream info note about Google-provider accounts.

Purpose: Final view. Shares `_account.scss` → runs after 05-05 (wave 6). Reuses AccountPassword + FormPassword; restyle only, zero behavior regression. The form primitives were already restyled under `.auth` in phase 04 — reuse those token-based styles where the form lives here.
Output: Restyled AccountPassword.vue + new `account--password` styles. FormPassword logic unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html
@.planning/phases/05-rediseno-cuenta/05-05-PLAN.md

<interfaces>
<!-- AccountPassword.vue (current): -->
const user = useSessionUser();
const isExternalProvider = computed(() => user.value?.provider !== "local");
// if external → <MemoDefault> ; else → <FormPassword />
// FormPassword ALREADY implements: current/new/repeat inputs, show/hide, "Generar segura", strength meter, match note, submit.
// DO NOT rebuild FormPassword — restyle the wrapper + (scoped, if needed) its look to the mockup.

<!-- Mockup card (lines 428-459): 560px max-width card; field labels $muted; inputs with amber focus ring; -->
<!-- "Generar segura" link (sparkle icon); strength meter under the new-password field; "Actualizar contraseña" amber submit. -->
<!-- Info note (lines 461-464): cream box, $tag shield icon, Google-provider text → maps to the existing isExternalProvider MemoDefault. -->

<!-- New phase-04 tokens: $ink $ink2 $muted $amber $amber_hover $cream $line $error $success $white. ($tag #A9772E literal w/ comment.) -->
<!-- SCSS: scss/components/_account.scss, modifier account--password. Phase-04 already styled form primitives under .auth — -->
<!-- reuse that approach (token-based) so the password fields match without duplicating raw values. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AccountPassword.vue to the mockup (header + card + info note)</name>
  <files>apps/website/app/components/AccountPassword.vue</files>
  <read_first>
    - apps/website/app/components/AccountPassword.vue (current — keep isExternalProvider guard, FormPassword, MemoDefault)
    - design/account.dc.html lines 421-465 (header, password card, info note)
    - apps/website/app/components/FormPassword.vue (DO NOT change logic — only scoped styling if needed)
    - apps/website/app/pages/cuenta/cambiar-contrasena.vue (DO NOT MODIFY)
  </read_first>
  <action>
    Restyle AccountPassword.vue to the mockup (block `account`, modifier `account--password`). Keep ALL script (user, isExternalProvider) and the two branches (MemoDefault when external provider, FormPassword otherwise). Structure:
    - Header: `account--password__eyebrow` "Seguridad" + h1 "Cambiar contraseña" (30px 800 $ink) + intro "Mantén tu cuenta segura. Usa una contraseña única que no utilices en otros servicios." (replace the current "Actualiza tu contraseña" header copy with the mockup copy).
    - `account--password__card` (max-width 560px, 1px $line, $white, radius 14px, padding 28px 30px) wrapping `<FormPassword />` (the form provides the fields + generate + strength + repeat + submit).
    - When `isExternalProvider`, keep rendering the MemoDefault guard, but also render (or style) the info note below the card: `account--password__note` cream box (1px $line, $cream bg, lucide `Shield` $tag) with the Google-provider text (mockup line 463). The note can render in BOTH branches (it's informational) — Claude's discretion; minimum: it appears as in the mockup.
    Update lucide imports (e.g. `Shield`). NO inline `style=`.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AccountPassword" || echo "no AccountPassword type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "account--password__card" apps/website/app/components/AccountPassword.vue` matches
    - `grep "FormPassword" apps/website/app/components/AccountPassword.vue` still matches (form reused)
    - `grep "Cambiar contraseña" apps/website/app/components/AccountPassword.vue` matches (mockup h1 copy)
    - `grep -c "style=" apps/website/app/components/AccountPassword.vue` returns 0
    - `git diff apps/website/app/pages/cuenta/cambiar-contrasena.vue` is EMPTY
    - Final pixel fidelity verified visually against design/account.dc.html lines 421-465.
  </acceptance_criteria>
  <done>Cambiar contraseña view matches the mockup; FormPassword logic untouched; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add account--password styles to _account.scss using phase-04 tokens</name>
  <files>apps/website/app/scss/components/_account.scss</files>
  <read_first>
    - apps/website/app/scss/components/_account.scss (add account--password rules; keep @use + other modifiers)
    - design/account.dc.html lines 428-464 (card + field + strength + info-note styling)
    - apps/website/app/scss/abstracts/_variables.scss (tokens — read only)
  </read_first>
  <action>
    Add `.account--password` rules to `_account.scss` mirroring the Task-1 markup. Use ONLY phase-04 tokens ($tag #A9772E literal with `// no token — mockup literal`). Concrete: `__eyebrow` $muted 13px 600; h1 $ink 30px 800; intro $ink2 15px; `__card` max-width 560px, 1px $line, $white, radius 14px, padding 28px 30px; field labels $muted 12.5px 600; inputs 1px $line radius 7px, focus border $amber_hover + ring rgba(247,201,126,.25); "Generar segura" link $ink underline; strength meter bars; `__note` cream box (1px $line, $cream, $tag icon, $ink2 text). If FormPassword already carries token-based field styles from phase 04, scope only what's missing — do not duplicate. Keep other `.account--*` modifiers untouched.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-06-check.css 2>&1 | head -5 && test -f /tmp/05-06-check.css && echo "SCSS compiles"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "account--password__card" apps/website/app/scss/components/_account.scss` matches
    - `grep -E "\\\$amber_hover|\\\$cream|\\\$line|\\\$muted" apps/website/app/scss/components/_account.scss` matches (tokens used)
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY
    - SCSS compiles (verify command).
    - Final pixel fidelity verified visually against design/account.dc.html lines 428-464.
  </acceptance_criteria>
  <done>Password styles compile and match the mockup; tokens unchanged.</done>
</task>

</tasks>

<verification>
- vue-tsc clean for AccountPassword.vue; app.scss compiles.
- cambiar-contrasena.vue page untouched; FormPassword logic unchanged.
- No inline styles; no token-value changes.
</verification>

<success_criteria>
Cambiar contraseña matches design/account.dc.html lines 421-465 in desktop and responsive, using phase-04 tokens, reusing FormPassword — zero behavior regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-06-SUMMARY.md`
</output>
