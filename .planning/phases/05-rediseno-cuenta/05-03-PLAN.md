---
phase: 05-rediseno-cuenta
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apps/website/app/components/AccountAnnouncements.vue
  - apps/website/app/components/CardProfileAd.vue
  - apps/website/app/scss/components/_account.scss
autonomous: true
requirements: [ACC-ADS]
must_haves:
  truths:
    - "Mis anuncios (/cuenta/mis-anuncios) renders the header + 5 status tabs with counts + the ad cards list + pagination, matching the mockup"
    - "Tab filtering, counts, pagination, and the empty state behave exactly as before (no regression)"
    - "Each ad card shows the category-color thumbnail, title, status badge, meta (vistas·contactos / expiró / motivo) and primary action + overflow menu"
  artifacts:
    - path: "apps/website/app/components/AccountAnnouncements.vue"
      provides: "Redesigned Mis anuncios view (tabs + list)"
      contains: "account--announcements__tab"
    - path: "apps/website/app/scss/components/_account.scss"
      provides: "account--announcements styles using new tokens"
      contains: "account--announcements__tab"
  key_links:
    - from: "apps/website/app/pages/cuenta/mis-anuncios.vue"
      to: "AccountAnnouncements.vue"
      via: "props (ads, tabs, currentFilter, pagination) — UNCHANGED contract"
      pattern: "AccountAnnouncements"
    - from: "AccountAnnouncements.vue"
      to: "CardProfileAd.vue"
      via: "v-for over ads"
      pattern: "CardProfileAd"
---

<objective>
Restyle the Mis anuncios view (`AccountAnnouncements.vue` + the per-ad `CardProfileAd.vue`) to match `design/account.dc.html` lines 142-214: header with "Publicar anuncio" amber CTA; a row of 5 tabs (Activos, Pendientes, Expirados, Rechazados, Baneados) each with a count pill and lucide icon, underline-active style; the ad list as cards with a category-colored thumbnail, title + status badge + featured pill, meta line, primary action button and an overflow "ellipsis-vertical" menu; the empty state; and pagination.

Purpose: View 2 inside the new shell. Shares `_account.scss` with 05-02 → runs after it (wave 3). Keeps the page↔component props contract and all tab/pagination behavior unchanged.
Output: Restyled AccountAnnouncements.vue + CardProfileAd.vue + new `account--announcements` styles. NO change to mis-anuncios.vue page logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-rediseno-cuenta/05-CONTEXT.md
@design/account.dc.html
@.planning/phases/05-rediseno-cuenta/05-02-PLAN.md

<interfaces>
<!-- mis-anuncios.vue → AccountAnnouncements.vue props (DO NOT CHANGE — page logic stays): -->
defineProps<{
  ads: Announcement[];                 // ad rows from userStore.loadUserAds
  currentFilter: string;               // "published"|"review"|"expired"|"rejected"|"banned"
  currentPage: number;
  pagination: { total: number; pageSize: number };
  isLoading: boolean;
  introText: string;
  tabs: { value: FilterType; label: string; count: number; icon: Component }[];
}>();
defineEmits<{ "filter-change":[filter]; "page-change":[page] }>();

<!-- Tab labels/icons are passed FROM the page (CheckCircle, Clock, AlertCircle, XCircle, Ban). -->
<!-- Mockup uses circle-check/clock/circle-off/circle-x/ban — keep the page's lucide icons; restyle, don't swap the contract. -->

<!-- CardProfileAd.vue renders one ad. The mockup card (lines 171-199): thumbnail tile (category color bg + lucide cat icon + photo count), -->
<!-- title + status badge + featured star pill, meta (cat dot + date + "N vistas · N contactos"), primary button + overflow menu. -->
<!-- Status badge tokens: activo→$success tint, vence→$warning tint, expirado→$cream/$muted, rechazado→$error tint, vendido→$success_strong tint. -->

<!-- New phase-04 tokens: $ink $ink2 $muted $amber $amber_hover $cream $line $success $success_strong $warning $error $white -->
<!-- SCSS: scss/components/_account.scss, modifier account--announcements. app.scss already @use's it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle AccountAnnouncements.vue (header + tabs + list shell) keeping the props contract</name>
  <files>apps/website/app/components/AccountAnnouncements.vue</files>
  <read_first>
    - apps/website/app/components/AccountAnnouncements.vue (current — keep props, emits, tab keyboard nav, ClientOnly, v-for over ads, pagination)
    - design/account.dc.html lines 142-214 (header CTA, tab bar with count pills + underline-active, list, empty state, pager)
    - design/account.dc.html lines 791-808 (tab styling: TABBASE underline-active amberH, count pill active=amber/inactive=cream)
    - apps/website/app/pages/cuenta/mis-anuncios.vue (DO NOT MODIFY — confirms the props passed in)
  </read_first>
  <action>
    Restyle the AccountAnnouncements.vue template to the mockup (block `account`, modifier `account--announcements`). Keep ALL script logic (props, emits, focusNextTab/focusPrevTab, ClientOnly, the `v-for="ad in ads"` rendering CardProfileAd, the pagination block, the empty state). Only markup/classes change:

    - Header: `account--announcements__header` with `__heading` ("Mis anuncios" h1 30px 800 $ink) + `__intro` ({{ introText }}) on the left, and a `<ButtonCreate>` / amber "Publicar anuncio" CTA on the right (mockup line 151 — keep the existing ButtonCreate component but ensure it renders the amber style, or wrap with the right class).
    - Tabs: replace the current `.account--announcements__list__menu` buttons with `account--announcements__tabs` containing `account--announcements__tab` buttons (one per `tabs` item). Each: lucide icon (`<component :is="tab.icon" :size="15" />`) + label + a `account--announcements__tab__count` pill showing `tab.count`. Active = `account--announcements__tab--active` (underline amber_hover, count pill amber). Keep `@click="$emit('filter-change', tab.value)"` and the keyboard nav handlers + roles for a11y.
    - List: `account--announcements__list` wrapping the `v-for` of `CardProfileAd`. Keep loading (`LoadingDefault`) and the empty state — restyle the empty state to the dashed-border card (mockup lines 160-166: 1.5px dashed $line, centered icon tile + title + message). Map the empty title/message per the current `tab.value` (reuse mockup emptyMap text lines 830-836 as the copy).
    - Pagination: keep `vue-awesome-paginate` but wrap in `account--announcements__pager` styled to the mockup pager (lines 202-211). (If restyling vue-awesome-paginate to the exact pill look is hard, keep functionality and style the container — Claude's discretion; functionality must not regress.)
    NO inline `style=`.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "AccountAnnouncements" || echo "no AccountAnnouncements type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "account--announcements__tab" apps/website/app/components/AccountAnnouncements.vue` returns >= 1 (tabs restyled)
    - `grep "account--announcements__tab__count" apps/website/app/components/AccountAnnouncements.vue` matches (count pill)
    - `grep "CardProfileAd" apps/website/app/components/AccountAnnouncements.vue` still matches (card reused, not replaced)
    - `grep -E "defineProps|filter-change|page-change" apps/website/app/components/AccountAnnouncements.vue` matches (props/emits contract intact)
    - `grep -c "style=" apps/website/app/components/AccountAnnouncements.vue` returns 0
    - `git diff apps/website/app/pages/cuenta/mis-anuncios.vue` is EMPTY (page logic untouched)
    - Final pixel fidelity verified visually against design/account.dc.html lines 142-214.
  </acceptance_criteria>
  <done>Mis anuncios header/tabs/list/empty/pager match the mockup; props contract and tab/pagination behavior unchanged; vue-tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle CardProfileAd.vue (ad card) + add account--announcements styles to _account.scss</name>
  <files>apps/website/app/components/CardProfileAd.vue, apps/website/app/scss/components/_account.scss</files>
  <read_first>
    - apps/website/app/components/CardProfileAd.vue (current ad card — keep its data/props + actions; restyle to the mockup card)
    - design/account.dc.html lines 171-199 (card: thumbnail tile, title+badge+featured, meta line, primary button + overflow menu)
    - design/account.dc.html lines 724-763 (badge/meta/action derivation per status — use as the status→badge/meta mapping reference)
    - apps/website/app/scss/components/_account.scss (add account--announcements rules; keep @use + other modifiers)
  </read_first>
  <action>
    1. Restyle `CardProfileAd.vue` to the mockup card hierarchy (use a BEM block consistent with the component, e.g. `card--profile-ad` OR scope under `account--announcements__item` — match the closest existing convention in _account.scss; prefer the component's own block `card--profile-ad`). Keep the existing props (the ad object) and any action handlers (renovar/destacar/estadísticas/ver/etc.). Card contents: thumbnail tile (`background` = category color from ad data, lucide category icon, photo-count chip), title + status badge (token-tinted) + optional featured star pill ($amber), meta row (category dot + date + "N vistas · N contactos" for active / "Expiró hace N días" / "Motivo: …"), primary action button (Destacar amber / Estadísticas secondary / Republicar / Ver motivo) and, when applicable, an `ellipsis-vertical` overflow menu (Estadísticas / Ver anuncio / Marcar como vendido / Dar de baja). Preserve current behavior; only restyle. NO inline styles.

    2. Add `.account--announcements` rules to `_account.scss` mirroring the Task-1 markup, plus the card block styles. Use ONLY phase-04 tokens (category thumbnail colors come from data/inline-bound CSS var or a `:style` binding for the dynamic category color ONLY — a dynamic background from data is allowed as a bound style since it is data, not static design; document it). Concrete: `__header` flex space-between; `__tabs` flex border-bottom 1px $line; `__tab` underline-transparent, `--active` border-bottom 2px $amber_hover + $ink 700; `__tab__count` pill (active amber/#26252b, inactive $cream/$muted); `__list` flex column gap 12px; empty state dashed $line; pager pill buttons (active border $amber_hover bg #FEF8EC). Badge tints map to $success/$warning/$error/$muted/$success_strong.
  </action>
  <verify>
    <automated>cd apps/website && node ../../node_modules/.bin/sass --no-source-map app/scss/app.scss /tmp/05-03-check.css 2>&1 | head -5 && test -f /tmp/05-03-check.css && echo "SCSS compiles" && node ../../node_modules/.bin/vue-tsc --noEmit -p tsconfig.json 2>&1 | grep -i "CardProfileAd" || echo "no CardProfileAd type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "account--announcements__tab--active" apps/website/app/scss/components/_account.scss` matches
    - `grep -E "\\\$amber_hover|\\\$success|\\\$warning|\\\$error" apps/website/app/scss/components/_account.scss` matches
    - `grep -c "style=" apps/website/app/components/CardProfileAd.vue` returns 0 OR only data-bound `:style` for the dynamic category color (no static design inline styles) — confirm any `:style` binds a data value, not a hardcoded design value
    - `git diff apps/website/app/scss/abstracts/_variables.scss` is EMPTY
    - SCSS compiles; vue-tsc clean for CardProfileAd.vue.
    - Final pixel fidelity verified visually against design/account.dc.html lines 171-199.
  </acceptance_criteria>
  <done>Ad card + announcements styles match the mockup, compile, vue-tsc clean; tokens unchanged.</done>
</task>

</tasks>

<verification>
- vue-tsc clean for AccountAnnouncements.vue + CardProfileAd.vue; app.scss compiles.
- mis-anuncios.vue page untouched; props/emits contract intact; tab + pagination behavior unchanged.
- No inline design styles; no token-value changes.
</verification>

<success_criteria>
Mis anuncios matches design/account.dc.html lines 142-214 in desktop and responsive, using phase-04 tokens, with tabs/counts/list/empty/pager — zero behavior regression.
</success_criteria>

<output>
After completion, create `.planning/phases/05-rediseno-cuenta/05-03-SUMMARY.md`
</output>
