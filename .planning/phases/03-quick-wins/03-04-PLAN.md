---
phase: 03-quick-wins
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/stores/app.store.ts
  - apps/dashboard/app/types/app.d.ts
  - apps/dashboard/nuxt.config.ts
autonomous: true
requirements:
  - QUICK-05
  - QUICK-07

must_haves:
  truths:
    - "AppStore contains only state relevant to the dashboard: referer and isMobileMenuOpen (and their actions)"
    - "nuxt.config.ts has no commented-out module blocks (GTM, i18n, image provider, manifest link)"
    - "The AppState type interface matches the pruned store — no dead fields"
  artifacts:
    - path: "apps/dashboard/app/stores/app.store.ts"
      provides: "Pruned AppStore with only referer and isMobileMenuOpen state"
    - path: "apps/dashboard/app/types/app.d.ts"
      provides: "Updated AppState interface without removed fields"
    - path: "apps/dashboard/nuxt.config.ts"
      provides: "Clean config with no commented-out blocks"
  key_links:
    - from: "apps/dashboard/app/stores/app.store.ts"
      to: "apps/dashboard/app/types/app.d.ts"
      via: "AppState interface drives store state type"
      pattern: "AppState"
---

<objective>
Remove website-specific state from AppStore and delete commented-out dead code from nuxt.config.ts.

Purpose: AppStore carries three fields that belong to the website frontend, not the dashboard: isSearchLightboxActive, isLoginLightboxActive, and contactFormSent. They are never used in any dashboard component. nuxt.config.ts has multiple commented-out blocks (GTM module, i18n config, image provider, manifest link) that add noise and confusion.

Output: A lean AppStore with only dashboard-relevant state; a clean nuxt.config.ts.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current AppState (app.d.ts) -->
export interface AppState {
  isSearchLightboxActive: boolean;   // website-only, remove
  isLoginLightboxActive: boolean;    // website-only, remove
  referer: string | null;            // keep
  contactFormSent: boolean;          // website-only, remove
  isMobileMenuOpen: boolean;         // keep
}

<!-- Current app.store.ts has these getters to remove: -->
// getIsSearchLightboxActive, getIsLoginLightboxActive, getContactFormSent

<!-- Current app.store.ts has these actions to remove: -->
// openSearchLightbox, closeSearchLightbox, toggleSearchLightbox
// openLoginLightbox, closeLoginLightbox, toggleLoginLightbox
// setContactFormSent, clearContactFormSent

<!-- Confirmed: no dashboard component references these fields/actions -->
<!-- grep result: zero usages in app/ outside the store and types files -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prune AppStore and AppState type</name>
  <files>
    apps/dashboard/app/stores/app.store.ts,
    apps/dashboard/app/types/app.d.ts
  </files>
  <action>
    **Before touching any file**, confirm no usages exist:
    ```
    grep -rn "isSearchLightboxActive\|isLoginLightboxActive\|contactFormSent\|openSearchLightbox\|closeSearchLightbox\|toggleSearchLightbox\|openLoginLightbox\|closeLoginLightbox\|toggleLoginLightbox\|setContactFormSent\|clearContactFormSent\|getIsSearchLightboxActive\|getIsLoginLightboxActive\|getContactFormSent" apps/dashboard/app/ --include="*.vue" --include="*.ts" | grep -v "app.store.ts\|app.d.ts"
    ```
    If any results appear, stop and document in the summary — do not remove fields that are in use.

    If confirmed unused, apply the following changes:

    **app.d.ts — remove three fields:**
    ```typescript
    export interface AppState {
      referer: string | null;
      isMobileMenuOpen: boolean;
    }
    ```

    **app.store.ts — remove state, getters, and actions for the three deleted fields:**

    Remove from `state()`:
    - `isSearchLightboxActive: false`
    - `isLoginLightboxActive: false`
    - `contactFormSent: false`

    Remove from `getters`:
    - `getIsSearchLightboxActive`
    - `getIsLoginLightboxActive`
    - `getContactFormSent`

    Remove from `actions`:
    - `openSearchLightbox`, `closeSearchLightbox`, `toggleSearchLightbox`
    - `openLoginLightbox`, `closeLoginLightbox`, `toggleLoginLightbox`
    - `setContactFormSent`, `clearContactFormSent`

    Keep everything related to `referer` (getReferer, setReferer, clearReferer) and `isMobileMenuOpen` (openMobileMenu, closeMobileMenu, toggleMobileMenu) unchanged.

    Keep `persist` configuration as-is.
  </action>
  <verify>
    <automated>grep -n "isSearchLightboxActive\|isLoginLightboxActive\|contactFormSent" apps/dashboard/app/stores/app.store.ts apps/dashboard/app/types/app.d.ts</automated>
  </verify>
  <done>
    - app.store.ts contains no reference to isSearchLightboxActive, isLoginLightboxActive, or contactFormSent
    - app.d.ts AppState interface has exactly two fields: referer and isMobileMenuOpen
    - referer and isMobileMenuOpen state, getters, and actions are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out dead code from nuxt.config.ts</name>
  <files>apps/dashboard/nuxt.config.ts</files>
  <action>
    Remove the following commented-out blocks from nuxt.config.ts. Remove only the comment blocks — do not touch any active configuration.

    **Block 1 — GTM module config (lines ~275-280):**
    ```
    // Google Tag Manager Configuration - Manual implementation for Nuxt 4
    // gtm: {
    //   id: process.env.GTM_ID || "GTM-N4B8LDKS",
    //   enabled: true,
    //   debug: false,
    // },
    ```
    Remove this entire comment block including the surrounding comment line.

    **Block 2 — i18n module in the modules array (line ~29):**
    ```
    // "@nuxtjs/i18n",
    ```
    Remove this commented-out entry from the modules array.

    **Block 3 — i18n config block (lines ~116-128):**
    ```
    // i18n: {
    //   defaultLocale: 'es',
    //   locales: ['es'],
    //   strategy: 'prefix_except_default',
    //   detectBrowserLanguage: {
    //     useCookie: true,
    //     cookieKey: 'i18n_redirected',
    //     redirectOn: 'root',
    //   },
    //   bundle: {
    //     optimizeTranslationDirective: false
    //   }
    // },
    ```
    Remove this entire commented-out config block.

    **Block 4 — image provider config (lines ~401-408):**
    ```
    // onError: false, // Deshabilitar onerror inline
    // quality: 80,
    // format: ["webp"],
    // provider: "ipx",
    // ipx: {
    //   maxAge: 60 * 60 * 24 * 7, // 7 días
    //   dir: "public",
    // },
    ```
    Remove these commented-out lines from inside the `image: {}` config block. Keep the active `image: { }` block itself (it can remain as an empty object or with the comment above it if meaningful).

    **Block 5 — manifest link (line ~223):**
    ```
    // { rel: "manifest", href: "/favicons/manifest.json" },
    ```
    Remove this commented-out link entry.

    **Block 6 — nuxt.config.ts top import comment:**
    ```
    // import { feedbackIntegration } from "@sentry/integrations";
    ```
    Remove this commented-out import line at the top of the file.

    Do NOT remove:
    - The `// Compatibility date for Nuxt features` and similar explanatory comments that label active sections
    - The `// Google Fonts served locally via @nuxtjs/google-fonts` comment (it explains an intentional absence)
    - The `// Disabled by default, enable when ready` comment on typeCheck (it communicates intent)
    - Any comment that is part of active code logic (e.g., inline comments on active lines)
    - The active `recaptchaSiteKey`, GTM ID in runtimeConfig, or any other active configuration
  </action>
  <verify>
    <automated>grep -n "gtm:\|@nuxtjs/i18n\|i18n:\|defaultLocale\|provider.*ipx\|manifest.json\|feedbackIntegration" apps/dashboard/nuxt.config.ts</automated>
  </verify>
  <done>
    - nuxt.config.ts contains no commented-out module entries or config blocks for GTM module, i18n, image provider, or manifest link
    - All active configuration (modules array, security, strapi, sentry, runtimeConfig, etc.) is unchanged
    - nuxt.config.ts remains valid TypeScript that Nuxt can parse
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. AppStore has no website-only fields:
   `grep "isSearch\|isLogin\|contactForm" apps/dashboard/app/stores/app.store.ts apps/dashboard/app/types/app.d.ts` — no output

2. nuxt.config.ts is clean:
   `grep "gtm:\|@nuxtjs/i18n\|i18n:\|feedbackIntegration\|manifest.json" apps/dashboard/nuxt.config.ts` — no output

3. TypeScript accepts the pruned store and type:
   `cd apps/dashboard && npx nuxi typecheck 2>&1 | grep "app.store\|app.d.ts" | head -5 || echo "No errors in store/types"`

4. Active config intact — Nuxt can start without errors:
   `cd apps/dashboard && npx nuxi prepare 2>&1 | tail -5`
</verification>

<success_criteria>
- AppStore contains exactly two state fields: referer and isMobileMenuOpen, plus their associated getters and actions
- nuxt.config.ts has no commented-out module blocks or config sections
- TypeScript type check passes for app.store.ts and app.d.ts
</success_criteria>

<output>
After completion, create `.planning/phases/03-quick-wins/03-04-SUMMARY.md`
</output>
