---
phase: 03-quick-wins
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/composables/useLogger.ts
  - apps/dashboard/app/plugins/console.client.ts
autonomous: true
requirements:
  - QUICK-04

must_haves:
  truths:
    - "A thrown error captured via useLogger's logError is sent to Sentry in production"
    - "console.error calls in production are NOT silenced — they reach Sentry's browser console integration and browser devtools"
    - "console.debug, console.warn, and console.info are still suppressed in production to reduce noise"
  artifacts:
    - path: "apps/dashboard/app/composables/useLogger.ts"
      provides: "useLogger composable with active Sentry captureException"
      contains: "Sentry.captureException"
    - path: "apps/dashboard/app/plugins/console.client.ts"
      provides: "Console plugin that suppresses debug/warn/info but preserves console.error"
  key_links:
    - from: "apps/dashboard/app/composables/useLogger.ts"
      to: "@sentry/nuxt"
      via: "import * as Sentry from \"@sentry/nuxt\"; Sentry.captureException(error)"
      pattern: "Sentry\\.captureException"
    - from: "apps/dashboard/app/plugins/console.client.ts"
      to: "browser console"
      via: "console.error is not overridden in production block"
      pattern: "console\\.error"
---

<objective>
Restore production error visibility: uncomment Sentry error reporting in useLogger and stop suppressing console.error in the production console plugin.

Purpose: Currently useLogger.logError is a no-op (Sentry call is commented out), and console.client.ts sets `console.error = () => {}` in production — meaning all runtime errors are silently discarded. This makes production debugging impossible.

Output: useLogger sends errors to Sentry; console.error is preserved in production.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Current useLogger.ts (broken — Sentry is commented out) -->
// import * as Sentry from "@sentry/nuxt";

export function useLogger() {
  const logError = (error: any) => {
    // Sentry.captureException(new Error(`Uh oh, something broke, here's the error: '${error}'`));
  };

  const logInfo = (message: string) => {
    // Sentry.captureMessage(message);
    console.info(message);
  };

  return { logError, logInfo };
}

<!-- Current console.client.ts (suppresses ALL output including errors in production) -->
export default defineNuxtPlugin(() => {
  if (import.meta.dev) {
    // development mode
  } else {
    console.debug = () => {};
    console.warn = () => {};
    console.error = () => {};   // <-- kills error visibility
    console.info = () => {};
  }
});

<!-- sentry.client.config.ts is already correctly configured and initializes Sentry SDK -->
<!-- @sentry/nuxt is already in package.json dependencies -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restore Sentry error capture in useLogger</name>
  <files>apps/dashboard/app/composables/useLogger.ts</files>
  <action>
    Rewrite useLogger.ts to actively capture errors via Sentry:

    ```typescript
    import * as Sentry from "@sentry/nuxt";

    export function useLogger() {
      const logError = (error: unknown) => {
        Sentry.captureException(error);
      };

      const logInfo = (message: string) => {
        Sentry.captureMessage(message);
      };

      return {
        logError,
        logInfo,
      };
    }
    ```

    Key changes:
    - Uncomment the `import * as Sentry from "@sentry/nuxt"` line
    - Replace the no-op logError body with `Sentry.captureException(error)` — pass the error directly, do not wrap in `new Error(...)` (wrapping loses the original stack trace)
    - Uncomment `Sentry.captureMessage(message)` in logInfo (keep it alongside or replace console.info — your choice, but logInfo must call Sentry.captureMessage)
    - Change `error: any` to `error: unknown` for better type safety

    Do NOT add conditional checks like `if (process.env.NODE_ENV === 'production')` — the Sentry SDK itself is only initialized in production (per sentry.client.config.ts and nuxt.config.ts `sentry.enabled` flag), so in development Sentry calls are no-ops.
  </action>
  <verify>
    <automated>grep -n "Sentry.captureException\|import.*Sentry" apps/dashboard/app/composables/useLogger.ts</automated>
  </verify>
  <done>
    - useLogger.ts imports Sentry from "@sentry/nuxt"
    - logError calls Sentry.captureException(error) with no surrounding comments
    - The file has no commented-out Sentry lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Preserve console.error in production console plugin</name>
  <files>apps/dashboard/app/plugins/console.client.ts</files>
  <action>
    Remove the `console.error = () => {};` line from the production block. Keep all other suppressions (debug, warn, info) as they are.

    The result should be:

    ```typescript
    export default defineNuxtPlugin(() => {
      if (import.meta.dev) {
        // development mode
      } else {
        console.debug = () => {};
        console.warn = () => {};
        console.info = () => {};
      }
    });
    ```

    Do NOT remove suppression of console.debug, console.warn, or console.info — only console.error must be restored.

    The rationale: Sentry's browser SDK hooks into unhandled errors and window.onerror. If console.error is overridden to a no-op before Sentry can capture it, errors are silently lost. Preserving console.error ensures errors reach both Sentry and the browser devtools.
  </action>
  <verify>
    <automated>grep -n "console.error" apps/dashboard/app/plugins/console.client.ts</automated>
  </verify>
  <done>
    - console.client.ts does not contain `console.error = () => {}`
    - console.client.ts still suppresses console.debug, console.warn, and console.info in production
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. useLogger captures errors via Sentry:
   `grep "Sentry.captureException" apps/dashboard/app/composables/useLogger.ts` — returns a match

2. console.error not suppressed:
   `grep "console.error = " apps/dashboard/app/plugins/console.client.ts` — returns no output

3. Other suppressions still present:
   `grep "console.debug\|console.warn\|console.info" apps/dashboard/app/plugins/console.client.ts` — returns 3 suppression lines
</verification>

<success_criteria>
- useLogger.logError(err) sends the error to Sentry via captureException
- console.error is not overridden in production — errors appear in browser devtools and are captured by Sentry's browser integration
- console.debug, console.warn, console.info remain suppressed in production
</success_criteria>

<output>
After completion, create `.planning/phases/03-quick-wins/03-03-SUMMARY.md`
</output>
