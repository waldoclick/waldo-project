---
phase: 122-migrate-strapi-entityservice-to-strapi-db-query-for-strapi-v5-compatibility
plan: 04
type: execute
wave: 2
depends_on: ["122-01", "122-02", "122-03"]
files_modified:
  - apps/strapi/tests/cron/subscription-charge.cron.test.ts
  - apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts
  - apps/strapi/tests/api/payment/services/pack.zoho.test.ts
  - apps/strapi/tests/api/payment/payment-pro-response.test.ts
  - apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts
  - apps/strapi/tests/api/payment/controllers/payment.test.ts
  - apps/strapi/tests/api/article/content-types/article/article.lifecycles.test.ts
autonomous: true
requirements: [MIG-02, MIG-03, MIG-04]

must_haves:
  truths:
    - "Zero strapi.entityService references in entire apps/strapi/src/ directory"
    - "Zero strapi.entityService references in entire apps/strapi/tests/ directory"
    - "All test mocks route through strapi.db.query instead of strapi.entityService"
    - "Full test suite passes (yarn test --no-coverage)"
    - "TypeScript compiles without errors (npx tsc --noEmit)"
  artifacts:
    - path: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      provides: "Updated test mocks using db.query dispatch pattern"
      contains: "strapi.db.query"
    - path: "apps/strapi/tests/api/payment/services/pack.zoho.test.ts"
      provides: "Updated Zoho test mocks using db.query"
      contains: "strapi.db.query"
  key_links:
    - from: "apps/strapi/tests/cron/subscription-charge.cron.test.ts"
      to: "strapi.db.query mock dispatch"
      via: "mockImplementation routing pattern per UID"
      pattern: "db.*query.*mockImplementation"
---

<objective>
Update all 7 test files that mock strapi.entityService to use strapi.db.query mocks instead. Run final verification sweep to confirm zero entityService references remain in the entire Strapi codebase.

Purpose: Tests that mock entityService will either fail or silently pass without intercepting calls. This plan ensures test infrastructure matches the migrated production code.
Output: 7 updated test files. Full green test suite. Zero entityService grep hits across all of apps/strapi/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-migrate-strapi-entityservice-to-strapi-db-query-for-strapi-v5-compatibility/122-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 7 test file mocks from entityService to db.query</name>
  <files>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts,
    apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts,
    apps/strapi/tests/api/payment/services/pack.zoho.test.ts,
    apps/strapi/tests/api/payment/payment-pro-response.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts,
    apps/strapi/tests/api/payment/controllers/payment.test.ts,
    apps/strapi/tests/api/article/content-types/article/article.lifecycles.test.ts
  </files>
  <read_first>
    apps/strapi/tests/cron/subscription-charge.cron.test.ts (most complex — has entityService mock + existing db.query mock),
    apps/strapi/tests/bootstrap/migrate-subscription-pro.test.ts,
    apps/strapi/tests/api/payment/services/pack.zoho.test.ts,
    apps/strapi/tests/api/payment/payment-pro-response.test.ts,
    apps/strapi/tests/api/payment/services/pro-cancellation.service.test.ts,
    apps/strapi/tests/api/payment/controllers/payment.test.ts,
    apps/strapi/tests/api/article/content-types/article/article.lifecycles.test.ts
  </read_first>
  <action>
    For each test file, the pattern is: remove `entityService: { ... }` from the global strapi mock and route those mock methods through the existing `strapi.db.query` mock using the UID-dispatch pattern established in Phase 120.

    **subscription-charge.cron.test.ts (~line 70):**
    This file already has a `db: { query: mockDbQuery }` mock. The entityService mock at line 70 has `findMany`, `create`, `update` methods.
    1. Delete the entire `entityService: { findMany: ..., create: ..., update: ... }` block from the strapi mock object.
    2. Expand the existing `mockDbQuery` function to handle the UIDs that were previously served by entityService:
       - `"api::subscription-payment.subscription-payment"` — needs findMany, create, update mocks
       - `"plugin::users-permissions.user"` — needs update mock
       - `"api::subscription-pro.subscription-pro"` — needs update mock (was the alias pattern)
    3. Use the mockImplementation routing pattern: `mockDbQuery.mockImplementation((uid: string) => { switch(uid) { case "api::subscription-payment.subscription-payment": return { findMany: ..., create: ..., update: ... }; ... } })`
    4. Update all test assertions that reference `strapi.entityService.findMany` to reference the appropriate db.query mock function instead.

    **pack.zoho.test.ts (~lines 17, 23, 105):**
    Replace `entityService: { findOne: mockFindOne }` with routing through db.query:
    `db: { query: jest.fn().mockImplementation((uid: string) => { if (uid === "plugin::users-permissions.user") return { findOne: mockFindOne }; }) }`
    Update line ~105 that references `strapi.entityService.findOne.mockResolvedValue(...)` to reference the mock through db.query instead.

    **payment-pro-response.test.ts (~line 79):**
    Replace `entityService: { ... }` with equivalent methods routed through db.query mock.

    **pro-cancellation.service.test.ts (~line 33):**
    Replace `entityService: { ... }` with db.query routing.

    **payment.test.ts (~line 103):**
    Replace `entityService: { ... }` with db.query routing.

    **migrate-subscription-pro.test.ts (~line 31):**
    Replace `entityService: { findMany: ... }` with db.query routing for `"plugin::users-permissions.user"`.

    **article.lifecycles.test.ts (~line 15):**
    Replace `entityService: { findOne: ... }` with db.query routing for `"api::article.article"`.

    For ALL files: ensure no unused variables remain after removing entityService mocks (CLAUDE.md rule: never leave unused vars).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && grep -r "entityService" tests/ | wc -l && yarn test --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 0 lines for entityService in tests/
    - All tests pass (yarn test --no-coverage exits 0)
    - No unused variables in any test file
    - db.query mock dispatch handles all UIDs previously served by entityService
  </acceptance_criteria>
  <done>All 7 test files updated. Zero entityService in test mocks. Full test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification sweep — zero entityService in entire Strapi codebase</name>
  <files>None (verification only)</files>
  <read_first>None</read_first>
  <action>
    Run comprehensive verification to confirm the migration is complete:

    1. `grep -r "entityService" apps/strapi/src/` — must return empty (zero hits in production code)
    2. `grep -r "entityService" apps/strapi/tests/` — must return empty (zero hits in test code)
    3. `cd apps/strapi && npx tsc --noEmit` — must exit 0 (TypeScript clean)
    4. `cd apps/strapi && yarn test --no-coverage` — must exit 0 (all tests pass)

    If any grep hits remain, identify and fix them. Common stragglers:
    - Comments referencing entityService (update comments to reference db.query)
    - String literals in error messages (update text)

    If TypeScript errors exist, they are likely from:
    - Leftover cast artifacts referencing `typeof strapi.entityService`
    - Missing import removal (if entityService types were imported somewhere)

    Report final counts in the summary.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && echo "=== entityService grep ===" && grep -r "entityService" src/ tests/ | wc -l && echo "=== TypeScript ===" && npx tsc --noEmit 2>&1 | tail -3 && echo "=== Tests ===" && yarn test --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "entityService" across src/ and tests/ returns 0 lines
    - npx tsc --noEmit exits 0
    - yarn test --no-coverage exits 0 with all tests passing
  </acceptance_criteria>
  <done>Migration verified complete. Zero entityService references in entire apps/strapi/ directory. TypeScript clean. All tests green.</done>
</task>

</tasks>

<verification>
- `grep -r "entityService" apps/strapi/` returns empty
- `cd apps/strapi && npx tsc --noEmit` exits 0
- `cd apps/strapi && yarn test --no-coverage` exits 0
</verification>

<success_criteria>
- 7 test files updated with db.query mocks
- Zero entityService references in entire apps/strapi/ (src + tests)
- TypeScript compiles cleanly
- Full test suite passes
- Phase 122 migration is verifiably complete
</success_criteria>

<output>
After completion, create `.planning/phases/122-migrate-strapi-entityservice-to-strapi-db-query-for-strapi-v5-compatibility/122-04-SUMMARY.md`
</output>
