---
phase: 060-mostrar-comprobante-webpay
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/website/tests/components/ResumeOrder.test.ts
  - apps/website/tests/pages/gracias.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Test scaffolds exist for ResumeOrder component validation"
    - "Test scaffolds exist for gracias.vue page validation"
    - "All Webpay receipt fields can be tested before implementation"
  artifacts:
    - path: "apps/website/tests/components/ResumeOrder.test.ts"
      provides: "Unit tests for ResumeOrder with Webpay fields"
      min_lines: 30
    - path: "apps/website/tests/pages/gracias.test.ts"
      provides: "Integration tests for gracias page with receipt display"
      min_lines: 30
  key_links:
    - from: "tests/components/ResumeOrder.test.ts"
      to: "app/components/ResumeOrder.vue"
      via: "import statement"
      pattern: "import.*ResumeOrder"
---

<objective>
Create test scaffolds for ResumeOrder component and gracias.vue page to enable TDD workflow for Webpay receipt display.

Purpose: Establish automated verification before implementation, following Nyquist validation protocol.
Output: Test files with initial test cases for all required Webpay receipt fields.
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/060-mostrar-comprobante-webpay/060-CONTEXT.md
@.planning/phases/060-mostrar-comprobante-webpay/060-RESEARCH.md
@.planning/phases/060-mostrar-comprobante-webpay/060-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ResumeOrder component test scaffold</name>
  <files>apps/website/tests/components/ResumeOrder.test.ts</files>
  <action>
Create Vitest test file for ResumeOrder.vue component with test cases covering:

1. Test: Component renders all Webpay receipt fields when summary prop includes payment_response data
   - Mock summary with payment_response: { authorization_code, payment_type_code, card_detail: { card_number }, commerce_code }
   - Assert all CardInfo components render with correct Spanish labels
   - Assert "No disponible" shown for missing fields

2. Test: Component shows placeholder for missing payment_response fields
   - Mock summary with partial payment_response (e.g., missing authorization_code)
   - Assert "No disponible" displayed for missing field

3. Test: Component handles null/undefined payment_response gracefully
   - Mock summary without payment_response field
   - Assert component doesn't crash, shows placeholders

Use @nuxt/test-utils for component mounting.
Follow existing test patterns from apps/website/tests/utils/*.test.ts.
Import ResumeOrder from "@/components/ResumeOrder.vue".
  </action>
  <verify>
    <automated>yarn test tests/components/ResumeOrder.test.ts</automated>
  </verify>
  <done>Test file exists, all 3 test cases defined, tests fail (RED phase - no implementation yet)</done>
</task>

<task type="auto">
  <name>Task 2: Create gracias.vue page test scaffold</name>
  <files>apps/website/tests/pages/gracias.test.ts</files>
  <action>
Create Vitest integration test file for gracias.vue page with test cases covering:

1. Test: prepareSummary() extracts Webpay fields from Order.payment_response
   - Mock Order with payment_response containing all Webpay fields
   - Call prepareSummary()
   - Assert returned object includes: authorizationCode, paymentType, cardLast4, commerceCode

2. Test: prepareSummary() handles missing payment_response fields
   - Mock Order with partial payment_response
   - Assert function doesn't throw, returns partial data

3. Test: Page displays receipt only when order.status === 'completed' or 'paid'
   - Mock successful order
   - Assert ResumeOrder component receives summary prop

Use @nuxt/test-utils for page testing.
Import prepareSummary function or test via page render.
  </action>
  <verify>
    <automated>yarn test tests/pages/gracias.test.ts</automated>
  </verify>
  <done>Test file exists, all 3 test cases defined, tests fail (RED phase - implementation in Plan 01)</done>
</task>

</tasks>

<verification>
- All test files created and executable
- Tests fail with meaningful errors (expected - no implementation yet)
- Test commands run in under 30 seconds
- No TypeScript errors in test files
</verification>

<success_criteria>
- Test scaffolds exist at specified paths
- `yarn test` runs both test files without crashing
- All tests in RED state (failing) awaiting implementation
- Test coverage includes all 4 required Webpay fields (authorization_code, payment_type_code, card last 4, commerce_code)
</success_criteria>

<output>
After completion, create `.planning/phases/060-mostrar-comprobante-webpay/060-00-SUMMARY.md`
</output>
