---
phase: 04-split-legal-pages-into-4-documents-with-dashboard-management
plan: 07
type: execute
wave: 4
depends_on: ["04-05", "04-06"]
files_modified:
  - apps/website/app/components/MenuMaintenance.vue
autonomous: true
requirements: [LEGAL-SPLIT-07]

must_haves:
  truths:
    - "Dashboard sidebar nav shows a link to the Cookies policy CRUD and a link to the Security policy CRUD"
    - "The existing Términos nav entry label updates to match the D-06 rename (handled together here since this is the same shared file touched by the rename in Plan 08)"
  artifacts:
    - path: "apps/website/app/components/MenuMaintenance.vue"
      provides: "2 new nav <li> entries + knownSubRoutes additions for cookies and security"
  key_links:
    - from: "apps/website/app/components/MenuMaintenance.vue"
      to: "/dashboard/maintenance/cookies"
      via: "NuxtLink to=\"/dashboard/maintenance/cookies\""
      pattern: "/dashboard/maintenance/cookies"
    - from: "apps/website/app/components/MenuMaintenance.vue"
      to: "/dashboard/maintenance/security"
      via: "NuxtLink to=\"/dashboard/maintenance/security\""
      pattern: "/dashboard/maintenance/security"
---

<objective>
Add 2 new dashboard sidebar nav entries — Cookies and Seguridad — to `MenuMaintenance.vue`, and register both new routes in its `knownSubRoutes` array so the "Mantenedores" root-active highlighting logic continues to work correctly.

Purpose: `MenuMaintenance.vue` is a single shared file — both new dashboard sections need entries added to it in the same edit to avoid two plans racing on the same file. This plan runs after both Plan 05 (Cookies dashboard routes) and Plan 06 (Security dashboard routes) exist, since the nav links point at routes those plans create.

Output: `MenuMaintenance.vue` with 2 new `<li>` nav entries (using distinct lucide-vue-next icons not already used for Términos/Políticas) and 2 new entries in `knownSubRoutes`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-CONTEXT.md
@.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-RESEARCH.md
</context>

<interfaces>
<!-- apps/website/app/components/MenuMaintenance.vue (full file, 215 lines) — current state, showing the exact insertion points -->
```vue
<template>
  <nav class="menu menu--maintenance">
    <ul class="menu--maintenance__list">
      <!-- ... Mantenedores root, Categorías, Condiciones, Packs, Regiones, Comunas, Preguntas frecuentes ... -->

      <!-- Políticas de privacidad -->
      <li
        class="menu--maintenance__item"
        :class="{
          'menu--maintenance__item--active': isRouteActive(
            '/dashboard/maintenance/policies',
          ),
        }"
      >
        <NuxtLink
          to="/dashboard/maintenance/policies"
          class="menu--maintenance__link"
        >
          <Shield class="menu--maintenance__icon" />
          <span>Políticas de privacidad</span>
        </NuxtLink>
      </li>

      <!-- Condiciones de Uso -->
      <li
        class="menu--maintenance__item"
        :class="{
          'menu--maintenance__item--active': isRouteActive(
            '/dashboard/maintenance/terms',
          ),
        }"
      >
        <NuxtLink
          to="/dashboard/maintenance/terms"
          class="menu--maintenance__link"
        >
          <ScrollText class="menu--maintenance__icon" />
          <span>Condiciones de Uso</span>
        </NuxtLink>
      </li>
    </ul>
  </nav>
</template>

<script setup lang="ts">
import { computed, watch } from "vue";
import { useRoute } from "vue-router";
import {
  Settings,
  Tag,
  FileCheck,
  Box,
  MapPin,
  Building,
  HelpCircle,
  Shield,
  ScrollText,
} from "lucide-vue-next";

const emit = defineEmits<{ (e: "close"): void }>();

const route = useRoute();

const isRouteActive = (path: string): boolean => {
  if (path === "/dashboard/maintenance")
    return route.path === "/dashboard/maintenance";
  return route.path.startsWith(path);
};

const knownSubRoutes = [
  "/dashboard/maintenance/categories",
  "/dashboard/maintenance/conditions",
  "/dashboard/maintenance/packs",
  "/dashboard/maintenance/regions",
  "/dashboard/maintenance/communes",
  "/dashboard/maintenance/faqs",
  "/dashboard/maintenance/policies",
  "/dashboard/maintenance/terms",
];

const isMaintenanceRootActive = computed(
  () =>
    route.path === "/dashboard/maintenance" ||
    (route.path.startsWith("/dashboard/maintenance/") &&
      !knownSubRoutes.some((r) => route.path.startsWith(r))),
);

watch(
  () => route.path,
  () => {
    emit("close");
  },
  { immediate: true },
);
</script>
```

Note: this plan's Task 1 also updates the "Condiciones de Uso" nav entry's visible label to "Términos y Condiciones de Uso" per D-06 — this label change is duplicated with Plan 08 (URL rename plan) ONLY in the sense that both touch label text; MenuMaintenance.vue is NOT in Plan 08's files_modified list (Plan 08 only touches the 8 reference-point files enumerated in 04-RESEARCH.md section 7, which does not include the dashboard's MenuMaintenance.vue — that file is dashboard-internal navigation, separate from the public-facing reference points). This plan owns the dashboard nav label exclusively.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Cookies and Seguridad nav entries, update Términos label, extend knownSubRoutes</name>
  <files>apps/website/app/components/MenuMaintenance.vue</files>
  <read_first>apps/website/app/components/MenuMaintenance.vue</read_first>
  <action>
Make 3 edits to `apps/website/app/components/MenuMaintenance.vue`:

1. Update the existing "Condiciones de Uso" `<li>` block's visible label from "Condiciones de Uso" to "Términos y Condiciones de Uso" (per D-06 — this is the dashboard nav's own copy of the label, separate from the public-facing reference points handled in Plan 08):
```vue
      <!-- Términos y Condiciones de Uso -->
      <li
        class="menu--maintenance__item"
        :class="{
          'menu--maintenance__item--active': isRouteActive(
            '/dashboard/maintenance/terms',
          ),
        }"
      >
        <NuxtLink
          to="/dashboard/maintenance/terms"
          class="menu--maintenance__link"
        >
          <ScrollText class="menu--maintenance__icon" />
          <span>Términos y Condiciones de Uso</span>
        </NuxtLink>
      </li>
```

2. Add 2 new `<li>` nav entries immediately after the "Términos y Condiciones de Uso" entry (using `Cookie` and `ShieldCheck` icons from `lucide-vue-next` — distinct from the `Shield` icon already used for Políticas de privacidad):
```vue
      <!-- Política de Cookies -->
      <li
        class="menu--maintenance__item"
        :class="{
          'menu--maintenance__item--active': isRouteActive(
            '/dashboard/maintenance/cookies',
          ),
        }"
      >
        <NuxtLink
          to="/dashboard/maintenance/cookies"
          class="menu--maintenance__link"
        >
          <Cookie class="menu--maintenance__icon" />
          <span>Política de Cookies</span>
        </NuxtLink>
      </li>

      <!-- Política de Seguridad -->
      <li
        class="menu--maintenance__item"
        :class="{
          'menu--maintenance__item--active': isRouteActive(
            '/dashboard/maintenance/security',
          ),
        }"
      >
        <NuxtLink
          to="/dashboard/maintenance/security"
          class="menu--maintenance__link"
        >
          <ShieldCheck class="menu--maintenance__icon" />
          <span>Política de Seguridad</span>
        </NuxtLink>
      </li>
```

3. In the `<script setup>` block: add `Cookie, ShieldCheck` to the `lucide-vue-next` import list, and add `"/dashboard/maintenance/cookies"` and `"/dashboard/maintenance/security"` to the `knownSubRoutes` array:
```ts
import {
  Settings,
  Tag,
  FileCheck,
  Box,
  MapPin,
  Building,
  HelpCircle,
  Shield,
  ScrollText,
  Cookie,
  ShieldCheck,
} from "lucide-vue-next";

// ...

const knownSubRoutes = [
  "/dashboard/maintenance/categories",
  "/dashboard/maintenance/conditions",
  "/dashboard/maintenance/packs",
  "/dashboard/maintenance/regions",
  "/dashboard/maintenance/communes",
  "/dashboard/maintenance/faqs",
  "/dashboard/maintenance/policies",
  "/dashboard/maintenance/terms",
  "/dashboard/maintenance/cookies",
  "/dashboard/maintenance/security",
];
```

Do not add any home-page or "featured" toggle here — this file is dashboard nav only (D-08 out of scope).
  </action>
  <verify>
    <automated>grep -q '/dashboard/maintenance/cookies' apps/website/app/components/MenuMaintenance.vue && grep -q '/dashboard/maintenance/security' apps/website/app/components/MenuMaintenance.vue && grep -q "Términos y Condiciones de Uso" apps/website/app/components/MenuMaintenance.vue && grep -q "Cookie," apps/website/app/components/MenuMaintenance.vue && grep -q "ShieldCheck" apps/website/app/components/MenuMaintenance.vue</automated>
  </verify>
  <done>MenuMaintenance.vue has 2 new nav entries (Cookies, Seguridad) pointing to the correct dashboard routes, knownSubRoutes includes both new paths, and the Términos entry's visible label reads "Términos y Condiciones de Uso".</done>
</task>

</tasks>

<verification>
Run from apps/website:
```bash
node ../../node_modules/.bin/vue-tsc --noEmit
```
Must exit 0. Manually confirm `knownSubRoutes` array has exactly 10 entries (8 pre-existing + 2 new) with no duplicates.
</verification>

<success_criteria>
- MenuMaintenance.vue nav includes working links to both new dashboard CRUD sections.
- knownSubRoutes correctly prevents the "Mantenedores" root link from being falsely highlighted when viewing the new sub-sections.
- Términos label updated to match D-06 in this dashboard-internal copy.
- vue-tsc --noEmit exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-07-SUMMARY.md`
</output>
