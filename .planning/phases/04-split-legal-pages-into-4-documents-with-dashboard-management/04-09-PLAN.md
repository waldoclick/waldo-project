---
phase: 04-split-legal-pages-into-4-documents-with-dashboard-management
plan: 09
type: execute
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07", "04-08"]
files_modified: []
autonomous: false
requirements: [LEGAL-SPLIT-10]

must_haves:
  truths:
    - "Public Cookies and Seguridad pages return real content (not 403/empty) after permissions are granted"
    - "Dashboard drag-and-drop reorder visually works for both new sections"
    - "Both public pages render correctly with brand-consistent styling"
  artifacts: []
  key_links: []
---

<objective>
Grant the manual Strapi admin-panel permissions required for the 2 new content-types (no code path exists in this repo to script this — confirmed by research: zero matches for `up_permissions` or `plugin::users-permissions` bootstrap code anywhere in the codebase, not even for the existing `term`/`policy`/`faq` content-types, which were granted the same way), then perform human visual verification of the complete feature.

Purpose: This is the final gate before the phase is considered done. All prior plans (01-08) build the code; this plan confirms the deployed/running system actually serves the new pages and that the dashboard's visual/interactive elements (drag-and-drop, accordion rendering) work as expected — things no automated command can fully confirm per this project's testing conventions ("Verify non-TypeScript file changes explicitly" — CLAUDE.md).

Output: Public find/findOne permissions granted for `cookie-policy` and `security-policy` in every relevant environment; human confirmation that the 4-document legal pages + matching dashboard sections work end-to-end.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-CONTEXT.md
@.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Grant Public find/findOne permissions for cookie-policy and security-policy</name>
  <files>N/A — Strapi admin panel UI action, no repo files touched</files>
  <action>
Grant Public find/findOne permissions for the 2 new content-types in the Strapi admin panel. This step cannot be scripted — this repo has zero code path that touches the `up_permissions` table or the `users-permissions` plugin's permission model (confirmed by codebase search during research: no seeder, migration, or bootstrap script does this anywhere, not even for the existing `term`/`policy`/`faq` content-types, which were granted access purely via manual admin-panel clicks with no corresponding code artifact).

For EACH environment where this phase is deployed (local dev, staging, production — as applicable):

1. Restart Strapi so it picks up the 2 new content-type schemas from Plan 01 (`cookie-policy`, `security-policy`).
2. Go to Strapi Admin → Settings → Users & Permissions plugin → Roles → **Public**.
3. Find **Cookie-policy** in the permissions list, check **find** and **findOne**.
4. Find **Security-policy** in the permissions list, check **find** and **findOne**.
5. Save.
6. Confirm `GET {strapi-url}/api/cookie-policies` and `GET {strapi-url}/api/security-policies` return `200` with seeded data (not `403 Forbidden` or empty), from an unauthenticated request (e.g. `curl` without an Authorization header, or a private/incognito browser tab).
  </action>
  <verify>
    <automated>MISSING — this is an admin-panel UI action with no CLI/API path in this repo; verification is the manual curl checks in step 6 above.</automated>
  </verify>
  <done>Public find/findOne permissions granted for both content-types in every relevant environment; unauthenticated GET requests to /api/cookie-policies and /api/security-policies return 200 with data.</done>
  <resume-signal>Reply "permissions granted" once done in all relevant environments (or specify which environments were completed if partial).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional end-to-end verification</name>
  <files>N/A — verification only, no files modified</files>
  <action>
Verify the complete legal-docs feature built across Plans 01-08: 2 new Strapi content-types with seeded content (13 Cookies rows, 19 Seguridad rows), 2 new public pages, 2 new dashboard CRUD sections with drag-and-drop reorder, and a full URL rename of the old Términos page with 301 redirect and label updates everywhere. Perform these 12 checks:

1. Visit `/condiciones-de-uso` — confirm it 301-redirects to `/terminos-y-condiciones-de-uso`, and the page renders the Términos accordion with H1 "Términos y Condiciones de Uso".
2. Visit `/politicas-de-privacidad` — confirm it still renders correctly with ONLY Privacidad content (no Cookies/Seguridad sections mixed in — this confirms the seeder split in Plan 02 worked).
3. Visit `/politicas-de-cookies` — confirm it renders an accordion with 13 sections, matching `docs/politica-de-cookies.md`'s topics.
4. Visit `/politicas-de-seguridad` — confirm it renders an accordion with 19 sections, matching `docs/politica-de-seguridad.md`'s topics.
5. Check the footer and "Menú informativo" (MenuAbout) — confirm the label reads "Términos y Condiciones de Uso" (not "Condiciones de uso"), and no new Cookies/Seguridad links were added there (out of scope per the resolved decision).
6. Check `/sitemap` — confirm it lists Términos y Condiciones de Uso, Política de Cookies, and Política de Seguridad as 3 distinct entries.
7. Go through registration (`/registro`) — confirm there are still exactly 2 acceptance checkboxes (Términos + Privacidad), and the Términos checkbox links to `/terminos-y-condiciones-de-uso` with the updated label. Confirm Cookies/Seguridad were NOT added to this flow.
8. Trigger the cookie-consent banner (clear the `site-cookies-accepted-v2` cookie and reload) — confirm the "Más información" button now links to `/politicas-de-cookies`, and the banner's body text references "Política de Cookies" (not "Política de Privacidad").
9. In the dashboard, go to `/dashboard/maintenance/cookies` — confirm the list renders, search/filter works, and dragging a row to reorder persists after a page refresh.
10. In the dashboard, go to `/dashboard/maintenance/security` — same checks as step 9.
11. In the dashboard sidebar (MenuMaintenance), confirm both new nav entries appear with working icons, and the Términos entry's label reads "Términos y Condiciones de Uso".
12. Confirm NO "featured"/"destacado" checkbox or home-page section exists anywhere for any of the 4 legal documents (Términos, Privacidad, Cookies, Seguridad) — this was explicitly out of scope (D-08).
  </action>
  <verify>
    <automated>MISSING — visual rendering and drag-and-drop interaction require a human observer per this project's convention (CLAUDE.md: visual/CSS work is verified by screenshot-and-look, not typecheck alone); no automated substitute exists for these 12 checks.</automated>
  </verify>
  <done>All 12 manual verification checks pass with no regressions to the existing Términos/Privacidad pages or registration flow.</done>
  <resume-signal>Type "approved" if all 12 checks pass, or describe which check(s) failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
This plan is entirely manual/human-gated — there is no automated command that substitutes for steps 1-12 above (permission grants require Strapi admin UI access; visual rendering and drag-and-drop interaction require a human observer, per this project's established convention that visual/CSS work is verified by screenshot-and-look, not by typecheck alone).
</verification>

<success_criteria>
- Public find/findOne permissions granted for cookie-policy and security-policy in every deployed environment.
- All 12 manual verification checks pass.
- No regressions to the existing Términos/Privacidad pages or registration flow.
</success_criteria>

<output>
After completion, create `.planning/phases/04-split-legal-pages-into-4-documents-with-dashboard-management/04-09-SUMMARY.md`
</output>
