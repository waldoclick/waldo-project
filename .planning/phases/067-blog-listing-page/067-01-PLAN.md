---
phase: 067-blog-listing-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/CardArticle.vue
  - apps/website/app/components/RelatedArticles.vue
autonomous: true
requirements: [BLOG-17, BLOG-20]

must_haves:
  truths:
    - "CardArticle renders cover image (NuxtImg webp/lazy), category badge linked to /blog?category=, title (60-char truncation), header/excerpt (120-char truncation), formatted publishedAt date, and NuxtLink 'Leer más' to /blog/[slug]"
    - "RelatedArticles renders a section with head (title, optional text, optional centerHead), loading/error/empty/grid states, and a CardArticle for each article in the articles prop"
  artifacts:
    - path: apps/website/app/components/CardArticle.vue
      provides: Article card component for listing and related sections
      exports: [default]
    - path: apps/website/app/components/RelatedArticles.vue
      provides: Related articles block component
      exports: [default]
  key_links:
    - from: apps/website/app/components/RelatedArticles.vue
      to: apps/website/app/components/CardArticle.vue
      via: "<CardArticle v-for='article in articles' :key='article.id' :article='article' />"
      pattern: "CardArticle.*:article="
    - from: apps/website/app/components/CardArticle.vue
      to: apps/website/app/types/article.d.ts
      via: "import type { Article }"
      pattern: "import type.*Article.*article.d.ts"
---

<objective>
Create the two leaf display components for blog articles: `CardArticle` (single article card) and `RelatedArticles` (related articles block that uses `CardArticle`).

Purpose: These components are consumed by Plan 03's `ArticleArchive` and `blog/index.vue`. Building them first as leaves means Plan 03 receives stable, tested contracts.
Output: `CardArticle.vue` and `RelatedArticles.vue` in `apps/website/app/components/`
</objective>

<execution_context>
@/home/gabriel/.config/Claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/066-article-infrastructure/066-01-SUMMARY.md
@.planning/phases/066-article-infrastructure/066-02-SUMMARY.md

<interfaces>
<!-- Types and contracts the executor needs. No codebase exploration required. -->

From apps/website/app/types/article.d.ts:
```typescript
import type { Category } from "@/types/category";
import type { Media, GalleryItem } from "@/types/ad";

export interface Article {
  id: number;
  documentId: string;
  title: string;
  header: string;
  body: string;
  slug: string;
  cover: Media[];
  gallery: GalleryItem[];
  categories: Category[];
  seo_title: string | null;
  seo_description: string | null;
  publishedAt: string | null;
  createdAt: string;
}
```

Category type (inferred from usage in codebase):
```typescript
interface Category { id: number; name: string; slug: string; color: string; }
```

Media type (from @/types/ad, used in CardAnnouncement pattern):
```typescript
// gallery[0]?.formats?.medium?.url || gallery[0]?.url || "" for first image
// cover array — same Media[] pattern
```

BEM classes available (Phase 066, no new SCSS needed):
- .card--article → __image, __info, __info__categories, __info__name, __info__excerpt, __info__date, __info__link
- .related--articles → __container, __head, __head--center, __title, __text, __loading, __error, __empty, __grid

RelatedAds.vue contract (replicate for articles):
```vue
withDefaults(defineProps<{
  ads: Ad[]; loading: boolean; error: string | null;
  title?: string; text?: string; centerHead?: boolean;
}>(), { title: "Anuncios relacionados", text: "", centerHead: false });
```

CardAnnouncement.vue: uses useImageProxy composable for image URLs
```typescript
import { useImageProxy } from "@/composables/useImage";
const { transformUrl } = useImageProxy();
// getFirstImage: gallery[0]?.formats?.medium?.url || gallery[0]?.url || ""
// then: transformUrl(firstImageUrl)
```

stringTruncate helper (used in CardAnnouncement — replicate inline):
```typescript
const stringTruncate = (str: string, length: number): string =>
  str.length > length ? str.slice(0, Math.max(0, length)) + "..." : str;
```

Date formatting: use Intl.DateTimeFormat or toLocaleDateString for publishedAt
- Locale: "es-CL", options: { year: "numeric", month: "long", day: "numeric" }
- Guard: publishedAt may be null (draft state) — show empty string when null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CardArticle.vue</name>
  <files>apps/website/app/components/CardArticle.vue</files>
  <action>
Create `apps/website/app/components/CardArticle.vue` as a new component that displays a single article card. Mirror the structure and patterns of `CardAnnouncement.vue` but use article-specific BEM classes and data.

Requirements (BLOG-17):
- Accepts a single prop: `article: Article` (use `<script setup lang="ts">` with `defineProps<{ article: Article }>()`)
- Import `type { Article }` from `@/types/article`
- Import `useImageProxy` from `@/composables/useImage` — use `transformUrl` for the cover image URL
- Cover image: use `article.cover[0]?.formats?.medium?.url || article.cover[0]?.url || ""` then transformUrl(); wrap in NuxtImg with `format="webp"`, `loading="lazy"`, `width="400"`, `height="300"`, `remote`; wrap the img in a NuxtLink to `/blog/${article.slug}`
- Category badge: `article.categories[0]` if present — render as NuxtLink to `/blog?category=${cat.slug}` with class `card--article__info__categories`; guard with `v-if="article.categories && article.categories.length > 0"`
- Title: `stringTruncate(article.title, 60)` in an `<h3>` with class `card--article__info__name`; wrap in NuxtLink to `/blog/${article.slug}`
- Header/excerpt: `stringTruncate(article.header, 120)` in a `<p>` with class `card--article__info__excerpt`
- Date: format `article.publishedAt` using `new Intl.DateTimeFormat("es-CL", { year: "numeric", month: "long", day: "numeric" }).format(new Date(article.publishedAt))` — guard with `v-if="article.publishedAt"`, class `card--article__info__date`
- "Leer más" link: NuxtLink to `/blog/${article.slug}` with class `card--article__info__link`, text "Leer más"

Template structure:
```html
<article class="card card--article">
  <div class="card--article__image">...</div>
  <div class="card--article__info">
    <nav class="card--article__info__categories">...</nav>
    <h3 class="card--article__info__name">...</h3>
    <p class="card--article__info__excerpt">...</p>
    <div class="card--article__info__date">...</div>
    <NuxtLink class="card--article__info__link">Leer más</NuxtLink>
  </div>
</article>
```

stringTruncate helper (inline, not imported):
```typescript
const stringTruncate = (str: string, length: number): string =>
  str.length > length ? str.slice(0, Math.max(0, length)) + "..." : str;
```

Do NOT add `onMounted` for CSS custom property (articles have no per-card category color tint — per STATE.md decision: "Blog hero: white background... no category color tint").
Do NOT use `box-shadow` or `transform: scale` (AGENTS.md rule).
</action>
  <verify>
    <automated>cd apps/website && yarn nuxt typecheck 2>&1 | grep -E "CardArticle|error TS" | head -20</automated>
  </verify>
  <done>CardArticle.vue exists; `nuxt typecheck` emits zero errors for CardArticle.vue; component renders cover, category, title (60-char), header (120-char), date, "Leer más" link with correct BEM classes</done>
</task>

<task type="auto">
  <name>Task 2: Create RelatedArticles.vue</name>
  <files>apps/website/app/components/RelatedArticles.vue</files>
  <action>
Create `apps/website/app/components/RelatedArticles.vue` that mirrors `RelatedAds.vue` exactly but uses Article type and CardArticle component. Replicate the pattern character-for-character, substituting only the ads-specific parts.

Requirements (BLOG-20):
- Use `<script setup lang="ts">` with `withDefaults(defineProps<{...}>(), {...})`
- Props: `articles: Article[]`, `loading: boolean`, `error: string | null`, `title?: string`, `text?: string`, `centerHead?: boolean`
- Defaults: `title: "Artículos relacionados"`, `text: ""`, `centerHead: false`
- Import `type { Article }` from `@/types/article`
- Import `CardArticle` from `@/components/CardArticle.vue`
- Import `LoadingDefault` from `@/components/LoadingDefault.vue`

Template: use BEM class `related--articles` (not `related--ads`):
```html
<section class="related related--articles">
  <div class="related--articles__container">
    <div :class="['related--articles__head', { 'related--articles__head--center': centerHead }]">
      <h2 class="related--articles__title">{{ title }}</h2>
      <div v-if="text" class="related--articles__text">{{ text }}</div>
    </div>
    <div v-if="loading" class="related--articles__loading"><LoadingDefault /></div>
    <div v-else-if="error" class="related--articles__error">{{ error }}</div>
    <div v-else-if="articles.length === 0" class="related--articles__empty">No se encontraron artículos relacionados</div>
    <div v-else class="related--articles__grid">
      <CardArticle v-for="article in articles" :key="article.id" :article="article" />
    </div>
  </div>
</section>
```

This is a pure presentational component — no data fetching, no stores, no router.
</action>
  <verify>
    <automated>cd apps/website && yarn nuxt typecheck 2>&1 | grep -E "RelatedArticles|error TS" | head -20</automated>
  </verify>
  <done>RelatedArticles.vue exists; `nuxt typecheck` emits zero errors; component accepts articles/loading/error/title/text/centerHead props; renders CardArticle for each article</done>
</task>

</tasks>

<verification>
Run full typecheck after both tasks:
```bash
cd apps/website && yarn nuxt typecheck
```
Both components must compile with zero TypeScript errors. Verify files exist:
- `apps/website/app/components/CardArticle.vue`
- `apps/website/app/components/RelatedArticles.vue`
</verification>

<success_criteria>
- CardArticle.vue renders all 6 required fields (cover image, category badge, title 60-char, header 120-char, publishedAt date, "Leer más" link) using correct card--article BEM classes
- RelatedArticles.vue mirrors RelatedAds.vue pattern using related--articles BEM classes and CardArticle
- `yarn nuxt typecheck` passes with zero errors in both new files
</success_criteria>

<output>
After completion, create `.planning/phases/067-blog-listing-page/067-01-SUMMARY.md`
</output>
