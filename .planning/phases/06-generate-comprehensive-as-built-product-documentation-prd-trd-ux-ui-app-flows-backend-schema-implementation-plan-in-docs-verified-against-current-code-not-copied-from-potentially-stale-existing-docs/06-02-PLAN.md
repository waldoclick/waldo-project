---
phase: 06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [docs/FLOWS.md]
autonomous: true
requirements: [D-01, D-02, D-06, D-07, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "A reader can follow each of the 6 core flows end-to-end with a diagram and prose"
    - "Each flow's happy path, error states, and role-gated branches are described"
    - "The cron section states the correct count of 6 active jobs (+1 manual), not the stale 4"
    - "The payment flow states order identity is always order.documentId"
  artifacts:
    - path: "docs/FLOWS.md"
      provides: "App Flow document: 6 flows, each with a Mermaid diagram + prose"
      contains: "```mermaid"
      min_lines: 250
  key_links:
    - from: "docs/FLOWS.md"
      to: "apps/strapi/src/api/payment/controllers/payment.ts"
      via: "payment flow re-derived from source"
      pattern: "documentId"
    - from: "docs/FLOWS.md"
      to: "apps/strapi/config/cron-tasks.ts"
      via: "cron flow re-derived from source (6 active jobs)"
      pattern: "cron"
---

<objective>
Produce `docs/FLOWS.md` (App Flow) — the highest-scrutiny deliverable of this phase. Six core flows, each with a Mermaid diagram plus prose covering happy path, error states, and role-gated branches. Every flow re-derived from the CURRENT source files listed in RESEARCH.md's per-flow file map — NOT paraphrased from `.planning/codebase/*`, `.planning/milestones/*`, or existing `docs/*.md` (those are leads to confirm, per D-06).

Purpose: FLOWS.md is consumed by PRD.md (acceptance criteria derive from flow happy paths) and IPD.md (ties phases to flows they built). Must be committed and accurate before Wave 2.
Output: `docs/FLOWS.md`.

The user emphasized flows twice ("Sobre todo revisa bien el tema de los flujos. Ordénalo bien") — this is the deliverable held to the strictest verification bar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-CONTEXT.md
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-RESEARCH.md

<corrected_facts>
<!-- Per D-10 (current code wins). Both CLAUDE.md and CONTEXT.md D-06 are STALE on these: -->
- Cron jobs: 6 ACTIVE (userCron, adCron, cleanupCron, backupCron, verificationCodeCleanupCron,
  subscriptionChargeCron[only when PRO_ENABLE=true]) + 1 MANUAL-ONLY (userConfirmedMigration via
  POST /api/cron-runner/user-confirmed-migration). NOT "4 cron jobs". Source: apps/strapi/config/cron-tasks.ts.
- Reservation restore logic lives in apps/strapi/src/cron/ad-free-reservation-restore.cron.ts (singular
  `cron/` dir). docs/reservation-system.md cites a wrong path (`crons/user-*.cron.ts`) — do not repeat it.
- Payment order identity is ALWAYS order.documentId; gateway fields (buy_order, token_ws) are audit-only.
- Audit-log envelope: { actor, actor_type, data } via logAuditInfo/logAuditWarn/logAuditError.
  Describe ONCE in the CRUD+audit-log flow; link to it from payment/ad flows, do not re-explain.
</corrected_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/FLOWS.md flows 1-3 — auth, ad lifecycle, payment</name>
  <files>docs/FLOWS.md</files>
  <read_first>
    - .planning/phases/06-.../06-RESEARCH.md § "Per-Flow File Map" (flows 1, 2, 3) — the authoritative list of files to open
    - Flow 1 (auth): apps/strapi/src/extensions/users-permissions/controllers/authController.ts, apps/strapi/src/api/auth-verify/controllers/auth-verify.ts, apps/strapi/src/api/auth-google/controllers/auth-google.ts, apps/strapi/src/api/auth-one-tap/controllers/auth-one-tap.ts, apps/website/server/api/auth/verify-code.post.ts, apps/website/server/api/[...].ts, apps/website/app/middleware/auth.ts, apps/website/app/middleware/dashboard-guard.global.ts
    - Flow 2 (ad lifecycle): apps/strapi/src/api/ad/controllers/ad.ts, apps/strapi/src/api/ad/services/ad.ts (computeAdStatus/publishAd), apps/strapi/src/api/ad/routes/, apps/strapi/src/api/ad/content-types/ad/schema.json, docs/ad-statuses.md (lead — verify)
    - Flow 3 (payment): apps/strapi/src/api/payment/controllers/payment.ts, apps/strapi/src/api/payment/routes/payment.ts, apps/strapi/src/api/payment/services/{ad,pack,checkout,free-ad}.service.ts, docs/payment-flow.md (lead — verify)
  </read_first>
  <action>
    Create docs/FLOWS.md. Structure:
    1. Title `# Application Flows (FLOWS)` + intro paragraph: as-built, each flow re-derived from current source (cite the per-flow file map).
    2. `## Table of Contents` (D-02) linking to all 6 flow sections + Preguntas abiertas.
    3. `## Flow 1: Authentication` — cover JWT via httpOnly proxy cookie `waldo_jwt`, two-step verification (login step 1 returns pendingToken → step 2 verifies code → JWT issued and set as httpOnly cookie by the Nitro server route), Google One Tap, Google OAuth, email auth. Include one ```mermaid``` diagram (sequenceDiagram fits the client↔proxy↔Strapi round-trips). Prose: happy path, error states (401 anonymous, invalid code, SSR fail-open in middleware), role-gated branch (dashboard-guard requires role=manager).
    4. `## Flow 2: Ad Creation → Moderation → Publish` — trace saveDraft → submit → manager approveAd/rejectAd/banned → computeAdStatus/publishAd → active/expired states. One ```mermaid``` diagram (stateDiagram-v2 fits the status machine: draft/active/rejected/banned/expired). Prose: happy path, error/rejection states, role gate (global::isManager on approve/reject/banned).
    5. `## Flow 3: Payment / Checkout` — trace the 7 payment routes; STATE EXPLICITLY that order identity is always `order.documentId` and gateway fields are audit-only (per CLAUDE.md payment rules). One ```mermaid``` diagram (sequenceDiagram: frontend → /payments/checkout → Webpay → /payments/webpay callback → order created → redirect ?order={documentId}). Prose: happy path, error states (payment failure, callback auth:false routes), free-ad vs paid branch. Link the audit-log envelope to Flow 5 rather than re-explaining.

    Each flow's diagram MUST be a fenced ```mermaid block. Body text English (D-12); quote any Spanish UI strings verbatim. Re-derive from the source files above — DO NOT paraphrase RESEARCH.md or docs/*.md prose; open the code and trace it.
  </action>
  <verify>
    <automated>test -f docs/FLOWS.md && grep -q "documentId" docs/FLOWS.md && [ "$(grep -c '```mermaid' docs/FLOWS.md)" -ge 3 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/FLOWS.md` exits 0
    - `grep -c '```mermaid' docs/FLOWS.md` returns ≥3 after this task
    - `grep -q "Flow 1: Authentication" docs/FLOWS.md` exits 0
    - `grep -q "Flow 2" docs/FLOWS.md` and `grep -q "Flow 3" docs/FLOWS.md` exit 0
    - `grep -q "waldo_jwt" docs/FLOWS.md` exits 0 (auth cookie named)
    - `grep -q "order.documentId\|documentId" docs/FLOWS.md` exits 0 (payment identity rule stated)
    - `grep -q "Table of Contents\|Índice" docs/FLOWS.md` exits 0
  </acceptance_criteria>
  <done>docs/FLOWS.md exists with flows 1-3, each having a Mermaid diagram and happy/error/role prose; auth cookie and payment documentId identity are explicitly stated; TOC present.</done>
</task>

<task type="auto">
  <name>Task 2: Add flows 4-6 to docs/FLOWS.md — reservations, CRUD+audit-log, cron</name>
  <files>docs/FLOWS.md</files>
  <read_first>
    - docs/FLOWS.md (current state from Task 1)
    - .planning/phases/06-.../06-RESEARCH.md § "Per-Flow File Map" (flows 4, 5, 6) and § "Common Pitfalls" (Pitfall 1 cron table, Pitfall 2 reservation path)
    - Flow 4 (reservations): apps/strapi/src/api/ad-reservation/, apps/strapi/src/api/ad-featured-reservation/, apps/strapi/src/cron/ad-free-reservation-restore.cron.ts, apps/strapi/src/api/ad/services/ad.ts (publishAd slot consumption), docs/reservation-system.md (lead — has WRONG cron path, correct it)
    - Flow 5 (CRUD+audit-log): apps/strapi/src/subscribers/audit-log.subscriber.ts, apps/strapi/src/utils/audit-log/index.ts, apps/strapi/src/index.ts (bootstrap registration)
    - Flow 6 (cron): apps/strapi/config/cron-tasks.ts (SOURCE OF TRUTH — 6 active + 1 manual), apps/strapi/config/server.ts, apps/strapi/src/cron/*.cron.ts (6 files), apps/strapi/src/api/cron-runner/controllers/cron-runner.ts
  </read_first>
  <action>
    Append three flow sections to docs/FLOWS.md, each with a ```mermaid``` diagram:
    6. `## Flow 4: Reservation System` — ad reservations + featured reservations, gift endpoint (global::isManager), restore-on-reject/ban via ad-free-reservation-restore.cron.ts (state the correct path; note docs/reservation-system.md's path is stale per D-10). One ```mermaid``` diagram. Prose: happy path (reserve → consume slot on publish → restore on reject/ban), error states, role gate.
    7. `## Flow 5: CRUD + Audit Log` — the global `strapi.db.lifecycles.subscribe()` hook registered as the first bootstrap statement, the `{ actor, actor_type, data }` envelope (actor = numeric id or "system"; actor_type = admin::user | plugin::users-permissions.user | system), logAuditInfo/Warn/Error writing structured log lines via Winston (Better Stack + 90-day local rotation), NOT a DB table. One ```mermaid``` diagram (flowchart: any create/update/delete → subscriber → logAudit envelope → Winston). Describe the envelope here as the canonical reference; note flows 2/3 link back to it.
    8. `## Flow 6: Cron Jobs` — STATE 6 ACTIVE cron jobs + 1 manual-only, correcting the stale "4 cron jobs" claim (per D-10, note the correction). Include a table (Key | Schedule | Source file | Purpose) for: userCron (0 2 * * *, ad-free-reservation-restore.cron.ts), adCron (0 1 * * *, ad-expiry.cron.ts), cleanupCron (0 4 * * 0, media-cleanup.cron.ts), backupCron (0 3 * * *, bbdd-backup.cron.ts), verificationCodeCleanupCron (0 4 * * *, verification-code-cleanup.cron.ts), subscriptionChargeCron (0 5 * * *, subscription-charge.cron.ts, only when PRO_ENABLE=true), and the manual-only userConfirmedMigration (POST /api/cron-runner/:name). One ```mermaid``` diagram (flowchart of the scheduler + manual trigger path).
    9. `## Preguntas abiertas` (D-09) — genuine flow-verification unknowns (e.g. RESEARCH.md Open Question about analytics-events.md staleness if relevant), or "Ninguna identificada." if none.

    Update the Table of Contents to link flows 4-6 and Preguntas abiertas. Each new flow's diagram MUST be a fenced ```mermaid block. Re-derive from source; DO NOT repeat the stale 4-cron count or the wrong reservation cron path.
  </action>
  <verify>
    <automated>[ "$(grep -c '```mermaid' docs/FLOWS.md)" -ge 6 ] && grep -qi "Preguntas abiertas" docs/FLOWS.md && grep -q "verificationCodeCleanupCron\|subscriptionChargeCron" docs/FLOWS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '```mermaid' docs/FLOWS.md` returns ≥6 (D-07: one per flow)
    - `grep -q "Flow 4" docs/FLOWS.md`, `grep -q "Flow 5" docs/FLOWS.md`, `grep -q "Flow 6" docs/FLOWS.md` all exit 0
    - `grep -q "verificationCodeCleanupCron" docs/FLOWS.md` AND `grep -q "subscriptionChargeCron" docs/FLOWS.md` exit 0 (proves the 6-cron correction, not stale 4)
    - `grep -q "ad-free-reservation-restore.cron.ts" docs/FLOWS.md` exits 0 (correct reservation path)
    - `grep -q "actor_type\|actor, actor_type, data\|audit" docs/FLOWS.md` exits 0 (audit envelope described)
    - `grep -qi "Preguntas abiertas" docs/FLOWS.md` exits 0 (D-09)
  </acceptance_criteria>
  <done>docs/FLOWS.md has all 6 flows with ≥6 Mermaid diagrams; cron section lists 6 active + 1 manual with correct keys/paths; audit envelope described once; Preguntas abiertas present.</done>
</task>

</tasks>

<verification>
- `grep -c '```mermaid' docs/FLOWS.md` ≥ 6
- Cron correction present (`verificationCodeCleanupCron` + `subscriptionChargeCron` both grep-hit)
- Reservation cron path correct (`ad-free-reservation-restore.cron.ts`)
- Payment `documentId` identity stated
- `grep -qi "Preguntas abiertas" docs/FLOWS.md`
</verification>

<success_criteria>
docs/FLOWS.md documents all 6 core flows, each re-derived from live source with a Mermaid diagram and happy/error/role-gated prose; the stale 4-cron and wrong reservation-path claims are corrected; the audit envelope is described once and cross-linked; self-contained TOC and Preguntas abiertas present.
</success_criteria>

<output>
After completion, create `.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-02-SUMMARY.md`
</output>
