---
phase: 06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/BSD.md]
autonomous: true
requirements: [D-01, D-02, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "A reader can see every Strapi content-type (21 entities) and its relationships in one ER diagram"
    - "A reader can find the field list and key relations for each core entity"
    - "A reader can locate the API endpoint surface without re-reading source"
  artifacts:
    - path: "docs/BSD.md"
      provides: "Backend Schema Document: ER diagram, entity/field tables, endpoint reference"
      contains: "erDiagram"
      min_lines: 200
  key_links:
    - from: "docs/BSD.md"
      to: "apps/strapi/src/api/*/content-types/*/schema.json"
      via: "entity/field derivation"
      pattern: "erDiagram"
    - from: "docs/BSD.md"
      to: "docs/permissions.md"
      via: "cross-link for endpoint permissions (no duplication)"
      pattern: "permissions\\.md"
---

<objective>
Produce `docs/BSD.md` (Backend Schema Document) — the single source of truth for the Waldo backend data model: an ER diagram covering all 21 Strapi content-types, per-entity field/relation tables, and an API endpoint reference. Verified against live `schema.json` files, NOT copied from the stale `docs/data-model.md` (which lists only 16 entities and predates Phase 4).

Purpose: BSD.md is the schema truth that TRD.md, PRD.md, and IPD.md (Wave 2) cross-reference. It must be committed and accurate before Wave 2 runs.
Output: `docs/BSD.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-CONTEXT.md
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-RESEARCH.md

<entity_set>
<!-- Verified 2026-07-02 via filesystem enumeration. 20 api content-types + User (extension) = 21 total. -->
20 under apps/strapi/src/api/*/content-types/*/schema.json:
  ad, ad-featured-reservation, ad-pack, ad-reservation, article, category,
  commune, condition, contact, cookie-policy, faq, order, policy, region,
  remaining, security-policy, subscription-payment, subscription-pro, term,
  verification-code
1 under apps/strapi/src/extensions/*/content-types/*/schema.json:
  User (apps/strapi/src/extensions/users-permissions/content-types/user/schema.json)
  — the ER diagram's central entity; the api/* glob MISSES it. Include it explicitly.
</entity_set>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/BSD.md — ER diagram + entity/field tables (all 21 entities)</name>
  <files>docs/BSD.md</files>
  <read_first>
    - apps/strapi/src/api/*/content-types/*/schema.json (ALL 20 files — read each to extract attributes and relation targets/cardinality)
    - apps/strapi/src/extensions/users-permissions/content-types/user/schema.json (the 21st entity — User)
    - docs/data-model.md (existing lead — verify against schema.json, DO NOT copy; it omits cookie-policy, security-policy, remaining, contact, verification-code)
    - .planning/phases/06-.../06-RESEARCH.md § "BSD Entity Set" and § "Common Pitfalls" (Pitfall 3)
  </read_first>
  <action>
    Create docs/BSD.md. Structure:
    1. Title `# Backend Schema Document (BSD)` + one-paragraph intro stating this is as-built, verified against `apps/strapi/src/**/content-types/*/schema.json` on the current codebase.
    2. `## Table of Contents` (required per D-02) — link to each `##` section below.
    3. `## Entity-Relationship Diagram` — a single ```mermaid erDiagram``` block containing ALL 21 entities: ad, ad-featured-reservation, ad-pack, ad-reservation, article, category, commune, condition, contact, cookie-policy, faq, order, policy, region, remaining, security-policy, subscription-payment, subscription-pro, term, verification-code, AND User. Derive relationships from each schema.json's `attributes` where `type: "relation"` — map `oneToMany`/`manyToOne`/`oneToOne`/`manyToMany` to Mermaid cardinality (`||--o{`, `}o--||`, `||--||`, `}o--o{`). Read `target` on each relation to connect the right entities.
    4. `## Entity Reference` — one `###` subsection per entity, each with a Markdown field table (columns: Field | Type | Notes/Relation). Populate from the entity's schema.json `attributes`. For User, pull from the extensions path. Note inline (short, per D-10) any place `docs/data-model.md` was wrong or incomplete.
    5. `## Preguntas abiertas` (required per D-09) — list genuine schema unknowns found during verification, or state "Ninguna identificada." explicitly if none.

    Body text in English (D-12). Quote any Spanish enum/label values verbatim if present. Cross-link `docs/permissions.md` and `docs/data-model.md` where relevant instead of duplicating their content.
    DO NOT copy entity lists from docs/data-model.md — derive every entity and field from the live schema.json files.
  </action>
  <verify>
    <automated>test -f docs/BSD.md && grep -c "erDiagram" docs/BSD.md | grep -qE '^[1-9]' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/BSD.md` exits 0
    - `grep -c "erDiagram" docs/BSD.md` returns ≥1
    - `grep -c '### ' docs/BSD.md` returns ≥21 (one entity reference subsection per entity)
    - `grep -qi "Preguntas abiertas" docs/BSD.md` exits 0 (D-09)
    - `grep -q "Table of Contents\|Índice" docs/BSD.md` exits 0 (D-02)
    - Each of these 21 entity names appears in BSD.md: `for e in ad article category commune condition contact cookie-policy faq order policy region remaining security-policy subscription-payment subscription-pro term verification-code ad-pack ad-reservation ad-featured-reservation User; do grep -qi "$e" docs/BSD.md || echo "MISSING $e"; done` prints nothing
  </acceptance_criteria>
  <done>docs/BSD.md exists with a 21-entity erDiagram, per-entity field tables, TOC, and a Preguntas abiertas section; every entity name is present.</done>
</task>

<task type="auto">
  <name>Task 2: Add API endpoint reference to docs/BSD.md</name>
  <files>docs/BSD.md</files>
  <read_first>
    - docs/BSD.md (current state from Task 1)
    - docs/permissions.md (existing endpoint/permission lead — cross-link, verify, do not blindly copy per D-10)
    - apps/strapi/src/api/payment/routes/payment.ts (payment endpoints: /payments/free-ad, /payments/checkout, /payments/webpay, /payments/thankyou/:documentId, /payments/pro, /payments/pro-response, /payments/pro-cancel)
    - apps/strapi/src/api/ad/routes/ (ad custom routes incl. approve/reject/banned)
    - apps/strapi/src/api/cron-runner/routes/cron-runner.ts (manual cron trigger POST /api/cron-runner/:name)
  </read_first>
  <action>
    Append a `## API Endpoint Reference` section to docs/BSD.md. Provide a Markdown table (columns: Method | Path | Auth/Policy | Purpose) covering the non-CRUD custom endpoints verified from the routes files above — at minimum the 7 payment routes, the ad moderation routes (approve/reject/banned), and the cron-runner manual trigger. For standard CRUD content-type endpoints, summarize the convention and cross-link `docs/permissions.md` rather than enumerating every auto-generated route (avoid duplication per Claude's Discretion note in CONTEXT.md).
    Update the Table of Contents to include this new section.
    English body text (D-12).
  </action>
  <verify>
    <automated>grep -q "API Endpoint Reference" docs/BSD.md && grep -q "/payments/webpay" docs/BSD.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "API Endpoint Reference" docs/BSD.md` exits 0
    - `grep -q "/payments/webpay" docs/BSD.md` exits 0
    - `grep -q "permissions.md" docs/BSD.md` exits 0 (cross-link present, not duplicated)
    - `grep -q "cron-runner" docs/BSD.md` exits 0
  </acceptance_criteria>
  <done>docs/BSD.md has an API Endpoint Reference section with the verified custom endpoints and a cross-link to permissions.md; TOC updated.</done>
</task>

</tasks>

<verification>
- `ls docs/BSD.md` succeeds
- `grep -c "erDiagram" docs/BSD.md` ≥ 1
- All 21 entity names present (see Task 1 acceptance loop)
- `grep -qi "Preguntas abiertas" docs/BSD.md` succeeds
</verification>

<success_criteria>
docs/BSD.md is a complete, self-contained backend schema reference: 21-entity ER diagram derived from live schema.json, per-entity field tables, custom endpoint reference cross-linking permissions.md, its own TOC, and a Preguntas abiertas section — no facts copied unverified from stale docs/data-model.md.
</success_criteria>

<output>
After completion, create `.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-01-SUMMARY.md`
</output>
