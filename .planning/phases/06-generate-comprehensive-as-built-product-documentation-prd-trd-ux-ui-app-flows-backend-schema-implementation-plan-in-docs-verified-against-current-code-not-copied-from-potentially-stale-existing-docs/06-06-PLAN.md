---
phase: 06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs
plan: 06
type: execute
wave: 2
depends_on: []
files_modified: [docs/UXD.md]
autonomous: true
requirements: [D-01, D-02, D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "A reader can find the full page inventory split into public vs dashboard"
    - "A reader can find the component taxonomy and BEM/SCSS conventions"
    - "The brand palette is documented with exact hex values quoted from CLAUDE.md"
  artifacts:
    - path: "docs/UXD.md"
      provides: "UX/UI Design Document: page inventory, component taxonomy, BEM/SCSS conventions, brand palette"
      contains: "BEM"
      min_lines: 150
  key_links:
    - from: "docs/UXD.md"
      to: "apps/website/app/pages/"
      via: "page inventory (public vs dashboard split)"
      pattern: "dashboard"
    - from: "docs/UXD.md"
      to: "docs/TRD.md"
      via: "forward cross-link to architecture's public-vs-dashboard note (link only, TRD not read)"
      pattern: "TRD.md"
---

<objective>
Produce `docs/UXD.md` (UX/UI Design Document) — page inventory (public vs dashboard split), component taxonomy/naming conventions, BEM/SCSS block list, and the brand palette (exact hex values quoted from CLAUDE.md, NOT re-derived). Verified against the live `apps/website/app/pages/`, `components/`, and `scss/` trees.

Purpose: the design-system reference. It emits a FORWARD markdown cross-link to `docs/TRD.md`'s public-vs-dashboard architecture note (a link only — do NOT read or restate TRD's content; TRD may be written in parallel). Everything UXD consumes comes from CLAUDE.md, RESEARCH.md, and the live `apps/website/app/` trees — no hard dependency on other Wave-2 plans, so this runs in parallel with them.
Output: `docs/UXD.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-CONTEXT.md
@.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-RESEARCH.md

<page_inventory>
<!-- Verified via RESEARCH.md § UXD Source Orientation; confirm live at write time. -->
Public pages (apps/website/app/pages/ top-level): anunciar, anuncios, blog, contacto, cuenta,
  login, onboarding, packs, pagar, pro, registro + files index.vue, [slug].vue,
  politicas-de-cookies.vue, politicas-de-privacidad.vue, politicas-de-seguridad.vue,
  terminos-y-condiciones-de-uso.vue, preguntas-frecuentes.vue, recuperar-contrasena.vue,
  restablecer-contrasena.vue, sitemap.vue, dev.vue.
Dashboard pages (apps/website/app/pages/dashboard/, gated by dashboard-guard.global.ts):
  account, ads, articles, featured, integrations, maintenance, orders, reservations, users, index.vue.
Components: apps/website/app/components/ — ~225 flat PascalCase .vue files (only icons/ subdir),
  role-prefixed (Form*, Card*, Lightbox*, Menu*, Login*, ...).
SCSS: apps/website/app/scss/abstracts/_variables.scss (palette), app.scss (entry),
  components/*.scss (one file per BEM block).
</page_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/UXD.md — page inventory, component taxonomy, BEM/SCSS conventions, brand palette</name>
  <files>docs/UXD.md</files>
  <read_first>
    - CLAUDE.md (repo root — SOURCE OF TRUTH for brand palette hex table and BEM conventions; quote the palette verbatim, do NOT re-derive hex values per D-10/D-11)
    - apps/website/app/pages/ (verify the public page inventory at write time)
    - apps/website/app/pages/dashboard/ (verify the dashboard page inventory)
    - apps/website/app/components/ (confirm component naming taxonomy — Form*/Card*/Lightbox*/Menu*/Login* prefixes)
    - apps/website/app/scss/abstracts/_variables.scss (brand palette source — cross-check against CLAUDE.md table)
    - apps/website/app/scss/components/ (BEM block → SCSS file mapping)
    - .planning/phases/06-.../06-RESEARCH.md § "UXD.md Source Orientation"
    - NOTE: do NOT read docs/TRD.md — it may be written in parallel and is not needed here; only emit a forward markdown link to it.
  </read_first>
  <action>
    Create docs/UXD.md. Structure:
    1. Title `# UX/UI Design Document (UXD)` + intro (as-built, verified against apps/website/app/).
    2. `## Table of Contents` (D-02).
    3. `## Inventario de páginas` — two subsections: "Públicas" and "Dashboard (rol manager, gated by dashboard-guard.global.ts)", each a table of routes/pages verified from apps/website/app/pages/. Add a forward markdown link `[docs/TRD.md](./TRD.md)` pointing to TRD's architecture note for the public-vs-dashboard split — a link only; do NOT read TRD or restate the merge fact (TRD owns it).
    4. `## Taxonomía de componentes` — describe the flat components/ directory and the role-prefix naming convention (Form*, Card*, Lightbox*, Menu*, Login*, etc.) per CLAUDE.md; give representative examples verified from the directory.
    5. `## Convenciones BEM y SCSS` — document the BEM rules from CLAUDE.md (block = single semantic noun, block--modifier namespace, __element sub-levels, no standalone hyphenated compound classes, one SCSS file per block). List a sample of the BEM blocks with their SCSS file (from scss/components/).
    6. `## Paleta de marca` — reproduce the brand color token table VERBATIM from CLAUDE.md (token | hex | usage) — do NOT invent or approximate any hex value (per D-11 and the always-read-brand-colors rule). Include the MJML email CTA rule (#ffd699 background, #313338 text).
    7. `## Preguntas abiertas` (D-09) — genuine unknowns (e.g. RESEARCH.md notes UXD orientation was structural-only, not a deep per-component visual audit — flag any component-level design questions), or "Ninguna identificada."

    English body (D-12); quote Spanish UI labels/copy verbatim where referenced. Quote the palette hex values exactly from CLAUDE.md. Write the `docs/TRD.md` cross-link as a plain markdown link without reading TRD.
  </action>
  <verify>
    <automated>test -f docs/UXD.md && grep -q "#ffd699" docs/UXD.md && grep -qi "dashboard-guard\|Dashboard" docs/UXD.md && grep -q "BEM" docs/UXD.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/UXD.md` exits 0
    - `grep -q "#ffd699" docs/UXD.md` AND `grep -q "#313338" docs/UXD.md` exit 0 (palette quoted from CLAUDE.md, not re-derived)
    - `grep -q "BEM" docs/UXD.md` exits 0 (BEM conventions documented)
    - `grep -qi "Dashboard" docs/UXD.md` AND `grep -qi "Públicas\|Public" docs/UXD.md` exit 0 (public-vs-dashboard split)
    - `grep -q "TRD.md" docs/UXD.md` exits 0 (forward cross-link present)
    - `grep -qi "Preguntas abiertas" docs/UXD.md` exits 0 (D-09)
    - `grep -q "Table of Contents\|Índice" docs/UXD.md` exits 0 (D-02)
  </acceptance_criteria>
  <done>docs/UXD.md exists with public/dashboard page inventory, component taxonomy, BEM/SCSS conventions, the verbatim brand palette from CLAUDE.md, a forward cross-link to TRD.md, TOC, and Preguntas abiertas.</done>
</task>

</tasks>

<verification>
- `grep -q "#ffd699" docs/UXD.md` and `grep -q "#313338" docs/UXD.md`
- `grep -q "BEM" docs/UXD.md`
- `grep -qi "Dashboard" docs/UXD.md`
- `grep -q "TRD.md" docs/UXD.md`
- `grep -qi "Preguntas abiertas" docs/UXD.md`
</verification>

<success_criteria>
docs/UXD.md documents the public/dashboard page inventory, component taxonomy, BEM/SCSS conventions, and the exact brand palette quoted from CLAUDE.md; emits a forward cross-link to TRD.md without reading it; self-contained with TOC and Preguntas abiertas; runs in parallel with the other Wave-2 plans (no hard depends_on).
</success_criteria>

<output>
After completion, create `.planning/phases/06-generate-comprehensive-as-built-product-documentation-prd-trd-ux-ui-app-flows-backend-schema-implementation-plan-in-docs-verified-against-current-code-not-copied-from-potentially-stale-existing-docs/06-06-SUMMARY.md`
</output>
