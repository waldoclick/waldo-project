---
phase: 14-account-featured-reservations-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/pages/reservations/index.vue
  - apps/dashboard/app/pages/reservations/free.vue
  - apps/dashboard/app/pages/reservations/used.vue
  - apps/dashboard/app/pages/reservations/[id].vue
autonomous: true
requirements:
  - URL-10

must_haves:
  truths:
    - "Navigating to /reservations loads the reservations index (redirect to /reservations/free)"
    - "Navigating to /reservations/free loads the reservations free list"
    - "Navigating to /reservations/used loads the reservations used list"
    - "Navigating to /reservations/[id] loads the reservation detail page"
    - "No file in apps/dashboard/app/pages/reservations/ contains /reservas/, /libres, or /usadas route references"
  artifacts:
    - path: "apps/dashboard/app/pages/reservations/index.vue"
      provides: "Reservations index redirect at /reservations"
    - path: "apps/dashboard/app/pages/reservations/free.vue"
      provides: "Reservations free list at /reservations/free"
    - path: "apps/dashboard/app/pages/reservations/used.vue"
      provides: "Reservations used list at /reservations/used"
    - path: "apps/dashboard/app/pages/reservations/[id].vue"
      provides: "Reservation detail page at /reservations/[id]"
  key_links:
    - from: "apps/dashboard/app/pages/reservations/index.vue"
      to: "/reservations/free"
      via: "navigateTo() redirect in script setup"
      pattern: "navigateTo.*reservations/free"
    - from: "apps/dashboard/app/pages/reservations/free.vue"
      to: "/reservations"
      via: "breadcrumb to binding"
      pattern: "reservations/free"
    - from: "apps/dashboard/app/pages/reservations/[id].vue"
      to: "/reservations/free"
      via: "breadcrumb to binding"
      pattern: "reservations/free"
---

<objective>
Rename the `reservas` page directory to `reservations`. Translate Spanish filenames to English within the directory. Update every internal route reference within those files to use the new English paths.

Purpose: Fulfils URL-10 — the `/reservations` route tree becomes a canonical English path.
Output: New `apps/dashboard/app/pages/reservations/` directory with correctly named and wired `.vue` files. The old `reservas/` directory is deleted.
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-catalog-segments-migration/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename reservas directory and files</name>
  <files>
    apps/dashboard/app/pages/reservations/index.vue
    apps/dashboard/app/pages/reservations/free.vue
    apps/dashboard/app/pages/reservations/used.vue
    apps/dashboard/app/pages/reservations/[id].vue
  </files>
  <action>
    Use `git mv` to rename so Git tracks renames cleanly (same pattern used in Phase 12 and Phase 13):

    ```bash
    # reservas → reservations (directory rename)
    git mv apps/dashboard/app/pages/reservas apps/dashboard/app/pages/reservations

    # Rename individual Spanish filenames inside reservations/
    git mv apps/dashboard/app/pages/reservations/libres.vue apps/dashboard/app/pages/reservations/free.vue
    git mv apps/dashboard/app/pages/reservations/usadas.vue apps/dashboard/app/pages/reservations/used.vue
    # Note: index.vue and [id].vue already have English-compatible names, moved with the directory
    ```

    Expected final structure:
    ```
    apps/dashboard/app/pages/reservations/
      index.vue
      free.vue
      used.vue
      [id].vue
    ```

    Verify:
    ```bash
    ls apps/dashboard/app/pages/reservations/
    ```
    `apps/dashboard/app/pages/reservas/` should not exist.
  </action>
  <verify>
    <automated>ls apps/dashboard/app/pages/reservations/ && ! ls apps/dashboard/app/pages/reservas/ 2>/dev/null && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - reservations/ exists with index.vue, free.vue, used.vue, [id].vue
    - reservas/ directory is gone
    - Git tracks all changes as renames (not deletions + additions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update internal route references in reservations pages</name>
  <files>
    apps/dashboard/app/pages/reservations/index.vue
    apps/dashboard/app/pages/reservations/free.vue
    apps/dashboard/app/pages/reservations/used.vue
    apps/dashboard/app/pages/reservations/[id].vue
  </files>
  <action>
    Update every Spanish route string inside the renamed files to its English equivalent.

    **reservations/index.vue** — update navigateTo redirect:
    - `await navigateTo("/reservas/libres", { replace: true })` → `await navigateTo("/reservations/free", { replace: true })`

    **reservations/free.vue** — update breadcrumb:
    - `{ label: "Reservas", to: "/reservas/libres" }` → `{ label: "Reservations", to: "/reservations/free" }`

    **reservations/used.vue** — update breadcrumb:
    - `{ label: "Reservas", to: "/reservas/libres" }` → `{ label: "Reservations", to: "/reservations/free" }`

    **reservations/[id].vue** — update breadcrumb:
    - `{ label: "Reservas", to: "/reservas/libres" }` → `{ label: "Reservations", to: "/reservations/free" }`

    Do NOT change: Spanish UI labels in templates (e.g., `title="Libres"`), Strapi API collection names (`"ad-reservations"`), Spanish comments, or anything outside these route path strings.

    After edits, verify no Spanish route strings remain:
    ```bash
    grep -r "/reservas" apps/dashboard/app/pages/reservations/
    grep -r "libres\|usadas" apps/dashboard/app/pages/reservations/
    ```
    Expected: no output from either command.
  </action>
  <verify>
    <automated>grep -r "/reservas" apps/dashboard/app/pages/reservations/ && echo "FAIL" || echo "PASS reservations"</automated>
  </verify>
  <done>
    - reservations/index.vue redirects to /reservations/free (not /reservas/libres)
    - reservations/free.vue, reservations/used.vue, reservations/[id].vue breadcrumbs point to /reservations/free
    - reservations/ pages contain no /reservas/, /libres, or /usadas route strings
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls apps/dashboard/app/pages/reservations/` — shows index.vue, free.vue, used.vue, [id].vue
2. `ls apps/dashboard/app/pages/reservas/` — returns "No such file or directory"
3. `grep -r "/reservas" apps/dashboard/app/pages/reservations/` — no output
4. `grep -r "libres\|usadas" apps/dashboard/app/pages/reservations/` — no output
5. `git status` — shows renames tracked by Git (not deletions + additions)
</verification>

<success_criteria>
- `apps/dashboard/app/pages/reservations/` exists with index.vue, free.vue, used.vue, [id].vue
- `apps/dashboard/app/pages/reservas/` does not exist
- Zero Spanish route strings (/reservas/, /libres, /usadas) inside reservations/ pages
- Git history shows renames (not file deletions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-account-featured-reservations-migration/14-02-SUMMARY.md` using the summary template.
</output>
