---
phase: 102-oneclick-inscription
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - apps/website/app/types/user.d.ts
  - apps/website/app/components/MemoPro.vue
  - apps/website/app/pages/pro/gracias.vue
autonomous: false
requirements:
  - FRNT-01
  - FRNT-02

must_haves:
  truths:
    - "Clicking 'Hazte PRO' calls POST /payments/pro-inscription/start and redirects browser to urlWebpay?TBK_TOKEN=token"
    - "/pro/gracias page shows confirmation with card type and masked card number after successful inscription"
    - "User type includes pro_status, pro_card_type, pro_card_last4, pro_expires_at fields"
  artifacts:
    - path: "apps/website/app/types/user.d.ts"
      provides: "User interface with subscription fields"
      contains: "pro_status"
    - path: "apps/website/app/components/MemoPro.vue"
      provides: "PRO subscription button wired to Oneclick start endpoint"
    - path: "apps/website/app/pages/pro/gracias.vue"
      provides: "Inscription confirmation page with card info"
  key_links:
    - from: "apps/website/app/components/MemoPro.vue"
      to: "/payments/pro-inscription/start"
      via: "useApiClient POST call"
      pattern: "payments/pro-inscription/start"
    - from: "apps/website/app/pages/pro/gracias.vue"
      to: "useStrapiUser"
      via: "fetchUser refresh to get updated card fields"
      pattern: "fetchUser|useStrapiUser"
---

<objective>
Wire the frontend to the Oneclick inscription flow — rewire MemoPro.vue and create the confirmation page.

Purpose: Complete the end-to-end user experience: click "Hazte PRO" -> Transbank enrollment -> confirmation with card info.
Output: Rewired MemoPro.vue component, new /pro/gracias page, updated User type.
</objective>

<execution_context>
@/home/gabriel/.claude/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-oneclick-inscription/102-RESEARCH.md
@.planning/phases/102-oneclick-inscription/102-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

API endpoints created in Plan 01:
```
POST /payments/pro-inscription/start (authenticated)
  Response: { data: { token: string, urlWebpay: string } }

GET /payments/pro-inscription/finish (public — Transbank redirect)
  On success: redirects to ${FRONTEND_URL}/pro/gracias?inscribed=true
  On cancel: redirects to ${FRONTEND_URL}/pagar/error?reason=cancelled
  On reject: redirects to ${FRONTEND_URL}/pagar/error?reason=rejected
```

User fields added in Plan 01:
```typescript
// Added to user schema.json:
pro_status: "active" | "inactive" | "cancelled"  // enum, default "inactive"
pro_card_type: string    // e.g. "Visa", "Mastercard"
pro_card_last4: string   // e.g. "6623"
pro_expires_at: string   // ISO datetime
tbk_user: string         // private, not sent to frontend
```

From apps/website/app/components/MemoPro.vue (current):
```typescript
const apiClient = useApiClient();
// Currently calls: apiClient("payments/pro", { method: "POST", body: { data: {} } })
// Currently redirects: window.location.href = `${response.data.url}?token=${response.data.token}`
```

From apps/website/app/types/user.d.ts (current):
```typescript
export interface User {
  id: number;
  username: string;
  email: string;
  // ... existing fields ...
  pro: boolean;
  // Missing: pro_status, pro_card_type, pro_card_last4, pro_expires_at
}
```

From apps/website/app/pages/pagar/error.vue:
```typescript
// Already supports ?reason=cancelled and ?reason=rejected query params
// No changes needed to error page
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update User type and rewire MemoPro.vue to Oneclick</name>
  <files>
    apps/website/app/types/user.d.ts,
    apps/website/app/components/MemoPro.vue
  </files>
  <action>
    1. In `apps/website/app/types/user.d.ts`:
       - Add after the `pro: boolean` field:
         - `pro_status: "active" | "inactive" | "cancelled" | null;`
         - `pro_card_type: string | null;`
         - `pro_card_last4: string | null;`
         - `pro_expires_at: string | null;`
       - Do NOT add `tbk_user` — it is marked `private: true` in Strapi schema and is never sent to the frontend

    2. In `apps/website/app/components/MemoPro.vue`:
       - Change the API call from `apiClient("payments/pro", ...)` to `apiClient("payments/pro-inscription/start", { method: "POST", body: { data: {} } })`
       - Change the redirect from `${response.data.url}?token=${response.data.token}` to `${response.data.urlWebpay}?TBK_TOKEN=${response.data.token}`
       - The response shape changes: `url` becomes `urlWebpay`, the token query param name changes from `token` to `TBK_TOKEN`
       - Update the validation check from `response?.data?.url && response?.data?.token` to `response?.data?.urlWebpay && response?.data?.token`
       - Keep the Swal confirmation dialog and error handling unchanged
       - Keep `<script setup>` without `lang="ts"` (matching current file)
  </action>
  <verify>
    <automated>cd apps/website && npx nuxi typecheck 2>&1 | head -30</automated>
  </verify>
  <done>
    - User type includes pro_status, pro_card_type, pro_card_last4, pro_expires_at
    - MemoPro.vue calls /payments/pro-inscription/start and redirects to urlWebpay?TBK_TOKEN=token
    - No typecheck errors introduced
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /pro/gracias confirmation page</name>
  <files>
    apps/website/app/pages/pro/gracias.vue
  </files>
  <action>
    Create `apps/website/app/pages/pro/gracias.vue`:

    1. Page structure (follow existing page patterns like `/pagar/gracias.vue` or `/pagar/error.vue`):
       - Import and use: `HeaderDefault`, `HeroFake`, `FooterDefault` (same layout as `/pagar/error.vue`)
       - `<script setup lang="ts">`
       - `definePageMeta({ middleware: "auth" })` — user must be logged in to see confirmation
       - SEO: `$setSEO({ title: "Suscripcion PRO Confirmada", ... })`, `useSeoMeta({ robots: "noindex, nofollow" })`

    2. Data loading:
       - Read `inscribed` from `useRoute().query.inscribed`
       - If `inscribed !== "true"`, redirect to home with `navigateTo("/")`
       - Call `useStrapi().fetchUser()` to refresh the user data (server already updated the user before redirecting here)
       - Get user via `useStrapiUser<User>()` — this will now include `pro_card_type` and `pro_card_last4`

    3. Template:
       - Show a success message using `MessageDefault` component with `type="success"`
       - Title: `"Tu suscripcion PRO esta activa"`
       - Description: `"Tu tarjeta ha sido registrada exitosamente. Ya puedes disfrutar de todos los beneficios PRO."`
       - Below the message, show card info section:
         - Card type: `{{ user?.pro_card_type }}` (e.g., "Visa")
         - Masked number: `**** **** **** {{ user?.pro_card_last4 }}` (e.g., "**** **** **** 6623")
       - Add a "Volver al inicio" button linking to `/` (use `NuxtLink`)
       - Style using existing BEM patterns — use `page` class wrapper, similar structure to `/pagar/error.vue`

    4. Do NOT make additional API calls — the user data was already updated server-side by `proInscriptionFinish` before the redirect. The `fetchUser()` call just refreshes the cached Strapi user.

    5. Use brand colors from CLAUDE.md: `$light_peach` for CTA button background, `$charcoal` for text.
  </action>
  <verify>
    <automated>cd apps/website && npx nuxi typecheck 2>&1 | head -30</automated>
  </verify>
  <done>
    - /pro/gracias page exists and renders confirmation with card type and last 4 digits
    - Page is auth-protected and only accessible with ?inscribed=true
    - User data is refreshed via fetchUser() to show updated card fields
    - No typecheck errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Oneclick inscription flow end-to-end</name>
  <files>none</files>
  <action>
    Human verifies the complete inscription flow works in sandbox environment. No code changes — verification only.
  </action>
  <verify>
    <automated>echo "Manual verification required — see how-to-verify steps"</automated>
  </verify>
  <done>
    - Full round-trip confirmed: button click -> Transbank sandbox -> /pro/gracias with card info
    - Cancellation flow confirmed: cancel on Transbank -> /pagar/error?reason=cancelled
    - User pro_status is "active" in Strapi admin after successful enrollment
  </done>
  <what-built>Complete Oneclick inscription flow: MemoPro.vue rewired to start endpoint, /pro/gracias confirmation page with card info display</what-built>
  <how-to-verify>
    1. Start the dev server: `cd apps/website && yarn dev`
    2. Start Strapi: `cd apps/strapi && yarn develop`
    3. Log in as a non-PRO user
    4. Navigate to a page showing the MemoPro component (e.g., user profile or ad creation)
    5. Click "Hazte PRO" — confirm the Swal dialog
    6. Verify you are redirected to Transbank's sandbox enrollment page
    7. Complete the enrollment using Transbank sandbox test card credentials
    8. After enrollment, verify you land on /pro/gracias?inscribed=true
    9. Verify the page shows card type and masked card number
    10. Verify the user's pro_status is "active" in Strapi admin
    11. Test cancellation: repeat steps 4-6 but cancel on Transbank's page — verify you land on /pagar/error?reason=cancelled
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/website && npx nuxi typecheck` — no errors
- User type has subscription fields
- MemoPro.vue calls correct endpoint with correct redirect URL
- /pro/gracias page renders card info from refreshed user
- Full flow works: button -> Transbank -> confirmation page
</verification>

<success_criteria>
- MemoPro.vue redirects to Transbank via /payments/pro-inscription/start
- /pro/gracias shows card type and last 4 digits after successful enrollment
- Cancelled/rejected inscriptions land on /pagar/error with appropriate reason
- Human verification confirms full round-trip works in sandbox
</success_criteria>

<output>
After completion, create `.planning/phases/102-oneclick-inscription/102-02-SUMMARY.md`
</output>
