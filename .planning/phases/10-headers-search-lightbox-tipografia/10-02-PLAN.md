---
phase: 10-headers-search-lightbox-tipografia
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/website/app/scss/components/_lightbox.scss
  - apps/website/app/components/LightboxSearch.vue
autonomous: false
requirements:
  - SEARCH-LIGHTBOX

must_haves:
  truths:
    - "El lightbox de búsqueda se abre al hacer clic en el ícono de búsqueda"
    - "El overlay tiene fondo rgba(38,37,43,.46) con backdrop-filter blur(4px)"
    - "La card tiene max-width:620px, border-radius:10px y sombra 0 28px 64px rgba(38,37,43,.30)"
    - "El input tiene font-size:18px y color $ink"
    - "La sección 'Últimas búsquedas' se muestra cuando hay búsquedas recientes"
    - "La sección 'Explora por categoría' se muestra siempre en estado vacío"
    - "Las filas tienen hover con bg:$cream y border-radius:5px"
    - "El botón Esc cierra el lightbox"
    - "El lightbox no conflictúa con el header fixed (z-index 1000 > z-index 50)"
  artifacts:
    - path: "apps/website/app/scss/components/_lightbox.scss"
      provides: "Estilos del lightbox--search alineados con design/index.dc.html líneas 192-252"
      contains: "lightbox--search"
    - path: "apps/website/app/components/LightboxSearch.vue"
      provides: "Componente funcional de búsqueda con recents y categorías"
      contains: "lightbox--search"
  key_links:
    - from: "LightboxSearch.vue"
      to: "appStore.isSearchLightboxActive"
      via: "computed isOpen"
      pattern: "isSearchLightboxActive"
    - from: "LightboxSearch.vue"
      to: "useSearchSuggestions composable"
      via: "recents, loadRecents, clearRecents"
      pattern: "useSearchSuggestions"
---

<objective>
Verificar y corregir el LightboxSearch contra `apps/design/index.dc.html` líneas 192-252. El componente está ~95% correcto — el work es verificación visual con correcciones menores si las hay.

Purpose: El lightbox de búsqueda ya tiene estructura, lógica y la mayoría de estilos correctos. Esta plan cierra los gaps visuales restantes: iconos con colores hardcodeados, cualquier discrepancia de padding/sizing, y verifica que el z-index no conflictúa con el header fixed del plan 10-01.

Output: LightboxSearch visualmente alineado con la maqueta. Sin cambios funcionales — toda la lógica (recents, categorías, sugerencias) ya funciona.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-headers-search-lightbox-tipografia/10-RESEARCH.md
@.planning/phases/10-headers-search-lightbox-tipografia/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar _lightbox.scss contra diseño y corregir gaps menores</name>
  <files>apps/website/app/scss/components/_lightbox.scss</files>
  <read_first>
    - apps/website/app/scss/components/_lightbox.scss (leer la sección `&--search` completa — líneas ~423-671)
    - apps/website/app/scss/abstracts/_variables.scss (confirmar $muted, $cream, $line, $ink, $tag, $ink2)
    - apps/design/index.dc.html (líneas 192-252 — spec del lightbox)
  </read_first>
  <action>
    Leer el bloque `&--search` en `_lightbox.scss` (líneas ~423-671) y comparar contra los valores del diseño. La mayoría ya está correcto — aplicar solo estas correcciones concretas:

    **Gap 1 — `&__row__lead` color hardcodeado:**
    ANTES (línea ~647):
    ```scss
    &__lead {
      flex: none;
      color: #9a96a0;
    ```
    DESPUÉS:
    ```scss
    &__lead {
      flex: none;
      color: $muted;
    ```
    Razón: `#9a96a0` es equivalente a `$muted` (#8a8794), usar la variable es lo correcto per CLAUDE.md.

    **Gap 2 — `&__row__trail` color hardcodeado:**
    ANTES (línea ~657):
    ```scss
    &__trail {
      flex: none;
      color: #c2bec7;
    ```
    DESPUÉS:
    ```scss
    &__trail {
      flex: none;
      color: $muted;
    ```
    Razón: `#c2bec7` es una variante más clara de $muted. Unificar al token.

    **Verificar (NO cambiar si ya está correcto):**
    - Overlay: `position: fixed; inset: 0; z-index: 1000` — correcto (1000 > header z-index 50)
    - Overlay padding: `92px 24px 32px` — ya correcto
    - Overlay bg: en `&__backdrop` debe ser `rgba(38, 37, 43, 0.46)` — ya correcto
    - Card: `max-width: 620px; border-radius: 10px; box-shadow: 0 28px 64px rgba(38, 37, 43, 0.3)` — ya correcto
    - Input: `font-size: 18px; color: $ink` — ya correcto
    - Esc button: `font-size: 12px; font-weight: 600; color: $muted; border: 1px solid $line; bg: #fff; padding: 5px 9px; border-radius: 4px` — ya correcto
    - "Últimas búsquedas" label: `font-size: 11px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: $muted` — ya correcto
    - "Borrar" button: `font-size: 12.5px; font-weight: 600; color: $muted` — ya correcto
    - Row hover: `background: $cream; border-radius: 5px` — ya correcto
    - `&__row__name`: `font-size: 14.5px; font-weight: 600; color: $ink` — ya correcto
    - `&__row__term`: `font-size: 14.5px; font-weight: 500; color: $ink` — ya correcto

    Si al leer el archivo se encuentran discrepancias adicionales con los valores del diseño (index.dc.html líneas 192-252), corregirlas también. Si NO hay discrepancias adicionales, hacer SOLO los dos cambios de color hardcodeado descritos arriba.

    NO cambiar: lógica del componente, clases BEM, estructura HTML en LightboxSearch.vue, valores que ya coinciden con el diseño.
  </action>
  <verify>
    <automated>grep -n "#9a96a0\|#c2bec7\|#9A96A0\|#C2BEC7" /home/gab/Code/waldo-project/apps/website/app/scss/components/_lightbox.scss</automated>
    Debe retornar 0 resultados (los hardcodes fueron reemplazados por $muted).
    
    También verificar:
    <automated>grep -n "z-index: 1000\|max-width: 620px\|border-radius: 10px\|font-size: 18px" /home/gab/Code/waldo-project/apps/website/app/scss/components/_lightbox.scss</automated>
    Debe retornar esas 4 propiedades con sus valores correctos.
  </verify>
  <done>Los colores hardcodeados en `__lead` y `__trail` reemplazados por $muted. Todos los valores del lightbox--search coinciden con index.dc.html líneas 192-252.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - _lightbox.scss con colores hardcodeados reemplazados por tokens SCSS
    - Verificación de todos los valores visuales del lightbox contra la maqueta
  </what-built>
  <how-to-verify>
    Con el servidor corriendo (`pnpm --filter website dev`), verificar en `/anuncios` o `/`:

    1. **Abrir el lightbox** — hacer clic en el ícono de lupa en el header:
       - El overlay debe aparecer con fondo oscuro semitransparente con blur
       - La card debe estar centrada, con bordes redondeados y sombra visible

    2. **Estado vacío (sin query)**:
       - Si hay búsquedas previas: sección "ÚLTIMAS BÚSQUEDAS" con ícono de reloj + términos + flecha
       - Sección "EXPLORA POR CATEGORÍA" con dots de color + nombres + conteos
       - Hover sobre cualquier fila: fondo $cream suave

    3. **Estado con query** — escribir algo en el input:
       - Sugerencias de anuncios deben aparecer
       - La fila "Buscar '[query]'" debe aparecer al final con ícono ámbar

    4. **Botón Esc** — hacer clic en "Esc" o presionar la tecla Escape:
       - El lightbox debe cerrarse

    5. **Z-index no conflicto** — el lightbox debe aparecer por encima del header fixed (z-index 50)

    6. **Recents** — buscar algo, cerrar, reabrir:
       - La búsqueda reciente debe aparecer en "ÚLTIMAS BÚSQUEDAS"
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si el lightbox se ve correcto y funciona, o describe qué problemas encontraste.</resume-signal>
</task>

</tasks>

<verification>
- `grep "#9a96a0\|#c2bec7" apps/website/app/scss/components/_lightbox.scss` retorna 0 resultados
- `grep "z-index: 1000" apps/website/app/scss/components/_lightbox.scss` retorna 1 resultado
- `grep "max-width: 620px" apps/website/app/scss/components/_lightbox.scss` retorna 1 resultado
- `pnpm --filter website typecheck` sin errores nuevos
</verification>

<success_criteria>
1. Los colores hardcodeados `#9a96a0` y `#c2bec7` reemplazados por `$muted`
2. El lightbox visual coincide con index.dc.html líneas 192-252
3. Las secciones "Últimas búsquedas" y "Explora por categoría" renderizan correctamente
4. El z-index del lightbox (1000) no conflictúa con el header fixed (50)
5. El botón Esc y la tecla Escape cierran el lightbox
</success_criteria>

<output>
Después de completar, crear `.planning/phases/10-headers-search-lightbox-tipografia/10-02-SUMMARY.md`
</output>
