---
phase: 10-headers-search-lightbox-tipografia
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/website/app/scss/components/_layout.scss
  - apps/website/app/scss/components/_toolbar.scss
  - apps/website/app/components/HeaderDashboard.vue
  - apps/website/app/scss/components/_hero.scss
autonomous: false
requirements:
  - HDR-ACCOUNT
  - HDR-DASHBOARD
  - TYPOGRAPHY

must_haves:
  truths:
    - "El header en /cuenta tiene height:70px (no 74px) y position:sticky (no fixed)"
    - "El contenido de /cuenta no se superpone con el header al cargar la página"
    - "El header en /cuenta tiene backdrop-filter:blur(14px) saturate(1.06)"
    - "El logo en /cuenta tiene height:28px (no 30px)"
    - "El topbar de /dashboard muestra botones de 38×38px con border-radius:8px y border:1px solid $line"
    - "El título de HeroHeaderDashboard usa font-size:20px, Poppins 700, color $ink, letter-spacing:-0.3px"
    - "Los colores hardcodeados #1a1a1a y #6b7280 en _hero.scss reemplazados por $ink y $muted"
  artifacts:
    - path: "apps/website/app/scss/components/_layout.scss"
      provides: "Override CSS de header en contexto .layout--account con position:sticky, height:70px, blur:14px"
      contains: ".layout--account .header--default"
    - path: "apps/website/app/scss/components/_hero.scss"
      provides: "hero--dashboard__title con color $ink y tamaños correctos; hero--dashboard__subtitle con color $muted"
      contains: "hero--dashboard__title"
    - path: "apps/website/app/scss/components/_toolbar.scss"
      provides: "toolbar--default__trigger ya tiene 38×38px — verificado"
      contains: "38px"
  key_links:
    - from: "apps/website/app/layouts/account.vue"
      to: ".header--default en .layout--account"
      via: "CSS scoping .layout--account .header--default { position: sticky }"
      pattern: "layout--account .header--default"
    - from: "apps/website/app/components/HeroHeaderDashboard.vue"
      to: "_hero.scss .hero--dashboard__title"
      via: "class hero--dashboard__title en <style scoped> + _hero.scss"
      pattern: "hero--dashboard__title"
---

<objective>
Corregir el header del área de cuenta, los estilos del topbar del dashboard y la tipografía del HeroHeaderDashboard. Este plan DEBE ejecutarse después de 10-01 porque el Wave 1 cambió `.header--default` a `position:fixed`, y el account layout necesita restaurar `position:sticky`.

Purpose: El account layout usa `<HeaderDefault>` y hereda el `position:fixed` del Wave 1 — sin override, el contenido de /cuenta quedaría solapado. Los botones del dashboard y la tipografía del hero dashboard tienen gaps de color/tamaño contra las maquetas.

Output: Header de cuenta sticky con 70px/blur-14px/logo-28px. Topbar dashboard con buttons verificados 38×38. HeroHeaderDashboard con tipografía Poppins 700 20px $ink y sin colores hardcodeados.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-headers-search-lightbox-tipografia/10-RESEARCH.md
@.planning/phases/10-headers-search-lightbox-tipografia/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Override CSS del header en layout account — position:sticky + height:70px + blur:14px + logo:28px</name>
  <files>apps/website/app/scss/components/_layout.scss</files>
  <read_first>
    - apps/website/app/scss/components/_layout.scss (leer la sección `&--account` — líneas ~127-176)
    - apps/website/app/scss/components/_header.scss (confirmar que _header.scss ahora tiene position:fixed tras el Wave 1)
    - apps/website/app/scss/abstracts/_variables.scss (confirmar $line, $amber, $amber_hover, $ink)
    - apps/design/account.dc.html (líneas 58-67 — spec del header de cuenta)
  </read_first>
  <action>
    En `apps/website/app/scss/components/_layout.scss`, dentro del bloque `&--account` (línea ~130), agregar un bloque de override CSS para el header al FINAL del bloque `&--account`, antes del cierre `}`:

    El override debe cubrir `.layout--account .header--default { ... }` con los valores de account.dc.html líneas 58-67:

    ```scss
    // Account header overrides (account.dc.html:58-67)
    // REQUIRED: restores position:sticky after Wave 1 changed .header--default to fixed.
    // Without this, account content overlaps the header on load.
    .header--default {
      position: sticky;
      height: 70px;
      backdrop-filter: blur(14px) saturate(1.06);
      -webkit-backdrop-filter: blur(14px) saturate(1.06);

      .header--default__inner {
        height: 70px;
      }

      .header--default__logo img {
        height: 28px;
      }

      .btn--announcement {
        font-size: 14.5px;
        padding: 11px 18px;
      }
    }
    ```

    COLOCACIÓN: Este bloque va DENTRO del `&--account { ... }` existente, al final de ese bloque, antes de la llave de cierre `}`. La clase resultante en CSS compilado será `.layout--account .header--default { ... }`.

    NOTA: En SCSS dentro de un bloque padre `&--account`, para referencia a clases hijas (no elementos BEM) que están en el DOM pero no son parte del bloque BEM `layout--account`, usar selectores de clase directamente sin `&`. El SCSS `&--account { .header--default { ... } }` compila a `.layout--account .header--default { ... }`.

    VERIFICAR que el sidebar en _layout.scss tiene `top: 70px` (ya está — confirmar sin cambiar):
    `&__sidebar { top: 70px; ... }` — línea ~152. Ya está correcto, no cambiar.

    NO agregar padding-top al `.layout--account` — la solución correcta es restaurar sticky.
  </action>
  <verify>
    <automated>grep -n "position: sticky\|height: 70px\|blur(14px)\|height: 28px" /home/gab/Code/waldo-project/apps/website/app/scss/components/_layout.scss</automated>
    Debe mostrar las 4 propiedades dentro del bloque de override.

    <automated>grep -n "header--default" /home/gab/Code/waldo-project/apps/website/app/scss/components/_layout.scss</automated>
    Debe mostrar el bloque override con `.header--default` dentro de `&--account`.
  </verify>
  <done>_layout.scss tiene el bloque `.layout--account .header--default` con position:sticky, height:70px, backdrop-filter:blur(14px) saturate(1.06), logo height:28px, y CTA padding/font ajustados. El contenido de /cuenta no se superpone con el header.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar y documentar toolbar--default (38×38 ya correcto) — corregir HeroHeaderDashboard tipografía</name>
  <files>
    apps/website/app/scss/components/_toolbar.scss,
    apps/website/app/components/HeaderDashboard.vue,
    apps/website/app/scss/components/_hero.scss
  </files>
  <read_first>
    - apps/website/app/scss/components/_toolbar.scss (confirmar que ya tiene 38×38 — no cambiar si está correcto)
    - apps/website/app/components/HeaderDashboard.vue (leer completo — ver estructura actual)
    - apps/website/app/components/HeroHeaderDashboard.vue (leer completo — ver el `&lt;style scoped&gt;`)
    - apps/website/app/scss/components/_hero.scss (leer sección `&--dashboard` — líneas 780-864)
    - apps/website/app/scss/abstracts/_variables.scss (confirmar $ink, $muted, $line, $cream, $ink2)
    - apps/design/dashboard.dc.html (líneas 69-134 — spec del topbar)
  </read_first>
  <action>
    **Parte A: Verificar toolbar--default (NO cambiar si ya está correcto)**

    Leer `_toolbar.scss`. El estado actual del `.toolbar--default__trigger` es:
    ```scss
    width: 38px;
    height: 38px;
    color: $ink2;
    background: $white;
    border: 1px solid $line;
    border-radius: 8px;
    ```
    Esto ya coincide con dashboard.dc.html líneas 69-134. Si está correcto, NO cambiar nada en `_toolbar.scss`.

    Si por alguna razón los valores difieren, corregir a: width:38px, height:38px, border-radius:8px, border:1px solid $line, bg:$white, color:$ink2, hover: bg:$cream color:$ink.

    **Parte B: HeaderDashboard.vue — verificar y conservar**

    Leer `HeaderDashboard.vue`. El componente usa `<ToolbarDefault />` que ya tiene los estilos correctos. El `<MenuUser />` comparte estilos con el header público — NO crear overrides que afecten el MenuUser en el header público.

    La decisión de arquitectura del research (mantener HeroHeaderDashboard como sección separada, no moverlo al topbar) se MANTIENE. Solo verificar que HeaderDashboard.vue no tiene código que requiera actualización.

    Si el `padding` del `.header--dashboard` en `_header.scss` dice `padding: 0 36px` pero el diseño pide `padding: 12px 36px`, corregir en `_header.scss` el bloque `&--dashboard` únicamente esa propiedad. NO modificar `height: 64px` — el padding vertical reemplaza el height fijo.

    **Parte C: _hero.scss — corregir tipografía hero--dashboard**

    En `apps/website/app/scss/components/_hero.scss`, sección `&--dashboard` (líneas 780-864):

    1. `.hero--dashboard__title` (línea ~824-838):

    ANTES:
    ```scss
    &__title {
      font-size: 32px;
      font-weight: 700;
      color: #1a1a1a;
      margin: 0;
      line-height: 1.2;

      @include screen-medium {
        font-size: 26px;
      }

      @include screen-small {
        font-size: 22px;
      }
    }
    ```

    DESPUÉS:
    ```scss
    &__title {
      font-family: 'Poppins', sans-serif;
      font-size: 20px;
      font-weight: 700;
      color: $ink;
      letter-spacing: -0.3px;
      margin: 0;
      line-height: 1.2;

      @include screen-medium {
        font-size: 18px;
      }

      @include screen-small {
        font-size: 16px;
      }
    }
    ```

    Razón: El diseño (dashboard.dc.html líneas 69-134) usa Poppins 700 20px $ink letter-spacing:-0.3px. El actual #1a1a1a es hardcodeado pre-redesign; $ink es #26252B. El tamaño de 32px es para el hero como sección standalone — el diseño usa 20px dentro del topbar (la sección ya existe debajo del topbar).

    2. `.hero--dashboard__subtitle` (líneas 853-858):

    ANTES:
    ```scss
    &__subtitle {
      font-size: 16px;
      color: #6b7280;
      margin: 0;
      line-height: 1.5;
    }
    ```

    DESPUÉS:
    ```scss
    &__subtitle {
      font-size: 16px;
      color: $muted;
      margin: 0;
      line-height: 1.5;
    }
    ```

    Razón: `#6b7280` es hardcodeado pre-redesign; `$muted` es #8a8794.

    3. `.hero--dashboard` border-bottom (línea ~783):

    ANTES:
    ```scss
    border-bottom: 1px solid #e5e5e5;
    ```

    DESPUÉS:
    ```scss
    border-bottom: 1px solid $line;
    ```

    Razón: `#e5e5e5` ≈ $line (#ece9e4). Usar el token.

    TAMBIÉN verificar en `HeroHeaderDashboard.vue` el bloque `&lt;style scoped&gt;`:
    Las clases `.hero--dashboard__title .title` y `.hero--dashboard__title-icon` son estilos de Vue scoped para el slot icon. Si contienen colores hardcodeados, corregirlos también a tokens SCSS. El `font-family`, `font-size`, `font-weight`, `color` y `letter-spacing` del título se controlan desde `_hero.scss`, no desde el scoped. El scoped solo controla la disposición del slot (flex, gap) — no cambiar eso.
  </action>
  <verify>
    <automated>grep -n "#1a1a1a\|#6b7280\|#e5e5e5\|32px\|26px\|22px" /home/gab/Code/waldo-project/apps/website/app/scss/components/_hero.scss</automated>
    Debe retornar 0 resultados dentro de la sección `&--dashboard` (estos hardcodes deben haber sido reemplazados).

    <automated>grep -n "font-size: 20px\|font-family: 'Poppins'\|letter-spacing: -0.3px\|color: \$ink\|color: \$muted" /home/gab/Code/waldo-project/apps/website/app/scss/components/_hero.scss</automated>
    Debe mostrar las propiedades correctas en el bloque `&--dashboard`.

    <automated>grep -n "width: 38px\|height: 38px\|border-radius: 8px" /home/gab/Code/waldo-project/apps/website/app/scss/components/_toolbar.scss</automated>
    Debe confirmar que los valores ya están presentes (38px, border-radius:8px).
  </verify>
  <done>_hero.scss tiene hero--dashboard__title con font-family Poppins, font-size:20px, color:$ink, letter-spacing:-0.3px. hero--dashboard__subtitle con color:$muted. border-bottom con $line. _toolbar.scss confirmado con 38×38 y border-radius:8px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - _layout.scss con override del header de cuenta: position:sticky, height:70px, blur:14px, logo:28px (Task 1)
    - _hero.scss con tipografía correcta: Poppins 700 20px $ink letter-spacing:-0.3px (Task 2)
    - _toolbar.scss confirmado con buttons 38×38 (Task 2)
    - Colores hardcodeados #1a1a1a, #6b7280, #e5e5e5 reemplazados por tokens en _hero.scss
  </what-built>
  <how-to-verify>
    Con el servidor corriendo (`pnpm --filter website dev`), verificar:

    **Header de cuenta (/cuenta):**
    1. Visitar `/cuenta` (requiere estar logueado — inyectar cookie `waldo_jwt` si es necesario)
    2. El header debe tener altura visiblemente menor que el header público (~70px vs 74px)
    3. El contenido de la página NO debe solaparse con el header al cargar
    4. Hacer scroll: el header debe pegarse al tope (sticky), no fijo como en el header público
    5. El logo debe verse ligeramente más pequeño que en el header público
    6. El botón "Anunciar ahora" debe verse con padding ligeramente menor

    **Dashboard header topbar (/dashboard):**
    7. Visitar `/dashboard` (requiere login con rol manager)
    8. Los botones del topbar (apps, ventas, pendientes, artículos, fullscreen) deben ser 38×38px con bordes visibles y border-radius:8px
    9. Hover sobre los botones: fondo $cream (beige claro)

    **HeroHeaderDashboard tipografía:**
    10. En cualquier página del dashboard, el título de la sección hero (debajo del topbar) debe verse con:
        - Fuente Poppins más "compacta" (letter-spacing negativo)
        - Tamaño visiblemente menor que antes (20px vs 32px anterior)
        - Color muy oscuro ($ink = #26252B), no negro puro
    11. El subtítulo (si aparece) debe verse en gris medio ($muted = #8a8794)
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si los tres contextos (cuenta, dashboard topbar, dashboard hero) se ven correctamente, o describe qué problemas encontraste.</resume-signal>
</task>

</tasks>

<verification>
- `grep "position: sticky" apps/website/app/scss/components/_layout.scss` retorna resultado dentro del bloque `.layout--account .header--default`
- `grep "height: 70px" apps/website/app/scss/components/_layout.scss` retorna 2 resultados (en `__inner` override y en el bloque padre)
- `grep "blur(14px)" apps/website/app/scss/components/_layout.scss` retorna 1 resultado
- `grep "#1a1a1a\|#6b7280\|#e5e5e5" apps/website/app/scss/components/_hero.scss` retorna 0 resultados en sección `&--dashboard`
- `grep "font-size: 20px\|letter-spacing: -0.3px" apps/website/app/scss/components/_hero.scss` retorna resultados en `&--dashboard`
- `grep "width: 38px\|height: 38px" apps/website/app/scss/components/_toolbar.scss` retorna resultados correctos
- `pnpm --filter website typecheck` sin errores nuevos
</verification>

<success_criteria>
1. El header en /cuenta es sticky (no fixed), con height:70px y blur:14px
2. El contenido de /cuenta no se solapa con el header en ningún momento
3. El logo en /cuenta mide 28px de alto
4. Los botones del topbar del dashboard son 38×38px con border:1px solid $line y border-radius:8px
5. El título del HeroHeaderDashboard usa Poppins 700 20px $ink con letter-spacing:-0.3px
6. Los colores hardcodeados (#1a1a1a, #6b7280, #e5e5e5) reemplazados por tokens ($ink, $muted, $line) en _hero.scss
7. Sin variables SCSS nuevas creadas — solo uso de variables existentes
</success_criteria>

<output>
Después de completar, crear `.planning/phases/10-headers-search-lightbox-tipografia/10-03-SUMMARY.md`
</output>
