---
phase: 076-dashboard-gift-lightbox
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/components/LightboxGift.vue
  - apps/dashboard/app/scss/components/_lightbox.scss
autonomous: true
requirements: [GIFT-03, GIFT-04, GIFT-05]

must_haves:
  truths:
    - "LightboxGift.vue renders with isOpen prop (controlled pattern)"
    - "Lightbox contains a numeric quantity input with min=1"
    - "Lightbox contains a searchable select showing only Authenticated users (firstName + lastName)"
    - "A Swal confirmation dialog fires before submitting the gift"
    - "On confirmation the appropriate gift endpoint is called and lightbox closes on success"
    - "Emits 'close' and 'gifted' events"
  artifacts:
    - path: "apps/dashboard/app/components/LightboxGift.vue"
      provides: "Gift lightbox component — quantity input + user select + Swal confirm + API call"
      exports: ["isOpen prop", "endpoint prop", "close emit", "gifted emit"]
    - path: "apps/dashboard/app/scss/components/_lightbox.scss"
      provides: "lightbox--gift SCSS modifier"
      contains: "lightbox--gift"
  key_links:
    - from: "LightboxGift.vue"
      to: "GET /api/users/authenticated"
      via: "useStrapiClient() on open"
      pattern: "users/authenticated"
    - from: "LightboxGift.vue"
      to: "POST {endpoint}/gift"
      via: "useStrapiClient() on confirm"
      pattern: "gift"
---

<objective>
Create the LightboxGift.vue component with quantity input, searchable user select, Swal confirmation, and API call — plus the SCSS modifier `lightbox--gift` in the existing `_lightbox.scss` file.

Purpose: Reusable gift lightbox consumed by both the ad-reservation and ad-featured-reservation detail pages in Phase 076 plan 02.
Output: LightboxGift.vue + lightbox--gift SCSS modifier (appended to _lightbox.scss).
</objective>

<execution_context>
@/home/gabriel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/gabriel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/075-strapi-gift-endpoints/075-01-SUMMARY.md
@.planning/phases/075-strapi-gift-endpoints/075-02-SUMMARY.md

<interfaces>
<!-- Key patterns from LightBoxArticles.vue (most recent lightbox — use as structural reference) -->

Props pattern (controlled):
```typescript
const props = withDefaults(defineProps<{ isOpen: boolean }>(), {})
const emit = defineEmits<{ (event: 'close' | 'gifted'): void }>()
```

Body scroll lock pattern:
```typescript
watch(() => props.isOpen, (isOpen) => {
  if (isOpen) {
    document.body.style.overflow = 'hidden'
    // reset local state
  } else {
    document.body.style.overflow = ''
  }
})
```

Swal usage:
```typescript
const { Swal } = useSweetAlert2()
const { isConfirmed } = await Swal.fire({ title, text, icon, showCancelButton: true, confirmButtonText, cancelButtonText })
```

useStrapiClient for non-Strapi-SDK calls:
```typescript
const client = useStrapiClient()
// GET
const result = await client<{ data: IUser[] }>('/users/authenticated')
// POST
await client(`/ad-reservations/gift`, { method: 'POST', body: { userId, quantity } })
```

GET /api/users/authenticated response shape (from 075-01):
```typescript
{ data: [{ id: number, firstName: string, lastName: string }] }
```

POST /api/ad-reservations/gift body:
```typescript
{ userId: number, quantity: number }
```

POST /api/ad-featured-reservations/gift body:
```typescript
{ userId: number, quantity: number }
```

LightBoxArticles BEM structure (reference):
- Root: class="lightbox lightbox--articles" with :class="{ 'is-open': isOpen }"
- Backdrop: class="lightbox--articles__backdrop" @click="handleClose"
- Box: class="lightbox--articles__box" role="dialog" aria-modal="true"
- Close button: class="lightbox__button" (uses base .lightbox__button already in _lightbox.scss)
- Header: class="lightbox--articles__header" > __title + __subtitle
- Actions: class="lightbox--articles__actions"

SCSS pattern for new modifier in _lightbox.scss (append at end, after &--articles block):
Uses same fixed-fullscreen + backdrop + box + animate pattern as --razon and --articles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LightboxGift.vue component</name>
  <files>apps/dashboard/app/components/LightboxGift.vue</files>
  <action>
Create `apps/dashboard/app/components/LightboxGift.vue` — a controlled gift lightbox.

**Props:**
- `isOpen: boolean` — controls visibility
- `endpoint: string` — Strapi path segment, e.g. `'ad-reservations'` or `'ad-featured-reservations'` (used to call `/${endpoint}/gift`)
- `label: string` — human label used in the Swal confirmation text, e.g. `'reservas'` or `'reservas destacadas'`

**Emits:** `'close'` | `'gifted'`

**State:**
- `users: ref<{ id: number; firstName: string; lastName: string }[]>([])` — loaded from GET /users/authenticated on open
- `usersLoading: ref(false)` — while fetching users
- `selectedUserId: ref<number | null>(null)` — bound to the select
- `userSearch: ref('')` — text filter for the user list
- `quantity: ref(1)` — numeric input value, min 1
- `loading: ref(false)` — while submitting

**Template structure (BEM: lightbox lightbox--gift):**
```html
<div :class="{ 'is-open': isOpen }" class="lightbox lightbox--gift">
  <div class="lightbox--gift__backdrop" @click="handleClose" />
  <div class="lightbox--gift__box" role="dialog" aria-modal="true">
    <button title="Cerrar" type="button" class="lightbox__button" @click="handleClose">
      <IconX :size="24" />
    </button>
    <div class="lightbox--gift__header">
      <div class="lightbox--gift__header__title">Regalar reservas</div>
      <div class="lightbox--gift__header__subtitle">Selecciona un usuario y la cantidad a regalar</div>
    </div>
    <!-- Quantity field -->
    <div class="lightbox--gift__field">
      <label for="lightbox-gift-quantity">Cantidad</label>
      <input id="lightbox-gift-quantity" v-model.number="quantity" type="number" min="1" />
    </div>
    <!-- User search + select -->
    <div class="lightbox--gift__field">
      <label for="lightbox-gift-search">Usuario</label>
      <input id="lightbox-gift-search" v-model="userSearch" type="text" placeholder="Buscar por nombre..." />
      <select v-model="selectedUserId" size="6" class="lightbox--gift__field__select">
        <option v-if="usersLoading" disabled value="">Cargando usuarios...</option>
        <option v-else-if="filteredUsers.length === 0" disabled value="">Sin resultados</option>
        <option v-for="u in filteredUsers" :key="u.id" :value="u.id">
          {{ u.firstName }} {{ u.lastName }}
        </option>
      </select>
    </div>
    <!-- Actions -->
    <div class="lightbox--gift__actions">
      <button class="btn btn--secondary" type="button" @click="handleClose">Cancelar</button>
      <button class="btn btn--primary" type="button" :disabled="loading || !selectedUserId || quantity < 1" @click="handleSubmit">
        {{ loading ? 'Enviando...' : 'Regalar' }}
      </button>
    </div>
  </div>
</div>
```

**Script logic:**

`filteredUsers` computed: filters `users` by `userSearch` (case-insensitive match on `firstName + ' ' + lastName`).

`watch(isOpen)`: on `true` → set `document.body.style.overflow = 'hidden'`, reset `quantity = 1`, `selectedUserId = null`, `userSearch = ''`, then call `loadUsers()`. On `false` → restore `document.body.style.overflow = ''`.

`loadUsers()`: sets `usersLoading = true`, calls `client<{ data: ... }>('/users/authenticated')`, assigns to `users.value`, sets `usersLoading = false`. Wrap in try/catch, log error on fail.

`handleSubmit()`: guards (no selectedUserId or quantity < 1 or loading). Shows Swal confirmation:
```
title: '¿Confirmar regalo?',
text: `Vas a regalar ${quantity} ${label} al usuario seleccionado.`,
icon: 'question',
showCancelButton: true,
confirmButtonText: 'Sí, regalar',
cancelButtonText: 'Cancelar'
```
If `!isConfirmed` → return. Set `loading = true`. Call `client(\`/${endpoint}/gift\`, { method: 'POST', body: { userId: selectedUserId.value, quantity: quantity.value } })`. On success: emit `'gifted'`, call `handleClose()`. On error: show `Swal.fire('Error', 'No se pudo enviar el regalo.', 'error')`. Finally: `loading = false`.

`handleClose()`: `document.body.style.overflow = ''`, emit `'close'`.

**Imports:** `ref`, `computed`, `watch` from `'vue'`; `X as IconX` from `'lucide-vue-next'`; `useStrapiClient` auto-imported; `useSweetAlert2` auto-imported.

**TypeScript:** No `any`. Define `interface IAuthUser { id: number; firstName: string; lastName: string }` inline.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | grep -E "LightboxGift|error TS" | head -20 || echo "typecheck ok"</automated>
  </verify>
  <done>LightboxGift.vue exists, accepts isOpen/endpoint/label props, emits close/gifted, passes TypeScript check with no new errors</done>
</task>

<task type="auto">
  <name>Task 2: Add lightbox--gift SCSS modifier to _lightbox.scss</name>
  <files>apps/dashboard/app/scss/components/_lightbox.scss</files>
  <action>
Append the `&--gift` modifier block to the `.lightbox { }` ruleset in `apps/dashboard/app/scss/components/_lightbox.scss`.

Add it after the `&--articles { ... }` block (the last block in the file), inside the `.lightbox { }` rule.

The modifier must follow the exact same fixed-fullscreen + backdrop + box + animate pattern as `&--razon` and `&--articles`. Use the same SCSS variables (`$charcoal`, `$platinum`, `$dodger_blue`, `$white_smoke`) already imported at the top of the file.

**SCSS to add (inside `.lightbox {}`, as a new `&--gift` block):**

```scss
&--gift {
  position: fixed;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition:
    opacity 0.3s ease,
    visibility 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;

  &.is-open {
    opacity: 1;
    visibility: visible;

    .lightbox--gift__box {
      transform: translateY(0) scale(1);
    }
  }

  &__backdrop {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    background-color: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(2px);
    z-index: 1;
  }

  &__box {
    background-color: white;
    max-width: 480px;
    width: calc(100% - 40px);
    padding: 40px 30px 30px;
    border-radius: 4px;
    box-shadow: 0 0 30px rgba($charcoal, 0.1);
    position: relative;
    z-index: 2;
    transform: translateY(-20px) scale(0.95);
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  &__header {
    display: flex;
    flex-direction: column;
    gap: 4px;

    &__title {
      font-weight: 700;
      font-size: 20px;
      color: $charcoal;
      line-height: 1.2;
    }

    &__subtitle {
      font-size: 13px;
      color: rgba($charcoal, 0.55);
    }
  }

  &__field {
    display: flex;
    flex-direction: column;
    gap: 6px;

    label {
      font-size: 13px;
      font-weight: 600;
      color: $charcoal;
    }

    input[type="number"],
    input[type="text"] {
      width: 100%;
      border: 1px solid $platinum;
      border-radius: 4px;
      padding: 8px 10px;
      font-size: 14px;
      color: $charcoal;
      font-family: inherit;

      &:focus {
        outline: none;
        border-color: $charcoal;
      }
    }

    &__select {
      width: 100%;
      border: 1px solid $platinum;
      border-radius: 4px;
      padding: 4px;
      font-size: 14px;
      color: $charcoal;
      font-family: inherit;
      background-color: $white_smoke;

      option {
        padding: 6px 8px;
      }

      &:focus {
        outline: none;
        border-color: $charcoal;
      }
    }
  }

  &__actions {
    display: flex;
    justify-content: flex-end;
    gap: 12px;
    padding-top: 4px;
  }
}
```

IMPORTANT: Do NOT add `box-shadow` or `transform: scale` to hover states (per AGENTS.md). The `scale(0.95)` on the box is an entry animation (not hover), which is the same pattern used in `--razon`.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | grep -E "scss|error TS" | head -10 || echo "scss ok"</automated>
  </verify>
  <done>_lightbox.scss has a &--gift modifier block; dashboard builds without SCSS errors</done>
</task>

</tasks>

<verification>
- `apps/dashboard/app/components/LightboxGift.vue` exists and contains `isOpen`, `endpoint`, `label` props
- `apps/dashboard/app/scss/components/_lightbox.scss` contains `&--gift` block
- `cd apps/dashboard && yarn nuxt typecheck` passes with no new TS errors related to LightboxGift
</verification>

<success_criteria>
LightboxGift.vue is a self-contained, reusable component: controlled by `isOpen` prop, fetches authenticated users on open, shows quantity input + searchable user select, fires Swal confirmation before calling the gift endpoint, emits 'close' and 'gifted'. SCSS modifier `lightbox--gift` provides matching visual style.
</success_criteria>

<output>
After completion, create `.planning/phases/076-dashboard-gift-lightbox/076-01-SUMMARY.md`
</output>
