---
phase: 04-auth-tokens-base
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/website/app/scss/components/_auth.scss
  - apps/website/app/scss/components/_introduce.scss
  - apps/website/app/components/IntroduceAuth.vue
  - apps/website/app/components/LoginWithGoogle.vue
autonomous: true
requirements: [TOK-03, AUTH-01, AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - "The auth layout is a 50/50 split, height 100vh, keeping the existing column-reverse on small screens"
    - "The brand panel is a cream rounded card with two CSS amber radial glows (no background image), a black logo, a 'Marketplace industrial' chip, bullet checks in amber circles, and a shield security footer"
    - "PictureDefault is removed from the brand panel"
    - "The Google button is amber with the multicolor Google logo inside a white circle"
  artifacts:
    - path: "apps/website/app/components/IntroduceAuth.vue"
      provides: "Restyled cream brand panel with LogoBlack, chip, lucide Check bullets, shield footer; no PictureDefault"
      contains: "LogoBlack"
    - path: "apps/website/app/scss/components/_introduce.scss"
      provides: "Cream card + radial glow styling for .introduce--auth"
      contains: "radial-gradient"
  key_links:
    - from: "IntroduceAuth.vue"
      to: "lucide-vue-next Check + Shield icons"
      via: "import"
      pattern: "from \"lucide-vue-next\""
    - from: ".introduce--auth"
      to: "$cream card + amber glows"
      via: "background / radial-gradient"
      pattern: "radial-gradient"
---

<objective>
Restyle the auth chrome and brand panel to the mockup: a 50/50 split layout, and a cream rounded brand card with two CSS amber radial glows, a black logo, a "Marketplace industrial" chip, amber-circle check bullets (lucide), and a shield security footer — replacing the current charcoal background image. Also restyle the Google button to the amber primary with the multicolor logo in a white circle.

Purpose: This is the shared auth shell (`.auth` layout + `.introduce--auth` brand panel) and the shared Google button, reused by ALL six auth screens. Getting it right here means every per-screen plan (Wave 3) inherits the new brand look automatically.
Output: `.auth` at 50/50; `.introduce--auth` as a cream card with glows; `IntroduceAuth.vue` restyled (LogoBlack, chip, lucide Check bullets, shield footer, PictureDefault dropped); `LoginWithGoogle.vue` as amber primary with colored Google SVG.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# New tokens from Plan 01 (via @use variables): $ink, $ink2, $muted, $amber,
# $amber_hover, $cream, $line.

# THE MOCKUP — brand panel is the <aside> block, lines 25-60:
#   aside: flex 1 1 50%, padding 22px
#   card: flex:1, bg $cream, border 1px $line, border-radius 20px, padding 52px,
#         flex column, justify space-between, position relative, overflow hidden
#   glow 1: top -130 right -110, 460x460, radial rgba(247,201,126,.42)→transparent 62%
#   glow 2: bottom -100 left -100, 320x320, radial rgba(247,201,126,.16)→transparent 65%
#   header row: black logo (height 29) + chip "Marketplace industrial"
#     chip: bg rgba(247,201,126,.32), color $ink, weight 600, 12px, padding 6px 13px,
#           radius 99px, with a 6px amber dot
#   title: $ink, weight 800, clamp(30px,2.8vw,44px), line-height 1.15, letter-spacing -.7px
#   kicker: $ink2 15px/600; bullets: 23px amber circle w/ check (stroke $ink), text $ink2 15px
#   footer: shield icon ($muted) + "Conexión protegida · datos cifrados…" $muted 13px/500
# Google button (login/reg1 in mockup, lines 76-79): amber bg, $ink text, radius 9px,
#   white 24px circle holding the multicolor Google <svg>, text "Continuar con Google"
@design/auth.dc.html

# Files being modified:
@apps/website/app/scss/components/_auth.scss
@apps/website/app/scss/components/_introduce.scss
@apps/website/app/components/IntroduceAuth.vue
@apps/website/app/components/LoginWithGoogle.vue

# Reference only:
@apps/website/app/components/LogoBlack.vue   # use this instead of LogoWhite
@apps/website/app/scss/components/_button.scss  # .btn--google base — override scoped under .auth in _form.scss (Plan 02); here only adjust IntroduceAuth/Google markup

<interfaces>
- `LogoBlack.vue` renders `/images/logo-black.svg` inside a `.logo` NuxtLink — import it the same way LogoWhite is imported today.
- lucide import pattern (from MenuUser.vue): `import { Check as IconCheck, Shield as IconShield } from "lucide-vue-next";` then `<IconCheck :size="12" />` in template.
- IntroduceAuth props unchanged: `title: string; subtitle?: string; list?: string[]`.
- The Google button already uses `.btn--primary` styling from Plan 02 (amber). This plan adjusts its INNER markup (white circle + colored SVG) and class for the circle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 50/50 split layout in _auth.scss (keep column-reverse on small)</name>
  <read_first>
    - apps/website/app/scss/components/_auth.scss (full file — current 45/55 split, `&__introduce { width: 45% }`, `__bg`, screen-large rules)
    - design/auth.dc.html lines 23-26, 62-64 (both panels `flex:1 1 50%`, aside padding 22px, main overflow-y auto)
    - apps/website/app/scss/abstracts/_mixins.scss lines 1-30 (screen-small / screen-large)
  </read_first>
  <action>
    Update `.auth` in `_auth.scss` to a 50/50 split:
    - Change `&__introduce { width: 45%; }` → `width: 50%;` and `&__form { width: 100%; ... }` so both panels are `flex-basis: 0; flex-grow: 1` (effectively 50/50). Set the introduce panel padding to `22px` (mockup) instead of `20px`.
    - Keep `height: 100vh` / `min-height: 100vh` and the existing `@include screen-large { flex-direction: column-reverse; }` block UNCHANGED (D-04: preserve the column-reverse responsive behavior).
    - The `&__introduce__bg` rule (charcoal + bg image) becomes unused once IntroduceAuth drops PictureDefault — you MAY remove the `&__introduce { &__bg { ... } }` sub-block here (subtractive), but only after confirming no other component uses `.auth__introduce__bg` (grep). If unsure, leave it.
    - Make `&__form` overflow `auto` (mockup `overflow-y:auto`) rather than `scroll` to avoid a permanent scrollbar.
    Use tokens; no inline styles; keep BEM. Do NOT change the column-reverse breakpoint.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'flex-direction: column-reverse' apps/website/app/scss/components/_auth.scss && grep -Eq 'width: 50%' apps/website/app/scss/components/_auth.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.auth__introduce` width is `50%` (not 45%)
    - `flex-direction: column-reverse` still present in the screen-large block
    - `100vh` still present
    - If `.auth__introduce__bg` removed, `grep -rn 'auth__introduce__bg' apps/website/app` returns no other consumer
  </acceptance_criteria>
  <done>Auth shell is 50/50, full height, column-reverse on small screens preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Cream brand card + amber glows in _introduce.scss</name>
  <read_first>
    - apps/website/app/scss/components/_introduce.scss (full file — current charcoal panel, `__bg`, `__content`, `__logo`, `__title`, `__details`, `__list`)
    - design/auth.dc.html lines 27-58 (card, glows, chip, title, kicker, bullets, shield footer)
    - apps/website/app/components/IntroduceAuth.vue (current markup the SCSS targets)
  </read_first>
  <action>
    Rewrite `.introduce--auth` in `_introduce.scss` to the cream card design. Replace the charcoal background and the `__bg` absolute-image styling. New structure (all under `.introduce--auth`):
    - Root `.introduce--auth`: `padding: 22px; height: 100%; width: 100%; display: flex;` (the card fills it). Remove the `background: $charcoal` from the root.
    - Card wrapper — the panel needs an inner card element. The card styling goes on `&__card` (Plan markup in Task 3 adds `<div class="introduce--auth__card">`):
      `&__card { position: relative; overflow: hidden; flex: 1; background: $cream; border: 1px solid $line; border-radius: 20px; padding: 52px; display: flex; flex-direction: column; justify-content: space-between; }`
    - Glows as `&__card__glow` + `--top` / `--bottom` modifiers, `position: absolute; border-radius: 50%; pointer-events: none;`
      `--top { top: -130px; right: -110px; width: 460px; height: 460px; background: radial-gradient(circle, rgba(247, 201, 126, 0.42), transparent 62%); }`
      `--bottom { bottom: -100px; left: -100px; width: 320px; height: 320px; background: radial-gradient(circle, rgba(247, 201, 126, 0.16), transparent 65%); }`
    - `&__content`: `position: relative; z-index: 2;` (so it sits above glows) — keep the flex-column but the header/body/footer are space-between on the card, so `__content` should hold the middle (title+bullets).
    - Header: `&__header { position: relative; display: flex; align-items: center; justify-content: space-between; }`
      chip: `&__header__chip { display: inline-flex; align-items: center; gap: 7px; background: rgba(247, 201, 126, 0.32); color: $ink; font-weight: 600; font-size: 12px; padding: 6px 13px; border-radius: 99px; }` with `&__dot { width: 6px; height: 6px; border-radius: 50%; background: $amber_hover; }`
    - `&__logo img { height: 29px; width: auto; }`
    - `&__title`: `color: $ink; font-weight: 800; font-size: clamp(30px, 2.8vw, 44px); line-height: 1.15; letter-spacing: -0.7px;` (drop the white/peach colors). Remove the `span/strong { color: $light_peach }` rule.
    - `&__details__title` (kicker): `color: $ink2; font-size: 15px; font-weight: 600;`
    - `&__details__list li`: `color: $ink2; font-size: 15px; line-height: 1.5; font-weight: 500; display: flex; gap: 13px; align-items: flex-start;`
      check circle: `&__check { flex: none; width: 23px; height: 23px; border-radius: 50%; background: $amber; display: grid; place-items: center; color: $ink; }` (the lucide Check sits inside, stroke = currentColor = $ink)
    - Footer: `&__footer { position: relative; display: flex; align-items: center; gap: 9px; color: $muted; font-size: 13px; font-weight: 500; }`
    Keep `@include screen-large` responsiveness reasonable (reduce padding on small). Use tokens; the only raw values allowed are the two glow rgba gradients and clamp() numbers. BEM nesting must mirror the markup in Task 3.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'radial-gradient(circle, rgba(247, 201, 126, 0.42)' apps/website/app/scss/components/_introduce.scss && grep -q '\$cream' apps/website/app/scss/components/_introduce.scss && grep -q 'border-radius: 20px' apps/website/app/scss/components/_introduce.scss && ! grep -q '\$charcoal' apps/website/app/scss/components/_introduce.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `_introduce.scss` contains `background: $cream` (or `$cream` on the card), `border: 1px solid $line`, `border-radius: 20px`
    - Two `radial-gradient(circle, rgba(247, 201, 126, ...` glows present (.42 and .16)
    - `$charcoal` no longer referenced in `_introduce.scss`
    - chip, logo (29px), title (clamp + $ink), check circle ($amber), footer ($muted) styles present
    - BEM hierarchy under `.introduce--auth` (no standalone hyphenated classes)
  </acceptance_criteria>
  <done>Brand panel is a cream rounded card with amber glows and the new typography.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle IntroduceAuth.vue markup (LogoBlack, chip, lucide bullets, shield footer; drop PictureDefault)</name>
  <read_first>
    - apps/website/app/components/IntroduceAuth.vue (full file — current LogoWhite + NuxtImg IconCheck bullets + PictureDefault)
    - apps/website/app/components/LogoBlack.vue (the replacement logo)
    - apps/website/app/components/MenuUser.vue line ~112 (lucide import pattern)
    - design/auth.dc.html lines 27-58 (target markup structure: card > header(logo+chip) > content(title+kicker+bullets) > footer(shield+text))
    - apps/website/app/scss/components/_introduce.scss (the BEM classes Task 2 defines — markup must match exactly)
  </read_first>
  <action>
    Rewrite the template of `IntroduceAuth.vue` to match the new BEM structure from Task 2, keeping the component's props (`title`, `subtitle`, `list`) and the existing sanitize logic (`getTitle`, `sanitizedSubtitle`, `getList`) intact. Markup:

    ```
    <div class="introduce introduce--auth">
      <div class="introduce--auth__card">
        <div class="introduce--auth__card__glow introduce--auth__card__glow--top" />
        <div class="introduce--auth__card__glow introduce--auth__card__glow--bottom" />

        <div class="introduce--auth__header">
          <div class="introduce--auth__logo"><LogoBlack /></div>
          <span class="introduce--auth__header__chip">
            <span class="introduce--auth__header__chip__dot" />Marketplace industrial
          </span>
        </div>

        <div class="introduce--auth__content">
          <h2 class="introduce--auth__title" v-html="getTitle" />
          <div class="introduce--auth__details">
            <h3 v-if="sanitizedSubtitle" class="introduce--auth__details__title" v-html="sanitizedSubtitle" />
            <ul v-if="getList.length > 0" class="introduce--auth__details__list">
              <li v-for="(item, index) in getList" :key="index">
                <span class="introduce--auth__details__list__check"><IconCheck :size="12" :stroke-width="3" /></span>
                {{ item }}
              </li>
            </ul>
          </div>
        </div>

        <div class="introduce--auth__footer">
          <IconShield :size="15" :stroke-width="2" />
          Conexión protegida · datos cifrados de extremo a extremo
        </div>
      </div>
    </div>
    ```

    Script changes:
    - Replace `import LogoWhite from "@/components/LogoWhite.vue";` with `import LogoBlack from "@/components/LogoBlack.vue";`
    - REMOVE `import PictureDefault from "@/components/PictureDefault.vue";` and its usage (drop the `__bg` block entirely) — D-05.
    - REMOVE `import IconCheck from "/images/icon-check-circle.svg";` (the NuxtImg check).
    - ADD `import { Check as IconCheck, Shield as IconShield } from "lucide-vue-next";` (D-09).
    - Keep `useSanitize`, `getTitle`, `sanitizedSubtitle`, `getList` exactly as they are.
    - The footer text "Conexión protegida · datos cifrados de extremo a extremo" is static per the mockup (same for all screens).
    Ensure NO unused imports remain (Codacy rule): LogoWhite, PictureDefault, the old IconCheck svg must all be gone. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'import LogoBlack' apps/website/app/components/IntroduceAuth.vue && grep -q 'from "lucide-vue-next"' apps/website/app/components/IntroduceAuth.vue && grep -q 'IconShield' apps/website/app/components/IntroduceAuth.vue && grep -q 'Marketplace industrial' apps/website/app/components/IntroduceAuth.vue && ! grep -q 'PictureDefault' apps/website/app/components/IntroduceAuth.vue && ! grep -q 'LogoWhite' apps/website/app/components/IntroduceAuth.vue && ! grep -q 'style="' apps/website/app/components/IntroduceAuth.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Imports `LogoBlack` and uses `<LogoBlack />`
    - Imports `Check as IconCheck` and `Shield as IconShield` from `lucide-vue-next`
    - Template contains `introduce--auth__card`, `introduce--auth__header__chip`, `introduce--auth__footer`, "Marketplace industrial"
    - No `PictureDefault`, no `LogoWhite`, no `/images/icon-check-circle.svg` import remaining
    - No inline `style="` attribute
    - Props (title/subtitle/list) and sanitize logic unchanged
  </acceptance_criteria>
  <done>Brand panel renders the cream card with black logo, chip, lucide check bullets, and shield footer.</done>
</task>

<task type="auto">
  <name>Task 4: Amber Google button with multicolor logo in white circle (LoginWithGoogle.vue)</name>
  <read_first>
    - apps/website/app/components/LoginWithGoogle.vue (full file — current NuxtImg icon-google.svg + .btn--google)
    - design/auth.dc.html lines 76-79 (white 24px circle + the multicolor Google SVG paths + "Continuar con Google")
    - apps/website/app/scss/components/_auth.scss (this plan owns it — add the `.btn--google__circle` rule scoped under `.auth` here)
    - apps/website/app/scss/components/_button.scss `&--google` (READ-ONLY reference; the amber bg comes from the Plan 02 `.auth .btn--primary` override since the button has `btn--primary`)
  </read_first>
  <action>
    Update `LoginWithGoogle.vue` so the button matches the mockup: a white 24px circle containing the multicolor Google SVG, on the amber primary button. The button already has classes `btn btn--primary btn--block btn--google`; the amber background comes from the Plan 02 `.auth .btn--primary` override, so keep `btn--primary`.
    - Replace the `<NuxtImg src="/images/icon-google.svg" />` with the inline multicolor Google `<svg>` (the exact 4-path version from the mockup, lines ~77) wrapped in a white circle span: `<span class="btn--google__circle"><svg ...>...</svg></span>`. KEEP the colored brand SVG — do NOT lucide-ify it. RATIONALE: D-09 (use lucide-vue-next) applies to UI iconography; the Google brand mark is a multicolor trademark SVG that lucide cannot replicate, and the mockup itself uses an inline SVG for it — so the inline 4-color SVG is the correct implementation here (#4285F4, #34A853, #FBBC05, #EA4335).
    - Add the `.btn--google__circle` styling to `_auth.scss` (NOT `_button.scss` — that file is shared/read-only for this phase). Scope it under `.auth` so it stays inside this area's namespace and does not collide with Plan 02's `_form.scss`:
      `.auth { .btn--google__circle { width: 24px; height: 24px; border-radius: 50%; background: $white; display: grid; place-items: center; flex: none; } }`
      (`.btn--google__circle` is a NEW class used only by this button; adding it retones no existing global rule.)
    - Keep the button text. The mockup says "Continuar con Google" (login) — but this shared component is used on both login and registro. Keep the existing label behavior; if a single label is needed, use "Continuar con Google". Do NOT change the click handler / `useProviders` / redirect logic (D-10 — behavior unchanged).
    - Remove the now-unused NuxtImg import if any. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q '#4285F4' apps/website/app/components/LoginWithGoogle.vue && grep -q 'btn--google__circle' apps/website/app/components/LoginWithGoogle.vue && grep -q 'btn--google__circle' apps/website/app/scss/components/_auth.scss && ! grep -q 'style="' apps/website/app/components/LoginWithGoogle.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - LoginWithGoogle.vue contains the multicolor Google SVG (`#4285F4`, `#34A853`, `#FBBC05`, `#EA4335`)
    - The SVG is wrapped in `.btn--google__circle` (white circle)
    - `.btn--google__circle` rule added under `.auth` in `_auth.scss` (additive, new class only); `_button.scss` is NOT modified
    - Click handler / useProviders / navigateTo logic unchanged
    - No inline `style="`; no unused imports
  </acceptance_criteria>
  <done>Google button is amber with the multicolor logo in a white circle, behavior unchanged.</done>
</task>

</tasks>

<verification>
- Brand panel renders cream card + glows + black logo + chip + lucide check bullets + shield footer (no image).
- 50/50 split, column-reverse preserved on small screens.
- Google button amber with colored logo.
- `pnpm codacy` passes (no unused imports, BEM compliant).
</verification>

<success_criteria>
- TOK-03: lucide Check + Shield icons applied in the brand panel; colored Google logo retained.
- AUTH-01..04 groundwork: the shared shell + brand panel match the mockup for every screen.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-03-SUMMARY.md`
</output>
