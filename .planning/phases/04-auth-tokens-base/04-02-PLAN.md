---
phase: 04-auth-tokens-base
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/website/app/scss/components/_form.scss
  - apps/website/app/scss/components/_strength.scss
autonomous: true
requirements: [TOK-01, TOK-03]
must_haves:
  truths:
    - "Inside the auth flow, inputs have a 1px $line border, 7px radius, and an amber focus ring"
    - "Inside the auth flow, the primary button (Google/submit) is amber; the secondary is white with a $line border"
    - "Inside the auth flow, labels are $muted 13px/600, the divider reads 'o con tu correo', password toggle + generate are styled per mockup, the strength meter uses feedback colors"
    - "The duplicate .form--verify block is removed from _form.scss (canonical owner is _verify-code.scss)"
    - "No global (un-nested) rule in _form.scss has its color/border changed — all new styling is scoped under .auth"
  artifacts:
    - path: "apps/website/app/scss/components/_form.scss"
      provides: ".auth-scoped form primitive overrides + register step-indicator classes"
      contains: "rgba(247, 201, 126, 0.25)"
    - path: "apps/website/app/scss/components/_strength.scss"
      provides: "4-bar strength meter colored with $error/$warning/$success/$success_strong"
      contains: "$success_strong"
  key_links:
    - from: ".auth .form-control:focus"
      to: "$amber_hover focus ring"
      via: "box-shadow"
      pattern: "0 0 0 3px rgba\\(247, 201, 126, 0\\.25\\)"
---

<objective>
Restyle the SHARED form primitives — inputs, labels, primary/secondary buttons, the separator, checkboxes, the password toggle/generate controls, and the 4-bar strength meter — to match `design/auth.dc.html`, but apply EVERY new style SCOPED under `.auth` so the rest of the site (checkout, profile, contact) is untouched. Also remove the duplicate `.form--verify` block from `_form.scss`.

Purpose: `_form.scss` and `_button.scss` rules (`.form-control`, `.form-label`, `.btn--primary`, `.btn--secondary`, bare `input/select/textarea`) are GLOBAL and used everywhere. The redesign must only change the auth screens. Scoping every override under `.auth` (and the already-auth-only `.form--verify`) keeps the global look intact while the auth pages get the new tokens.
Output: A `.auth { ... }` override block (in `_form.scss`) that retones inputs, labels, buttons, separator, checkboxes, password controls + register step-indicator; `_strength.scss` recolored with feedback tokens; the duplicate `.form--verify` removed from `_form.scss`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# New tokens added in Plan 01 — available via `@use "../abstracts/variables" as *`:
# $ink #26252b, $ink2 #56535f, $muted #8a8794, $placeholder #b6b2bb,
# $amber #f7c97e, $amber_hover #efb85c, $cream #f6f4f1, $line #ece9e4,
# $error #e4534b, $warning #e8902c, $success #3b9e63, $success_strong #1f8a5b,
# $strength_empty #eae7e2

# THE MOCKUP — translate these inline styles to BEM/SCSS:
#   inputs: border 1px $line, radius 7px, padding 14px 15px, font 15px color $ink,
#           focus: border-color $amber_hover + box-shadow 0 0 0 3px rgba(247,201,126,.25)
#   label span: 13px / 600 / $muted, margin-bottom 8px
#   primary btn (amber): bg $amber, hover $amber_hover, color $ink, radius 9px, weight 700
#   secondary btn (white): border 1px $line, color $ink, bg #fff, radius 7px, hover bg $cream
#   divider: 1px $line lines + 12.5px $muted center text
#   password toggle: text "Mostrar/Ocultar", $ink2, hover $ink, absolute right
#   generate btn: "Generar segura", $ink underline, sparkles icon
#   checkboxes: 18px, accent-color $amber_hover, label 14px $ink2
#   strength: 4 bars (score-driven colors) + label, empty bar $strength_empty
#   register step indicator: two 28x4 amber pills + "Paso 2 de 2 · acceso" text
@design/auth.dc.html

# Files being modified:
@apps/website/app/scss/components/_form.scss
@apps/website/app/scss/components/_strength.scss

# Reference only — DO NOT EDIT. .btn--primary/.btn--secondary base lives here;
# you OVERRIDE them scoped under .auth in _form.scss, never edit them here.
@apps/website/app/scss/components/_button.scss

<interfaces>
Existing markup classes the overrides must target (from FormLogin/FormRegister/etc.):
- Inputs: `.form-control` (input), `.form-label` (label), `.form-group`, `.form-group--password`
- Password: `.form-group--password__show-password` (toggle), `.form-group--password__generate`,
  `.form-group--password__topbar`, `.form-group--password__field`
- Checkboxes: `.form-check`, `.form-check-input`, `.form-check-label`
- Buttons (markup unchanged this plan): `.btn.btn--primary`, `.btn.btn--secondary`, `.btn.btn--block`, `.btn.btn--google`
- Separator (page-owned): `.auth__form__separator`
- Strength (PasswordStrength.vue): `.strength`, `.strength__bars`, `.strength__bar`,
  `.strength__bar--level{1..4}`, `.strength__label`, `.strength__label--level{1..4}`
NOTE: register step-indicator markup classes (e.g. `.form--register__steps`) will be added by Plan 05;
define the SCSS for them here so Plan 05 just adds the markup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the duplicate .form--verify block from _form.scss (subtractive)</name>
  <read_first>
    - apps/website/app/scss/components/_form.scss lines 444-489 (the trailing `.form--verify` block)
    - apps/website/app/scss/components/_verify-code.scss (the canonical, fuller `.form--verify` owner)
  </read_first>
  <action>
    `_form.scss` ends with a `.form--verify { ... }` block (the `// 6-digit OTP verify code input` comment near line 451 through line 488). This duplicates `_verify-code.scss`, which is the canonical owner (it has `&__resend` and a `::placeholder` rule the _form.scss copy lacks). Per CLAUDE.md's subtractive-refactor rule, DELETE the entire trailing `.form--verify { ... }` block from `_form.scss` (from the `// 6-digit OTP verify code input` comment to its closing brace). Leave `_verify-code.scss` as the single source. Do NOT delete the `.form-control[style*="--textarea-height"]` rule that sits just above it.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && test "$(grep -c '\.form--verify' apps/website/app/scss/components/_form.scss)" = "0" && grep -q '\.form--verify' apps/website/app/scss/components/_verify-code.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.form--verify' _form.scss` returns 0
    - `_verify-code.scss` still contains `.form--verify`
    - `.form-control[style*="--textarea-height"]` rule still present in _form.scss
  </acceptance_criteria>
  <done>Duplicate OTP block removed from _form.scss; _verify-code.scss is the sole owner.</done>
</task>

<task type="auto">
  <name>Task 2: Add a .auth-scoped override block to _form.scss (inputs, labels, buttons, separator, checkboxes, password controls, register steps)</name>
  <read_first>
    - apps/website/app/scss/components/_form.scss (full file — understand `.form-control`, `.form-label`, `.form-group--password*`, `.form-check*`)
    - apps/website/app/scss/components/_button.scss lines 1-130 (the `.btn--primary` / `.btn--secondary` / `.btn--google` bases you will override — DO NOT edit this file)
    - apps/website/app/scss/components/_auth.scss lines 99-175 (`.auth__form__separator` markup the divider override targets)
    - design/auth.dc.html lines 81-100 (inputs, label, divider, toggle), 159-211 (register step pills, generate, checkboxes, primary/secondary btn pairing)
  </read_first>
  <action>
    Add a NEW `.auth { ... }` override block to `_form.scss` (after the existing `.form { ... }` block, before or after the textarea-height rule — anywhere top-level). Everything below is nested INSIDE `.auth` so it only affects the auth pages. DO NOT modify the existing global `.form-control` / `.form-label` / `.btn--primary` rules in place. Use the new variables (already imported via the existing `@use "../abstracts/variables" as *` at top of file).

    Implement, all nested under `.auth`:

    1. Inputs (`.form-control`, and bare `input`, `select` if needed):
       `border: 1px solid $line; border-radius: 7px; background: #fff; color: $ink; font-size: 15px;`
       Keep `input.form-control { height: 48px; }` feel but use padding `14px 15px`.
       `&::placeholder { color: $placeholder; }`
       Focus state (override the global charcoal focus):
       `&:focus, &:active { border: 1px solid $amber_hover; box-shadow: 0 0 0 3px rgba(247, 201, 126, 0.25); }`
       (the `:hover` should NOT show the charcoal border inside auth — set it to `$line` or the focus treatment).

    2. Labels (`.form-label`, `.form__label`): the mockup labels are STATIC above the input (not the floating `top:-6px` absolute label). Inside `.auth`, set
       `.form-label, .form__label { position: static; left: auto; top: auto; padding: 0; background: transparent; font-size: 13px; font-weight: 600; color: $muted; margin-bottom: 8px; }`

    3. Primary button (`.btn--primary`, includes `.btn--google`):
       `background: $amber; color: $ink; border-radius: 9px; font-weight: 700;`
       `&:hover, &:focus, &:active { background: $amber_hover; color: $ink; }`

    4. Secondary button (`.btn--secondary`):
       `background: #fff; color: $ink; border: 1px solid $line; border-radius: 7px; text-decoration: none; font-weight: 600;`
       `&:hover, &:focus, &:active { background: $cream; color: $ink; border-color: $line; }`

    5. Separator (`.auth__form__separator`): retone the `::before`/`::after` lines to `background: $line;` and the center text to `color: $muted; font-size: 12.5px; font-weight: 500;`. (Markup/text stays page-owned.)

    6. Password controls:
       `.form-group--password__show-password { color: $ink2; font-weight: 600; font-size: 13px; &:hover { color: $ink; } }`
       `.form-group--password__generate { color: $ink; text-decoration: underline; font-weight: 700; font-size: 12.5px; }`

    7. Checkboxes:
       `.form-check-input { accent-color: $amber_hover; }` (the mockup uses native checkbox + accent-color; keep markup as-is)
       `.form-check-label { color: $ink2; font-weight: 500; a { color: $ink; font-weight: 700; text-decoration: underline; } }`

    8. Register step indicator (define SCSS now; Plan 05 adds the markup):
       Add `.form--register__steps { display: flex; align-items: center; gap: 7px; margin-bottom: 30px; }`
       `.form--register__steps__pill { width: 28px; height: 4px; border-radius: 99px; background: $amber; }`
       `.form--register__steps__label { font-size: 12.5px; color: $muted; font-weight: 500; margin-left: 5px; }`
       (These live under `.auth` too.)

    Follow BEM and the no-inline-style rule. Use only `$...` tokens, never raw hex except the exact focus-ring rgba `rgba(247, 201, 126, 0.25)` (the mockup's literal alpha — acceptable as the canonical focus ring; the base color is $amber).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'rgba(247, 201, 126, 0.25)' apps/website/app/scss/components/_form.scss && grep -q '\$amber_hover' apps/website/app/scss/components/_form.scss && grep -q '\.form--register__steps__pill' apps/website/app/scss/components/_form.scss && grep -Eq '^\.auth \{|^\.auth\{' apps/website/app/scss/components/_form.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `_form.scss` contains a top-level `.auth {` block (grep `^\.auth`)
    - The string `rgba(247, 201, 126, 0.25)` appears (focus ring)
    - `$amber`, `$amber_hover`, `$line`, `$cream`, `$muted`, `$ink`, `$ink2`, `$placeholder` are all referenced inside the `.auth` block
    - `.form--register__steps__pill` and `.form--register__steps__label` defined
    - The pre-existing global `.form { &__control { border: 2px solid $platinum; ... } }` rule is UNCHANGED (still says `2px solid $platinum`)
    - No raw hex other than the focus-ring rgba inside the new `.auth` block
  </acceptance_criteria>
  <done>All auth form primitives retoned to the mockup, scoped under `.auth`; global rules unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Recolor the strength meter with feedback tokens (_strength.scss)</name>
  <read_first>
    - apps/website/app/scss/components/_strength.scss (full file — current level colors)
    - apps/website/app/components/PasswordStrength.vue (markup: `.strength__bar--level{1..4}`, `.strength__label--level{1..4}`)
    - design/auth.dc.html lines 368-382 (colorByScore: 1→#E4534B, 2→#E8902C, 3→#3B9E63, 4→#1F8A5B; empty bar #EAE7E2; label colored to match score)
  </read_first>
  <action>
    Recolor `_strength.scss` to match the mockup's feedback palette using the new tokens. PasswordStrength.vue maps `computePasswordStrength` to a 1-4 score; keep the markup, change only colors:
    - `.strength__bar` empty: `background-color: $strength_empty;`
    - `.strength__bar--level1 { background-color: $error; }`
    - `.strength__bar--level2 { background-color: $warning; }`
    - `.strength__bar--level3 { background-color: $success; }`
    - `.strength__bar--level4 { background-color: $success_strong; }`
    - `.strength__label--level1 { color: $error; }`
    - `.strength__label--level2 { color: $warning; }`
    - `.strength__label--level3 { color: $success; }`
    - `.strength__label--level4 { color: $success_strong; }`
    NOTE: `.strength` is rendered only inside auth forms (FormRegister/FormResetPassword), so it is effectively auth-scoped already — editing `_strength.scss` directly is safe (no other consumer). Confirm with a grep for `<PasswordStrength` before editing; if it appears outside auth, scope under `.auth` instead.
    Keep `bar` height/radius and `label` sizing as-is.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q '\$success_strong' apps/website/app/scss/components/_strength.scss && grep -q '\$error' apps/website/app/scss/components/_strength.scss && grep -q '\$warning' apps/website/app/scss/components/_strength.scss && grep -q '\$strength_empty' apps/website/app/scss/components/_strength.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `_strength.scss` references `$error`, `$warning`, `$success`, `$success_strong`, `$strength_empty`
    - No `<PasswordStrength` usage exists outside auth components (verified by grep); if it does, the recolor is scoped under `.auth`
    - level1→$error, level2→$warning, level3→$success, level4→$success_strong mapping present
  </acceptance_criteria>
  <done>Strength meter shows red/orange/green/strong-green per score matching the mockup.</done>
</task>

</tasks>

<verification>
- `.form--verify` exists only in `_verify-code.scss` (0 occurrences in `_form.scss`).
- New input/label/button/separator/checkbox/password/step styles are all nested under `.auth` in `_form.scss`.
- Global `.form__control` / `.btn--primary` base rules unchanged.
- `pnpm codacy` / lint passes for SCSS.
</verification>

<success_criteria>
- TOK-01: auth form primitives point at the new tokens (amber buttons, $line inputs, amber focus ring).
- TOK-03 groundwork: strength + control styling matches the mockup (lucide icons themselves come in markup plans).
- Rest of the site visually unchanged (overrides scoped under `.auth`).
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-02-SUMMARY.md`
</output>
