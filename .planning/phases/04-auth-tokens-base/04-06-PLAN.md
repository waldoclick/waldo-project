---
phase: 04-auth-tokens-base
plan: 06
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - apps/website/app/pages/login/verificar.vue
  - apps/website/app/components/FormVerifyCode.vue
  - apps/website/app/scss/components/_verify-code.scss
autonomous: true
requirements: [AUTH-03, TOK-03]
must_haves:
  truths:
    - "The verify screen matches the mockup: left-aligned title + description, 6 OTP boxes (62px tall, 10px radius, amber focus ring), amber 'Verificar' button, and the resend countdown line"
    - "The back link uses a lucide ChevronLeft"
    - "Verification behavior is unchanged (digit auto-advance/backspace/paste, verify-code endpoint, redirect logic, resend countdown)"
  artifacts:
    - path: "apps/website/app/scss/components/_verify-code.scss"
      provides: "OTP digit boxes restyled to 62px / 10px radius / $line border / amber focus"
      contains: "rgba(247, 201, 126, 0.25)"
  key_links:
    - from: "FormVerifyCode.vue handleVerify"
      to: "/auth/verify-code"
      via: "apiClient"
      pattern: "auth/verify-code"
---

<objective>
Restyle the VERIFY (2FA) screen (`pages/login/verificar.vue`, `FormVerifyCode.vue`, `_verify-code.scss`) to match `design/auth.dc.html`: the 6 OTP boxes become 62px tall with 10px radius, a $line border and an amber focus ring; the "Verificar" submit becomes the amber primary; the back link becomes lucide ChevronLeft; the resend countdown line stays. Behavior untouched.

Purpose: AUTH-03. Applies the shared tokens to the OTP-specific markup/SCSS. `_verify-code.scss` is already the sole owner of `.form--verify` (the duplicate was removed from `_form.scss` in Plan 02).
Output: OTP boxes restyled; verify submit amber; page back link lucide; resend logic intact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# THE MOCKUP - verify screen, lines 266-283:
#   h2 "Verificacion en dos pasos", description "Enviamos un codigo de 6 digitos..."
#   label "Codigo de verificacion" ($muted 13px/600)
#   6 inputs: flex:1, height 62px, text-align center, font 24px/700 $ink,
#     border 1.5px $line, border-radius 10px, focus border $amber_hover + ring rgba(247,201,126,.25)
#   amber "Verificar" button; resend line "Reenviar en {n}s" ($muted)
@design/auth.dc.html

# Files being modified:
@apps/website/app/pages/login/verificar.vue
@apps/website/app/components/FormVerifyCode.vue
@apps/website/app/scss/components/_verify-code.scss

# Reference (Plans 02/03 - DO NOT edit):
@apps/website/app/scss/components/_form.scss
@apps/website/app/components/IntroduceAuth.vue

<interfaces>
- `_verify-code.scss` owns `.form--verify`, `.form--verify__label`, `.form--verify__digits`,
  `.form--verify__digits__input`, `.form--verify__resend`. It imports variables via
  `@use "../abstracts/variables" as *` (new tokens available).
- FormVerifyCode.vue keeps digit logic (handleDigitInput/Keydown/Paste), handleVerify,
  handleResend, resendCooldown - all UNCHANGED.
- The page (verificar.vue) owns the back link, title, description, and the resend button.
- lucide: `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle OTP boxes in _verify-code.scss (62px, 10px radius, $line, amber focus)</name>
  <read_first>
    - apps/website/app/scss/components/_verify-code.scss (full file - current 48px/$platinum/charcoal styling)
    - design/auth.dc.html line 275 (the OTP input inline styles to translate)
  </read_first>
  <action>
    Update `.form--verify` in `_verify-code.scss` to the mockup, using the new tokens:
    - `&__label`: `font-size: 13px; font-weight: 600; color: $muted; margin-bottom: 12px;`
    - `&__digits`: keep `display: flex; gap: 10px;` (the mockup uses gap 10px).
    - `&__digits__input`:
      `height: 62px; border: 1.5px solid $line; border-radius: 10px; background: #fff;`
      `font-size: 24px; font-weight: 700; color: $ink; text-align: center;`
      `&::placeholder { color: $placeholder; }`
      `&:focus { border-color: $amber_hover; box-shadow: 0 0 0 3px rgba(247, 201, 126, 0.25); }`
      Keep `flex: 1; min-width: 0; padding: 0; outline: none; caret-color:` (set caret to `$ink`).
    - `&__resend`: keep as-is (wrapper).
    Replace all `$platinum` / `$charcoal` references in this file with the new tokens. Use only tokens except the focus-ring rgba literal. BEM unchanged.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'height: 62px' apps/website/app/scss/components/_verify-code.scss && grep -q 'border-radius: 10px' apps/website/app/scss/components/_verify-code.scss && grep -q 'rgba(247, 201, 126, 0.25)' apps/website/app/scss/components/_verify-code.scss && grep -q '\$amber_hover' apps/website/app/scss/components/_verify-code.scss && ! grep -q '\$platinum' apps/website/app/scss/components/_verify-code.scss && ! grep -q '\$charcoal' apps/website/app/scss/components/_verify-code.scss && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - OTP input is `height: 62px`, `border: 1.5px solid $line`, `border-radius: 10px`
    - Focus uses `border-color: $amber_hover` + `box-shadow: 0 0 0 3px rgba(247, 201, 126, 0.25)`
    - `$platinum` and `$charcoal` no longer referenced in `_verify-code.scss`
    - label uses `$muted`; font 24px/700 `$ink`
  </acceptance_criteria>
  <done>OTP boxes match the mockup (62px, 10px radius, amber focus).</done>
</task>

<task type="auto">
  <name>Task 2: FormVerifyCode.vue submit amber; behavior unchanged</name>
  <read_first>
    - apps/website/app/components/FormVerifyCode.vue (full file - the Verificar button class + all digit/verify/resend logic)
    - design/auth.dc.html line 279 (amber "Verificar" button)
  </read_first>
  <action>
    The "Verificar" button in `FormVerifyCode.vue` already uses `btn btn--block btn--primary` - which Plan 02 now renders amber inside `.auth`. CONFIRM the class is `btn btn--block btn--primary` (amber) and leave it. Make NO logic changes: handleDigitInput/Keydown/Paste, handleVerify (`/auth/verify-code`, fetchUser, manager redirect, meStore reset, onboarding/redirect), handleResend, resendCooldown, defineExpose must all stay identical. This task is essentially a confirmation that the existing primary class produces the amber button; if the class differs, set it to `btn btn--block btn--primary`. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'btn btn--block btn--primary' apps/website/app/components/FormVerifyCode.vue && grep -q 'auth/verify-code' apps/website/app/components/FormVerifyCode.vue && grep -q 'handleResend' apps/website/app/components/FormVerifyCode.vue && grep -q 'defineExpose' apps/website/app/components/FormVerifyCode.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - "Verificar" button is `btn btn--block btn--primary` (amber via Plan 02)
    - handleVerify still posts to `/auth/verify-code`; redirect/resend logic unchanged
    - `defineExpose({ handleResend, resendCooldown, resending })` intact
  </acceptance_criteria>
  <done>Verify submit is amber; all OTP/verify/resend behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: verificar.vue lucide back link</name>
  <read_first>
    - apps/website/app/pages/login/verificar.vue (full file - back link img, title, description, resend button)
    - apps/website/app/pages/login/index.vue (lucide back-link pattern from Plan 04)
  </read_first>
  <action>
    In `pages/login/verificar.vue`, replace the back-link `<img :src="mobileMenuClose">` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keeping `<span>Volver</span>` and the NuxtLink to `/login`. Remove the unused `mobileMenuClose` import. Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`. Leave the title, description, the `auth__form__help` resend button + its `handleResend`/`resendCooldown` computed bindings, and `definePageMeta` UNCHANGED. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/login/verificar.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/login/verificar.vue && grep -q 'handleResend' apps/website/app/pages/login/verificar.vue && ! grep -q 'style="' apps/website/app/pages/login/verificar.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucide ChevronLeft in back link; `mobileMenuClose` import removed
    - Resend button + computed bindings (`handleResend`, `resendCooldown`, `resending`) unchanged
    - No inline `style=`
  </acceptance_criteria>
  <done>Verify page uses lucide back link; resend countdown intact.</done>
</task>

</tasks>

<verification>
- Verify screen matches the mockup (62px OTP boxes, amber focus, amber Verificar, lucide back link, resend line).
- `pnpm codacy` passes (no unused `mobileMenuClose`).
- Verification flow unchanged (auto-advance, paste, verify-code endpoint, redirect, resend).
</verification>

<success_criteria>
- AUTH-03: verify looks like the new mockup.
- TOK-03: lucide ChevronLeft applied.
- Zero behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-06-SUMMARY.md`
</output>
