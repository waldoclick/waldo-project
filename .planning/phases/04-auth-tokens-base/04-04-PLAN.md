---
phase: 04-auth-tokens-base
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - apps/website/app/pages/login/index.vue
  - apps/website/app/components/FormLogin.vue
autonomous: true
requirements: [AUTH-01, TOK-03]
must_haves:
  truths:
    - "The login screen matches the mockup: left-aligned title + subtitle, amber Google button FIRST, divider, email+password fields, white secondary 'Iniciar sesion' submit, help links"
    - "The back link uses a lucide ChevronLeft icon (not the mobile-menu-close image)"
    - "Login behavior is unchanged (pendingToken flow then /login/verificar, resend confirmation, validation)"
  artifacts:
    - path: "apps/website/app/pages/login/index.vue"
      provides: "Login page restyled to mockup order (Google first, divider, fields, secondary submit)"
      contains: "ChevronLeft"
  key_links:
    - from: "FormLogin.vue submit"
      to: "/auth/local then /login/verificar"
      via: "apiClient + router.push"
      pattern: "auth/local"
---

<objective>
Restyle the LOGIN screen (`pages/login/index.vue` + `FormLogin.vue`) to match `design/auth.dc.html`: left-aligned heading + subtitle, the amber Google button FIRST, the "o con tu correo" divider, the email/password fields with the password show/hide toggle, a white secondary "Iniciar sesion" submit, and the help links, with the back link switched to a lucide ChevronLeft. Behavior is untouched.

Purpose: AUTH-01. The shared tokens, brand panel, and form primitives already exist (Plans 01-03); this plan applies them to the login screen's specific markup and ordering.
Output: Login page renders the mockup layout/order; FormLogin submit becomes the white secondary button; back link uses lucide.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# THE MOCKUP — login screen, lines 70-104:
#   back link (top): lucide chevron-left + "Ir al inicio"
#   h2 "Ingresa a tu cuenta" (left, 31px/800), subtitle "Bienvenido de vuelta a Waldo.click(R)" (15px)
#   ORDER: amber Google button FIRST -> divider "o con tu correo" -> email -> password(+toggle)
#          -> white secondary "Iniciar sesion" submit -> help links
@design/auth.dc.html

# Files being modified:
@apps/website/app/pages/login/index.vue
@apps/website/app/components/FormLogin.vue

# Reference (already restyled in Plans 02/03 - DO NOT edit):
@apps/website/app/scss/components/_auth.scss
@apps/website/app/scss/components/_form.scss
@apps/website/app/components/LoginWithGoogle.vue
@apps/website/app/components/IntroduceAuth.vue

<interfaces>
- Page currently renders FormLogin THEN Google; mockup wants Google FIRST then divider then fields.
- Plan 02 added `.auth .btn--secondary` (white, $line border) and `.auth .btn--primary` (amber).
- `_auth.scss` exposes `.auth__form__back`, `.auth__form__title`, `.auth__form__separator`,
  `.auth__form__social`, `.auth__form__help`, `.auth__form__fields`, `.auth__form__description`.
- Subtitle: render as `<p class="auth__form__description">` (class already exists, auth-scoped) to avoid
  needing new SCSS in another plan's file.
- lucide import: `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder + restyle login page markup (Google first, divider, lucide back link, subtitle)</name>
  <read_first>
    - apps/website/app/pages/login/index.vue (full file - current order, back-link img, title, FormLogin, separator, LoginWithGoogle, help)
    - design/auth.dc.html lines 66-104 (back link, h2, subtitle, Google-first ordering, divider, help links)
    - apps/website/app/components/MenuUser.vue line ~112 (lucide import pattern)
    - apps/website/app/scss/components/_auth.scss (the `.auth__form__*` classes available)
  </read_first>
  <action>
    Edit `pages/login/index.vue` template to match the mockup ordering and back link, WITHOUT changing any data-loading, SEO, providers, or middleware logic:
    - Replace the back-link `<img :src="mobileMenuClose" ...>` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keeping the `<span>Ir al inicio</span>` and the `.auth__form__back` NuxtLink to `/`. Remove the now-unused `mobileMenuClose` import (Codacy).
    - Keep `<h1 class="auth__form__title title">Ingresa a tu cuenta</h1>`. Add a subtitle line directly under it: `<p class="auth__form__description">Bienvenido de vuelta a Waldo.click(R)</p>` (use the real (R) registered symbol as elsewhere in the page).
    - REORDER: move the `auth__form__social` block (`<LoginWithGoogle />`) ABOVE the `auth__form__fields` block, and place the `auth__form__separator` BETWEEN the Google button and the fields. Change the separator text from "o" to "o con tu correo".
    - Keep the `auth__form__help` links exactly (markup/targets unchanged).
    - Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`.
    Do NOT touch `useLazyAsyncData('providers')`, `$setSEO`, `$setStructuredData`, `definePageMeta`, or the title/subtitle/list constants used by IntroduceAuth. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/login/index.vue && grep -q 'o con tu correo' apps/website/app/pages/login/index.vue && grep -q 'Bienvenido de vuelta' apps/website/app/pages/login/index.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/login/index.vue && ! grep -q 'style="' apps/website/app/pages/login/index.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Imports and uses `ChevronLeft` from lucide-vue-next in the back link
    - Separator text reads "o con tu correo"
    - Subtitle "Bienvenido de vuelta" present as `.auth__form__description`
    - `<LoginWithGoogle />` renders BEFORE `<FormLogin />` in the template
    - `mobileMenuClose` import removed; no inline `style=`
    - providers/SEO/middleware logic unchanged
  </acceptance_criteria>
  <done>Login page renders Google-first, divider, fields, help links, with lucide back link.</done>
</task>

<task type="auto">
  <name>Task 2: FormLogin submit becomes white secondary; confirm password toggle styling</name>
  <read_first>
    - apps/website/app/components/FormLogin.vue (full file - submit button class, password toggle markup)
    - design/auth.dc.html lines 100 (the login submit is the white secondary "Iniciar sesion" button)
    - apps/website/app/scss/components/_form.scss (the `.auth .btn--secondary` override from Plan 02)
  </read_first>
  <action>
    In `FormLogin.vue`, change the main submit button class from `btn btn--block btn--primary` to `btn btn--block btn--secondary` (the mockup's login submit is the white secondary button, since Google is now the amber primary). Keep `:disabled`, `type="submit"`, the sending spinner text, and ALL script logic (schema, handleSubmit, pendingToken flow, resend-confirmation) EXACTLY as-is. The resend-confirmation button keeps `btn--secondary` (already correct). The password show/hide toggle markup (`.form-group--password__show-password` with "Mostrar"/"Ocultar") is already correct and styled by Plan 02 - leave it. Do NOT change validation, endpoints, or navigation. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'btn btn--block btn--secondary' apps/website/app/components/FormLogin.vue && grep -q 'auth/local' apps/website/app/components/FormLogin.vue && grep -q 'pendingToken' apps/website/app/components/FormLogin.vue && grep -q 'Mostrar' apps/website/app/components/FormLogin.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The submit button uses `btn--secondary` (white) not `btn--primary`
    - `handleSubmit` still posts to `/auth/local` and sets `pendingToken` (behavior unchanged)
    - Password toggle ("Mostrar"/"Ocultar") markup intact
    - No inline `style=`; no logic/validation changes
  </acceptance_criteria>
  <done>Login submit is the white secondary button; behavior unchanged.</done>
</task>

</tasks>

<verification>
- Login screen visually matches the mockup (Google first, divider, fields, white submit, help, lucide back link).
- `pnpm codacy` passes (no unused `mobileMenuClose`).
- Login flow still works (pendingToken to /login/verificar).
</verification>

<success_criteria>
- AUTH-01: login looks like the new mockup.
- TOK-03: lucide ChevronLeft used in the back link.
- Zero behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-04-SUMMARY.md`
</output>
