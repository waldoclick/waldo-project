---
phase: 04-auth-tokens-base
plan: 05
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - apps/website/app/pages/registro/index.vue
  - apps/website/app/pages/registro/confirmar.vue
  - apps/website/app/components/FormRegister.vue
autonomous: true
requirements: [AUTH-02, TOK-03]
must_haves:
  truths:
    - "The register screen matches the mockup: left-aligned title + subtitle, amber Google button FIRST, divider 'o con tus datos', step-1 fields (type/nombres/apellidos/RUT), step-2 fields (email/password+generate/repeat/3 checkboxes)"
    - "Step 1 'Siguiente' is the white secondary button; step 2 'Registrate' is the amber primary; step 2 shows the two-pill step indicator"
    - "The confirmar page back link uses a lucide ChevronLeft and renders the new shell"
    - "Register behavior is unchanged (2-step validation, username sanitization, reserved-username guard, register endpoint, email-confirmation branch)"
  artifacts:
    - path: "apps/website/app/components/FormRegister.vue"
      provides: "Restyled 2-step register form with step indicator + generate + amber/secondary buttons"
      contains: "form--register__steps"
  key_links:
    - from: "FormRegister.vue handleSubmit"
      to: "/auth/local/register"
      via: "apiClient"
      pattern: "auth/local/register"
---

<objective>
Restyle the REGISTER flow (`pages/registro/index.vue`, `pages/registro/confirmar.vue`, `FormRegister.vue`) to match `design/auth.dc.html`: amber Google button first, "o con tus datos" divider, the 2-step form (step 1 = type/nombres/apellidos/RUT with a white "Siguiente"; step 2 = email/password+generate/repeat/3 checkboxes with the two-pill step indicator and an amber "Registrate"), and lucide back links. Behavior untouched.

Purpose: AUTH-02. Applies the shared tokens/primitives (Plans 01-03) to the register screens and their specific 2-step markup + ordering.
Output: Register page reordered to mockup; FormRegister step buttons + step indicator + generate styled; confirmar page uses lucide back link.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# THE MOCKUP:
#   register step 1, lines 107-153: h2 "Crea tu cuenta", subtitle "Empieza gratis - 3 anuncios
#     incluidos", amber Google FIRST, divider "o con tus datos", select Tipo de cuenta,
#     grid Nombres/Apellidos, RUT, white secondary "Siguiente" (+ arrow-right), help "Inicia sesion"
#   register step 2, lines 156-213: two amber pills + "Paso 2 de 2 - acceso", email,
#     password (with "Generar segura" sparkles link + show/hide + strength + helper note),
#     repeat (+ match note), 3 checkboxes (age / privacidad / condiciones),
#     grid [white "Volver"] [amber "Registrate"]
@design/auth.dc.html

# Files being modified:
@apps/website/app/pages/registro/index.vue
@apps/website/app/pages/registro/confirmar.vue
@apps/website/app/components/FormRegister.vue

# Reference (already restyled in Plans 02/03 - DO NOT edit):
@apps/website/app/scss/components/_form.scss     # has .auth .btn--primary/--secondary + .form--register__steps* + generate/checkbox styling
@apps/website/app/components/IntroduceAuth.vue
@apps/website/app/components/LoginWithGoogle.vue

<interfaces>
- Plan 02 defined SCSS for `.form--register__steps`, `.form--register__steps__pill`,
  `.form--register__steps__label` (step indicator). This plan adds the MARKUP using those classes.
- Plan 02 added `.auth .btn--secondary` (white) and `.auth .btn--primary` (amber).
- FormRegister.vue keeps `step` ref (1/2), `getSchema`, `handleSubmit`, RUT formatting,
  reserved-username guard, username sanitization, register endpoint logic - all UNCHANGED.
- The page (registro/index.vue) owns the back link, title, subtitle, separator, social, help.
- lucide: `import { ChevronLeft as IconChevronLeft, ArrowRight as IconArrowRight } from "lucide-vue-next";`
  (ArrowRight only if you add it to the Siguiente button inside FormRegister).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder + restyle registro/index.vue (Google first, divider 'o con tus datos', subtitle, lucide back link)</name>
  <read_first>
    - apps/website/app/pages/registro/index.vue (full file - order, back-link img, title, FormRegister, separator, LoginWithGoogle, help)
    - design/auth.dc.html lines 107-153 (register step 1 layout + ordering)
    - apps/website/app/pages/login/index.vue (sibling page just restyled in Plan 04 - replicate its pattern per CLAUDE.md "replicate closest equivalent")
  </read_first>
  <action>
    Mirror the Plan 04 login changes for `pages/registro/index.vue`:
    - Back link: replace `<img :src="mobileMenuClose">` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keep `<span>Ir al inicio</span>`. Remove the unused `mobileMenuClose` import.
    - Keep `<h1 class="auth__form__title title">Crea tu cuenta</h1>`. Add subtitle `<p class="auth__form__description">Empieza gratis - 3 anuncios incluidos</p>` (use a real bullet "·" as in the mockup).
    - REORDER: `<LoginWithGoogle />` (auth__form__social) FIRST, then `auth__form__separator` (text "o con tus datos"), then `<FormRegister />` (auth__form__fields).
    - Keep the `auth__form__help` "Inicia sesion" link.
    - Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`.
    Do NOT change `useAsyncData('providers')`, SEO, structured data, middleware, or the IntroduceAuth title/subtitle/list constants. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/registro/index.vue && grep -q 'o con tus datos' apps/website/app/pages/registro/index.vue && grep -q 'Empieza gratis' apps/website/app/pages/registro/index.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/registro/index.vue && ! grep -q 'style="' apps/website/app/pages/registro/index.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucide ChevronLeft in back link; `mobileMenuClose` import removed
    - Separator text "o con tus datos"
    - Subtitle "Empieza gratis" present
    - `<LoginWithGoogle />` renders before `<FormRegister />`
    - providers/SEO/middleware unchanged; no inline `style=`
  </acceptance_criteria>
  <done>Register page reordered to mockup with lucide back link and subtitle.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle FormRegister.vue (step indicator, generate, button classes, RUT/checkbox markup)</name>
  <read_first>
    - apps/website/app/components/FormRegister.vue (full file - step 1/2 markup, buttons, generate, checkboxes, all script logic)
    - design/auth.dc.html lines 159-211 (step indicator pills, generate sparkles, button pairing, checkboxes)
    - apps/website/app/scss/components/_form.scss (the `.form--register__steps*` classes Plan 02 defined + generate/checkbox/password styling)
    - apps/website/app/components/MenuUser.vue line ~112 (lucide import pattern)
  </read_first>
  <action>
    Restyle the markup of `FormRegister.vue` to the mockup. Keep EVERY script block unchanged (form ref, step ref, getSchema, watch, RUT formatting, handleShowPassword, handleGeneratePassword, handleSubmit with username sanitization + reserved-username guard + register endpoint + email-confirmation branch). Markup changes only:
    - At the TOP of step 2 (`<div v-if="step === 2" ...>`), add the two-pill step indicator using the Plan 02 classes:
      `<div class="form--register__steps"><span class="form--register__steps__pill" /><span class="form--register__steps__pill" /><span class="form--register__steps__label">Paso 2 de 2 · acceso</span></div>`
    - "Generar segura" button: replace the literal `✦` glyph with a lucide Sparkles icon: `<IconSparkles :size="13" />` keeping the existing `.form-group--password__generate` button and text "Generar segura". Add `import { Sparkles as IconSparkles } from "lucide-vue-next";`. (Optional ArrowRight on "Siguiente" per mockup — add `<IconArrowRight :size="16" />` inside the Siguiente button and import it; if you add it, ensure it is used so Codacy is happy.)
    - Button classes in the `form__send` footer:
      * Step 1 "Siguiente": the single submit button currently is `btn btn--block btn--primary`. For step 1 it should be the white secondary. Since the same `<button>` serves both steps via `v-if` spans, split or class-bind: keep ONE submit button but bind its class so step 1 = `btn--secondary` and step 2 = `btn--primary`. Use `:class="['btn', 'btn--block', step === 1 ? 'btn--secondary' : 'btn--primary']"`. The "Volver" button (step 2 only) stays `btn btn--block btn--secondary`.
      * This keeps step 2 = amber "Registrate", step 1 = white "Siguiente", Volver = white — matching the mockup.
    - Keep the password show/hide toggle, PasswordStrength, the 3 checkboxes (age/privacidad/condiciones) markup and their `name` attributes EXACTLY (validation depends on them).
    - No inline `style=`. No unused imports (the `✦` removal must not leave dead refs).
    Do NOT alter: yup schemas, the `RESERVED_USERNAMES` guard, `form.value.username` sanitization, the `/auth/local/register` call, the jwt-vs-email-confirmation branching, or `router.push` targets.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'form--register__steps' apps/website/app/components/FormRegister.vue && grep -q 'Paso 2 de 2' apps/website/app/components/FormRegister.vue && grep -q 'Sparkles' apps/website/app/components/FormRegister.vue && grep -q "step === 1 ? 'btn--secondary' : 'btn--primary'" apps/website/app/components/FormRegister.vue && grep -q 'auth/local/register' apps/website/app/components/FormRegister.vue && grep -q 'RESERVED_USERNAMES' apps/website/app/components/FormRegister.vue && ! grep -q 'style="' apps/website/app/components/FormRegister.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Step 2 contains the `.form--register__steps` indicator + "Paso 2 de 2 · acceso"
    - "Generar segura" uses lucide Sparkles (no `✦` literal left)
    - Submit button class is `step === 1 ? 'btn--secondary' : 'btn--primary'`
    - `handleSubmit`, `getSchema`, RUT watch, `RESERVED_USERNAMES`, `/auth/local/register` all unchanged
    - 3 checkbox `name` attributes (accepted_age_confirmation / accepted_terms / accepted_usage_terms) intact
    - No inline `style=`; no unused imports
  </acceptance_criteria>
  <done>Register form shows step indicator, lucide generate, correct amber/secondary buttons; behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: confirmar.vue lucide back link + subtitle</name>
  <read_first>
    - apps/website/app/pages/registro/confirmar.vue (full file - back link img, title, description, resend)
    - design/auth.dc.html lines 62-68 (back link pattern)
    - apps/website/app/pages/login/index.vue (lucide back-link pattern from Plan 04)
  </read_first>
  <action>
    In `pages/registro/confirmar.vue`, replace the back-link `<img :src="mobileMenuClose">` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keep `<span>Volver</span>` and the NuxtLink to `/registro`. Remove the unused `mobileMenuClose` import. Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`. Leave the title, the `.auth__form__description` confirmation text, the resend countdown logic, and `definePageMeta` UNCHANGED. The page already inherits the new brand panel via `.auth` (no further change needed). No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/registro/confirmar.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/registro/confirmar.vue && grep -q 'startCountdown' apps/website/app/pages/registro/confirmar.vue && ! grep -q 'style="' apps/website/app/pages/registro/confirmar.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucide ChevronLeft in the back link; `mobileMenuClose` import removed
    - Resend countdown logic (`startCountdown`, `handleResend`) unchanged
    - No inline `style=`
  </acceptance_criteria>
  <done>Confirmar page uses lucide back link and the new shell; behavior unchanged.</done>
</task>

</tasks>

<verification>
- Register screens (step 1, step 2, confirmar) match the mockup.
- `pnpm codacy` passes (no unused imports, no `✦` literal, BEM compliant).
- Registration flow unchanged (2-step validation, reserved-username, register endpoint, email-confirmation branch).
</verification>

<success_criteria>
- AUTH-02: register looks like the new mockup (both steps + confirmar).
- TOK-03: lucide Sparkles/ChevronLeft applied.
- Zero behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-05-SUMMARY.md`
</output>
