---
phase: 04-auth-tokens-base
plan: 07
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - apps/website/app/pages/recuperar-contrasena.vue
  - apps/website/app/pages/restablecer-contrasena.vue
  - apps/website/app/components/FormForgotPassword.vue
  - apps/website/app/components/FormResetPassword.vue
autonomous: true
requirements: [AUTH-04, TOK-03]
must_haves:
  truths:
    - "The recover screen matches the mockup: left-aligned title + description, email field, amber 'Enviar enlace' button, 'Inicia sesion' help link, lucide back link"
    - "The reset screen matches the mockup: title + description, new-password field (with generate + show/hide + strength + helper note), repeat field (with match note), amber 'Guardar contrasena' button, lucide back link"
    - "Both screens' behavior is unchanged (forgot-password / reset-password endpoints, validation, token guard, generate)"
    - "recuperar-contrasena.vue no longer imports the unused LoginWithGoogle / LoginWithFacebook components"
  artifacts:
    - path: "apps/website/app/components/FormResetPassword.vue"
      provides: "Reset form restyled with lucide generate + amber submit"
      contains: "Sparkles"
  key_links:
    - from: "FormForgotPassword.vue onSubmit"
      to: "/auth/forgot-password"
      via: "apiClient"
      pattern: "auth/forgot-password"
    - from: "FormResetPassword.vue onSubmit"
      to: "/auth/reset-password"
      via: "apiClient"
      pattern: "auth/reset-password"
---

<objective>
Restyle the RECOVER and RESET screens (`recuperar-contrasena.vue`, `restablecer-contrasena.vue`, `FormForgotPassword.vue`, `FormResetPassword.vue`) to match `design/auth.dc.html`: amber primary submit buttons, lucide back links, lucide "Generar segura" on reset, and the description copy from the mockup. Remove the dead `LoginWithGoogle`/`LoginWithFacebook` imports from the recover page. Behavior untouched.

Purpose: AUTH-04. Applies the shared tokens/primitives to the two password-recovery screens and their specific markup.
Output: Recover + reset screens restyled to mockup; dead imports removed; endpoints/validation unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-auth-tokens-base/04-CONTEXT.md
@CLAUDE.md

# THE MOCKUP:
#   recover, lines 215-230: h2 "Recupera tu contrasena", description
#     "Introduce tu correo y te enviaremos un enlace...", email field,
#     amber "Enviar enlace", help "Inicia sesion"
#   reset, lines 232-264: h2 "Crea una nueva contrasena", description
#     "Elige una contrasena nueva y segura...", password (Generar segura sparkles + show/hide
#     + strength + helper note), repeat (+ match note), amber "Guardar contrasena", help "Inicia sesion"
@design/auth.dc.html

# Files being modified:
@apps/website/app/pages/recuperar-contrasena.vue
@apps/website/app/pages/restablecer-contrasena.vue
@apps/website/app/components/FormForgotPassword.vue
@apps/website/app/components/FormResetPassword.vue

# Reference (Plans 02/03 - DO NOT edit):
@apps/website/app/scss/components/_form.scss     # .auth .btn--primary (amber) + generate/password/strength styling
@apps/website/app/components/IntroduceAuth.vue
@apps/website/app/pages/login/index.vue          # lucide back-link pattern (Plan 04)

<interfaces>
- FormForgotPassword.vue submit is already `btn btn--block btn--primary` (amber via Plan 02) - keep.
- FormResetPassword.vue submit is already `btn btn--block btn--primary` (amber) - keep; it has a
  `✦ Generar segura` button (`.form-group--password__generate`) to lucide-ify.
- Both pages own the back link, title, description, and help link.
- recuperar-contrasena.vue currently imports LoginWithGoogle AND LoginWithFacebook but renders NEITHER
  (dead imports - Codacy unused-import violation) - remove them.
- lucide: `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";` (pages),
  `import { Sparkles as IconSparkles } from "lucide-vue-next";` (FormResetPassword).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: recuperar-contrasena.vue lucide back link + remove dead imports + description</name>
  <read_first>
    - apps/website/app/pages/recuperar-contrasena.vue (full file - back-link img, title, FormForgotPassword, help, the dead LoginWithGoogle/LoginWithFacebook imports)
    - design/auth.dc.html lines 215-230 (recover layout + description copy)
    - apps/website/app/pages/login/index.vue (lucide back-link pattern)
  </read_first>
  <action>
    Edit `pages/recuperar-contrasena.vue`:
    - Replace the back-link `<img :src="mobileMenuClose">` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keep `<span>Ir al inicio</span>` and NuxtLink to `/`. Remove the unused `mobileMenuClose` import.
    - REMOVE the dead imports `import LoginWithGoogle from "@/components/LoginWithGoogle.vue";` and `import LoginWithFacebook from "@/components/LoginWithFacebook.vue";` (neither is rendered - Codacy unused-import).
    - Keep `<h1 class="auth__form__title title">Recupera tu contrasena</h1>`. Add description `<p class="auth__form__description">Introduce tu correo y te enviaremos un enlace para crear una nueva contrasena.</p>` directly under the title (use the real accent characters as in the existing page).
    - Keep `<FormForgotPassword />` and the `auth__form__help` "Inicia sesion" link.
    - Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`.
    Do NOT change SEO, structured data, the IntroduceAuth title/subtitle/list constants, or middleware. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/recuperar-contrasena.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/recuperar-contrasena.vue && ! grep -q 'LoginWithGoogle' apps/website/app/pages/recuperar-contrasena.vue && ! grep -q 'LoginWithFacebook' apps/website/app/pages/recuperar-contrasena.vue && ! grep -q 'style="' apps/website/app/pages/recuperar-contrasena.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucide ChevronLeft in back link; `mobileMenuClose` import removed
    - `LoginWithGoogle` and `LoginWithFacebook` imports removed (no longer referenced)
    - Description copy from the mockup present as `.auth__form__description`
    - SEO/middleware unchanged; no inline `style=`
  </acceptance_criteria>
  <done>Recover page restyled, dead imports gone, lucide back link.</done>
</task>

<task type="auto">
  <name>Task 2: restablecer-contrasena.vue lucide back link + description</name>
  <read_first>
    - apps/website/app/pages/restablecer-contrasena.vue (full file - back-link img, title, FormResetPassword, help)
    - design/auth.dc.html lines 232-264 (reset layout + description copy)
  </read_first>
  <action>
    Edit `pages/restablecer-contrasena.vue`:
    - Replace the back-link `<img :src="mobileMenuCloseSrc">` with `<IconChevronLeft :size="17" :stroke-width="2.2" />`, keep `<span>Ir al inicio</span>` and NuxtLink to `/`. Remove the unused `mobileMenuClose` import and the `mobileMenuCloseSrc` const.
    - Keep `<h2 class="auth__form__title title">Restablece tu contrasena</h2>`. Add description `<p class="auth__form__description">Elige una contrasena nueva y segura para tu cuenta. No podras reutilizar la anterior.</p>` under the title.
    - Keep `<FormResetPassword />` and the `auth__form__help` "Inicia sesion" link.
    - Add `import { ChevronLeft as IconChevronLeft } from "lucide-vue-next";`.
    Do NOT change the `page.data` SEO object, structured data, or middleware. The `page.data.list` still feeds IntroduceAuth - leave it. No inline `style=`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'ChevronLeft' apps/website/app/pages/restablecer-contrasena.vue && ! grep -q 'mobileMenuClose' apps/website/app/pages/restablecer-contrasena.vue && ! grep -q 'style="' apps/website/app/pages/restablecer-contrasena.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - lucide ChevronLeft in back link; `mobileMenuClose` import AND `mobileMenuCloseSrc` const removed
    - Description copy from the mockup present
    - No inline `style=`; SEO/middleware unchanged
  </acceptance_criteria>
  <done>Reset page restyled with lucide back link and description.</done>
</task>

<task type="auto">
  <name>Task 3: FormForgotPassword.vue + FormResetPassword.vue button/icon confirm + lucide generate</name>
  <read_first>
    - apps/website/app/components/FormForgotPassword.vue (full file - submit button, onSubmit logic)
    - apps/website/app/components/FormResetPassword.vue (full file - generate button with ✦, submit, onSubmit, token guard)
    - design/auth.dc.html lines 226 (Enviar enlace), 241 (Generar segura sparkles), 260 (Guardar contrasena)
    - apps/website/app/components/MenuUser.vue line ~112 (lucide import pattern)
  </read_first>
  <action>
    - `FormForgotPassword.vue`: the submit button is already `btn btn--block btn--primary` (amber via Plan 02). CONFIRM and leave it; the text "Enviar" matches "Enviar enlace" intent (keep the existing "Enviar"/"Enviando…" text, or update the idle label to "Enviar enlace" to match the mockup - low-risk text change). Make NO change to the yup schema, `onSubmit` (`/auth/forgot-password`), or `router.push`. No inline `style=`.
    - `FormResetPassword.vue`: replace the literal `✦` in the `.form-group--password__generate` button with `<IconSparkles :size="13" />`, keeping the text "Generar segura". Add `import { Sparkles as IconSparkles } from "lucide-vue-next";`. The submit button stays `btn btn--block btn--primary` (amber); keep its "Restablecer Contrasena"/"Restableciendo…" text (mockup says "Guardar contrasena" - you MAY update the idle label to "Guardar contrasena" to match, but this is the only text touch allowed). Keep `PasswordStrength`, the show/hide toggle, the repeat field, the `route.query.token` guard, `handleGeneratePassword`, and `onSubmit` (`/auth/reset-password`) EXACTLY. No `✦` literal left. No inline `style=`. No unused imports.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q 'auth/forgot-password' apps/website/app/components/FormForgotPassword.vue && grep -q 'btn btn--block btn--primary' apps/website/app/components/FormForgotPassword.vue && grep -q 'Sparkles' apps/website/app/components/FormResetPassword.vue && ! grep -q '✦' apps/website/app/components/FormResetPassword.vue && grep -q 'auth/reset-password' apps/website/app/components/FormResetPassword.vue && grep -q 'PasswordStrength' apps/website/app/components/FormResetPassword.vue && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - FormForgotPassword submit is amber `btn--primary`; `onSubmit` still posts `/auth/forgot-password`
    - FormResetPassword "Generar segura" uses lucide Sparkles (no `✦` literal)
    - FormResetPassword submit amber; `onSubmit` still posts `/auth/reset-password`; token guard + PasswordStrength + generate intact
    - No inline `style=`; no unused imports
  </acceptance_criteria>
  <done>Both recovery forms styled to mockup (amber submit, lucide generate); endpoints/validation unchanged.</done>
</task>

</tasks>

<verification>
- Recover + reset screens match the mockup.
- `pnpm codacy` passes (no unused imports incl. removed LoginWithGoogle/LoginWithFacebook, no `✦` literal).
- forgot-password / reset-password flows unchanged (endpoints, validation, token guard).
</verification>

<success_criteria>
- AUTH-04: reset/forgot look like the new mockup.
- TOK-03: lucide ChevronLeft + Sparkles applied.
- Zero behavior changes; dead imports removed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-auth-tokens-base/04-07-SUMMARY.md`
</output>
