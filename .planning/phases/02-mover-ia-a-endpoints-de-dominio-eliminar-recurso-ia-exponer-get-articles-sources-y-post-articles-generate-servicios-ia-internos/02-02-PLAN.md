---
phase: 02-mover-ia-a-endpoints-de-dominio-eliminar-recurso-ia-exponer-get-articles-sources-y-post-articles-generate-servicios-ia-internos
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - apps/website/app/components/LightBoxArticles.vue
  - apps/strapi/src/api/ia/routes/ia.ts
  - apps/strapi/src/api/ia/controllers/ia.ts
  - apps/strapi/src/api/search/routes/search.ts
  - apps/strapi/src/api/search/controllers/search.ts
autonomous: false

must_haves:
  truths:
    - "The dashboard news flow (search → select → generate → save) works end-to-end against the new domain endpoints"
    - "The frontend no longer references /ia/ or /search/tavily and no longer builds the AI prompt or selects a provider"
    - "The ia resource (routes + controller) no longer exists"
    - "The search resource (routes + controller) no longer exists"
    - "Saving a generated article still uses POST /articles unchanged"
  artifacts:
    - path: "apps/website/app/components/LightBoxArticles.vue"
      provides: "Dashboard article creation UI wired to /articles/sources and /articles/generate"
      contains: "/articles/generate"
  key_links:
    - from: "apps/website/app/components/LightBoxArticles.vue"
      to: "/articles/sources"
      via: "client GET with q param"
      pattern: "/articles/sources"
    - from: "apps/website/app/components/LightBoxArticles.vue"
      to: "/articles/generate"
      via: "client POST with selected source"
      pattern: "/articles/generate"
---

<objective>
Cut the dashboard over to the new domain endpoints and delete the now-orphaned `ia` and `search`
resources. The frontend stops building the AI prompt and stops choosing a provider; it calls
`GET /articles/sources` and `POST /articles/generate`. Saving stays on `POST /articles`. Because the
frontend is the only consumer (verified) and we control it in the same change, the old routes are
removed immediately with no deprecation window (D-10).

Purpose: Complete the anti-pattern correction — AI becomes invisible to the client (D-06, specifics),
and the dead `ia`/`search` surfaces are gone.
Output: Updated LightBoxArticles.vue and deleted `api/ia` + `api/search` directories.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-mover-ia-a-endpoints-de-dominio-eliminar-recurso-ia-exponer-get-articles-sources-y-post-articles-generate-servicios-ia-internos/02-CONTEXT.md
@.planning/phases/02-mover-ia-a-endpoints-de-dominio-eliminar-recurso-ia-exponer-get-articles-sources-y-post-articles-generate-servicios-ia-internos/02-01-PLAN.md

<interfaces>
<!-- New endpoints created in Plan 01 (Wave 1). Use these directly. -->

GET /articles/sources?q=<query>   (policy: isManager)
  → response: { sources: Array<{ title, link, snippet, date, source, imageUrl? }> }

POST /articles/generate           (policy: isManager)
  body: { source: { title, link, snippet, date } }
  → response: { text: string }   // raw AI draft text, JSON-string body (D-08). NOT persisted.

POST /articles (UNCHANGED — still the save step)
  body: { data: { title, header, body, seo_title, seo_description, source_url, is_published } }

<!-- Current frontend (apps/website/app/components/LightBoxArticles.vue) facts: -->
- line 275: fetchFromApi() calls client("/search/tavily", { method:"POST", body:{ query:q, num:10 } }), reads result.news.
- lines 160-197: DEFAULT_GEMINI_PROMPT — the prompt template (now lives in the backend; delete from here).
- line 230: geminiPrompt ref initialized to DEFAULT_GEMINI_PROMPT.
- line 248: watch resets geminiPrompt on open.
- lines 269-272: goToPrompt() sets geminiPrompt + currentStep=3.
- Step 3 template (lines 114-149): a "Prompt" textarea bound to geminiPrompt + "Crear" button → handleGenerate.
- lines 374-386: handleGenerate builds fullPrompt (template + article context) then client("/ia/cerebras", { body:{ prompt: fullPrompt } }).
- lines 388-399: JSON.parse(result.text) into { title, header, body, seo_title, seo_description }, then articlesStore.setAICache.
- lines 402-426: duplicate check via GET /articles?filters[source_url][$eq]=link.
- lines 428-442: save via POST /articles with { data: {...} }. KEEP THIS UNCHANGED.

useApiClient() routes through the Nuxt proxy which injects the Authorization header server-side — no manual auth needed.
search.store (useSearchStore) cache is kept (D-09 discretion); only the fetch endpoint changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut LightBoxArticles over to the domain endpoints; stop building prompt/provider</name>
  <read_first>
    - apps/website/app/components/LightBoxArticles.vue (FULL file — the component being rewritten)
    - apps/website/app/stores/search.store.ts (useSearchStore cache API — kept)
    - apps/website/app/stores/articles.store.ts (AI cache + POST /articles save — kept)
    - .planning/phases/02-mover-ia-a-endpoints-de-dominio-eliminar-recurso-ia-exponer-get-articles-sources-y-post-articles-generate-servicios-ia-internos/02-01-PLAN.md (new endpoint contracts)
  </read_first>
  <action>
    Update apps/website/app/components/LightBoxArticles.vue so the AI is invisible to the client
    (D-06, specifics). Make these minimal-churn changes:

    1) Search call — `fetchFromApi(q)` (line ~274): change the endpoint from `"/search/tavily"` to
       `"/articles/sources"`, switch to a GET with the query as a `q` param, and read the new domain
       key `sources` instead of `news`:
       ```ts
       const result = await client<{ sources: ITavilyResult[] }>("/articles/sources", {
         method: "GET",
         params: { q },
       });
       const news = result.sources || [];
       searchStore.setTavily(q, news);
       return news;
       ```
       (Keep the searchStore cache behavior — D-09.)

    2) Generate call — in `handleGenerate` (lines ~372-396): REMOVE the client-side prompt assembly
       (the `fullPrompt` template concatenation) and the `/ia/cerebras` call. Replace with a single
       call to the domain generate endpoint passing the SELECTED SOURCE (domain data, not a prompt):
       ```ts
       const result = await client<{ text: string }>("/articles/generate", {
         method: "POST",
         body: {
           source: {
             title: item.title,
             link: item.link,
             snippet: bodyText,
             date: item.date ?? "",
           },
         },
       });
       ```
       Keep the existing `JSON.parse(result.text)` into { title, header, body, seo_title, seo_description }
       and the `articlesStore.setAICache(item.link, parsed)` call exactly as-is.

    3) Remove the now-dead prompt UI (forced consequence of D-06 — the client no longer owns the prompt):
       - Delete the `DEFAULT_GEMINI_PROMPT` constant (lines ~160-197).
       - Delete the `geminiPrompt` ref (line ~230) and every reference to it (the `geminiPrompt.value = ...`
         resets in the `watch` line ~248 and in `goToPrompt` lines ~269-272).
       - Delete Step 3's prompt textarea + its label (lines ~123-130). Collapse Step 3 to a simple
         confirmation step: keep the header ("Generar artículo") and the actions row (← Volver / Crear),
         change the subtitle to "Revisa la noticia seleccionada y genera el artículo con IA", and change
         the "Crear" button `:disabled` from `loading || !geminiPrompt.trim()` to just `loading`.
       - `goToPrompt()` keeps `currentStep.value = 3` but drops the geminiPrompt line.
    4) The duplicate-check (GET /articles?filters) and the save (`POST /articles` with { data })
       at lines ~402-442 MUST remain unchanged.
    5) Remove any now-unused imports/vars left behind (CLAUDE.md: never leave unused vars/imports —
       do not `_`-prefix). Do NOT change Spanish UI labels other than the one subtitle noted above.
  </action>
  <verify>
    <automated>cd apps/website && ! grep -q "/ia/" app/components/LightBoxArticles.vue && ! grep -q "/search/tavily" app/components/LightBoxArticles.vue && ! grep -q "DEFAULT_GEMINI_PROMPT" app/components/LightBoxArticles.vue && ! grep -q "geminiPrompt" app/components/LightBoxArticles.vue && grep -q "/articles/sources" app/components/LightBoxArticles.vue && grep -q "/articles/generate" app/components/LightBoxArticles.vue && grep -q '"/articles"' app/components/LightBoxArticles.vue && echo FRONT_CUTOVER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "/ia/" LightBoxArticles.vue` → no match (provider call gone)
    - `grep "/search/tavily" LightBoxArticles.vue` → no match (search endpoint replaced)
    - `grep "DEFAULT_GEMINI_PROMPT" LightBoxArticles.vue` → no match (prompt template gone)
    - `grep "geminiPrompt" LightBoxArticles.vue` → no match (prompt ref + UI gone)
    - `grep "/articles/sources" LightBoxArticles.vue` and `grep "/articles/generate" LightBoxArticles.vue` → both match
    - `grep '"/articles"' LightBoxArticles.vue` → still matches (save + dup-check unchanged)
    - `cd apps/website && node ../../node_modules/.bin/vue-tsc --noEmit` exits 0 (no unused-var / type errors introduced)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete the ia and search resources</name>
  <read_first>
    - apps/strapi/src/api/ia/routes/ia.ts (to be deleted)
    - apps/strapi/src/api/ia/controllers/ia.ts (to be deleted)
    - apps/strapi/src/api/search/routes/search.ts (to be deleted)
    - apps/strapi/src/api/search/controllers/search.ts (to be deleted)
    - 02-02-PLAN.md Task 1 (confirms LightBoxArticles is migrated — these resources are now orphaned)
  </read_first>
  <action>
    Delete the two orphaned resources entirely (D-04, D-10). Both were exposed-internal-service
    anti-patterns whose sole consumer (LightBoxArticles) is migrated in Task 1.
    - Remove the directory `apps/strapi/src/api/ia/` (routes/ia.ts + controllers/ia.ts) with `git rm -r apps/strapi/src/api/ia`.
    - Remove the directory `apps/strapi/src/api/search/` (routes/search.ts + controllers/search.ts) with `git rm -r apps/strapi/src/api/search`.
    Do NOT delete any `apps/strapi/src/services/*` — the AI services and the tavily service stay
    internal and are consumed by the article controller (D-05). Do NOT touch `services/search-console`
    (a different, unrelated service).
    After deletion, confirm nothing else imports the deleted resources (there should be no importers —
    services are imported directly, not via the api resources).
  </action>
  <verify>
    <automated>cd apps/strapi && test ! -d src/api/ia && test ! -d src/api/search && grep -rn "api/ia/\|api/search/\|ia.cerebras\|search.tavily" src && echo SHOULD_BE_EMPTY_ABOVE; test -z "$(grep -rn 'api::ia\|api::search' src)" && echo RESOURCES_DELETED</automated>
  </verify>
  <acceptance_criteria>
    - Directory `apps/strapi/src/api/ia/` does not exist
    - Directory `apps/strapi/src/api/search/` does not exist
    - `grep -rn "api::ia\|api::search" apps/strapi/src` → no matches
    - `grep -rn "ia.cerebras\|search.tavily" apps/strapi/src` → no matches
    - `apps/strapi/src/services/cerebras/`, `.../tavily/`, `.../anthropic/`, `.../gemini/`, `.../groq/`, `.../deepseek/` all still exist (services kept — D-05)
    - `apps/strapi/src/api/article/routes/00-article-custom.ts` still exists (new domain routes intact)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard news flow end-to-end</name>
  <action>
    Human-verify the dashboard news flow end-to-end after the cutover. Claude has already automated
    everything (frontend wiring + resource deletion in Tasks 1-2); this task only confirms behavior is
    unchanged. Follow the steps in <how-to-verify> and pause for the resume signal.
  </action>
  <what-built>
    LightBoxArticles now searches via GET /articles/sources, generates via POST /articles/generate
    (backend builds the prompt + selects the provider with a fallback chain, default Cerebras), and
    saves via the unchanged POST /articles. The /ia and /search resources are deleted.
  </what-built>
  <how-to-verify>
    1. Start the stack (Strapi + website) and log in to the dashboard as a Manager user.
    2. Open the article-creation lightbox (the "+ / crear artículo" action that opens LightBoxArticles).
    3. Step 1: enter a search query (the default industrial-news query is fine) and click "Siguiente →".
       Expect: results table populates (data came from GET /articles/sources).
    4. Step 2: select one news item, click "Siguiente →".
       Expect: Step 3 shows the confirmation view with NO prompt textarea, just "← Volver" and "Crear".
    5. Step 3: click "Crear".
       Expect: an article draft is generated and created; you land on / the lightbox closes / the new
       draft appears in the articles list (same behavior as before the refactor).
    6. Repeat the same source once more.
       Expect: the "Esta noticia ya existe" duplicate warning appears (dup-check via POST /articles path intact).
    7. (Optional) In the browser network tab confirm there are NO calls to /ia/* or /search/tavily —
       only /articles/sources, /articles/generate, and /articles.
  </how-to-verify>
  <resume-signal>Type "approved" if the full search→select→generate→save flow works and behaves as before, or describe any issue.</resume-signal>
</task>

</tasks>

<verification>
- Frontend references no `/ia/` or `/search/tavily`; prompt template + provider choice are gone from the client.
- `api/ia` and `api/search` directories are deleted; no `api::ia` / `api::search` references remain.
- AI + tavily services remain internal (D-05) and are reachable only via the article domain endpoints.
- Human verification confirms the dashboard news flow is unchanged in behavior.
</verification>

<success_criteria>
- The dashboard creates news articles via the domain endpoints with identical behavior to before.
- The `ia` and `search` resources no longer exist; the AI is invisible to the client.
- `POST /articles` save path is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mover-ia-a-endpoints-de-dominio-eliminar-recurso-ia-exponer-get-articles-sources-y-post-articles-generate-servicios-ia-internos/02-02-SUMMARY.md`
</output>
