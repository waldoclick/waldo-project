---
phase: quick
plan: 260619-n4i
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/ad-view/services/ad-view.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Ad detail page shows a non-zero views count after visiting the page as the ad owner"
    - "WhatsApp button renders in the sidebar when the seller has a whatsapp number in the DB"
  artifacts:
    - path: "apps/strapi/src/api/ad-view/services/ad-view.ts"
      provides: "recordView without owner exclusion guard"
  key_links:
    - from: "apps/strapi/src/api/ad-view/services/ad-view.ts"
      to: "api::ad-view.ad-view DB table"
      via: "strapi.db.query create"
      pattern: "strapi\\.db\\.query.*ad-view.*create"
---

<objective>
Fix two broken features on the ad detail page (/anuncios/[slug]):
1. Views counter always shows 0 — caused by the owner-exclusion guard in recordView preventing any row from being inserted during local dev (the test user IS the ad owner).
2. WhatsApp button never appears — the seller (gabrielburgos, id=2) has no whatsapp value in the SQLite DB, so hasWhatsapp resolves false.

Purpose: Make the ad detail page functional for local dev verification.
Output: Views increment on page load; WhatsApp CTA renders in the sidebar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260619-n4i-fix-views-counter-and-whatsapp-button-on/260619-n4i-RESEARCH.md

Key facts from research:
- Owner exclusion: apps/strapi/src/api/ad-view/services/ad-view.ts line 73
  `if (viewerId && adUser?.id === viewerId) return;`
  Test user gabrielburgos (id=2) IS the ad owner → zero views ever inserted.
- sha256 visitor_hash dedup (ip+ua+day) at line 86 already prevents day-inflation.
- WhatsApp flow: AdSingle.vue hasWhatsapp = !!(u?.has_whatsapp || u?.whatsapp)
  Manager bypass skips sanitizeAdForPublic → reads raw u.whatsapp from DB.
  If DB value is null → hasWhatsapp = false → button hidden.
- SQLite DB: apps/strapi/.tmp/data.db, user id=2 = gabrielburgos.
- No frontend changes needed — pipeline, CSS, and HeroAnnouncement template are all correct.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove owner-exclusion guard from recordView</name>
  <files>apps/strapi/src/api/ad-view/services/ad-view.ts</files>
  <action>
Remove lines 71–73 (the "Step 3: Owner exclusion" comment block + guard):

```
        // Step 3: Owner exclusion
        const adUser = ad.user as Record<string, unknown> | null | undefined;
        if (viewerId && adUser?.id === viewerId) return;
```

The adUser variable is declared solely for this guard. After removing the guard, adUser is unused — delete it too. The `ad` variable is still needed for Step 5 (`ad.id`), so keep it.

Also update the JSDoc block at line 37–38:
- Remove: `* - Owner exclusion: if viewerId === ad.user.id, returns early (no row).`

And update the class-level JSDoc at line 6:
- Remove: `* Records one row per visitor per day, excluding the ad owner.`
- Replace with: `* Records one row per visitor per day (sha256 dedup: ip+ua+day).`

Rationale: visitor_hash dedup already prevents day-level inflation. Owners legitimately visit their own pages. The exclusion made sense in theory but broke local dev where the only test account is the ad owner.

After editing, confirm TypeScript is clean:
```
cd /home/gab/Code/waldo-project/apps/strapi && npx tsc --noEmit 2>&1 | head -20
```

Then commit:
```
git add apps/strapi/src/api/ad-view/services/ad-view.ts
git commit -m "fix(ad-view): remove owner-exclusion guard from recordView — visitor_hash dedup prevents inflation"
```

Restart Strapi for the change to take effect (TypeScript is compiled at startup).
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project/apps/strapi && npx tsc --noEmit 2>&1; grep -n "adUser" apps/strapi/src/api/ad-view/services/ad-view.ts | wc -l</automated>
  </verify>
  <done>
    - Line 73 guard is gone (grep "adUser" returns 0 hits)
    - TypeScript exits 0
    - Commit created
    - After Strapi restart, visiting /anuncios/[slug] as owner inserts a row → views count increments
  </done>
</task>

<task type="auto">
  <name>Task 2: Set whatsapp number for test user in SQLite</name>
  <files>apps/strapi/.tmp/data.db</files>
  <action>
Set the whatsapp field for user id=2 (gabrielburgos) in the SQLite database:

```bash
sqlite3 /home/gab/Code/waldo-project/apps/strapi/.tmp/data.db \
  "UPDATE up_users SET whatsapp='+56912345678' WHERE id=2;"
```

Verify the update:
```bash
sqlite3 /home/gab/Code/waldo-project/apps/strapi/.tmp/data.db \
  "SELECT id, username, whatsapp FROM up_users WHERE id=2;"
```

Expected output: `2|gabrielburgos|+56912345678`

No Strapi restart required — DB reads are live. The manager bypass path (ad.ts:942) skips sanitizeAdForPublic and reads raw user.whatsapp directly from the populate result. After this update, AdSingle.vue will resolve hasWhatsapp = !!(undefined || '+56912345678') = true and render the WhatsApp button.

No git commit needed — .tmp/data.db is gitignored (local dev data only).
  </action>
  <verify>
    <automated>sqlite3 /home/gab/Code/waldo-project/apps/strapi/.tmp/data.db "SELECT whatsapp FROM up_users WHERE id=2;"</automated>
  </verify>
  <done>
    - sqlite3 query returns '+56912345678'
    - Visiting /anuncios/[slug] renders the WhatsApp button in the sidebar
  </done>
</task>

</tasks>

<verification>
After both tasks and Strapi restart:
1. Visit http://localhost:3000/anuncios/[any-slug-owned-by-gabrielburgos]
2. Views count shows > 0 (increments on each new day or first visit of the day)
3. WhatsApp button visible in sidebar contact card
4. TypeScript clean: `cd apps/strapi && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- recordView no longer exits early for the ad owner → view rows are inserted
- User id=2 has whatsapp='+56912345678' in SQLite → button renders
- No frontend files modified
- No CSS modified
- Strapi TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260619-n4i-fix-views-counter-and-whatsapp-button-on/260619-n4i-SUMMARY.md`
</output>
