# Quick Task 260619-t66: Align blog list + article to maqueta — Context

**Gathered:** 2026-06-20
**Status:** Ready for planning

<domain>
## Task Boundary

Align /blog (list) and /blog/[slug] (article) to the Claude Design maqueta
("Waldoclick" project, index.dc.html sections BLOG LISTADO + BLOG ARTÍCULO, and
BlogCard.dc.html). Deep review — several pieces are missing, especially on the
article page.

Reference material staged in this task's `reference/`:
- maqueta-blog-list.html (index.dc.html lines 1275–1351)
- maqueta-blog-article.html (index.dc.html lines 1353–1453)
- BlogCard.dc.html content (see below / re-fetch via DesignSync if needed)
</domain>

<decisions>
## Implementation Decisions (locked by user)

### Article layout
- FULL rebuild to the maqueta: REMOVE the big HeroDefault hero from /blog/[slug].
  Use a slim breadcrumb bar (Waldo › Blog › Category + "Volver al blog" on the
  right), then a 2-column grid `minmax(0,1fr) 308px`, gap 56px, align-items start.
- Left **article** column (max-width 720): category badge (hue per category),
  H1 title, excerpt/header, meta row (author avatar "W" + author + date + read
  time), 16:9 cover image, content blocks, "Leer más" (see below), then a footer
  row ("¿Te sirvió este artículo?" + Compartir/share button + "Ver más artículos").
- Right **aside** (sticky top:86px, 308px): card "Destacados en Waldo" (featured
  ads list, ~4) + "Ver todos los avisos" button; then dark "¿Vendes equipos?"
  CTA card → "Publicar aviso" (/anunciar).
- Bottom: "Sigue leyendo" section (cream bg, border-top) with 3 related BlogCards.

### "Leer más" button
- REPLICATE the maqueta: collapse the article body to a preview with a white
  fade-out gradient + centered "Leer más en el artículo" button (amber) that
  expands the full content. Client-side toggle.

### Sidebar "Destacados en Waldo" ads
- Load FEATURED (destacados) ads. Resolve the existing featured-ads query/store;
  do NOT add a brand-new endpoint if one exists. Limit ~4. Each row: 62x62 thumb
  + category eyebrow + 2-line title + price.

### Blog list hero
- Enrich via HeroDefault's `eyebrow` slot (do NOT modify HeroDefault component):
  badge "Blog Waldo" (book icon, amber-tint bg) + title "Activos industriales,
  explicados" + subtitle "Guías de compra y venta, lectura de mercado, mantención
  y logística. Todo lo que necesitas saber para mover maquinaria con confianza."

### Blog list (rest)
- BlogCard alignment: 16:9 media, category badge overlay (top-left, white/blur bg
  + hue dot), title (Poppins 700 18px), 2-line excerpt, meta (date · read time
  with clock). Per-category hue wash for the no-image background + kicker.
- Toolbar: category dropdown + "Buscar artículos…" search + "Limpiar filtros".
  Reconcile with existing FilterArticles (don't duplicate; restyle if needed).
- Pager: add range text "Mostrando X–Y de N artículos".
</decisions>

<specifics>
## Specific notes
- NOT touching the HeroDefault component itself (only passing props/slots).
- Read time: compute from body length (~200 wpm) with a sensible fallback; the
  maqueta shows "{n} min de lectura".
- Per-category hue map (from BlogCard.dc.html): {'Guía de compra':62,'Mercado':238,
  'Mantención':18,'Vender mejor':166,'Financiamiento':96,'Logística':210,
  'Seguridad':352}; washBg=oklch(0.955 0.035 H), onColor=oklch(0.50 0.13 H),
  baseColor=oklch(0.66 0.16 H). Map to the ad's category name when available.
- Reuse existing components/composables; don't add extra Strapi calls beyond what's
  needed for featured ads (and reuse existing populate where possible).
- Tokens: existing $ink/$ink2/$muted/$amber/$amber_hover/$cream/$line. Strict BEM,
  no compound classes, container rule (vertical padding on section).
</specifics>

<canonical_refs>
## Canonical References
- reference/maqueta-blog-list.html, reference/maqueta-blog-article.html
- BlogCard.dc.html (DesignSync get_file, project 7986357f-d4e1-401d-83b6-5c848f49bd83)
- Current components: ArticleArchive, CardArticle, ArticleSingle, RelatedArticles,
  RelatedAds, FilterArticles; pages blog/index.vue, blog/[slug].vue.
</canonical_refs>
