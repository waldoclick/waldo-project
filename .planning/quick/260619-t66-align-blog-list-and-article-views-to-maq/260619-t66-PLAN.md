---
phase: quick-260619-t66
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/utils/readTime.ts
  - apps/website/app/utils/categoryHue.ts
  - apps/website/tests/utils/readTime.test.ts
  - apps/website/tests/utils/categoryHue.test.ts
  - apps/website/app/components/CardArticle.vue
  - apps/website/app/components/FilterArticles.vue
  - apps/website/app/components/ArticleArchive.vue
  - apps/website/app/components/ArticleSingle.vue
  - apps/website/app/pages/blog/index.vue
  - apps/website/app/pages/blog/[slug].vue
  - apps/website/app/scss/components/_card.scss
  - apps/website/app/scss/components/_article.scss
  - apps/website/app/scss/components/_filter.scss
autonomous: true
requirements: [T66-LIST, T66-ARTICLE]

must_haves:
  truths:
    - "Two pure utils (getReadTime, getCategoryHue) exist with 100% unit-test coverage and are imported (not re-derived) by every consumer"
    - "/blog hero shows the 'Blog Waldo' badge + 'Activos industriales, explicados' title + locked subtitle, all via HeroDefault's eyebrow slot (HeroDefault unmodified)"
    - "Blog cards (CardArticle) match BlogCard.dc.html: 16:9 media, category badge overlay top-left, per-category hue wash + monospace kicker when no image, Poppins 700 18px title, 2-line excerpt clamp, meta footer with computed read time"
    - "Blog toolbar shows category dropdown + 'Buscar artículos…' search + 'Limpiar filtros'; search adds a Strapi filter to the existing loadArticles call (NO new endpoint)"
    - "Pager shows 'Mostrando X–Y de N artículos' range text beside the page buttons"
    - "/blog/[slug] renders a slim breadcrumb bar (Waldo › Blog › {category} + 'Volver al blog') — the big HeroDefault hero is removed"
    - "/blog/[slug] is a 2-column grid minmax(0,1fr) 308px with article left (badge, H1, excerpt, meta row, 16:9 cover, body) and sticky aside right"
    - "Article body renders collapsed with a white fade overlay + amber 'Leer más en el artículo' button; clicking expands to full body (client-side toggle)"
    - "Aside shows 'Destacados en Waldo' card (~4 featured-ad rows with thumb/category/title/price) + 'Ver todos los avisos', and a dark '¿Vendes equipos?' CTA → 'Publicar aviso'"
    - "Featured ads for the aside load through the page's existing useAsyncData (Promise.all), NOT an ad-hoc client call"
    - "Article footer row shows '¿Te sirvió este artículo?' + share + 'Ver más artículos'; 'Sigue leyendo' section (cream bg) shows 3 related BlogCards"
    - "SEO ($setSEO/$setStructuredData), article_view analytics fire, 404 showError, and existing useAsyncData keys are all preserved in both pages"
  artifacts:
    - path: "apps/website/app/utils/readTime.ts"
      provides: "getReadTime(body): number — minutes from word count (~200wpm) with fallback"
      exports: ["getReadTime"]
    - path: "apps/website/app/utils/categoryHue.ts"
      provides: "getCategoryHue(name): { washBg, onColor, baseColor } oklch from locked hue map + default fallback"
      exports: ["getCategoryHue"]
    - path: "apps/website/app/components/CardArticle.vue"
      provides: "Blog card aligned to BlogCard.dc.html"
    - path: "apps/website/app/pages/blog/[slug].vue"
      provides: "Rebuilt 2-column article page (breadcrumb + article + aside + sigue leyendo)"
    - path: "apps/website/app/components/ArticleSingle.vue"
      provides: "2-column article layout with read-more collapse and featured-ads aside"
  key_links:
    - from: "apps/website/app/components/CardArticle.vue"
      to: "apps/website/app/utils/categoryHue.ts + readTime.ts"
      via: "import + computed"
      pattern: "getCategoryHue|getReadTime"
    - from: "apps/website/app/pages/blog/[slug].vue"
      to: "adsStore.loadAds in useAsyncData Promise.all"
      via: "featured-ads load wired to page data"
      pattern: "loadAds.*sort_priority"
    - from: "apps/website/app/components/ArticleSingle.vue"
      to: "featuredAds prop"
      via: "aside renders rows from prop"
      pattern: ":featured-ads|featuredAds"
---

<objective>
Deep-align /blog (list) and /blog/[slug] (article) to the Claude Design maqueta
(reference/maqueta-blog-list.html, reference/maqueta-blog-article.html). The list
needs targeted polish (hero eyebrow, card alignment, toolbar restyle, pager range).
The article needs a major rebuild: remove the hero, add a breadcrumb bar, switch to
a 2-column layout with a sticky aside (featured ads + sell-CTA), a "Leer más"
collapse, and a "Sigue leyendo" related section.

Purpose: Bring the public blog views to the approved v1.47 design with strict BEM
and existing tokens, reusing components/utils instead of forking.
Output: Two new pure utils (+ tests), an updated CardArticle, a restyled toolbar,
a rebuilt article page, and supporting SCSS under existing _card/_article/_filter.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260619-t66-align-blog-list-and-article-views-to-maq/260619-t66-CONTEXT.md
@.planning/quick/260619-t66-align-blog-list-and-article-views-to-maq/reference/maqueta-blog-list.html
@.planning/quick/260619-t66-align-blog-list-and-article-views-to-maq/reference/maqueta-blog-article.html

Current files:
@apps/website/app/pages/blog/index.vue
@apps/website/app/pages/blog/[slug].vue
@apps/website/app/components/CardArticle.vue
@apps/website/app/components/ArticleSingle.vue
@apps/website/app/components/ArticleArchive.vue
@apps/website/app/components/FilterArticles.vue
@apps/website/app/components/HeroDefault.vue
@apps/website/app/components/RelatedArticles.vue
@apps/website/app/components/ShareDefault.vue
@apps/website/app/scss/components/_card.scss
@apps/website/app/scss/components/_article.scss
@apps/website/app/scss/abstracts/_variables.scss

<interfaces>
<!-- Extracted from codebase — use these directly, do NOT re-explore. -->

Tokens (app/scss/abstracts/_variables.scss): $ink #26252b, $ink2 #56535f,
$muted #8a8794, $placeholder #b6b2bb, $amber #f7c97e, $amber_hover #efb85c,
$amber_tint #fef8ec, $tag #a9772e, $cream #f6f4f1, $line #ece9e4, $white #ffffff,
$success_strong #1f8a5b. USE THESE ONLY — no new/modified tokens.

`.container` (app/scss/base/_container.scss): width:100%; max-width:1200px;
margin:0 auto; padding:0 32px (18px on small). This EXACTLY matches the maqueta's
`max-width:1200px;margin:0 auto;padding:* 32px` wrappers. Container rule: vertical
padding goes on the <section>; the container div only `@extend .container`.

Article type (app/types/article.d.ts): { id, documentId, title, header, body,
slug, cover: Media[], gallery, categories: Category[], seo_title, seo_description,
publishedAt, createdAt }. cover[0].formats.medium.url | thumbnail.url.

Ad type (app/types/ad.d.ts): { id, documentId, title, name, slug,
category: number|Category, gallery: GalleryItem[], price, currency, ... }.
Category may be object with { name, slug, color }.

Category (app/types/category.d.ts): { name, slug, color? }. NOTE: categories DO
carry an optional `color`, but the maqueta uses its own per-NAME hue map keyed by
Spanish design names — real Strapi categories likely won't match, so getCategoryHue
MUST have a default fallback hue.

formatCurrency(amount, options?) from app/utils/price.ts → "es-CL" CLP, returns "--"
for null/NaN. Use for sidebar ad prices.

Featured-ads load pattern (from app/pages/index.vue lines 59-69):
  await adsStore.loadAds({}, { page: 1, pageSize: 4 }, ["sort_priority:asc", "createdAt:desc"]);
  return adsStore.ads;
Reuse EXACTLY this. Do NOT invent an endpoint. pageSize:4 for the aside.

useImageProxy().transformUrl(url) — used by CardArticle/CardAnnouncement to proxy
remote image URLs.

useSanitize().parseMarkdown(body) — markdown → HTML for the article body (already
used in ArticleSingle). The body is ONE v-html blob; you CANNOT cheaply split it
into preview/rest block arrays like the maqueta's aPreview/aRest. The read-more is
a CSS max-height collapse + fade overlay, not a block split (see Task 3).

useAdAnalytics().articleView(id, title, category) — fired once per article in
[slug].vue. PRESERVE this.

Existing BEM trees to EXTEND (do not fork, do not create new partials):
- card--article (_card.scss ~936): __media (16:9), __media__cat (overlay badge,
  white/blur bg, __dot), __body, __body__title, __body__excerpt (2-line clamp),
  __body__footer (__sep, __read, __read__icon). ADD: __media__wash (no-image hue
  bg) + __media__kicker (monospace) under __media namespace.
- article--single (_article.scss ~133): currently flex 2-col. REBUILD to the
  maqueta grid. New elements live under article--single namespace (e.g.
  article--single__breadcrumb, article--single__grid, article--single__main,
  article--single__main__badge, article--single__main__meta, article--single__body,
  article--single__readmore, article--single__footer, article--single__aside,
  article--single__aside__featured, article--single__aside__featured__row,
  article--single__aside__cta, article--single__related). NO compound/standalone
  hyphenated classes.
- filter--articles (_filter.scss): restyle toolbar to dropdown + search + clear.

Per-category hue map (locked, from BlogCard.dc.html — keys are design category
names): {'Guía de compra':62,'Mercado':238,'Mantención':18,'Vender mejor':166,
'Financiamiento':96,'Logística':210,'Seguridad':352}. Derived colors:
  washBg = oklch(0.955 0.035 H); onColor = oklch(0.50 0.13 H);
  baseColor = oklch(0.66 0.16 H). Default hue (name not in map) = 38 (amber-ish).
These oklch() values are computed INLINE styles for category accents — allowed,
NOT a brand-token change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create read-time + category-hue utils (with tests), align CardArticle, restyle blog toolbar + pager + hero eyebrow</name>
  <files>apps/website/app/utils/readTime.ts, apps/website/app/utils/categoryHue.ts, apps/website/tests/utils/readTime.test.ts, apps/website/tests/utils/categoryHue.test.ts, apps/website/app/components/CardArticle.vue, apps/website/app/components/FilterArticles.vue, apps/website/app/components/ArticleArchive.vue, apps/website/app/pages/blog/index.vue, apps/website/app/scss/components/_card.scss, apps/website/app/scss/components/_filter.scss</files>
  <behavior>
    getReadTime(body):
    - "" or null/undefined → 1 (fallback, never 0)
    - 200 words → 1, 201 words → 2 (ceil at ~200 wpm)
    - strips markdown/html-ish tokens enough to count words sensibly
    getCategoryHue(name):
    - "Guía de compra" → hue 62; "Seguridad" → hue 352 (returns { washBg, onColor, baseColor } as oklch strings)
    - unknown name (e.g. "Cualquiera") → default hue 38, still returns valid oklch strings
    - "" / undefined → default hue 38
  </behavior>
  <action>
    1. Create app/utils/readTime.ts exporting `getReadTime(body: string | null | undefined): number`
       (~200 wpm, Math.ceil, min 1, strip tags/markdown punctuation before counting).
       Create app/utils/categoryHue.ts exporting
       `getCategoryHue(name: string | null | undefined): { washBg: string; onColor: string; baseColor: string }`
       using the locked hue map (see interfaces) with default hue 38 fallback.
       Both are pure, auto-imported via Nuxt app/utils. Per CLAUDE.md, utils get
       100% unit coverage at creation — write mirrored tests in
       apps/website/tests/utils/ (cross the tests/→app boundary: import from
       "../../app/utils/readTime"). Cover the behavior cases above.

    2. CardArticle.vue — align to BlogCard.dc.html:
       - Keep 16:9 __media. When NO cover: render __media__wash (background = the
         category's washBg via :style) + __media__kicker (monospace category name).
         When cover: keep <NuxtImg>. Keep __media__cat overlay badge (white/blur),
         but set its text color to onColor and __dot background to baseColor via
         :style from getCategoryHue(categoryName).
       - Title: Poppins 700 18px (already in _card.scss — verify). Excerpt: keep
         2-line clamp. Footer: replace hardcoded "5 min de lectura" with
         `{{ getReadTime(article.body) }} min de lectura` (keep IconClock).
       - Do NOT add extra API calls; use article fields already populated.

    3. FilterArticles.vue + _filter.scss — restyle the toolbar to match
       maqueta-blog-list.html lines ~18-44: category dropdown (custom button +
       panel OR keep <select> restyled — keep it simple and SSR-safe), a
       "Buscar artículos…" search input, and a "Limpiar filtros" link shown only
       when filters/query are active. DO NOT duplicate fetch logic — the search
       writes route.query.q (debounced ok) and category writes route.query.category,
       exactly like the existing category/order watchers. RESOLVE the `order` select:
       keep it working but restyle to fit, OR remove it AND drop `order` from the
       page's useAsyncData key/watch/sort in the SAME change so the query surface
       stays consistent (do not leave it half-wired). Reconcile, do not fork.

    4. blog/index.vue:
       - Hero: pass eyebrow content via HeroDefault's #eyebrow slot — badge
         "Blog Waldo" (book icon, background rgba(247,201,126,.28), color $tag) +
         override title "Activos industriales, explicados" via #title slot +
         subtitle "Guías de compra y venta, lectura de mercado, mantención y
         logística. Todo lo que necesitas saber para mover maquinaria con
         confianza." DO NOT modify HeroDefault.vue.
       - Wire search: add route.query.q to the useAsyncData key + watch, and add a
         Strapi filter to the EXISTING loadArticles call
         (e.g. filters $or title/header $containsi). NO new endpoint. Preserve the
         existing key shape and SEO/structured-data blocks.

    5. ArticleArchive.vue — add the pager range text "Mostrando X–Y de N artículos"
       beside the page buttons (compute from pagination.page/pageSize/total). Keep
       the existing vue-awesome-paginate. Add range under article--archive__paginate
       namespace.

    STRICT BEM: new elements under their block/modifier namespace; no standalone
    compound classes. Brand tokens only (oklch inline accents allowed). Container
    rule respected. No box-shadow / scale transforms.
  </action>
  <verify>
    <automated>cd apps/website && pnpm vitest run tests/utils/readTime.test.ts tests/utils/categoryHue.test.ts && pnpm nuxt typecheck</automated>
    Then Playwright on http://localhost:3000/blog: screenshot full page →
    reference/after/blog-list-after.png; compare hero badge/title/subtitle, card
    media (image + no-image hue wash), toolbar (dropdown+search+clear), and pager
    range text against reference/maqueta-blog-list.html. Capture a before shot first
    if not already in reference/after/. Do NOT mark done on typecheck alone.
  </verify>
  <done>
    Both utils exist with passing unit tests; typecheck clean. /blog visually
    matches the maqueta: enriched hero, aligned cards (with hue wash on no-image),
    restyled toolbar with working search/category/clear, and pager range text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild article page scaffold — remove hero, add breadcrumb bar + 2-column grid (article column: badge, H1, excerpt, meta row, 16:9 cover, body)</name>
  <files>apps/website/app/pages/blog/[slug].vue, apps/website/app/components/ArticleSingle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    1. blog/[slug].vue — REMOVE the <HeroDefault> block (and its import + the
       formattedDate-in-hero usage if now unused; keep formattedDate if reused by
       the meta row, otherwise pass createdAt into ArticleSingle and format there).
       PRESERVE EXACTLY: the useAsyncData key `article-${route.params.slug}`, the
       $setSEO/$setStructuredData watch, the article_view analytics fire + its
       slug-change guard reset, and the 404 showError(watchEffect). Pass the article
       (and related) into ArticleSingle as before. Keep RelatedArticles OR move the
       related grid into ArticleSingle's "Sigue leyendo" — decide in Task 5; for now
       leave RelatedArticles as-is so the page still renders.

    2. ArticleSingle.vue — rebuild markup toward the maqueta (article column only in
       this task; aside/read-more/footer/related come in Tasks 3-5, but scaffold
       their containers so the grid is correct):
       - Breadcrumb bar: <section> (vertical padding ~94px top 6px bottom) with a
         container div (@extend .container). nav "Waldo › Blog › {category}" (chevrons)
         + right-aligned "Volver al blog" (← link to /blog). Class:
         article--single__breadcrumb (+ __nav, __nav__sep, __back).
       - Grid wrapper: <section> (padding 20px 0 76px) → div.article--single__grid
         (@extend .container; grid-template-columns: minmax(0,1fr) 308px; gap 56px;
         align-items:start; collapse to 1fr on screen-large/medium).
       - Left column article--single__main (max-width 720): category badge
         (article--single__main__badge — bg $cream, border $line, text = onColor,
         dot = baseColor via :style from getCategoryHue(category name)); H1
         (article--single__main__title, Poppins 800, clamp(30px,3.2vw,40px)); excerpt
         (article--single__main__excerpt = article.header); meta row
         (article--single__main__meta: avatar "W" on $amber + author "Waldo" +
         separators + date + read time via getReadTime(article.body)); 16:9 cover
         (article--single__main__cover, only when cover exists) using transformUrl;
         then the body container (article--single__body, parseMarkdown v-html) —
         this is where Task 3 adds the collapse.
       - Right column: scaffold <aside class="article--single__aside"> (sticky
         top:86px) empty for now (Task 4 fills it).
    3. _article.scss — REPLACE the old article--single flex layout with the maqueta
       grid + all new __breadcrumb / __grid / __main / __main__badge / __main__title
       / __main__excerpt / __main__meta / __main__cover / __body / __aside element
       styles. Keep the existing __body__description typography rules (rename/move
       under __body as needed — the parseMarkdown prose styles must survive). STRICT
       BEM under article--single; no compound classes. Tokens only.
  </action>
  <verify>
    <automated>cd apps/website && pnpm nuxt typecheck</automated>
    Get a real slug: open http://localhost:3000/blog, take the first article's
    /blog/{slug}. Playwright screenshot → reference/after/blog-article-scaffold.png;
    confirm hero is gone, breadcrumb bar present, 2-col grid with article column
    (badge/H1/excerpt/meta/cover/body) matches reference/maqueta-blog-article.html.
    Confirm SEO/analytics/404 still wired (no console errors, title updates).
  </verify>
  <done>
    Hero removed; breadcrumb bar + 2-col grid render with a complete article column
    matching the maqueta; SEO, article_view, 404 and useAsyncData key preserved;
    typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: "Leer más" collapse — CSS max-height + fade overlay + amber expand button</name>
  <files>apps/website/app/components/ArticleSingle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    Implement the read-more as a CSS collapse (NOT the maqueta's block split — the
    body is one parseMarkdown v-html blob):
    - Wrap article--single__body in article--single__readmore. Default state
      (SSR-safe): readmore has a fixed max-height + overflow:hidden, with a white
      fade-out gradient overlay (article--single__readmore__fade,
      linear-gradient(rgba(255,255,255,0), #fff)) pinned to the bottom, plus a
      centered control article--single__readmore__more containing an amber button
      "Leer más en el artículo" (bg $amber, hover $amber_hover, $ink text, down
      arrow icon) and a read-label "{n} min de lectura" (getReadTime).
    - Client-side ref `expanded` toggled on click adds modifier
      article--single__readmore--expanded → removes the max-height cap, hides the
      fade + button. Use a plain reactive ref in <script setup>; default false so
      SSR renders collapsed.
    - Keep all prose typography intact while collapsed.
  </action>
  <verify>
    <automated>cd apps/website && pnpm nuxt typecheck</automated>
    Playwright on the real /blog/{slug}: screenshot collapsed →
    reference/after/blog-article-collapsed.png (fade + amber button visible), click
    the button, screenshot expanded → reference/after/blog-article-expanded.png
    (full body, no fade/button). Compare to reference/maqueta-blog-article.html
    lines ~43-51. Do NOT mark done on typecheck alone.
  </verify>
  <done>
    Body renders collapsed with fade + amber "Leer más" button on load; clicking
    expands to full content and removes the fade/button; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Sticky aside — "Destacados en Waldo" featured-ads card + dark "¿Vendes equipos?" CTA (featured ads wired via page useAsyncData)</name>
  <files>apps/website/app/pages/blog/[slug].vue, apps/website/app/components/ArticleSingle.vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    1. blog/[slug].vue — wire featured ads into the EXISTING useAsyncData. Convert
       the related fetch + a featured-ads fetch into a structure that loads both
       (use Promise.all or sequential within the same useAsyncData callback; keep
       the article fetch first since related depends on its category). Add:
         const adsStore = useAdsStore();
         await adsStore.loadAds({}, { page: 1, pageSize: 4 }, ["sort_priority:asc", "createdAt:desc"]);
         featuredAds = adsStore.ads;
       Extend ArticlePageData with `featuredAds: Ad[]` (default []). Pass
       :featured-ads="pageData.featuredAds" to ArticleSingle. NO ad-hoc client call,
       NO new endpoint.
    2. ArticleSingle.vue — accept `featuredAds: Ad[]` prop (default []). Fill the
       aside (maqueta lines ~65-88):
       - article--single__aside__featured (white card, border $line, radius 10px):
         eyebrow "Destacados en Waldo" (dot $amber, $tag text). For each ad (≤4):
         article--single__aside__featured__row (NuxtLink to /anuncios/{slug}): 62x62
         thumb (transformUrl of gallery[0] medium/url, $cream fallback) +
         __row__cat eyebrow (category name; color via getCategoryHue or category.color)
         + __row__title (2-line clamp) + __row__price (formatCurrency(ad.price)).
         Rows separated by border-top $line. Then a "Ver todos los avisos" link
         (→ /anuncios) styled as the outlined button.
       - article--single__aside__cta (dark #26252B card, radius 10px): h4
         "¿Vendes equipos?" (white) + text "Publica tu aviso y llega a compradores
         de todo Chile. Los primeros 3 son gratis." + amber "Publicar aviso" link
         (→ /anunciar, $amber bg, $ink text, hover $amber_hover).
       - Keep the aside sticky top:86px (from Task 2). Hide gracefully if
         featuredAds empty (still show the CTA card).
    3. _article.scss — add article--single__aside__featured (+ __row, __row__thumb,
       __row__cat, __row__title, __row__price, __link/button) and
       article--single__aside__cta styles. STRICT BEM; tokens only; the dark card
       uses #26252B (= $ink) and white text.
  </action>
  <verify>
    <automated>cd apps/website && pnpm nuxt typecheck</automated>
    Playwright on /blog/{slug}: screenshot the aside →
    reference/after/blog-article-aside.png; confirm 4 featured-ad rows (thumb/cat/
    title/price), "Ver todos los avisos", and the dark "¿Vendes equipos?" CTA match
    reference/maqueta-blog-article.html. Verify in DevTools/network there is NO new
    Strapi call from the component (featured ads came with the page payload).
  </verify>
  <done>
    Aside shows the "Destacados en Waldo" card with 4 featured-ad rows + "Ver todos
    los avisos" and the dark sell-CTA; featured ads load via the page's useAsyncData
    (no extra component call); typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: Article footer row + "Sigue leyendo" related section; final list+article visual pass</name>
  <files>apps/website/app/components/ArticleSingle.vue, apps/website/app/pages/blog/[slug].vue, apps/website/app/scss/components/_article.scss</files>
  <action>
    1. ArticleSingle.vue — add the footer row at the end of the article column
       (article--single__footer, border-top $line): left group "¿Te sirvió este
       artículo?" + a Compartir/share control (reuse ShareDefault, or a copy-link
       button that toggles "Enlace copiado" with a check — match the maqueta's copy
       behavior); right "Ver más artículos" link (→ /blog, $line border, hover
       border $ink).
    2. "Sigue leyendo" — render the related grid. Accept a `relatedArticles: Article[]`
       prop on ArticleSingle and render a <section class="...__related"> (cream bg,
       border-top $line, padding 56px 0 72px, margin-top 48px) with a container,
       h2 "Sigue leyendo", and a 3-col grid of CardArticle (slice to 3). Pass
       :related-articles from [slug].vue. THEN remove the now-duplicate
       <RelatedArticles> from blog/[slug].vue (and its import) so related shows once,
       inside the article flow, matching the maqueta. (If keeping RelatedArticles is
       cleaner, that's acceptable — but do NOT render related twice.)
       Add article--single__related (+ __grid) styles in _article.scss.
    3. Final reconciliation pass: re-run the list page and article page end-to-end;
       fix any spacing/token drift against both maquetas. Run pnpm codacy if quick.
  </action>
  <verify>
    <automated>cd apps/website && pnpm nuxt typecheck && pnpm vitest run tests/utils/readTime.test.ts tests/utils/categoryHue.test.ts</automated>
    Playwright on /blog/{slug}: full-page screenshot → reference/after/blog-article-full.png;
    confirm footer row (¿Te sirvió? + share + Ver más artículos) and "Sigue leyendo"
    (3 cards, cream bg, rendered ONCE) match reference/maqueta-blog-article.html.
    Also re-shot /blog → reference/after/blog-list-final.png. Do NOT mark done on
    typecheck alone.
  </verify>
  <done>
    Article footer row + "Sigue leyendo" (3 cards, shown once) match the maqueta;
    both /blog and /blog/[slug] visually align end-to-end; utils tests + typecheck
    pass.
  </done>
</task>

</tasks>

<verification>
- pnpm nuxt typecheck clean (apps/website).
- pnpm vitest run on the two new util test files passes (100% coverage at creation).
- Visual: /blog matches maqueta-blog-list.html (hero eyebrow, cards w/ hue wash,
  toolbar, pager range). /blog/[slug] matches maqueta-blog-article.html (breadcrumb,
  2-col grid, read-more collapse, aside featured+CTA, footer, sigue leyendo).
- No new Strapi calls beyond the featured-ads load wired into useAsyncData.
- SEO/structured-data, article_view analytics, 404 handling, and useAsyncData keys
  preserved in both pages.
- Strict BEM (no compound/standalone classes), brand tokens only (oklch inline
  accents allowed), container rule respected, HeroDefault unmodified.
</verification>

<success_criteria>
All must_haves truths observable on http://localhost:3000/blog and a real
/blog/{slug}, verified by before/after screenshots in reference/after/ compared to
the two maqueta references, with typecheck + util tests green.
</success_criteria>

<output>
After completion, create
`.planning/quick/260619-t66-align-blog-list-and-article-views-to-maq/260619-t66-SUMMARY.md`
</output>
