---
phase: quick-260404-o0g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # Strapi collection type
  - apps/strapi/src/api/term/content-types/term/schema.json
  - apps/strapi/src/api/term/controllers/term.ts
  - apps/strapi/src/api/term/routes/term.ts
  - apps/strapi/src/api/term/services/term.ts
  # Website
  - apps/website/app/types/term.d.ts
  - apps/website/app/stores/terms.store.ts
  - apps/website/app/components/TermsDefault.vue
  - apps/website/app/pages/condiciones-de-uso.vue
  - apps/website/tests/stores/terms.store.test.ts
  # Dashboard
  - apps/dashboard/app/stores/settings.store.ts
  - apps/dashboard/app/components/TermsDefault.vue
  - apps/dashboard/app/components/FormTerm.vue
  - apps/dashboard/app/pages/terms/index.vue
  - apps/dashboard/app/pages/terms/new.vue
  - apps/dashboard/app/pages/terms/[id]/index.vue
  - apps/dashboard/app/pages/terms/[id]/edit.vue
  - apps/dashboard/app/components/MenuDefault.vue
autonomous: true
requirements: [QUICK-O0G]
must_haves:
  truths:
    - "Strapi has a 'term' collection type with title, text, and order fields"
    - "Dashboard has a full CRUD section for Condiciones de Uso at /terms"
    - "Dashboard menu shows Condiciones de Uso under Mantenedores"
    - "Website has a /condiciones-de-uso page that renders terms as accordion"
    - "Terms list in dashboard supports drag-and-drop reorder"
  artifacts:
    - path: "apps/strapi/src/api/term/content-types/term/schema.json"
      provides: "Term collection type schema"
      contains: "collectionType"
    - path: "apps/dashboard/app/components/TermsDefault.vue"
      provides: "Terms list with drag-and-drop"
    - path: "apps/website/app/pages/condiciones-de-uso.vue"
      provides: "Public terms of use page"
  key_links:
    - from: "apps/website/app/pages/condiciones-de-uso.vue"
      to: "apps/website/app/stores/terms.store.ts"
      via: "useTermsStore().loadTerms()"
    - from: "apps/dashboard/app/components/TermsDefault.vue"
      to: "Strapi /terms API"
      via: "useApiClient('terms')"
---

<objective>
Create a "Condiciones de Uso" (Terms of Use) feature that replicates the exact same pattern as "Politicas de Privacidad" across all three apps: Strapi collection type, dashboard CRUD section, and website public page.

Purpose: The platform needs a Terms of Use section managed from the dashboard, displayed on the website, following the established pattern.
Output: Full term collection type in Strapi, dashboard management pages at /terms, website page at /condiciones-de-uso.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Existing patterns to replicate exactly -->

From apps/strapi/src/api/policy/content-types/policy/schema.json:
```json
{
  "kind": "collectionType",
  "collectionName": "policies",
  "info": { "singularName": "policy", "pluralName": "policies", "displayName": "Policy" },
  "options": { "draftAndPublish": false },
  "pluginOptions": {},
  "attributes": {
    "title": { "type": "string", "required": true },
    "text": { "type": "text" },
    "order": { "type": "integer" }
  }
}
```

From apps/strapi/src/api/policy/controllers/policy.ts:
```typescript
import { factories } from "@strapi/strapi";
export default factories.createCoreController("api::policy.policy");
```

From apps/website/app/types/policy.d.ts:
```typescript
export interface Policy {
  id: number; title: string; text: string; order: number | null;
  createdAt: string; updatedAt: string; publishedAt: string | null;
}
export interface PolicyResponse {
  data: Policy[]; meta: { pagination: { page: number; pageSize: number; pageCount: number; total: number; } };
}
```

Dashboard settings.store.ts sections pattern:
- Add `terms` to SettingsState interface
- Add `terms` ref with `sortBy: "order:asc"` (same as policies)
- Add `getTermsFilters` computed
- Add `"terms"` case to getSectionSettings switch
- Export terms state and getter

Dashboard MenuDefault.vue already has Mantenedores submenu — add "Condiciones de Uso" entry using `ScrollText` icon from lucide-vue-next at route `/terms`.

MenuDefault isMantenedoresActive computed must also include `isRouteActive("/terms")`.
Menu watch() must include `path.startsWith("/terms")` in the mantenedores condition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Strapi term collection type and website term types/store/page</name>
  <files>
    apps/strapi/src/api/term/content-types/term/schema.json,
    apps/strapi/src/api/term/controllers/term.ts,
    apps/strapi/src/api/term/routes/term.ts,
    apps/strapi/src/api/term/services/term.ts,
    apps/website/app/types/term.d.ts,
    apps/website/app/stores/terms.store.ts,
    apps/website/app/components/TermsDefault.vue,
    apps/website/app/pages/condiciones-de-uso.vue,
    apps/website/tests/stores/terms.store.test.ts
  </files>
  <action>
    **Strapi collection type** — Create `apps/strapi/src/api/term/` directory structure identical to `apps/strapi/src/api/policy/`:
    - `content-types/term/schema.json`: Same structure as policy schema.json but with `collectionName: "terms"`, `singularName: "term"`, `pluralName: "terms"`, `displayName: "Term"`. Same attributes: `title` (string, required), `text` (text), `order` (integer). `draftAndPublish: false`.
    - `controllers/term.ts`: `factories.createCoreController("api::term.term")`
    - `routes/term.ts`: `factories.createCoreRouter("api::term.term")`
    - `services/term.ts`: `factories.createCoreService("api::term.term")`

    **Website type** — Create `apps/website/app/types/term.d.ts` identical to `policy.d.ts` but with `Term` and `TermResponse` interface names.

    **Website store** — Create `apps/website/app/stores/terms.store.ts` as exact copy of `policies.store.ts` pattern:
    - Store name: `"terms"`
    - Import `Term, TermResponse` from `@/types/term`
    - API endpoint: `"terms"` (not `"policies"`)
    - State: `terms` ref, `lastFetchTimestamp`, `loading`, `error`
    - Action: `loadTerms()` with same cache logic (CACHE_DURATION = 3600000, PAGE_SIZE = 50, sort: `["order:asc"]`)
    - Persist with `persistedState.localStorage`
    - Include persist audit comment: `// persist: CORRECT — static reference data with 1-hour cache TTL; safe to reuse across sessions`

    **Website component** — Create `apps/website/app/components/TermsDefault.vue` identical to `PoliciesDefault.vue`:
    - BEM block: `terms terms--default` (not `policies`)
    - Title: "Condiciones de uso"
    - Hero text: "Al utilizar la plataforma Waldo.click® para la publicación y compra de anuncios de activos industriales, usted acepta las siguientes condiciones de uso:"
    - Uses `AccordionDefault` with `:questions="terms"` prop
    - Props: `terms: Term[]` (import from `@/types/term`)

    **Website page** — Create `apps/website/app/pages/condiciones-de-uso.vue` identical to `politicas-de-privacidad.vue`:
    - Layout: `"about"` (same as privacy policy page)
    - Uses `useTermsStore()` and `loadTerms()`
    - `useAsyncData` key: `"terms"` with `default: () => []`
    - SEO title: "Condiciones de Uso"
    - SEO description: "Revisa las condiciones de uso de Waldo.click® para la publicación y compra de anuncios de activos industriales en nuestra plataforma."
    - Structured data: WebPage with same pattern
    - URL: `${config.public.baseUrl}/condiciones-de-uso`

    **Website test** — Create `apps/website/tests/stores/terms.store.test.ts` identical to `policies.store.test.ts`:
    - Same 3 test cases: first fetch, skip when cache fresh, fetch when cache stale
    - Use `useTermsStore`, endpoint `"terms"`, `store.terms`, `store.loadTerms()`
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace website vitest run tests/stores/terms.store.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Strapi term collection type exists with schema, controller, router, service. Website has term type, store with cache, TermsDefault component, condiciones-de-uso page, and passing store tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard term management pages and wire into menu/settings</name>
  <files>
    apps/dashboard/app/stores/settings.store.ts,
    apps/dashboard/app/components/TermsDefault.vue,
    apps/dashboard/app/components/FormTerm.vue,
    apps/dashboard/app/pages/terms/index.vue,
    apps/dashboard/app/pages/terms/new.vue,
    apps/dashboard/app/pages/terms/[id]/index.vue,
    apps/dashboard/app/pages/terms/[id]/edit.vue,
    apps/dashboard/app/components/MenuDefault.vue
  </files>
  <action>
    **Settings store** — Edit `apps/dashboard/app/stores/settings.store.ts`:
    - Add `terms: SectionSettings` to `SettingsState` interface
    - Add `const terms = ref<SectionSettings>({ ...defaultSectionSettings, sortBy: "order:asc" })` (same as policies)
    - Add `getTermsFilters` computed getter (same pattern as getPoliciesFilters)
    - Add `case "terms": return terms;` to `getSectionSettings` switch
    - Add `terms` to return object (state section)
    - Add `getTermsFilters` to return object (getters section)

    **TermsDefault.vue** — Create as exact copy of `PoliciesDefault.vue` (dashboard version) with these replacements:
    - BEM: `terms terms--default` everywhere (replace `policies` with `terms`)
    - `settingsStore.terms` instead of `settingsStore.policies`
    - Section: `"terms" as const`
    - `filters = computed(() => settingsStore.getTermsFilters)`
    - Interface: same `Policy` shape but named `Term` (id, documentId, title, text, order, updatedAt, createdAt)
    - API endpoint: `"terms"` in fetchTerms
    - All user-facing text: "Buscar Condiciones..." (search), "Ver Condicion" / "Editar Condicion" (action titles), "No se encontraron condiciones" (empty), "Cargando condiciones..." (loading)
    - Route: `/terms/${termId}` and `/terms/${termId}/edit`
    - handleReorder: PUT to `/terms/${u.documentId}`
    - Draggable handle class: `.terms--default__drag`

    **FormTerm.vue** — Create as exact copy of `FormPolicy.vue` with:
    - BEM: `form form--term`
    - Interface `TermData` (same shape as PolicyData)
    - API endpoint: `"/terms"` for POST, `"/terms/${termId}"` for PUT
    - All Swal messages: "Condicion" instead of "Politica"
    - Submit labels: "Crear Condicion de Uso" / "Actualizar Condicion de Uso"
    - Router redirects: `/terms/${id}` and `/terms`

    **Pages** — Create 4 pages mirroring the policies pages exactly:
    
    `apps/dashboard/app/pages/terms/index.vue`:
    - HeroDefault title: "Condiciones de Uso"
    - Action button: "Agregar Condicion" linking to `/terms/new`
    - Uses `TermsDefault` component
    - Breadcrumbs: `[{ label: "Condiciones de Uso" }]`
    
    `apps/dashboard/app/pages/terms/new.vue`:
    - HeroDefault title: "Nueva Condicion de Uso"
    - Uses `FormTerm` component
    - Breadcrumbs: `[{ label: "Condiciones de Uso", to: "/terms" }, { label: "Nueva" }]`
    
    `apps/dashboard/app/pages/terms/[id]/index.vue`:
    - Exact same pattern as policies/[id]/index.vue
    - API call: `apiClient("terms", { method: "GET", params: { filters: { id: { $eq: Number(id) } } } })`
    - useAsyncData key: `term-${route.params.id}`
    - Title fallback: "Condicion de Uso"
    - Edit link: `/terms/${route.params.id}/edit`
    - Breadcrumbs: `[{ label: "Condiciones de Uso", to: "/terms" }, ...]`
    
    `apps/dashboard/app/pages/terms/[id]/edit.vue`:
    - Exact same pattern as policies/[id]/edit.vue
    - API call: same as detail but key `term-edit-${route.params.id}`
    - Title fallback: "Condicion de Uso"
    - Breadcrumbs: `[{ label: "Condiciones de Uso", to: "/terms" }, ..., { label: "Editar" }]`
    - Uses `FormTerm` component

    **MenuDefault.vue** — Edit to add "Condiciones de Uso" entry:
    - Import `ScrollText` from lucide-vue-next
    - Add a new `<li>` submenu item under Mantenedores, right after the existing "Politicas" entry:
      ```
      <li class="menu--default__subitem" :class="{ 'menu--default__subitem--active': isRouteActive('/terms') }">
        <NuxtLink to="/terms" class="menu--default__sublink">
          <ScrollText class="menu--default__subicon" />
          <span>Condiciones de Uso</span>
        </NuxtLink>
      </li>
      ```
    - Add `isRouteActive("/terms")` to `isMantenedoresActive` computed
    - Add `path.startsWith("/terms")` to the mantenedores condition in the watch
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && npx nuxi typecheck apps/dashboard 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard has full /terms CRUD (list with drag-and-drop, create, view, edit), menu entry under Mantenedores, settings store section for terms, all TypeScript-clean.</done>
</task>

</tasks>

<verification>
1. Strapi: `apps/strapi/src/api/term/content-types/term/schema.json` exists with correct schema
2. Website: `yarn workspace website vitest run tests/stores/terms.store.test.ts` passes (3 tests)
3. Dashboard: `npx nuxi typecheck apps/dashboard` passes clean
4. Menu: `/terms` route appears in MenuDefault.vue under Mantenedores
5. All files follow existing patterns exactly (no new abstractions, no new dependencies)
</verification>

<success_criteria>
- Strapi term collection type mirrors policy collection type exactly
- Dashboard /terms section has list (drag-and-drop), create, view, edit pages
- Website /condiciones-de-uso page renders terms via AccordionDefault
- Website terms store has cache guard and passes 3 unit tests
- Dashboard settings store includes terms section with order:asc default sort
- Dashboard menu shows "Condiciones de Uso" under Mantenedores
- TypeScript compiles clean in dashboard
</success_criteria>

<output>
After completion, create `.planning/quick/260404-o0g-hay-que-hacer-un-nuevo-collect-type-y-un/260404-o0g-SUMMARY.md`
</output>
