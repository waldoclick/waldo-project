---
phase: quick-260411-m0h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/FormLogin.vue
  - apps/website/app/components/FormRegister.vue
  - apps/website/app/components/FormForgotPassword.vue
  - apps/website/app/components/FormResetPassword.vue
  - apps/dashboard/app/components/FormLogin.vue
  - apps/dashboard/app/components/FormForgotPassword.vue
  - apps/dashboard/app/components/FormResetPassword.vue
  - apps/dashboard/app/components/FormVerifyCode.vue
autonomous: true
requirements: [MAXLEN-01]
must_haves:
  truths:
    - "All email fields reject input beyond 254 characters (both yup and HTML maxlength)"
    - "All password fields reject input beyond 50 characters (both yup and HTML maxlength)"
    - "Name and lastname fields reject input beyond 50 characters"
    - "RUT field rejects input beyond 12 characters"
    - "Verification code field remains capped at 6 characters"
  artifacts:
    - path: "apps/website/app/components/FormLogin.vue"
      provides: "Email max 254, password max 50"
    - path: "apps/website/app/components/FormRegister.vue"
      provides: "Email max 254, password max 50, name max 50, lastname max 50, rut max 12"
    - path: "apps/website/app/components/FormForgotPassword.vue"
      provides: "Email max 254"
    - path: "apps/website/app/components/FormResetPassword.vue"
      provides: "Email max 254, password max 50"
    - path: "apps/dashboard/app/components/FormLogin.vue"
      provides: "Email max 254, password max 50"
    - path: "apps/dashboard/app/components/FormForgotPassword.vue"
      provides: "Email max 254"
    - path: "apps/dashboard/app/components/FormResetPassword.vue"
      provides: "Email max 254, password max 50"
    - path: "apps/dashboard/app/components/FormVerifyCode.vue"
      provides: "Code max 6 (already present)"
  key_links: []
---

<objective>
Add maxlength limits to all auth form inputs across website and dashboard apps.

Purpose: Prevent excessively long input from reaching the backend and improve UX by stopping input at the HTML level while also validating via yup schema.
Output: 8 modified Vue components with dual-layer maxlength enforcement (HTML attribute + yup .max() validation).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/website/app/components/FormLogin.vue
@apps/website/app/components/FormRegister.vue
@apps/website/app/components/FormForgotPassword.vue
@apps/website/app/components/FormResetPassword.vue
@apps/dashboard/app/components/FormLogin.vue
@apps/dashboard/app/components/FormForgotPassword.vue
@apps/dashboard/app/components/FormResetPassword.vue
@apps/dashboard/app/components/FormVerifyCode.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add maxlength to website auth forms (4 files)</name>
  <files>
    apps/website/app/components/FormLogin.vue
    apps/website/app/components/FormRegister.vue
    apps/website/app/components/FormForgotPassword.vue
    apps/website/app/components/FormResetPassword.vue
  </files>
  <action>
For each file, apply TWO changes per field — add `.max(N, "message")` to the yup schema AND add `maxlength="N"` to the `<Field>` element.

**FormLogin.vue** (lines 88-94 schema, lines 11-18 and 24-30 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.required()`
- email Field: add `maxlength="254"`
- password yup chain: add `.max(50, "Máximo 50 caracteres")` before `.required()`
- password Field: add `maxlength="50"`

**FormRegister.vue** (schema in getSchema() lines 236-295, template lines 28-62 step 1, lines 68-111 step 2):
- Step 1 schema (yup.object at line 236):
  - firstname: add `.max(50, "Máximo 50 caracteres")` before `.matches()`
  - lastname: add `.max(50, "Máximo 50 caracteres")` before `.matches()`
  - rut: add `.max(12, "Máximo 12 caracteres")` before `.test()`
- Step 1 template Fields:
  - firstname Field (line 28): add `maxlength="50"`
  - lastname Field (line 43): add `maxlength="50"`
  - rut Field (line 54): add `maxlength="12"`
- Step 2 schema (yup.object at line 267):
  - email: add `.max(254, "Máximo 254 caracteres")` before `.email()`
  - password: already has `.max(20, ...)` — change to `.max(50, "Máximo 50 caracteres")` to match the 50-char rule
  - confirm_password: no max needed (validated via oneOf against password)
- Step 2 template Fields:
  - email Field (line 68): add `maxlength="254"`
  - password Field (line 81): add `maxlength="50"`
  - confirm_password Field (line 103): add `maxlength="50"`

**FormForgotPassword.vue** (lines 38-43 schema, lines 9-15 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.email()`
- email Field: add `maxlength="254"`

**FormResetPassword.vue** (lines 61-68 schema, lines 9-16 and 23-29 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.email()`
- email Field: add `maxlength="254"`
- password yup chain: add `.max(50, "Máximo 50 caracteres")` before `.required()`
- password Field: add `maxlength="50"`
- code Field (hidden, line 19): do NOT add maxlength — it is a hidden token field

Do NOT modify any other code, imports, logic, or styling. Only touch yup schema chains and Field element attributes.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -n 'maxlength\|\.max(' apps/website/app/components/FormLogin.vue apps/website/app/components/FormRegister.vue apps/website/app/components/FormForgotPassword.vue apps/website/app/components/FormResetPassword.vue | head -40</automated>
  </verify>
  <done>All 4 website auth forms have maxlength HTML attributes on every text/email/password Field AND corresponding .max() in yup schemas. FormRegister password max changed from 20 to 50.</done>
</task>

<task type="auto">
  <name>Task 2: Add maxlength to dashboard auth forms (4 files)</name>
  <files>
    apps/dashboard/app/components/FormLogin.vue
    apps/dashboard/app/components/FormForgotPassword.vue
    apps/dashboard/app/components/FormResetPassword.vue
    apps/dashboard/app/components/FormVerifyCode.vue
  </files>
  <action>
For each file, apply TWO changes per field — add `.max(N, "message")` to the yup schema AND add `maxlength="N"` to the `<Field>` or `<input>` element.

**FormLogin.vue** (lines 89-95 schema, lines 12-18 and 25-31 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.required()`
- email Field: add `maxlength="254"`
- password yup chain: add `.max(50, "Máximo 50 caracteres")` before `.required()`
- password Field: add `maxlength="50"`

**FormForgotPassword.vue** (lines 38-42 schema, lines 9-15 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.email()`
- email Field: add `maxlength="254"`

**FormResetPassword.vue** (lines 60-67 schema, lines 9-15 and 22-29 template):
- email yup chain: add `.max(254, "Máximo 254 caracteres")` before `.email()`
- email Field: add `maxlength="254"`
- password yup chain: add `.max(50, "Máximo 50 caracteres")` before `.required()`
- password Field: add `maxlength="50"`
- code Field (hidden, line 19): do NOT add maxlength — it is a hidden token field

**FormVerifyCode.vue** — This file uses a plain `<input>` (NOT vee-validate Field) and already has `maxlength="6"` on line 7. No yup schema exists (validation is via computed regex). NO changes needed — verify the existing maxlength="6" is present and leave untouched.

Do NOT modify any other code, imports, logic, or styling. Only touch yup schema chains and Field/input element attributes.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -n 'maxlength\|\.max(' apps/dashboard/app/components/FormLogin.vue apps/dashboard/app/components/FormForgotPassword.vue apps/dashboard/app/components/FormResetPassword.vue apps/dashboard/app/components/FormVerifyCode.vue | head -30</automated>
  </verify>
  <done>All 4 dashboard auth forms have maxlength HTML attributes on every text/email/password Field AND corresponding .max() in yup schemas. FormVerifyCode already had maxlength="6" and remains unchanged.</done>
</task>

</tasks>

<verification>
Run grep across all 8 files to confirm every field has both maxlength attribute and .max() yup validation:

```bash
cd /home/gab/Code/waldo-project
# Check all maxlength attributes
grep -rn 'maxlength=' apps/website/app/components/Form{Login,Register,ForgotPassword,ResetPassword}.vue apps/dashboard/app/components/Form{Login,ForgotPassword,ResetPassword,VerifyCode}.vue

# Check all .max() yup calls
grep -rn '\.max(' apps/website/app/components/Form{Login,Register,ForgotPassword,ResetPassword}.vue apps/dashboard/app/components/Form{Login,ForgotPassword,ResetPassword}.vue

# TypeScript check both apps
cd apps/website && npx nuxi typecheck 2>&1 | tail -5
cd ../../apps/dashboard && npx nuxi typecheck 2>&1 | tail -5
```
</verification>

<success_criteria>
- Every email Field has maxlength="254" and yup .max(254)
- Every password Field has maxlength="50" and yup .max(50)
- Every name/lastname Field has maxlength="50" and yup .max(50)
- RUT Field has maxlength="12" and yup .max(12)
- Verification code input retains maxlength="6"
- No other code changes made
- TypeScript compiles without new errors
</success_criteria>

<output>
After completion, create `.planning/quick/260411-m0h-add-maxlength-limits-to-all-auth-form-in/260411-m0h-SUMMARY.md`
</output>
