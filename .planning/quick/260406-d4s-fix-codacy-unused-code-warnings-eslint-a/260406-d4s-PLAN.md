---
phase: quick
plan: 260406-d4s
type: execute
wave: 1
depends_on: []
files_modified:
  - .codacy/tools-configs/eslint.config.mjs
  - .eslintrc.json
  - apps/strapi/src/api/payment/services/ad.service.ts
  - apps/strapi/src/api/payment/services/checkout.service.ts
  - apps/strapi/src/api/ad/services/ad.ts
  - apps/strapi/src/services/flow/services/flow.service.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Codacy ESLint no-unused-vars rule accepts _ prefixed parameters without warning"
    - "No genuinely dead code remains (unused variables that are defined but never read)"
  artifacts:
    - path: ".codacy/tools-configs/eslint.config.mjs"
      provides: "ESLint config with argsIgnorePattern for _ prefix"
      contains: "argsIgnorePattern"
    - path: ".eslintrc.json"
      provides: "Root ESLint config with argsIgnorePattern for _ prefix"
      contains: "argsIgnorePattern"
  key_links: []
---

<objective>
Fix Codacy unused-code warnings by adding argsIgnorePattern/varsIgnorePattern for `_` prefix to ESLint configs used by Codacy, and remove genuinely dead code (unused variables that are not interface/type parameter placeholders).

Purpose: Codacy flags `_` prefixed parameters as unused because the Codacy ESLint config and root `.eslintrc.json` lack the ignore pattern. Additionally, some `_` prefixed variables in Strapi are actually used (misleading naming) or truly dead.
Output: Clean Codacy unused-code report, consistent ESLint ignore patterns across all configs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.codacy/tools-configs/eslint.config.mjs
@.eslintrc.json
@apps/website/eslint.config.mjs
@apps/dashboard/eslint.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add argsIgnorePattern to Codacy and root ESLint configs</name>
  <files>.codacy/tools-configs/eslint.config.mjs, .eslintrc.json</files>
  <action>
1. In `.codacy/tools-configs/eslint.config.mjs`, update the `"no-unused-vars"` rule from `["error"]` to include ignore patterns matching the app-level configs:
   ```js
   "no-unused-vars": ["error", {
     argsIgnorePattern: "^_",
     varsIgnorePattern: "^_",
     caughtErrorsIgnorePattern: "^_",
     destructuredArrayIgnorePattern: "^_",
     ignoreRestSiblings: true
   }],
   ```

2. In `.eslintrc.json`, update the `"no-unused-vars"` rule from `"warn"` to include the same ignore patterns:
   ```json
   "no-unused-vars": ["warn", {
     "argsIgnorePattern": "^_",
     "varsIgnorePattern": "^_",
     "caughtErrorsIgnorePattern": "^_",
     "destructuredArrayIgnorePattern": "^_",
     "ignoreRestSiblings": true
   }]
   ```

This aligns Codacy's analysis with the app-level ESLint configs so `_` prefixed parameters in interfaces, destructuring rest patterns, and intentionally-ignored callback args are not flagged.
  </action>
  <verify>
    <automated>grep -c "argsIgnorePattern" .codacy/tools-configs/eslint.config.mjs .eslintrc.json</automated>
  </verify>
  <done>Both Codacy and root ESLint configs include argsIgnorePattern/varsIgnorePattern for `_` prefix, matching the app-level configs</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead code and fix misleading _ prefixed variable names in Strapi</name>
  <files>apps/strapi/src/api/payment/services/ad.service.ts, apps/strapi/src/api/payment/services/checkout.service.ts, apps/strapi/src/api/ad/services/ad.ts, apps/strapi/src/services/flow/services/flow.service.ts</files>
  <action>
Fix variables that are prefixed with `_` but are actually USED (misleading naming -- makes it look intentionally unused when it is not):

1. `apps/strapi/src/api/payment/services/ad.service.ts`:
   - Rename `_zohoEmail` to `zohoEmail` (lines ~473, 480, 481) -- it IS used in conditionals and function calls
   - Rename `_zohoAmount` to `zohoAmount` (lines ~474, 494, 501) -- it IS used in object properties

2. `apps/strapi/src/api/payment/services/checkout.service.ts`:
   - Rename `_amount` to `amount` (lines ~388, 402, 409) -- it IS used in object properties

3. `apps/strapi/src/api/ad/services/ad.ts`:
   - Rename `_zohoEmail` to `zohoEmail` (lines ~677, 680, 681) -- it IS used

4. `apps/strapi/src/services/flow/services/flow.service.ts`:
   - Check `_endpoint` (line ~138) -- if it is used, rename to `endpoint`. If it is truly unused/dead code, remove it entirely.

Do NOT touch `_` prefixed parameters in interfaces/types (e.g., gateway.interface.ts, zoho/interfaces.ts, groq.types.ts, etc.) -- those are legitimate placeholders indicating the parameter name for implementors.

Do NOT touch `_` prefixed destructured rest variables (e.g., `_password` in userController.ts) -- those are correctly using the convention to discard values.
  </action>
  <verify>
    <automated>grep -rn '\b_zohoEmail\b\|_zohoAmount\b\|_amount\b' apps/strapi/src/api/payment/services/ad.service.ts apps/strapi/src/api/payment/services/checkout.service.ts apps/strapi/src/api/ad/services/ad.ts | wc -l</automated>
  </verify>
  <done>Zero hits for misleadingly-named `_zohoEmail`, `_zohoAmount`, `_amount` in production code. Interface/type placeholders remain with `_` prefix as intended.</done>
</task>

</tasks>

<verification>
- `grep -c "argsIgnorePattern" .codacy/tools-configs/eslint.config.mjs .eslintrc.json` returns 1 for each file
- `grep -rn '\b_zohoEmail\b\|\b_zohoAmount\b' apps/strapi/src --include='*.ts' | grep -v '__tests__' | grep -v '.test.' | wc -l` returns 0
- TypeScript compiles: `cd apps/strapi && npx tsc --noEmit` passes (or at least no new errors)
</verification>

<success_criteria>
- Codacy ESLint config and root .eslintrc.json both include argsIgnorePattern/varsIgnorePattern for `_` prefix
- No misleadingly-named `_` prefixed variables that are actually used remain in Strapi production code
- Interface/type parameter placeholders correctly retain `_` prefix
</success_criteria>

<output>
After completion, create `.planning/quick/260406-d4s-fix-codacy-unused-code-warnings-eslint-a/260406-d4s-SUMMARY.md`
</output>
