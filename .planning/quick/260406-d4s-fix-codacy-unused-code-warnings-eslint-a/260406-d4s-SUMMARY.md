---
phase: quick
plan: 260406-d4s
subsystem: codacy, strapi
tags: [eslint, codacy, unused-vars, dead-code]
dependency_graph:
  requires: []
  provides: [clean-codacy-unused-code-report]
  affects: [strapi-payment-services, strapi-ad-service, strapi-flow-service]
tech_stack:
  added: []
  patterns: [argsIgnorePattern for _ prefix convention]
key_files:
  created: []
  modified:
    - .codacy/tools-configs/eslint.config.mjs
    - .eslintrc.json
    - apps/strapi/src/api/payment/services/ad.service.ts
    - apps/strapi/src/api/payment/services/checkout.service.ts
    - apps/strapi/src/api/ad/services/ad.ts
    - apps/strapi/src/services/flow/services/flow.service.ts
key_decisions:
  - Added argsIgnorePattern/varsIgnorePattern for ^_ to Codacy ESLint config (force-added despite .codacy/.gitignore)
  - Removed _endpoint dead code from flow.service.ts rather than renaming (truly unused placeholder)
metrics:
  duration: 5m
  completed: "2026-04-06"
  tasks_completed: 2
  files_modified: 6
---

# Phase quick Plan 260406-d4s: Fix Codacy Unused-Code Warnings ESLint Summary

**One-liner:** Added argsIgnorePattern/varsIgnorePattern for `^_` to Codacy and root ESLint configs; renamed misleading `_`-prefixed variables in Strapi services to clarify they are actively used.

## What Was Done

### Task 1: Add argsIgnorePattern to Codacy and root ESLint configs

Updated `.codacy/tools-configs/eslint.config.mjs` and `.eslintrc.json` to include ignore patterns for the `_` prefix convention:

```js
"no-unused-vars": ["error", {
  argsIgnorePattern: "^_",
  varsIgnorePattern: "^_",
  caughtErrorsIgnorePattern: "^_",
  destructuredArrayIgnorePattern: "^_",
  ignoreRestSiblings: true
}]
```

This aligns Codacy's ESLint analysis with the app-level configs in `apps/website/eslint.config.mjs` and `apps/dashboard/eslint.config.mjs` so intentional `_`-prefixed placeholders are no longer flagged.

Note: `.codacy/tools-configs/` is listed in `.codacy/.gitignore` (generated by Codacy CLI), so force-added with `git add -f`.

### Task 2: Remove dead code and fix misleading _ prefixed variable names in Strapi

Renamed variables that had `_` prefix despite being actively used in production code:

| File | Old name | New name | Used in |
|------|----------|----------|---------|
| `ad.service.ts` | `_zohoEmail` | `zohoEmail` | conditional guard + findContact() call |
| `ad.service.ts` | `_zohoAmount` | `zohoAmount` | createDeal + updateContactStats |
| `checkout.service.ts` | `_amount` | `amount` | createDeal + updateContactStats |
| `api/ad/services/ad.ts` | `_zohoEmail` | `zohoEmail` | conditional guard + findContact() call |

Also removed genuinely dead code:
- `flow.service.ts`: removed `_endpoint` variable that was declared but never used (placeholder getPaymentStatus method uses dummy data, never calls the endpoint)

TypeScript compiles cleanly after all changes (`npx tsc --noEmit` — no output = no errors).

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None relevant to this plan's goal.

## Self-Check

- [x] `.codacy/tools-configs/eslint.config.mjs` contains `argsIgnorePattern`
- [x] `.eslintrc.json` contains `argsIgnorePattern`
- [x] Zero hits for `_zohoEmail`, `_zohoAmount`, `_amount` in modified production files
- [x] `_endpoint` removed from `flow.service.ts`
- [x] TypeScript compiles cleanly

## Self-Check: PASSED
