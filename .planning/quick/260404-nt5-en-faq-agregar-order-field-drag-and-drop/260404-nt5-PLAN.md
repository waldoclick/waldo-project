---
phase: quick-260404-nt5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/api/faq/content-types/faq/schema.json
  - apps/strapi/dist/src/api/faq/content-types/faq/schema.json
  - apps/dashboard/app/components/FaqsDefault.vue
  - apps/dashboard/app/scss/components/_faqs.scss
autonomous: true
requirements: [FAQ-ORDER, FAQ-DND, FAQ-LIST-UI]

must_haves:
  truths:
    - "FAQ items can be reordered via drag-and-drop in the dashboard list"
    - "Reorder persists the new order values to Strapi via PUT requests"
    - "ID column is hidden, order column is shown after drag handle"
    - "Pagination is removed; all records load on a single page with record count footer"
    - "Featured (Destacado) column is preserved"
  artifacts:
    - path: "apps/strapi/src/api/faq/content-types/faq/schema.json"
      provides: "order integer field in FAQ schema"
      contains: "\"order\""
    - path: "apps/dashboard/app/components/FaqsDefault.vue"
      provides: "Drag-and-drop FAQ list with order column"
      contains: "draggable"
    - path: "apps/dashboard/app/scss/components/_faqs.scss"
      provides: "Drag handle, footer, saving indicator styles"
      contains: "&__drag"
  key_links:
    - from: "FaqsDefault.vue"
      to: "/api/faqs/:documentId"
      via: "PUT request in handleReorder"
      pattern: "apiClient.*faqs.*PUT"
---

<objective>
Apply the same features from PoliciesDefault.vue to FaqsDefault.vue: add order field to FAQ schema, add drag-and-drop reorder, hide ID column, show order column after drag handle, remove pagination (show all records with record count footer), hide page size selector.

Purpose: Consistent admin experience between FAQ and Policy list views with manual ordering support.
Output: FAQ list with drag-and-drop reorder matching the policies implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/dashboard/app/components/PoliciesDefault.vue (REFERENCE — copy this pattern exactly)
@apps/dashboard/app/components/FaqsDefault.vue (TARGET — transform to match policies)
@apps/dashboard/app/scss/components/_policies.scss (REFERENCE — copy drag/footer styles)
@apps/dashboard/app/scss/components/_faqs.scss (TARGET — add drag/footer styles)
@apps/strapi/src/api/faq/content-types/faq/schema.json (TARGET — add order field)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add order field to FAQ Strapi schema</name>
  <files>apps/strapi/src/api/faq/content-types/faq/schema.json, apps/strapi/dist/src/api/faq/content-types/faq/schema.json</files>
  <action>
Add `"order": { "type": "integer" }` to the `attributes` object in both the src and dist schema.json files for the FAQ content type. Place it after the existing `text` attribute. The FAQ schema currently has: title (string), featured (boolean), text (text). After the change it should have: title, featured, text, order.
  </action>
  <verify>
    <automated>cat apps/strapi/src/api/faq/content-types/faq/schema.json | grep -q '"order"' && cat apps/strapi/dist/src/api/faq/content-types/faq/schema.json | grep -q '"order"' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both FAQ schema files contain the order integer attribute.</done>
</task>

<task type="auto">
  <name>Task 2: Transform FaqsDefault.vue to match PoliciesDefault.vue pattern</name>
  <files>apps/dashboard/app/components/FaqsDefault.vue, apps/dashboard/app/scss/components/_faqs.scss</files>
  <action>
Rewrite FaqsDefault.vue following PoliciesDefault.vue as the EXACT reference, with these FAQ-specific differences:

**Template changes (mirroring PoliciesDefault.vue):**
1. Remove `:page-sizes="[10, 25, 50, 100]"` from `<FilterDefault>` (keep sort-options)
2. Add drag-note paragraph before table-wrapper: `<p v-if="!isDraggable" class="faqs--default__drag-note">El arrastre para reordenar no esta disponible mientras se filtra.</p>`
3. Replace `<TableDefault :columns="tableColumns">` with inline table structure (div.table.table--default > table > thead + draggable tbody) — copy from PoliciesDefault.vue
4. Table columns in thead: empty th (drag), "Orden", "Pregunta", "Respuesta", "Destacado", "Fecha", "Acciones" (right-aligned)
5. In draggable tbody template: drag handle button (GripVertical), order cell (`faq.order ?? "-"`), title cell (keep existing tooltip/truncate), text cell (keep existing tooltip/truncate), featured cell (keep BadgeDefault Destacado/No destacado), date cell, actions cell (keep Eye/Pencil buttons)
6. Draggable props: `v-model="allFaqs"`, `tag="tbody"`, `item-key="id"`, `handle=".faqs--default__drag"`, `:disabled="!isDraggable"`, `class="table--default__body"`, `@end="handleReorder"`
7. Replace `<PaginationDefault>` with footer div matching PoliciesDefault: record count + saving indicator
8. Use `allFaqs` instead of `paginatedFaqs` in empty state check

**Script changes:**
1. Update `Faq` interface: add `documentId: string` and `order: number | null`
2. Add imports: `GripVertical` from lucide-vue-next, `draggable` from vuedraggable
3. Remove imports: `TableDefault`, `PaginationDefault`
4. Remove: `paginationMeta` ref, `totalPages` computed, `totalRecords` computed, `paginatedFaqs` computed, `tableColumns` array
5. Add: `saving` ref (false), `isDraggable` computed (`!settingsStore.faqs.searchTerm`)
6. In `fetchFaqs`: change pagination to `{ pageSize: 200 }` (fetch all records, no page/pageSize from store). Remove `paginationMeta.value` assignment from response handling
7. Add `handleReorder` async function — copy exactly from PoliciesDefault but change `allPolicies` to `allFaqs`, `policies` endpoint to `faqs` endpoint, and variable names accordingly
8. Add sort options for order: `{ value: "order:asc", label: "Orden asc." }` and `{ value: "order:desc", label: "Orden desc." }` as first two entries in sortOptions
9. Update watch: remove `() => settingsStore.faqs.pageSize` and `() => settingsStore.faqs.currentPage` from watch array (keep only searchTerm and sortBy)
10. Keep `handleViewFaq` and `handleEditFaq` using numeric id (unchanged)

**SCSS changes in _faqs.scss:**
1. Remove `&__pagination` block (lines 105-107)
2. Add these blocks (copy from _policies.scss, just change class context to faqs--default namespace):
   - `&__drag` block with grab cursor, 28x28, no border/bg, color: $davys_grey, :active grabbing, `&--disabled` not-allowed + opacity 0.3, `&__icon` 16x16
   - `&__drag-note` block: font-size 13px, color $davys_grey, margin 0, padding 0 4px
   - `&__footer` block: flex, center, space-between, gap 12px
   - `&__count` block: font-size 13px, color $davys_grey, margin 0
   - `&__saving` block: font-size 13px, color $davys_grey, margin 0
  </action>
  <verify>
    <automated>cd apps/dashboard && npx nuxi typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>FaqsDefault.vue has drag-and-drop reorder, order column, no ID column, no pagination, footer with record count, featured column preserved. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `cd apps/dashboard && npx nuxi typecheck` passes with no errors
- FAQ schema has order field in both src and dist
- FaqsDefault.vue contains `draggable` import and component usage
- FaqsDefault.vue contains `handleReorder` function with PUT to `/faqs/:documentId`
- No `PaginationDefault` import or usage in FaqsDefault.vue
- No `tableColumns` array — columns hardcoded in template
- Featured (Destacado) badge column still present
</verification>

<success_criteria>
FAQ list view in dashboard matches policies list view pattern: drag-and-drop reorder with order persistence, no pagination, all records on single page with count footer, order column visible, ID column hidden, featured column preserved.
</success_criteria>

<output>
After completion, create `.planning/quick/260404-nt5-en-faq-agregar-order-field-drag-and-drop/260404-nt5-01-SUMMARY.md`
</output>
