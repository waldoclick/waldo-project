---
phase: quick-260405-tf1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/strapi/src/extensions/users-permissions/content-types/user/schema.json
  - apps/website/app/types/user.d.ts
  - apps/website/app/types/form-register.d.ts
  - apps/website/app/components/FormRegister.vue
  - apps/website/app/components/FormTerms.vue
  - apps/website/app/components/LightboxTerms.vue
  - apps/website/app/composables/useUser.ts
autonomous: true
requirements: [TF1-01]

must_haves:
  truths:
    - "Registration form has three checkboxes: age confirmation, privacy policy, usage terms"
    - "Terms lightbox (FormTerms) has three matching checkboxes with same labels"
    - "Lightbox body text does NOT mention terms or privacy policy — links only in checkboxes"
    - "accepted_terms checkbox label says only 'politicas de privacidad' with link to /politicas-de-privacidad"
    - "New checkbox label says 'condiciones de uso' with link to /condiciones-de-uso"
    - "User cannot register or accept terms without checking all three checkboxes"
  artifacts:
    - path: "apps/strapi/src/extensions/users-permissions/content-types/user/schema.json"
      contains: "accepted_usage_terms"
    - path: "apps/website/app/types/user.d.ts"
      contains: "accepted_usage_terms"
    - path: "apps/website/app/types/form-register.d.ts"
      contains: "accepted_usage_terms"
  key_links:
    - from: "apps/website/app/composables/useUser.ts"
      to: "hasAcceptedTerms computed"
      via: "checks accepted_usage_terms === true alongside existing booleans"
      pattern: "accepted_usage_terms.*===.*true"
    - from: "apps/website/app/composables/useUser.ts"
      to: "acceptTerms function"
      via: "sends accepted_usage_terms: true in update payload"
      pattern: "accepted_usage_terms.*true"
---

<objective>
Add a third checkbox for "condiciones de uso" in both the registration form and the terms acceptance lightbox. Remove terms/policy text from the lightbox body so links only appear in the checkboxes themselves.

Purpose: Each legal document (privacy policy, usage terms) gets its own dedicated checkbox with a link, making consent explicit and separated.
Output: Updated registration form, terms form, lightbox, Strapi user schema, and type definitions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/strapi/src/extensions/users-permissions/content-types/user/schema.json
@apps/website/app/types/user.d.ts
@apps/website/app/types/form-register.d.ts
@apps/website/app/components/FormRegister.vue
@apps/website/app/components/FormTerms.vue
@apps/website/app/components/LightboxTerms.vue
@apps/website/app/composables/useUser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accepted_usage_terms field to Strapi user schema and website types</name>
  <files>
    apps/strapi/src/extensions/users-permissions/content-types/user/schema.json,
    apps/website/app/types/user.d.ts,
    apps/website/app/types/form-register.d.ts
  </files>
  <action>
    1. In `schema.json`, add a new field `accepted_usage_terms` after `accepted_terms`:
       ```json
       "accepted_usage_terms": {
         "type": "boolean",
         "default": false
       }
       ```

    2. In `apps/website/app/types/user.d.ts`, add `accepted_usage_terms: boolean;` after `accepted_terms`.

    3. In `apps/website/app/types/form-register.d.ts`, add `accepted_usage_terms: boolean;` after `accepted_terms`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -q "accepted_usage_terms" apps/strapi/src/extensions/users-permissions/content-types/user/schema.json && grep -q "accepted_usage_terms" apps/website/app/types/user.d.ts && grep -q "accepted_usage_terms" apps/website/app/types/form-register.d.ts && echo "PASS"</automated>
  </verify>
  <done>All three files contain the new accepted_usage_terms field.</done>
</task>

<task type="auto">
  <name>Task 2: Add condiciones de uso checkbox to FormRegister and FormTerms, update LightboxTerms body text, update useUser composable</name>
  <files>
    apps/website/app/components/FormRegister.vue,
    apps/website/app/components/FormTerms.vue,
    apps/website/app/components/LightboxTerms.vue,
    apps/website/app/composables/useUser.ts
  </files>
  <action>
    **FormRegister.vue:**

    1. In the `form` ref initial value, add `accepted_usage_terms: false`.

    2. Change the `accepted_terms` checkbox label from "Acepto los terminos y las politicas de privacidad" to just "Acepto las politicas de privacidad" (keep the NuxtLink to `/politicas-de-privacidad`).

    3. Add a new checkbox block AFTER the `accepted_terms` block (copy the same form-group/form-check pattern):
       - Field: `id="accepted_usage_terms"`, `v-model="form.accepted_usage_terms"`, `name="accepted_usage_terms"`, `type="checkbox"`, `:value="true"`, `:unchecked-value="false"`
       - Label: `Acepto las <NuxtLink to="/condiciones-de-uso" target="_blank">condiciones de uso</NuxtLink>`
       - Add `<ErrorMessage name="accepted_usage_terms" />` after the form-check div.

    4. In the yup validation schema (step 2 branch), add:
       ```typescript
       accepted_usage_terms: yup
         .boolean()
         .oneOf([true], "Debes aceptar las condiciones de uso")
         .required("Debes aceptar las condiciones de uso"),
       ```

    5. Update the `accepted_terms` validation message from "Debes aceptar los terminos y politicas de privacidad" to "Debes aceptar las politicas de privacidad".

    **FormTerms.vue:**

    1. Add a new ref: `const usageTermsAccepted = ref(false);`

    2. Update the `canSubmit` computed to also require `usageTermsAccepted.value`.

    3. Change the `terms-accepted` checkbox label from "Acepto los terminos y las politicas de privacidad" to "Acepto las politicas de privacidad" (keep the NuxtLink to `/politicas-de-privacidad`).

    4. Add a new checkbox block AFTER the terms-accepted block:
       - `<input id="terms-usage-accepted" v-model="usageTermsAccepted" type="checkbox" class="form-check-input" />`
       - Label: `Acepto las <NuxtLink to="/condiciones-de-uso" target="_blank">condiciones de uso</NuxtLink>`

    **LightboxTerms.vue:**

    1. Change the `lightbox--terms__text` content from:
       "Para continuar usando el sitio debes confirmar que eres mayor de edad y aceptar nuestros terminos y politicas de privacidad."
       to:
       "Para continuar usando el sitio debes confirmar que eres mayor de edad y aceptar los siguientes terminos."
       Remove the NuxtLink entirely from this text — links now only live in the checkboxes.

    **useUser.ts composable:**

    1. Update `hasAcceptedTerms` computed to also check `user.value.accepted_usage_terms === true` in the return condition.

    2. Update `acceptTerms` function to include `accepted_usage_terms: true` in the updateUserProfile payload.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && npx nuxi typecheck --dir apps/website 2>&1 | tail -5</automated>
  </verify>
  <done>
    - FormRegister has 3 checkboxes: age, privacy policy (link to /politicas-de-privacidad), usage terms (link to /condiciones-de-uso)
    - FormTerms has 3 matching checkboxes with same labels and links
    - LightboxTerms body text is generic (no links or policy mentions)
    - useUser hasAcceptedTerms checks all three booleans
    - useUser acceptTerms sends all three booleans as true
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
- `npx nuxi typecheck --dir apps/website` passes
- Visually: registration page step 2 shows three checkboxes with correct labels and links
- Visually: terms lightbox shows three checkboxes, body text has no links
- Submit button disabled until all three checkboxes checked (both forms)
</verification>

<success_criteria>
- Three separate consent checkboxes in both FormRegister and FormTerms
- Each legal document linked only from its own checkbox, not from body text
- Strapi user schema has accepted_usage_terms boolean field
- TypeScript types updated and type check passes
</success_criteria>

<output>
After completion, create `.planning/quick/260405-tf1-en-el-registro-y-en-el-lightbox-de-acept/260405-tf1-SUMMARY.md`
</output>
