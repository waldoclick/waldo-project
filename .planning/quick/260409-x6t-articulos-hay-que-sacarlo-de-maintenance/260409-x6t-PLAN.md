---
phase: 260409-x6t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/pages/articles/index.vue
  - apps/dashboard/app/pages/articles/new.vue
  - apps/dashboard/app/pages/articles/[id]/index.vue
  - apps/dashboard/app/pages/articles/[id]/edit.vue
  - apps/dashboard/app/pages/maintenance/articles/index.vue
  - apps/dashboard/app/pages/maintenance/articles/new.vue
  - apps/dashboard/app/pages/maintenance/articles/[id]/index.vue
  - apps/dashboard/app/pages/maintenance/articles/[id]/edit.vue
  - apps/dashboard/app/components/MenuMain.vue
  - apps/dashboard/app/components/MenuMaintenance.vue
  - apps/dashboard/app/components/MenuMobile.vue
  - apps/dashboard/app/components/ToolbarDefault.vue
  - apps/dashboard/app/components/FormArticle.vue
autonomous: true
requirements:
  - X6T-01
  - X6T-02
  - X6T-03

must_haves:
  truths:
    - "Visiting /articles renders the articles list page"
    - "Visiting /articles/new renders the new article form"
    - "Visiting /articles/{id} renders the article detail page"
    - "Visiting /articles/{id}/edit renders the edit article form"
    - "MenuMain rail shows a Newspaper icon linking to /articles"
    - "MenuMaintenance no longer lists Artículos"
    - "All internal references to /maintenance/articles point to /articles"
    - "Old /maintenance/articles routes return 404 (files removed)"
  artifacts:
    - path: "apps/dashboard/app/pages/articles/index.vue"
      provides: "Articles list page at /articles"
    - path: "apps/dashboard/app/pages/articles/new.vue"
      provides: "New article form at /articles/new"
    - path: "apps/dashboard/app/pages/articles/[id]/index.vue"
      provides: "Article detail page at /articles/{id}"
    - path: "apps/dashboard/app/pages/articles/[id]/edit.vue"
      provides: "Article edit page at /articles/{id}/edit"
  key_links:
    - from: "apps/dashboard/app/components/MenuMain.vue"
      to: "/articles"
      via: "NuxtLink in rail"
      pattern: "to=\"/articles\""
    - from: "apps/dashboard/app/components/FormArticle.vue"
      to: "/articles"
      via: "router.push after save"
      pattern: "router.push\\(\"/articles\""
---

<objective>
Move articles maintainer out of /maintenance into top-level /articles, add a Newspaper rail icon in MenuMain linking to it, and remove the Artículos entry from MenuMaintenance.

Purpose: Articles is high-traffic content (already pinned in MenuMobile and ToolbarDefault) — promoting it from a maintenance sub-route to a top-level dashboard section reduces clicks and aligns with the rail-based navigation pattern established in 260409-kru / 260409-pr8 / 260409-ujs.

Output: New apps/dashboard/app/pages/articles/ directory mirroring the previous structure, MenuMain rail updated, all references repointed, old maintenance/articles files removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@apps/dashboard/app/layouts/dashboard.vue
@apps/dashboard/app/components/MenuMain.vue
@apps/dashboard/app/components/MenuMaintenance.vue
@apps/dashboard/app/components/MenuMobile.vue
@apps/dashboard/app/components/ToolbarDefault.vue
@apps/dashboard/app/components/FormArticle.vue
@apps/dashboard/app/pages/maintenance/articles/index.vue
@apps/dashboard/app/pages/maintenance/articles/new.vue
@apps/dashboard/app/pages/maintenance/articles/[id]/index.vue
@apps/dashboard/app/pages/maintenance/articles/[id]/edit.vue

<interfaces>
<!-- MenuMain currently exposes a single union prop activeMenu. -->
<!-- Articles will be a flat rail link (no panel), like the dashboard home "/" link — -->
<!-- so MenuMain's activeMenu union does NOT need to grow, and dashboard.vue's -->
<!-- resolveActiveMenu does NOT need a new branch. -->

From apps/dashboard/app/components/MenuMain.vue:
```ts
defineProps<{
  activeMenu: "default" | "users" | "maintenance" | "integrations";
}>();
```

From apps/dashboard/app/layouts/dashboard.vue:
```ts
const resolveActiveMenu = (
  path: string,
): "default" | "users" | "maintenance" | "integrations" => {
  if (path.startsWith("/users")) return "users";
  if (path.startsWith("/maintenance")) return "maintenance";
  if (path.startsWith("/integrations")) return "integrations";
  return "default";
};
```

Existing precedent — flat rail link without panel (the dashboard home icon):
```vue
<NuxtLink to="/" class="menu--main__btn" aria-label="Dashboard">
  <LayoutDashboard class="menu--main__icon" />
</NuxtLink>
```

The Newspaper icon is already imported from lucide-vue-next in MenuMaintenance.vue and MenuMobile.vue — same import path applies in MenuMain.vue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move /maintenance/articles pages to /articles using git mv</name>
  <files>
    apps/dashboard/app/pages/articles/index.vue,
    apps/dashboard/app/pages/articles/new.vue,
    apps/dashboard/app/pages/articles/[id]/index.vue,
    apps/dashboard/app/pages/articles/[id]/edit.vue
  </files>
  <action>
    Use `git mv` (per CLAUDE.md refactoring rule: "git mv for all Nuxt page directory and file renames — preserves Git rename history") to relocate the entire articles directory:

    ```bash
    cd apps/dashboard/app/pages
    mkdir -p articles
    git mv maintenance/articles/index.vue articles/index.vue
    git mv maintenance/articles/new.vue articles/new.vue
    git mv maintenance/articles/[id] articles/[id]
    rmdir maintenance/articles 2>/dev/null || true
    ```

    Then update internal hardcoded route strings inside the moved files (breadcrumbs, NuxtLink `to` attributes, page titles linking back to the list):
    - `apps/dashboard/app/pages/articles/index.vue` — line 13 area: `to="/maintenance/articles/new"` → `to="/articles/new"`
    - `apps/dashboard/app/pages/articles/new.vue` — line 25 area: breadcrumb `{ label: "Artículos", to: "/maintenance/articles" }` → `to: "/articles"`
    - `apps/dashboard/app/pages/articles/[id]/index.vue` — line 7: `:to="\`/maintenance/articles/${route.params.id}/edit\`"` → `:to="\`/articles/${route.params.id}/edit\`"`; line 151: breadcrumb `to: "/maintenance/articles"` → `to: "/articles"`
    - `apps/dashboard/app/pages/articles/[id]/edit.vue` — apply same breadcrumb / link updates (read the file first to find all `/maintenance/articles` occurrences and rewrite each to `/articles`)

    Do NOT change Spanish UI labels ("Artículos", "Nuevo artículo", etc.) — only the route paths.

    Do NOT change the file structure, imports, components used, store calls, or any logic — this is a pure relocation + path string update.
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -rn "/maintenance/articles" app/pages/articles/ ; test ! -d app/pages/maintenance/articles && echo OK</automated>
  </verify>
  <done>
    apps/dashboard/app/pages/articles/ contains index.vue, new.vue, [id]/index.vue, [id]/edit.vue with all internal route strings rewritten to /articles. apps/dashboard/app/pages/maintenance/articles/ no longer exists. Git history shows renames (not delete + add).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Newspaper rail icon to MenuMain and remove Artículos from MenuMaintenance</name>
  <files>
    apps/dashboard/app/components/MenuMain.vue,
    apps/dashboard/app/components/MenuMaintenance.vue
  </files>
  <action>
    **MenuMain.vue** — Add a new flat rail link for Articles. Place it AFTER the dashboard home link and BEFORE the Users link (so the rail order becomes: Dashboard → Articles → Users → Maintenance → Integrations). Follow the EXACT pattern of the existing dashboard home link (flat NuxtLink with no `:class` active state, no `activeMenu` check) — articles is a flat top-level route with no submenu panel:

    ```vue
    <NuxtLink to="/articles" class="menu--main__btn" aria-label="Artículos">
      <Newspaper class="menu--main__icon" />
    </NuxtLink>
    ```

    Update the lucide-vue-next import line to add `Newspaper`:
    ```ts
    import { LayoutDashboard, Newspaper, Users, Settings, Plug } from "lucide-vue-next";
    ```

    Do NOT modify the `defineProps<{ activeMenu: ... }>()` union — articles has no panel, so the union stays as `"default" | "users" | "maintenance" | "integrations"`. Do NOT touch dashboard.vue layout — `resolveActiveMenu` does not need an `/articles` branch (it will fall through to `"default"`, which is correct since articles has no nav panel).

    **MenuMaintenance.vue** — Remove the entire Artículos `<li>` block (the one wrapping `<NuxtLink to="/maintenance/articles">` with the Newspaper icon). Also remove `"/maintenance/articles"` from the `knownSubRoutes` array. Remove `Newspaper` from the lucide-vue-next import line if it is no longer used elsewhere in the file (it should not be after this removal — verify by searching the file).
  </action>
  <verify>
    <automated>cd apps/dashboard && grep -n "Newspaper\|/articles" app/components/MenuMain.vue && ! grep -n "articles" app/components/MenuMaintenance.vue && yarn nuxt typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    MenuMain.vue contains a Newspaper rail link to /articles between Dashboard and Users. MenuMaintenance.vue no longer references articles (no list item, no knownSubRoutes entry, no Newspaper import). Dashboard typecheck passes with zero new errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Repoint remaining references and verify build</name>
  <files>
    apps/dashboard/app/components/MenuMobile.vue,
    apps/dashboard/app/components/ToolbarDefault.vue,
    apps/dashboard/app/components/FormArticle.vue
  </files>
  <action>
    Update the remaining three files that still link to `/maintenance/articles` (per CLAUDE.md atomic-rename rule: "File renames must update all import references atomically in the same commit"):

    - **MenuMobile.vue** line 18: `to="/maintenance/articles"` → `to="/articles"`
    - **ToolbarDefault.vue** line 7: `to="/maintenance/articles"` → `to="/articles"`
    - **FormArticle.vue** line 276: `router.push("/maintenance/articles")` → `router.push("/articles")`

    After edits, run a project-wide grep to confirm zero remaining references:

    ```bash
    cd apps/dashboard && grep -rn "/maintenance/articles" app/ server/ 2>/dev/null
    ```

    Expected: empty output.

    Then run the dashboard typecheck and the root Codacy scan to confirm no regressions:

    ```bash
    yarn workspace dashboard nuxt typecheck
    yarn codacy
    ```

    Do NOT touch any Strapi backend code — articles is a Strapi content type and the API routes remain unchanged. Only the dashboard URL prefix is changing.
  </action>
  <verify>
    <automated>cd apps/dashboard && ! grep -rn "/maintenance/articles" app/ server/ 2>/dev/null && yarn nuxt typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>
    Zero references to `/maintenance/articles` anywhere in apps/dashboard/. Dashboard typecheck passes. `yarn codacy` reports no new issues introduced by this change.
  </done>
</task>

</tasks>

<verification>
1. Manual smoke test (dev server): visit `/articles`, `/articles/new`, click into an existing article to reach `/articles/{id}`, click "Editar" to reach `/articles/{id}/edit`, save the form, confirm the redirect lands on `/articles` (not `/maintenance/articles`).
2. Confirm the MenuMain rail shows the Newspaper icon between the Dashboard and Users icons, and clicking it navigates to `/articles`.
3. Confirm MenuMaintenance no longer shows an Artículos entry.
4. Confirm `/maintenance/articles` returns Nuxt's 404 page.
5. `yarn nuxt typecheck` clean.
6. `yarn codacy` clean.
</verification>

<success_criteria>
- /articles, /articles/new, /articles/{id}, /articles/{id}/edit all render the same content as the previous /maintenance/articles equivalents
- MenuMain rail has a Newspaper icon linking to /articles
- MenuMaintenance no longer lists Artículos
- Zero references to `/maintenance/articles` in apps/dashboard/
- Git history shows renames (git log --follow works on the moved files)
- Dashboard typecheck and Codacy clean
</success_criteria>

<output>
After completion, create `.planning/quick/260409-x6t-articulos-hay-que-sacarlo-de-maintenance/260409-x6t-SUMMARY.md`
</output>
