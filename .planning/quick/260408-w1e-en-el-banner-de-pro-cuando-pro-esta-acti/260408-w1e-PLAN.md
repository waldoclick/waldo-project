---
phase: quick
plan: 260408-w1e
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/components/MemoPro.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - "When pro_status is 'active', a green 'Activa' badge appears next to 'Suscripción PRO'"
    - "When pro_status is 'cancelled', the red 'Cancelada' badge still appears"
    - "Card info line does not show '**** ' prefix when pro_card_type is null/undefined"
  artifacts:
    - path: apps/website/app/components/MemoPro.vue
      provides: Status badge for both active and cancelled states
  key_links:
    - from: MemoPro.vue
      to: user.pro_status
      via: v-if condition on badge span
      pattern: "pro_status === 'active'"
---

<objective>
Restore the status badge display for active PRO subscriptions in MemoPro.vue.

Purpose: When a user has an active PRO subscription, the banner shows "Suscripción PRO" with no status indicator — it looks broken. The badge for 'active' was removed in a refactor and must be restored.
Output: MemoPro.vue with both active and cancelled badge spans visible.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/website/app/components/MemoPro.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore active status badge and fix card info display</name>
  <files>apps/website/app/components/MemoPro.vue</files>
  <action>
In the subscriber section (inside `<template v-else>`), the `<p class="memo--pro__text__status">` currently only has a badge span for `cancelled`. Add a sibling badge span for `active` status.

Replace this block:

```html
<p class="memo--pro__text__status">
  <span
    v-if="user?.pro_status === 'cancelled'"
    class="memo--pro__text__status__badge memo--pro__text__status__badge--cancelled"
  >
    Cancelada
  </span>
  Suscripción PRO
</p>
```

With:

```html
<p class="memo--pro__text__status">
  <span
    v-if="user?.pro_status === 'active'"
    class="memo--pro__text__status__badge memo--pro__text__status__badge--active"
  >
    Activa
  </span>
  <span
    v-if="user?.pro_status === 'cancelled'"
    class="memo--pro__text__status__badge memo--pro__text__status__badge--cancelled"
  >
    Cancelada
  </span>
  Suscripción PRO
</p>
```

Also fix the card info line so it does not display "**** " when `pro_card_type` is null. Replace:

```html
<p class="memo--pro__text__card">
  {{ user?.pro_card_type }} **** {{ user?.pro_card_last4 }}
</p>
```

With:

```html
<p v-if="user?.pro_card_type && user?.pro_card_last4" class="memo--pro__text__card">
  {{ user?.pro_card_type }} **** {{ user?.pro_card_last4 }}
</p>
```

Check if the `--active` modifier CSS class already exists in the companion SCSS file. Read the SCSS file to confirm — if `memo--pro__text__status__badge--active` is missing, add it following the same pattern as `--cancelled` but using `$magic_mint` (#c2e3d9) background with `$charcoal` (#313338) text. Do NOT add `box-shadow` or `transform: scale`.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && yarn workspace website typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Active subscriptions show a green "Activa" badge
    - Cancelled subscriptions still show the red "Cancelada" badge
    - Card info line is hidden when card data is absent
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
Run `yarn workspace website typecheck` — must exit with 0 errors related to MemoPro.vue.
Visually: log in with an active PRO user and confirm the banner shows the "Activa" badge alongside "Suscripción PRO".
</verification>

<success_criteria>
MemoPro.vue renders the status badge for both active and cancelled PRO subscriptions. No TypeScript errors introduced.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-w1e-en-el-banner-de-pro-cuando-pro-esta-acti/260408-w1e-SUMMARY.md`
</output>
