# Maqueta CSS vars → existing SCSS tokens (app/scss/abstracts/_variables.scss)
DO NOT create or modify tokens — these already exist, use them.
--ink   #26252B → $ink
--ink2  #56535F → $ink2
--muted #8A8794 → $muted
--amber #F7C97E → $amber
--amberH#EFB85C → $amber_hover
--cream #F6F4F1 → $cream
--line  #ECE9E4 → $line
--tag   #26252B → $ink (same)
