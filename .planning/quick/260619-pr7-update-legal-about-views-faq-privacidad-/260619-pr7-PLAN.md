---
phase: quick-260619-pr7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/app/scss/components/_layout.scss
  - apps/website/app/scss/components/_menu.scss
  - apps/website/app/scss/components/_accordion.scss
  - apps/website/app/scss/components/_contact.scss
  - apps/website/app/scss/components/_address.scss
  - apps/website/app/scss/components/_sitemap.scss
  - apps/design
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
autonomous: true
requirements: [PR7-01, PR7-02, PR7-03, PR7-04, PR7-05, PR7-06]

must_haves:
  truths:
    - "The /preguntas-frecuentes, /politicas-de-privacidad, /condiciones-de-uso legal shell + accordion render pixel-matched to legal-faq.png (cream header w/ amber glow, breadcrumb, H1, intro, sticky 5-link sidebar with active=cream/amber-tint state, accordion cards, dark Empieza ahora CTA, footer)"
    - "The /contacto view renders pixel-matched to legal-contacto.png (form fields + phone row + counter + amber submit; white contact block with mailto + 3 social buttons) while preserving the existing FormContact validation/submit/Swal logic"
    - "The /sitemap view renders the 3-column grid treatment from the maqueta (amber-dot uppercase headers w/ $line underline, link rows with right chevron + $line separators); dynamic categories/comunas content is preserved (no store/API changes)"
    - "apps/design is removed from the workspace and pnpm install resolves cleanly with no dangling references"
  artifacts:
    - path: "apps/website/app/scss/components/_layout.scss"
      provides: ".layout--about legal shell (header glow, sticky sidebar, content grid)"
      contains: "&--about"
    - path: "apps/website/app/scss/components/_accordion.scss"
      provides: "shared AccordionDefault styling (faq/priv/cond + homepage featured FAQ)"
      contains: ".accordion"
    - path: "apps/website/app/scss/components/_contact.scss"
      provides: ".contact--default form styling"
      contains: "&--default"
    - path: "apps/website/app/scss/components/_sitemap.scss"
      provides: ".sitemap--default 3-col grid treatment"
      contains: "&--default"
    - path: "pnpm-workspace.yaml"
      provides: "workspace package list without apps/design"
  key_links:
    - from: "app/layouts/about.vue"
      to: ".layout--about + .menu--about + WhyCta"
      via: "NuxtLayout name=about wraps all 5 pages"
      pattern: "layout--about"
    - from: "ContactDefault.vue"
      to: "FormContact + AddressDefault"
      via: ".contact--default__form / __address"
      pattern: "contact--default"
---

<objective>
Audit-and-fix the 5 legal/about public views (faq, privacidad, condiciones, contacto, sitemap) against the maqueta, then remove the apps/design workspace.

IMPORTANT — READ FIRST: Phase 07 (07-02/07-03) ALREADY migrated these 5 views to the maqueta. The components (about.vue layout, MenuAbout, AccordionDefault, ContactDefault/FormContact/AddressDefault, SitemapDefault) have correct BEM, and _layout/_menu/_accordion/_contact/_address/_sitemap.scss already carry the maqueta's tokens and spacing. This is NOT a rebuild. The job is: screenshot each view at :3000 → diff vs the maqueta reference → fix ONLY the diffs → re-verify by looking. Do NOT rewrite working, phase-07-owned shared code. If a view already matches, mark it verified and touch nothing.

Purpose: pixel-match the 5 legal views to the approved maqueta and drop the now-unused design workspace.
Output: screenshots in /reference/after/, minimal SCSS diffs where needed, apps/design removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260619-pr7-update-legal-about-views-faq-privacidad-/reference/maqueta-legal-section.html
@.planning/quick/260619-pr7-update-legal-about-views-faq-privacidad-/reference/legal-faq.png
@.planning/quick/260619-pr7-update-legal-about-views-faq-privacidad-/reference/legal-contacto.png
@.planning/quick/260619-pr7-update-legal-about-views-faq-privacidad-/reference/tokens.md
@CLAUDE.md

<constraints>
HARD CONSTRAINTS (CLAUDE.md + project memory):
- Use EXISTING SCSS tokens in app/scss/abstracts/_variables.scss. DO NOT create new tokens, DO NOT modify existing ones. Confirmed existing redesign tokens: $ink #26252b, $ink2 #56535f, $muted #8a8794, $amber #f7c97e, $amber_hover #efb85c, $amber_tint #fef8ec, $tag #a9772e, $cream #f6f4f1, $line #ece9e4, $silver #c8c8c9, $white #fff. (Note: tokens.md maps maqueta --tag → $ink for general text, but the maqueta email-icon stroke is #A9772E which is the $tag token — _address.scss already uses $tag correctly; leave it.)
- Strict BEM: every class under the block/modifier namespace. NEVER standalone hyphenated/compound classes. Mirror existing naming.
- Pages stay composition-only. Update components + their single SCSS file only. Do NOT move markup into pages.
- Container rule: a div @extending .container stays clean; vertical padding goes on the <section>.
- Brand/redesign colors only via tokens — no invented hexes.
- Do NOT add extra Strapi/API calls. faq/policies/terms come from existing stores; sitemap categories/comunas come from existing stores — KEEP THEM.
- Do NOT break SEO/structured-data or the useAsyncData keys in the pages ("faqs", "policies", "terms", "sitemap-data").
- faq/priv/cond SHARE AccordionDefault (also rendered with featured FAQs on the HOMEPAGE). Any _accordion.scss change is GLOBAL — verify the homepage FAQ too.
- VISUAL VERIFICATION IS MANDATORY (project memory: never verify a redesign by typecheck alone). Each view task screenshots the running site at http://localhost:3000 (public pages, no auth/JWT), compares side-by-side to the maqueta reference, captures into /reference/after/, and notes diffs fixed. Run typecheck too, but never mark a view done on typecheck alone.
</constraints>

<setup>
Before the view tasks, ensure the website dev server is running and reachable:
- `pnpm --filter website dev` (or confirm an existing server) and confirm http://localhost:3000 responds.
- Use Playwright (mcp or scripted) for screenshots at 1440px-wide desktop viewport (matches maqueta max-width 1200px container) and a 390px mobile pass for layout sanity.
- Reference targets: legal-faq.png → faq/priv/cond; legal-contacto.png → contacto; maqueta HTML `<!-- MAPA DEL SITIO -->` block → sitemap (no screenshot exists for sitemap; use the HTML treatment as the visual target).
</setup>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify+fix legal shell (header, sidebar, CTA) on faq/priv/cond/contacto/sitemap</name>
  <files>apps/website/app/scss/components/_layout.scss, apps/website/app/scss/components/_menu.scss</files>
  <action>
    The legal shell is `.layout--about` (in _layout.scss) + `.menu--about` sidebar (in _menu.scss) + WhyCta closing CTA, all wrapped by layouts/about.vue. These already match the maqueta `<!-- VISTAS PÚBLICAS (LEGAL) -->` header/sidebar/CTA blocks — confirm, don't rebuild.

    Screenshot /preguntas-frecuentes (and one of /contacto or /sitemap to confirm the shell on a non-accordion view) at desktop. Diff against legal-faq.png and the maqueta HTML, focusing on the shell ONLY (accordion body is Task 2, contact form is Task 3):
    - Header: bg $cream, border-bottom $line, radial amber glow top-right (top -130px / right -90px / 460px), container max 1200px, padding 108px 32px 40px, breadcrumb "Waldo › {Title}" 13.5px $muted with chevron sep, H1 Poppins 800 clamp(32px,3.6vw,46px) letter-spacing -.7px, intro 17px line-height 1.55 $ink2 max 680px.
    - Body grid: 248px / minmax(0,1fr), gap 56px, align-items start, padding 48px 32px 88px.
    - Sidebar (`.menu--about`): sticky top 96px, 5 links w/ lucide icons, inactive $ink2 + cream hover, ACTIVE = $amber_tint bg + $ink label + $tag icon + weight 600 (matches legal-faq.png left rail). Each link 11px 14px padding, radius 4px, gap 11px.
    - Closing CTA: confirm WhyCta `.why-cta__panel` matches the maqueta `<!-- cierre vista pública (CTA) -->` (dark $ink card, radius 6, glow, "Empieza ahora", amber "Explorar anuncios" + outline "Anunciar ahora"). Only touch WhyCta SCSS if it visibly diverges.

    Fix ONLY measurable diffs, using existing tokens and existing BEM names. If the shell already matches, change nothing in these files.
  </action>
  <verify>
    <automated>cd apps/website && pnpm typecheck</automated>
    Playwright screenshot of /preguntas-frecuentes (desktop + 390px) saved to .planning/quick/260619-pr7-update-legal-about-views-faq-privacidad-/reference/after/shell-faq-desktop.png (+ -mobile). Visually compare to legal-faq.png; header/sidebar/CTA match.
  </verify>
  <done>Legal shell (cream header + glow + breadcrumb + H1 + intro, sticky sidebar with correct active state, dark CTA, footer) matches legal-faq.png and the maqueta on faq/priv/cond/contacto/sitemap; diffs (if any) fixed with existing tokens; typecheck clean; after-screenshots saved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify+fix the accordion (faq / privacidad / condiciones) — SHARED block</name>
  <files>apps/website/app/scss/components/_accordion.scss</files>
  <action>
    faq/priv/cond all render the SHARED AccordionDefault → `.accordion` in _accordion.scss. It already matches the maqueta accordion block — confirm and fix only diffs. The shared component is ALSO used by the homepage featured-FAQ section, so any change here is global.

    Maqueta accordion target values to confirm:
    - Item: border 1px $line, radius 4px, white bg, gap 12px between items.
    - Head button: padding 20px 22px (NOTE: existing is 22px 24px — a likely small diff; let the screenshot decide whether to align to 20px 22px), question Poppins 600 17px line-height 1.35 $ink.
    - Chevron: in a 30x30 radius-4 box, bg $amber_tint, stroke $tag, rotate 180° on open (existing `.rotated`). Maqueta open state tints the box; existing uses a constant $amber_tint box — keep unless the screenshot shows a clear mismatch.
    - Body: padding 0 22px 22px (existing 0 24px 24px), paragraphs 15.5px line-height 1.68 $ink2, content max-width 640px, smooth height/opacity transition (existing Transition name="accordion" is fine).

    Use existing tokens and existing BEM (`.accordion--item__head` / `__body`). Do NOT touch _faq.scss / FaqDefault.vue / AccordionDefault.vue logic. If alignment to 20px 22px / 0 22px 22px is applied, it is a global change — re-verify the homepage.
  </action>
  <verify>
    <automated>cd apps/website && pnpm typecheck</automated>
    Playwright screenshots saved to /reference/after/: accordion-faq.png (/preguntas-frecuentes), accordion-priv.png (/politicas-de-privacidad), accordion-cond.png (/condiciones-de-uso), and accordion-home.png (homepage featured FAQ — REGRESSION CHECK). All accordion views match legal-faq.png; homepage FAQ not regressed.
  </verify>
  <done>Accordion cards on faq/priv/cond match legal-faq.png (item border/radius, head padding+typography, amber-tint chevron box rotating on open, body spacing/typography); homepage featured FAQ visually unchanged/correct; typecheck clean; 4 after-screenshots saved.</done>
</task>

<task type="auto">
  <name>Task 3: Verify+fix contacto view (form + contact block) — preserve form logic</name>
  <files>apps/website/app/scss/components/_contact.scss, apps/website/app/scss/components/_address.scss</files>
  <action>
    /contacto renders ContactDefault → FormContact (`.contact--default__form`) + AddressDefault (`.address--default`). Both SCSS already match the maqueta `<!-- CONTÁCTANOS -->` block — confirm and fix only diffs.

    PRESERVE the existing FormContact validation (yup schema), submit (apiClient POST /contacts → /contacto/gracias), Swal error handling, prefill, and char counter logic. RESTYLE ONLY — do not change FormContact.vue/ContactDefault.vue/AddressDefault.vue script or markup unless a class is missing for a maqueta element.

    Maqueta target values to confirm:
    - Form max-width 600px, fields gap 18px. Label 13px 600 $ink (hint "(opcional)" $muted 500). Control: border $line, bg $cream, padding 13px 15px, radius 4px, font 15px $ink; focus → border $amber + bg #fff. Phone row = InputPhone (country select + number) themed to $cream/$line with amber focus (already in _contact.scss). Textarea maxlength 300 + counter "{n} caracteres restantes" 12.5px $muted align-end. Submit = $amber bg, $ink text (#26252B), 14px 32px, radius 4px, weight 600, align-start, hover $amber_hover.
    - Contact block (`.address--default`): margin-top 40px (note: about.vue/ContactDefault gaps the form/address 40px via `.contact--default { gap: 40px }` — confirm spacing reads as 40px), white bg, border $line, radius 4px, padding 24px 26px, wrap gap 28px 56px. "Correo electrónico" eyebrow (12px 700 uppercase $muted) + mailto:contacto@waldo.click ($ink 600, $tag icon). "Redes sociales" eyebrow + 3 social buttons (38x38, radius 4, $cream bg, $line border, hover #fff).

    Use existing tokens + existing BEM. Fix only measurable diffs.
  </action>
  <verify>
    <automated>cd apps/website && pnpm typecheck</automated>
    Playwright screenshots saved to /reference/after/: contacto-desktop.png + contacto-mobile.png (390px). Visually compare to legal-contacto.png; form fields, phone row, counter, amber submit, and white contact block (mailto + 3 social icons) match. Confirm form still submits (no script changes / logic intact).
  </verify>
  <done>/contacto matches legal-contacto.png (form layout/typography/colors, phone row, counter, amber submit; white contact block with mailto + 3 social buttons); FormContact validation/submit/Swal/prefill/counter logic unchanged; typecheck clean; after-screenshots saved.</done>
</task>

<task type="auto">
  <name>Task 4: Verify sitemap view treatment (3-col grid) — visual-only, keep dynamic content</name>
  <files>apps/website/app/scss/components/_sitemap.scss</files>
  <action>
    /sitemap renders SitemapDefault → `.sitemap--default` (3-col grid). _sitemap.scss already implements the maqueta treatment — confirm; fix only diffs.

    The maqueta's literal columns (Explorar / Tu cuenta / Información) are mockup PLACEHOLDER content. The note "use the maqueta HTML only for sitemap" governs the VISUAL TREATMENT, not which links exist. KEEP the page's real dynamic content (Páginas Principales + dynamic Categorías/Comunas from the existing stores). DO NOT change sitemap.vue, SitemapDefault.vue, the useAsyncData "sitemap-data" key, or the store calls.

    Maqueta target values to confirm in _sitemap.scss:
    - Grid: repeat(3, minmax(0,1fr)), gap 44px 40px.
    - Column header (`__block__title`): Poppins 700 13px uppercase letter-spacing .04em $ink, padding-bottom 13px, border-bottom 1px $line, with a 7px amber dot (`__dot` = $amber).
    - Link row (`__block__list__row`): flex space-between, 15px $ink2, padding 11px 0, border-bottom 1px $line, right chevron ($silver, 15px), hover $ink.

    Likely already matches → expect zero changes. If a measurable diff exists, fix with existing tokens/BEM only.
  </action>
  <verify>
    <automated>cd apps/website && pnpm typecheck</automated>
    Playwright screenshots saved to /reference/after/: sitemap-desktop.png + sitemap-mobile.png. Visually compare to the maqueta `<!-- MAPA DEL SITIO -->` block (3-col grid, amber-dot uppercase headers w/ underline, link rows with right chevron + separators).
  </verify>
  <done>/sitemap renders the maqueta 3-column grid treatment (amber-dot uppercase headers w/ $line underline, link rows with right chevron + $line separators); dynamic categories/comunas content preserved; no store/page/useAsyncData changes; typecheck clean; after-screenshots saved.</done>
</task>

<task type="auto">
  <name>Task 5: Remove apps/design workspace</name>
  <files>apps/design, pnpm-workspace.yaml, pnpm-lock.yaml</files>
  <action>
    apps/design (workspace package "waldo-design") is unused — reference screenshots are already copied into this task's /reference. The only repo reference is a code COMMENT in apps/strapi/src/api/ad/services/contact-mask.ts (not an import) — leave it or optionally drop the path mention; it does not affect the build.

    Steps:
    1. Re-grep to confirm no import/dependency references: `grep -rn -e 'apps/design' -e 'waldo-design' apps/website apps/dashboard apps/strapi --include='*.ts' --include='*.vue' --include='*.json' --include='*.js' | grep -v node_modules` — expect only the contact-mask.ts comment.
    2. `rm -rf apps/design`.
    3. Remove the line `  - 'apps/design'` (line 4) from pnpm-workspace.yaml.
    4. `pnpm install` to refresh pnpm-lock.yaml; confirm the workspace resolves with no errors.

    COMMIT NOTE (project memory): the website pre-commit hook auto-stages ALL files. To keep this atomic, stage ONLY apps/design (deletion), pnpm-workspace.yaml, and pnpm-lock.yaml, then commit with `--no-verify`:
    `git add -A apps/design pnpm-workspace.yaml pnpm-lock.yaml && git commit --no-verify -m "chore: remove apps/design workspace (using Claude Design MCP directly)"`
    Do NOT sweep the untracked .planning/quick/** files or any other unrelated changes into this commit.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && pnpm install --frozen-lockfile 2>&1 | tail -5; test ! -d apps/design && echo "apps/design removed"; ! grep -q "apps/design" pnpm-workspace.yaml && echo "workspace line removed"</automated>
  </verify>
  <done>apps/design directory deleted, removed from pnpm-workspace.yaml, lockfile refreshed and resolves cleanly; atomic commit "chore: remove apps/design workspace (using Claude Design MCP directly)" created with --no-verify (only the 3 intended paths staged).</done>
</task>

</tasks>

<verification>
- All 5 views (faq, priv, cond, contacto, sitemap) screenshot-verified at :3000 against their maqueta references; after-screenshots in /reference/after/.
- Homepage featured FAQ re-checked (shared accordion regression guard).
- `pnpm --filter website typecheck` clean.
- Only existing tokens used; no token added/modified; strict BEM preserved; pages remain composition-only; no extra API calls; useAsyncData keys + SEO/structured-data intact.
- apps/design removed; `pnpm install` resolves.
</verification>

<success_criteria>
- faq/priv/cond/contacto pixel-match legal-faq.png / legal-contacto.png; sitemap matches the maqueta HTML treatment.
- FormContact logic (validation/submit/Swal/prefill/counter) unchanged.
- apps/design gone from the workspace; lockfile clean.
- Each view task committed atomically; apps/design removal committed with the exact chore message.
</success_criteria>

<output>
After completion, append a row to .planning/STATE.md "Quick Tasks Completed" table for 260619-pr7 with the commit(s).
</output>
