---
wave: 1
files_modified:
  - apps/strapi/src/api/order/controllers/order.ts
---

## Plan: fix 500 error on orders/me endpoint

**Task:** Apply same sort + populate normalization from `find` to `me` method.

**Root cause:** `mis-ordenes.vue` sends `sort: ["createdAt:desc"]` (array of strings). The `me` method passed this directly to `db.query orderBy` which only accepts `{ field: direction }` objects — causing the 500.

### Task 1: Normalize sort and populate in `me` method

- Add sort normalization (handles string `"createdAt:desc"`, string array `["createdAt:desc"]`, and qs array-as-object `{ '0': 'createdAt:desc' }`)
- Add populate normalization (same logic as `find` — handles `"*"`, arrays, array-as-objects)
- Replace `ctx.internalServerError()` with `ctx.throw(500, error)` for error visibility
