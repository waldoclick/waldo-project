---
phase: quick-260409-h79
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/app/types/subscription-pro.ts
  - apps/dashboard/app/types/subscription-payment.ts
  - apps/dashboard/app/stores/settings.store.ts
  - apps/dashboard/app/components/SubscriptionProsDefault.vue
  - apps/dashboard/app/components/SubscriptionPaymentsDefault.vue
  - apps/dashboard/app/scss/components/_subscription-pros.scss
  - apps/dashboard/app/scss/components/_subscription-payments.scss
  - apps/dashboard/app/scss/app.scss
  - apps/dashboard/app/pages/subscription-pros/index.vue
  - apps/dashboard/app/pages/subscription-pros/[id]/index.vue
  - apps/dashboard/app/pages/subscription-payments/index.vue
  - apps/dashboard/app/pages/subscription-payments/[id]/index.vue
autonomous: true
requirements:
  - QUICK-260409-H79

must_haves:
  truths:
    - "Admin can navigate to /subscription-pros and see a paginated table of PRO subscriptions with user email, card type, card last 4, and pending invoice status"
    - "Admin can click a subscription-pro row and see a detail page with full read-only subscription data (excluding private tbk_user and inscription_token fields)"
    - "Admin can navigate to /subscription-payments and see a paginated table of subscription payments with user email, amount formatted as currency, status badge, period range, and charged_at"
    - "Admin can click a subscription-payment row and see a detail page with full read-only payment data (excluding private payment_response)"
    - "Search, sort, pagination, and page-size filters persist between visits in the settings store, following the exact packs pattern"
    - "Status values approved/failed/deactivated are rendered with BadgeDefault color variants (success/error/neutral)"
  artifacts:
    - path: apps/dashboard/app/types/subscription-pro.ts
      provides: "SubscriptionPro TypeScript interface"
      contains: "export interface SubscriptionPro"
    - path: apps/dashboard/app/types/subscription-payment.ts
      provides: "SubscriptionPayment TypeScript interface with status union"
      contains: "export interface SubscriptionPayment"
    - path: apps/dashboard/app/components/SubscriptionProsDefault.vue
      provides: "Table component mirroring PacksDefault.vue"
      min_lines: 150
    - path: apps/dashboard/app/components/SubscriptionPaymentsDefault.vue
      provides: "Table component mirroring PacksDefault.vue with status badges"
      min_lines: 150
    - path: apps/dashboard/app/scss/components/_subscription-pros.scss
      provides: "BEM styles for subscription-pros block"
      contains: ".subscription-pros"
    - path: apps/dashboard/app/scss/components/_subscription-payments.scss
      provides: "BEM styles for subscription-payments block"
      contains: ".subscription-payments"
    - path: apps/dashboard/app/pages/subscription-pros/index.vue
      provides: "Listing page for subscription-pros"
    - path: apps/dashboard/app/pages/subscription-pros/[id]/index.vue
      provides: "Detail page for a single subscription-pro"
    - path: apps/dashboard/app/pages/subscription-payments/index.vue
      provides: "Listing page for subscription-payments"
    - path: apps/dashboard/app/pages/subscription-payments/[id]/index.vue
      provides: "Detail page for a single subscription-payment"
  key_links:
    - from: apps/dashboard/app/components/SubscriptionProsDefault.vue
      to: /api/subscription-pros
      via: "useApiClient GET with populate=user"
      pattern: "apiClient\\(\\\"subscription-pros\\\""
    - from: apps/dashboard/app/components/SubscriptionPaymentsDefault.vue
      to: /api/subscription-payments
      via: "useApiClient GET with populate=user"
      pattern: "apiClient\\(\\\"subscription-payments\\\""
    - from: apps/dashboard/app/stores/settings.store.ts
      to: SectionSettings union
      via: "subscriptionPros and subscriptionPayments keys in SettingsState and getSectionSettings switch"
      pattern: "subscriptionPros|subscriptionPayments"
    - from: apps/dashboard/app/scss/app.scss
      to: components/subscription-pros, components/subscription-payments
      via: "@use directives in alphabetical order"
      pattern: "@use \\\"components/subscription"
---

<objective>
Create read-only dashboard views for `subscription-pros` and `subscription-payments` Strapi collections, mirroring exactly the existing `packs` pattern (list page, detail page, table component, SCSS, settings store, types).

Purpose: Admins need visibility into active PRO subscription records and their payment history without editing them.
Output: Two new dashboard sections fully wired to Strapi, following the house BEM/Nuxt 4 conventions in CLAUDE.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- Reference files the executor MUST read before writing anything. -->
<!-- The house rule "replicate the closest existing equivalent" applies here. -->

Reference (packs pattern — copy structure exactly):
@apps/dashboard/app/components/PacksDefault.vue
@apps/dashboard/app/pages/packs/index.vue
@apps/dashboard/app/pages/packs/[id]/index.vue
@apps/dashboard/app/types/pack.ts
@apps/dashboard/app/scss/components/_packs.scss
@apps/dashboard/app/stores/settings.store.ts

Strapi schemas (for field shapes):
@apps/strapi/src/api/subscription-pro/content-types/subscription-pro/schema.json
@apps/strapi/src/api/subscription-payment/content-types/subscription-payment/schema.json

<interfaces>
<!-- Contracts the executor will work with — extracted from referenced files. -->

From apps/dashboard/app/types/pack.ts (template shape):
```typescript
export interface Pack {
  id: number;
  name: string;
  // ...
  createdAt: string;
  updatedAt: string;
}
```

Entities to model (from prior conversation analysis):

subscription-pros endpoint `/api/subscription-pros`, read-only:
- id: number
- documentId: string
- user: { id, email, username, ... } (oneToOne, populate it)
- card_type: string | null
- card_last4: string | null
- pending_invoice: boolean
- createdAt / updatedAt: string
- OMIT: tbk_user, inscription_token (private in Strapi)

subscription-payments endpoint `/api/subscription-payments`, read-only:
- id: number
- documentId: string
- user: { id, email, ... } (manyToOne, populate it)
- amount: number (integer CLP)
- status: "approved" | "failed" | "deactivated"
- parent_buy_order: string | null
- child_buy_order: string | null
- authorization_code: string | null
- response_code: string | number | null
- period_start: string (date)
- period_end: string (date)
- charged_at: string (datetime) | null
- charge_attempts: number
- next_charge_attempt: string | null
- createdAt / updatedAt: string
- OMIT in table: payment_response (JSON blob, too large)

Existing composables / utils available (auto-imported by Nuxt):
- useApiClient()  — HTTP client, same signature used in PacksDefault.vue
- useSettingsStore() — Pinia store, extend with subscriptionPros + subscriptionPayments
- formatDate from @/utils/date
- formatCurrency from @/utils/price

Components available (auto-imported):
- HeroDefault, SearchDefault, FilterDefault, TableDefault, TableRow, TableCell,
  BadgeDefault, PaginationDefault, BoxContent, BoxInformation, CardInfo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add types + extend settings store for both subscription sections</name>
  <files>
    apps/dashboard/app/types/subscription-pro.ts,
    apps/dashboard/app/types/subscription-payment.ts,
    apps/dashboard/app/stores/settings.store.ts
  </files>
  <action>
Before writing anything, READ `apps/dashboard/app/types/pack.ts` and `apps/dashboard/app/stores/settings.store.ts` completely.

1) Create `apps/dashboard/app/types/subscription-pro.ts`:
   - Export `interface SubscriptionPro` with: id (number), documentId (string), card_type (string | null), card_last4 (string | null), pending_invoice (boolean), createdAt (string), updatedAt (string), and an optional nested `user` of shape `{ id: number; documentId?: string; email: string; username?: string } | null`.
   - Do NOT include `tbk_user` or `inscription_token` — they are private in Strapi and must never appear in the dashboard type surface.

2) Create `apps/dashboard/app/types/subscription-payment.ts`:
   - Export `type SubscriptionPaymentStatus = "approved" | "failed" | "deactivated";`
   - Export `interface SubscriptionPayment` with: id, documentId, amount (number), status (SubscriptionPaymentStatus), parent_buy_order (string | null), child_buy_order (string | null), authorization_code (string | null), response_code (string | null), period_start (string | null), period_end (string | null), charged_at (string | null), charge_attempts (number), next_charge_attempt (string | null), createdAt, updatedAt, and the same optional `user` shape as above.
   - Do NOT include `payment_response` in the type (keep JSON blob out of the dashboard surface).

3) Edit `apps/dashboard/app/stores/settings.store.ts`:
   - Add `subscriptionPros: SectionSettings` and `subscriptionPayments: SectionSettings` to the `SettingsState` interface (alphabetically grouped near `packs`).
   - Add matching `ref<SectionSettings>({ ...defaultSectionSettings })` declarations for both.
   - Add `getSubscriptionProsFilters` and `getSubscriptionPaymentsFilters` computed getters mirroring `getPacksFilters`.
   - Add both cases to `getSectionSettings` switch.
   - Export both refs and getters in the returned object.
   - Do not touch the existing `persist` config — it already covers the entire store. The `// persist:` audit comment requirement in CLAUDE.md applies when ADDING persist; it already exists here.

Follow the existing alphabetical / grouping order wherever possible.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | tail -40</automated>
  </verify>
  <done>
    Types compile without errors. `useSettingsStore()` exposes `subscriptionPros`, `subscriptionPayments`, `getSubscriptionProsFilters`, `getSubscriptionPaymentsFilters`. No `tbk_user`, `inscription_token`, or `payment_response` appear in the type files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create table components + SCSS for both subscription sections</name>
  <files>
    apps/dashboard/app/components/SubscriptionProsDefault.vue,
    apps/dashboard/app/components/SubscriptionPaymentsDefault.vue,
    apps/dashboard/app/scss/components/_subscription-pros.scss,
    apps/dashboard/app/scss/components/_subscription-payments.scss,
    apps/dashboard/app/scss/app.scss
  </files>
  <action>
Before writing, READ `apps/dashboard/app/components/PacksDefault.vue` and `apps/dashboard/app/scss/components/_packs.scss` completely. These are the templates to replicate.

1) `SubscriptionProsDefault.vue`:
   - Copy `PacksDefault.vue` structure exactly (template, script, imports). Do NOT add a `<style>` tag — SCSS is global.
   - Root element: `<section class="subscription-pros subscription-pros--default">` (block + modifier per BEM rule).
   - Replace every `packs--default__*` class with `subscription-pros--default__*`.
   - `section = "subscriptionPros" as const`.
   - `allPacks` → `allItems: ref<SubscriptionPro[]>`.
   - Endpoint: `apiClient("subscription-pros", { method: "GET", params: { ...searchParams, populate: { user: { fields: ["email", "username"] } } } })`.
   - Search filters: `$or` over `user.email` `$containsi` and `card_last4` `$containsi` (wrap in the same searchParams shape as PacksDefault).
   - Sort options: `createdAt:desc`, `createdAt:asc`, `user.email:asc`, `pending_invoice:desc` (Pendiente primero).
   - Columns (in order): ID, Usuario (user.email), Tarjeta (card_type + " ****" + card_last4, or "-"), Factura pendiente (BadgeDefault with variant `"outline"` showing "Sí"/"No"), Fecha de creación (formatDate), Acciones.
   - Actions: only a "Ver" button routing to `/subscription-pros/${item.id}` (NO edit — read-only).
   - Keep the exact same `watch([...], fetchItems, { immediate: true })` pattern.
   - Import `SubscriptionPro` type from `@/types/subscription-pro`.

2) `SubscriptionPaymentsDefault.vue`:
   - Same copy-and-rename process from `PacksDefault.vue`.
   - Root element: `<section class="subscription-payments subscription-payments--default">`.
   - `section = "subscriptionPayments" as const`.
   - Endpoint: `apiClient("subscription-payments", { method: "GET", params: { ...searchParams, populate: { user: { fields: ["email", "username"] } } } })`.
   - Search filters: `$or` over `user.email` `$containsi`, `parent_buy_order` `$containsi`, `child_buy_order` `$containsi`.
   - Sort options: `createdAt:desc`, `createdAt:asc`, `charged_at:desc`, `amount:desc`, `amount:asc`, `status:asc`.
   - Columns: ID, Usuario (user.email), Monto (formatCurrency(amount)), Estado (BadgeDefault — see status helper below), Período (`${formatDate(period_start)} → ${formatDate(period_end)}`), Cobrado (formatDate(charged_at) or "-"), Intentos (charge_attempts), Acciones.
   - Status helper: a small `statusVariant(status)` function returning `"success"` for `"approved"`, `"error"` for `"failed"`, `"neutral"` for `"deactivated"`. Pass that to `<BadgeDefault :variant="statusVariant(item.status)">`. If BadgeDefault does not support `"neutral"`/`"error"` variants, fall back to `"outline"` — check its prop union first.
   - Actions: only a "Ver" button routing to `/subscription-payments/${item.id}`.
   - Import `SubscriptionPayment` and `SubscriptionPaymentStatus` from `@/types/subscription-payment`.

3) SCSS files — copy `_packs.scss` byte-for-byte and rename blocks:
   - `_subscription-pros.scss`: `.subscription-pros { &--default { /* same declarations as .packs--default */ } }`
   - `_subscription-payments.scss`: `.subscription-payments { &--default { /* same */ } }`
   - No additions, no removals. Preserve the `@use` imports at the top.

4) `apps/dashboard/app/scss/app.scss`:
   - Add `@use "components/subscription-payments";` and `@use "components/subscription-pros";` in alphabetical order in the `// components` block. Based on the current file, they go between `@use "components/stats";` and `@use "components/swal";` (subscription-payments and subscription-pros both sort before "swal" and after "stats"/"statistics"). Verify alphabetical placement before inserting.

BEM rules (CLAUDE.md):
- Root element has BOTH block and modifier classes.
- Never use standalone hyphenated compound class names.
- All children belong to `subscription-pros--default__*` / `subscription-payments--default__*` namespace.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | tail -40 && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both components render without TS errors. SCSS files exist and are imported in `app.scss`. No `<style>` blocks inside Vue files. BEM compound class names are never used. Tables fetch via `useApiClient` with `populate: { user }`. `fetchItems` is triggered by `watch(..., { immediate: true })` — never `onMounted`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create list + detail pages for both subscription sections</name>
  <files>
    apps/dashboard/app/pages/subscription-pros/index.vue,
    apps/dashboard/app/pages/subscription-pros/[id]/index.vue,
    apps/dashboard/app/pages/subscription-payments/index.vue,
    apps/dashboard/app/pages/subscription-payments/[id]/index.vue
  </files>
  <action>
Before writing, READ `apps/dashboard/app/pages/packs/index.vue` and `apps/dashboard/app/pages/packs/[id]/index.vue` completely.

1) `pages/subscription-pros/index.vue`:
   - Mirror `pages/packs/index.vue` exactly. NO `Agregar` button in the hero actions slot (read-only views).
   - Title: `"Subscripciones PRO"`.
   - Breadcrumbs: `[{ label: "Subscripciones PRO" }]`.
   - Renders `<SubscriptionProsDefault />`.
   - `definePageMeta({ layout: "dashboard" })`.

2) `pages/subscription-pros/[id]/index.vue`:
   - Mirror `pages/packs/[id]/index.vue` structure (HeroDefault + BoxContent + BoxInformation + CardInfo composition). NO edit button in actions slot.
   - `useAsyncData` key: `` `subscription-pro-${route.params.id}` ``.
   - Fetch: `apiClient("subscription-pros", { method: "GET", params: { filters: { documentId: { $eq: id } }, populate: { user: { fields: ["email", "username"] } } } })`, with the same fallback as the packs detail page using `` `subscription-pros/${id}` `` for numeric ids.
   - Title: `item.value?.user?.email || "Subscripción PRO"`.
   - Breadcrumbs: `[{ label: "Subscripciones PRO", to: "/subscription-pros" }, ...(item.value?.user?.email ? [{ label: item.value.user.email }] : [])]`.
   - CardInfos in main content (columns=2): Usuario (email), Tipo de tarjeta, Últimos 4 dígitos, Factura pendiente ("Sí"/"No").
   - Sidebar (columns=1): Fecha de creación, Última modificación (both via formatDate).
   - Always add `default: () => null` to `useAsyncData` per the CLAUDE.md rule about eliminating `T | undefined`.

3) `pages/subscription-payments/index.vue`:
   - Mirror the same pattern. NO `Agregar` button.
   - Title: `"Pagos de subscripción"`.
   - Breadcrumbs: `[{ label: "Pagos de subscripción" }]`.
   - Renders `<SubscriptionPaymentsDefault />`.

4) `pages/subscription-payments/[id]/index.vue`:
   - Same shape as the subscription-pros detail page. NO edit button.
   - `useAsyncData` key: `` `subscription-payment-${route.params.id}` ``.
   - Fetch from `subscription-payments` with populate user.
   - Title: `` `Pago #${item.value?.id ?? ''}` ``.
   - Breadcrumbs: `[{ label: "Pagos de subscripción", to: "/subscription-payments" }, ...(item.value ? [{ label: \`Pago #${item.value.id}\` }] : [])]`.
   - Main CardInfos (columns=2): Usuario, Monto (formatCurrency), Estado (plain text label or BadgeDefault in a span), Parent buy order, Child buy order, Código de autorización, Código de respuesta, Intentos de cobro.
   - Sidebar (columns=1): Período inicio (formatDate), Período fin (formatDate), Cobrado el (formatDate), Próximo intento (formatDate), Fecha de creación, Última modificación.
   - DO NOT render `payment_response` — it is omitted from the dashboard surface.
   - Add `default: () => null` to `useAsyncData`.

All pages must include `definePageMeta({ layout: "dashboard" })`. Spanish labels for user-visible text. Never pair `await storeAction()` with `useAsyncData` in the same page. Never use `onMounted` as a data-loading trigger.
  </action>
  <verify>
    <automated>cd apps/dashboard && yarn nuxt typecheck 2>&1 | tail -50 && yarn lint 2>&1 | tail -20</automated>
  </verify>
  <done>
    Four page files exist and compile. Typecheck passes. Lint passes. Navigating (in runtime) to `/subscription-pros`, `/subscription-pros/:id`, `/subscription-payments`, `/subscription-payments/:id` would render populated read-only views. No edit buttons, no "Agregar" buttons. No `payment_response`, `tbk_user`, or `inscription_token` surfaced.
  </done>
</task>

</tasks>

<verification>
Global checks after all three tasks:

- `cd apps/dashboard && yarn nuxt typecheck` → 0 errors
- `cd apps/dashboard && yarn lint` → 0 errors
- `yarn codacy` at repo root → no new issues introduced
- `grep -rn "tbk_user\|inscription_token\|payment_response" apps/dashboard/app` → no matches (these fields must not leak into the dashboard surface)
- `grep -rn "onMounted" apps/dashboard/app/components/SubscriptionProsDefault.vue apps/dashboard/app/components/SubscriptionPaymentsDefault.vue` → no matches
- `grep -rn "<style" apps/dashboard/app/components/SubscriptionProsDefault.vue apps/dashboard/app/components/SubscriptionPaymentsDefault.vue` → no matches
- `grep -n "subscription-pros\|subscription-payments" apps/dashboard/app/scss/app.scss` → two matches, in alphabetical order within the components block
</verification>

<success_criteria>
- All 12 listed files created/modified exactly as specified in `files_modified`.
- Dashboard typecheck + lint + build all pass.
- Four new routes exist and fetch from Strapi with `populate=user`.
- Every must_have truth is observable in the running dashboard.
- No edit, create, or delete affordances surface on any of the four new pages (read-only per scope).
- BEM compliance: root element has both block + modifier, no hyphenated standalone class names, all children namespaced under the modifier.
- No private Strapi fields (`tbk_user`, `inscription_token`, `payment_response`) appear anywhere in the dashboard type, component, or page files.
</success_criteria>

<output>
After completion, create `.planning/quick/260409-h79-crear-vistas-de-subscription-en-el-dashb/260409-h79-SUMMARY.md`
</output>
