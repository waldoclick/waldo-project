---
phase: quick
plan: 260329-pa1
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/website/nuxt.config.ts
  - apps/dashboard/nuxt.config.ts
autonomous: true
requirements: [CSP-FIX]

must_haves:
  truths:
    - "No CSP violations in staging console for GTM, reCAPTCHA, Hotjar, Cloudflare, Sentry, LogRocket"
    - "connect-src allows Cloudflare RUM beacon and GA4 analytics connections"
    - "Both website and dashboard CSP configs are aligned for shared services"
  artifacts:
    - path: "apps/website/nuxt.config.ts"
      provides: "Website CSP configuration with complete directives"
      contains: "contentSecurityPolicy"
    - path: "apps/dashboard/nuxt.config.ts"
      provides: "Dashboard CSP configuration with complete directives"
      contains: "contentSecurityPolicy"
  key_links:
    - from: "apps/website/nuxt.config.ts"
      to: "nuxt-security module"
      via: "security.headers.contentSecurityPolicy"
      pattern: "contentSecurityPolicy"
---

<objective>
Fix CSP violations observed in staging by adding missing domain entries to script-src, connect-src, and worker-src directives in both website and dashboard nuxt.config.ts files.

Purpose: Eliminate CSP report-only violations so the policy can be enforced in production without breaking GTM, reCAPTCHA, Hotjar, Cloudflare Insights, Sentry, and LogRocket.
Output: Updated CSP configurations in both apps.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/website/nuxt.config.ts (lines 34-155 — security block)
@apps/dashboard/nuxt.config.ts (lines 32-112 — security block)

<interfaces>
Current website CSP already has most script-src domains. Key gaps identified by comparing
the reported violations against existing config:

WEBSITE (apps/website/nuxt.config.ts):
- connect-src: Missing Cloudflare RUM beacon domain and GA4 connect endpoints
- script-src: Already has most domains; verify gtag/js path is covered by base domain

DASHBOARD (apps/dashboard/nuxt.config.ts):
- connect-src: Has `https://cloudflareinsights.com` but missing `static.` prefix variant
- connect-src: Missing Hotjar websocket/API connections (website has them, dashboard does not)
- script-src: Missing Hotjar domains (website has them, dashboard does not — if dashboard uses Hotjar)

Both apps already have worker-src: ['self', 'blob:'] which covers Sentry and LogRocket workers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing CSP domains to website nuxt.config.ts</name>
  <files>apps/website/nuxt.config.ts</files>
  <action>
Edit the `contentSecurityPolicy` object in `apps/website/nuxt.config.ts` (inside the security.headers block starting around line 49). Make the following specific additions:

**connect-src** — add these entries that are currently missing:
- `"https://static.cloudflareinsights.com"` — Cloudflare Insights beacon POST requests
- `"https://cloudflareinsights.com"` — Cloudflare Insights alternate domain
- `"https://www.googletagmanager.com"` — GA4 beacon/collect calls via GTM
- `"https://region1.google-analytics.com"` — GA4 regional analytics endpoint
- `"https://*.hotjar.com"` — Hotjar API and events connections (complement the existing wss:// entries)
- `"https://vc.hotjar.io"` — Hotjar visitor recordings upload
- `"https://*.lgrckt-in.com"` — LogRocket ingest connections (script already allowed, connect was missing)

**script-src** — add this entry:
- `"https://www.googletagmanager.com/gtag/js"` is already covered by `"https://www.googletagmanager.com"` — no change needed, but verify no other script-src violations remain

Do NOT modify any other directives (worker-src, child-src already have blob:). Do NOT remove any existing entries. Keep the same formatting and comment style.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -A 30 '"connect-src"' apps/website/nuxt.config.ts | grep -c 'cloudflareinsights\|googletagmanager\|hotjar.com\|lgrckt-in' | xargs test 4 -le</automated>
  </verify>
  <done>Website connect-src includes Cloudflare Insights, GTM, Hotjar API, and LogRocket ingest domains. No existing entries removed.</done>
</task>

<task type="auto">
  <name>Task 2: Align dashboard CSP with website for shared services</name>
  <files>apps/dashboard/nuxt.config.ts</files>
  <action>
Edit the `contentSecurityPolicy` object in `apps/dashboard/nuxt.config.ts` (security.headers block starting around line 43). Make these additions:

**connect-src** — add missing domains to match website coverage:
- `"https://static.cloudflareinsights.com"` — currently only has `https://cloudflareinsights.com` without `static.` prefix; add both for completeness
- `"https://www.googletagmanager.com"` — GA4 beacon/collect calls
- `"https://region1.google-analytics.com"` — GA4 regional analytics endpoint
- `"https://*.lgrckt-in.com"` — LogRocket ingest connections

Note: Dashboard does NOT use Hotjar or Zoho SalesIQ (those are website-only), so do NOT add Hotjar or Zoho domains to dashboard.

Do NOT modify any other directives. Do NOT remove existing entries. Keep the same formatting style.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && grep -A 20 '"connect-src"' apps/dashboard/nuxt.config.ts | grep -c 'static.cloudflareinsights\|googletagmanager\|lgrckt-in' | xargs test 3 -le</automated>
  </verify>
  <done>Dashboard connect-src includes Cloudflare Insights (with static. prefix), GTM, and LogRocket ingest domains. No existing entries removed.</done>
</task>

<task type="auto">
  <name>Task 3: Verify both apps build successfully with updated CSP</name>
  <files>apps/website/nuxt.config.ts, apps/dashboard/nuxt.config.ts</files>
  <action>
Run TypeScript type checking for both apps to confirm the updated nuxt.config.ts files have no syntax errors. The CSP arrays are plain string arrays so this primarily validates no typos broke the config object structure.

Run: `cd /home/gab/Code/waldo-project && yarn workspace website nuxt typecheck` and `yarn workspace dashboard nuxt typecheck`

If typecheck fails on unrelated pre-existing errors, at minimum confirm both configs parse correctly by running: `node -e "require('./apps/website/nuxt.config.ts')"` — but since this is TS, use the Nuxt prepare/typecheck path.
  </action>
  <verify>
    <automated>cd /home/gab/Code/waldo-project && node -e "const fs = require('fs'); const w = fs.readFileSync('apps/website/nuxt.config.ts', 'utf8'); const d = fs.readFileSync('apps/dashboard/nuxt.config.ts', 'utf8'); if (!w.includes('static.cloudflareinsights.com') || !d.includes('static.cloudflareinsights.com')) { process.exit(1); } console.log('CSP configs validated');"</automated>
  </verify>
  <done>Both nuxt.config.ts files parse without errors. New CSP domains are present in the correct directives.</done>
</task>

</tasks>

<verification>
1. `grep -c "static.cloudflareinsights" apps/website/nuxt.config.ts` returns at least 1 in connect-src
2. `grep -c "googletagmanager" apps/website/nuxt.config.ts` returns at least 2 (script-src + connect-src)
3. `grep -c "static.cloudflareinsights" apps/dashboard/nuxt.config.ts` returns at least 1 in connect-src
4. No entries removed from either file compared to current state
5. Both config files have valid TypeScript syntax
</verification>

<success_criteria>
- Website connect-src has 7 new domain entries for Cloudflare, GTM, GA4, Hotjar API, and LogRocket
- Dashboard connect-src has 4 new domain entries for Cloudflare, GTM, GA4, and LogRocket
- No existing CSP entries removed from either app
- Both nuxt.config.ts files maintain valid syntax
- worker-src already has blob: in both apps (confirmed, no changes needed)
</success_criteria>

<output>
After completion, create `.planning/quick/260329-pa1-fix-csp-violations-in-staging-add-missin/260329-pa1-SUMMARY.md`
</output>
