import { WeatherService } from "./weather.service";
import { WeatherData, WeatherError } from "./interfaces";

export { WeatherService, WeatherData, WeatherError };

export default new WeatherService();
