{
  "name": "waldo-project",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/strapi",
    "apps/website",
    "apps/dashboard"
  ],
  "scripts": {
    "dev": "lsof -ti:24678,1337,3000,3001,3002 | xargs kill -9 2>/dev/null || true && turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "test": "turbo run test",
    "codacy": "turbo run codacy",
    "prepare": "husky",
    "strapi:upgrade": "cd apps/strapi && NPM_REGISTRY_URL=https://registry.npmjs.org/ npx @strapi/upgrade latest"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "next": "15.4.6"
  }
}

